---
phase: quick-260327-o38
plan: 01
subsystem: ui
tags: [react-table, tanstack-table, sorting, datatable]

requires:
  - phase: none
    provides: existing DataTable component
provides:
  - "Dual-mode sorting in DataTable: automatic client-side when no onSortingChange, manual server-side when onSortingChange provided"
  - "Voters table sortable columns with accessorFn enabling sort arrow visibility"
affects: [voters, members, voter-lists, voter-tags, sessions, call-lists, dnc, roster, imports]

tech-stack:
  added: []
  patterns:
    - "DataTable dual-mode sorting: omit onSortingChange for automatic client-side sort, provide onSortingChange for server-side sort"

key-files:
  created: []
  modified:
    - web/src/components/shared/DataTable.tsx
    - web/src/components/shared/DataTable.test.tsx
    - web/src/routes/campaigns/$campaignId/voters/index.tsx

key-decisions:
  - "Conditional manualSorting based on onSortingChange presence - zero API changes needed for all client-side tables"
  - "accessorFn on voter columns maps to API sort fields (last_name, party, registration_city, age)"

patterns-established:
  - "DataTable client-side sorting: columns with accessorKey get automatic sort when no onSortingChange prop passed"

requirements-completed: [SORT-01, SORT-02]

duration: 2min
completed: 2026-03-27
---

# Quick Task 260327-o38: Fix Sort Buttons on All DataTable Columns

**Dual-mode sorting in DataTable: conditional getSortedRowModel for client-side tables, accessorFn on Voters columns for server-side sort arrow visibility**

## Performance

- **Duration:** 2 min
- **Started:** 2026-03-27T21:25:21Z
- **Completed:** 2026-03-27T21:27:31Z
- **Tasks:** 2
- **Files modified:** 3

## Accomplishments
- DataTable now supports automatic client-side sorting when onSortingChange is not provided (uses getSortedRowModel)
- DataTable preserves manual server-side sorting when onSortingChange IS provided (unchanged behavior)
- Voters table sort arrows now visible on Name, Party, City, Age columns (accessorFn added)
- All 10+ client-side tables gain functional sorting with zero per-table code changes

## Task Commits

Each task was committed atomically:

1. **Task 1: Enable dual-mode sorting in DataTable component** - `124a89c` (fix)
2. **Task 2: Add accessorFn to Voters table sortable columns** - `48e0c3e` (fix)

## Files Created/Modified
- `web/src/components/shared/DataTable.tsx` - Added getSortedRowModel import, conditional manualSorting based on onSortingChange presence
- `web/src/components/shared/DataTable.test.tsx` - Added 2 tests: client-side sort reorders rows, manual mode delegates to onSortingChange
- `web/src/routes/campaigns/$campaignId/voters/index.tsx` - Added accessorFn to full_name, party, city, age columns

## Decisions Made
- Conditional manualSorting based on onSortingChange prop presence: simplest approach, no interface changes needed, backward compatible
- accessorFn values match SORT_COLUMN_MAP keys (last_name, party, registration_city, age) for consistency between client/server sort behavior

## Deviations from Plan

None - plan executed exactly as written.

## Issues Encountered

None

## User Setup Required

None - no external service configuration required.

## Known Stubs

None - all functionality is fully wired.

## Next Steps
- Visual verification: Voters table sort arrows visible, client-side table sorting functional
- All existing DataTable consumers automatically benefit from client-side sort

## Self-Check: PASSED

- All 3 modified files exist on disk
- All 2 task commits found in git log (124a89c, 48e0c3e)
- 14/14 DataTable tests pass
- 0 TypeScript errors

---
*Quick task: 260327-o38*
*Completed: 2026-03-27*
