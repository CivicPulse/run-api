---
phase: quick
plan: 260407-10o
type: execute
wave: 1
depends_on: []
files_modified:
  - web/src/hooks/useVoterContacts.ts
  - web/src/hooks/useVoterContacts.test.ts
  - .planning/milestones/v1.14-MILESTONE-AUDIT.md
  - .planning/MILESTONES.md
  - .planning/STATE.md
autonomous: true
requirements:
  - SRCH-04
must_haves:
  truths:
    - "Deleting a voter address invalidates the campaign voter query cache so search/list views refetch immediately."
    - "Frontend regression coverage proves the address delete mutation invalidates both contact and voter query families."
    - "The active v1.14 milestone audit is rerun and no longer reports the address-delete freshness seam as broken."
  artifacts:
    - path: "web/src/hooks/useVoterContacts.ts"
      provides: "useDeleteAddress invalidates both contact and voter queries"
      contains: "qc.invalidateQueries({ queryKey: [\"voters\", campaignId] })"
    - path: "web/src/hooks/useVoterContacts.test.ts"
      provides: "regression coverage for address-delete invalidation"
      contains: "useDeleteAddress"
    - path: ".planning/milestones/v1.14-MILESTONE-AUDIT.md"
      provides: "rerun milestone audit with SRCH-04 restored to satisfied"
      contains: "status: passed"
  key_links: []
---

<objective>
Close the reopened v1.14 SRCH-04 freshness gap by invalidating voter queries after address deletion, prove the fix with a frontend hook regression test, and rerun the active milestone audit plus state docs so v1.14 returns to passed status.

Purpose: Retire the audit blocker without changing the broader voter search contract.
Output: Hook fix, regression test, and updated v1.14 audit/state artifacts.
</objective>

<context>
@web/src/hooks/useVoterContacts.ts
@web/src/hooks/useVoterContacts.test.ts
@.planning/milestones/v1.14-MILESTONE-AUDIT.md
@.planning/MILESTONES.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix address-delete invalidation and add regression coverage</name>
  <files>web/src/hooks/useVoterContacts.ts, web/src/hooks/useVoterContacts.test.ts</files>
  <action>
Update `useDeleteAddress` so the mutation success path invalidates both the voter contact query and the campaign voter query family. Add a regression test that executes the hook, confirms the delete endpoint is called, and asserts `invalidateQueries` runs for `["voters", campaignId, voterId, "contacts"]` and `["voters", campaignId]`.
  </action>
  <verify>
    <automated>npm --prefix web test -- --run src/hooks/useVoterContacts.test.ts</automated>
  </verify>
  <done>Address deletion now refreshes voter-page search/list queries, and the regression test passes.</done>
</task>

<task type="auto">
  <name>Task 2: Rerun the active v1.14 milestone audit</name>
  <files>.planning/milestones/v1.14-MILESTONE-AUDIT.md, .planning/MILESTONES.md, .planning/STATE.md</files>
  <action>
Re-evaluate SRCH-04 against the patched hook and the passing regression test. Update the active milestone audit from `gaps_found` to `passed`, revise the summary/integration sections to state the address-delete seam is now wired, and bring `MILESTONES.md` plus `STATE.md` back in sync with the re-audit result.
  </action>
  <verify>
    <automated>rg -n "status: passed|SRCH-04|address delete" .planning/milestones/v1.14-MILESTONE-AUDIT.md .planning/MILESTONES.md .planning/STATE.md</automated>
  </verify>
  <done>v1.14 audit/state docs show passed status and no open SRCH-04 blocker.</done>
</task>

</tasks>

<success_criteria>
- `useDeleteAddress` invalidates `["voters", campaignId]` in addition to voter contacts
- `web/src/hooks/useVoterContacts.test.ts` covers the address-delete invalidation path
- `.planning/milestones/v1.14-MILESTONE-AUDIT.md` shows `status: passed` with 15/15 requirements satisfied
- `.planning/MILESTONES.md` and `.planning/STATE.md` no longer describe v1.14 as blocked on SRCH-04
</success_criteria>
