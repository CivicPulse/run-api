---
phase: quick
plan: 260319-241
type: execute
wave: 1
depends_on: []
files_modified:
  - web/src/routes/__root.tsx
autonomous: true
requirements: []
must_haves:
  truths:
    - "Sidebar header separator and top bar border-b align horizontally"
    - "Both headers render at 64px (h-16) height"
  artifacts:
    - path: "web/src/routes/__root.tsx"
      provides: "Root layout with aligned headers"
      contains: "h-16"
  key_links: []
---

<objective>
Fix the misaligned header section between the sidebar and top bar in the web UI.

Purpose: The sidebar header is 64px (h-16 equivalent) while the top bar header is 56px (h-14), causing an 8px visual misalignment where the sidebar separator and top bar border don't line up.
Output: Both headers at equal height, visually aligned.
</objective>

<execution_context>
@/home/kwhatcher/.claude/get-shit-done/workflows/execute-plan.md
@/home/kwhatcher/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@web/src/routes/__root.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Align top bar header height with sidebar header</name>
  <files>web/src/routes/__root.tsx</files>
  <action>
On line 244 of web/src/routes/__root.tsx, change the header className from `h-14` to `h-16`:

Before: `<header className="flex h-14 shrink-0 items-center gap-2 border-b px-4">`
After:  `<header className="flex h-16 shrink-0 items-center gap-2 border-b px-4">`

This makes the top bar header 64px to match the sidebar header (SidebarHeader with p-2 padding + SidebarMenuButton size="lg" h-12 = 64px total).
  </action>
  <verify>
    <automated>cd /home/kwhatcher/projects/run-api/web && npx tsc --noEmit 2>&1 | tail -5</automated>
  </verify>
  <done>Top bar header height is h-16 (64px), matching the sidebar header height exactly so their bottom borders align horizontally.</done>
</task>

</tasks>

<verification>
- TypeScript compilation passes with no errors
- The header element in __root.tsx uses `h-16` class
</verification>

<success_criteria>
Both the sidebar separator and top bar border-b render at the same vertical position (64px from top), eliminating the 8px misalignment.
</success_criteria>

<output>
After completion, create `.planning/quick/260319-241-fix-the-misaligned-header-section-of-the/260319-241-SUMMARY.md`
</output>
