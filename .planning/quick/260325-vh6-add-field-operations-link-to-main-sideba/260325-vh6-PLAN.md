---
phase: quick
plan: 260325-vh6
type: execute
wave: 1
depends_on: []
files_modified: [web/src/routes/__root.tsx]
autonomous: true
requirements: [add-field-ops-link]
---

<objective>
Add a "Field Operations" link to the Campaign section of the main sidebar menu, positioned after "Volunteers" and before the "Organization" section. The link should point to `/field/${campaignId}` and use an appropriate Lucide icon.
</objective>

<tasks>

<task type="auto">
  <name>Task 1: Add Field Operations nav item to sidebar</name>
  <files>web/src/routes/__root.tsx</files>
  <action>
In `web/src/routes/__root.tsx`:

1. Import the `Navigation` icon from lucide-react (represents field operations / navigation / routing).
2. Add a new entry to the `navItems` array after "Volunteers":
   ```
   { to: `/field/${campaignId}`, label: "Field Operations", icon: Navigation }
   ```

That's it — single file, two-line change.
  </action>
  <verify>
    <automated>cd /home/kwhatcher/projects/civicpulse/run-api && npx --yes tsc --noEmit --project web/tsconfig.json 2>&1 | head -20</automated>
  </verify>
  <done>
    - "Field Operations" link appears in sidebar after "Volunteers"
    - Links to /field/${campaignId}
    - Uses Navigation icon from lucide-react
  </done>
</task>

</tasks>
