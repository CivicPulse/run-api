---
phase: 56-feature-gap-builds
plan: 03
type: execute
wave: 3
depends_on:
  - 56-02
files_modified: []
autonomous: false
requirements:
  - FEAT-01
  - FEAT-02
  - FEAT-03

must_haves:
  truths:
    - "Edit note dialog is visible with pre-filled text when user clicks Edit"
    - "After saving an edit, the updated text appears in the History tab"
    - "After deleting a note, it no longer appears in the History tab"
    - "Non-note interactions have no edit/delete actions visible"
    - "Walk list rename dialog is accessible from both canvassing index and detail page"
    - "After renaming, the new name appears on both pages"
  artifacts: []
  key_links: []
---

<objective>
Visually verify all three features (note edit, note delete, walk list rename) work correctly in the browser.

Purpose: Confirm the frontend UI renders correctly and the full stack (UI -> API -> DB) works end-to-end before marking the phase complete.

Output: Playwright MCP screenshots confirming all features work, saved to screenshots/ directory.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/56-feature-gap-builds/56-01-SUMMARY.md
@.planning/phases/56-feature-gap-builds/56-02-SUMMARY.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Visual verification of note edit/delete and walk list rename features</name>
  <files></files>
  <action>
Verify the following features visually using Playwright MCP or browser. Ensure Docker Compose services are running (`docker compose up -d`) and seed data is loaded (`docker compose exec api bash -c "PYTHONPATH=/home/app python /home/app/scripts/seed.py"`). Take screenshots for each test and save to `screenshots/` directory.

**Test 1: Note Edit (FEAT-01)**
1. Navigate to any voter detail page's History tab (e.g., a seeded voter with existing notes)
2. If no notes exist, add one using the "Add a Note" form
3. Verify the note shows a 3-dot menu icon (MoreVertical)
4. Click the 3-dot menu -- confirm "Edit" and "Delete" options appear
5. Click "Edit" -- confirm a Dialog opens with the note text pre-filled in a textarea
6. Change the text and click "Save"
7. Confirm the updated text appears in the History tab and a success toast shows

**Test 2: Note Delete (FEAT-02)**
1. On the same voter History tab, click the 3-dot menu on a note
2. Click "Delete" -- confirm a ConfirmDialog appears with "Delete Note" title and destructive styling
3. Click "Delete" in the dialog
4. Confirm the note disappears from the list and a success toast shows

**Test 3: Non-note interactions have no actions (FEAT-01/FEAT-02 safety)**
1. Look at non-note interactions (e.g., tag_added, import, door_knock) in the History tab
2. Confirm they do NOT show a 3-dot menu or any edit/delete actions

**Test 4: Walk List Rename from Index (FEAT-03)**
1. Navigate to the canvassing index page
2. Find a walk list row in the table
3. Verify the row has a 3-dot menu (replacing the old standalone delete button)
4. Click the 3-dot menu -- confirm "Rename" and "Delete" options appear
5. Click "Rename" -- confirm a Dialog opens with the current name in an Input
6. Change the name and click "Save"
7. Confirm the new name appears in the table and a success toast shows

**Test 5: Walk List Rename from Detail (FEAT-03)**
1. Click into a walk list detail page
2. Verify a small pencil icon appears next to the walk list name in the header
3. Click the pencil icon -- confirm a Dialog opens with the current name
4. Change the name and click "Save"
5. Confirm the new name appears in the header
6. Navigate back to the canvassing index -- confirm the new name appears there too
  </action>
  <verify>User types "approved" after visually confirming all 5 tests pass</verify>
  <done>All 5 visual tests pass: note edit, note delete, non-note safety, walk list rename from index, walk list rename from detail. Screenshots saved.</done>
</task>

</tasks>

<verification>
All 5 manual tests pass with visual confirmation via screenshots.
</verification>

<success_criteria>
- Note edit dialog opens with pre-filled text, saves successfully, updated text visible
- Note delete dialog confirms, note removed from list
- Non-note interactions show no edit/delete actions
- Walk list rename works from both canvassing index (dropdown) and detail page (pencil icon)
- Renamed walk list name visible on both pages after save
- All screenshots saved to screenshots/ directory
</success_criteria>

<output>
After completion, create `.planning/phases/56-feature-gap-builds/56-03-SUMMARY.md`
</output>
