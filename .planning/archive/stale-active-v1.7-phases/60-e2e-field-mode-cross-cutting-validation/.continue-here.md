---
phase: 60-e2e-field-mode-cross-cutting-validation
task: debug-remaining-51
total_tasks: 5 plans complete + ongoing debug
status: in_progress
last_updated: 2026-03-30T16:44:40.387Z
---

<current_state>
Phase 60 gap closure plans (60-01 through 60-05) are fully complete. Extended debug work
has reduced E2E failures from 222→88→45→77(fresh env)→51 across 7 rounds in 4 sessions.
236 tests now passing, 51 failing, 67 did not run (cascading), 9 skipped.
Suite runs in ~8.5 minutes with 4 workers against dev server.

This session reset from a completely clean environment (docker compose down -v) to ensure
reproducibility. Fixed Alembic migration compatibility, added org memberships for E2E users,
and used two rounds of agent teams to fix 26 of 77 fresh-environment failures.
</current_state>

<completed_work>

**Infrastructure fixes (this session):**
- alembic/env.py: Added _run_sync_migrations() — uses psycopg2 when DATABASE_URL_SYNC available (asyncpg rejects complex DDL)
- alembic/versions/017_procrastinate_schema.py: Use exec_driver_sql() to bypass SQLAlchemy text() parameter handling for PL/pgSQL %% markers
- scripts/create-e2e-users.py: Added organization_members records for all E2E users (org_owner/org_admin/org_member roles)
- web/e2e/helpers.ts: Minor navigation improvements
- web/src/hooks/useFormGuard.ts: Form guard fix
- web/src/components/canvassing/map/TurfOverviewMap.tsx: Component fix
- web/src/routes/campaigns/$campaignId/canvassing/index.tsx: Route fix

**Spec fixes (this session, 26 tests fixed):**
- login.spec.ts: admin@localhost → owner1@localhost (password change prompt)
- field-mode.volunteer.spec.ts: Added BASE_URL to all browser.newContext() calls (5 tests)
- phase13-voter-verify.spec.ts: Wait for contacts data before asserting
- phase21-integration-polish.spec.ts: Increased timeout, wait for campaign link visibility
- phase27-filter-wiring.spec.ts: Wait for accordion animation before slider interaction
- phase32-verify.spec.ts: Added dismissTourIfPresent() before button click
- connected-journey.spec.ts: Increased timeout for Create Campaign link
- voter-crud/contacts/tags/notes/lists/import/filters/data-validation specs: Various selector and navigation fixes
- campaign-settings/org-management/org-switcher specs: Navigation and timing fixes
- phone-banking/call-lists-dnc/shifts/surveys/turfs/walk-lists specs: Individual fixes
- volunteer-signup/volunteer-tags-availability specs: Selector fixes
- uat-tooltip-popovers.spec.ts: Partial tooltip fixes

**Previous sessions (rounds 1-5):**
- Plans 60-01 through 60-05 complete
- Debug rounds reduced failures 222→88→45
</completed_work>

<remaining_work>

**51 E2E failures in 7 categories:**

1. **RBAC campaign-level navigation (9 failures):**
   - rbac.manager.spec.ts (3): phone banking buttons, surveys button, volunteers roster
   - rbac.viewer.spec.ts (6): voters, canvassing, phone banking (2), volunteers, settings members
   - rbac.volunteer.spec.ts (1): volunteers roster
   - role-gated.volunteer.spec.ts (1): volunteer type toggle
   - Root cause: Campaign page navigation timing — specs can't find UI elements

2. **Voter suite cascading (8 failures):**
   - voter-crud:361, contacts:129, tags:79, notes:176, lists:173, import:79, filters:160, data-validation:162
   - These are LATER tests in serial blocks — early tests now pass but downstream have new issues

3. **Phase verification (8 failures):**
   - phase13 (5): voter-verify VOTR-06 through VOTR-11
   - phase14 (2): import-verify IMPT-01, IMPT-06
   - phase29 (2): registration_county chip and filter
   - phase35 (4): a11y-audit ARIA/contrast tests

4. **Operations (6 failures):**
   - surveys:100, shifts:312, phone-banking:453, turfs:442, walk-lists:290, call-lists:240

5. **Volunteer (3 failures):**
   - volunteers:110, volunteer-signup:21, volunteer-tags:99

6. **UI/cross-cutting (5 failures):**
   - uat-tooltip-popovers (3), cross-cutting:208, a11y-walk-list:169

7. **Other (3 failures):**
   - campaign-settings:206/367, navigation:248, org-management:161
</remaining_work>

<decisions_made>

- Alembic sync driver: asyncpg can't handle CREATE FUNCTION with $$ bodies or multi-statement SQL → use psycopg2 when available
- exec_driver_sql for migration 017: SQLAlchemy text() conflicts with PL/pgSQL %% RAISE format specifiers
- Org memberships in create-e2e-users.py: Resolved all org-level RBAC failures (owner→org_owner, admin→org_admin, others→org_member)
- owner1@localhost for login test: ZITADEL admin forces password change on fresh environments
- BASE_URL in field-mode spec: browser.newContext() doesn't inherit Playwright config baseURL
</decisions_made>

<blockers>
- No hard blockers — all 51 failures are fixable with spec-level changes
- 34 files modified but not yet committed
</blockers>

<context>
Clean environment approach proved valuable — uncovered the Alembic migration incompatibility
and org membership gap that wouldn't have been found on existing volumes. The 6-agent team
approach worked well for parallelizing non-overlapping spec fixes but some agents timed out
before completing (rbac-fix, ui-fix). Two rounds of agents were needed.

The remaining 51 failures are a mix of: (a) specs that were never touched by agents (RBAC
campaign-level, cross-cutting, a11y, some phase specs), (b) cascading failures where fixing
early tests in serial blocks exposed issues in later tests, and (c) timing/selector issues
in operational domain specs.
</context>

<next_action>
1. Commit all 34 modified files as WIP
2. Dispatch targeted agents for the 7 failure categories
3. Focus on RBAC campaign-level nav first (9 failures) — likely a common sidebar/navigation pattern
4. Then voter cascading (8) and phase verification (8)
5. Run full suite validation after all fixes
6. Close Phase 60 when at 0 failures

Run tests: cd web && E2E_USE_DEV_SERVER=1 npx playwright test --reporter=line
</next_action>
