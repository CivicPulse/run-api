---
phase: 63-phone-banking-api-data-resilience
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/api/v1/phone_banks.py
  - app/services/phone_bank.py
  - tests/unit/test_phone_bank.py
  - tests/unit/test_api_phone_banks.py
autonomous: true
requirements:
  - E2E-15
must_haves:
  truths:
    - "Deleting a non-active phone-bank session returns 204 and removes the session."
    - "Deleting an active session is rejected with a 422 problem response and clear validation detail."
    - "Deleting a session removes session-caller assignments but does not delete call history, voter interactions, or DNC effects."
  artifacts:
    - path: "app/api/v1/phone_banks.py"
      provides: "DELETE /campaigns/{campaign_id}/phone-bank-sessions/{session_id} route with 204/422/404 handling"
      contains: "@router.delete(...phone-bank-sessions/{session_id}...)"
    - path: "app/services/phone_bank.py"
      provides: "Guarded hard-delete service method enforcing non-active-only deletion"
      contains: "async def delete_session"
    - path: "tests/unit/test_phone_bank.py"
      provides: "Service-level regression tests for delete behavior"
      contains: "test_delete_session"
    - path: "tests/unit/test_api_phone_banks.py"
      provides: "Route-level tests for 204/422/404 response contract"
      contains: "test_delete_session"
  key_links:
    - from: "app/api/v1/phone_banks.py"
      to: "app/services/phone_bank.py"
      via: "route delegates delete semantics to service"
      pattern: "await _phone_bank_service\.delete_session"
    - from: "app/services/phone_bank.py"
      to: "SessionCaller + PhoneBankSession rows"
      via: "delete assignments then delete session"
      pattern: "delete\(SessionCaller\)|session\.delete\(pb_session\)"
---

<objective>
Add API-complete guarded hard delete for phone-bank sessions so PB-10 can execute real delete lifecycle behavior.

Purpose: Close INT-02 contract drift and enforce session lifecycle safety exactly per locked decisions D-01, D-02, and D-03.
Output: New DELETE route + service method + backend tests proving 204/422/404 contract and deletion boundaries.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/63-phone-banking-api-data-resilience/63-CONTEXT.md
@.planning/phases/63-phone-banking-api-data-resilience/63-RESEARCH.md
@app/api/v1/phone_banks.py
@app/services/phone_bank.py
@tests/unit/test_phone_bank.py

<interfaces>
From app/api/v1/phone_banks.py:
```python
@router.patch("/campaigns/{campaign_id}/phone-bank-sessions/{session_id}")
async def update_session(...)

@router.delete("/campaigns/{campaign_id}/phone-bank-sessions/{session_id}/callers/{caller_user_id}")
async def remove_caller(...)
```

From app/services/phone_bank.py:
```python
class PhoneBankService:
    async def get_session(self, session: AsyncSession, session_id: uuid.UUID) -> PhoneBankSession | None
    async def remove_caller(self, session: AsyncSession, session_id: uuid.UUID, user_id: str) -> None
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add failing tests for guarded session delete contract</name>
  <files>tests/unit/test_phone_bank.py, tests/unit/test_api_phone_banks.py</files>
  <behavior>
    - Test 1: deleting `draft|paused|completed` session succeeds and caller assignments are removed (per D-02, D-03).
    - Test 2: deleting `active` session raises validation error surfaced as 422 problem response (per D-02).
    - Test 3: deleting unknown session returns 404-style not-found behavior.
  </behavior>
  <action>Create/extend unit tests first (RED) for service and route contract before implementation. Add focused route tests in `tests/unit/test_api_phone_banks.py` using existing dependency override patterns and assert status/body for 204, 422, and 404. Keep payload wording flexible but assert response type/title reflect session delete failure for active sessions. Explicitly reference decisions in test names/comments: D-01/D-02/D-03.</action>
  <verify>
    <automated>uv run pytest tests/unit/test_phone_bank.py tests/unit/test_api_phone_banks.py -k "delete_session" -x</automated>
  </verify>
  <done>Delete contract behaviors are codified as automated tests that initially fail before implementation.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement DELETE session route + guarded hard-delete service</name>
  <files>app/services/phone_bank.py, app/api/v1/phone_banks.py</files>
  <behavior>
    - Test 1: DELETE endpoint returns 204 No Content for non-active session deletion (D-01, D-02).
    - Test 2: Active session delete returns 422 problem response (D-02).
    - Test 3: Service deletes session + SessionCaller links but preserves downstream audit data (D-03).
  </behavior>
  <action>Implement `PhoneBankService.delete_session(...)` with lifecycle guardrails: fetch session, reject `active` with ValueError message suitable for route 422 mapping, delete `SessionCaller` rows for the session, then delete the session row (hard delete) per D-01/D-03. Add route `DELETE /campaigns/{campaign_id}/phone-bank-sessions/{session_id}` in `app/api/v1/phone_banks.py` with manager+ role enforcement, `ensure_user_synced`, existing problem-response style for 404/422, commit, and explicit `Response(status_code=204)`. Do not introduce soft-delete columns or schema changes.</action>
  <verify>
    <automated>uv run pytest tests/unit/test_phone_bank.py tests/unit/test_api_phone_banks.py -k "delete_session" -x && uv run ruff check app/api/v1/phone_banks.py app/services/phone_bank.py tests/unit/test_phone_bank.py tests/unit/test_api_phone_banks.py</automated>
  </verify>
  <done>Backend exposes API-complete guarded delete behavior matching D-01/D-02/D-03 and all delete-focused tests pass.</done>
</task>

</tasks>

<verification>
Run delete-focused backend tests and Ruff checks from task verify blocks.
</verification>

<success_criteria>
- PB-10 prerequisite endpoint exists and returns 204 on valid non-active delete.
- Active-session delete attempts reliably return 422, not silent success.
- Caller assignments are removed with session deletion while audit records remain untouched.
</success_criteria>

<output>
After completion, create `.planning/phases/63-phone-banking-api-data-resilience/63-01-SUMMARY.md`
</output>
