---
phase: 38-tech-debt-cleanup
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - web/e2e/phase31-canvassing.spec.ts
  - web/e2e/phase32-verify.spec.ts
  - web/src/hooks/useCallingSession.ts
  - web/src/routes/field/$campaignId/canvassing.tsx
autonomous: true
requirements: []

must_haves:
  truths:
    - "Playwright getByText('Survey Questions') no longer matches two elements"
    - "useCallingSession handleOutcome useCallback includes campaignId and sessionId in deps"
    - "canvassing.tsx has no unused variable warning for isRunning"
  artifacts:
    - path: "web/e2e/phase31-canvassing.spec.ts"
      provides: "Unambiguous Survey Questions selector"
      contains: "getByRole(\"heading\", { name: \"Survey Questions\" })"
    - path: "web/e2e/phase32-verify.spec.ts"
      provides: "Unambiguous Survey Questions selector"
      contains: "getByRole(\"heading\", { name: \"Survey Questions\" })"
    - path: "web/src/hooks/useCallingSession.ts"
      provides: "Complete useCallback dependency array"
      contains: "campaignId, sessionId"
    - path: "web/src/routes/field/$campaignId/canvassing.tsx"
      provides: "No unused isRunning variable"
  key_links: []
---

<objective>
Fix 3 quick tech debt items from the v1.4 milestone audit: Playwright selector ambiguity, useCallback missing dependencies, and unused variable.

Purpose: Close deterministic code quality issues identified in the milestone audit.
Output: 4 files cleaned up with no behavior changes.
</objective>

<execution_context>
@/home/kwhatcher/.claude/get-shit-done/workflows/execute-plan.md
@/home/kwhatcher/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/38-tech-debt-cleanup/38-CONTEXT.md
@.planning/phases/38-tech-debt-cleanup/38-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix Playwright selector ambiguity and useCallback deps</name>
  <files>web/e2e/phase31-canvassing.spec.ts, web/e2e/phase32-verify.spec.ts, web/src/hooks/useCallingSession.ts</files>
  <read_first>
    - web/e2e/phase31-canvassing.spec.ts (line 153: ambiguous getByText selector)
    - web/e2e/phase32-verify.spec.ts (lines 339-341: ambiguous getByText selector)
    - web/src/hooks/useCallingSession.ts (line 241: useCallback dependency array)
    - web/src/components/field/InlineSurvey.tsx (lines 122-129: SheetTitle + sr-only div causing ambiguity)
  </read_first>
  <action>
1. In `web/e2e/phase31-canvassing.spec.ts` at line 153, replace:
   ```
   const surveyHeading = page.getByText("Survey Questions")
   ```
   with:
   ```
   const surveyHeading = page.getByRole("heading", { name: "Survey Questions" })
   ```
   This targets the SheetTitle h2 element only, avoiding the sr-only aria-live div that also contains "Survey Questions" text.

2. In `web/e2e/phase32-verify.spec.ts` at line 341, replace:
   ```
   page.getByText("Survey Questions"),
   ```
   with:
   ```
   page.getByRole("heading", { name: "Survey Questions" }),
   ```

3. In `web/src/hooks/useCallingSession.ts` at line 241, replace the dependency array:
   ```
   [currentEntry, callStartedAt, phoneNumberUsed, recordCall, recordOutcome],
   ```
   with:
   ```
   [currentEntry, callStartedAt, phoneNumberUsed, recordCall, recordOutcome, campaignId, sessionId],
   ```
   Both campaignId (from route params) and sessionId (from callingStore) are primitive strings, so adding them is safe and will not cause unnecessary re-renders.
  </action>
  <verify>
    <automated>cd /home/kwhatcher/projects/run-api/web && grep -n 'getByRole("heading", { name: "Survey Questions" })' e2e/phase31-canvassing.spec.ts e2e/phase32-verify.spec.ts && grep -n 'campaignId, sessionId' src/hooks/useCallingSession.ts</automated>
  </verify>
  <acceptance_criteria>
    - web/e2e/phase31-canvassing.spec.ts contains `getByRole("heading", { name: "Survey Questions" })` (no getByText("Survey Questions") remaining)
    - web/e2e/phase32-verify.spec.ts contains `getByRole("heading", { name: "Survey Questions" })` (no getByText("Survey Questions") remaining)
    - web/src/hooks/useCallingSession.ts line ~241 dependency array contains `campaignId, sessionId`
    - No instances of `getByText("Survey Questions")` remain in either e2e file
  </acceptance_criteria>
  <done>Playwright selectors use getByRole("heading") for Survey Questions, useCallback deps include campaignId and sessionId</done>
</task>

<task type="auto">
  <name>Task 2: Remove unused isRunning variable and verify TypeScript compiles</name>
  <files>web/src/routes/field/$campaignId/canvassing.tsx</files>
  <read_first>
    - web/src/routes/field/$campaignId/canvassing.tsx (line 57: unused isRunning destructure, line 60: s.isRunning used inside selector callback)
  </read_first>
  <action>
1. In `web/src/routes/field/$campaignId/canvassing.tsx`, delete line 57:
   ```
   const isRunning = useTourStore((s) => s.isRunning)
   ```
   This line is redundant because line 60 accesses `s.isRunning` inside the `shouldShowQS` selector callback. The top-level `isRunning` const is never referenced elsewhere in the component.

2. Run TypeScript compilation to confirm no type errors:
   ```
   cd /home/kwhatcher/projects/run-api/web && npx tsc --noEmit
   ```
   This validates that removing the variable doesn't break any references and that the useCallback deps change from Task 1 is also type-safe.
  </action>
  <verify>
    <automated>cd /home/kwhatcher/projects/run-api/web && ! grep -n "const isRunning = useTourStore" src/routes/field/\$campaignId/canvassing.tsx && npx tsc --noEmit 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - web/src/routes/field/$campaignId/canvassing.tsx does NOT contain `const isRunning = useTourStore`
    - `npx tsc --noEmit` exits with code 0 (no type errors)
    - The `shouldShowQS` selector on the following line still references `s.isRunning` inside its callback (unchanged)
  </acceptance_criteria>
  <done>Unused isRunning variable removed, TypeScript compiles cleanly</done>
</task>

</tasks>

<verification>
1. `cd /home/kwhatcher/projects/run-api/web && grep -rn 'getByText("Survey Questions")' e2e/` returns no matches
2. `cd /home/kwhatcher/projects/run-api/web && grep -n 'campaignId, sessionId' src/hooks/useCallingSession.ts` returns the updated dep array
3. `cd /home/kwhatcher/projects/run-api/web && grep -c 'const isRunning' src/routes/field/\$campaignId/canvassing.tsx` returns 0
4. `cd /home/kwhatcher/projects/run-api/web && npx tsc --noEmit` exits 0
</verification>

<success_criteria>
- Zero instances of ambiguous `getByText("Survey Questions")` in e2e tests
- useCallingSession handleOutcome dependency array includes campaignId and sessionId
- No unused isRunning variable in canvassing.tsx
- TypeScript compilation passes
</success_criteria>

<output>
After completion, create `.planning/phases/38-tech-debt-cleanup/38-01-SUMMARY.md`
</output>
