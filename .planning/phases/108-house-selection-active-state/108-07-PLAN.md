---
phase: 108-house-selection-active-state
plan: 07
type: execute
wave: 6
depends_on: [108-01, 108-02, 108-03, 108-04, 108-05, 108-06]
files_modified:
  - .planning/phases/108-house-selection-active-state/108-VERIFICATION-RESULTS.md
  - .planning/REQUIREMENTS.md
  - .planning/STATE.md
autonomous: true
requirements: [SELECT-01, SELECT-02, SELECT-03]
must_haves:
  truths:
    - "uv run ruff check . passes clean"
    - "uv run pytest passes at phase 107 baseline (1118 pass, 0 fail)"
    - "cd web && npx vitest run passes with phase 108 additions"
    - "cd web && ./scripts/run-e2e.sh passes on TWO consecutive runs"
    - "SELECT-01, SELECT-02, SELECT-03 marked [x] in REQUIREMENTS.md"
    - "108-VERIFICATION-RESULTS.md documents all gate results with timestamps"
  artifacts:
    - path: ".planning/phases/108-house-selection-active-state/108-VERIFICATION-RESULTS.md"
      provides: "Phase 108 exit gate evidence (ruff + pytest + vitest + 2x Playwright)"
      contains: "SELECT-0"
    - path: ".planning/REQUIREMENTS.md"
      provides: "SELECT-01/02/03 marked complete"
      contains: "[x] **SELECT-01"
  key_links:
    - from: "108-VERIFICATION-RESULTS.md"
      to: "e2e-runs.jsonl"
      via: "Two consecutive green run-e2e.sh timestamps"
      pattern: "e2e-runs"
---

<objective>
Phase 108 exit gate — run the full 4-suite verification (ruff + pytest +
vitest + 2x Playwright via run-e2e.sh), author
`108-VERIFICATION-RESULTS.md` documenting every gate result with timestamps
and diff counts, mark SELECT-01/02/03 complete in REQUIREMENTS.md, and
update STATE.md with phase completion. Mirror phase 107's exit gate pattern.

Purpose: Close phase 108 on a trustworthy baseline with no regressions from
phase 107 or earlier.

Output:
- New file `108-VERIFICATION-RESULTS.md`
- Updated `REQUIREMENTS.md` with SELECT-01/02/03 marked [x]
- Updated `STATE.md` with phase 108 position advanced
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/107-canvassing-wizard-fixes/107-VERIFICATION-RESULTS.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/108-house-selection-active-state/108-CONTEXT.md

<interfaces>
From phase 107 exit gate (STATE.md, 2026-04-11):
- pytest baseline: 1118 pass, 0 fail, 0 todo
- vitest baseline: 708 pass, 0 fail, 24 todo
- Playwright baseline: 305 pass, 0 fail, 66 skipped
- TWO consecutive green Playwright runs required per phase 106 D-13 pattern

Phase 108 changes:
- web/src/hooks/useCanvassingWizard.ts (Plan 108-02)
- web/src/hooks/useCanvassingWizard.test.ts (new test — Plan 108-02)
- web/src/hooks/useCanvassingWizard.state-machine.test.ts (NEW — Plan 108-05, 5 tests)
- web/src/components/field/CanvassingMap.tsx (Plan 108-03 refactor)
- web/src/components/field/CanvassingMap.test.tsx (≥4 new tests — Plan 108-03)
- web/src/components/field/HouseholdCard.test.tsx (1 new test — Plan 108-02)
- web/src/routes/field/$campaignId/canvassing.tsx (Plan 108-03)
- web/src/index.css (Plan 108-03)
- web/e2e/canvassing-wizard.spec.ts (Plan 108-01 fixture House C)
- web/e2e/canvassing-house-selection.spec.ts (NEW — Plan 108-06, 4 tests)

No Python changes — ruff + pytest are regression-only gates.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run the 4-suite exit gate and collect evidence</name>
  <files>.planning/phases/108-house-selection-active-state/108-VERIFICATION-RESULTS.md</files>
  <read_first>
    - .planning/phases/107-canvassing-wizard-fixes/107-VERIFICATION-RESULTS.md (format template)
    - .planning/STATE.md (phase 107 baselines)
    - web/e2e-runs.jsonl (tail — understand the wrapper log format)
  </read_first>
  <action>
Run each gate in order and capture output. For each gate, record the command, timestamp, pass/fail/skip counts, and deltas from phase 107 baseline.

**Gate 1 — Ruff:**
```bash
uv run ruff check . 2>&1 | tee /tmp/108-ruff.log
uv run ruff format --check . 2>&1 | tee -a /tmp/108-ruff.log
```
Expected: clean, no diffs.

**Gate 2 — Pytest:**
```bash
uv run pytest 2>&1 | tail -20 | tee /tmp/108-pytest.log
```
Expected: 1118 pass (phase 107 baseline), 0 fail. If any delta, investigate and fix. NO Python changes in phase 108 = NO test delta is acceptable.

**Gate 3 — Vitest:**
```bash
cd web && npx vitest run 2>&1 | tail -30 | tee /tmp/108-vitest.log
cd web && npx tsc --noEmit 2>&1 | tee /tmp/108-tsc.log
```
Expected: phase 107 baseline (708 pass) PLUS new phase 108 tests:
- +1 hook test (Plan 108-02 Task 2)
- +1 HouseholdCard render-path test (Plan 108-02 Task 3)
- +4 CanvassingMap component tests (Plan 108-03 Task 3)
- +5 state-machine tests (Plan 108-05)
= ~719+ pass total. Adjust expectation based on actual deltas. 0 fail.

**Gate 4 — Playwright (TWO consecutive greens per phase 106 D-13 pattern):**
```bash
cd web && ./scripts/run-e2e.sh 2>&1 | tee /tmp/108-e2e-run-1.log
# Wait for completion, check last jsonl entry
tail -1 e2e-runs.jsonl
# Run again
cd web && ./scripts/run-e2e.sh 2>&1 | tee /tmp/108-e2e-run-2.log
tail -1 e2e-runs.jsonl
```
Expected: BOTH runs green. Zero fails. Skipped count may match or exceed phase 107 (66). Pass count must include the 4 new phase 108 tests plus any new phase 107 baseline.

If the first run fails due to auth staleness (the known phase 107 failure mode), clear `playwright/.auth/*.json` once and retry — the wrapper re-mints tokens. Document the clear in the results file if it was needed.

**Write `108-VERIFICATION-RESULTS.md`:**

Mirror the phase 107 format. Top-level structure:

```markdown
# Phase 108 Verification Results

**Phase:** 108 — House Selection & Active-State
**Gate run:** 2026-04-11
**Status:** PASSED | FAILED

## Baseline (from phase 107 exit gate)

| Suite | Pass | Fail | Skip/Todo |
|---|---|---|---|
| pytest | 1118 | 0 | 0 |
| vitest | 708 | 0 | 24 |
| Playwright | 305 | 0 | 66 |

## Phase 108 Results

### Gate 1 — Ruff
**Command:** `uv run ruff check . && uv run ruff format --check .`
**Timestamp:** {ISO}
**Result:** PASS (no diffs)

### Gate 2 — Pytest
**Command:** `uv run pytest`
**Timestamp:** {ISO}
**Result:** {N} pass, 0 fail — delta vs 107 baseline: 0 (no Python changes in phase 108)

### Gate 3 — Vitest + tsc
**Command:** `cd web && npx vitest run && npx tsc --noEmit`
**Timestamp:** {ISO}
**Result:** {N} pass, 0 fail
**Delta vs 107 baseline:** +{N} from phase 108 additions
- +1 useCanvassingWizard.test.ts (handleJumpToAddress pin-clear)
- +1 HouseholdCard.test.tsx (list-tap render-path guard)
- +4 CanvassingMap.test.tsx (click wiring, panTo x2, volunteer non-interactive)
- +5 useCanvassingWizard.state-machine.test.ts (list-tap, map-tap, auto-advance, skip, resume)
**tsc:** clean exit

### Gate 4 — Playwright (2x consecutive)
**Wrapper:** `web/scripts/run-e2e.sh`
**Run 1 timestamp:** {ISO from e2e-runs.jsonl}
**Run 1 result:** {P/F/S counts}
**Run 2 timestamp:** {ISO from e2e-runs.jsonl}
**Run 2 result:** {P/F/S counts}
**Delta vs 107 baseline:** +4 from canvassing-house-selection.spec.ts
**Auth staleness retry needed:** yes/no

## Requirements Closed

- [x] SELECT-01: list-tap handleJumpToAddress wrap (Plans 108-02, 108-06)
- [x] SELECT-02: map-tap interactivity (Plans 108-03, 108-06)
- [x] SELECT-03: state machine audit (Plans 108-04 docs + 108-05 test)

## Test Coverage Obligations (TEST-01/02/03 per D-12)

- [x] Unit: hook + state-machine tests
- [x] Component: HouseholdCard render-path + CanvassingMap prop-capture
- [x] E2E: canvassing-house-selection.spec.ts (4 tests)

## Known Stubs / Deferred

- Reconciliation state machine section — deferred to phase 110 (D-10)

## Regressions

None.

---
*Phase 108 exit gate: PASSED*
```

Fill in the actual numbers from the gate runs. If any gate fails, investigate and fix — DO NOT mark the phase complete until all gates are green.
  </action>
  <verify>
    <automated>test -f .planning/phases/108-house-selection-active-state/108-VERIFICATION-RESULTS.md && grep -q "SELECT-01" .planning/phases/108-house-selection-active-state/108-VERIFICATION-RESULTS.md && grep -q "SELECT-02" .planning/phases/108-house-selection-active-state/108-VERIFICATION-RESULTS.md && grep -q "SELECT-03" .planning/phases/108-house-selection-active-state/108-VERIFICATION-RESULTS.md && grep -qE "PASSED|PASS" .planning/phases/108-house-selection-active-state/108-VERIFICATION-RESULTS.md</automated>
  </verify>
  <done>
- All 4 gates run and documented in `108-VERIFICATION-RESULTS.md`
- Two consecutive green Playwright runs recorded with timestamps from `e2e-runs.jsonl`
- 0 failures across all suites
- 0 regression vs phase 107 baseline (only additions)
- Phase marked PASSED at the bottom of the results file
  </done>
</task>

<task type="auto">
  <name>Task 2: Mark SELECT-01/02/03 complete + update STATE.md</name>
  <files>.planning/REQUIREMENTS.md, .planning/STATE.md</files>
  <read_first>
    - .planning/REQUIREMENTS.md (SELECT section at lines 28-32)
    - .planning/STATE.md (current position block, accumulated context)
    - .planning/phases/108-house-selection-active-state/108-VERIFICATION-RESULTS.md (from Task 1)
  </read_first>
  <action>
**Edit 1 — REQUIREMENTS.md:**

Change the three unchecked SELECT items from `[ ]` to `[x]`:

```markdown
### SELECT — House Selection & Active-State

- [x] **SELECT-01**: Volunteers can tap any house in the household list to set it as the active house.
- [x] **SELECT-02**: Volunteers can tap any house marker on the map to set it as the active house.
- [x] **SELECT-03**: The active-house state machine is reviewed end-to-end so a volunteer standing at a specific address can reliably make that address active regardless of entry point (list, map, auto-advance, skip, resume).
```

Do NOT modify other requirements. Do NOT alter the Traceability table at the bottom (phase mapping is unchanged).

**Edit 2 — STATE.md:**

1. Update the frontmatter progress counter:
```yaml
progress:
  total_phases: 5
  completed_phases: 3   # was 2
  total_plans: 22       # was 15 — adjust based on actual plan count (15 + 7 = 22)
  completed_plans: 22   # was 15
  percent: 60           # 3/5
```

2. Update `stopped_at` to: `Completed 108-07-PLAN.md (phase 108 exit gate PASSED)`

3. Update `last_updated` and `last_activity` to current ISO / date.

4. Append to `## Accumulated Context / ### Decisions` a new line:
```
- [Phase 108]: Phase 108 exit gate PASSED — pytest {N}/{F}/{T}, vitest {N}/{F}/{T}, Playwright {N}/{F}/{S} on two consecutive greens. SELECT-01/02/03 closed. 108-STATE-MACHINE.md captures the active-house state machine with reconciliation placeholder for phase 110.
```

5. Update `## Current Position`:
```
Phase: 109
Plan: Not started
Status: Ready for planning
Last activity: 2026-04-11
```

6. Update the `Progress:` bar to reflect 60%.

Do NOT touch blockers, concerns, or session continuity fields beyond what's listed above.

**Edit 3 — commit:**

```bash
cd /home/kwhatcher/projects/civicpulse/run-api
git add .planning/phases/108-house-selection-active-state/108-VERIFICATION-RESULTS.md .planning/REQUIREMENTS.md .planning/STATE.md
git commit -m "docs(108): phase 108 exit gate PASSED — SELECT-01/02/03 complete

pytest {N}/{F}/{T}, vitest {N}/{F}/{T}, Playwright {N}/{F}/{S} on
two consecutive greens. 108-STATE-MACHINE.md captures the active-house
state machine with reconciliation placeholder for phase 110."
```
  </action>
  <verify>
    <automated>grep -q "\[x\] \*\*SELECT-01" .planning/REQUIREMENTS.md && grep -q "\[x\] \*\*SELECT-02" .planning/REQUIREMENTS.md && grep -q "\[x\] \*\*SELECT-03" .planning/REQUIREMENTS.md && grep -q "Phase 108" .planning/STATE.md</automated>
  </verify>
  <done>
- SELECT-01/02/03 marked [x] in REQUIREMENTS.md
- STATE.md frontmatter reflects completed_phases=3, total_plans=22, percent=60
- STATE.md Current Position advanced to Phase 109
- Accumulated Context has a new [Phase 108] decision entry
- Phase 107 entries preserved unchanged
- Commit created with conventional commit message
  </done>
</task>

</tasks>

<verification>
- `uv run ruff check .` clean
- `uv run pytest` at phase 107 baseline
- `cd web && npx vitest run` passes with +11 new tests
- `cd web && ./scripts/run-e2e.sh` passes twice consecutively
- `108-VERIFICATION-RESULTS.md` exists with PASSED status
- REQUIREMENTS.md SELECT items all [x]
- STATE.md reflects phase 108 completion
</verification>

<success_criteria>
- Phase 108 exit gate PASSED across all 4 suites
- Two consecutive green Playwright runs via run-e2e.sh
- No regressions from phase 107 baseline
- SELECT-01/02/03 closed with full test coverage
- Project state advanced to phase 109 readiness
</success_criteria>

<output>
Create `.planning/phases/108-house-selection-active-state/108-07-SUMMARY.md`
with the final gate tallies and commit hashes, then produce a phase-level
SUMMARY at `108-SUMMARY.md` aggregating all 7 plan outcomes.
</output>
</content>
</invoke>