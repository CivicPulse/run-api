---
phase: 108-house-selection-active-state
plan: 04
type: execute
wave: 4
depends_on: [108-02, 108-03]
files_modified:
  - .planning/phases/108-house-selection-active-state/108-STATE-MACHINE.md
  - .planning/ROADMAP.md
autonomous: true
requirements: [SELECT-03]
must_haves:
  truths:
    - "108-STATE-MACHINE.md exists with a Mermaid state diagram"
    - "108-STATE-MACHINE.md contains a transition table with all 5 entry points"
    - "108-STATE-MACHINE.md ends with a Reconciliation (deferred to phase 110) placeholder section"
    - "ROADMAP.md phase 108 Canonical refs list includes 108-STATE-MACHINE.md"
  artifacts:
    - path: ".planning/phases/108-house-selection-active-state/108-STATE-MACHINE.md"
      provides: "SELECT-03 audit deliverable — Mermaid diagram + transition table + reconciliation placeholder"
      contains: "mermaid"
    - path: ".planning/ROADMAP.md"
      provides: "Updated phase 108 canonical refs including STATE-MACHINE.md"
      contains: "108-STATE-MACHINE"
  key_links:
    - from: "108-STATE-MACHINE.md transition table"
      to: "Plan 108-05 behavioral integration test"
      via: "Test asserts every documented transition"
      pattern: "transition"
---

<objective>
SELECT-03 (docs half) — Author `108-STATE-MACHINE.md` containing the locked
audit deliverable per D-08: a Mermaid state diagram of all active-house
transitions, a transition table with columns Source/Trigger/Target/Side
effects, narrative documentation of the 5 entry points (list-tap, map-tap,
auto-advance, skip, resume), and a `## Reconciliation (deferred to phase 110)`
placeholder section with the open questions from D-10. Update ROADMAP.md so
phase 108's canonical refs list this file.

Purpose: Deliver the documentation half of SELECT-03. The test half is
Plan 108-05 which verifies the transitions documented here.

Output:
- New file `108-STATE-MACHINE.md`
- Updated `ROADMAP.md` phase 108 entry
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/108-house-selection-active-state/108-CONTEXT.md
@.planning/phases/108-house-selection-active-state/108-RESEARCH.md
@.planning/ROADMAP.md

<interfaces>
From CONTEXT.md D-08/D-09/D-10:
- 5 entry points: list-tap, map-tap, auto-advance, skip, resume
- Document structure: Mermaid diagram + transition table + reconciliation placeholder
- NO XState refactor, NO useReducer — docs + single behavioral test only

From RESEARCH.md §Runtime State Inventory (Resume semantics):
- canvassingStore persists to sessionStorage key `canvassing-wizard` v1
- Persisted fields: walkListId, currentAddressIndex, completedEntries,
  skippedEntries, lastActiveAt, sortMode, locationStatus, locationSnapshot
- pinnedHouseholdKey is hook-local, NOT persisted
- ResumePrompt onResume: () => {} — no mutation on resume path
- currentAddressIndex is read via zustand-persist merge on mount

From Plan 108-02 output:
- handleJumpToAddress now clears pinnedHouseholdKey before delegating
- Same wrap pattern as advanceAddress (107-08.1) and skipEntry (107-08.1)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write 108-STATE-MACHINE.md with Mermaid diagram + transition table</name>
  <files>.planning/phases/108-house-selection-active-state/108-STATE-MACHINE.md</files>
  <read_first>
    - .planning/phases/108-house-selection-active-state/108-CONTEXT.md D-08/D-09/D-10/D-11
    - .planning/phases/108-house-selection-active-state/108-RESEARCH.md §Runtime State Inventory (lines 434-473)
    - web/src/hooks/useCanvassingWizard.ts (verify the actual state transitions: advanceAddress, skipEntry, handleJumpToAddress, and the render-time pin block)
    - web/src/stores/canvassingStore.ts (verify jumpToAddress, advanceAddress, skipEntry actions and what they mutate)
    - web/src/components/field/ResumePrompt.tsx (verify resume path does not mutate)
  </read_first>
  <action>
Create `.planning/phases/108-house-selection-active-state/108-STATE-MACHINE.md` with the following structure. Fill each section based on the actual code read in `read_first`.

Structure:

```markdown
# Phase 108 — Active-House State Machine (SELECT-03 Audit)

**Scope:** The state machine governing which household is "active" in the
canvassing wizard, and how every entry point reaches a consistent target.

**Authored:** 2026-04-11
**Audit type:** Documentation + single behavioral integration test
  (per CONTEXT D-08 — NO XState refactor, NO useReducer extraction)

***

## State Variables

| Variable | Location | Persisted | Purpose |
|---|---|---|---|
| currentAddressIndex | canvassingStore | yes (sessionStorage) | Index into the ordered households list |
| pinnedHouseholdKey | useCanvassingWizard hook | no (in-memory) | Render-time pin preventing sort-flicker on GPS updates |
| lastActiveAt | canvassingStore | yes | Timestamp of last navigation for resume UX |
| sortMode | canvassingStore | yes | sequence or distance — affects household ordering |
| locationSnapshot | canvassingStore | yes | Last known coordinates for distance sort |

`pinnedHouseholdKey` is the load-bearing detail that caused 107-08.1 and
108-02 fixes: intentional navigation MUST clear it, sort-mode/GPS updates
MUST preserve it.

***

## State Diagram

` + '```' + `mermaid
stateDiagram-v2
    [*] --> OnHousehold_i : mount (resume from persisted index)

    OnHousehold_i --> OnHousehold_j : list-tap j
    OnHousehold_i --> OnHousehold_j : map-tap j
    OnHousehold_i --> OnHousehold_i+1 : auto-advance after outcome
    OnHousehold_i --> OnHousehold_i+1 : skip
    OnHousehold_i --> OnHousehold_i : GPS/sort update (pin preserved)
    OnHousehold_i --> OnHousehold_clamp : households list shrinks (clamp)

    note right of OnHousehold_j
      Entry-point side effects:
      - pinnedHouseholdKey := null (intentional nav)
      - currentAddressIndex := j
      - lastActiveAt := now
      - haptic vibrate(50)
      - card swap + ARIA announce
    end note
` + '```' + `

***

## Transition Table

| # | Source | Trigger | Target | Side Effects | Clears pin? |
|---|---|---|---|---|---|
| 1 | On household i | DoorListView row tap (list-tap) | On household j | setPinnedHouseholdKey(null); storeJumpToAddress(j); vibrate(50); card swap; ARIA announce | yes |
| 2 | On household i | CanvassingMap marker tap (map-tap) | On household j | setPinnedHouseholdKey(null); storeJumpToAddress(j); vibrate(50); card swap; ARIA announce; map.panTo(lat,lng) | yes |
| 3 | On household i | House-level outcome submit (auto-advance) | On household i+1 | setPinnedHouseholdKey(null); storeAdvanceAddress(); toast + vibrate; card swap; ARIA announce | yes |
| 4 | On household i | Skip button tap | On household i+1 (next pending) | setPinnedHouseholdKey(null); storeSkipEntry(id); Undo toast; card swap | yes |
| 5 | (unmounted) | Route mount from sessionStorage | On household persistedIndex | zustand-persist merge → currentAddressIndex = persisted; pinnedHouseholdKey starts null and is re-set by render-time pin block | n/a (starts null) |
| 6 | On household i | GPS update / sortMode change | On household i (same) | pin preserved; list order may shift | no |
| 7 | On household i (where i >= households.length) | households list shrinks | On household clamp | storeJumpToAddress(households.length-1) via useLayoutEffect — uses RAW store action, does NOT clear pin | no |

**Key invariant:** Rows 1-4 (intentional navigation) all clear pin. Rows 6-7
(incidental index changes) preserve pin. This split is enforced in code by
the wrap-the-action pattern (Plan 108-02 + 107-08.1) — the hook-level
callbacks clear pin, the raw store actions do not.

***

## Entry Point Narrative

### 1. List-tap (SELECT-01)

**UI:** DoorListView row button. See `DoorListView.tsx:108-160`.
**Flow:** onClick → onJump(index) → handleJumpToAddress(index) → wrapped
callback clears pin → storeJumpToAddress → card swap → focus moves to
HouseholdCard address heading (existing route effect).
**Test coverage:** Plan 108-02 Task 2 (hook unit), Task 3 (render-path guard
in HouseholdCard.test.tsx), Plan 108-05 (behavioral state-machine test),
Plan 108-06 (E2E).

### 2. Map-tap (SELECT-02)

**UI:** CanvassingMap marker. See `CanvassingMap.tsx` (after Plan 108-03).
**Flow:** eventHandlers.click → handleMarkerClick → onHouseholdSelect(index)
→ handleJumpToAddress(index) → same wrap as list-tap → map.panTo with
reduced-motion gate.
**Test coverage:** Plan 108-03 Task 3 (component tests), Plan 108-05
(behavioral state-machine test), Plan 108-06 (E2E).

### 3. Auto-advance after outcome (CANV-01, inherited from phase 107)

**UI:** InlineSurvey / outcome button submit in HouseholdCard.
**Flow:** handleOutcome → HOUSE_LEVEL_OUTCOMES check → advanceAfterOutcome
→ wrapped advanceAddress (107-08.1) → clears pin → storeAdvanceAddress.
**Test coverage:** phase 107 plans 04, 08, 08.1.

### 4. Skip (CANV-02, inherited from phase 107)

**UI:** Skip button in HouseholdCard.
**Flow:** handleSkipAddress → wrapped skipEntry (107-08.1) → clears pin →
storeSkipEntry + advanceAddress → Undo toast.
**Test coverage:** phase 107 plans 05, 08, 08.1.

### 5. Resume

**UI:** ResumePrompt toast on mount when persisted state exists.
**Flow:** zustand-persist merge reads sessionStorage on store creation →
currentAddressIndex is already set before mount → ResumePrompt shows
"Pick up at door N of M" → onResume: () => {} (no mutation, leaves state
alone) OR onStartOver: handleJumpToAddress(firstPendingIdx).
**Pin behavior on resume:** pinnedHouseholdKey starts null (hook-local, not
persisted). The render-time pin block re-pins to whatever is at
currentAddressIndex on first render. No cross-session pin leak.
**Test coverage:** Plan 108-05 (behavioral test with seeded sessionStorage),
Plan 108-06 (E2E resume-after-navigation test).

***

## Reconciliation (deferred to phase 110)

This section is a PLACEHOLDER. Phase 110 (OFFLINE-01/02/03) will fill it in
as part of offline-queue hardening work.

**Open questions phase 110 must answer:**

1. **Active-house persistence on reconnect.** When the offline outcome queue
   replays after reconnect, does `currentAddressIndex` stay where the
   volunteer left it, or does it jump to whatever the last queued action
   implies?

2. **Server/client divergence.** If the server's last-active state
   (inferred from the last ingested outcome's `household_key`) differs from
   the client's `currentAddressIndex`, which wins? Client (volunteer intent
   is authoritative) or server (data-truth reconciliation)?

3. **Conflict surfacing.** How are reconciliation conflicts surfaced to the
   volunteer — silent merge, toast, modal, or a dedicated review queue?

4. **Queue ordering.** If the volunteer skipped a house offline and then
   activated it again (list-tap or map-tap) before the skip synced, which
   intent wins on reconcile?

Phase 108 deliberately leaves these unanswered per CONTEXT D-10. The
state machine above covers the ONLINE path only. Phase 110 extends it.

***

*Phase: 108-house-selection-active-state*
*Entry points (D-09): list-tap, map-tap, auto-advance, skip, resume*
*Reconciliation: deferred to phase 110 (D-10)*
```

**Important:** The Mermaid block above uses backtick-escape tokens because this plan file itself embeds the target. When the executor writes `108-STATE-MACHINE.md` they must use real triple-backticks, not the escape tokens.

Run:
```bash
test -f .planning/phases/108-house-selection-active-state/108-STATE-MACHINE.md
grep -c "mermaid" .planning/phases/108-house-selection-active-state/108-STATE-MACHINE.md
grep -c "Reconciliation (deferred to phase 110)" .planning/phases/108-house-selection-active-state/108-STATE-MACHINE.md
grep -c "list-tap\|map-tap\|auto-advance\|skip\|resume\|Resume" .planning/phases/108-house-selection-active-state/108-STATE-MACHINE.md
```
  </action>
  <verify>
    <automated>test -f .planning/phases/108-house-selection-active-state/108-STATE-MACHINE.md && grep -q "mermaid" .planning/phases/108-house-selection-active-state/108-STATE-MACHINE.md && grep -q "Reconciliation (deferred to phase 110)" .planning/phases/108-house-selection-active-state/108-STATE-MACHINE.md && grep -qE "list-tap" .planning/phases/108-house-selection-active-state/108-STATE-MACHINE.md && grep -qE "map-tap" .planning/phases/108-house-selection-active-state/108-STATE-MACHINE.md && grep -qE "auto-advance" .planning/phases/108-house-selection-active-state/108-STATE-MACHINE.md</automated>
  </verify>
  <done>
- `108-STATE-MACHINE.md` exists
- Contains a `mermaid` fenced code block (state diagram)
- Contains a transition table covering all 5 entry points
- Contains a `## Reconciliation (deferred to phase 110)` placeholder section with ≥3 open questions
- Each of the 5 entry points (list-tap, map-tap, auto-advance, skip, resume) has a narrative paragraph
- State variables section names pinnedHouseholdKey as hook-local/not persisted
  </done>
</task>

<task type="auto">
  <name>Task 2: Update ROADMAP.md phase 108 canonical refs</name>
  <files>.planning/ROADMAP.md</files>
  <read_first>
    - .planning/ROADMAP.md (lines 240-310 — phase 107 entry for format reference, phase 108 entry at ~268-280)
  </read_first>
  <action>
Update `.planning/ROADMAP.md` phase 108 entry to reflect the 7 plans and add a canonical refs line pointing to `108-STATE-MACHINE.md`.

Find the phase 108 block starting `#### Phase 108: House Selection & Active-State`.

1. Update `**Plans:**` line to `**Plans:** 7 plans`
2. Add a plan list section below success criteria (mirror phase 107 format):

```markdown
Plans:
- [ ] 108-01-PLAN.md — Wave 0: Spikes A1 (Space key) + A2 (L.DivIcon) + fixture House C
- [ ] 108-02-PLAN.md — SELECT-01: wrap handleJumpToAddress, hook unit test, HouseholdCard render-path guard
- [ ] 108-03-PLAN.md — SELECT-02: CanvassingMapMarkers refactor, panTo, ARIA, CSS, component tests
- [ ] 108-04-PLAN.md — SELECT-03 docs: 108-STATE-MACHINE.md Mermaid + transition table + reconciliation placeholder
- [ ] 108-05-PLAN.md — SELECT-03 test: behavioral integration test covering all 5 entry points
- [ ] 108-06-PLAN.md — E2E: canvassing-house-selection.spec.ts (list-tap, map-tap, resume)
- [ ] 108-07-PLAN.md — Phase exit gate: ruff + pytest + vitest + 2x run-e2e.sh, 108-VERIFICATION-RESULTS.md
```

3. Under `**Dependencies:** Phase 107.` add a `**Canonical refs:**` line:

```markdown
**Canonical refs:** 108-CONTEXT.md, 108-UI-SPEC.md, 108-RESEARCH.md, 108-SPIKES.md, 108-STATE-MACHINE.md
```

Do not modify other phases. Do not change the phase 108 Goal or Requirements lines — they are already correct.
  </action>
  <verify>
    <automated>grep -q "108-STATE-MACHINE.md" .planning/ROADMAP.md && grep -c "108-0.-PLAN.md" .planning/ROADMAP.md | grep -vE "^[0-6]$"</automated>
  </verify>
  <done>
- ROADMAP.md phase 108 block lists 7 plans
- Canonical refs line includes 108-STATE-MACHINE.md
- No other phase blocks modified
  </done>
</task>

</tasks>

<verification>
- `test -f .planning/phases/108-house-selection-active-state/108-STATE-MACHINE.md`
- `grep -q "mermaid" 108-STATE-MACHINE.md`
- `grep -q "Reconciliation (deferred to phase 110)" 108-STATE-MACHINE.md`
- `grep -q "108-STATE-MACHINE.md" .planning/ROADMAP.md`
</verification>

<success_criteria>
- D-08 satisfied: audit doc exists with Mermaid diagram + transition table (no refactor)
- D-09 satisfied: all 5 entry points documented
- D-10 satisfied: reconciliation placeholder with phase 110 questions
- ROADMAP.md reflects the phase 108 plan set
</success_criteria>

<output>
Create `.planning/phases/108-house-selection-active-state/108-04-SUMMARY.md`
noting the file sizes, rendered Mermaid check (eyeball the syntax), and
commit hashes.
</output>
</content>
</invoke>