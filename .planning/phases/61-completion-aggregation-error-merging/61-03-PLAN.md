---
phase: 61-completion-aggregation-error-merging
plan: 03
type: execute
wave: 3
depends_on: ["61-02"]
files_modified:
  - tests/integration/test_import_parallel_processing.py
autonomous: true
requirements: [PROG-01, PROG-02, PROG-03, PROG-05]
must_haves:
  truths:
    - "Concurrent chunk completion leaves the parent import finalized exactly once."
    - "The parent import totals equal the sum of durable chunk rows after the last chunk finishes."
    - "A partial-success chunk set exposes one merged parent error artifact and the COMPLETED_WITH_ERRORS terminal status."
  artifacts:
    - path: "tests/integration/test_import_parallel_processing.py"
      provides: "integration coverage for concurrent chunk completion, fan-in totals, and merged error artifact behavior"
  key_links:
    - from: "tests/integration/test_import_parallel_processing.py"
      to: "app/tasks/import_task.py"
      via: "two chunk workers finishing against one parent import"
      pattern: "process_import_chunk"
    - from: "tests/integration/test_import_parallel_processing.py"
      to: "app/services/import_service.py"
      via: "assert locked finalizer aggregate/merge behavior through the task entrypoint"
      pattern: "COMPLETED_WITH_ERRORS|error_report_key|imported_rows"
---

<objective>
Prove the new fan-in path works under concurrent chunk completion rather than only under unit-level mocks.

Purpose: Close the Phase 61 verification loop with integration evidence that the last-chunk race ends in one parent finalization, one merged artifact, and aggregate counters derived from chunk rows.
Output: Extended `test_import_parallel_processing.py` coverage for mixed-outcome chunk completion and exactly-once parent finalization.
</objective>

<execution_context>
@/home/kwhatcher/.codex/get-shit-done/workflows/execute-plan.md
@/home/kwhatcher/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/61-completion-aggregation-error-merging/61-CONTEXT.md
@.planning/phases/61-completion-aggregation-error-merging/61-RESEARCH.md
@.planning/phases/61-completion-aggregation-error-merging/61-VALIDATION.md
@.planning/phases/60-parent-split-parallel-processing/60-03-SUMMARY.md
@.planning/phases/61-completion-aggregation-error-merging/61-02-SUMMARY.md
@tests/integration/test_import_parallel_processing.py

<interfaces>
From tests/integration/test_import_parallel_processing.py:
```python
def _make_parent_job(import_job_id: str, campaign_id: str) -> MagicMock: ...
def _make_chunk(import_job_id: str, campaign_id: str, row_start: int, row_end: int): ...
```

From app/tasks/import_task.py after Plan 02:
```python
await service.maybe_finalize_chunked_import(
    session=session,
    storage=storage,
    job=job,
    campaign_id=campaign_id,
)
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend the integration harness for exactly-once parent finalization</name>
  <files>tests/integration/test_import_parallel_processing.py</files>
  <read_first>
    - tests/integration/test_import_parallel_processing.py
    - app/tasks/import_task.py
    - app/services/import_service.py
    - app/models/import_job.py
  </read_first>
  <acceptance_criteria>
    - `rg -n "COMPLETED_WITH_ERRORS|maybe_finalize_chunked_import|error_report_key" tests/integration/test_import_parallel_processing.py` finds integration assertions for parent partial-success status, merged artifact, and finalizer invocation.
    - `uv run pytest tests/integration/test_import_parallel_processing.py -x` fails or is missing before the new assertions are implemented.
  </acceptance_criteria>
  <behavior>
    - Test 1: two chunk workers targeting the same parent can both reach terminal state, but the parent finalizer runs effectively once and leaves one terminal parent outcome.
    - Test 2: a `COMPLETED` chunk plus a `FAILED` chunk produces parent `COMPLETED_WITH_ERRORS`, aggregated `imported_rows`/`skipped_rows`/`phones_created`, and one merged `error_report_key`.
    - Test 3: two successful chunks produce parent `COMPLETED` with summed counters and no erroneous failure summary.
  </behavior>
  <action>Expand `tests/integration/test_import_parallel_processing.py` beyond the Phase 60 session-shape test. Reuse the existing parent/chunk factory helpers, but add explicit per-chunk `imported_rows`, `skipped_rows`, `phones_created`, and `error_report_key` values, then patch the service seam so `process_import_chunk()` exercises the Plan 02 finalizer call. Model two concrete scenarios: `(chunk_a=COMPLETED, chunk_b=FAILED, merged key='imports/.../merged-errors.csv')` and `(chunk_a=COMPLETED, chunk_b=COMPLETED)`. Assert the parent counters equal the sum of chunk rows, the mixed-outcome scenario ends in `ImportStatus.COMPLETED_WITH_ERRORS`, and only one parent `error_report_key` survives as the merged artifact contract from D-09 through D-13.</action>
  <verify>
    <automated>uv run pytest tests/integration/test_import_parallel_processing.py -x</automated>
  </verify>
  <done>The integration harness proves the fan-in path behaves correctly when multiple chunk workers finish against the same parent import.</done>
</task>

</tasks>

<verification>
Run `uv run pytest tests/integration/test_import_parallel_processing.py -x`.
Confirm the mixed-outcome integration case ends with one parent terminal status, one merged parent error artifact, and aggregate totals that match the chunk fixtures.
</verification>

<success_criteria>
Phase 61 is backed by integration evidence that concurrent chunk completion produces exactly one authoritative parent result with correct aggregate counters and partial-success status handling.
</success_criteria>

<output>
After completion, create `.planning/phases/61-completion-aggregation-error-merging/61-03-SUMMARY.md`
</output>
