---
phase: 61-completion-aggregation-error-merging
plan: 01
type: execute
wave: 1
depends_on: ["60-03"]
files_modified:
  - alembic/versions/023_phase61_chunk_aggregation_contracts.py
  - app/models/import_job.py
  - app/services/import_service.py
  - tests/unit/test_import_service.py
autonomous: true
requirements: [PROG-01, PROG-05]
must_haves:
  truths:
    - "Chunk rows persist every counter needed to recompute the parent import totals, including phones_created."
    - "The database accepts COMPLETED_WITH_ERRORS as a parent import status without truncation or write failure."
    - "Chunk-scoped progress writes still avoid direct parent aggregation while leaving parent totals derivable from SQL SUM."
  artifacts:
    - path: "alembic/versions/023_phase61_chunk_aggregation_contracts.py"
      provides: "schema change adding import_chunks.phones_created and widening import_jobs.status for COMPLETED_WITH_ERRORS"
    - path: "app/models/import_job.py"
      provides: "ImportStatus.COMPLETED_WITH_ERRORS plus ImportChunk.phones_created ORM fields"
    - path: "app/services/import_service.py"
      provides: "chunk progress persistence that writes phones_created onto ImportChunk rows"
    - path: "tests/unit/test_import_service.py"
      provides: "coverage for chunk phones_created persistence and the new terminal status contract"
  key_links:
    - from: "app/services/import_service.py"
      to: "app/models/import_job.py"
      via: "chunk progress target writes phones_created on ImportChunk instead of parent ImportJob"
      pattern: "progress_target\\.phones_created|ImportStatus\\.COMPLETED_WITH_ERRORS"
    - from: "alembic/versions/023_phase61_chunk_aggregation_contracts.py"
      to: "app/models/import_job.py"
      via: "status column width and chunk counter schema stay aligned with ORM enums/fields"
      pattern: "phones_created|status"
---

<objective>
Create the persistent contract Phase 61 depends on before any finalization logic is added.

Purpose: Satisfy D-05 and PROG-05 by making chunk rows the durable source of truth for `phones_created` and by widening `import_jobs.status` so `COMPLETED_WITH_ERRORS` can be stored safely.
Output: A migration, updated import models, chunk progress persistence for `phones_created`, and unit coverage locking the contract in place.
</objective>

<execution_context>
@/home/kwhatcher/.codex/get-shit-done/workflows/execute-plan.md
@/home/kwhatcher/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/61-completion-aggregation-error-merging/61-CONTEXT.md
@.planning/phases/61-completion-aggregation-error-merging/61-RESEARCH.md
@.planning/phases/61-completion-aggregation-error-merging/61-VALIDATION.md
@.planning/phases/60-parent-split-parallel-processing/60-03-SUMMARY.md
@alembic/versions/022_import_chunks.py
@app/models/import_job.py
@app/services/import_service.py
@tests/unit/test_import_service.py

<interfaces>
From app/models/import_job.py:
```python
class ImportStatus(enum.StrEnum):
    PENDING = "pending"
    UPLOADED = "uploaded"
    QUEUED = "queued"
    PROCESSING = "processing"
    CANCELLING = "cancelling"
    CANCELLED = "cancelled"
    COMPLETED = "completed"
    FAILED = "failed"
```

From app/models/import_job.py:
```python
class ImportChunk(Base):
    imported_rows: Mapped[int | None] = mapped_column(default=0)
    skipped_rows: Mapped[int | None] = mapped_column(default=0)
    last_committed_row: Mapped[int | None] = mapped_column(default=0)
    error_report_key: Mapped[str | None] = mapped_column(String(500))
```

From app/services/import_service.py:
```python
async def process_import_range(
    self,
    *,
    job: ImportJob,
    import_job_id: str,
    session: AsyncSession,
    storage: StorageService,
    campaign_id: str,
    row_start: int,
    row_end: int | None,
    chunk: ImportChunk | None = None,
) -> tuple[int, int, int]: ...
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add unit coverage for the Phase 61 schema contract</name>
  <files>tests/unit/test_import_service.py</files>
  <read_first>
    - tests/unit/test_import_service.py
    - app/models/import_job.py
    - app/services/import_service.py
    - alembic/versions/022_import_chunks.py
  </read_first>
  <acceptance_criteria>
    - `rg -n "COMPLETED_WITH_ERRORS|phones_created" tests/unit/test_import_service.py` returns assertions covering both the new parent status and chunk-local `phones_created` persistence.
    - `uv run pytest tests/unit/test_import_service.py -x` fails or is missing before the production changes are applied.
  </acceptance_criteria>
  <behavior>
    - Test 1: when `process_import_range(..., chunk=chunk)` processes phone-bearing rows, it writes `chunk.phones_created` and does not mutate `job.phones_created` directly, per D-05 and D-14.
    - Test 2: the chunk progress sink starts `phones_created` at `0` for a chunk target and preserves the parent-only serial contract when `chunk=None`.
    - Test 3: the import model exposes `ImportStatus.COMPLETED_WITH_ERRORS` so later finalization logic can target the exact partial-success status from D-01.
  </behavior>
  <action>Extend `tests/unit/test_import_service.py` with focused RED coverage for the missing schema contract. Use the existing `process_import_range()` chunk-progress tests as the base, add explicit assertions that `chunk.phones_created` receives the batch phone count while `job.phones_created` remains unchanged for chunk processing, and add one import-model assertion that `ImportStatus.COMPLETED_WITH_ERRORS.value == "completed_with_errors"` per D-01/D-05. Do not add parent finalization logic or error merging in this plan.</action>
  <verify>
    <automated>uv run pytest tests/unit/test_import_service.py -x</automated>
  </verify>
  <done>Tests define the missing persistence contract for chunk `phones_created` and the new partial-success parent status before implementation begins.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement the Phase 61 aggregation schema prerequisites</name>
  <files>alembic/versions/023_phase61_chunk_aggregation_contracts.py, app/models/import_job.py, app/services/import_service.py, tests/unit/test_import_service.py</files>
  <read_first>
    - alembic/versions/022_import_chunks.py
    - alembic/versions/002_voter_data_models.py
    - app/models/import_job.py
    - app/services/import_service.py
    - tests/unit/test_import_service.py
  </read_first>
  <acceptance_criteria>
    - `rg -n "COMPLETED_WITH_ERRORS" app/models/import_job.py alembic/versions/023_phase61_chunk_aggregation_contracts.py` finds the enum value and migration widening logic.
    - `rg -n "phones_created" app/models/import_job.py app/services/import_service.py alembic/versions/023_phase61_chunk_aggregation_contracts.py` finds the new chunk column plus chunk-target persistence.
    - `uv run pytest tests/unit/test_import_service.py -x` passes.
  </acceptance_criteria>
  <action>Create `alembic/versions/023_phase61_chunk_aggregation_contracts.py` that adds nullable `phones_created` to `import_chunks` and widens `import_jobs.status` from `VARCHAR(20)` to a length that safely stores `"completed_with_errors"` per the research note and PROG-05. Update `app/models/import_job.py` to add `ImportStatus.COMPLETED_WITH_ERRORS` and `ImportChunk.phones_created`. In `app/services/import_service.py`, extend the chunk progress-target branch inside `process_import_range()` and its counters so chunk execution persists `phones_created` on `ImportChunk`, while serial imports continue writing the parent `ImportJob` directly. Do not aggregate parent totals yet, do not change chunk terminal-state finalization ownership, and do not implement cancellation or crash-resume behavior from D-15.</action>
  <verify>
    <automated>uv run pytest tests/unit/test_import_service.py -x</automated>
  </verify>
  <done>The database and ORM can represent `COMPLETED_WITH_ERRORS`, chunk rows persist `phones_created`, and the service writes the new chunk counter without regressing the serial import path.</done>
</task>

</tasks>

<verification>
Run `uv run pytest tests/unit/test_import_service.py -x`.
Confirm the new migration is the only schema change in this plan and that `process_import_range()` now leaves chunk rows with `phones_created` populated for later SQL aggregation.
</verification>

<success_criteria>
Phase 61 has the persistence contract it needs: chunk rows carry every counter required for SQL fan-in, and the database can store the partial-success terminal status without truncation.
</success_criteria>

<output>
After completion, create `.planning/phases/61-completion-aggregation-error-merging/61-01-SUMMARY.md`
</output>
