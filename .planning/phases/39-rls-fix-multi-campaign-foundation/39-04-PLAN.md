---
phase: 39-rls-fix-multi-campaign-foundation
plan: 04
type: execute
wave: 4
depends_on: [39-02, 39-03]
files_modified:
  - web/src/routes/__root.tsx
autonomous: false
requirements: [DATA-06]

must_haves:
  truths:
    - "Settings button is hidden when campaignId is not in the URL path"
    - "Settings button links correctly when campaignId IS in the URL path"
    - "No runtime errors when navigating between campaign list and campaign pages"
  artifacts:
    - path: "web/src/routes/__root.tsx"
      provides: "Defensive guard on settings button visibility"
      contains: "campaignId"
  key_links:
    - from: "web/src/routes/__root.tsx"
      to: "/campaigns/${campaignId}/settings"
      via: "Link with campaignId guard"
      pattern: "campaignId.*settings"
---

<objective>
Fix the settings button navigation and add a defensive guard when campaignId is unavailable.

Purpose: The settings button in the sidebar footer links to `/campaigns/${campaignId}/settings`, but `campaignId` is extracted from the URL via regex. When navigating from the campaign list page (no campaignId in URL), the link resolves to `/campaigns/undefined/settings`, causing a 404. Per D-13: fix root cause AND add defensive UI guard.

Output: Settings button hidden when campaignId unavailable, correctly linked when available.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/39-rls-fix-multi-campaign-foundation/39-CONTEXT.md

@web/src/routes/__root.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add campaignId guard to settings button in sidebar</name>
  <files>web/src/routes/__root.tsx</files>
  <read_first>
    - web/src/routes/__root.tsx (full file — understand AppSidebar component, campaignId extraction at line 56-57, settings link at line 131)
  </read_first>
  <action>
    The settings button issue is already partially guarded by the early return at line 59:
    ```tsx
    if (!campaignId || campaignId === "new") return null
    ```
    This means the entire sidebar (including the settings button) does not render when `campaignId` is not in the URL. So the settings button with `undefined` campaignId should already be unreachable.

    **Investigation needed:** The bug may be that `campaignId` is sometimes extracted but invalid (e.g., a non-UUID string), or that the regex match succeeds but the campaignId doesn't correspond to a valid campaign. Verify the actual bug behavior.

    **Regardless, add an explicit defensive guard per D-13:**

    1. In the `AppSidebar` component, the settings `Link` at line 131 currently reads:
    ```tsx
    <Link to={`/campaigns/${campaignId}/settings` as string}>
    ```

    2. The `RequireRole` wrapper already gates visibility to admins. Add an additional `campaignId` check. Since `campaignId` is already checked at line 59 (null/new causes full sidebar to not render), this is truly defense-in-depth. But to be explicit per D-13, wrap the settings footer in its own guard:

    Replace the SidebarFooter section (lines 123-138) with:
    ```tsx
    {campaignId && campaignId !== "new" && (
      <RequireRole minimum="admin">
        <SidebarFooter>
          <SidebarMenu>
            <SidebarMenuItem>
              <SidebarMenuButton
                asChild
                isActive={location.pathname.includes("/settings")}
              >
                <Link to={`/campaigns/${campaignId}/settings`}>
                  <Settings />
                  <span>Settings</span>
                </Link>
              </SidebarMenuButton>
            </SidebarMenuItem>
          </SidebarMenu>
        </SidebarFooter>
      </RequireRole>
    )}
    ```

    3. Remove the `as string` type assertion on the Link `to` prop — it's unnecessary and hides type errors.

    4. Verify no TypeScript errors: `cd web && npx tsc --noEmit`
  </action>
  <verify>
    <automated>cd /home/kwhatcher/projects/civicpulse/run-api/web && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - web/src/routes/__root.tsx contains `campaignId && campaignId !== "new"` guard around the settings SidebarFooter section
    - web/src/routes/__root.tsx does NOT contain `as string` on the settings Link to prop
    - `cd web && npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>Settings button has explicit defensive guard — hidden when campaignId is unavailable, correctly linked when present.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify settings button and campaign list in browser</name>
  <files>web/src/routes/__root.tsx</files>
  <action>
    Human verification of all Phase 39 deliverables in the browser. No code changes — this is a manual smoke test of the complete phase output.
    Start services with `docker compose up -d`, run migrations with `docker compose exec api bash -c "alembic upgrade head"`, then verify 4 areas below.
  </action>
  <verify>
    <automated>uv run pytest -x</automated>
  </verify>
  <done>Human confirms: settings button hidden when no campaignId, links correctly when present; campaign list shows all member campaigns; no cross-campaign data leaks; full test suite green.</done>
  <what-built>
    Complete Phase 39 RLS fix and multi-campaign foundation:
    1. Transaction-scoped set_config (DATA-01)
    2. Pool checkout event (DATA-02)
    3. Centralized get_campaign_db dependency replacing 244 inline calls (DATA-03)
    4. Multi-campaign membership fix in ensure_user_synced (DATA-04)
    5. Data migration backfilling CampaignMember records (DATA-05)
    6. Settings button defensive guard (DATA-06)
  </what-built>
  <how-to-verify>
    Start services: `docker compose up -d`

    Run migrations: `docker compose exec api bash -c "alembic upgrade head"`

    1. **Settings button (DATA-06):**
       - Navigate to http://localhost:5173/ (campaign list — no campaignId in URL)
       - Verify: Settings button is NOT visible in sidebar (sidebar may not render at all)
       - Click into a campaign
       - Verify: Settings button IS visible in sidebar footer (admin role required)
       - Click settings button
       - Verify: Navigates to `/campaigns/{uuid}/settings` correctly (not `/campaigns/undefined/settings`)

    2. **Campaign list (DATA-05):**
       - Navigate to campaign list page
       - Verify: All campaigns the user has membership in are displayed
       - If user has multiple campaigns in the same org, verify ALL appear (not just the most recent)

    3. **Cross-campaign isolation (DATA-01, DATA-02, DATA-03):**
       - Open two browser tabs, navigate to two different campaigns
       - In campaign A, view voters list
       - Switch to campaign B, view voters list
       - Verify: Each campaign shows only its own voters (no cross-campaign data)

    4. **Run full test suite:**
       ```bash
       uv run pytest -x
       ```
       Verify: All tests pass
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues found</resume-signal>
</task>

</tasks>

<verification>
- TypeScript compiles without errors
- Settings button hidden when no campaignId in URL path
- Settings button links correctly when campaignId present
- Full test suite passes
</verification>

<success_criteria>
- Settings button navigation works correctly in all scenarios
- Human verification confirms RLS isolation, campaign list, and settings button
</success_criteria>

<output>
After completion, create `.planning/phases/39-rls-fix-multi-campaign-foundation/39-04-SUMMARY.md`
</output>
