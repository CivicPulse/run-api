---
phase: 24-import-pipeline-enhancement
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/services/import_service.py
  - tests/unit/test_import_parsing.py
  - tests/unit/test_field_mapping.py
autonomous: true
requirements: [IMPT-03, IMPT-04, IMPT-07]

must_haves:
  truths:
    - "parse_propensity('77%') returns 77"
    - "parse_propensity('Not Eligible') returns None"
    - "parse_propensity('') returns None"
    - "normalize_phone('(555) 123-4567') returns '5551234567'"
    - "normalize_phone('1-555-123-4567') returns '5551234567' (strips leading 1)"
    - "normalize_phone('123') returns None (too short)"
    - "CANONICAL_FIELDS includes __cell_phone with L2 phone column aliases"
    - "CANONICAL_FIELDS propensity entries include L2 official headers"
    - "L2 official column headers (Voters_CellPhoneFull, CommercialData_EstimatedHHIncomeAmount, etc.) map via suggest_field_mapping"
  artifacts:
    - path: "tests/unit/test_import_parsing.py"
      provides: "Unit tests for parse_propensity, normalize_phone, parse_voting_history"
      min_lines: 80
    - path: "app/services/import_service.py"
      provides: "parse_propensity, normalize_phone, parse_voting_history functions + expanded CANONICAL_FIELDS"
      contains: "def parse_propensity"
  key_links:
    - from: "tests/unit/test_import_parsing.py"
      to: "app/services/import_service.py"
      via: "imports parse_propensity, normalize_phone, parse_voting_history"
      pattern: "from app.services.import_service import"
---

<objective>
Add independently-testable utility functions for propensity parsing, phone normalization, and voting history extraction to import_service.py. Expand CANONICAL_FIELDS with __cell_phone entry and L2 official column header aliases. Write comprehensive unit tests for all parsing functions.

Purpose: These utility functions and expanded aliases are the building blocks for Plan 02's pipeline integration. By making them standalone and tested first, Plan 02 can focus on wiring without debugging parsing logic.
Output: Three tested pure functions (parse_propensity, normalize_phone, parse_voting_history) and CANONICAL_FIELDS expanded with L2 vendor aliases and __cell_phone special field.
</objective>

<execution_context>
@/home/kwhatcher/.claude/get-shit-done/workflows/execute-plan.md
@/home/kwhatcher/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/24-import-pipeline-enhancement/24-CONTEXT.md
@.planning/phases/24-import-pipeline-enhancement/24-RESEARCH.md

<interfaces>
<!-- Key types and contracts from existing codebase. -->

From app/services/import_service.py:
```python
CANONICAL_FIELDS: dict[str, list[str]] = { ... }  # 43 entries currently
_ALIAS_LIST: list[str]  # Built from CANONICAL_FIELDS
_ALIAS_TO_FIELD: dict[str, str]  # Reverse lookup
_VOTER_COLUMNS: set[str]  # 37 column names that map to Voter model columns
def suggest_field_mapping(csv_columns: list[str]) -> dict[str, str | None]: ...
```

From app/services/dnc.py:
```python
PHONE_REGEX = re.compile(r"^\d{10,15}$")  # Existing phone validation pattern
```

From app/models/voter.py:
```python
voting_history: Mapped[list | None] = mapped_column(ARRAY(String), default=list)
propensity_general: Mapped[int | None] = mapped_column(SmallInteger)
propensity_primary: Mapped[int | None] = mapped_column(SmallInteger)
propensity_combined: Mapped[int | None] = mapped_column(SmallInteger)
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add utility functions and expand CANONICAL_FIELDS</name>
  <files>app/services/import_service.py, tests/unit/test_import_parsing.py, tests/unit/test_field_mapping.py</files>
  <behavior>
    - parse_propensity("77%") == 77
    - parse_propensity("42") == 42
    - parse_propensity("0%") == 0
    - parse_propensity("100%") == 100
    - parse_propensity("101%") == None (out of range)
    - parse_propensity("Not Eligible") == None
    - parse_propensity("High") == None
    - parse_propensity("N/A") == None
    - parse_propensity("") == None
    - parse_propensity("  ") == None
    - normalize_phone("(555) 123-4567") == "5551234567"
    - normalize_phone("555-123-4567") == "5551234567"
    - normalize_phone("5551234567") == "5551234567"
    - normalize_phone("1-555-123-4567") == "5551234567" (strip leading US country code)
    - normalize_phone("+1 555 123 4567") == "5551234567"
    - normalize_phone("123") == None (too few digits)
    - normalize_phone("") == None
    - normalize_phone("abc") == None
    - parse_voting_history({"General_2024": "Y", "Primary_2022": "A", "General_2020": "N", "Primary_2020": "E", "Some_Other_Col": "value"}) == ["General_2024", "Primary_2020", "Primary_2022"] (sorted, only Y/A/E, only General_/Primary_ patterns)
    - parse_voting_history({"General_2024": "N", "Primary_2022": "N"}) == [] (no voted entries)
    - parse_voting_history({}) == []
    - parse_voting_history({"General_2024": " y "}) == ["General_2024"] (strip+upper)
    - CANONICAL_FIELDS["__cell_phone"] includes aliases: "cellphone", "cell_phone", "phone", "voters_cellphonefull", "voters_cellphone"
    - __cell_phone is NOT in _VOTER_COLUMNS
    - suggest_field_mapping(["Voters_CellPhoneFull"]) maps to "__cell_phone"
    - suggest_field_mapping(["CommercialData_EstimatedHHIncomeAmount"]) returns None (unmapped L2 commercial field)
    - L2 aliases for propensity fields: "general_turnout_score", "primary_turnout_score"
    - L2 aliases for demographics: "ethnicgroups_ethnicgroup1desc" (already present), "voters_language" (already present)
    - L2 aliases for mailing: "mail_vaddressline1", "mail_vaddressline2", "mail_vcity", "mail_vstate", "mail_vzip"
    - L2 aliases for household: "voters_hhid" (already alias for household_id)
  </behavior>
  <action>
    1. Create `tests/unit/test_import_parsing.py` with three test classes:
       - `TestPropensityParsing`: Tests for `parse_propensity()` covering valid percentages (with/without %), edge values (0, 100), out-of-range (101, -1), non-numeric ("Not Eligible", "High", "N/A"), empty strings, whitespace-only.
       - `TestPhoneNormalization`: Tests for `normalize_phone()` covering formatted US numbers, 10-digit, 11-digit with leading 1, too short, empty, non-numeric.
       - `TestVotingHistoryParsing`: Tests for `parse_voting_history()` covering Y/A/E as voted, N as not voted, only General_YYYY/Primary_YYYY patterns, sorted output, empty dict, whitespace handling.

    2. Add three module-level functions to `app/services/import_service.py` (above the class definition, after the _ALIAS_TO_FIELD construction):

       `parse_propensity(value: str) -> int | None`:
       - Return None if value is falsy or whitespace-only
       - Use `_PROPENSITY_RE = re.compile(r"^(\d+)%?$")` to extract digits
       - Return int if 0 <= score <= 100, else None
       - Add `import re` to the imports

       `normalize_phone(value: str) -> str | None`:
       - Return None if value is falsy
       - Strip all non-digit characters: `re.sub(r"\D", "", value)`
       - If 11 digits and starts with "1", strip leading "1"
       - Return the result if exactly 10 digits, else None

       `parse_voting_history(row: dict[str, str]) -> list[str]`:
       - Use `_VOTING_HISTORY_RE = re.compile(r"^(General|Primary)_(\d{4})$")`
       - Use `_VOTED_VALUES = frozenset({"Y", "A", "E"})`
       - For each key/value in row, if key matches pattern and value.strip().upper() in _VOTED_VALUES, append key to history
       - Return sorted history list

    3. Add `__cell_phone` to CANONICAL_FIELDS with aliases:
       ```python
       "__cell_phone": [
           "__cell_phone",
           "cellphone",
           "cell_phone",
           "phone",
           "cell",
           # L2 official headers
           "voters_cellphonefull",
           "voters_cellphone",
       ],
       ```
       NOTE: __cell_phone must NOT be added to _VOTER_COLUMNS (it routes to VoterPhone, not Voter row).

    4. Expand existing CANONICAL_FIELDS entries with additional L2 official header aliases:
       - `propensity_general`: add "general_turnout_score"
       - `propensity_primary`: add "primary_turnout_score"
       - `propensity_combined`: add "combined_turnout_score", "overall_turnout_score"
       - `mailing_line1`: add "mail_vaddressline1"
       - `mailing_line2`: add "mail_vaddressline2"
       - `mailing_city`: add "mail_vcity"
       - `mailing_state`: add "mail_vstate"
       - `mailing_zip`: add "mail_vzip", "mail_vzipcode"
       - `mailing_zip4`: add "mail_vzip4"
       - `mailing_country`: add "mail_vcountry"
       - `marital_status`: add "commercialdata_maritalstatus"
       - `cell_phone_confidence`: add "cellphoneconfidence"
       - `household_party_registration`: add "voters_hhpartyregistration"
       - `household_size`: add "voters_hhsize"
       - `family_id`: add "voters_familyid"
       - `registration_zip4`: add "voters_zip4"
       - `spoken_language`: keep existing aliases (voters_language already present)
       - `military_status`: add "commercialdata_militaryactive"
       - `party_change_indicator`: add "voters_partychangeindicator"
       Add vendor-grouping comments (e.g., `# L2 official headers`) above L2-specific aliases.

    5. Add tests in `tests/unit/test_field_mapping.py`:
       - `test_l2_expanded_aliases`: Test that suggest_field_mapping maps L2 official phone/propensity/mailing headers correctly
       - `test_cell_phone_maps_to_special_field`: Test that "Voters_CellPhoneFull" maps to "__cell_phone"
       - `test_cell_phone_not_in_voter_columns`: Assert __cell_phone not in _VOTER_COLUMNS

    6. Run tests to verify all pass.
  </action>
  <verify>
    <automated>cd /home/kwhatcher/projects/run-api && uv run pytest tests/unit/test_import_parsing.py tests/unit/test_field_mapping.py -x -q</automated>
  </verify>
  <done>
    - parse_propensity, normalize_phone, parse_voting_history functions exist and all unit tests pass
    - CANONICAL_FIELDS includes __cell_phone and expanded L2 aliases
    - __cell_phone is NOT in _VOTER_COLUMNS
    - suggest_field_mapping correctly maps L2 official column headers to canonical fields
  </done>
</task>

</tasks>

<verification>
```bash
cd /home/kwhatcher/projects/run-api && uv run pytest tests/unit/test_import_parsing.py tests/unit/test_field_mapping.py -x -q
```
</verification>

<success_criteria>
- Three pure functions (parse_propensity, normalize_phone, parse_voting_history) exist in import_service.py with comprehensive unit tests
- CANONICAL_FIELDS expanded with __cell_phone and L2 official header aliases
- All existing tests still pass: `uv run pytest tests/unit/ -x -q`
</success_criteria>

<output>
After completion, create `.planning/phases/24-import-pipeline-enhancement/24-01-SUMMARY.md`
</output>
