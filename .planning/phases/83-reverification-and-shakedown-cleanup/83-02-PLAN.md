# 83-02 Plan

## Goal

Remove or intentionally archive the temporary production shakedown residue.

## Tasks

1. Inventory the temporary campaigns, voters, volunteers, walk-list entries, call-list entries, import jobs, tokens, and helper scripts created during the shakedown.
2. Delete or archive the production-side residue once the user explicitly approves cleanup.
3. Record what was removed, what was archived, and any residue intentionally retained.

## Verification

- Production-only: cleanup commands succeed without touching non-shakedown tenant data, and the closeout notes reflect the final disposition of every temporary artifact class.
