---
phase: 77-quality-accessibility-test-coverage
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - web/public/leaflet/marker-icon.png
  - web/public/leaflet/marker-icon-2x.png
  - web/public/leaflet/marker-shadow.png
  - web/src/components/field/CanvassingMap.tsx
  - web/src/components/canvassing/DoorKnockDialog.tsx
  - web/src/components/canvassing/WalkListGenerateDialog.tsx
  - web/src/components/field/InlineSurvey.tsx
autonomous: true
requirements:
  - QUAL-01
  - QUAL-02
must_haves:
  truths:
    - "Leaflet marker icons are served from the app's own /leaflet/ path, not unpkg.com"
    - "DoorKnockDialog 'Result' select has an explicit htmlFor/id label association"
    - "WalkListGenerateDialog 'Turf' select has an explicit htmlFor/id label association"
    - "InlineSurvey multiple_choice radio items have explicit htmlFor/id label associations"
  artifacts:
    - path: "web/public/leaflet/marker-icon.png"
      provides: "Self-hosted Leaflet marker (1x)"
    - path: "web/public/leaflet/marker-icon-2x.png"
      provides: "Self-hosted Leaflet marker (retina)"
    - path: "web/public/leaflet/marker-shadow.png"
      provides: "Self-hosted Leaflet marker shadow"
  key_links:
    - from: "web/src/components/field/CanvassingMap.tsx"
      to: "web/public/leaflet/*.png"
      via: "L.Icon iconUrl='/leaflet/marker-icon.png'"
      pattern: "/leaflet/marker-icon"
---

<objective>
Close QUAL-01 (Leaflet self-host) and QUAL-02 (A11y label associations) — both are small, surgical frontend fixes that pair naturally in a single plan.

Purpose: Field users lose map markers when offline/CDN unreachable; screen reader users hear unlabeled form fields in the 3 named dialogs.
Output: Self-hosted leaflet assets + 3 dialogs with explicit label associations.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/77-quality-accessibility-test-coverage/77-CONTEXT.md

@web/src/components/field/CanvassingMap.tsx
@web/src/components/canvassing/DoorKnockDialog.tsx
@web/src/components/canvassing/WalkListGenerateDialog.tsx
@web/src/components/field/InlineSurvey.tsx

<interfaces>
<!-- Radix Select needs explicit id on SelectTrigger and htmlFor on Label -->
<!-- React's useId() generates SSR-stable unique IDs -->

From react:
```ts
export function useId(): string;
```

CanvassingMap.tsx (current, lines 18-47) defines three `L.Icon` objects (volunteerIcon, activeHouseholdIcon, householdIcon), each referencing `https://unpkg.com/leaflet@1.9.4/dist/images/marker-{icon,icon-2x,shadow}.png`. Replace ALL nine URL references with `/leaflet/marker-{icon,icon-2x,shadow}.png` (9 lines total: 19-21, 29-31, 40-42).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Self-host Leaflet marker assets and switch CanvassingMap to absolute paths</name>
  <files>
    web/public/leaflet/marker-icon.png,
    web/public/leaflet/marker-icon-2x.png,
    web/public/leaflet/marker-shadow.png,
    web/src/components/field/CanvassingMap.tsx
  </files>
  <action>
    Per locked decision D: "Leaflet: web/public/leaflet/ + absolute URL" (QUAL-01).

    Step 1: Copy the three Leaflet images from the installed npm package into web/public/leaflet/. The leaflet package is already a dependency, so images exist at `web/node_modules/leaflet/dist/images/`:
      mkdir -p web/public/leaflet
      cp web/node_modules/leaflet/dist/images/marker-icon.png web/public/leaflet/
      cp web/node_modules/leaflet/dist/images/marker-icon-2x.png web/public/leaflet/
      cp web/node_modules/leaflet/dist/images/marker-shadow.png web/public/leaflet/

    Step 2: Edit web/src/components/field/CanvassingMap.tsx and replace ALL 9 unpkg references (lines 19-21, 29-31, 40-42) with absolute same-origin paths:
      - "https://unpkg.com/leaflet@1.9.4/dist/images/marker-icon.png" -> "/leaflet/marker-icon.png"
      - "https://unpkg.com/leaflet@1.9.4/dist/images/marker-icon-2x.png" -> "/leaflet/marker-icon-2x.png"
      - "https://unpkg.com/leaflet@1.9.4/dist/images/marker-shadow.png" -> "/leaflet/marker-shadow.png"

    Use absolute URLs (leading slash) so assets resolve the same way regardless of the current route depth. No unpkg fallback — field users must work fully offline.
  </action>
  <verify>
    <automated>cd web &amp;&amp; test -f public/leaflet/marker-icon.png &amp;&amp; test -f public/leaflet/marker-icon-2x.png &amp;&amp; test -f public/leaflet/marker-shadow.png &amp;&amp; ! grep -q "unpkg.com" src/components/field/CanvassingMap.tsx &amp;&amp; grep -c "/leaflet/marker" src/components/field/CanvassingMap.tsx | grep -q "^9$"</automated>
  </verify>
  <done>Three PNGs committed under web/public/leaflet/. CanvassingMap.tsx contains zero "unpkg.com" references and exactly 9 "/leaflet/marker" references. Existing CanvassingMap.test.tsx still passes.</done>
</task>

<task type="auto">
  <name>Task 2: Add explicit htmlFor/id label associations to 3 dialogs</name>
  <files>
    web/src/components/canvassing/DoorKnockDialog.tsx,
    web/src/components/canvassing/WalkListGenerateDialog.tsx,
    web/src/components/field/InlineSurvey.tsx
  </files>
  <action>
    Per locked decision: "A11y: useId() + explicit htmlFor/id (3 dialogs only)" (QUAL-02). Scope is strictly these 3 files — do NOT touch other dialogs.

    DoorKnockDialog.tsx (~line 98-115): The "Result" field has `<Label>Result</Label>` followed by a Radix `<Select>` with `<SelectTrigger>` that has no id. Add `const resultId = useId()` (import useId from "react"), change to `<Label htmlFor={resultId}>Result</Label>` and `<SelectTrigger id={resultId}>`.

    WalkListGenerateDialog.tsx (~line 87-101): The "Turf" field has `<Label>Turf</Label>` + Radix `<Select>` + `<SelectTrigger>` with no id. Add `const turfId = useId()`, change to `<Label htmlFor={turfId}>Turf</Label>` and `<SelectTrigger id={turfId}>`.

    InlineSurvey.tsx (~line 232-253): The multiple_choice branch wraps `<RadioGroupItem>` inside `<Label>`, which does NOT work for Radix `role="radio"` buttons (H33 from codebase review). Replace with explicit htmlFor/id pairing per SurveyWidget convention: generate a unique id per choice inside the map (e.g. use question.id + choice as the id, or call useId() at the component top and concat with an index), and render:
      <div className="min-h-11 flex items-center gap-2">
        <RadioGroupItem value={choice} id={choiceId} />
        <Label htmlFor={choiceId} className="cursor-pointer flex-1">{choice}</Label>
      </div>
    Keep the 44px touch target (min-h-11) and cursor-pointer on the label so clicking text still selects.

    Do NOT add any other a11y changes, ARIA attributes, or refactors — scope is strictly these 3 fixes.
  </action>
  <verify>
    <automated>cd web &amp;&amp; npx tsc --noEmit &amp;&amp; grep -q "htmlFor={resultId}" src/components/canvassing/DoorKnockDialog.tsx &amp;&amp; grep -q "htmlFor={turfId}" src/components/canvassing/WalkListGenerateDialog.tsx &amp;&amp; grep -q "htmlFor=" src/components/field/InlineSurvey.tsx</automated>
  </verify>
  <done>All 3 dialogs have explicit htmlFor/id associations using useId(). `npx tsc --noEmit` passes from web/ root. No other files modified.</done>
</task>

</tasks>

<verification>
- `cd web && npx tsc --noEmit` passes.
- `cd web && npm test -- --run` passes (existing CanvassingMap.test.tsx must still pass with new paths).
- Grep confirms zero unpkg references in CanvassingMap.tsx.
- Grep confirms htmlFor attribute present in all 3 dialog files.
</verification>

<success_criteria>
- Leaflet markers are loaded from `/leaflet/marker-*.png` only.
- Three dialogs have WCAG-compliant label/control associations via useId() + htmlFor/id.
- No changes outside the 4 source files + 3 new PNGs.
</success_criteria>

<output>
After completion, create `.planning/phases/77-quality-accessibility-test-coverage/77-01-SUMMARY.md`
</output>
