---
phase: 62-resilience-cancellation
plan: 02
type: execute
wave: 2
depends_on: ["62-01"]
files_modified:
  - app/tasks/import_task.py
  - app/services/import_service.py
  - tests/unit/test_import_service.py
  - tests/integration/test_import_parallel_processing.py
autonomous: true
requirements: [RESL-01, RESL-03]
must_haves:
  truths:
    - "Chunk retries resume from the same durable chunk row instead of recreating chunk state."
    - "Chunk-local last_committed_row checkpoints survive crashes and only new rows are processed on retry."
    - "Successful sibling chunks remain valid even when one chunk crashes and later resumes."
  artifacts:
    - path: "app/tasks/import_task.py"
      provides: "retry-safe chunk lifecycle that cooperates with resume semantics"
    - path: "app/services/import_service.py"
      provides: "shared ranged import path that preserves chunk-local checkpoints on retry"
    - path: "tests/unit/test_import_service.py"
      provides: "focused resume assertions for chunk targets"
    - path: "tests/integration/test_import_parallel_processing.py"
      provides: "integration evidence for mixed success/crash-resume chunk outcomes"
---

<objective>
Harden chunk retry behavior so worker crashes resume from chunk-local durable progress without duplicating committed work.

Purpose: Fulfill RESL-03 while reinforcing RESL-01 by keeping retries bound to the same `ImportChunk` row and preserving successful sibling chunk results.
Output: Retry-safe chunk task semantics and resume-focused unit/integration coverage.
</objective>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add chunk-resume coverage for retry paths</name>
  <files>tests/unit/test_import_service.py, tests/integration/test_import_parallel_processing.py</files>
  <action>Add tests showing that a chunk with `last_committed_row > 0` resumes after its prior durable checkpoint and does not reprocess already committed rows. Include one focused integration-style scenario where one chunk completes and another resumes after an injected failure without invalidating the parent partial-result model.</action>
  <verify>
    <automated>uv run pytest tests/unit/test_import_service.py tests/integration/test_import_parallel_processing.py -x</automated>
  </verify>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement retry-safe chunk lifecycle handling</name>
  <files>app/tasks/import_task.py, app/services/import_service.py, tests/unit/test_import_service.py, tests/integration/test_import_parallel_processing.py</files>
  <action>Preserve retry behavior around existing chunk rows by ensuring resumable chunks are allowed back into `PROCESSING` without clearing their durable counters, and rely on `last_committed_row` inside `process_import_range()` to skip already committed rows. Keep retries on the same chunk record so parent aggregation remains deterministic and sibling chunk results stay intact.</action>
  <verify>
    <automated>uv run pytest tests/unit/test_import_service.py tests/integration/test_import_parallel_processing.py -x</automated>
  </verify>
</task>

</tasks>

<output>
After completion, create `.planning/phases/62-resilience-cancellation/62-02-SUMMARY.md`
</output>
