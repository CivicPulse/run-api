---
phase: 47-integration-consistency-documentation-cleanup
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - app/api/v1/call_lists.py
  - app/api/v1/campaigns.py
  - app/api/v1/config.py
  - app/api/v1/dnc.py
  - app/api/v1/field.py
  - app/api/v1/imports.py
  - app/api/v1/invites.py
  - app/api/v1/members.py
  - app/api/v1/org.py
  - app/api/v1/turfs.py
  - app/api/v1/users.py
  - app/api/v1/voter_contacts.py
  - app/api/v1/voter_interactions.py
  - app/api/v1/voter_lists.py
  - app/api/v1/voters.py
  - app/api/v1/voter_tags.py
  - app/api/v1/walk_lists.py
autonomous: true
requirements: [OBS-03, OBS-04]
must_haves:
  truths:
    - "Every endpoint across all 17 remaining route files has a @limiter.limit decorator"
    - "Bulk/import endpoints use stricter 5/minute rate limit per D-02"
    - "Auth-sensitive invite creation uses 10/minute rate limit per D-01"
    - "All authenticated endpoints use get_user_or_ip_key for per-user rate limiting"
    - "join.py remains unchanged (already rate-limited per D-04)"
  artifacts:
    - path: "app/api/v1/call_lists.py"
      provides: "Rate-limited call list endpoints"
      contains: "@limiter.limit"
    - path: "app/api/v1/imports.py"
      provides: "Rate-limited import endpoints with bulk tier"
      contains: "5/minute"
    - path: "app/api/v1/dnc.py"
      provides: "Rate-limited DNC endpoints with bulk tier"
      contains: "5/minute"
  key_links:
    - from: "app/api/v1/imports.py"
      to: "app/core/rate_limit.py"
      via: "import limiter, get_user_or_ip_key"
      pattern: "from app\\.core\\.rate_limit import limiter, get_user_or_ip_key"
---

<objective>
Apply rate limiting decorators to all endpoints in the remaining 17 route files (94 endpoints), completing INT-02 closure. Includes special bulk/auth-sensitive tiers per D-01/D-02.

Purpose: Complete rate limiting coverage across all API endpoints, with appropriate tiers for expensive and auth-sensitive operations.
Output: 17 route files updated with @limiter.limit decorators and request: Request parameters.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/47-integration-consistency-documentation-cleanup/47-CONTEXT.md
@.planning/phases/47-integration-consistency-documentation-cleanup/47-RESEARCH.md

<interfaces>
<!-- From app/core/rate_limit.py -->
```python
from app.core.rate_limit import limiter, get_user_or_ip_key

# Standard tiers (per D-01):
# GET authenticated:  @limiter.limit("60/minute", key_func=get_user_or_ip_key)
# POST/PATCH/DELETE:  @limiter.limit("30/minute", key_func=get_user_or_ip_key)

# Special tiers (per D-02):
# Bulk/import:        @limiter.limit("5/minute", key_func=get_user_or_ip_key)
# Auth-sensitive:     @limiter.limit("10/minute", key_func=get_user_or_ip_key)
```

<!-- Decorator order (CRITICAL): @router.method() THEN @limiter.limit() THEN async def -->
<!-- request: Request MUST be in function signature (SlowAPI requirement) -->
<!-- join.py already done -- DO NOT MODIFY -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rate limit call_lists, campaigns, config, dnc, field, imports, invites, members, org</name>
  <files>app/api/v1/call_lists.py, app/api/v1/campaigns.py, app/api/v1/config.py, app/api/v1/dnc.py, app/api/v1/field.py, app/api/v1/imports.py, app/api/v1/invites.py, app/api/v1/members.py, app/api/v1/org.py</files>
  <read_first>
    - app/api/v1/call_lists.py (8 endpoints)
    - app/api/v1/campaigns.py (5 endpoints, 2 already have request: Request)
    - app/api/v1/config.py (1 endpoint)
    - app/api/v1/dnc.py (5 endpoints including bulk_import_dnc)
    - app/api/v1/field.py (1 endpoint)
    - app/api/v1/imports.py (7 endpoints, 4 already have request: Request)
    - app/api/v1/invites.py (4 endpoints, 1 already has request: Request)
    - app/api/v1/members.py (4 endpoints, 3 already have request: Request)
    - app/api/v1/org.py (5 endpoints)
    - app/core/rate_limit.py (limiter and get_user_or_ip_key definitions)
  </read_first>
  <action>
    For each of the 9 files, apply these changes:

    **Step 1: Add imports** (skip if already present):
    ```python
    from fastapi import Request  # add to existing fastapi import if not present
    from app.core.rate_limit import limiter, get_user_or_ip_key
    ```

    **Step 2: Add `request: Request` parameter and `@limiter.limit` decorator to every endpoint.**

    **Standard tiers (per D-01, D-03):**
    - GET endpoints: `@limiter.limit("60/minute", key_func=get_user_or_ip_key)`
    - POST/PATCH/DELETE endpoints: `@limiter.limit("30/minute", key_func=get_user_or_ip_key)`

    **Special tiers requiring attention (per D-02):**

    **dnc.py:**
    - `bulk_import_dnc` (POST): `@limiter.limit("5/minute", key_func=get_user_or_ip_key)` -- bulk operation per D-02
    - All other dnc endpoints: standard tiers

    **imports.py:**
    - `initiate_import` (POST): `@limiter.limit("5/minute", key_func=get_user_or_ip_key)` -- bulk CSV import per D-02
    - `confirm_mapping` (POST): `@limiter.limit("5/minute", key_func=get_user_or_ip_key)` -- triggers bulk processing per D-02
    - `detect_columns` (POST): `@limiter.limit("30/minute", key_func=get_user_or_ip_key)` -- standard write (lightweight)
    - All GET endpoints: `@limiter.limit("60/minute", key_func=get_user_or_ip_key)`
    - `delete_import` (DELETE): `@limiter.limit("30/minute", key_func=get_user_or_ip_key)`

    **invites.py:**
    - `create_invite` (POST): `@limiter.limit("10/minute", key_func=get_user_or_ip_key)` -- auth-sensitive per D-01
    - `accept_invite` (POST): `@limiter.limit("10/minute", key_func=get_user_or_ip_key)` -- auth-sensitive per D-01
    - All other invite endpoints: standard tiers

    **campaigns.py, config.py, call_lists.py, field.py, members.py, org.py:** All standard tiers.

    **Note for campaigns.py, imports.py, invites.py, members.py:** Some endpoints already have `request: Request` -- do NOT add duplicates. Just add the `@limiter.limit` decorator.

    **Step 3:** Run `uv run ruff check` and `uv run ruff format` on each file.
  </action>
  <verify>
    <automated>uv run ruff check app/api/v1/call_lists.py app/api/v1/campaigns.py app/api/v1/config.py app/api/v1/dnc.py app/api/v1/field.py app/api/v1/imports.py app/api/v1/invites.py app/api/v1/members.py app/api/v1/org.py && echo "LINT PASS"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "@limiter.limit" app/api/v1/call_lists.py` returns 8
    - `grep -c "@limiter.limit" app/api/v1/campaigns.py` returns 5
    - `grep -c "@limiter.limit" app/api/v1/config.py` returns 1
    - `grep -c "@limiter.limit" app/api/v1/dnc.py` returns 5
    - `grep -c "@limiter.limit" app/api/v1/field.py` returns 1
    - `grep -c "@limiter.limit" app/api/v1/imports.py` returns 7
    - `grep -c "@limiter.limit" app/api/v1/invites.py` returns 4
    - `grep -c "@limiter.limit" app/api/v1/members.py` returns 4
    - `grep -c "@limiter.limit" app/api/v1/org.py` returns 5
    - `grep "5/minute" app/api/v1/dnc.py` matches `bulk_import_dnc` decorator
    - `grep "5/minute" app/api/v1/imports.py` matches `initiate_import` and `confirm_mapping` decorators
    - `grep "10/minute" app/api/v1/invites.py` matches `create_invite` and `accept_invite` decorators
    - `uv run ruff check` passes on all 9 files
  </acceptance_criteria>
  <done>40 endpoints across 9 files have rate limiting with correct standard and special tiers</done>
</task>

<task type="auto">
  <name>Task 2: Rate limit turfs, users, voter_contacts, voter_interactions, voter_lists, voters, voter_tags, walk_lists</name>
  <files>app/api/v1/turfs.py, app/api/v1/users.py, app/api/v1/voter_contacts.py, app/api/v1/voter_interactions.py, app/api/v1/voter_lists.py, app/api/v1/voters.py, app/api/v1/voter_tags.py, app/api/v1/walk_lists.py</files>
  <read_first>
    - app/api/v1/turfs.py (7 endpoints, 0 have request: Request -- NOTE: Plan 01 may have already modified this file for RLS fix)
    - app/api/v1/users.py (3 endpoints)
    - app/api/v1/voter_contacts.py (11 endpoints)
    - app/api/v1/voter_interactions.py (2 endpoints)
    - app/api/v1/voter_lists.py (8 endpoints)
    - app/api/v1/voters.py (7 endpoints)
    - app/api/v1/voter_tags.py (5 endpoints)
    - app/api/v1/walk_lists.py (11 endpoints)
    - app/core/rate_limit.py (limiter and get_user_or_ip_key definitions)
  </read_first>
  <action>
    Same mechanical pattern for all 8 files:

    **Step 1: Add imports** (skip if already present):
    ```python
    from fastapi import Request  # add to existing fastapi import if not present
    from app.core.rate_limit import limiter, get_user_or_ip_key
    ```

    **Step 2: Add `request: Request` parameter and `@limiter.limit` decorator to every endpoint.**

    **All endpoints in these files use standard tiers (per D-01, D-03):**
    - GET endpoints: `@limiter.limit("60/minute", key_func=get_user_or_ip_key)`
    - POST/PATCH/DELETE endpoints: `@limiter.limit("30/minute", key_func=get_user_or_ip_key)`

    No special bulk/auth-sensitive endpoints in these files.

    **turfs.py note:** This file may already be modified by Plan 01 (RLS fix). Read the current state before modifying. The `from app.db.session import get_db` line should already be removed. Just add rate limit imports and decorators.

    **Step 3:** Run `uv run ruff check` and `uv run ruff format` on each file.
  </action>
  <verify>
    <automated>uv run ruff check app/api/v1/turfs.py app/api/v1/users.py app/api/v1/voter_contacts.py app/api/v1/voter_interactions.py app/api/v1/voter_lists.py app/api/v1/voters.py app/api/v1/voter_tags.py app/api/v1/walk_lists.py && echo "LINT PASS"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "@limiter.limit" app/api/v1/turfs.py` returns 7
    - `grep -c "@limiter.limit" app/api/v1/users.py` returns 3
    - `grep -c "@limiter.limit" app/api/v1/voter_contacts.py` returns 11
    - `grep -c "@limiter.limit" app/api/v1/voter_interactions.py` returns 2
    - `grep -c "@limiter.limit" app/api/v1/voter_lists.py` returns 8
    - `grep -c "@limiter.limit" app/api/v1/voters.py` returns 7
    - `grep -c "@limiter.limit" app/api/v1/voter_tags.py` returns 5
    - `grep -c "@limiter.limit" app/api/v1/walk_lists.py` returns 11
    - All decorators include `key_func=get_user_or_ip_key`
    - `uv run ruff check` passes on all 8 files
  </acceptance_criteria>
  <done>54 endpoints across 8 files have rate limiting decorators with correct tiers and per-user key functions</done>
</task>

</tasks>

<verification>
- `uv run ruff check app/api/v1/` passes on all files
- Total `@limiter.limit` across all 17 files = 94
- Bulk endpoints (dnc bulk_import, imports initiate/confirm) use "5/minute"
- Auth-sensitive endpoints (invite create/accept) use "10/minute"
- All other GET use "60/minute", all other writes use "30/minute"
- All authenticated endpoints use `key_func=get_user_or_ip_key`
</verification>

<success_criteria>
All 94 endpoints in the remaining 17 route files are rate-limited with correct tiers. Combined with Plan 02's 73 endpoints and join.py's 2 existing endpoints, all 169 API endpoints are now rate-limited.
</success_criteria>

<output>
After completion, create `.planning/phases/47-integration-consistency-documentation-cleanup/47-03-SUMMARY.md`
</output>
