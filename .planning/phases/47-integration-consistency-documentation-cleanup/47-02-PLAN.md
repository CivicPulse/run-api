---
phase: 47-integration-consistency-documentation-cleanup
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - app/api/v1/dashboard.py
  - app/api/v1/volunteers.py
  - app/api/v1/phone_banks.py
  - app/api/v1/shifts.py
  - app/api/v1/surveys.py
autonomous: true
requirements: [OBS-03, OBS-04]
must_haves:
  truths:
    - "Every endpoint in dashboard.py has a @limiter.limit decorator"
    - "Every endpoint in volunteers.py has a @limiter.limit decorator"
    - "Every endpoint in phone_banks.py has a @limiter.limit decorator"
    - "Every endpoint in shifts.py has a @limiter.limit decorator"
    - "Every endpoint in surveys.py has a @limiter.limit decorator"
    - "All authenticated endpoints use get_user_or_ip_key for per-user rate limiting"
  artifacts:
    - path: "app/api/v1/dashboard.py"
      provides: "Rate-limited dashboard endpoints"
      contains: "@limiter.limit"
    - path: "app/api/v1/volunteers.py"
      provides: "Rate-limited volunteer endpoints"
      contains: "@limiter.limit"
    - path: "app/api/v1/phone_banks.py"
      provides: "Rate-limited phone bank endpoints"
      contains: "@limiter.limit"
    - path: "app/api/v1/shifts.py"
      provides: "Rate-limited shift endpoints"
      contains: "@limiter.limit"
    - path: "app/api/v1/surveys.py"
      provides: "Rate-limited survey endpoints"
      contains: "@limiter.limit"
  key_links:
    - from: "app/api/v1/dashboard.py"
      to: "app/core/rate_limit.py"
      via: "import limiter, get_user_or_ip_key"
      pattern: "from app\\.core\\.rate_limit import limiter, get_user_or_ip_key"
---

<objective>
Apply rate limiting decorators to all endpoints in the 5 highest-endpoint-count route files (73 endpoints total), closing part of INT-02 from the v1.5 milestone audit.

Purpose: Enforce rate limiting on the bulk of API endpoints to prevent abuse and fulfill OBS-03/OBS-04.
Output: 5 route files updated with @limiter.limit decorators and request: Request parameters.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/47-integration-consistency-documentation-cleanup/47-CONTEXT.md
@.planning/phases/47-integration-consistency-documentation-cleanup/47-RESEARCH.md

<interfaces>
<!-- From app/core/rate_limit.py -->
```python
from app.core.rate_limit import limiter, get_user_or_ip_key

# Authenticated endpoint (per-user key, per D-03):
@router.get("/path")
@limiter.limit("60/minute", key_func=get_user_or_ip_key)
async def my_endpoint(request: Request, ...):

# Write endpoint:
@router.post("/path")
@limiter.limit("30/minute", key_func=get_user_or_ip_key)
async def my_write_endpoint(request: Request, ...):
```

<!-- Decorator order (CRITICAL): @router.method() THEN @limiter.limit() THEN async def -->
<!-- request: Request MUST be in function signature (SlowAPI requirement) -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add rate limiting to dashboard.py, volunteers.py, phone_banks.py</name>
  <files>app/api/v1/dashboard.py, app/api/v1/volunteers.py, app/api/v1/phone_banks.py</files>
  <read_first>
    - app/api/v1/dashboard.py (18 GET endpoints, 0 have request: Request)
    - app/api/v1/volunteers.py (16 endpoints mix of GET/POST/PATCH/DELETE, 0 have request: Request)
    - app/api/v1/phone_banks.py (14 endpoints mix of GET/POST/PATCH/DELETE, 0 have request: Request)
    - app/api/v1/join.py (reference pattern for @limiter.limit usage)
    - app/core/rate_limit.py (limiter and get_user_or_ip_key definitions)
  </read_first>
  <action>
    For each of the 3 files, apply these mechanical changes:

    **Step 1: Add imports** at the top of each file:
    ```python
    from fastapi import Request  # add to existing fastapi import if not present
    from app.core.rate_limit import limiter, get_user_or_ip_key
    ```

    **Step 2: For every endpoint function**, add two things:
    a. `request: Request,` as the first parameter after `self` (if any)
    b. `@limiter.limit(...)` decorator between `@router.method(...)` and `async def`

    **Rate limit tiers (per D-01, D-03):**
    - GET endpoints: `@limiter.limit("60/minute", key_func=get_user_or_ip_key)`
    - POST/PATCH/DELETE endpoints: `@limiter.limit("30/minute", key_func=get_user_or_ip_key)`

    **dashboard.py specifics:** All 18 endpoints are GET -- all get `"60/minute"`.

    **volunteers.py specifics:** Mix of GET/POST/PATCH/DELETE. All authenticated, all get `get_user_or_ip_key`.

    **phone_banks.py specifics:** Mix of GET/POST/PATCH/DELETE. All authenticated, all get `get_user_or_ip_key`.

    **Step 3:** Run `uv run ruff check` on each file and `uv run ruff format` to fix any formatting issues.

    **Decorator order (CRITICAL):** `@router.get(...)` THEN `@limiter.limit(...)` THEN `async def`.
  </action>
  <verify>
    <automated>uv run ruff check app/api/v1/dashboard.py app/api/v1/volunteers.py app/api/v1/phone_banks.py && echo "LINT PASS" || echo "LINT FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - `app/api/v1/dashboard.py` contains `from app.core.rate_limit import limiter, get_user_or_ip_key`
    - `grep -c "@limiter.limit" app/api/v1/dashboard.py` returns 18
    - `grep -c "request: Request" app/api/v1/dashboard.py` returns at least 18
    - `grep -c "@limiter.limit" app/api/v1/volunteers.py` returns 16
    - `grep -c "request: Request" app/api/v1/volunteers.py` returns at least 16
    - `grep -c "@limiter.limit" app/api/v1/phone_banks.py` returns 14
    - `grep -c "request: Request" app/api/v1/phone_banks.py` returns at least 14
    - All `@limiter.limit` decorators for GET use `"60/minute"`
    - All `@limiter.limit` decorators for POST/PATCH/DELETE use `"30/minute"`
    - All decorators include `key_func=get_user_or_ip_key`
    - `uv run ruff check` passes on all 3 files
  </acceptance_criteria>
  <done>48 endpoints across 3 files have rate limiting decorators with correct tiers and per-user key functions</done>
</task>

<task type="auto">
  <name>Task 2: Add rate limiting to shifts.py and surveys.py</name>
  <files>app/api/v1/shifts.py, app/api/v1/surveys.py</files>
  <read_first>
    - app/api/v1/shifts.py (14 endpoints, 0 have request: Request)
    - app/api/v1/surveys.py (11 endpoints, 0 have request: Request)
    - app/core/rate_limit.py (limiter and get_user_or_ip_key definitions)
  </read_first>
  <action>
    Same mechanical pattern as Task 1:

    **Step 1: Add imports** to each file:
    ```python
    from fastapi import Request  # add to existing fastapi import
    from app.core.rate_limit import limiter, get_user_or_ip_key
    ```

    **Step 2: For every endpoint function**, add:
    a. `request: Request,` as first parameter
    b. `@limiter.limit(...)` decorator between `@router.method(...)` and `async def`

    **Rate limit tiers (per D-01, D-03):**
    - GET endpoints: `@limiter.limit("60/minute", key_func=get_user_or_ip_key)`
    - POST/PATCH/DELETE endpoints: `@limiter.limit("30/minute", key_func=get_user_or_ip_key)`

    **shifts.py specifics:** 14 endpoints, mix of GET/POST/PATCH/DELETE. All authenticated.

    **surveys.py specifics:** 11 endpoints, mix of GET/POST/PATCH/DELETE. All authenticated.

    **Step 3:** Run `uv run ruff check` and `uv run ruff format` on each file.
  </action>
  <verify>
    <automated>uv run ruff check app/api/v1/shifts.py app/api/v1/surveys.py && echo "LINT PASS" || echo "LINT FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "@limiter.limit" app/api/v1/shifts.py` returns 14
    - `grep -c "request: Request" app/api/v1/shifts.py` returns at least 14
    - `grep -c "@limiter.limit" app/api/v1/surveys.py` returns 11
    - `grep -c "request: Request" app/api/v1/surveys.py` returns at least 11
    - All `@limiter.limit` decorators for GET use `"60/minute"`
    - All `@limiter.limit` decorators for POST/PATCH/DELETE use `"30/minute"`
    - All decorators include `key_func=get_user_or_ip_key`
    - `uv run ruff check` passes on both files
  </acceptance_criteria>
  <done>25 endpoints across 2 files have rate limiting decorators with correct tiers and per-user key functions</done>
</task>

</tasks>

<verification>
- `uv run ruff check app/api/v1/dashboard.py app/api/v1/volunteers.py app/api/v1/phone_banks.py app/api/v1/shifts.py app/api/v1/surveys.py` passes
- Total `@limiter.limit` count across 5 files = 73
- All GET endpoints use "60/minute"
- All POST/PATCH/DELETE endpoints use "30/minute"
- All use `key_func=get_user_or_ip_key`
</verification>

<success_criteria>
73 endpoints across 5 high-traffic route files are rate-limited with tiered limits and per-user key functions per D-01/D-03.
</success_criteria>

<output>
After completion, create `.planning/phases/47-integration-consistency-documentation-cleanup/47-02-SUMMARY.md`
</output>
