---
phase: 04-phone-banking
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/models/call_list.py
  - app/models/phone_bank.py
  - app/models/dnc.py
  - app/models/__init__.py
  - app/schemas/call_list.py
  - app/schemas/phone_bank.py
  - app/schemas/dnc.py
  - alembic/versions/004_phone_banking.py
  - tests/unit/test_call_lists.py
  - tests/unit/test_phone_bank.py
  - tests/unit/test_dnc.py
  - tests/integration/test_phone_banking_rls.py
autonomous: true
requirements:
  - PHONE-01
  - PHONE-02
  - PHONE-03
  - PHONE-04
  - PHONE-05

must_haves:
  truths:
    - "All phone banking tables exist in database with correct columns and constraints"
    - "RLS policies isolate call_lists, call_list_entries, phone_bank_sessions, session_callers, and do_not_call by campaign_id"
    - "InteractionType enum includes PHONE_CALL value"
    - "Pydantic schemas validate all phone banking request/response contracts"
    - "Test stub files exist for all phone banking test coverage"
  artifacts:
    - path: "app/models/call_list.py"
      provides: "CallList, CallListEntry, CallListStatus, EntryStatus, CallResultCode enums"
      contains: "class CallList"
    - path: "app/models/phone_bank.py"
      provides: "PhoneBankSession, SessionCaller, SessionStatus enum"
      contains: "class PhoneBankSession"
    - path: "app/models/dnc.py"
      provides: "DoNotCallEntry, DNCReason enum"
      contains: "class DoNotCallEntry"
    - path: "app/schemas/call_list.py"
      provides: "Call list create/response/entry schemas"
      exports: ["CallListCreate", "CallListResponse", "CallListEntryResponse"]
    - path: "app/schemas/phone_bank.py"
      provides: "Session, caller, call recording schemas"
      exports: ["PhoneBankSessionCreate", "CallRecordCreate"]
    - path: "app/schemas/dnc.py"
      provides: "DNC entry schemas"
      exports: ["DNCEntryCreate", "DNCEntryResponse"]
    - path: "alembic/versions/004_phone_banking.py"
      provides: "Migration with all tables, RLS policies, PHONE_CALL InteractionType"
      contains: "004_phone_banking"
    - path: "tests/unit/test_call_lists.py"
      provides: "Skip-marked test stubs for call list service"
    - path: "tests/unit/test_phone_bank.py"
      provides: "Skip-marked test stubs for phone bank service"
    - path: "tests/unit/test_dnc.py"
      provides: "Skip-marked test stubs for DNC service"
    - path: "tests/integration/test_phone_banking_rls.py"
      provides: "Skip-marked test stubs for RLS integration"
  key_links:
    - from: "app/models/call_list.py"
      to: "app/models/voter_interaction.py"
      via: "CallResultCode used in PHONE_CALL interaction payload"
      pattern: "CallResultCode"
    - from: "alembic/versions/004_phone_banking.py"
      to: "all new tables"
      via: "CREATE TABLE + ENABLE ROW LEVEL SECURITY"
      pattern: "enable_row_level_security"
---

<objective>
Create all phone banking SQLAlchemy models, Pydantic schemas, Alembic migration with RLS policies, and test stub scaffolding.

Purpose: Establish the data layer foundation that Plans 04-02 and 04-03 build services and API routes on top of.
Output: 5 new database tables (call_lists, call_list_entries, phone_bank_sessions, session_callers, do_not_call), full schema contracts, PHONE_CALL InteractionType, and skip-marked test stubs.
</objective>

<execution_context>
@/home/kwhatcher/.claude/get-shit-done/workflows/execute-plan.md
@/home/kwhatcher/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-phone-banking/04-CONTEXT.md
@.planning/phases/04-phone-banking/04-RESEARCH.md

<interfaces>
<!-- Existing models and patterns the executor needs -->

From app/models/voter_interaction.py:
```python
class InteractionType(enum.StrEnum):
    NOTE = "note"
    TAG_ADDED = "tag_added"
    TAG_REMOVED = "tag_removed"
    IMPORT = "import"
    CONTACT_UPDATED = "contact_updated"
    DOOR_KNOCK = "door_knock"
    SURVEY_RESPONSE = "survey_response"
    # Phase 4 adds: PHONE_CALL = "phone_call"
```

From app/models/walk_list.py (pattern to follow):
```python
class WalkList(Base):
    __tablename__ = "walk_lists"
    __table_args__ = (Index("ix_walk_lists_campaign_id", "campaign_id"),)
    id: Mapped[uuid.UUID] = mapped_column(primary_key=True, default=uuid.uuid4)
    campaign_id: Mapped[uuid.UUID] = mapped_column(ForeignKey("campaigns.id"), nullable=False)
    # ... status, name, script_id, created_by, etc.
```

From app/models/voter_contact.py:
```python
class VoterPhone(Base):
    __tablename__ = "voter_phones"
    id, campaign_id, voter_id, value (String(50)), type (String(20)), is_primary, source, created_at, updated_at
```

From app/models/__init__.py:
```python
# Must add new models to __all__ list
from app.models.campaign import Campaign
# ... existing imports ...
```

From alembic/versions/003_canvassing_operations.py (RLS pattern):
```python
# Direct RLS: campaign_id column on table
op.execute("ALTER TABLE turfs ENABLE ROW LEVEL SECURITY")
op.execute("ALTER TABLE turfs FORCE ROW LEVEL SECURITY")
op.execute("CREATE POLICY turfs_isolation ON turfs USING (campaign_id = current_setting('app.current_campaign_id', true)::uuid)")
op.execute("GRANT SELECT, INSERT, UPDATE, DELETE ON turfs TO app_user")

# Subquery RLS: child table without campaign_id
op.execute("CREATE POLICY walk_list_entries_isolation ON walk_list_entries USING (call_list_id IN (SELECT id FROM walk_lists WHERE campaign_id = current_setting('app.current_campaign_id', true)::uuid))")
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Phone banking models, schemas, and migration</name>
  <files>
    app/models/call_list.py,
    app/models/phone_bank.py,
    app/models/dnc.py,
    app/models/__init__.py,
    app/models/voter_interaction.py,
    app/schemas/call_list.py,
    app/schemas/phone_bank.py,
    app/schemas/dnc.py,
    alembic/versions/004_phone_banking.py
  </files>
  <action>
    **Models (follow walk_list.py pattern exactly):**

    1. `app/models/call_list.py` -- CallList and CallListEntry models:
       - `CallListStatus(StrEnum)`: draft, active, completed
       - `EntryStatus(StrEnum)`: available, in_progress, completed, max_attempts, terminal
       - `CallResultCode(StrEnum)`: answered, no_answer, busy, wrong_number, voicemail, refused, deceased, disconnected
       - `CallList(Base)`: id (UUID PK), campaign_id (FK campaigns), voter_list_id (FK voter_lists, nullable), script_id (FK survey_scripts, nullable), name (String(255)), status (String(50), default DRAFT), total_entries (Integer, default 0), completed_entries (Integer, default 0), max_attempts (Integer, default 3), claim_timeout_minutes (Integer, default 30), cooldown_minutes (Integer, default 60), created_by (FK users), created_at, updated_at. Index on campaign_id.
       - `CallListEntry(Base)`: id (UUID PK), call_list_id (FK call_lists), voter_id (FK voters), priority_score (Integer, default 0), phone_numbers (JSONB, default list), status (String(50), default AVAILABLE), attempt_count (Integer, default 0), claimed_by (FK users, nullable), claimed_at (nullable), last_attempt_at (nullable), phone_attempts (JSONB, nullable). Indexes: (call_list_id, status) composite, (call_list_id, priority_score) composite.
       - Use native_enum=False on all StrEnum columns per project convention.

    2. `app/models/phone_bank.py` -- PhoneBankSession and SessionCaller:
       - `SessionStatus(StrEnum)`: draft, active, paused, completed
       - `PhoneBankSession(Base)`: id (UUID PK), campaign_id (FK campaigns), call_list_id (FK call_lists), name (String(255)), status (String(50), default DRAFT), scheduled_start (nullable), scheduled_end (nullable), created_by (FK users), created_at, updated_at. Index on campaign_id.
       - `SessionCaller(Base)`: id (UUID PK), session_id (FK phone_bank_sessions), user_id (FK users), check_in_at (nullable), check_out_at (nullable), created_at. Index on session_id.

    3. `app/models/dnc.py` -- DoNotCallEntry:
       - `DNCReason(StrEnum)`: refused, voter_request, registry_import, manual
       - `DoNotCallEntry(Base)`: id (UUID PK), campaign_id (FK campaigns), phone_number (String(50)), reason (String(50)), added_by (FK users), added_at (server_default=func.now()). Unique index on (campaign_id, phone_number).

    4. `app/models/voter_interaction.py` -- Add PHONE_CALL = "phone_call" to InteractionType enum.

    5. `app/models/__init__.py` -- Add imports for CallList, CallListEntry, PhoneBankSession, SessionCaller, DoNotCallEntry and include in __all__.

    **Schemas (follow existing walk_list.py / canvass.py schema patterns):**

    6. `app/schemas/call_list.py`:
       - `CallListCreate`: name (str), voter_list_id (UUID | None), script_id (UUID | None), max_attempts (int = 3), claim_timeout_minutes (int = 30), cooldown_minutes (int = 60)
       - `CallListResponse`: id, name, status, total_entries, completed_entries, max_attempts, claim_timeout_minutes, cooldown_minutes, voter_list_id, script_id, created_by, created_at, updated_at
       - `CallListEntryResponse`: id, voter_id, priority_score, phone_numbers, status, attempt_count, claimed_by, claimed_at, last_attempt_at, phone_attempts
       - `ClaimEntriesRequest`: batch_size (int = 5)
       - `CallListSummaryResponse`: id, name, status, total_entries, completed_entries

    7. `app/schemas/phone_bank.py`:
       - `PhoneBankSessionCreate`: name (str), call_list_id (UUID), scheduled_start (datetime | None), scheduled_end (datetime | None)
       - `PhoneBankSessionUpdate`: name (str | None), status (str | None), scheduled_start (datetime | None), scheduled_end (datetime | None)
       - `PhoneBankSessionResponse`: id, name, status, call_list_id, scheduled_start, scheduled_end, created_by, created_at, updated_at
       - `SessionCallerResponse`: id, session_id, user_id, check_in_at, check_out_at, created_at
       - `CallRecordCreate`: call_list_entry_id (UUID), result_code (str), phone_number_used (str), call_started_at (datetime), call_ended_at (datetime), notes (str | None), survey_responses (list | None), survey_complete (bool = True)
       - `CallRecordResponse`: id (UUID), result_code, phone_number_used, call_started_at, call_ended_at, notes, interaction_id (UUID)
       - `SessionProgressResponse`: session_id, total_entries, completed, in_progress, available, callers (list of caller stats)
       - `CallerProgressItem`: user_id, calls_made, check_in_at, check_out_at
       - `ReassignRequest`: new_caller_id (str)

    8. `app/schemas/dnc.py`:
       - `DNCEntryCreate`: phone_number (str), reason (str = "manual")
       - `DNCEntryResponse`: id, phone_number, reason, added_by, added_at
       - `DNCCheckRequest`: phone_number (str)
       - `DNCCheckResponse`: is_dnc (bool), entry (DNCEntryResponse | None)
       - `DNCImportResponse`: added (int), skipped (int), invalid (int)

    **Migration:**

    9. `alembic/versions/004_phone_banking.py`:
       - Create tables: call_lists, call_list_entries, phone_bank_sessions, session_callers, do_not_call
       - All columns, indexes, and constraints matching the models above
       - RLS policies:
         - Direct campaign_id isolation: call_lists, phone_bank_sessions, do_not_call
         - Subquery isolation: call_list_entries via call_lists.campaign_id (one-hop), session_callers via phone_bank_sessions.campaign_id (one-hop)
       - GRANT SELECT, INSERT, UPDATE, DELETE to app_user on all tables
       - NOTE: Do NOT add PHONE_CALL to any database enum -- InteractionType uses native_enum=False (VARCHAR), so the new Python enum value works automatically without a migration step
       - Use same Alembic patterns as 003_canvassing_operations.py (revision, down_revision, upgrade/downgrade functions)
  </action>
  <verify>
    <automated>cd /home/kwhatcher/projects/run-api && uv run python -c "from app.models.call_list import CallList, CallListEntry, CallResultCode, EntryStatus; from app.models.phone_bank import PhoneBankSession, SessionCaller, SessionStatus; from app.models.dnc import DoNotCallEntry, DNCReason; from app.models.voter_interaction import InteractionType; assert InteractionType.PHONE_CALL == 'phone_call'; print('All models import OK')" && uv run python -c "from app.schemas.call_list import CallListCreate, CallListResponse, CallListEntryResponse; from app.schemas.phone_bank import PhoneBankSessionCreate, CallRecordCreate, SessionProgressResponse; from app.schemas.dnc import DNCEntryCreate, DNCEntryResponse, DNCImportResponse; print('All schemas import OK')"</automated>
  </verify>
  <done>All 5 model files created with correct columns/indexes/FKs. All 3 schema files created. Migration file exists with table creation and RLS policies. InteractionType includes PHONE_CALL. models/__init__.py updated.</done>
</task>

<task type="auto">
  <name>Task 2: Skip-marked test stubs for all phone banking tests</name>
  <files>
    tests/unit/test_call_lists.py,
    tests/unit/test_phone_bank.py,
    tests/unit/test_dnc.py,
    tests/integration/test_phone_banking_rls.py
  </files>
  <action>
    Create skip-marked test stubs following the Phase 3 Wave 0 pattern (03-00-PLAN): each test function has `@pytest.mark.skip(reason="stub")` decorator, a docstring describing the test, and `raise NotImplementedError` body. No production imports at module level.

    1. `tests/unit/test_call_lists.py` -- stubs for:
       - test_generate_call_list_from_voter_list (PHONE-01: generates frozen snapshot from voter universe, filters by phone + DNC)
       - test_generate_call_list_phone_validation (PHONE-01: filters out invalid phone numbers)
       - test_generate_call_list_dnc_filtering (PHONE-01: excludes DNC numbers)
       - test_claim_entries_returns_batch (PHONE-01: claim-on-fetch returns correct batch size ordered by priority)
       - test_claim_entries_skip_locked (PHONE-01: concurrent claims don't get same entries)
       - test_release_stale_claims (PHONE-01: stale entries released on next claim)
       - test_entry_status_transitions (PHONE-03: available -> in_progress -> completed/terminal/max_attempts)
       - test_auto_recycle_no_answer (PHONE-01: no_answer entries return to pool after cooldown)
       - test_terminal_outcome_never_retried (PHONE-03: refused/deceased entries never return to pool)
       - test_priority_score_ordering (PHONE-01: entries returned in descending priority order)
       - test_call_list_status_lifecycle (draft -> active -> completed, no backward)
       - test_multi_phone_entry (PHONE-01: entry includes all voter phone numbers ordered by priority)

    2. `tests/unit/test_phone_bank.py` -- stubs for:
       - test_create_session (session creation with call list reference)
       - test_session_status_lifecycle (draft -> active -> paused -> completed)
       - test_session_with_script (PHONE-02: session's call list has script attached, questions retrievable)
       - test_assign_caller (assign caller to session)
       - test_caller_check_in_check_out (check_in_at/check_out_at timestamps)
       - test_record_call_answered (PHONE-03: record answered outcome with timestamps)
       - test_record_call_no_answer (PHONE-03: no_answer outcome, entry recycled)
       - test_record_call_with_survey (PHONE-04: answered call + survey responses recorded)
       - test_partial_survey_saved (PHONE-04: partial survey marked incomplete)
       - test_interaction_events_created (PHONE-05: PHONE_CALL interaction event emitted)
       - test_survey_response_interaction (PHONE-05: SURVEY_RESPONSE interaction event emitted)
       - test_refused_auto_dnc (PHONE-05: refused outcome auto-adds to DNC)
       - test_wrong_number_marks_phone_only (PHONE-03: wrong_number marks phone, not person)
       - test_session_progress (supervisor progress view with per-caller stats)
       - test_reassign_entry (supervisor reassign entry between callers)
       - test_force_release_entry (supervisor force-release claimed entry)
       - test_session_pause_blocks_claims (paused session prevents new claims)
       - test_session_complete_releases_entries (completing session releases all in_progress entries)

    3. `tests/unit/test_dnc.py` -- stubs for:
       - test_add_dnc_entry (manual add)
       - test_add_dnc_duplicate_ignored (duplicate phone_number + campaign_id skipped)
       - test_auto_flag_refused (PHONE-05: refused call auto-creates DNC entry)
       - test_bulk_import_csv (import CSV with phone_number column, reports stats)
       - test_bulk_import_invalid_phones (invalid phones reported as invalid count)
       - test_delete_dnc_entry (remove from DNC)
       - test_check_dnc (check if phone is on DNC list)

    4. `tests/integration/test_phone_banking_rls.py` -- stubs for:
       - test_call_list_rls_isolation (campaign A can't see campaign B call lists)
       - test_call_list_entry_rls_isolation (entries isolated via call_list parent)
       - test_phone_bank_session_rls_isolation (sessions isolated by campaign_id)
       - test_session_caller_rls_isolation (callers isolated via session parent)
       - test_dnc_rls_isolation (DNC entries isolated by campaign_id)
  </action>
  <verify>
    <automated>cd /home/kwhatcher/projects/run-api && uv run pytest tests/unit/test_call_lists.py tests/unit/test_phone_bank.py tests/unit/test_dnc.py tests/integration/test_phone_banking_rls.py --collect-only -q 2>&1 | tail -5</automated>
  </verify>
  <done>All 4 test files created with skip-marked stubs. pytest --collect-only finds all test functions. No production imports at module level. Each stub has descriptive docstring and NotImplementedError body.</done>
</task>

</tasks>

<verification>
- All model files import without errors
- All schema files import without errors
- Migration file exists and has proper revision chain
- Test stubs collected by pytest (not erroring on import)
- InteractionType.PHONE_CALL exists
- RLS policies defined for all 5 tables in migration
</verification>

<success_criteria>
- 5 model classes (CallList, CallListEntry, PhoneBankSession, SessionCaller, DoNotCallEntry) importable
- 3 enum classes (CallResultCode, EntryStatus, DNCReason) importable
- All Pydantic schemas importable with correct fields
- Migration 004 exists with table creation + RLS + grants
- 42 test stubs collected by pytest
- `uv run pytest tests/ -x --collect-only` passes (no import errors)
</success_criteria>

<output>
After completion, create `.planning/phases/04-phone-banking/04-01-SUMMARY.md`
</output>
