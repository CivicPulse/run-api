---
phase: 44-ui-ux-polish-frontend-hardening
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - web/src/components/shared/RouteErrorBoundary.tsx
  - web/src/main.tsx
  - web/src/routes/__root.tsx
  - web/src/routes/campaigns/$campaignId.tsx
  - web/src/routes/campaigns/$campaignId/voters.tsx
  - web/src/routes/campaigns/$campaignId/canvassing.tsx
  - web/src/routes/campaigns/$campaignId/phone-banking.tsx
  - web/src/routes/campaigns/$campaignId/volunteers.tsx
  - web/src/routes/campaigns/$campaignId/settings.tsx
  - web/src/routes/campaigns/$campaignId/surveys.tsx
  - web/src/routes/org/members.tsx
  - web/src/routes/org/settings.tsx
autonomous: true
requirements:
  - OBS-05
  - UX-01

must_haves:
  truths:
    - "Route errors render a Card-based fallback UI with retry button and dashboard link"
    - "Unhandled errors in any route section are caught by the nearest error boundary"
    - "Sidebar slides over (overlays) content on all screen sizes, never pushes"
    - "When sidebar is hidden, content uses full viewport width"
  artifacts:
    - path: "web/src/components/shared/RouteErrorBoundary.tsx"
      provides: "Reusable error boundary component"
      exports: ["RouteErrorBoundary"]
    - path: "web/src/main.tsx"
      provides: "Global defaultErrorComponent on router"
      contains: "defaultErrorComponent"
  key_links:
    - from: "web/src/main.tsx"
      to: "web/src/components/shared/RouteErrorBoundary.tsx"
      via: "defaultErrorComponent import"
      pattern: "defaultErrorComponent.*RouteErrorBoundary"
    - from: "web/src/routes/campaigns/$campaignId.tsx"
      to: "web/src/components/shared/RouteErrorBoundary.tsx"
      via: "errorComponent option"
      pattern: "errorComponent.*RouteErrorBoundary"
---

<objective>
Create the RouteErrorBoundary component and wire it as the default error boundary on the router and on each major section layout route. Also update the sidebar to use slide-over (offcanvas) behavior with defaultOpen={false}.

Purpose: Graceful error handling across all routes (OBS-05) and sidebar overlay behavior (UX-01) are foundational UX improvements.
Output: RouteErrorBoundary.tsx, updated main.tsx, updated route files with errorComponent, sidebar offcanvas behavior.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/44-ui-ux-polish-frontend-hardening/44-CONTEXT.md
@.planning/phases/44-ui-ux-polish-frontend-hardening/44-RESEARCH.md

<interfaces>
<!-- Key types and contracts the executor needs -->

From web/src/main.tsx:
```typescript
import { RouterProvider, createRouter } from "@tanstack/react-router"
const router = createRouter({ routeTree })
// ADD: defaultErrorComponent: RouteErrorBoundary
```

From web/src/routes/__root.tsx:
```typescript
// SidebarProvider currently has no defaultOpen prop
// ADD: <SidebarProvider defaultOpen={false}>
// Sidebar already uses collapsible="offcanvas" via shadcn defaults
```

TanStack Router errorComponent props:
```typescript
interface RouteErrorProps {
  error: Error
  reset: () => void
  info?: { componentStack?: string }
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create RouteErrorBoundary component, wire as defaultErrorComponent, and add errorComponent to section routes</name>
  <files>
    web/src/components/shared/RouteErrorBoundary.tsx,
    web/src/main.tsx,
    web/src/routes/campaigns/$campaignId.tsx,
    web/src/routes/campaigns/$campaignId/voters.tsx,
    web/src/routes/campaigns/$campaignId/canvassing.tsx,
    web/src/routes/campaigns/$campaignId/phone-banking.tsx,
    web/src/routes/campaigns/$campaignId/volunteers.tsx,
    web/src/routes/campaigns/$campaignId/settings.tsx,
    web/src/routes/campaigns/$campaignId/surveys.tsx,
    web/src/routes/org/members.tsx,
    web/src/routes/org/settings.tsx
  </files>
  <read_first>
    web/src/main.tsx,
    web/src/routes/campaigns/$campaignId.tsx,
    web/src/routes/campaigns/$campaignId/voters.tsx,
    web/src/routes/campaigns/$campaignId/canvassing.tsx,
    web/src/routes/campaigns/$campaignId/phone-banking.tsx,
    web/src/routes/campaigns/$campaignId/volunteers.tsx,
    web/src/routes/campaigns/$campaignId/settings.tsx,
    web/src/routes/campaigns/$campaignId/surveys.tsx,
    web/src/routes/org/members.tsx,
    web/src/routes/org/settings.tsx,
    web/src/components/ui/card.tsx,
    web/src/components/ui/button.tsx
  </read_first>
  <action>
    1. Create `web/src/components/shared/RouteErrorBoundary.tsx` per D-10:
       - Import `useRouter` from `@tanstack/react-router`
       - Import `Card`, `CardContent`, `CardHeader`, `CardTitle` from `@/components/ui/card`
       - Import `Button` from `@/components/ui/button`
       - Import `AlertTriangle` from `lucide-react`
       - Accept props `{ error: Error; reset: () => void }` (per TanStack Router errorComponent contract)
       - Render centered Card with:
         - AlertTriangle icon (h-10 w-10, text-destructive)
         - CardTitle "Something went wrong"
         - Description paragraph: "An unexpected error occurred. Please try again."
         - Two buttons: "Try Again" (calls `reset()`, variant="default") and "Go to Dashboard" (calls `router.navigate({ to: "/" })`, variant="outline")
       - Export as named export `RouteErrorBoundary`

    2. Update `web/src/main.tsx`:
       - Add import: `import { RouteErrorBoundary } from "@/components/shared/RouteErrorBoundary"`
       - Change `createRouter({ routeTree })` to `createRouter({ routeTree, defaultErrorComponent: RouteErrorBoundary })`
       - This serves as the top-level app error boundary per D-09

    3. For each of these layout/section route files, add `errorComponent: RouteErrorBoundary` to the route options object in `createFileRoute(...)({...})`:
       - `web/src/routes/campaigns/$campaignId.tsx` (campaign section)
       - `web/src/routes/campaigns/$campaignId/voters.tsx` (voters section)
       - `web/src/routes/campaigns/$campaignId/canvassing.tsx` (canvassing section)
       - `web/src/routes/campaigns/$campaignId/phone-banking.tsx` (phone banking section)
       - `web/src/routes/campaigns/$campaignId/volunteers.tsx` (volunteers section)
       - `web/src/routes/campaigns/$campaignId/settings.tsx` (settings section)
       - `web/src/routes/campaigns/$campaignId/surveys.tsx` (surveys section)
       - `web/src/routes/org/members.tsx` (org members)
       - `web/src/routes/org/settings.tsx` (org settings)
       Add `import { RouteErrorBoundary } from "@/components/shared/RouteErrorBoundary"` to each file.

    Per D-08: route-level error boundaries wrapping each major section.
    Per D-11: do NOT add component-level error boundaries.
  </action>
  <verify>
    <automated>cd /home/kwhatcher/projects/civicpulse/run-api/web && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - RouteErrorBoundary.tsx exists and contains `export function RouteErrorBoundary`
    - RouteErrorBoundary.tsx contains `Something went wrong`
    - RouteErrorBoundary.tsx contains `reset()` call
    - RouteErrorBoundary.tsx contains `router.navigate({ to: "/" })`
    - main.tsx contains `defaultErrorComponent: RouteErrorBoundary`
    - campaigns/$campaignId.tsx contains `errorComponent: RouteErrorBoundary`
    - campaigns/$campaignId/voters.tsx contains `errorComponent: RouteErrorBoundary`
    - campaigns/$campaignId/canvassing.tsx contains `errorComponent: RouteErrorBoundary`
    - campaigns/$campaignId/phone-banking.tsx contains `errorComponent: RouteErrorBoundary`
    - campaigns/$campaignId/volunteers.tsx contains `errorComponent: RouteErrorBoundary`
    - campaigns/$campaignId/settings.tsx contains `errorComponent: RouteErrorBoundary`
    - campaigns/$campaignId/surveys.tsx contains `errorComponent: RouteErrorBoundary`
    - org/members.tsx contains `errorComponent: RouteErrorBoundary`
    - org/settings.tsx contains `errorComponent: RouteErrorBoundary`
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>RouteErrorBoundary component created, wired as defaultErrorComponent on router, and added as errorComponent on all 9 section layout routes. TypeScript compiles cleanly.</done>
</task>

<task type="auto">
  <name>Task 2: Update sidebar to slide-over offcanvas with defaultOpen false</name>
  <files>
    web/src/routes/__root.tsx
  </files>
  <read_first>
    web/src/routes/__root.tsx,
    web/src/components/ui/sidebar.tsx
  </read_first>
  <action>
    Per D-02 and D-03: Update `__root.tsx` RootLayout to make sidebar slide-over on all screen sizes with no persistent icon rail.

    1. In the `RootLayout` function, find `<SidebarProvider>` (currently no props or defaultOpen not set).
    2. Change to `<SidebarProvider defaultOpen={false}>`.
       - Per research: The `defaultOpen` prop is only used when no cookie exists. Returning users keep their saved preference. New users start with sidebar hidden.
       - The `Sidebar` component already uses `collapsible="offcanvas"` behavior from shadcn defaults, so no change needed there.
    3. Per D-01: Keep the two-group sidebar structure (Campaign + Organization) as-is. Do NOT flatten.
    4. Per D-03: No icon rail. The existing `<SidebarRail />` component is a resize handle, not an icon rail -- verify this is correct by reading sidebar.tsx. If SidebarRail renders a visible rail when collapsed, remove it. If it's just a drag handle for resizing, keep it.

    Note: The `__root.tsx` Loader2 spinner on line 233 is for initial auth initialization (not data loading) -- this is acceptable per D-15 and should NOT be replaced with skeletons.
  </action>
  <verify>
    <automated>cd /home/kwhatcher/projects/civicpulse/run-api/web && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - __root.tsx contains `<SidebarProvider defaultOpen={false}>`
    - __root.tsx still contains `SidebarGroupLabel>Campaign</SidebarGroupLabel` (per D-01, two groups preserved)
    - __root.tsx still contains `SidebarGroupLabel>Organization</SidebarGroupLabel` (per D-01)
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>Sidebar defaults to hidden (offcanvas), slides over content on toggle, content uses full viewport when sidebar is hidden. Two-group structure preserved per D-01.</done>
</task>

</tasks>

<verification>
- `cd web && npx tsc --noEmit` exits 0
- RouteErrorBoundary.tsx exists with Card-based UI, retry button, and dashboard link
- All 9 section routes have errorComponent set
- main.tsx has defaultErrorComponent set
- Sidebar starts hidden with offcanvas behavior
</verification>

<success_criteria>
- Error boundaries catch and display friendly UI for route errors across all major sections
- Sidebar slides over content (not push) with no icon rail when hidden
- TypeScript compiles without errors
</success_criteria>

<output>
After completion, create `.planning/phases/44-ui-ux-polish-frontend-hardening/44-01-SUMMARY.md`
</output>
