---
phase: 44-ui-ux-polish-frontend-hardening
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - web/src/routes/campaigns/$campaignId.tsx
  - web/src/routes/campaigns/$campaignId/volunteers/shifts/index.tsx
  - web/src/routes/campaigns/$campaignId/settings/general.tsx
  - web/src/routes/campaigns/$campaignId/canvassing/index.tsx
  - web/src/routes/campaigns/$campaignId/voters/index.tsx
  - web/src/routes/campaigns/$campaignId/voters/lists/index.tsx
  - web/src/routes/campaigns/$campaignId/voters/tags/index.tsx
  - web/src/routes/campaigns/$campaignId/voters/imports/index.tsx
  - web/src/routes/campaigns/$campaignId/phone-banking/sessions/index.tsx
  - web/src/routes/campaigns/$campaignId/phone-banking/call-lists/index.tsx
  - web/src/routes/campaigns/$campaignId/phone-banking/dnc/index.tsx
  - web/src/routes/campaigns/$campaignId/surveys/index.tsx
  - web/src/routes/campaigns/$campaignId/volunteers/roster/index.tsx
  - web/src/routes/campaigns/$campaignId/volunteers/tags/index.tsx
  - web/src/routes/campaigns/$campaignId/settings/members.tsx
  - web/src/routes/index.tsx
  - web/src/routes/org/members.tsx
autonomous: true
requirements:
  - OBS-06
  - OBS-07

must_haves:
  truths:
    - "Every list page shows a contextual empty state message (not blank table or generic 'No data')"
    - "Every data-loading page shows a skeleton or matching-layout placeholder during fetch"
    - "No page-level Loader2 spinners remain (button-level spinners are acceptable)"
  artifacts:
    - path: "web/src/routes/campaigns/$campaignId.tsx"
      provides: "Skeleton loading replacing Loader2 spinner"
      contains: "Skeleton"
    - path: "web/src/routes/campaigns/$campaignId/volunteers/shifts/index.tsx"
      provides: "Skeleton loading replacing Loader2 spinner"
      contains: "Skeleton"
  key_links:
    - from: "web/src/routes/campaigns/$campaignId.tsx"
      to: "web/src/components/ui/skeleton.tsx"
      via: "Skeleton import for loading state"
      pattern: "import.*Skeleton.*from.*@/components/ui/skeleton"
---

<objective>
Audit and fix all list pages for contextual empty states (OBS-06) and replace all page-level Loader2 spinners with layout-matching skeletons (OBS-07).

Purpose: Users should never see a blank table or a generic spinner -- every page needs contextual feedback.
Output: All list pages have meaningful empty states. All page-level loading uses Skeleton components.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/44-ui-ux-polish-frontend-hardening/44-CONTEXT.md
@.planning/phases/44-ui-ux-polish-frontend-hardening/44-RESEARCH.md

<interfaces>
From web/src/components/shared/DataTable.tsx:
```typescript
// Already supports empty state + skeleton loading:
// emptyIcon, emptyTitle, emptyDescription, emptyAction props
// isLoading renders 5 skeleton rows automatically
```

From web/src/components/ui/skeleton.tsx:
```typescript
import { Skeleton } from "@/components/ui/skeleton"
// Usage: <Skeleton className="h-8 w-48" />
```

From web/src/components/shared/EmptyState.tsx:
```typescript
export function EmptyState({ icon, title, description, action, className }: EmptyStateProps)
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace page-level Loader2 spinners with layout-matching skeletons</name>
  <files>
    web/src/routes/campaigns/$campaignId.tsx,
    web/src/routes/campaigns/$campaignId/volunteers/shifts/index.tsx,
    web/src/routes/campaigns/$campaignId/settings/general.tsx
  </files>
  <read_first>
    web/src/routes/campaigns/$campaignId.tsx,
    web/src/routes/campaigns/$campaignId/volunteers/shifts/index.tsx,
    web/src/routes/campaigns/$campaignId/settings/general.tsx,
    web/src/components/ui/skeleton.tsx
  </read_first>
  <action>
    Per D-15 and D-16: Replace page-level Loader2 spinners with Skeleton components that match the content layout. Button-level Loader2 usage is acceptable and should NOT be changed.

    **Files to update (page-level Loader2):**

    1. `web/src/routes/campaigns/$campaignId.tsx` (line ~24-28):
       - Currently: centered `<Loader2 className="h-6 w-6 animate-spin text-muted-foreground" />`
       - Replace with: `<div className="space-y-4"><Skeleton className="h-8 w-48" /><div className="grid gap-4 md:grid-cols-3"><Skeleton className="h-32 rounded-lg" /><Skeleton className="h-32 rounded-lg" /><Skeleton className="h-32 rounded-lg" /></div></div>`
       - Add import: `import { Skeleton } from "@/components/ui/skeleton"`
       - Remove `Loader2` from lucide import if no longer used in this file

    2. `web/src/routes/campaigns/$campaignId/volunteers/shifts/index.tsx` (line ~164-167):
       - Currently: centered `<Loader2 className="h-6 w-6 animate-spin text-muted-foreground" />`
       - Replace with: card-shaped skeletons matching the shift cards layout: `<div className="space-y-4"><Skeleton className="h-8 w-48" /><div className="space-y-3">{Array.from({ length: 3 }).map((_, i) => <Skeleton key={i} className="h-24 rounded-lg" />)}</div></div>`
       - Add import: `import { Skeleton } from "@/components/ui/skeleton"`
       - Remove `Loader2` from lucide import if no longer used

    3. `web/src/routes/campaigns/$campaignId/settings/general.tsx` (line ~67-71):
       - Currently: centered `<Loader2 className="h-6 w-6 animate-spin text-muted-foreground" />`
       - Replace with: form-shaped skeletons: `<div className="space-y-6 max-w-2xl"><Skeleton className="h-8 w-48" /><Skeleton className="h-10 w-full" /><Skeleton className="h-10 w-full" /><Skeleton className="h-10 w-full" /><Skeleton className="h-10 w-32" /></div>`
       - Add import: `import { Skeleton } from "@/components/ui/skeleton"`
       - Keep the button-level `<Loader2>` on line ~121 (inside submit button) -- that is acceptable per D-15

    **Files to NOT change (button-level Loader2 only):**
    - `org/settings.tsx` -- Loader2 on line 116 is inside submit button (acceptable)
    - `campaigns/new.tsx` -- Loader2 on line 527 is inside submit button (acceptable)
    - `settings/danger.tsx` -- Loader2 on line 230 is inside submit button (acceptable)
    - `field/` routes -- field mode is out of scope for this phase
  </action>
  <verify>
    <automated>cd /home/kwhatcher/projects/civicpulse/run-api/web && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - campaigns/$campaignId.tsx contains `import { Skeleton }` and `<Skeleton` in loading block
    - campaigns/$campaignId.tsx does NOT contain `Loader2` in loading block (Loader2 removed from that conditional)
    - volunteers/shifts/index.tsx contains `import { Skeleton }` and `<Skeleton` in loading block
    - settings/general.tsx contains `import { Skeleton }` and `<Skeleton` in loading block
    - settings/general.tsx still contains `Loader2` (button-level usage preserved)
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>All 3 page-level Loader2 spinners replaced with layout-matching Skeleton components. Button-level Loader2 usage preserved where appropriate.</done>
</task>

<task type="auto">
  <name>Task 2: Audit and fix empty states on all list pages for contextual messaging</name>
  <files>
    web/src/routes/campaigns/$campaignId/canvassing/index.tsx,
    web/src/routes/campaigns/$campaignId/voters/index.tsx,
    web/src/routes/campaigns/$campaignId/voters/lists/index.tsx,
    web/src/routes/campaigns/$campaignId/voters/tags/index.tsx,
    web/src/routes/campaigns/$campaignId/voters/imports/index.tsx,
    web/src/routes/campaigns/$campaignId/phone-banking/sessions/index.tsx,
    web/src/routes/campaigns/$campaignId/phone-banking/call-lists/index.tsx,
    web/src/routes/campaigns/$campaignId/phone-banking/dnc/index.tsx,
    web/src/routes/campaigns/$campaignId/surveys/index.tsx,
    web/src/routes/campaigns/$campaignId/volunteers/roster/index.tsx,
    web/src/routes/campaigns/$campaignId/volunteers/tags/index.tsx,
    web/src/routes/campaigns/$campaignId/volunteers/shifts/index.tsx,
    web/src/routes/campaigns/$campaignId/settings/members.tsx,
    web/src/routes/index.tsx,
    web/src/routes/org/members.tsx
  </files>
  <read_first>
    web/src/routes/campaigns/$campaignId/canvassing/index.tsx,
    web/src/routes/campaigns/$campaignId/voters/index.tsx,
    web/src/routes/campaigns/$campaignId/voters/lists/index.tsx,
    web/src/routes/campaigns/$campaignId/voters/tags/index.tsx,
    web/src/routes/campaigns/$campaignId/voters/imports/index.tsx,
    web/src/routes/campaigns/$campaignId/phone-banking/sessions/index.tsx,
    web/src/routes/campaigns/$campaignId/phone-banking/call-lists/index.tsx,
    web/src/routes/campaigns/$campaignId/phone-banking/dnc/index.tsx,
    web/src/routes/campaigns/$campaignId/surveys/index.tsx,
    web/src/routes/campaigns/$campaignId/volunteers/roster/index.tsx,
    web/src/routes/campaigns/$campaignId/volunteers/tags/index.tsx,
    web/src/routes/campaigns/$campaignId/volunteers/shifts/index.tsx,
    web/src/routes/campaigns/$campaignId/settings/members.tsx,
    web/src/routes/index.tsx,
    web/src/routes/org/members.tsx,
    web/src/components/shared/EmptyState.tsx,
    web/src/components/shared/DataTable.tsx
  </read_first>
  <action>
    Per D-12, D-13, D-14: Audit every list page and ensure each has a contextual empty state. Most pages already use `DataTable` with `emptyTitle`/`emptyIcon` or `EmptyState` component. This task verifies all are correct and fixes any that use generic messaging or lack empty states entirely.

    For each list page, verify it has contextual empty state props. If missing or generic, add/update with the following contextual messaging:

    | Page | emptyIcon | emptyTitle | emptyDescription |
    |------|-----------|------------|------------------|
    | voters/index.tsx | Users | "No voters yet" | "Import a voter file or add voters manually to get started." |
    | voters/lists/index.tsx | ClipboardList | "No voter lists yet" | "Create a voter list to organize voters for outreach." |
    | voters/tags/index.tsx | Tag | "No voter tags yet" | "Create tags to categorize and filter voters." |
    | voters/imports/index.tsx | Upload | "No imports yet" | "Import a CSV file to add voters to your campaign." |
    | canvassing/index.tsx (turfs tab) | Map | "No turfs yet" | "Create a turf to define canvassing areas on the map." |
    | canvassing/index.tsx (walk lists tab) | ClipboardList | "No walk lists yet" | "Create a walk list from a turf to assign to canvassers." |
    | phone-banking/sessions/index.tsx | Phone | "No sessions yet" | "Create a phone bank session to get started." |
    | phone-banking/call-lists/index.tsx | Phone | "No call lists yet" | "Create a call list to organize voters for phone banking." |
    | phone-banking/dnc/index.tsx | PhoneOff | "No DNC entries" | "Voters added to the Do Not Call list will appear here." |
    | surveys/index.tsx | FileText | "No survey scripts yet" | "Create a survey script with questions for canvassers and callers." |
    | volunteers/roster/index.tsx | Users | "No volunteers yet" | "Register volunteers or invite them to join the campaign." |
    | volunteers/tags/index.tsx | Tag | "No volunteer tags yet" | "Create tags to organize and categorize volunteers." |
    | volunteers/shifts/index.tsx | Calendar | "No shifts scheduled" | "Create a shift to schedule volunteer activities." |
    | settings/members.tsx | Users | "No team members" | "Invite team members to collaborate on this campaign." |
    | index.tsx (org dashboard) | BarChart3 | "No campaigns yet" | "Create your first campaign to get started." |
    | org/members.tsx | Users | "No members yet" | "Add members to your organization." |

    For pages using `DataTable`, set the `emptyIcon`, `emptyTitle`, `emptyDescription` props.
    For pages using direct `EmptyState` component, update the props accordingly.
    For pages that show lists without DataTable (e.g., shifts uses direct card rendering), add an `EmptyState` component in the empty branch of the conditional.

    Ensure all icons are imported from `lucide-react`.
    Do NOT change any empty state that already has contextual messaging (just verify it exists).
  </action>
  <verify>
    <automated>cd /home/kwhatcher/projects/civicpulse/run-api/web && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - voters/index.tsx contains `No voters yet`
    - voters/lists/index.tsx contains `No voter lists yet`
    - voters/tags/index.tsx contains `No voter tags yet`
    - canvassing/index.tsx contains `No turfs yet`
    - phone-banking/sessions/index.tsx contains `No sessions yet`
    - phone-banking/call-lists/index.tsx contains `No call lists yet`
    - phone-banking/dnc/index.tsx contains `No DNC entries`
    - surveys/index.tsx contains `No survey scripts yet`
    - volunteers/roster/index.tsx contains `No volunteers yet`
    - volunteers/shifts/index.tsx contains `No shifts scheduled`
    - settings/members.tsx contains `No team members`
    - org/members.tsx contains `No members yet`
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>All list pages display contextual empty state messages with appropriate icons and descriptions. No page shows a blank table or generic "No data" text.</done>
</task>

</tasks>

<verification>
- `cd web && npx tsc --noEmit` exits 0
- No page-level Loader2 spinners remain (grep confirms)
- All list pages have contextual empty state messaging
</verification>

<success_criteria>
- Every list page shows a meaningful empty state with icon, title, and description when data is empty
- Every data-loading page shows layout-matching skeletons during fetch
- TypeScript compiles without errors
</success_criteria>

<output>
After completion, create `.planning/phases/44-ui-ux-polish-frontend-hardening/44-03-SUMMARY.md`
</output>
