---
phase: 65-chunk-planning-concurrency-cap-closure
plan: 01
type: execute
wave: 1
depends_on: ["64-03"]
files_modified:
  - app/services/storage.py
  - app/services/import_service.py
  - tests/unit/test_import_service.py
autonomous: true
requirements: [CHUNK-06]
must_haves:
  truths:
    - "Chunk planning changes when the same row count is paired with materially different file sizes."
    - "Chunk sizing still respects the asyncpg bind-parameter ceiling for wide CSVs."
    - "Parent orchestration can obtain object size without re-downloading the entire file."
  artifacts:
    - path: "app/services/storage.py"
      provides: "S3 metadata lookup returning object byte length for an import file"
    - path: "app/services/import_service.py"
      provides: "file-size-aware chunk planning helper that clamps by bind limit and byte budget"
    - path: "tests/unit/test_import_service.py"
      provides: "coverage for file-size-sensitive chunk planning and storage metadata lookup seams"
  key_links:
    - from: "app/tasks/import_task.py"
      to: "app/services/storage.py"
      via: "object-size metadata lookup before chunk planning"
      pattern: "get_object_size"
    - from: "app/services/import_service.py"
      to: "calculate_effective_rows_per_write"
      via: "bind-limit clamp remains part of chunk planning"
      pattern: "calculate_effective_rows_per_write"
    - from: "app/services/import_service.py"
      to: "plan_chunk_ranges"
      via: "file-size-aware rows-per-chunk calculation"
      pattern: "file_size_bytes"
---

<objective>
Repair CHUNK-06 by making chunk planning depend on both mapped-column bind pressure and file size without changing the existing chunk schema or serial-routing contract.

Purpose: Phase 59 and 60 already provide deterministic chunk boundaries, but the current planner ignores object size entirely. This plan adds the missing storage metadata seam and a real byte-aware clamp so later orchestration uses a repaired planner instead of the row-count-only helper.
Output: An object-size lookup in `StorageService`, a file-size-aware `plan_chunk_ranges(...)` implementation, and unit coverage proving the planner produces different ranges for the same row count when file size changes.
</objective>

<execution_context>
@/home/kwhatcher/.codex/get-shit-done/workflows/execute-plan.md
@/home/kwhatcher/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/65-chunk-planning-concurrency-cap-closure/65-RESEARCH.md
@.planning/phases/59-chunk-schema-configuration/59-02-SUMMARY.md
@.planning/phases/60-parent-split-parallel-processing/60-01-SUMMARY.md
@app/services/storage.py
@app/services/import_service.py
@tests/unit/test_import_service.py

<interfaces>
From app/services/storage.py:
```python
class StorageService:
    async def download_file(
        self, key: str, chunk_size: int = 65_536
    ) -> AsyncIterator[bytes]: ...
```

From app/services/import_service.py:
```python
def calculate_effective_rows_per_write(
    mapped_column_count: int,
    target_rows: int,
    max_bind_params: int = DEFAULT_MAX_BIND_PARAMS,
    extra_columns_per_row: int = DEFAULT_EXTRA_COLUMNS_PER_ROW,
) -> int: ...

def plan_chunk_ranges(
    total_rows: int,
    mapped_column_count: int,
    chunk_size_default: int,
) -> list[tuple[int, int]]: ...
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add RED coverage for file-size-aware chunk planning and storage metadata lookup</name>
  <files>tests/unit/test_import_service.py</files>
  <read_first>
    - tests/unit/test_import_service.py
    - app/services/import_service.py
    - app/services/storage.py
    - .planning/phases/65-chunk-planning-concurrency-cap-closure/65-RESEARCH.md
  </read_first>
  <acceptance_criteria>
    - `tests/unit/test_import_service.py` contains a test whose name includes `different_file_sizes`.
    - `tests/unit/test_import_service.py` contains at least one assertion matching `assert .* != .*` for two `plan_chunk_ranges(...)` results with identical `total_rows`.
    - `tests/unit/test_import_service.py` contains the literal string `file_size_bytes=`.
    - `tests/unit/test_import_service.py` contains a storage metadata test that references `ContentLength` or `get_object_size`.
  </acceptance_criteria>
  <behavior>
    - Test 1: two calls to `plan_chunk_ranges(total_rows=12000, mapped_column_count=8, chunk_size_default=4000, file_size_bytes=...)` return different ranges when one file is much larger in bytes than the other.
    - Test 2: a very high `mapped_column_count` still clamps chunk size to the bind-limit result even when `file_size_bytes` is small.
    - Test 3: `StorageService.get_object_size("imports/file.csv")` returns `int(response["ContentLength"])` from a mocked S3 `head_object` call.
  </behavior>
  <action>Extend `tests/unit/test_import_service.py` before changing production code. Add one test that calls `plan_chunk_ranges(total_rows=12000, mapped_column_count=8, chunk_size_default=4000, file_size_bytes=24_000_000)` and `plan_chunk_ranges(total_rows=12000, mapped_column_count=8, chunk_size_default=4000, file_size_bytes=1_200_000)` and asserts the larger file yields more chunk ranges. Add one test that calls `plan_chunk_ranges(total_rows=25000, mapped_column_count=400, chunk_size_default=settings.import_chunk_size_default, file_size_bytes=500_000)` and asserts the first range still ends at row `81` to preserve the existing bind-limit clamp. Add one async test for `StorageService.get_object_size()` that mocks `head_object(Bucket=settings.s3_bucket, Key=\"imports/file.csv\")` and asserts the return value is the integer `ContentLength` field. Do not weaken or delete the existing deterministic range tests; convert them to the new function signature where needed.</action>
  <verify>
    <automated>uv run pytest tests/unit/test_import_service.py -x</automated>
  </verify>
  <done>Failing unit tests describe the exact CHUNK-06 contract: file size materially affects chunk ranges, bind-limit safety remains intact, and storage metadata is available without full-file streaming.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement object-size lookup and byte-aware chunk planning</name>
  <files>app/services/storage.py, app/services/import_service.py, tests/unit/test_import_service.py</files>
  <read_first>
    - app/services/storage.py
    - app/services/import_service.py
    - tests/unit/test_import_service.py
    - .planning/phases/59-chunk-schema-configuration/59-02-SUMMARY.md
    - .planning/phases/65-chunk-planning-concurrency-cap-closure/65-RESEARCH.md
  </read_first>
  <acceptance_criteria>
    - `app/services/storage.py` contains `async def get_object_size(`.
    - `app/services/storage.py` contains `head_object(` and `ContentLength`.
    - `app/services/import_service.py` contains `file_size_bytes: int | None`.
    - `app/services/import_service.py` contains both `calculate_effective_rows_per_write(` and `min(` inside `plan_chunk_ranges`.
    - `uv run pytest tests/unit/test_import_service.py -x` passes.
  </acceptance_criteria>
  <behavior>
    - Test 1: `StorageService.get_object_size()` reads object length through S3 metadata only.
    - Test 2: `plan_chunk_ranges()` clamps rows per chunk using `min(bind_limited_rows, file_limited_rows, chunk_size_default)` with `max(1, ...)` protection.
    - Test 3: the previous deterministic row-count-only planning behavior remains the fallback when `file_size_bytes` is `None` or `total_rows <= 0`.
  </behavior>
  <action>In `app/services/storage.py`, add `async def get_object_size(self, key: str) -> int:` that opens an S3 client with `_client_kwargs()`, calls `head_object(Bucket=settings.s3_bucket, Key=key)`, and returns `int(response[\"ContentLength\"])`. In `app/services/import_service.py`, change `plan_chunk_ranges` to `def plan_chunk_ranges(total_rows: int, mapped_column_count: int, chunk_size_default: int, file_size_bytes: int | None = None) -> list[tuple[int, int]]`. Keep the existing bind-limit clamp via `calculate_effective_rows_per_write(...)`. Add a byte-budget clamp derived from average row width: compute `average_bytes_per_row = max(file_size_bytes // max(total_rows, 1), 1)` when `file_size_bytes` is provided, set `target_chunk_bytes = 2_000_000`, and derive `file_limited_rows = max(1, target_chunk_bytes // average_bytes_per_row)`. Choose `chunk_rows = max(1, min(bind_limited_rows, file_limited_rows, chunk_size_default))` when `file_size_bytes` is present, otherwise keep the existing bind-limit-only path. Preserve inclusive 1-based row ranges exactly as today. Do not add new settings, schema fields, or task orchestration logic in this plan.</action>
  <verify>
    <automated>uv run pytest tests/unit/test_import_service.py -x</automated>
  </verify>
  <done>`StorageService` exposes a reusable object-size metadata call and `plan_chunk_ranges(...)` now produces bind-safe, byte-aware chunk ranges that materially differ for wide versus narrow files with the same row count.</done>
</task>

</tasks>

<verification>
Run `uv run pytest tests/unit/test_import_service.py -x`.
Confirm the planner signature now includes `file_size_bytes`, `StorageService` uses `head_object`, and the same-row-count/different-size unit case proves chunk ranges actually change.
</verification>

<success_criteria>
CHUNK-06 is implemented at the service layer: the runtime has a durable object-size lookup seam and a chunk planner that adapts to both bind pressure and file size rather than row count alone.
</success_criteria>

<output>
After completion, create `.planning/phases/65-chunk-planning-concurrency-cap-closure/65-01-SUMMARY.md`
</output>
