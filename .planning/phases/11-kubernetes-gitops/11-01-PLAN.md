---
phase: 11-kubernetes-gitops
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - k8s/deployment.yaml
  - k8s/service.yaml
  - k8s/configmap.yaml
  - k8s/secret-template.yaml.example
autonomous: true
requirements:
  - K8S-01
  - K8S-02
  - K8S-04

must_haves:
  truths:
    - "Deployment has init container running alembic upgrade head before main container starts"
    - "Liveness probe hits /health/live and readiness probe hits /health/ready on port 8000"
    - "ClusterIP Service exposes the API on port 8000 with selector app: run-api"
    - "Secret template documents all 11 secret keys with kubectl create secret commands"
    - "Both init container and main container use envFrom referencing run-api-config and run-api-secret"
  artifacts:
    - path: "k8s/deployment.yaml"
      provides: "Full Deployment with init container, probes, envFrom, resource limits"
      contains: "initContainers"
    - path: "k8s/service.yaml"
      provides: "ClusterIP Service on port 8000"
      contains: "ClusterIP"
    - path: "k8s/configmap.yaml"
      provides: "Non-sensitive configuration (APP_NAME, DEBUG, CORS_ALLOWED_ORIGINS)"
      contains: "run-api-config"
    - path: "k8s/secret-template.yaml.example"
      provides: "Secret template with all 11 keys and kubectl commands"
      contains: "kubectl create secret"
  key_links:
    - from: "k8s/deployment.yaml"
      to: "k8s/configmap.yaml"
      via: "envFrom configMapRef"
      pattern: "configMapRef.*run-api-config"
    - from: "k8s/deployment.yaml"
      to: "k8s/secret-template.yaml.example"
      via: "envFrom secretRef"
      pattern: "secretRef.*run-api-secret"
    - from: "k8s/deployment.yaml initContainers"
      to: "k8s/configmap.yaml + k8s/secret-template.yaml.example"
      via: "envFrom on init container (Alembic needs DATABASE_URL_SYNC)"
      pattern: "initContainers[\\s\\S]*envFrom"
---

<objective>
Create the core Kubernetes workload manifests: expand the existing Deployment stub with init container for Alembic migrations, health probes, envFrom, and resource limits; add a ClusterIP Service; add a ConfigMap for non-sensitive config; and add a documented Secret template.

Purpose: These are the foundational manifests that define how the run-api workload runs on the baremetal cluster.
Output: 4 manifest files in k8s/ (deployment.yaml expanded, service.yaml, configmap.yaml, secret-template.yaml.example)
</objective>

<execution_context>
@/home/kwhatcher/.claude/get-shit-done/workflows/execute-plan.md
@/home/kwhatcher/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-kubernetes-gitops/11-CONTEXT.md
@.planning/phases/11-kubernetes-gitops/11-RESEARCH.md

@k8s/deployment.yaml
@app/core/config.py

<interfaces>
<!-- Existing deployment stub to expand in-place -->
From k8s/deployment.yaml (current stub):
```yaml
apiVersion: apps/v1
kind: Deployment
metadata:
  name: run-api
  namespace: run-api
spec:
  replicas: 1
  selector:
    matchLabels:
      app: run-api
  template:
    metadata:
      labels:
        app: run-api
    spec:
      containers:
        - name: run-api
          image: ghcr.io/civicpulse/run-api:latest
          ports:
            - containerPort: 8000
```

<!-- CI sed pattern that must continue to work (matches ALL image: ghcr.io/civicpulse/run-api: lines) -->
From .github/workflows/publish.yml:
```
sed -i "s|image: ghcr.io/civicpulse/run-api:.*|image: ghcr.io/civicpulse/run-api:sha-${{ github.sha }}|" k8s/deployment.yaml
```

<!-- All env vars from app/core/config.py Settings class -->
ConfigMap keys: APP_NAME, DEBUG, CORS_ALLOWED_ORIGINS
Secret keys: DATABASE_URL, DATABASE_URL_SYNC, ZITADEL_ISSUER, ZITADEL_PROJECT_ID, ZITADEL_SERVICE_CLIENT_ID, ZITADEL_SERVICE_CLIENT_SECRET, S3_ENDPOINT_URL, S3_ACCESS_KEY_ID, S3_SECRET_ACCESS_KEY, S3_BUCKET, S3_REGION
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Expand Deployment and create ConfigMap + Secret template</name>
  <files>k8s/deployment.yaml, k8s/configmap.yaml, k8s/secret-template.yaml.example</files>
  <action>
**k8s/deployment.yaml** -- Expand the existing stub in-place (do NOT create a new file). Keep the existing metadata (name: run-api, namespace: run-api, labels app: run-api). Add:

1. Init container named `migrate`:
   - Same image as main container (`ghcr.io/civicpulse/run-api:latest` -- CI sed updates both)
   - command: `["python", "-m", "alembic", "upgrade", "head"]`
   - envFrom referencing both `run-api-config` (configMapRef) and `run-api-secret` (secretRef)
   - Resources: requests 128Mi/100m, limits 256Mi/200m

2. Main container updates:
   - Add envFrom referencing both `run-api-config` (configMapRef) and `run-api-secret` (secretRef)
   - Liveness probe: httpGet /health/live port 8000, initialDelaySeconds 5, periodSeconds 15, failureThreshold 3
   - Readiness probe: httpGet /health/ready port 8000, initialDelaySeconds 10, periodSeconds 10, failureThreshold 3
   - Resources: requests 128Mi/100m, limits 512Mi/500m
   - securityContext: runAsNonRoot true (image uses `app` user)

3. Keep the comment about CI auto-updating the image tag at the top of the file.

CRITICAL: The CI sed pattern `s|image: ghcr.io/civicpulse/run-api:.*|...` matches ALL image lines. Both init container and main container image lines MUST use the exact format `image: ghcr.io/civicpulse/run-api:latest` so sed updates both. Do NOT add comments on the same line as image: lines.

**k8s/configmap.yaml** -- Create new file:
- apiVersion: v1, kind: ConfigMap
- metadata: name `run-api-config`, namespace `run-api`
- data:
  - APP_NAME: "CivicPulse Run API"
  - DEBUG: "false"
  - CORS_ALLOWED_ORIGINS: '["https://run.civpulse.org"]'

**k8s/secret-template.yaml.example** -- Create new file (NOT .yaml so ArgoCD ignores it):
- Include a commented-out Secret YAML with placeholder values for all 11 keys
- Include the full `kubectl create secret generic run-api-secret` command with --from-literal for each key
- Clearly document that DATABASE_URL uses `postgresql+asyncpg://` prefix and DATABASE_URL_SYNC uses `postgresql+psycopg2://` prefix
- Mark file header: "# SECRET TEMPLATE -- DO NOT apply directly. Use kubectl command below."
  </action>
  <verify>
    <automated>python -c "import yaml; [yaml.safe_load(open(f)) for f in ['k8s/deployment.yaml', 'k8s/configmap.yaml']]; print('YAML valid')"</automated>
  </verify>
  <done>deployment.yaml has initContainers with alembic command, envFrom on both containers, liveness/readiness probes, resource limits. configmap.yaml has 3 non-sensitive keys. secret-template.yaml.example documents all 11 secret keys with kubectl command.</done>
</task>

<task type="auto">
  <name>Task 2: Create ClusterIP Service</name>
  <files>k8s/service.yaml</files>
  <action>
Create `k8s/service.yaml`:
- apiVersion: v1, kind: Service
- metadata: name `run-api`, namespace `run-api`
- spec:
  - type: ClusterIP
  - selector: app: run-api (matches Deployment pod labels)
  - ports: port 8000, targetPort 8000, protocol TCP
  </action>
  <verify>
    <automated>python -c "import yaml; d=yaml.safe_load(open('k8s/service.yaml')); assert d['spec']['type']=='ClusterIP'; assert d['spec']['ports'][0]['port']==8000; print('Service valid')"</automated>
  </verify>
  <done>ClusterIP Service exists at k8s/service.yaml, exposes port 8000, selects pods with label app: run-api.</done>
</task>

</tasks>

<verification>
All 4 manifest files parse as valid YAML (except secret-template.yaml.example which is documentation).
Deployment has initContainers, probes, envFrom, and resource limits.
Service is ClusterIP type on port 8000.
ConfigMap has 3 keys matching CONTEXT.md decisions.
Secret template has all 11 keys matching app/core/config.py.
CI sed pattern still works: both image lines in deployment.yaml match `image: ghcr.io/civicpulse/run-api:.*`.
</verification>

<success_criteria>
- k8s/deployment.yaml expanded with init container, probes, envFrom, resources
- k8s/service.yaml created as ClusterIP on port 8000
- k8s/configmap.yaml created with APP_NAME, DEBUG, CORS_ALLOWED_ORIGINS
- k8s/secret-template.yaml.example created with all 11 secret keys documented
- All .yaml files parse without errors
</success_criteria>

<output>
After completion, create `.planning/phases/11-kubernetes-gitops/11-01-SUMMARY.md`
</output>
