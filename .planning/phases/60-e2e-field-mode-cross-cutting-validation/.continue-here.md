---
phase: 60-e2e-field-mode-cross-cutting-validation
task: debug-remaining-87-failures
total_tasks: 5 plans complete + ongoing debug
status: in_progress
last_updated: 2026-03-30T11:39:07.931Z
---

<current_state>
Phase 60 gap closure plans (60-04, 60-05) are fully executed. Live E2E validation
revealed 157 pre-existing failures beyond Phase 60's original scope. Four debugging
rounds reduced failures from 157 to 87, with 177 tests now passing. Speed optimizations
are committed (auth caching, networkidle waits, shared setup, dev server mode, sharding).
</current_state>

<completed_work>

**Phase 60 Plans:**
- Plan 60-01: Field mode E2E spec - Done
- Plan 60-02: Cross-cutting and navigation E2E specs - Done
- Plan 60-03: Full suite validation, bug tracking, old spec cleanup - Done
- Plan 60-04: Fix broken waitForURL regex in 35 spec files (53 replacements) - Done
- Plan 60-05: Phone banking BUG-01 tracking, auth/CORS fixes, REQUIREMENTS.md update - Done

**Debug Rounds (157 → 87 failures):**
- Round 1: Campaign link regex, stale UUIDs, sidebar viewport, login rewrite, RBAC scoping (157→108)
- Round 2: require_role ensure_user_synced, a11y helpers, voter link locators, org campaign fallback (108→86)
- Round 3: Bearer token auth on page.request, campaign creation fix, sidebar nav (86→81)
- Round 4: HTTP baseURL, a11y mock routes, shift timezone, survey nav, API-first campaign lookup (81→87, with test count changes from config updates)

**Speed Optimizations:**
- 106 waitForTimeout calls → waitForLoadState("networkidle") (saves ~144s)
- Auth state caching (skip re-auth when tokens < 30 min old)
- Shared auth-setup project (1 project instead of 5)
- Dev server mode (E2E_USE_DEV_SERVER=1 skips build+preview)
- Shard support (E2E_SHARD=1/4)
- Parallel-by-project test runner script
- 3 legacy specs pruned
</completed_work>

<remaining_work>

**87 E2E Failures (categorized):**
- 52 UI element mismatches — tests expect elements with different names/structure
- 9 timing/loading — OIDC init hangs, slow responses under concurrency
- 7 real a11y violations — color-contrast (text-destructive/70), touch targets, scrollable regions
- 3 strict mode — locators resolving to multiple elements
- 16 other — API format issues, cascade from serial blocks

**Infrastructure:**
- 3 manual DB fixes need Alembic migrations:
  1. Manager CampaignMember role: "viewer" → "manager"
  2. campaigns.zitadel_org_id index: unique → non-unique
  3. import_jobs.last_committed_row column added
- VERIFICATION.md needs updating with final debug results
- Debug session file needs archiving

**Backlog:**
- 999.1: Upgrade ZITADEL to v3/v4 (would unlock API-based auth for faster E2E setup)
</remaining_work>

<decisions_made>

- Sidebar defaultOpen=true eliminates 231 viewport errors — user confirmed this is fine UX
- API-based auth (#2) not feasible with ZITADEL v2.71.6 — added upgrade to backlog 999.1
- Reduced Playwright workers to 4 to prevent ZITADEL overload
- Auth state caching with 30-min TTL — delete playwright/.auth/ to force re-auth
- Legacy phase specs (debug-call-list-entries, phase-13-verification, phase-15-verification) deleted as redundant
</decisions_made>

<blockers>
- 52 UI element mismatch failures need test expectations updated to match actual app UI
- DB fixes are not persisted in migrations — will be lost on DB recreate
</blockers>

<context>
The original Phase 60 scope was: fix VERIFICATION.md gaps (broken waitForURL, phone banking
tracking, REQUIREMENTS.md consistency). That's fully done. The 87 remaining failures are
pre-existing issues discovered during live validation — they're test expectations that don't
match the current UI, real a11y CSS issues, and timing problems. Diminishing returns — each
round fixes fewer issues. The most impactful remaining work is (1) creating Alembic migrations
for the 3 DB fixes, (2) fixing the 7 real a11y violations (CSS changes), and (3) deciding
whether to update or delete the 52 tests with wrong element expectations.
</context>

<next_action>
Options when resuming:
1. Fix remaining 87 failures — start with the 7 a11y CSS fixes (highest value), then
   the 52 UI element mismatches (update test expectations to match actual app)
2. Create Alembic migrations for the 3 manual DB fixes
3. Close Phase 60 — update VERIFICATION.md, archive debug session, mark phase complete

Run tests: cd web && E2E_USE_DEV_SERVER=1 npx playwright test --reporter=line
Debug file: .planning/debug/e2e-suite-157-failures.md
</next_action>
