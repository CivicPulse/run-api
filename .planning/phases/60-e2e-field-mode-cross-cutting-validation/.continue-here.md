---
phase: 60-e2e-field-mode-cross-cutting-validation
task: debug-remaining-45-failures
total_tasks: 5 plans complete + ongoing debug
status: in_progress
last_updated: 2026-03-30T14:57:17.939Z
---

<current_state>
Phase 60 gap closure plans (60-01 through 60-05) are fully complete. Extended debug work
has reduced E2E failures from 222→88→45 across 6 rounds in 3 sessions. 212 tests now
passing, 45 failing, 98 did not run (cascading from serial block failures), 8 skipped.
Suite runs in ~7 minutes with 4 workers against dev server.
</current_state>

<completed_work>

**Phase 60 Plans:**
- Plan 60-01: Field mode E2E spec - Done
- Plan 60-02: Cross-cutting and navigation E2E specs - Done
- Plan 60-03: Full suite validation, bug tracking, old spec cleanup - Done
- Plan 60-04: Fix broken waitForURL regex in 35 spec files (53 replacements) - Done
- Plan 60-05: Phone banking BUG-01 tracking, auth/CORS fixes, REQUIREMENTS.md update - Done

**Debug Rounds 1-4 (222 → 88 failures):**
- Sidebar defaultOpen, RBAC ensure_user_synced, Bearer token auth, HTTP baseURL,
  a11y mock routes, API-first campaign lookup, various spec-specific fixes

**Debug Round 5 — this session (88 → 45 failures, commit afea64a):**
- helpers.ts: switched getSeedCampaignId to /me/campaigns API (flat list, no pagination)
- a11y source fixes: destructive contrast token (4.31→5.0), nav opacity, scrollable pre tabIndex
- phase32: tour/QuickStart dismissal, outcome button aria-label format, progress text
- phase35-a11y-audit: propensity badge selectors → semantic tokens
- phase27-filter: page.route() replaces waitForRequest for ky fetch interception
- phase14-import: gotoAndWaitReady for RequireRole timing
- phase13-voter: h2→h1 heading, accordion filters, tab timing
- field-mode.volunteer: waitForAuth helper for parallel worker load
- rbac.volunteer: auth timing, roster heading selector
- table-sort: sort column assertion updated (0→4, bug was fixed)
- phase29: empty state vs table handling, chip class selector

**Speed Optimizations (from prior sessions):**
- 106 waitForTimeout → networkidle waits (~144s saved)
- Auth state caching (30-min TTL)
- Shared auth-setup project, dev server mode, shard support
</completed_work>

<remaining_work>

**45 E2E Failures in 3 categories:**

1. **Parallel-timing regressions (13 failures)** — pass individually, fail under 4 workers:
   - phase35-a11y-audit (5), phase13-voter-verify (2), phase14-import (2),
     phase32-verify (2), field-mode.volunteer (2)
   - Root cause: auth initialization timing under parallel load
   - Fix: export waitForAuth from helpers.ts, use in all specs

2. **Long-tail single-failure specs (19 failures)** — scattered-specs agent stopped early:
   - voter-crud, voter-contacts, voter-tags, voter-notes, voter-lists,
     voter-import, voter-filters, volunteers, volunteer-tags, walk-lists,
     turfs, shifts, surveys, phone-banking, org-management, data-validation,
     call-lists-dnc, a11y-walk-list, phase21-integration-polish

3. **Small clusters (13 failures):**
   - uat-tooltip-popovers (3), table-sort (3), volunteer-signup (2),
     cross-cutting (2), campaign-settings (2), uat-overlap-highlight (1)

**Infrastructure (non-E2E):**
- 3 Alembic migrations needed (manager role, campaign index, import_jobs column)
- VERIFICATION.md needs updating with final debug results
</remaining_work>

<decisions_made>

- getSeedCampaignId uses /me/campaigns (flat list) instead of /campaigns (paginated)
- Real a11y violations fixed in source (not just test expectations)
- page.route() for ky fetch interception — Playwright postData() fails with fetch API
- waitForAuth helper polls for Loading... disappearance — better than networkidle under parallel load
- Tour/QuickStart dismissal pattern for field mode tests
- 5 parallel agents used for batch fixing — effective for non-overlapping spec files
</decisions_made>

<blockers>
- Auth timing under parallel workers is #1 cross-cutting issue
- 3 Alembic migrations not yet created (not blocking E2E work)
</blockers>

<context>
The iterative debug approach has been effective (222→45 over 3 sessions). The biggest
remaining leverage point is making waitForAuth a shared utility in helpers.ts — the
field-mode.volunteer agent already created a working version that polls for the Loading...
screen to disappear. Exporting this to helpers.ts and using it in getSeedCampaignId and
navigateToSeedCampaign would fix many parallel-timing regressions. The 19 long-tail specs
each need individual investigation but likely share common patterns (stale button names,
element selectors, strict mode violations from test data accumulation).
</context>

<next_action>
Start with:
1. Export waitForAuth from helpers.ts as cross-cutting fix → re-run full suite
2. Fix remaining long-tail specs (19 single-failure files)
3. Fix small clusters (tooltip, sort, signup, cross-cutting, campaign-settings)
4. Create Alembic migrations for 3 manual DB fixes
5. Close Phase 60 — update VERIFICATION.md, mark complete

Run tests: cd web && E2E_USE_DEV_SERVER=1 npx playwright test --reporter=line
</next_action>
