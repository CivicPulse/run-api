---
phase: 60-e2e-field-mode-cross-cutting-validation
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - web/e2e/cross-cutting.spec.ts
  - web/e2e/navigation.spec.ts
autonomous: true
requirements: [E2E-21]
must_haves:
  truths:
    - "Cross-cutting spec exercises form navigation guards, toast notifications, and rate limiting"
    - "Cross-cutting spec exercises empty states on a fresh campaign, loading skeletons with delayed API, and error boundary on invalid campaign"
    - "Navigation spec exercises campaign sidebar links, org navigation links, and breadcrumb/back navigation"
    - "Empty state tests create a fresh campaign via API to guarantee no seed data interference"
    - "Form guard tests verify the useBlocker dialog appears when navigating away from dirty forms"
  artifacts:
    - path: "web/e2e/cross-cutting.spec.ts"
      provides: "Cross-cutting E2E spec covering CROSS-01..03 and UI-01..03"
      contains: "test.describe"
    - path: "web/e2e/navigation.spec.ts"
      provides: "Navigation E2E spec covering NAV-01..03"
      contains: "test.describe"
  key_links:
    - from: "web/e2e/cross-cutting.spec.ts"
      to: "/api/v1/campaigns"
      via: "Creates fresh campaign via API for empty state testing"
      pattern: "page\\.request\\.post.*campaigns"
    - from: "web/e2e/cross-cutting.spec.ts"
      to: "web/src/components/shared/RouteErrorBoundary.tsx"
      via: "Tests error boundary rendering on invalid campaign ID"
      pattern: "Something went wrong"
    - from: "web/e2e/navigation.spec.ts"
      to: "web/src/routes/campaigns/$campaignId/"
      via: "Tests sidebar navigation to all campaign sub-routes"
      pattern: "getByRole.*link"
---

<objective>
Write the cross-cutting and navigation Playwright E2E specs covering 9 test cases: form navigation guards (CROSS-01), toast notifications (CROSS-02), rate limiting (CROSS-03), empty states (UI-01), loading skeletons (UI-02), error boundary (UI-03), campaign sidebar navigation (NAV-01), org navigation (NAV-02), and breadcrumb/back navigation (NAV-03).

Purpose: Automates cross-cutting UI behavior validation per E2E-21. These tests verify the app's polish layer -- the behaviors that make it feel professional and robust.
Output: `web/e2e/cross-cutting.spec.ts` and `web/e2e/navigation.spec.ts`
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/60-e2e-field-mode-cross-cutting-validation/60-CONTEXT.md
@.planning/phases/60-e2e-field-mode-cross-cutting-validation/60-RESEARCH.md

@web/playwright.config.ts
@web/e2e/voter-crud.spec.ts
@docs/testing-plan.md

<interfaces>
<!-- Playwright config: unsuffixed specs run under owner auth (chromium project) -->
From web/playwright.config.ts:
```typescript
{
  name: "chromium",
  use: {
    ...devices["Desktop Chrome"],
    storageState: "playwright/.auth/owner.json",
  },
  dependencies: ["setup-owner"],
  testIgnore: /.*\.setup\.ts/,
  testMatch: /^(?!.*\.(admin|manager|volunteer|viewer)\.spec\.ts).*\.spec\.ts$/,
}
```

From web/e2e/voter-crud.spec.ts (navigateToSeedCampaign pattern):
```typescript
async function navigateToSeedCampaign(page: Page): Promise<string> {
  await page.goto("/")
  await page.waitForURL(/\/(campaigns|org)/, { timeout: 15_000 })
  const campaignLink = page.getByRole("link", { name: /macon|bibb|campaign/i }).first()
  await campaignLink.click()
  await page.waitForURL(/campaigns\/([a-f0-9-]+)/, { timeout: 10_000 })
  return page.url().match(/campaigns\/([a-f0-9-]+)/)?.[1] ?? ""
}
```

Key components for cross-cutting tests:
- RouteErrorBoundary: renders "Something went wrong" card with "Try Again" and "Go to Dashboard" buttons
- EmptyState: renders icon, title, description, action button
- useFormGuard: uses TanStack Router useBlocker with shouldBlockFn: () => isDirty
- Toast: sonner library, elements have [data-sonner-toast] attribute
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write cross-cutting.spec.ts covering CROSS-01..03 and UI-01..03</name>
  <files>web/e2e/cross-cutting.spec.ts</files>
  <read_first>
    web/playwright.config.ts
    web/e2e/voter-crud.spec.ts
    web/src/components/shared/RouteErrorBoundary.tsx
    web/src/components/shared/EmptyState.tsx
    web/src/components/shared/DataTable.tsx
    web/src/hooks/useFormGuard.ts
    web/src/routes/campaigns/new.tsx
    web/src/routes/campaigns/$campaignId/voters/index.tsx
    web/src/routes/campaigns/$campaignId/canvassing/index.tsx
    web/src/routes/campaigns/$campaignId/phone-banking/call-lists.tsx
    web/src/routes/campaigns/$campaignId/surveys/index.tsx
    web/src/routes/campaigns/$campaignId/volunteers/index.tsx
    web/src/routes/campaigns/$campaignId/settings.tsx
    docs/testing-plan.md
  </read_first>
  <action>
Create `web/e2e/cross-cutting.spec.ts` with 6 test cases. This spec runs as owner (unsuffixed, desktop viewport).

**File structure:**

1. **Imports and helpers:**
   ```typescript
   import { test, expect, type Page } from "@playwright/test"
   ```
   - `navigateToSeedCampaign(page)` -- standard pattern from voter-crud.spec.ts
   - `createEmptyCampaignViaApi(page)` -- POST to `/api/v1/campaigns` with `{ name: "E2E Empty State Test", type: "general" }`. Returns campaign ID. Forward cookies. Use full URL `https://localhost:4173/api/v1/campaigns`.
   - `deleteEmptyCampaignViaApi(page, campaignId)` -- DELETE to clean up test campaign after empty state checks

2. **Test cases:**

   **`test.describe.serial("Cross-Cutting — Form Guards", () => { ... })`**
   - **CROSS-01: form navigation guard on dirty form** -- Navigate to `/campaigns/new`. Fill in the campaign name field with "Unsaved Campaign" via `page.getByLabel(/name/i).first().fill("Unsaved Campaign")`. Click a sidebar or navigation link to attempt leaving (e.g., click the logo or "All Campaigns" link). Assert a blocking dialog appears -- look for `page.getByRole('alertdialog')` or `page.getByText(/unsaved|leave|stay/i)`. Click "Stay" (or equivalent button inside the dialog). Assert still on the form page (campaign name field still visible with the filled value). Then attempt navigation again. Click "Leave" (or equivalent). Assert navigation proceeds (URL changes away from `/campaigns/new`).

   **`test.describe.serial("Cross-Cutting — Toasts", () => { ... })`**
   - **CROSS-02: toast notifications on CRUD operations** -- Navigate to seed campaign voters page. Create a voter via the UI form (fill minimal fields: first name, last name). Submit. Assert a success toast appears: `expect(page.locator('[data-sonner-toast]')).toBeVisible({ timeout: 5_000 })`. Assert the toast contains success text (e.g., "created" or "success"). Optionally test delete toast by deleting a test-created voter and asserting toast. Assert toasts auto-dismiss after a few seconds: `expect(page.locator('[data-sonner-toast]')).not.toBeVisible({ timeout: 10_000 })`.

   **`test.describe.serial("Cross-Cutting — Rate Limiting", () => { ... })`**
   - **CROSS-03: rate limiting enforcement** -- Navigate to seed campaign. Use `page.request` to rapidly fire 35 API calls to the same endpoint (e.g., `GET /api/v1/campaigns/{campaignId}/voters`). Forward cookies. Collect response statuses. Assert that at least one response has status 429. NOTE: If rate limiting is not configured on the local dev environment, `test.skip()` with a comment noting this is a production-only concern. Read the API middleware or config to determine if rate limiting is active locally before testing.

   **`test.describe.serial("Cross-Cutting — Empty States", () => { ... })`**
   - **UI-01: empty states on list pages** -- Create a fresh campaign via API (`createEmptyCampaignViaApi`). Navigate to each list page in the fresh campaign and assert empty state components are visible. Check at minimum these 8 pages:
     - Voters: `/campaigns/{id}/voters` -- assert text matching "No voters" or "import" CTA
     - Canvassing (Turfs): `/campaigns/{id}/canvassing` -- assert "No turfs" or similar
     - Phone Banking (Call Lists): `/campaigns/{id}/phone-banking/call-lists` -- assert "No call lists" or similar
     - Surveys: `/campaigns/{id}/surveys` -- assert "No surveys" or similar
     - Volunteers: `/campaigns/{id}/volunteers` -- assert "No volunteers" or similar
     - Shifts: `/campaigns/{id}/volunteers/shifts` -- assert "No shifts" or similar
     - Voter Tags: `/campaigns/{id}/voters/tags` -- assert "No tags" or similar
     - Voter Lists: `/campaigns/{id}/voters/lists` -- assert "No lists" or similar

     For each page: navigate, wait for content to load (use `waitForURL` or `expect(...).toBeVisible()`), then assert the empty state message. Read EmptyState.tsx to determine the actual rendered text patterns (it uses title + description props).

     Clean up: delete the test campaign via API at the end.

   **`test.describe.serial("Cross-Cutting — Loading & Errors", () => { ... })`**
   - **UI-02: loading skeletons visible during fetch** -- Use `page.route()` to intercept a data API call (e.g., GET `/api/v1/campaigns/*/voters*`) and delay the response by 2 seconds:
     ```typescript
     await page.route('**/api/v1/campaigns/*/voters*', async (route) => {
       await new Promise(r => setTimeout(r, 2000))
       await route.continue()
     })
     ```
     Navigate to the voters page. Assert skeleton/loading elements are visible (look for `[data-loading]`, `role="status"`, skeleton CSS classes like `animate-pulse`, or `aria-busy="true"`). Wait for data to load. Assert skeletons disappear and real content appears. Unroute after test.

   - **UI-03: error boundary on invalid campaign** -- Navigate to `/campaigns/00000000-0000-0000-0000-000000000000/dashboard`. Assert the RouteErrorBoundary renders -- look for text "Something went wrong" (read RouteErrorBoundary.tsx for exact text). Assert a "Try Again" or "Go to Dashboard" button is visible. Click the dashboard/home button. Assert navigation succeeds to a valid page.

**Critical implementation notes:**
- These specs are unsuffixed (no role suffix) so they run under owner auth (chromium project)
- Per D-14: Use seed data for read operations, API-created fresh campaign for empty state tests
- Per D-15: Use `test.describe.serial` within each concern block
- Per D-17: Use API calls for campaign creation/deletion, not UI
- Assert toasts immediately after triggering action (they auto-dismiss per Pitfall 8 in RESEARCH.md)
- For form guard dialog, use flexible locators -- the dialog may render as alertdialog role or contain "unsaved"/"leave"/"stay" text
- Read actual component source to determine correct assertion text
  </action>
  <verify>
    <automated>cd /home/kwhatcher/projects/civicpulse/run-api/web && npx playwright test cross-cutting --reporter=line 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - web/e2e/cross-cutting.spec.ts exists
    - File contains `CROSS-01` through `CROSS-03` in test names or comments
    - File contains `UI-01` through `UI-03` in test names or comments
    - File contains `createEmptyCampaignViaApi` helper function
    - File contains `page.route` for API response delay (loading skeleton test)
    - File contains assertion on `[data-sonner-toast]` or sonner toast locator
    - File contains navigation to `/campaigns/00000000-0000-0000-0000-000000000000` for error boundary test
    - File contains `test.describe.serial` for ordered lifecycle flows
    - File does NOT contain `.volunteer.spec.ts` or `.admin.spec.ts` suffix (runs as owner)
    - File contains form guard dialog assertion (alertdialog or unsaved/leave/stay text)
    - Playwright test command exits without syntax errors
  </acceptance_criteria>
  <done>cross-cutting.spec.ts contains 6 test cases covering CROSS-01..03 (form guards, toasts, rate limiting) and UI-01..03 (empty states, loading skeletons, error boundary) with API-based campaign creation for empty states and route interception for loading skeleton visibility</done>
</task>

<task type="auto">
  <name>Task 2: Write navigation.spec.ts covering NAV-01..03</name>
  <files>web/e2e/navigation.spec.ts</files>
  <read_first>
    web/playwright.config.ts
    web/e2e/voter-crud.spec.ts
    web/src/routes/campaigns/$campaignId/dashboard.tsx
    web/src/routes/campaigns/$campaignId.tsx
    web/src/components/layout/CampaignSidebar.tsx
    web/src/components/layout/AppSidebar.tsx
    web/src/routes/org/members.tsx
    web/src/routes/org/settings.tsx
    docs/testing-plan.md
  </read_first>
  <action>
Create `web/e2e/navigation.spec.ts` with 3 test cases. This spec runs as owner (unsuffixed, desktop viewport).

**File structure:**

1. **Imports and helpers:**
   ```typescript
   import { test, expect, type Page } from "@playwright/test"
   ```
   - `navigateToSeedCampaign(page)` -- standard pattern

2. **Test cases:**

   **`test.describe.serial("Navigation", () => { ... })`**

   - **NAV-01: campaign sidebar navigation** -- Navigate to seed campaign dashboard. Read the sidebar component source to determine exact link text. For each sidebar link, verify navigation:
     - Click "Dashboard" link -> assert URL contains `/dashboard`
     - Click "Voters" link -> assert URL contains `/voters`
     - Click "Canvassing" or "Field Operations" link -> assert URL contains `/canvassing`
     - Click "Phone Banking" link -> assert URL contains `/phone-banking`
     - Click "Surveys" link -> assert URL contains `/surveys`
     - Click "Volunteers" link -> assert URL contains `/volunteers`
     - Click "Settings" link -> assert URL contains `/settings`

     For each link: use `page.getByRole('link', { name: /link_text/i })`. After clicking, use `page.waitForURL()` or `expect(page).toHaveURL()` to verify correct navigation. Assert the active link has a visual indicator (check for `aria-current="page"` or a CSS class difference -- read the sidebar component to determine the active state pattern).

     Additionally, test sidebar collapse on mobile viewport: resize viewport to 390px width, verify sidebar is hidden or collapsed, verify it can be toggled open via a menu/hamburger button.

   - **NAV-02: organization navigation** -- Navigate to the org level (click org logo or "All Campaigns" link in sidebar). Verify organization nav links:
     - "All Campaigns" or org dashboard link -> assert URL is `/` or `/org`
     - "Members" link -> assert URL contains `/org/members`
     - "Settings" link -> assert URL contains `/org/settings`

     For each link: click and assert URL. Read the AppSidebar or org layout component for actual link text.

   - **NAV-03: breadcrumb / back navigation** -- Navigate deep into the app: go to seed campaign -> voters -> click a specific voter to open detail. Assert URL contains voter ID. Use `page.goBack()` (browser back button). Assert URL returns to the voters list page. Test direct URL entry: navigate directly to a deep route URL (e.g., paste campaign settings URL). Assert the page loads correctly without errors. Verify the back button from the deep route still works.

**Critical implementation notes:**
- Read the actual sidebar component source (CampaignSidebar.tsx, AppSidebar.tsx) to get the exact link text and active state patterns
- Per D-13: Domain-based naming, no phase prefix
- Per D-15: Serial within the describe block
- Sidebar links may use different text than expected -- read the component source to determine exact link names
- The sidebar may use a Sheet/drawer pattern on mobile -- read the layout component to understand the responsive behavior
  </action>
  <verify>
    <automated>cd /home/kwhatcher/projects/civicpulse/run-api/web && npx playwright test navigation --reporter=line 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - web/e2e/navigation.spec.ts exists
    - File contains `NAV-01` through `NAV-03` in test names or comments
    - File contains assertions for at least 7 campaign sidebar links (Dashboard, Voters, Canvassing, Phone Banking, Surveys, Volunteers, Settings)
    - File contains assertions for at least 3 org navigation links
    - File contains `page.goBack()` for back navigation test
    - File contains `waitForURL` or `toHaveURL` assertions for URL verification
    - File contains `test.describe.serial`
    - Playwright test command exits without syntax errors
  </acceptance_criteria>
  <done>navigation.spec.ts contains 3 test cases covering NAV-01 (campaign sidebar with 7+ links and mobile collapse), NAV-02 (org navigation with 3+ links), and NAV-03 (breadcrumb/back navigation and direct URL entry)</done>
</task>

</tasks>

<verification>
- `ls web/e2e/cross-cutting.spec.ts web/e2e/navigation.spec.ts` confirms both files exist
- `grep -c "test(" web/e2e/cross-cutting.spec.ts` shows 6 test cases
- `grep -c "test(" web/e2e/navigation.spec.ts` shows 3 test cases
- `grep "CROSS-\|UI-\|NAV-" web/e2e/cross-cutting.spec.ts web/e2e/navigation.spec.ts` confirms all test case IDs
- `cd web && npx playwright test cross-cutting navigation --reporter=line` runs without syntax errors
</verification>

<success_criteria>
- cross-cutting.spec.ts and navigation.spec.ts exist with 9 combined test cases covering all E2E-21 requirements
- Both specs run under owner auth (unsuffixed) at desktop viewport
- Empty state tests use API-created fresh campaign
- Loading skeleton tests use route interception to delay API response
- Form guard tests verify useBlocker dialog
- Navigation tests verify all sidebar links and back navigation
- Specs run without syntax errors
</success_criteria>

<output>
After completion, create `.planning/phases/60-e2e-field-mode-cross-cutting-validation/60-02-SUMMARY.md`
</output>
