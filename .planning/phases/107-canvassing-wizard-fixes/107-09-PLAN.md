---
phase: 107
plan: 09
type: execute
wave: 5
depends_on: ["107-01", "107-02", "107-03", "107-04", "107-05", "107-06", "107-07", "107-08"]
files_modified:
  - .planning/phases/107-canvassing-wizard-fixes/107-VERIFICATION-RESULTS.md
autonomous: true
requirements: [CANV-01, CANV-02, CANV-03, FORMS-01]
must_haves:
  truths:
    - "`uv run ruff check .` exits 0"
    - "`uv run pytest` exits 0"
    - "`cd web && npx vitest run` exits 0"
    - "`cd web && ./scripts/run-e2e.sh` exits 0 on a fresh run"
    - "107-VERIFICATION-RESULTS.md documents each suite's exit code, test counts, and run timestamps"
    - "The doc cross-references the must_haves from Plans 01-08 and confirms each is met"
  artifacts:
    - path: ".planning/phases/107-canvassing-wizard-fixes/107-VERIFICATION-RESULTS.md"
      provides: "Phase 107 exit-gate document analogous to 106-EXIT-GATE.md"
  key_links:
    - from: ".planning/phases/107-canvassing-wizard-fixes/107-VERIFICATION-RESULTS.md"
      to: "pytest + vitest + playwright full-suite results"
      via: "stdout snippets + exit codes + timestamps"
      pattern: "exit 0"
---

<objective>
Run all three test suites (pytest, vitest, Playwright via run-e2e.sh) on a
clean tree after all phase 107 code landed, and record the results in
`107-VERIFICATION-RESULTS.md`. This is the phase 107 analog of
`106-EXIT-GATE.md`.

Purpose: Prove phase 107 is green across the full trustworthy baseline from
phase 106, with no regressions.
Output: One markdown document with exit codes, test counts, timestamps, and
must-have cross-reference.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/106-test-baseline-trustworthiness/106-EXIT-GATE.md
@.planning/phases/107-canvassing-wizard-fixes/107-CONTEXT.md
@.planning/phases/107-canvassing-wizard-fixes/107-01-PLAN.md
@.planning/phases/107-canvassing-wizard-fixes/107-02-PLAN.md
@.planning/phases/107-canvassing-wizard-fixes/107-03-PLAN.md
@.planning/phases/107-canvassing-wizard-fixes/107-04-PLAN.md
@.planning/phases/107-canvassing-wizard-fixes/107-05-PLAN.md
@.planning/phases/107-canvassing-wizard-fixes/107-06-PLAN.md
@.planning/phases/107-canvassing-wizard-fixes/107-07-PLAN.md
@.planning/phases/107-canvassing-wizard-fixes/107-08-PLAN.md
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run all 3 suites and write 107-VERIFICATION-RESULTS.md</name>
  <files>.planning/phases/107-canvassing-wizard-fixes/107-VERIFICATION-RESULTS.md</files>
  <read_first>
    - .planning/phases/106-test-baseline-trustworthiness/106-EXIT-GATE.md (format to mirror)
    - .planning/phases/107-canvassing-wizard-fixes/107-CONTEXT.md §Decisions (extract must_haves to verify)
    - CLAUDE.md (E2E run policy — always `web/scripts/run-e2e.sh`; Python always `uv run`)
  </read_first>
  <action>
**Step 1 — Pre-flight sanity.**

Confirm all Plans 01-08 have landed by checking the files they modified exist and are in their expected state:
```
test -f web/src/hooks/usePrefersReducedMotion.ts
test -f web/src/types/canvassing.test.ts
test -f tests/integration/test_door_knocks.py
test -f web/src/components/field/InlineSurvey.test.tsx
test -f web/e2e/canvassing-wizard.spec.ts
test -f .planning/phases/107-canvassing-wizard-fixes/107-FORMS-AUDIT.md
grep -q "HOUSE_LEVEL_OUTCOMES" web/src/hooks/useCanvassingWizard.ts
grep -q "notesRequired" web/src/components/field/InlineSurvey.tsx
grep -q "Skipped — Undo" web/src/hooks/useCanvassingWizard.ts
```
Any failure here means a prior plan didn't land — STOP and report.

**Step 2 — Ruff.**
```
uv run ruff check . 2>&1 | tee /tmp/107-ruff.log
```
Capture exit code. Expected: 0.

**Step 3 — Pytest (full suite).**
```
uv run pytest 2>&1 | tee /tmp/107-pytest.log
```
Capture exit code + summary line (the `====== N passed, M skipped in ...s ======` at the end).

**Step 4 — Vitest (full suite).**
```
cd web && npx vitest run 2>&1 | tee /tmp/107-vitest.log
```
Capture exit code + summary. Expected: all pass.

**Step 5 — Playwright via run-e2e.sh (full suite).**
```
cd web && ./scripts/run-e2e.sh 2>&1 | tee /tmp/107-playwright.log
```
Capture exit code, pass/fail/skip counts, duration. Default is 8 workers per phase 106 D-13. Record the new line appended to `web/e2e-runs.jsonl` (the last line of that file after the run).

**Step 6 — Write the verification document.**

Create `.planning/phases/107-canvassing-wizard-fixes/107-VERIFICATION-RESULTS.md` using this exact structure (fill in actual values):

```markdown
# Phase 107 — Verification Results

**Phase:** 107-canvassing-wizard-fixes
**Requirements:** CANV-01, CANV-02, CANV-03, FORMS-01
**Run date:** <ISO-8601 UTC timestamp>
**Run host:** <short hostname>
**Git SHA at verification:** <output of `git rev-parse HEAD`>

## Suite Results

| Suite | Command | Exit | Summary | Duration |
|-------|---------|------|---------|----------|
| Ruff  | `uv run ruff check .` | <0> | <`All checks passed!` or counts> | <s> |
| Pytest | `uv run pytest` | <0> | <`N passed, M skipped`> | <s> |
| Vitest | `cd web && npx vitest run` | <0> | <`N passed`> | <s> |
| Playwright | `cd web && ./scripts/run-e2e.sh` | <0> | <`N passed, M skipped`> | <s> |

## run-e2e.sh Log Entry

```json
<paste the last line of web/e2e-runs.jsonl>
```

## Must-Have Verification (from Plans 01-08)

| # | Must-have | Plan | Evidence |
|---|-----------|------|----------|
| 1 | `usePrefersReducedMotion` hook exists and tests pass | 107-01 | 5 vitest tests in usePrefersReducedMotion.test.ts green |
| 2 | `HOUSE_LEVEL_OUTCOMES` set exists | 107-02 | 5 vitest tests in canvassing.test.ts green |
| 3 | Door-knock POST accepts empty/null notes | 107-03 | 4 pytest integration tests in test_door_knocks.py green |
| 4 | CANV-01: multi-voter household advances on house-level outcome | 107-04 | "house-level outcome on multi-voter household advances immediately" unit test + `canvassing-wizard.spec.ts` CANV-01 E2E |
| 5 | Triple-channel feedback wired with reduced-motion honor | 107-04 | Toast + vibrate + focus-ref unit tests green; aria-live region present |
| 6 | CANV-02: Skip advances synchronously with isPending guard | 107-05 | "double-tap Skip" unit test + `canvassing-wizard.spec.ts` CANV-02 E2E; `setTimeout(..., 300)` removed |
| 7 | CANV-03: `notesRequired` prop decoupled from `isControlled` | 107-06 | InlineSurvey.test.tsx 7 tests green; both call sites pass `notesRequired={false}` |
| 8 | FORMS-01 audit document exists | 107-07 | 107-FORMS-AUDIT.md with 2 REMOVED + 2 KEPT rows |
| 9 | Phase 107 E2E spec covers CANV-01/02/03 + FORMS-01 D-17 | 107-08 | canvassing-wizard.spec.ts green via run-e2e.sh |
| 10 | Full 3-suite green | 107-09 | This document |

## Roadmap Success Criteria (from ROADMAP.md §Phase 107)

- [x] (1) Submitting an outcome on the active house automatically transitions the wizard to the next house — verified by "house-level outcome on multi-voter household advances immediately" unit test and canvassing-wizard.spec.ts CANV-01.
- [x] (2) Tapping "Skip house" advances past the current house in one tap — verified by "double-tap Skip" guard test and canvassing-wizard.spec.ts CANV-02.
- [x] (3) Any outcome saves with empty notes — verified by `test_door_knocks.py` integration tests, InlineSurvey.test.tsx notesRequired=false tests, and canvassing-wizard.spec.ts CANV-03.
- [x] (4) FORMS-01 audit doc exists — 107-FORMS-AUDIT.md.
- [x] (5) Unit + integration + E2E cover all changes — plans 01-08 coverage matrix above.

## Regression Check vs Phase 106

- Pytest count delta: <phase 106 was 1114 passing, current count = ___>
- Vitest count delta: <phase 106 was 675 passing, current count = ___>
- Playwright: no new flakes; rbac cluster still green.

## Conclusion

Phase 107 exit gate: **<PASS | FAIL>**.

If any suite exited non-zero, this document records the failure, the logs are preserved at `/tmp/107-*.log`, and the plan returns to the failing gate for gap closure. Do NOT mark the phase complete on red.
```

**Step 7 — Lint the markdown and commit nothing else.** The only file modified by this plan is `107-VERIFICATION-RESULTS.md`.
  </action>
  <verify>
    <automated>test -f .planning/phases/107-canvassing-wizard-fixes/107-VERIFICATION-RESULTS.md && grep -q "Phase 107 — Verification Results" .planning/phases/107-canvassing-wizard-fixes/107-VERIFICATION-RESULTS.md && grep -q "PASS" .planning/phases/107-canvassing-wizard-fixes/107-VERIFICATION-RESULTS.md</automated>
  </verify>
  <acceptance_criteria>
    - `uv run ruff check .` exits 0 during this task
    - `uv run pytest` exits 0 during this task
    - `cd web && npx vitest run` exits 0 during this task
    - `cd web && ./scripts/run-e2e.sh` exits 0 during this task
    - `test -f .planning/phases/107-canvassing-wizard-fixes/107-VERIFICATION-RESULTS.md` exits 0
    - `grep -c "exit 0\\|Exit.*0\\| 0 \\|<0>" .planning/phases/107-canvassing-wizard-fixes/107-VERIFICATION-RESULTS.md` returns >= 4 (one per suite; placeholder `<0>` accepted as a transitional marker but should be replaced with actual `0`)
    - `grep -n "107-FORMS-AUDIT.md" .planning/phases/107-canvassing-wizard-fixes/107-VERIFICATION-RESULTS.md` returns a match
    - `grep -n "Phase 107 exit gate: \\*\\*PASS\\*\\*" .planning/phases/107-canvassing-wizard-fixes/107-VERIFICATION-RESULTS.md` returns a match (OR the doc records a documented FAIL and the executor STOPS)
    - A new entry exists in `web/e2e-runs.jsonl` after the Playwright run
  </acceptance_criteria>
  <done>All 4 suites green; verification document committed; phase 107 ready for `/gsd-verify-work`.</done>
</task>

</tasks>

<verification>
- All four suite commands exit 0.
- Verification document exists and records PASS.
</verification>

<success_criteria>
Phase 107 exit gate passed. CANV-01, CANV-02, CANV-03, FORMS-01 all verified end-to-end.
</success_criteria>

<output>
After completion, create `.planning/phases/107-canvassing-wizard-fixes/107-09-SUMMARY.md`
</output>
