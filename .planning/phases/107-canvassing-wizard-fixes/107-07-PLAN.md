---
phase: 107
plan: 07
type: execute
wave: 4
depends_on: ["107-04", "107-05", "107-06"]
files_modified:
  - .planning/phases/107-canvassing-wizard-fixes/107-FORMS-AUDIT.md
autonomous: true
requirements: [FORMS-01]
must_haves:
  truths:
    - "107-FORMS-AUDIT.md exists at the phase path and enumerates every save-blocking validator found in field-mode (canvassing + phone banking)"
    - "Each row lists: file + line, field, current validator, intent, disposition, justification, tests added"
    - "Every D-19 item is represented: InlineSurvey notes (canvassing), SmsComposer body, SmsBulkSendSheet body, phone-banking call notes"
    - "Dispositions match D-19 exactly: InlineSurvey notes=REMOVED, SmsComposer=KEPT, SmsBulkSendSheet=KEPT, phone-banking notes=REMOVED"
  artifacts:
    - path: ".planning/phases/107-canvassing-wizard-fixes/107-FORMS-AUDIT.md"
      provides: "FORMS-01 audit deliverable per D-12"
  key_links:
    - from: ".planning/phases/107-canvassing-wizard-fixes/107-FORMS-AUDIT.md"
      to: "Plan 06 code changes (InlineSurvey decoupling) + Plans 04/05 E2E coverage"
      via: "audit rows cite the exact files and tests"
      pattern: "InlineSurvey.*REMOVED"
---

<objective>
Produce the FORMS-01 audit deliverable per D-12 at
`.planning/phases/107-canvassing-wizard-fixes/107-FORMS-AUDIT.md`. Per D-19
the audit surface is exactly 4 hits pre-classified by the researcher. This
plan formalizes the audit into the documented artifact; the code changes
themselves already landed in Plan 06 (Wave 2).

Purpose: Close FORMS-01 with the documented audit trail required by D-12.
Output: One markdown audit file.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/107-canvassing-wizard-fixes/107-CONTEXT.md
@.planning/phases/107-canvassing-wizard-fixes/107-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write 107-FORMS-AUDIT.md with the 4 pre-classified hits (D-12, D-14, D-19)</name>
  <files>.planning/phases/107-canvassing-wizard-fixes/107-FORMS-AUDIT.md</files>
  <read_first>
    - .planning/phases/107-canvassing-wizard-fixes/107-CONTEXT.md §Decisions D-12, D-13, D-14, D-15, D-19
    - .planning/phases/107-canvassing-wizard-fixes/107-RESEARCH.md §4 (the full inventory table + the "What is NOT a hit" list for kept-for-trail items)
    - web/src/components/field/InlineSurvey.tsx (for line numbers after Plan 06 landed)
    - web/src/components/field/SmsComposer.tsx (line 70 area)
    - web/src/components/field/SmsBulkSendSheet.tsx (line 93 area)
  </read_first>
  <action>
Create `.planning/phases/107-canvassing-wizard-fixes/107-FORMS-AUDIT.md` with the following exact content (adapt line numbers to match the current state of the files AFTER Plan 06 merged):

```markdown
# Phase 107 — Field-Mode Form Requiredness Audit (FORMS-01)

**Phase:** 107-canvassing-wizard-fixes
**Requirement:** FORMS-01 — Field-mode form audit identifies every `required`
validator and every form field in the canvassing and phone-banking flows;
each is reviewed against its UX intent and over-eager validations are removed.
**Decision references:** D-12 (audit format), D-13 (scope = canvassing +
phone-banking field mode only), D-14 (over-eager criterion), D-15 (product
bugs get todos, not fixes), D-19 (phone-banking call notes disposition).
**Audited on:** 2026-04-11 (post Plans 04/05/06).
**Audit method:** `rg` across `web/src/components/field/*.tsx` (33 files) and
`web/src/routes/field/$campaignId/**/*.tsx` (3 route files). Patterns:
`required`, `\.min\(1`, `trim\(\)\.length`, `disabled=\{`, `canSubmit`,
`isValid`, `useForm`, `zodResolver`. See RESEARCH.md §4 for the full
methodology.

## D-14 criterion (binary)

A save-blocking validator is JUSTIFIED only if absence of the field would
cause **data integrity loss** (the API genuinely can't process the record
without it) OR **real-world harm** (e.g., sending an empty SMS to a voter).
Everything else is over-eager.

## Save-blocking validators (the actual audit)

| # | File | Line | Field | Current validator | Intent | Disposition | Justification | Tests added |
|---|------|------|-------|-------------------|--------|-------------|---------------|-------------|
| 1 | `web/src/components/field/InlineSurvey.tsx` | 135, 152 | `notes` (canvassing path) | `requiresNotes = isControlled` → `notes.trim().length > 0` blocks save button | Block save until volunteer types notes | **REMOVED** | CANV-03 bug. Notes are bonus context; the outcome IS the record. D-09 decoupling via explicit `notesRequired?: boolean` prop (default `false`). Canvassing passes `false`. | `InlineSurvey.test.tsx` (7 tests, Plan 06) + `useCanvassingWizard.test.ts` "empty notes on handleSubmitContact" (Plan 04) + `canvassing-wizard.spec.ts` empty-notes E2E (Plan 08) |
| 2 | `web/src/components/field/SmsComposer.tsx` | 70 | `body` (SMS message body) | `body.trim().length === 0` disables Send | Prevent sending empty SMS | **KEPT** | Safety-critical per D-14: sending an empty SMS is real-world harm (recipient confusion + carrier flagging + wasted credit). | n/a — behavior unchanged. |
| 3 | `web/src/components/field/SmsBulkSendSheet.tsx` | 93 | `body` (bulk SMS body) | `body.trim().length === 0` disables Queue | Prevent bulk-sending empty SMS | **KEPT** | Safety-critical per D-14: same rationale as #2, amplified by the bulk-send blast radius. | n/a — behavior unchanged. |
| 4 | `web/src/components/field/InlineSurvey.tsx` | 292 (controlled-mode `field-call-notes` block) | `notes` (phone-banking call notes) | Was coupled to `requiresNotes = isControlled` before Plan 06; now gated on explicit `notesRequired` prop | Block save until volunteer types call notes | **REMOVED** (per D-19) | D-19 extends the canvassing rule to phone banking: notes are optional, the volunteer marks the call outcome and may add notes but isn't blocked. "Consistency with CANV-03; outcome IS the record, notes are bonus context." Phone-banking route explicitly passes `notesRequired={false}` in Plan 06. | `InlineSurvey.test.tsx` regression guard test `"isControlled=true with notesRequired=false does NOT require notes"` (Plan 06) + `canvassing-wizard.spec.ts` phone-banking empty-notes coverage if added (Plan 08) |

## Kept-for-trail items (NOT save-blocking, documented for completeness)

| File | Line | Field | What it is | Why it's not a hit |
|------|------|-------|------------|--------------------|
| `web/src/components/field/InlineSurvey.tsx` | 147 | `isSurveyComplete` | Gates controlled-mode submit when survey questions exist but are unanswered | Survey answers are data integrity — the survey response is the point of the call. **KEPT.** Do not confuse with the notes validator. |
| `web/src/components/field/OutcomeGrid.tsx` | 27 | `disabled={disabled}` | While-saving guard (+ while-skip-pending per Plan 05) | This is a race-guard, not a requiredness validator. Not in FORMS-01 scope. |
| `web/src/components/field/PhoneNumberList.tsx` | 276, 308 | `disabled={isDisabled}` | Gated on calling-hours and active-call state | Operational gate, not requiredness. Not in FORMS-01 scope. |

## Form libraries checked

- **`useForm` / `zodResolver`**: NOT USED in field-mode per RESEARCH.md §4. The field-mode wizard uses local `useState`; this is intentional and CONTEXT.md §code_context confirms it is out of scope to refactor.
- **HTML `required` attribute**: ZERO occurrences in `web/src/components/field/` per grep.

## Audit-discovered product bugs (D-15 → `.planning/todos/pending/`)

None surfaced. The audit is binary — `REMOVED` or `KEPT` — and all four hits have clear dispositions per D-14 and D-19. If the user on review flips the D-19 disposition back to "keep phone-banking notes required," the change is a single-line flip in `web/src/routes/field/$campaignId/phone-banking.tsx` and a row update here.

## Summary

- **Total save-blocking validators in field mode:** 4
- **Removed:** 2 (InlineSurvey canvassing notes, phone-banking call notes)
- **Kept:** 2 (SmsComposer body, SmsBulkSendSheet body — both safety-critical)
- **Net effect:** Volunteers can save canvassing outcomes and phone-banking call records without typing notes. SMS composition still requires a body.
- **Code landed in:** Plan 06 (`107-06-PLAN.md`). This document is the audit trail.

## Cross-references

- Requirements: FORMS-01 (milestone v1.18, traceability row in REQUIREMENTS.md:92)
- Decision log: CONTEXT.md §Decisions D-12, D-13, D-14, D-15, D-19
- Research log: RESEARCH.md §4 (full inventory) and §6 risk #5 (phone-banking call-site caveat)
- Code: `web/src/components/field/InlineSurvey.tsx`, `web/src/routes/field/$campaignId/canvassing.tsx`, `web/src/routes/field/$campaignId/phone-banking.tsx`
- Tests: `web/src/components/field/InlineSurvey.test.tsx`, `web/src/hooks/useCanvassingWizard.test.ts`, `web/e2e/canvassing-wizard.spec.ts` (Plan 08)
```

Verify line numbers by opening each cited file (InlineSurvey.tsx, SmsComposer.tsx, SmsBulkSendSheet.tsx) BEFORE committing and updating any row whose line number has drifted after Plans 04/05/06 landed.
  </action>
  <verify>
    <automated>test -f .planning/phases/107-canvassing-wizard-fixes/107-FORMS-AUDIT.md && grep -q "FORMS-01" .planning/phases/107-canvassing-wizard-fixes/107-FORMS-AUDIT.md && grep -q "InlineSurvey" .planning/phases/107-canvassing-wizard-fixes/107-FORMS-AUDIT.md && grep -q "SmsComposer" .planning/phases/107-canvassing-wizard-fixes/107-FORMS-AUDIT.md && grep -q "SmsBulkSendSheet" .planning/phases/107-canvassing-wizard-fixes/107-FORMS-AUDIT.md && grep -q "phone-banking call notes" .planning/phases/107-canvassing-wizard-fixes/107-FORMS-AUDIT.md && grep -q "REMOVED" .planning/phases/107-canvassing-wizard-fixes/107-FORMS-AUDIT.md && grep -q "KEPT" .planning/phases/107-canvassing-wizard-fixes/107-FORMS-AUDIT.md</automated>
  </verify>
  <acceptance_criteria>
    - `test -f .planning/phases/107-canvassing-wizard-fixes/107-FORMS-AUDIT.md` exits 0
    - `grep -c "| \*\*REMOVED\*\*" .planning/phases/107-canvassing-wizard-fixes/107-FORMS-AUDIT.md` returns 2
    - `grep -c "| \*\*KEPT\*\*" .planning/phases/107-canvassing-wizard-fixes/107-FORMS-AUDIT.md` returns 2
    - `grep -n "InlineSurvey.tsx.*135" .planning/phases/107-canvassing-wizard-fixes/107-FORMS-AUDIT.md` returns a match
    - `grep -n "SmsComposer.tsx" .planning/phases/107-canvassing-wizard-fixes/107-FORMS-AUDIT.md` returns a match
    - `grep -n "SmsBulkSendSheet.tsx" .planning/phases/107-canvassing-wizard-fixes/107-FORMS-AUDIT.md` returns a match
    - `grep -n "phone-banking call notes" .planning/phases/107-canvassing-wizard-fixes/107-FORMS-AUDIT.md` returns a match
    - `grep -n "D-19" .planning/phases/107-canvassing-wizard-fixes/107-FORMS-AUDIT.md` returns a match
    - `grep -n "D-14" .planning/phases/107-canvassing-wizard-fixes/107-FORMS-AUDIT.md` returns a match
  </acceptance_criteria>
  <done>Audit file exists with all 4 D-19 rows, correct dispositions, justifications, and test cross-references.</done>
</task>

</tasks>

<verification>
- File exists and contains the 4-row audit table.
- All decision IDs (D-12, D-14, D-19) are cited.
</verification>

<success_criteria>
FORMS-01 deliverable produced per D-12; all code changes already landed via Plan 06.
</success_criteria>

<output>
After completion, create `.planning/phases/107-canvassing-wizard-fixes/107-07-SUMMARY.md`
</output>
