---
phase: 62-ci-auth-policy-automation
plan: 02
type: execute
wave: 2
depends_on:
  - 62-01
files_modified:
  - .github/workflows/pr.yml
  - web/scripts/run-e2e.sh
autonomous: true
requirements:
  - INFRA-03
  - VAL-01
must_haves:
  truths:
    - "CI verifies no-MFA policy presence before running Playwright shards."
    - "CI fails immediately with clear message when policy verification fails."
    - "Auth bootstrap determinism is checked via setup-only Playwright projects before the full suite path."
  artifacts:
    - path: ".github/workflows/pr.yml"
      provides: "Policy verification + auth setup smoke gates in CI"
      contains: "strict policy verify step and setup-only Playwright command"
    - path: "web/scripts/run-e2e.sh"
      provides: "Local parity hook for strict policy verification before E2E run"
      contains: "strict policy command invocation path"
  key_links:
    - from: ".github/workflows/pr.yml"
      to: "scripts/create-e2e-users.py"
      via: "docker compose exec policy verification command"
      pattern: "create-e2e-users.py --verify-policy-only --strict-policy"
    - from: ".github/workflows/pr.yml"
      to: "web/playwright.config.ts"
      via: "Playwright setup project execution"
      pattern: "playwright test --project=.*auth"
---

<objective>
Wire CI and local E2E entrypoints to enforce strict no-MFA policy verification and deterministic auth bootstrap before broad E2E execution.

Purpose: Close infrastructure drift that causes non-deterministic auth setup in fresh environments.
Output: Updated CI workflow and local runner gating policy/auth setup checks.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/62-ci-auth-policy-automation/62-RESEARCH.md
@.planning/phases/62-ci-auth-policy-automation/62-VALIDATION.md
@.github/workflows/pr.yml
@web/scripts/run-e2e.sh
@scripts/create-e2e-users.py

<interfaces>
From .github/workflows/pr.yml:
```yaml
jobs:
  e2e-tests:
    steps:
      - name: Create E2E test users
        run: docker compose exec -T api bash -c "PYTHONPATH=/home/app python scripts/create-e2e-users.py"
      - name: Run E2E tests
        run: npx playwright test --shard=${{ matrix.shardIndex }}/${{ matrix.shardTotal }}
```

From web/scripts/run-e2e.sh:
```bash
ensure_e2e_users() {
  docker compose ... python scripts/create-e2e-users.py
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add strict CI policy verification + auth-setup smoke gate</name>
  <files>.github/workflows/pr.yml</files>
  <read_first>
    - .github/workflows/pr.yml
    - scripts/create-e2e-users.py
    - web/playwright.config.ts
  </read_first>
  <action>Update `e2e-tests` job steps to: (1) keep user provisioning step, (2) add a new step immediately after provisioning that runs `docker compose exec -T api bash -c "PYTHONPATH=/home/app python scripts/create-e2e-users.py --verify-policy-only --strict-policy"`, and (3) add a setup-only auth smoke command before shard execution: `npx playwright test --project=setup-owner --project=setup-admin --project=setup-manager --project=setup-volunteer --project=setup-viewer --workers=1`. Keep current shard command unchanged after the smoke gate. Ensure failure messages in new step names are explicit about no-MFA policy drift.</action>
  <acceptance_criteria>
    - `pr.yml` contains `--verify-policy-only --strict-policy` command in e2e-tests job.
    - `pr.yml` contains setup-only Playwright auth project command before shard test command.
    - Existing shard command remains present and unchanged in behavior.
  </acceptance_criteria>
  <verify>
    <automated>rg -n "verify-policy-only --strict-policy|playwright test --project=setup-owner|--project=setup-viewer|--shard=" .github/workflows/pr.yml</automated>
  </verify>
  <done>CI now enforces policy presence and auth-setup determinism before expensive E2E shard execution.</done>
</task>

<task type="auto">
  <name>Task 2: Mirror strict policy gate in local run-e2e helper</name>
  <files>web/scripts/run-e2e.sh</files>
  <read_first>
    - web/scripts/run-e2e.sh
    - scripts/create-e2e-users.py
  </read_first>
  <action>Extend `ensure_e2e_users()` in `web/scripts/run-e2e.sh` to run a strict policy verification command after user ensure succeeds: `docker compose -f "$WEB_DIR/../docker-compose.yml" exec -T api bash -c "PYTHONPATH=/home/app PAT_PATH=/home/app/zitadel-data/pat.txt python scripts/create-e2e-users.py --verify-policy-only --strict-policy"`. If strict verification fails, print a clear error and exit non-zero to prevent running tests with policy drift. Keep existing user-creation fallback behavior intact.</action>
  <acceptance_criteria>
    - `run-e2e.sh` includes strict policy verification invocation with both flags.
    - Script exits early on strict-policy failure path before Playwright command execution.
    - Existing `create-e2e-users.py` provisioning call remains in place.
  </acceptance_criteria>
  <verify>
    <automated>rg -n "verify-policy-only --strict-policy|ensure_e2e_users|exit 1" web/scripts/run-e2e.sh</automated>
  </verify>
  <done>Local E2E runner enforces the same auth-policy prerequisite as CI for deterministic parity.</done>
</task>

</tasks>

<verification>
Use ripgrep commands from each task to confirm strict policy and auth setup gating are present in workflow/script definitions.
</verification>

<success_criteria>
- CI workflow fails fast when no-MFA policy verification fails.
- CI runs auth setup projects deterministically before sharded suite.
- Local run helper mirrors strict policy gate for CI/local parity.
</success_criteria>

<output>
After completion, create `.planning/phases/62-ci-auth-policy-automation/62-02-SUMMARY.md`
</output>
