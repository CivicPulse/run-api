---
phase: 62-ci-auth-policy-automation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/create-e2e-users.py
  - tests/unit/test_create_e2e_users_policy.py
autonomous: true
requirements:
  - INFRA-01
  - VAL-01
must_haves:
  truths:
    - "Running create-e2e-users.py on a fresh ZITADEL instance applies org login policy without manual console work."
    - "The policy helper fails with explicit actionable error text when policy ensure/verify cannot complete."
    - "Policy ensure behavior is regression-protected by automated unit tests."
  artifacts:
    - path: "scripts/create-e2e-users.py"
      provides: "Idempotent org login-policy ensure + strict verification code path"
      contains: "policy ensure/verify helper functions and strict failure behavior"
    - path: "tests/unit/test_create_e2e_users_policy.py"
      provides: "Unit coverage for policy search/create/update and failure cases"
      contains: "tests for success, idempotent no-op, and fail-fast error paths"
  key_links:
    - from: "scripts/create-e2e-users.py"
      to: "ZITADEL management API"
      via: "httpx api_call with bearer PAT + Host header"
      pattern: "api_call\(.*policy|login"
    - from: "tests/unit/test_create_e2e_users_policy.py"
      to: "scripts/create-e2e-users.py"
      via: "mocked httpx responses validating helper decisions"
      pattern: "assert.*policy"
---

<objective>
Automate org-level no-MFA login policy management inside the E2E provisioning script and verify it with deterministic unit tests.

Purpose: Remove manual policy setup drift so fresh CI/local auth bootstrap is reproducible.
Output: Policy ensure/verify logic in provisioning script plus dedicated unit tests.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/62-ci-auth-policy-automation/62-RESEARCH.md
@.planning/phases/62-ci-auth-policy-automation/62-VALIDATION.md
@scripts/create-e2e-users.py
@scripts/bootstrap-zitadel.py
@.planning/v1.7-MILESTONE-AUDIT.md

<interfaces>
From scripts/create-e2e-users.py:
```python
def api_call(client: httpx.Client, method: str, path: str, pat: str, json: dict | None = None, *, allow_conflict: bool = False) -> dict | None
def find_project_id(client: httpx.Client, pat: str) -> str
def main() -> None
```

From scripts/bootstrap-zitadel.py:
```python
def api_call(client: httpx.Client, method: str, path: str, pat: str, json: dict | None = None, *, allow_conflict: bool = False) -> dict | None
def _host_header() -> str
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add policy-helper test coverage scaffold (RED → GREEN)</name>
  <files>tests/unit/test_create_e2e_users_policy.py</files>
  <read_first>
    - tests/unit/test_lifespan.py
    - tests/unit/test_security.py
    - scripts/create-e2e-users.py
  </read_first>
  <behavior>
    - Test 1: when policy search returns a compliant policy (no MFA enforcement), helper returns success without issuing create/update.
    - Test 2: when no policy exists, helper issues create call with no-MFA fields then verifies created state.
    - Test 3: when API returns non-2xx during strict verify, helper raises SystemExit with message containing endpoint + status.
  </behavior>
  <action>Create `tests/unit/test_create_e2e_users_policy.py` with pytest unit tests that mock `httpx.Client.request` responses for policy search/create/update flows. Use concrete expected payload assertions for no-MFA target state (empty or disabled second-factor configuration plus equivalent org login policy flag values) and assert strict failure exits include actionable context text. Keep fixtures/helpers local to this test file and follow existing AsyncMock/MagicMock style used in unit tests.</action>
  <acceptance_criteria>
    - File `tests/unit/test_create_e2e_users_policy.py` exists.
    - Test file contains at least 3 tests covering compliant no-op, create path, and strict failure path.
    - `uv run pytest tests/unit/test_create_e2e_users_policy.py -x -q` exits 0 after implementation.
  </acceptance_criteria>
  <verify>
    <automated>uv run pytest tests/unit/test_create_e2e_users_policy.py -x -q</automated>
  </verify>
  <done>Policy helper behaviors are encoded in deterministic tests and run green locally.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement org login policy ensure/verify in provisioning script</name>
  <files>scripts/create-e2e-users.py</files>
  <read_first>
    - scripts/create-e2e-users.py
    - scripts/bootstrap-zitadel.py
    - .planning/v1.7-MILESTONE-AUDIT.md
  </read_first>
  <behavior>
    - Test 1: helper detects existing compliant policy and does not mutate state.
    - Test 2: helper creates or updates policy when missing/non-compliant, then re-reads and confirms compliance.
    - Test 3: strict mode exits non-zero with clear remediation when policy cannot be verified.
  </behavior>
  <action>Add policy helpers to `scripts/create-e2e-users.py` to explicitly search and enforce org-level login policy for no-MFA auth setup before user creation. Reuse existing `api_call`/Host-header conventions from bootstrap script. Add strict CLI mode flag parsing (`--verify-policy-only` and `--strict-policy`) so CI can run policy verification without full user provisioning when needed. Ensure `main()` runs policy ensure before iterating users and raises `SystemExit(1)` with explicit endpoint/status/body details on verification failure. Keep script idempotent (safe repeated runs) and preserve current user provisioning output.</action>
  <acceptance_criteria>
    - `scripts/create-e2e-users.py` contains dedicated policy ensure/verify helpers invoked before user creation.
    - Script supports `--verify-policy-only` and `--strict-policy` flags.
    - Running `uv run pytest tests/unit/test_create_e2e_users_policy.py -x -q` passes with new logic.
    - `uv run ruff check scripts/create-e2e-users.py tests/unit/test_create_e2e_users_policy.py` passes.
  </acceptance_criteria>
  <verify>
    <automated>uv run pytest tests/unit/test_create_e2e_users_policy.py -x -q && uv run ruff check scripts/create-e2e-users.py tests/unit/test_create_e2e_users_policy.py</automated>
  </verify>
  <done>Provisioning script deterministically enforces/validates no-MFA org policy and fails fast on policy drift.</done>
</task>

</tasks>

<verification>
Run policy unit tests and Ruff checks exactly as listed in task verify blocks.
</verification>

<success_criteria>
- Fresh ZITADEL bootstrap path has script-level no-MFA policy enforcement with no manual console dependency.
- Policy behavior has automated regression coverage.
- Strict verification failure mode is explicit and actionable.
</success_criteria>

<output>
After completion, create `.planning/phases/62-ci-auth-policy-automation/62-01-SUMMARY.md`
</output>
