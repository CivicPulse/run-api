---
phase: 07-integration-wiring-fixes
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/main.py
  - app/db/base.py
  - tests/unit/test_lifespan.py
  - tests/unit/test_model_coverage.py
autonomous: true
requirements:
  - AUTH-02
  - AUTH-03
  - AUTH-05
  - AUTH-07
  - PHONE-01
  - PHONE-02
  - PHONE-03
  - PHONE-04
  - PHONE-05

must_haves:
  truths:
    - "App startup initializes ZitadelService on app.state so campaign CRUD, invite accept, and member management work without AttributeError"
    - "App startup fails fast with RuntimeError when ZITADEL config is missing, credentials are invalid, or ZITADEL is unreachable"
    - "All model files in app/models/ are imported in app/db/base.py so Alembic autogenerate discovers all tables"
    - "A regression test prevents future model import drift"
  artifacts:
    - path: "app/main.py"
      provides: "ZitadelService lifespan initialization with fail-fast validation"
      contains: "zitadel_service"
    - path: "app/db/base.py"
      provides: "Complete model imports for Alembic discovery"
      contains: "app.models.call_list"
    - path: "tests/unit/test_lifespan.py"
      provides: "Lifespan wiring tests, startup failure tests, campaign E2E flow test"
      min_lines: 50
    - path: "tests/unit/test_model_coverage.py"
      provides: "Filesystem-based model coverage regression test"
      min_lines: 15
  key_links:
    - from: "app/main.py:lifespan"
      to: "app.state.zitadel_service"
      via: "ZitadelService instantiation + _get_token() validation"
      pattern: "app\\.state\\.zitadel_service\\s*=\\s*zitadel_service"
    - from: "app/db/base.py"
      to: "app/models/call_list.py, app/models/dnc.py, app/models/phone_bank.py"
      via: "import statements after Base definition"
      pattern: "import app\\.models\\.(call_list|dnc|phone_bank)"
    - from: "tests/unit/test_model_coverage.py"
      to: "app/db/base.py"
      via: "filesystem glob vs file content string matching"
      pattern: "import app\\.models\\."
---

<objective>
Fix runtime wiring gaps (INT-01, INT-02, FLOW-01) that prevent campaign creation flow and Alembic model discovery from working correctly.

Purpose: All production code for these features exists but is not wired at startup. These surgical fixes connect existing code so the app functions end-to-end.
Output: Modified app/main.py with ZitadelService lifespan init, complete app/db/base.py model imports, comprehensive tests for both fixes.
</objective>

<execution_context>
@/home/kwhatcher/.claude/get-shit-done/workflows/execute-plan.md
@/home/kwhatcher/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-integration-wiring-fixes/07-CONTEXT.md
@.planning/phases/07-integration-wiring-fixes/07-RESEARCH.md

<interfaces>
<!-- Key types and contracts the executor needs. -->

From app/main.py (current lifespan — lines 20-46):
```python
@asynccontextmanager
async def lifespan(app: FastAPI) -> AsyncGenerator[None]:
    from app.core.security import JWKSManager
    from app.services.storage import StorageService
    from app.tasks.broker import broker
    # JWKSManager, StorageService, broker initialized on app.state
    # ZitadelService is MISSING — this plan adds it
```

From app/services/zitadel.py (constructor and validation method):
```python
class ZitadelService:
    def __init__(self, issuer: str, client_id: str, client_secret: str): ...
    async def _get_token(self) -> str: ...  # raises httpx.HTTPStatusError (401) or ZitadelUnavailableError (connection)
```

From app/core/config.py (settings fields):
```python
zitadel_issuer: str
zitadel_service_client_id: str = ""
zitadel_service_client_secret: str = ""
```

From app/db/base.py (lines 14-29 — 14 model imports, missing 3):
```python
# Currently imported: campaign, campaign_member, import_job, invite, user, voter,
#   voter_contact, voter_interaction, voter_list, turf, walk_list, survey, volunteer, shift
# Missing: call_list, dnc, phone_bank
```

Route handlers that access zitadel_service (already wired, need app.state populated):
- campaigns.py:38 — `request.app.state.zitadel_service` (create)
- campaigns.py:155 — (delete)
- invites.py:120 — (accept)
- members.py:115/170/218 — (assign/remove/transfer)

Existing test mock pattern (from test_api_campaigns.py:138):
```python
app.state.zitadel_service = zitadel  # AsyncMock
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: ZitadelService lifespan init and base.py model imports</name>
  <files>app/main.py, app/db/base.py, tests/unit/test_lifespan.py, tests/unit/test_model_coverage.py</files>
  <behavior>
    - test_zitadel_service_initialized: lifespan sets app.state.zitadel_service to a ZitadelService instance
    - test_startup_fails_missing_config_client_id: RuntimeError raised when zitadel_service_client_id is empty
    - test_startup_fails_missing_config_client_secret: RuntimeError raised when zitadel_service_client_secret is empty
    - test_startup_fails_invalid_credentials: RuntimeError raised when _get_token() returns 401 (httpx.HTTPStatusError)
    - test_startup_fails_unreachable: RuntimeError raised when _get_token() raises ZitadelUnavailableError (connection refused/timeout)
    - test_all_models_imported_in_base: every .py file in app/models/ (except __init__.py) has a matching "import app.models.<name>" in app/db/base.py
  </behavior>
  <action>
    Write tests FIRST (RED), then implement fixes (GREEN):

    **Tests (test_lifespan.py):**
    1. test_zitadel_service_initialized: Mock settings with valid zitadel config, mock ZitadelService._get_token to return successfully, mock StorageService.ensure_bucket, mock broker.startup/shutdown. Run lifespan context manager. Assert app.state.zitadel_service is a ZitadelService instance.
    2. test_startup_fails_missing_config_client_id: Patch settings.zitadel_service_client_id="" and zitadel_service_client_secret="valid". Assert RuntimeError with match "ZITADEL service account not configured".
    3. test_startup_fails_missing_config_client_secret: Same but client_secret="" and client_id="valid".
    4. test_startup_fails_invalid_credentials: Mock _get_token to raise httpx.HTTPStatusError with 401 response. Assert RuntimeError with match about invalid credentials.
    5. test_startup_fails_unreachable: Mock _get_token to raise ZitadelUnavailableError. Assert RuntimeError with match about unreachable.

    **Tests (test_model_coverage.py):**
    6. test_all_models_imported_in_base: Use pathlib.Path.glob("*.py") on app/models/ to find all model files (exclude __init__.py and _-prefixed). Read app/db/base.py content. Assert each model has "import app.models.<stem>" in base.py content.

    **Implementation (app/main.py lifespan):**
    Add ZitadelService init between JWKSManager and StorageService:
    - Import ZitadelService inside lifespan function body (avoid circular imports)
    - Check `not settings.zitadel_service_client_id or not settings.zitadel_service_client_secret` — raise RuntimeError if either empty/None
    - Instantiate ZitadelService with issuer, client_id, client_secret from settings
    - Call `await zitadel_service._get_token()` wrapped in try/except:
      - Catch httpx.HTTPStatusError — raise RuntimeError("ZITADEL credentials invalid: ...")
      - Catch ZitadelUnavailableError — raise RuntimeError("ZITADEL unreachable: ...")
    - Store on app.state.zitadel_service
    - Log success with logger.info

    **Implementation (app/db/base.py):**
    Add 3 missing imports after line 29 (after existing `import app.models.shift`):
    ```
    import app.models.call_list  # noqa: E402, F401
    import app.models.dnc  # noqa: E402, F401
    import app.models.phone_bank  # noqa: E402, F401
    ```
  </action>
  <verify>
    <automated>cd /home/kwhatcher/projects/run-api && uv run pytest tests/unit/test_lifespan.py tests/unit/test_model_coverage.py -x -q</automated>
  </verify>
  <done>
    - All 6+ tests pass
    - app/main.py lifespan initializes ZitadelService with fail-fast validation
    - app/db/base.py imports all 17 model modules (14 existing + 3 added)
    - Model coverage regression test prevents future drift
  </done>
</task>

<task type="auto">
  <name>Task 2: Campaign creation E2E flow test (FLOW-01 closure)</name>
  <files>tests/unit/test_lifespan.py</files>
  <action>
    Add an E2E campaign creation flow test to test_lifespan.py that validates the full FLOW-01 gap closure:

    **test_campaign_create_e2e_flow:**
    1. Create the FastAPI app via create_app()
    2. Mock ZitadelService (AsyncMock) with create_org returning a valid org_id
    3. Mock _get_token to succeed (for lifespan startup validation)
    4. Mock StorageService.ensure_bucket, broker.startup/shutdown
    5. Set valid ZITADEL config on settings
    6. Use httpx.AsyncClient with ASGITransport to send a POST to /api/v1/campaigns with valid campaign data and a mocked JWT auth header
    7. Assert 201 response with campaign data returned
    8. Assert zitadel_service.create_org was called (proving app.state.zitadel_service was accessible from the route handler)

    Follow the existing test patterns from test_api_campaigns.py for auth mocking (patch jwt_required dependency, provide campaign fixture data). The key difference is this test goes through the full lifespan to prove the wiring works end-to-end.

    Also run the full test suite to confirm no regressions.
  </action>
  <verify>
    <automated>cd /home/kwhatcher/projects/run-api && uv run pytest tests/unit/test_lifespan.py tests/unit/test_model_coverage.py -x -q && uv run pytest tests/unit/ -x -q</automated>
  </verify>
  <done>
    - E2E campaign creation test passes, proving FLOW-01 is closed
    - Full unit test suite passes with no regressions
    - ZitadelService accessible from route handler via app.state during lifespan
  </done>
</task>

</tasks>

<verification>
1. `uv run pytest tests/unit/test_lifespan.py tests/unit/test_model_coverage.py -x -v` — all new tests pass
2. `uv run pytest tests/unit/ -x -q` — full unit suite green, no regressions
3. `grep -c "import app.models" app/db/base.py` returns 17 (all model files imported)
4. `grep "zitadel_service" app/main.py` shows initialization in lifespan
</verification>

<success_criteria>
- ZitadelService initialized in app lifespan with fail-fast startup validation
- All 17 model modules imported in app/db/base.py for Alembic discovery
- 6+ unit tests covering: lifespan wiring, 3 startup failure scenarios, model coverage regression, campaign E2E flow
- Full unit test suite passes
</success_criteria>

<output>
After completion, create `.planning/phases/07-integration-wiring-fixes/07-01-SUMMARY.md`
</output>
