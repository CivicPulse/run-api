---
phase: 76-reliability-backend-infrastructure
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - tests/unit/test_zitadel_timeouts.py
  - tests/unit/test_db_engine_timeouts.py
  - tests/unit/test_settings_dedup.py
  - tests/unit/test_dnc_upload_size_limit.py
  - tests/unit/test_import_filename_sanitize.py
  - tests/unit/test_request_logging_ip.py
  - tests/unit/test_alembic_ini_interpolation.py
autonomous: true
requirements: [REL-04, REL-05, REL-06, REL-07, REL-09, REL-10, REL-11]
must_haves:
  truths:
    - "Running pytest surfaces each of the 7 REL gaps as a failing test"
    - "Each fix in plans 02-05 will have a pre-existing test that flips to pass"
    - "Tests run without network (mock httpx, asyncpg, MinIO)"
  artifacts:
    - path: "tests/unit/test_zitadel_timeouts.py"
      provides: "Assertion that every httpx.AsyncClient in zitadel.py has timeout=10.0"
    - path: "tests/unit/test_db_engine_timeouts.py"
      provides: "Assertion that engine was created with pool_timeout=10 and statement_timeout=30000"
    - path: "tests/unit/test_settings_dedup.py"
      provides: "Assertion that trusted_proxy_cidrs defaults to the Cloudflare CIDR list and rate_limit_unauthenticated is 60/minute"
    - path: "tests/unit/test_dnc_upload_size_limit.py"
      provides: "Assertion that a >10MB DNC CSV upload returns 413"
    - path: "tests/unit/test_import_filename_sanitize.py"
      provides: "Assertion that '../etc/passwd' and similar inputs become '{uuid}-etc-passwd' style safe keys"
    - path: "tests/unit/test_request_logging_ip.py"
      provides: "Assertion that X-Real-IP is ignored unless scope client is in trusted_proxy_cidrs"
    - path: "tests/unit/test_alembic_ini_interpolation.py"
      provides: "Assertion that alembic.ini sqlalchemy.url contains %(DATABASE_URL_SYNC)s"
  key_links:
    - from: "tests/unit/test_*"
      to: "pytest discovery"
      via: "tests/ directory convention"
      pattern: "def test_"
---

<objective>
Create failing unit/integration test stubs for all seven REL fixes in Phase 76. These tests MUST fail on main today and turn green as plans 02-05 land. This is the Wave 0 test scaffold that closes the Nyquist loop for every subsequent task.

Purpose: Every downstream plan has a pre-existing automated verification target. No fix ships without a test already watching it.
Output: Seven pytest files under tests/unit/ that currently fail, each pinned to a specific REL requirement and CODEBASE-REVIEW finding.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/76-reliability-backend-infrastructure/76-CONTEXT.md
@.planning/CODEBASE-REVIEW-2026-04-04.md
@CLAUDE.md
@app/services/zitadel.py
@app/db/session.py
@app/core/config.py
@app/api/v1/dnc.py
@app/api/v1/imports.py
@app/core/middleware/request_logging.py
@app/core/rate_limit.py
@docker-compose.yml
@alembic.ini
@tests/conftest.py
@tests/unit/conftest.py

<interfaces>
<!-- Key hooks the tests will target — from source files above -->

From app/services/zitadel.py:
- Class ZitadelService with async methods that each instantiate `httpx.AsyncClient(...)` inline.
- Every AsyncClient call site currently has NO `timeout=` kwarg. Tests assert source text contains `timeout=10.0` on every AsyncClient construction.

From app/db/session.py:
- `engine = create_async_engine(settings.database_url, echo=..., pool_pre_ping=True, pool_size=20, max_overflow=20)` — no pool_timeout, no connect_args. Tests assert engine kwargs after import.

From app/core/config.py:
- `Settings` pydantic-settings class. Currently declares `trusted_proxy_cidrs: list[str] = []` at line 45 AND `trusted_proxy_cidrs: list[str] = [<Cloudflare CIDRs>]` at line 69. Same for `rate_limit_unauthenticated`. Tests assert the EFFECTIVE default matches the later (Cloudflare / "60/minute") values.

From app/api/v1/dnc.py:
- `bulk_import_dnc` at line 78: `content = await file.read()` with no cap. Tests post a fake CSV with >10MB payload and assert 413.

From app/api/v1/imports.py:
- `initiate_import` at line 136: `file_key = f"imports/{campaign_id}/{job.id}/{original_filename}"`. Tests call with malicious filenames (`../../../etc/passwd`, `foo\x00.csv`, `C:\\Windows\\evil.csv`) and assert file_key contains `{uuid}-safe-basename` pattern.

From app/core/middleware/request_logging.py:
- `client_ip = (_get_header(headers, b"cf-connecting-ip") or _get_header(headers, b"x-real-ip") or scope.get("client", ("",))[0])` at lines 81-85. Unconditionally trusts headers. Tests assert that when scope client is UNTRUSTED, X-Real-IP is ignored.

From app/core/rate_limit.py:
- `_is_trusted_proxy(host: str) -> bool` helper at line 23. The logging middleware must call this.

From alembic.ini:
- Line 5: `sqlalchemy.url = postgresql+psycopg2://postgres:postgres@localhost:5432/run_api`. Tests assert the file content contains `%(DATABASE_URL_SYNC)s` instead of the hardcoded URL.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write failing tests for ZITADEL timeouts, DB engine timeouts, and Settings dedup</name>
  <files>
    tests/unit/test_zitadel_timeouts.py,
    tests/unit/test_db_engine_timeouts.py,
    tests/unit/test_settings_dedup.py
  </files>
  <behavior>
    tests/unit/test_zitadel_timeouts.py:
    - test_every_async_client_has_10s_timeout: Read app/services/zitadel.py as text, find every `httpx.AsyncClient(` occurrence, assert the matching `)` span contains `timeout=10.0` (or `timeout=httpx.Timeout(10.0)`). Currently FAILS — no timeouts set.

    tests/unit/test_db_engine_timeouts.py:
    - test_engine_has_pool_timeout: import `app.db.session`, introspect `engine.pool._timeout` (SQLAlchemy QueuePool exposes `_timeout`). Assert == 10. Currently FAILS — default 30.
    - test_engine_has_statement_timeout_connect_arg: introspect `engine.sync_engine.url` and `engine.sync_engine.dialect` — easier: assert `"statement_timeout"` appears in `engine.sync_engine.pool._creator.__closure__` OR re-read module source and assert `connect_args={"server_settings": {"statement_timeout": "30000"}}` literal present in app/db/session.py. Pick the source-text approach for simplicity. Currently FAILS.

    tests/unit/test_settings_dedup.py:
    - test_trusted_proxy_cidrs_default_is_cloudflare_list: instantiate `Settings()` with no env override, assert `len(settings.trusted_proxy_cidrs) >= 22` and `"173.245.48.0/20" in settings.trusted_proxy_cidrs`.
    - test_rate_limit_unauthenticated_is_60_per_minute: assert `settings.rate_limit_unauthenticated == "60/minute"`.
    - test_settings_file_has_no_duplicate_field_declarations: read app/core/config.py as text, count occurrences of `trusted_proxy_cidrs: list[str]` and `rate_limit_unauthenticated: str` — each must appear EXACTLY ONCE. Currently FAILS (each appears twice).
  </behavior>
  <action>
    Create three pytest files. Use plain `def test_*` functions (asyncio_mode=auto is set in pyproject but these are sync).

    For test_zitadel_timeouts.py: Use `Path("app/services/zitadel.py").read_text()` and regex `r"httpx\.AsyncClient\([^)]*\)"` to extract every AsyncClient call. For each, assert `"timeout=10.0" in call` OR `"timeout=httpx.Timeout(10" in call`.

    For test_db_engine_timeouts.py: Read source text of app/db/session.py. Assert `"pool_timeout=10"` in text AND `'statement_timeout": "30000"'` in text. Simple, no runtime introspection gymnastics.

    For test_settings_dedup.py: Import `Settings` from app.core.config, instantiate fresh (clear env in conftest if needed), assert defaults. Also read config.py source and use `text.count("trusted_proxy_cidrs: list[str]") == 1` and `text.count('rate_limit_unauthenticated: str = ') == 1`.

    All three tests MUST fail when run against current main. Do NOT fix the code — that's plans 02/03.

    Run `uv run pytest tests/unit/test_zitadel_timeouts.py tests/unit/test_db_engine_timeouts.py tests/unit/test_settings_dedup.py -x` to confirm failure.

    Run `uv run ruff check tests/unit/test_zitadel_timeouts.py tests/unit/test_db_engine_timeouts.py tests/unit/test_settings_dedup.py` — must pass.
  </action>
  <verify>
    <automated>uv run pytest tests/unit/test_zitadel_timeouts.py tests/unit/test_db_engine_timeouts.py tests/unit/test_settings_dedup.py 2>&amp;1 | tee /tmp/t1.log; grep -E "FAILED|failed" /tmp/t1.log &amp;&amp; uv run ruff check tests/unit/test_zitadel_timeouts.py tests/unit/test_db_engine_timeouts.py tests/unit/test_settings_dedup.py</automated>
  </verify>
  <done>Three test files exist, each has at least 1 test that fails on current main with a clear assertion message, ruff clean.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Write failing tests for DNC upload cap, import filename sanitization, and alembic.ini interpolation</name>
  <files>
    tests/unit/test_dnc_upload_size_limit.py,
    tests/unit/test_import_filename_sanitize.py,
    tests/unit/test_alembic_ini_interpolation.py
  </files>
  <behavior>
    tests/unit/test_dnc_upload_size_limit.py:
    - test_dnc_bulk_import_rejects_over_10mb: POST to /api/v1/campaigns/{cid}/dnc/import with multipart file part carrying Content-Length header of 11MB (11 * 1024 * 1024). Assert response.status_code == 413. Use existing TestClient fixtures (mirror tests/unit/test_dnc.py setup).
    - test_dnc_bulk_import_accepts_under_10mb: same but with 9MB payload of valid CSV rows. Assert status != 413 (200 or 201 — allow either).

    tests/unit/test_import_filename_sanitize.py:
    - test_initiate_import_strips_path_traversal: call initiate_import with original_filename='../../../etc/passwd'. Assert returned file_key does NOT contain '..' and matches regex r'imports/[0-9a-f-]{36}/[0-9a-f-]{36}/[0-9a-f-]{36}-[A-Za-z0-9_.-]+$'.
    - test_initiate_import_strips_null_bytes: original_filename='foo\x00.csv'. Assert no null byte in file_key.
    - test_initiate_import_strips_backslashes: original_filename='C:\\Windows\\evil.csv'. Assert no backslash in file_key.
    - test_initiate_import_prepends_uuid: verify the basename portion of the file_key starts with a UUID followed by '-'.

    tests/unit/test_alembic_ini_interpolation.py:
    - test_alembic_ini_uses_env_interpolation: read alembic.ini as text, assert `"%(DATABASE_URL_SYNC)s" in content` and assert the hardcoded `"postgresql+psycopg2://postgres:postgres@localhost:5432/run_api"` is NOT on the `sqlalchemy.url` line.
  </behavior>
  <action>
    Create three pytest files.

    test_dnc_upload_size_limit.py: Follow pattern from tests/unit/test_dnc.py — import TestClient, mock auth. Craft multipart body via `client.post(..., files={"file": ("big.csv", b"a" * (11*1024*1024), "text/csv")})`. For speed, consider using io.BytesIO and a stream that reports Content-Length without allocating the full buffer; if simpler, actually allocate 11MB (fine for local pytest). Assert status_code == 413.

    test_import_filename_sanitize.py: Mirror tests/unit/test_import_upload.py. Mock `request.app.state.storage_service.generate_upload_url` to return a stub URL. Call the initiate_import endpoint via TestClient with query param original_filename set to the malicious input. Inspect the returned `file_key` field and assert sanitization rules.

    test_alembic_ini_interpolation.py: Pure source-text read. `Path("alembic.ini").read_text()`, assert interpolation marker present.

    All three tests MUST fail on current main.

    Run `uv run pytest tests/unit/test_dnc_upload_size_limit.py tests/unit/test_import_filename_sanitize.py tests/unit/test_alembic_ini_interpolation.py -x`.

    Run `uv run ruff check` on the new files.
  </action>
  <verify>
    <automated>uv run pytest tests/unit/test_dnc_upload_size_limit.py tests/unit/test_import_filename_sanitize.py tests/unit/test_alembic_ini_interpolation.py 2>&amp;1 | tee /tmp/t2.log; grep -E "FAILED|failed" /tmp/t2.log &amp;&amp; uv run ruff check tests/unit/test_dnc_upload_size_limit.py tests/unit/test_import_filename_sanitize.py tests/unit/test_alembic_ini_interpolation.py</automated>
  </verify>
  <done>Three test files exist, DNC and import tests fail with current code (accept oversize upload / accept traversal path), alembic test fails (hardcoded URL present), ruff clean.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Write failing test for request-logging trusted-proxy IP check</name>
  <files>tests/unit/test_request_logging_ip.py</files>
  <behavior>
    - test_ip_from_untrusted_client_ignores_x_real_ip: Build an ASGI scope with client=('8.8.8.8', 12345) — NOT in Cloudflare CIDR list. Headers include `x-real-ip: 1.2.3.4`. Run StructlogMiddleware and capture the log record. Assert client_ip == '8.8.8.8' (NOT '1.2.3.4').
    - test_ip_from_trusted_proxy_honors_x_real_ip: client=('173.245.48.1', 12345) — IS in Cloudflare CIDR list. Headers include `x-real-ip: 1.2.3.4`. Assert client_ip == '1.2.3.4'.
    - test_ip_from_trusted_proxy_honors_cf_connecting_ip: client=('173.245.48.1', 12345), headers include `cf-connecting-ip: 9.9.9.9` and `x-real-ip: 1.2.3.4`. Assert client_ip == '9.9.9.9' (cf-connecting-ip takes precedence).
    - test_ip_falls_back_to_scope_client_when_no_headers: client=('10.0.0.1', 12345), no proxy headers. Assert client_ip == '10.0.0.1'.
  </behavior>
  <action>
    Create tests/unit/test_request_logging_ip.py.

    Use structlog's `capture_logs` context manager OR monkeypatch `app.core.middleware.request_logging._logger` with a recording logger.

    Instantiate StructlogMiddleware around a trivial async app (a lambda that calls send with a 200 response). Drive it manually:
    ```python
    async def receive(): return {"type": "http.request", "body": b"", "more_body": False}
    sent = []
    async def send(msg): sent.append(msg)
    ```
    Build `scope = {"type": "http", "path": "/api/v1/foo", "method": "GET", "headers": [(b"x-real-ip", b"1.2.3.4")], "client": ("8.8.8.8", 12345), "query_string": b""}`.

    `await middleware(scope, receive, send)`, then inspect the captured log record's `client_ip`.

    All four assertions MUST fail on current main (the code unconditionally trusts the header, so untrusted-client test expects 8.8.8.8 but gets 1.2.3.4 → fails).

    Run `uv run pytest tests/unit/test_request_logging_ip.py -x` and `uv run ruff check tests/unit/test_request_logging_ip.py`.
  </action>
  <verify>
    <automated>uv run pytest tests/unit/test_request_logging_ip.py 2>&amp;1 | tee /tmp/t3.log; grep -E "FAILED|failed" /tmp/t3.log &amp;&amp; uv run ruff check tests/unit/test_request_logging_ip.py</automated>
  </verify>
  <done>Test file exists with 4 test cases; on current main the untrusted-client tests fail (IP resolves to spoofed header value); ruff clean.</done>
</task>

</tasks>

<verification>
uv run pytest tests/unit/test_zitadel_timeouts.py tests/unit/test_db_engine_timeouts.py tests/unit/test_settings_dedup.py tests/unit/test_dnc_upload_size_limit.py tests/unit/test_import_filename_sanitize.py tests/unit/test_alembic_ini_interpolation.py tests/unit/test_request_logging_ip.py
Expected: at least 7 failing tests (one per file minimum).
uv run ruff check tests/unit/test_*.py
Expected: clean.
</verification>

<success_criteria>
- Seven new test files exist under tests/unit/
- Each covers one REL requirement from the phase
- All tests fail when run against current main
- ruff check passes on all new files
</success_criteria>

<output>
Create `.planning/phases/76-reliability-backend-infrastructure/76-01-SUMMARY.md` listing each test file, the REL requirement it covers, and the exact failure message when run on main.
</output>
