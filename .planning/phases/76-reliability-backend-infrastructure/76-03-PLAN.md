---
phase: 76-reliability-backend-infrastructure
plan: 03
type: execute
wave: 1
depends_on: ["76-01"]
files_modified:
  - app/services/zitadel.py
  - app/db/session.py
autonomous: true
requirements: [REL-04, REL-05]
must_haves:
  truths:
    - "Every httpx.AsyncClient constructed in ZitadelService has a 10-second timeout"
    - "The async SQLAlchemy engine has pool_timeout=10"
    - "Every DB statement is bounded by statement_timeout=30000 (30s) via connect_args"
    - "A slow/unreachable ZITADEL never blocks the event loop beyond 10s per call"
  artifacts:
    - path: "app/services/zitadel.py"
      provides: "ZitadelService with httpx.AsyncClient(timeout=10.0, ...) on every call site"
    - path: "app/db/session.py"
      provides: "create_async_engine with pool_timeout=10 and statement_timeout connect_arg"
  key_links:
    - from: "app/services/zitadel.py"
      to: "httpx.AsyncClient"
      via: "timeout=10.0 kwarg on every construction"
      pattern: "httpx\\.AsyncClient\\([^)]*timeout=10\\.0"
    - from: "app/db/session.py"
      to: "create_async_engine"
      via: "connect_args server_settings"
      pattern: "statement_timeout.*30000"
---

<objective>
Add explicit 10-second timeouts to every ZitadelService httpx.AsyncClient (REL-04 / H6) and configure pool/statement timeouts on the async SQLAlchemy engine (REL-05 / H14). These two fixes live in different files and are parallelizable with the config-hygiene plan.

Purpose: Prevents a degraded ZITADEL from indefinitely blocking the event loop, and prevents DB queries or pool checkout from stalling the API.
Output: Two source files updated, Wave 0 timeout tests flipped to passing.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/76-reliability-backend-infrastructure/76-CONTEXT.md
@.planning/CODEBASE-REVIEW-2026-04-04.md
@app/services/zitadel.py
@app/db/session.py
@tests/unit/test_zitadel_timeouts.py
@tests/unit/test_db_engine_timeouts.py
@tests/unit/test_zitadel_token.py
@tests/unit/test_pool_events.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add 10s timeout to every httpx.AsyncClient in ZitadelService (REL-04 / H6)</name>
  <files>app/services/zitadel.py</files>
  <behavior>
    - Every `httpx.AsyncClient(` in zitadel.py includes `timeout=10.0`
    - Calls to a slow mock httpx transport raise httpx.TimeoutException after ~10s (verified via unit test that patches transport)
    - Existing ZITADEL tests in test_zitadel_token.py continue to pass
  </behavior>
  <action>
    Per D-03 (lock): **10s per call** on all httpx.AsyncClient instantiations in app/services/zitadel.py.

    Find every occurrence of `httpx.AsyncClient(` and add `timeout=10.0` to the kwargs. The current call sites are at lines 62, 117, 153, 188, 241, 295, 375, 426, 487 (approximately — grep to confirm).

    Example transformations:
    - `async with httpx.AsyncClient(headers=self._extra_headers, verify=self._verify_tls) as client:` → `async with httpx.AsyncClient(headers=self._extra_headers, verify=self._verify_tls, timeout=10.0) as client:`
    - `async with httpx.AsyncClient(verify=self._verify_tls) as client:` → `async with httpx.AsyncClient(verify=self._verify_tls, timeout=10.0) as client:`

    Do NOT change any other behavior — no new env var, no timeout override (Claude's discretion per CONTEXT says env override is allowed if trivial, but for this plan we keep it a hardcoded 10.0 to stay in scope).

    Run `uv run ruff check app/services/zitadel.py && uv run ruff format app/services/zitadel.py`.

    Run `uv run pytest tests/unit/test_zitadel_timeouts.py tests/unit/test_zitadel_token.py -x`.
  </action>
  <verify>
    <automated>uv run pytest tests/unit/test_zitadel_timeouts.py tests/unit/test_zitadel_token.py -x &amp;&amp; uv run ruff check app/services/zitadel.py</automated>
  </verify>
  <done>Every httpx.AsyncClient in zitadel.py has timeout=10.0, Wave 0 test passes, existing zitadel tests still pass, ruff clean.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add pool_timeout and statement_timeout to async engine (REL-05 / H14)</name>
  <files>app/db/session.py</files>
  <behavior>
    - `create_async_engine` is called with `pool_timeout=10`
    - `create_async_engine` is called with `connect_args={"server_settings": {"statement_timeout": "30000"}}`
    - Pool checkout stalls are bounded to 10s (raises TimeoutError if pool exhausted)
    - Any single statement taking > 30s is aborted by Postgres with a statement_timeout error
    - Existing tests in test_pool_events.py continue to pass (RLS reset on checkout still fires)
  </behavior>
  <action>
    Per D-04 (lock): Update `create_async_engine` call at lines 16-22 of app/db/session.py.

    Current:
    ```python
    engine = create_async_engine(
        settings.database_url,
        echo=settings.debug,
        pool_pre_ping=True,
        pool_size=20,
        max_overflow=20,
    )
    ```

    New:
    ```python
    engine = create_async_engine(
        settings.database_url,
        echo=settings.debug,
        pool_pre_ping=True,
        pool_size=20,
        max_overflow=20,
        pool_timeout=10,
        connect_args={"server_settings": {"statement_timeout": "30000"}},
    )
    ```

    Note: asyncpg supports `server_settings` in `connect_args`; this is the standard idiom. The value "30000" is in milliseconds per Postgres `statement_timeout` semantics.

    Do NOT add an env-var override for the statement timeout in this plan (deferred per CONTEXT).

    Run `uv run ruff check app/db/session.py && uv run ruff format app/db/session.py`.

    Run `uv run pytest tests/unit/test_db_engine_timeouts.py tests/unit/test_pool_events.py -x`.

    Run a broader DB smoke: `uv run pytest tests/unit/test_rls_middleware.py -x` to confirm the RLS reset event still fires cleanly.
  </action>
  <verify>
    <automated>uv run pytest tests/unit/test_db_engine_timeouts.py tests/unit/test_pool_events.py tests/unit/test_rls_middleware.py -x &amp;&amp; uv run ruff check app/db/session.py</automated>
  </verify>
  <done>Engine has pool_timeout=10 and statement_timeout connect_arg, Wave 0 test passes, pool_events and rls_middleware tests still pass, ruff clean.</done>
</task>

</tasks>

<verification>
uv run pytest tests/unit/test_zitadel_timeouts.py tests/unit/test_db_engine_timeouts.py tests/unit/test_zitadel_token.py tests/unit/test_pool_events.py -x
uv run ruff check app/services/zitadel.py app/db/session.py
</verification>

<success_criteria>
- Every httpx.AsyncClient in zitadel.py has timeout=10.0
- Engine has pool_timeout=10 and connect_args with statement_timeout=30000
- Wave 0 timeout tests flip from failing to passing
- No regression in existing ZITADEL or DB pool tests
</success_criteria>

<output>
Create `.planning/phases/76-reliability-backend-infrastructure/76-03-SUMMARY.md` with the list of AsyncClient call sites modified (with line numbers), the engine kwargs diff, and test pass confirmation.
</output>
