---
phase: 59-chunk-schema-configuration
plan: 02
type: execute
wave: 2
depends_on: ["59-01"]
files_modified:
  - app/services/import_service.py
  - app/tasks/import_task.py
  - tests/unit/test_import_service.py
  - tests/unit/test_import_task.py
autonomous: true
requirements: [CHUNK-05, CHUNK-06]
must_haves:
  truths:
    - "Chunk row ranges can be derived deterministically from total_rows and mapped-column count using the existing bind-limit math."
    - "Unknown or below-threshold imports explicitly stay on the proven serial path."
    - "Even above-threshold imports still run through the current serial worker path until Phase 60 adds trusted fan-out."
  artifacts:
    - path: "app/services/import_service.py"
      provides: "chunk sizing helpers and serial-routing helper"
    - path: "app/tasks/import_task.py"
      provides: "serial-path decision seam without child-task fan-out"
    - path: "tests/unit/test_import_service.py"
      provides: "helper coverage for bind-limit-aware chunk sizing"
  key_links:
    - from: "app/tasks/import_task.py"
      to: "app.services.import_service.should_use_serial_import"
      via: "background task routing decision"
      pattern: "should_use_serial_import"
    - from: "app/services/import_service.py"
      to: "settings.import_chunk_size_default"
      via: "chunk range planning"
      pattern: "import_chunk_size_default"
---

<objective>
Add the reusable chunk-sizing and serial-routing seams needed for later phases while preserving the current one-task serial import runtime in Phase 59.

Purpose: Implement D-04, D-05, D-06, D-07, and D-10 now so Phase 60 can add chunk fan-out on top of tested helpers instead of editing batch math and task routing in the same phase.
Output: Pure chunk sizing helpers, explicit serial-routing logic, and tests proving no runtime behavior change.
</objective>

<execution_context>
@/home/kwhatcher/.codex/get-shit-done/workflows/execute-plan.md
@/home/kwhatcher/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/59-chunk-schema-configuration/59-CONTEXT.md
@.planning/phases/59-chunk-schema-configuration/59-RESEARCH.md
@.planning/phases/59-chunk-schema-configuration/59-VALIDATION.md
@.planning/phases/57-recovery-engine-completion-hardening/57-01-SUMMARY.md
@.planning/phases/58-test-coverage/58-01-SUMMARY.md
@app/services/import_service.py
@app/tasks/import_task.py
@tests/unit/test_import_service.py
@tests/unit/test_import_task.py

<interfaces>
From app/services/import_service.py:
```python
class ImportService:
    async def process_import_file(
        self,
        import_job_id: str,
        session: AsyncSession,
        storage: StorageService,
        campaign_id: str,
    ) -> None: ...
```

Current batch math inside process_import_file:
```python
max_params = 32_767
num_mapped = sum(1 for v in (job.field_mapping or {}).values() if v)
cols_per_row = max(num_mapped + 4, 1)
effective_batch_size = min(settings.import_batch_size, max_params // cols_per_row)
```

From app/tasks/import_task.py:
```python
@procrastinate_app.task(name="process_import", queue="imports")
async def process_import(import_job_id: str, campaign_id: str) -> None: ...
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extract bind-limit-aware chunk sizing helpers</name>
  <files>app/services/import_service.py</files>
  <read_first>
app/services/import_service.py
app/core/config.py
.planning/phases/59-chunk-schema-configuration/59-CONTEXT.md
.planning/phases/59-chunk-schema-configuration/59-RESEARCH.md
  </read_first>
  <action>Refactor `app/services/import_service.py` so the current `32_767` bind-limit calculation becomes a pure helper instead of being buried in `process_import_file()`. Add `calculate_effective_rows_per_write(mapped_column_count: int, target_rows: int, max_bind_params: int = 32767, extra_columns_per_row: int = 4) -> int`, `plan_chunk_ranges(total_rows: int, mapped_column_count: int, chunk_size_default: int) -> list[tuple[int, int]]`, and `should_use_serial_import(total_rows: int | None, serial_threshold: int) -> bool`. `plan_chunk_ranges` must return 1-based inclusive `(row_start, row_end)` tuples, return `[]` for non-positive totals, and use the bind-limit helper plus `chunk_size_default` to size each range deterministically per D-06 and D-07. Update `process_import_file()` to call the helper for its effective batch size so the batch and chunk math share one source of truth.</action>
  <acceptance_criteria>
`rg "def calculate_effective_rows_per_write|def plan_chunk_ranges|def should_use_serial_import" app/services/import_service.py` finds all three helpers.
`rg "32767|32_767" app/services/import_service.py` shows the bind ceiling lives in the shared helper path instead of duplicated math.
`rg "calculate_effective_rows_per_write\\(" app/services/import_service.py` shows `process_import_file()` reuses the helper.
  </acceptance_criteria>
  <verify>
    <automated>uv run pytest tests/unit/test_import_service.py -x</automated>
  </verify>
  <done>Chunk sizing and serial-routing decisions are expressed as pure helpers, and the existing batch-size calculation now flows through the same helper that later chunk planning will use.</done>
</task>

<task type="auto">
  <name>Task 2: Add a serial-routing seam in the import task without enabling chunk fan-out</name>
  <files>app/tasks/import_task.py</files>
  <read_first>
app/tasks/import_task.py
app/services/import_service.py
app/core/config.py
.planning/phases/59-chunk-schema-configuration/59-CONTEXT.md
.planning/phases/59-chunk-schema-configuration/59-RESEARCH.md
  </read_first>
  <action>Update `process_import()` in `app/tasks/import_task.py` to evaluate `should_use_serial_import(job.total_rows, settings.import_serial_threshold)` after loading the job and before calling the service. For `total_rows is None` and `total_rows <= settings.import_serial_threshold`, explicitly continue on the existing `service.process_import_file(...)` path per D-04 and D-10. For `total_rows > settings.import_serial_threshold`, add a clear info log that chunk fan-out is deferred until Phase 60 and still call `service.process_import_file(...)` exactly once. Do not enqueue child tasks, create `ImportChunk` records, alter API behavior, or change advisory-lock / recovery semantics in this phase.</action>
  <acceptance_criteria>
`rg "should_use_serial_import" app/tasks/import_task.py` shows the task uses the shared routing helper.
`rg "Phase 60|deferred" app/tasks/import_task.py` finds the explicit no-fan-out log message for above-threshold totals.
`rg "process_import_file\\(" app/tasks/import_task.py` still shows a single serial service call path.
  </acceptance_criteria>
  <verify>
    <automated>uv run pytest tests/unit/test_import_task.py -x</automated>
  </verify>
  <done>The background worker now has an explicit routing seam for later chunk fan-out, but Phase 59 still executes the current serial import path for every job.</done>
</task>

<task type="auto">
  <name>Task 3: Add helper and routing regression tests for Phase 59 behavior</name>
  <files>tests/unit/test_import_service.py, tests/unit/test_import_task.py</files>
  <read_first>
tests/unit/test_import_service.py
tests/unit/test_import_task.py
app/services/import_service.py
app/tasks/import_task.py
  </read_first>
  <action>Extend `tests/unit/test_import_service.py` with coverage for `calculate_effective_rows_per_write`, `plan_chunk_ranges`, and `should_use_serial_import`. Include one high-column-count edge case where bind-limit math reduces rows below `settings.import_chunk_size_default`, one multi-range case for a large file, and one empty/zero-row case. Extend `tests/unit/test_import_task.py` so `process_import()` is exercised with `job.total_rows = None`, `5000`, and `20000`, proving that the task still calls `service.process_import_file()` exactly once in all three cases and never attempts child-task fan-out. Keep the existing success/failure/recovery assertions intact.</action>
  <acceptance_criteria>
`rg "calculate_effective_rows_per_write|plan_chunk_ranges|should_use_serial_import" tests/unit/test_import_service.py` finds dedicated helper tests.
`rg "total_rows = None|5000|20000" tests/unit/test_import_task.py` finds routing scenarios for unknown, below-threshold, and above-threshold files.
`rg "assert_awaited_once|await_count == 1" tests/unit/test_import_task.py` confirms the serial service path remains singular.
  </acceptance_criteria>
  <verify>
    <automated>uv run pytest tests/unit/test_import_service.py tests/unit/test_import_task.py -x</automated>
  </verify>
  <done>Automated coverage proves the new helpers work, the serial threshold logic is explicit, and Phase 59 still runs the existing serial import implementation regardless of file size.</done>
</task>

</tasks>

<verification>
Run `uv run pytest tests/unit/test_import_service.py tests/unit/test_import_task.py -x` after implementation.
Spot-check that no code in this plan creates chunks, enqueues child tasks, or modifies API response schemas.
</verification>

<success_criteria>
The codebase exposes tested chunk sizing and routing helpers, and the background import task has an explicit seam for future chunk fan-out while preserving the current serial import behavior in Phase 59.
</success_criteria>

<output>
After completion, create `.planning/phases/59-chunk-schema-configuration/59-02-SUMMARY.md`
</output>
