# 78-01 Plan

## Goal

Add reproducible regressions for the six reported tenant-containment failures so the phase can be verified against the actual shakedown paths instead of inferred behavior.

## Tasks

1. Add a DB-backed integration suite for the list-members, call-lists, voter-interactions, turf voters, volunteer subresources, and `/field/me` leak paths.
2. Add unit regressions for the new service guards so verification still runs when the local integration database is unavailable.
3. Keep the integration suite committed even if local execution is blocked, so the full proof is ready once PostgreSQL is available.

## Verification

- `uv run pytest tests/unit/test_phase78_tenant_containment.py tests/unit/test_volunteers.py tests/unit/test_volunteer_gaps.py -q`
- `uv run pytest tests/integration/test_phase78_tenant_containment.py -q`
