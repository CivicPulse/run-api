# 79-03 Plan

## Goal

Enforce the Phase 79 security-header and HTTPS posture required by the production shakedown.

## Tasks

1. Add app-level middleware for CSP, `X-Frame-Options`, `X-Content-Type-Options`, and HSTS on secure responses using defaults compatible with the current SPA/OIDC deployment.
2. Update the repo-owned ingress or deployment configuration so the production path documents and, where possible, enforces HTTP-to-HTTPS redirect behavior for the Cloudflare -> Traefik setup.
3. Add verification coverage or deployment notes that make the expected production edge behavior explicit instead of relying on tribal knowledge from the shakedown report.

## Verification

- Re-run the Phase 79 header/edge regressions and inspect the generated ingress/app config for the expected security posture.
