# 82-02 Plan

## Goal

Close the remaining lifecycle and field-data drift with explicit, test-backed contract alignment.

## Tasks

1. Remove the stale `field/me.canvassing.total` behavior by deriving canvassing totals from live `walk_list_entries` instead of trusting denormalized counters.
2. Record the current supported lifecycle semantics that differ from the original shakedown assumptions so the milestone no longer depends on implicit behavior.
3. Re-run focused field and campaign-service tests covering the corrected field aggregation path and the retained lifecycle contract.

## Verification

- `uv run pytest tests/test_field_me.py tests/unit/test_campaign_service.py -q`
