# 82-03 Plan

## Goal

Resolve the remaining production-surface documentation drift and explicit disposition decisions for the shakedown findings.

## Tasks

1. Disable interactive FastAPI docs and OpenAPI exposure in production while preserving local/development docs.
2. Record the supported contract or operational disposition for the remaining Phase 82 findings: DNC behavior, docs exposure, SPA catch-all behavior, rate-limit posture, and `pg_stat_statements`.
3. Re-run focused security and field tests covering the production-only docs behavior and the Phase 82 closeout changes.

## Verification

- `uv run pytest tests/unit/test_phase79_security_errors.py tests/test_field_me.py tests/unit/test_campaign_service.py -q`
