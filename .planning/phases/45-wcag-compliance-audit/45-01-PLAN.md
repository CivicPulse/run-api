---
phase: 45-wcag-compliance-audit
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - web/package.json
  - web/src/components/shared/SkipNav.tsx
  - web/src/components/shared/VisuallyHidden.tsx
  - web/src/components/shared/LiveRegion.tsx
  - web/src/routes/__root.tsx
  - web/src/index.css
  - web/e2e/axe-test.ts
autonomous: true
requirements:
  - A11Y-03

must_haves:
  truths:
    - "SkipNav link is visible on keyboard focus and jumps to main content"
    - "Root layout has semantic landmarks (nav, main, header)"
    - "Muted-foreground text meets WCAG AA 4.5:1 contrast ratio"
    - "axe-core shared test fixture is ready for parameterized scanning"
  artifacts:
    - path: "web/src/components/shared/SkipNav.tsx"
      provides: "Skip navigation link component"
      exports: ["SkipNav"]
    - path: "web/src/components/shared/VisuallyHidden.tsx"
      provides: "Screen-reader-only content wrapper"
      exports: ["VisuallyHidden"]
    - path: "web/src/components/shared/LiveRegion.tsx"
      provides: "aria-live announcement component"
      exports: ["LiveRegion"]
    - path: "web/e2e/axe-test.ts"
      provides: "Shared axe-core Playwright fixture"
      exports: ["test", "expect"]
  key_links:
    - from: "web/src/routes/__root.tsx"
      to: "web/src/components/shared/SkipNav.tsx"
      via: "import and render at top of RootLayout"
      pattern: "import.*SkipNav"
    - from: "web/src/routes/__root.tsx"
      to: "main#main-content"
      via: "id attribute on main element"
      pattern: 'id="main-content"'
---

<objective>
Install @axe-core/playwright, create shared accessibility utility components (SkipNav, VisuallyHidden, LiveRegion), add ARIA landmarks and skip-nav to the root layout, and fix theme contrast for WCAG AA compliance.

Purpose: Foundation layer that all subsequent accessibility plans depend on -- shared components, correct landmarks, and compliant contrast ratios.
Output: Shared a11y components, remediated root layout, axe-core test fixture, contrast-compliant theme.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/45-wcag-compliance-audit/45-CONTEXT.md
@.planning/phases/45-wcag-compliance-audit/45-RESEARCH.md

@web/src/routes/__root.tsx
@web/src/index.css
@web/playwright.config.ts
@web/src/components/field/OfflineBanner.tsx
@web/src/components/field/FieldHeader.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install axe-core, create shared a11y components, create axe fixture</name>
  <files>web/package.json, web/src/components/shared/SkipNav.tsx, web/src/components/shared/VisuallyHidden.tsx, web/src/components/shared/LiveRegion.tsx, web/e2e/axe-test.ts</files>
  <read_first>web/package.json, web/playwright.config.ts, web/src/components/field/OfflineBanner.tsx, web/src/components/field/FieldHeader.tsx</read_first>
  <action>
1. Install @axe-core/playwright as dev dependency:
   ```bash
   cd web && npm install -D @axe-core/playwright
   ```

2. Create `web/src/components/shared/SkipNav.tsx` (per D-08):
   - Export `SkipNav` component accepting optional `targetId` prop (default: `"main-content"`)
   - Render an `<a>` tag with `href={#${targetId}}`
   - Use Tailwind `sr-only` classes by default, `focus:not-sr-only focus:fixed focus:top-4 focus:left-4 focus:z-50 focus:rounded focus:bg-background focus:px-4 focus:py-2 focus:text-foreground focus:shadow-lg focus:ring-2 focus:ring-ring` on focus
   - Text content: "Skip to main content"

3. Create `web/src/components/shared/VisuallyHidden.tsx` (per D-08):
   - Export `VisuallyHidden` component accepting `children` and optional `as` element type (default: `"span"`)
   - Render with Tailwind `sr-only` class
   - This wraps screen-reader-only text

4. Create `web/src/components/shared/LiveRegion.tsx` (per D-08):
   - Export `LiveRegion` component accepting `message: string` and optional `politeness: 'polite' | 'assertive'` (default: `"polite"`)
   - Render a `<div>` with `role="status"`, `aria-live={politeness}`, `aria-atomic="true"`, `className="sr-only"`
   - Children: `{message}`
   - Follow the pattern from `web/src/components/field/OfflineBanner.tsx`

5. Create `web/e2e/axe-test.ts` shared Playwright fixture (per D-01):
   ```typescript
   import { test as base } from '@playwright/test';
   import AxeBuilder from '@axe-core/playwright';

   type AxeFixture = {
     makeAxeBuilder: () => AxeBuilder;
   };

   export const test = base.extend<AxeFixture>({
     makeAxeBuilder: async ({ page }, use) => {
       const makeAxeBuilder = () =>
         new AxeBuilder({ page })
           .withTags(['wcag2a', 'wcag2aa', 'wcag21a', 'wcag21aa'])
           .exclude('.leaflet-container');
       await use(makeAxeBuilder);
     },
   });

   export { expect } from '@playwright/test';
   ```
  </action>
  <verify>
    <automated>cd /home/kwhatcher/projects/civicpulse/run-api/web && node -e "require('@axe-core/playwright')" && test -f src/components/shared/SkipNav.tsx && test -f src/components/shared/VisuallyHidden.tsx && test -f src/components/shared/LiveRegion.tsx && test -f e2e/axe-test.ts && echo "PASS"</automated>
  </verify>
  <acceptance_criteria>
    - web/package.json contains "@axe-core/playwright" in devDependencies
    - web/src/components/shared/SkipNav.tsx contains `export function SkipNav` and `href={` and `sr-only` and `focus:not-sr-only`
    - web/src/components/shared/VisuallyHidden.tsx contains `export function VisuallyHidden` and `sr-only`
    - web/src/components/shared/LiveRegion.tsx contains `export function LiveRegion` and `aria-live` and `aria-atomic`
    - web/e2e/axe-test.ts contains `AxeBuilder` and `wcag2aa` and `.exclude('.leaflet-container')`
  </acceptance_criteria>
  <done>@axe-core/playwright installed, three shared a11y components created, axe-core fixture ready</done>
</task>

<task type="auto">
  <name>Task 2: Add landmarks, skip-nav, and focus indicators to root layout; fix theme contrast</name>
  <files>web/src/routes/__root.tsx, web/src/index.css</files>
  <read_first>web/src/routes/__root.tsx, web/src/index.css, web/src/components/shared/SkipNav.tsx</read_first>
  <action>
1. Edit `web/src/routes/__root.tsx` RootLayout (authenticated admin branch):
   - Import `SkipNav` from `@/components/shared/SkipNav`
   - Add `<SkipNav />` as the FIRST child inside the `<SidebarProvider>` wrapper (before `<AppSidebar />`)
   - Add `id="main-content"` to the `<main>` element (currently `<main className="flex-1 overflow-x-hidden p-4">`)
   - Add `aria-label="Open sidebar"` to `<SidebarTrigger>` (the hamburger menu button)
   - Wrap the `<header>` element with `role="banner"` and `aria-label="Top navigation bar"`
   - The Sidebar component from shadcn already renders as `<aside>` with data attributes -- verify during implementation, add `aria-label="Main navigation"` to the `<Sidebar>` component in AppSidebar if not already present

2. Also add skip-nav to the field route branch of RootLayout:
   - Add `<SkipNav />` as first child in the field route `<div>`
   - The field routes already have their own landmarks via FieldHeader

3. Add `aria-label="User menu"` to the UserMenu DropdownMenuTrigger Button if not present

4. Fix theme contrast in `web/src/index.css` (per D-11):
   - Change `--muted-foreground: oklch(0.556 0 0);` to `--muted-foreground: oklch(0.45 0 0);` in `:root` (light mode)
     - Current oklch(0.556) = ~#8b8b8b = 3.5:1 contrast on white (FAILS AA)
     - New oklch(0.45) = ~#6b6b6b = 5.0:1 contrast on white (PASSES AA 4.5:1)
   - Verify `--destructive: oklch(0.577 0.245 27.325)` -- this is used for error text on white background. If contrast is below 4.5:1, adjust. The computed red at this lightness should be ~4.6:1 which passes, but verify.

5. Add visible focus indicator styles to `web/src/index.css` in the `@layer base` block:
   ```css
   @layer base {
     * {
       @apply border-border outline-ring/50;
     }
     body {
       @apply bg-background text-foreground;
     }
     /* Visible focus indicators for keyboard navigation (A11Y-03) */
     :focus-visible {
       @apply outline-2 outline-offset-2 outline-ring;
     }
   }
   ```
  </action>
  <verify>
    <automated>cd /home/kwhatcher/projects/civicpulse/run-api && grep -q 'SkipNav' web/src/routes/__root.tsx && grep -q 'id="main-content"' web/src/routes/__root.tsx && grep -q 'oklch(0.45 0 0)' web/src/index.css && grep -q 'focus-visible' web/src/index.css && echo "PASS"</automated>
  </verify>
  <acceptance_criteria>
    - web/src/routes/__root.tsx contains `import { SkipNav }` from shared components
    - web/src/routes/__root.tsx contains `<SkipNav` rendered before AppSidebar
    - web/src/routes/__root.tsx contains `id="main-content"` on the main element
    - web/src/routes/__root.tsx contains `aria-label="Open sidebar"` on SidebarTrigger
    - web/src/index.css `:root` block contains `--muted-foreground: oklch(0.45 0 0)` (not 0.556)
    - web/src/index.css contains `:focus-visible` rule with `outline-2 outline-offset-2`
  </acceptance_criteria>
  <done>Root layout has SkipNav, ARIA landmarks, labeled interactive elements; theme passes WCAG AA contrast; visible focus indicators on all focusable elements</done>
</task>

</tasks>

<verification>
- `cd web && npx tsc --noEmit` passes (no TypeScript errors)
- `grep -r "SkipNav\|VisuallyHidden\|LiveRegion" web/src/components/shared/` shows all three components
- `grep "main-content" web/src/routes/__root.tsx` shows id on main element
- `grep "0.45" web/src/index.css` shows updated muted-foreground contrast value
</verification>

<success_criteria>
- Three shared a11y components exist and export correctly
- Root layout renders SkipNav before sidebar, main has id="main-content", header has role="banner"
- Theme contrast ratio for muted-foreground meets 4.5:1 on white
- axe-core fixture ready for parameterized route scanning
- TypeScript compilation passes
</success_criteria>

<output>
After completion, create `.planning/phases/45-wcag-compliance-audit/45-01-SUMMARY.md`
</output>
