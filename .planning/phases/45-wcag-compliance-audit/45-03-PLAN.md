---
phase: 45-wcag-compliance-audit
plan: 03
type: execute
wave: 2
depends_on: ["45-01"]
files_modified:
  - web/e2e/a11y-scan.spec.ts
autonomous: true
requirements:
  - A11Y-01

must_haves:
  truths:
    - "axe-core scan runs against every admin and field route"
    - "Zero critical and serious violations on all scanned routes"
    - "Violation results saved as JSON artifacts per route"
  artifacts:
    - path: "web/e2e/a11y-scan.spec.ts"
      provides: "Parameterized axe-core scan across all routes"
      contains: "makeAxeBuilder"
  key_links:
    - from: "web/e2e/a11y-scan.spec.ts"
      to: "web/e2e/axe-test.ts"
      via: "import shared fixture"
      pattern: "import.*axe-test"
---

<objective>
Create a parameterized Playwright test that runs axe-core WCAG 2.1 AA scans on every admin and field route, saves violation artifacts per route, and gates on zero critical/serious violations. Then fix any violations discovered during the scan.

Purpose: Automated CI-gateable accessibility compliance verification across the entire application.
Output: Passing axe-core scan test covering all routes, with JSON artifact output.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/45-wcag-compliance-audit/45-CONTEXT.md
@.planning/phases/45-wcag-compliance-audit/45-RESEARCH.md
@.planning/phases/45-wcag-compliance-audit/45-01-SUMMARY.md

@web/e2e/axe-test.ts
@web/playwright.config.ts
@web/src/routeTree.gen.ts

<interfaces>
From web/e2e/axe-test.ts (created in Plan 01):
```typescript
import { test as base } from '@playwright/test';
import AxeBuilder from '@axe-core/playwright';

type AxeFixture = {
  makeAxeBuilder: () => AxeBuilder;
};

export const test: TestType<AxeFixture>;
export { expect } from '@playwright/test';
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create parameterized axe-core route scan test</name>
  <files>web/e2e/a11y-scan.spec.ts</files>
  <read_first>web/e2e/axe-test.ts, web/playwright.config.ts, web/src/routeTree.gen.ts</read_first>
  <action>
Create `web/e2e/a11y-scan.spec.ts` (per D-01, D-02, D-03, D-04):

1. Import `test` and `expect` from `./axe-test` (the shared fixture from Plan 01)

2. Define the route array with ALL admin and field routes. Use the full list from RESEARCH.md. Each entry has `path`, `name`, and optional `needsCampaign` boolean:
   ```typescript
   const ROUTES = [
     // Org-level
     { path: '/', name: 'org-dashboard' },
     { path: '/org/members', name: 'org-members' },
     { path: '/org/settings', name: 'org-settings' },
     // Campaign-level
     { path: '/campaigns/$id/dashboard', name: 'campaign-dashboard', needsCampaign: true },
     { path: '/campaigns/$id/voters', name: 'voters-list', needsCampaign: true },
     { path: '/campaigns/$id/voters/$voterId', name: 'voter-detail', needsCampaign: true },
     { path: '/campaigns/$id/voters/imports', name: 'voter-imports', needsCampaign: true },
     { path: '/campaigns/$id/voters/imports/new', name: 'voter-import-new', needsCampaign: true },
     { path: '/campaigns/$id/voters/lists', name: 'voter-lists', needsCampaign: true },
     { path: '/campaigns/$id/voters/lists/$listId', name: 'voter-list-detail', needsCampaign: true },
     { path: '/campaigns/$id/voters/tags', name: 'voter-tags', needsCampaign: true },
     { path: '/campaigns/$id/canvassing', name: 'canvassing-overview', needsCampaign: true },
     { path: '/campaigns/$id/canvassing/turfs/new', name: 'turf-new', needsCampaign: true },
     { path: '/campaigns/$id/canvassing/turfs/$turfId', name: 'turf-edit', needsCampaign: true },
     { path: '/campaigns/$id/canvassing/walk-lists/$walkListId', name: 'walk-list-detail', needsCampaign: true },
     { path: '/campaigns/$id/phone-banking', name: 'phone-banking-overview', needsCampaign: true },
     { path: '/campaigns/$id/phone-banking/sessions', name: 'phone-sessions', needsCampaign: true },
     { path: '/campaigns/$id/phone-banking/sessions/$sessionId', name: 'phone-session-detail', needsCampaign: true },
     { path: '/campaigns/$id/phone-banking/call-lists', name: 'call-lists', needsCampaign: true },
     { path: '/campaigns/$id/phone-banking/call-lists/$callListId', name: 'call-list-detail', needsCampaign: true },
     { path: '/campaigns/$id/phone-banking/dnc', name: 'dnc-list', needsCampaign: true },
     { path: '/campaigns/$id/phone-banking/my-sessions', name: 'my-sessions', needsCampaign: true },
     { path: '/campaigns/$id/volunteers', name: 'volunteers-overview', needsCampaign: true },
     { path: '/campaigns/$id/volunteers/roster', name: 'volunteer-roster', needsCampaign: true },
     { path: '/campaigns/$id/volunteers/$volunteerId', name: 'volunteer-detail', needsCampaign: true },
     { path: '/campaigns/$id/volunteers/shifts', name: 'volunteer-shifts', needsCampaign: true },
     { path: '/campaigns/$id/volunteers/shifts/$shiftId', name: 'shift-detail', needsCampaign: true },
     { path: '/campaigns/$id/volunteers/tags', name: 'volunteer-tags', needsCampaign: true },
     { path: '/campaigns/$id/volunteers/register', name: 'volunteer-register', needsCampaign: true },
     { path: '/campaigns/$id/surveys', name: 'surveys', needsCampaign: true },
     { path: '/campaigns/$id/surveys/$scriptId', name: 'survey-detail', needsCampaign: true },
     { path: '/campaigns/$id/settings', name: 'settings-overview', needsCampaign: true },
     { path: '/campaigns/$id/settings/general', name: 'settings-general', needsCampaign: true },
     { path: '/campaigns/$id/settings/members', name: 'settings-members', needsCampaign: true },
     { path: '/campaigns/$id/settings/danger', name: 'settings-danger', needsCampaign: true },
     // Field routes
     { path: '/field/$id', name: 'field-hub', needsCampaign: true },
     { path: '/field/$id/canvassing', name: 'field-canvassing', needsCampaign: true },
     { path: '/field/$id/phone-banking', name: 'field-phone-banking', needsCampaign: true },
   ];
   ```

3. For each route, create a test using `test.describe` and `test()`:
   ```typescript
   for (const route of ROUTES) {
     test(`a11y scan: ${route.name}`, async ({ page, makeAxeBuilder }, testInfo) => {
       await page.goto(route.path);
       // Wait for any loading states to resolve
       await page.waitForLoadState('networkidle');

       const results = await makeAxeBuilder().analyze();

       // Save full results as JSON artifact (per D-04)
       await testInfo.attach(`axe-${route.name}`, {
         body: JSON.stringify(results, null, 2),
         contentType: 'application/json',
       });

       // Gate on zero critical/serious violations (per D-02)
       const critical = results.violations.filter(
         (v) => v.impact === 'critical' || v.impact === 'serious'
       );

       // Log moderate/minor as warnings
       const warnings = results.violations.filter(
         (v) => v.impact === 'moderate' || v.impact === 'minor'
       );
       if (warnings.length > 0) {
         console.warn(
           `[${route.name}] ${warnings.length} moderate/minor violations:`,
           warnings.map((w) => `${w.id}: ${w.description}`).join(', ')
         );
       }

       expect(critical, `Critical/serious violations on ${route.name}: ${critical.map(v => `${v.id}: ${v.help}`).join('; ')}`).toEqual([]);
     });
   }
   ```

4. Note: The test will navigate to each route. Since this is an E2E test against the preview server, the app needs to be running with seed data. The existing Playwright config handles the webServer setup. Routes with `$id` params will need actual IDs from seed data -- use the same pattern as other e2e tests in the project (check existing tests for how they handle campaign/voter IDs). If existing tests use hardcoded seed IDs, use the same IDs. If they use a setup step, follow that pattern.

5. Important: The test must handle auth. Check how existing e2e tests (e.g., phase35-a11y-audit.spec.ts) handle authentication. Follow the same pattern.
  </action>
  <verify>
    <automated>cd /home/kwhatcher/projects/civicpulse/run-api && grep -q 'makeAxeBuilder' web/e2e/a11y-scan.spec.ts && grep -q 'axe-test' web/e2e/a11y-scan.spec.ts && grep -q 'critical' web/e2e/a11y-scan.spec.ts && grep -c 'name:' web/e2e/a11y-scan.spec.ts | xargs test 30 -le && echo "PASS"</automated>
  </verify>
  <acceptance_criteria>
    - web/e2e/a11y-scan.spec.ts exists and imports from `./axe-test`
    - File contains ROUTES array with at least 35 route entries (admin + field)
    - Each route has `path` and `name` properties
    - Test uses `makeAxeBuilder().analyze()` from the shared fixture
    - Test attaches JSON results via `testInfo.attach()` (per D-04)
    - Test filters violations by `impact === 'critical' || impact === 'serious'` (per D-02)
    - Test expects `critical` array `toEqual([])` -- zero tolerance for critical/serious
    - Moderate/minor violations are logged as console.warn, not assertion failures
  </acceptance_criteria>
  <done>Parameterized axe-core scan covers all admin and field routes, saves JSON artifacts per route, gates on zero critical/serious violations, logs moderate/minor as warnings</done>
</task>

<task type="auto">
  <name>Task 2: Fix any axe-core violations discovered during scan</name>
  <files>web/src/routes/__root.tsx, web/src/components/shared/DataTable.tsx, web/src/components/shared/EmptyState.tsx, web/src/components/shared/TooltipIcon.tsx</files>
  <read_first>web/src/routes/__root.tsx, web/src/components/shared/DataTable.tsx, web/src/components/shared/EmptyState.tsx, web/src/components/shared/TooltipIcon.tsx, web/e2e/a11y-scan.spec.ts</read_first>
  <action>
Run the axe-core scan test from Task 1 and fix any critical/serious violations found. Common violations to expect and fix:

1. **Missing button labels (button-name):**
   - Check `SidebarTrigger` in __root.tsx -- add `aria-label="Open sidebar"` if not already done in Plan 01
   - Check any icon-only buttons throughout shared components -- add `aria-label` attributes
   - The UserMenu `<Button>` in __root.tsx should have `aria-label="User menu"`

2. **Missing link text (link-name):**
   - Check if any `<Link>` components render icon-only without text content
   - Add `aria-label` to icon-only links

3. **Color contrast (color-contrast):**
   - Should be mostly fixed by Plan 01's muted-foreground adjustment
   - If additional violations found, fix specific CSS classes or component styles

4. **Missing form labels (label):**
   - Check DataTable filter inputs for `<label>` or `aria-label`
   - Check search inputs across the app

5. **Image alt text (image-alt):**
   - Check any `<img>` tags for missing `alt` attributes
   - For decorative images, add `alt=""` and `aria-hidden="true"`

6. **Heading hierarchy (heading-order):**
   - Per D-09: audit page headings. Each route should have one h1, sections use h2
   - Fix routes that skip heading levels (e.g., h1 to h3)

7. **TooltipIcon accessibility:**
   - Add `aria-describedby` linkage between the trigger and tooltip content
   - Ensure the Popover trigger has an accessible name

This task is REACTIVE -- run the scan first, then fix what it finds. The specific files listed above are the most likely targets based on the codebase analysis, but fix whatever the scan reports.

Run the fix cycle: scan -> fix -> rescan -> verify zero critical/serious.
  </action>
  <verify>
    <automated>cd /home/kwhatcher/projects/civicpulse/run-api/web && npx playwright test e2e/a11y-scan.spec.ts --reporter=list 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `npx playwright test e2e/a11y-scan.spec.ts` exits with code 0 (all routes pass)
    - Zero critical and serious axe violations on any route
    - JSON artifact files attached to test results for each route
    - Any modified component files pass `npx tsc --noEmit`
  </acceptance_criteria>
  <done>All admin and field routes pass axe-core WCAG 2.1 AA scan with zero critical/serious violations</done>
</task>

</tasks>

<verification>
- `cd web && npx playwright test e2e/a11y-scan.spec.ts --reporter=list` -- all routes pass
- JSON artifacts saved per route in test results
- Zero critical/serious violations across all routes
</verification>

<success_criteria>
- Parameterized axe-core scan test exists and covers 35+ routes
- All routes pass with zero critical/serious violations
- Moderate/minor warnings logged but do not gate CI
- Violation results saved as JSON artifacts per route for auditing
</success_criteria>

<output>
After completion, create `.planning/phases/45-wcag-compliance-audit/45-03-SUMMARY.md`
</output>
