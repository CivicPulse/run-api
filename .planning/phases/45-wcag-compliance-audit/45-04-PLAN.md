---
phase: 45-wcag-compliance-audit
plan: 04
type: execute
wave: 3
depends_on: ["45-01", "45-02", "45-03"]
files_modified:
  - web/e2e/a11y-voter-search.spec.ts
  - web/e2e/a11y-voter-import.spec.ts
  - web/e2e/a11y-walk-list.spec.ts
  - web/e2e/a11y-phone-bank.spec.ts
  - web/e2e/a11y-campaign-settings.spec.ts
autonomous: true
requirements:
  - A11Y-02
  - A11Y-03

must_haves:
  truths:
    - "Screen reader can navigate voter search flow via heading hierarchy and ARIA landmarks"
    - "Screen reader can complete voter import wizard at every step"
    - "Screen reader can navigate walk list creation flow"
    - "Screen reader can navigate phone bank session flow"
    - "Screen reader can navigate campaign settings flow"
    - "All 5 flows are completable via keyboard alone"
  artifacts:
    - path: "web/e2e/a11y-voter-search.spec.ts"
      provides: "Voter search accessibility flow test"
      contains: "ariaSnapshot\\|getByRole"
    - path: "web/e2e/a11y-voter-import.spec.ts"
      provides: "Voter import accessibility flow test"
      contains: "ariaSnapshot\\|getByRole"
    - path: "web/e2e/a11y-walk-list.spec.ts"
      provides: "Walk list creation accessibility flow test"
      contains: "ariaSnapshot\\|getByRole"
    - path: "web/e2e/a11y-phone-bank.spec.ts"
      provides: "Phone bank session accessibility flow test"
      contains: "ariaSnapshot\\|getByRole"
    - path: "web/e2e/a11y-campaign-settings.spec.ts"
      provides: "Campaign settings accessibility flow test"
      contains: "ariaSnapshot\\|getByRole"
  key_links:
    - from: "web/e2e/a11y-voter-search.spec.ts"
      to: "web/src/routes/campaigns/$campaignId/voters"
      via: "Playwright page.goto and keyboard navigation"
      pattern: "page.goto.*voters"
    - from: "web/e2e/a11y-voter-import.spec.ts"
      to: "web/src/routes/campaigns/$campaignId/voters/imports/new"
      via: "Multi-step wizard accessibility verification"
      pattern: "page.goto.*imports"
---

<objective>
Create 5 Playwright accessibility flow tests that verify screen reader compatibility and keyboard operability for the 5 critical flows: voter search, voter import, walk list creation, phone bank session, and campaign settings.

Purpose: Proves the application is usable by screen reader users and keyboard-only users through the most important workflows.
Output: 5 passing flow tests that combine ARIA tree assertions with keyboard navigation verification.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/45-wcag-compliance-audit/45-CONTEXT.md
@.planning/phases/45-wcag-compliance-audit/45-RESEARCH.md
@.planning/phases/45-wcag-compliance-audit/45-01-SUMMARY.md
@.planning/phases/45-wcag-compliance-audit/45-02-SUMMARY.md
@.planning/phases/45-wcag-compliance-audit/45-03-SUMMARY.md

@web/playwright.config.ts
@web/e2e/axe-test.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create voter search and voter import flow tests</name>
  <files>web/e2e/a11y-voter-search.spec.ts, web/e2e/a11y-voter-import.spec.ts</files>
  <read_first>web/e2e/axe-test.ts, web/playwright.config.ts, web/src/routes/campaigns/$campaignId/voters/index.tsx, web/src/routes/campaigns/$campaignId/voters/imports/new.tsx, web/e2e/a11y-scan.spec.ts</read_first>
  <action>
Create two flow test files (per D-05, D-06):

**File 1: `web/e2e/a11y-voter-search.spec.ts`**

Test: "voter search flow is keyboard-operable and screen-reader-friendly"

1. Navigate to the voters list page
2. Verify ARIA landmarks exist:
   - `page.getByRole('navigation')` is visible (sidebar nav)
   - `page.getByRole('main')` is visible (content area)
3. Verify heading hierarchy:
   - Use `page.getByRole('main').ariaSnapshot()` or `page.getByRole('heading', { level: 1 })` to verify an h1 exists
   - Verify no heading levels are skipped
4. Keyboard navigation test:
   - Press Tab repeatedly to reach the search/filter input
   - Verify the focused element has an accessible name (aria-label or associated label)
   - Type a search term via keyboard
   - Verify results update (a table or list appears)
5. Verify labeled form controls:
   - All inputs have associated labels or aria-label
   - Filter dropdowns are keyboard-accessible

**File 2: `web/e2e/a11y-voter-import.spec.ts`** (per D-07)

Test: "voter import wizard is accessible at every step"

1. Navigate to voter import page (/campaigns/$id/voters/imports/new)
2. **Step 1 - File upload:**
   - Verify heading hierarchy (h1 for page, h2 for step)
   - Verify file input has an accessible label
   - Verify the upload button/dropzone is keyboard-accessible
3. **Step 2 - Column mapping (if applicable):**
   - Verify select/dropdown elements have labels
   - Verify keyboard navigation between mapping fields
4. **Step 3 - Preview:**
   - Verify data table has proper table semantics
   - Verify column headers are associated with cells
5. **Step 4 - Import progress:**
   - Verify aria-live region announces progress updates
   - Verify completion message is announced

Each test combines keyboard Tab/Enter navigation with `getByRole()` and `ariaSnapshot()` assertions (per D-06). Follow the same auth/setup pattern used by a11y-scan.spec.ts from Plan 03.
  </action>
  <verify>
    <automated>cd /home/kwhatcher/projects/civicpulse/run-api && test -f web/e2e/a11y-voter-search.spec.ts && test -f web/e2e/a11y-voter-import.spec.ts && grep -q 'getByRole' web/e2e/a11y-voter-search.spec.ts && grep -q 'getByRole' web/e2e/a11y-voter-import.spec.ts && echo "PASS"</automated>
  </verify>
  <acceptance_criteria>
    - web/e2e/a11y-voter-search.spec.ts exists with test for voter search flow
    - Test verifies ARIA landmarks (navigation, main) via `getByRole`
    - Test verifies heading hierarchy via `getByRole('heading')`
    - Test includes keyboard navigation via `page.keyboard.press('Tab')` and `page.keyboard.type()`
    - web/e2e/a11y-voter-import.spec.ts exists with multi-step wizard test (per D-07)
    - Import test verifies accessibility at each wizard step, not just start/end
    - Both tests use Playwright ARIA assertions (`getByRole`, `ariaSnapshot`, `toHaveAttribute`)
  </acceptance_criteria>
  <done>Voter search and voter import flow tests verify keyboard operability and screen reader compatibility</done>
</task>

<task type="auto">
  <name>Task 2: Create walk list, phone bank, and campaign settings flow tests</name>
  <files>web/e2e/a11y-walk-list.spec.ts, web/e2e/a11y-phone-bank.spec.ts, web/e2e/a11y-campaign-settings.spec.ts</files>
  <read_first>web/e2e/a11y-voter-search.spec.ts, web/src/routes/campaigns/$campaignId/canvassing/index.tsx, web/src/routes/campaigns/$campaignId/phone-banking/index.tsx, web/src/routes/campaigns/$campaignId/settings/index.tsx</read_first>
  <action>
Create three flow test files (per D-05, D-06):

**File 1: `web/e2e/a11y-walk-list.spec.ts`**

Test: "walk list creation flow is keyboard-operable and screen-reader-friendly"

1. Navigate to canvassing overview page
2. Verify ARIA landmarks (nav, main) and heading hierarchy (h1)
3. Keyboard navigate to a turf or walk list link
4. Verify the turf form (if navigating to create):
   - Skip-map link is tab-reachable (from Plan 02)
   - Form fields have labels
   - Submit button is keyboard-accessible
5. Verify walk list detail page:
   - Table has proper semantics
   - Keyboard can reach action buttons

**File 2: `web/e2e/a11y-phone-bank.spec.ts`**

Test: "phone bank session flow is keyboard-operable and screen-reader-friendly"

1. Navigate to phone banking overview page
2. Verify ARIA landmarks and heading hierarchy
3. Keyboard navigate to session list or create button
4. Verify session detail page:
   - Caller assignment controls are keyboard-accessible
   - Call outcome buttons have accessible names
   - Status updates use aria-live regions

**File 3: `web/e2e/a11y-campaign-settings.spec.ts`**

Test: "campaign settings flow is keyboard-operable and screen-reader-friendly"

1. Navigate to campaign settings page
2. Verify ARIA landmarks and heading hierarchy
3. Verify tab/section navigation:
   - Settings tabs (general, members, danger) are keyboard-navigable
   - Tab panels have proper ARIA roles (shadcn Tabs should handle this)
4. Verify form fields:
   - All settings inputs have labels
   - Save button is keyboard-accessible
5. Verify danger zone:
   - Destructive actions have confirmation dialogs
   - Dialog is keyboard-trapped and returns focus on close (per D-10)

All three tests follow the same pattern as Task 1 files: combined keyboard navigation + ARIA tree assertions (per D-06). Use the same auth/setup pattern from the voter search test.

After creating all 5 flow tests, run them:
```bash
cd web && npx playwright test --grep "a11y" --reporter=list
```

If any tests fail due to actual accessibility issues in the components (not test logic bugs), fix the underlying component accessibility issues. Common fixes:
- Add missing aria-label to icon-only buttons
- Add missing label associations to form inputs
- Fix heading hierarchy on specific route pages
- Add aria-live to status update regions
  </action>
  <verify>
    <automated>cd /home/kwhatcher/projects/civicpulse/run-api && test -f web/e2e/a11y-walk-list.spec.ts && test -f web/e2e/a11y-phone-bank.spec.ts && test -f web/e2e/a11y-campaign-settings.spec.ts && grep -q 'getByRole' web/e2e/a11y-walk-list.spec.ts && grep -q 'keyboard' web/e2e/a11y-phone-bank.spec.ts && grep -q 'getByRole' web/e2e/a11y-campaign-settings.spec.ts && echo "PASS"</automated>
  </verify>
  <acceptance_criteria>
    - web/e2e/a11y-walk-list.spec.ts exists with walk list creation flow test
    - web/e2e/a11y-phone-bank.spec.ts exists with phone bank session flow test
    - web/e2e/a11y-campaign-settings.spec.ts exists with campaign settings flow test
    - All three tests verify ARIA landmarks via `getByRole('navigation')` and `getByRole('main')`
    - All three tests include keyboard navigation assertions
    - Campaign settings test verifies dialog focus management (per D-10)
    - All 5 flow tests pass: `cd web && npx playwright test --grep "a11y" --reporter=list` exits 0
  </acceptance_criteria>
  <done>All 5 critical flows verified as keyboard-operable and screen-reader-friendly via Playwright ARIA assertions</done>
</task>

</tasks>

<verification>
- `cd web && npx playwright test --grep "a11y" --reporter=list` -- all tests pass
- 5 flow test files exist in web/e2e/
- Each flow combines keyboard navigation and ARIA tree assertions
</verification>

<success_criteria>
- 5 critical flow tests pass: voter search, voter import, walk list creation, phone bank session, campaign settings
- Each test verifies ARIA landmarks, heading hierarchy, and keyboard navigation
- Voter import test covers every wizard step (per D-07)
- All interactive components in tested flows are reachable via keyboard with visible focus
- Tests combine keyboard-only navigation AND accessibility tree assertions in single tests (per D-06)
</success_criteria>

<output>
After completion, create `.planning/phases/45-wcag-compliance-audit/45-04-SUMMARY.md`
</output>
