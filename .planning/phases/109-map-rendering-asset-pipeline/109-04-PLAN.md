---
phase: 109-map-rendering-asset-pipeline
plan: 04
plan_id: 109-04
type: execute
wave: 4
depends_on: [109-02, 109-03]
files_modified:
  - web/src/components/canvassing/map/VoterMarkerLayer.test.tsx
  - web/src/components/canvassing/map/leafletIcons.test.ts
autonomous: true
requirements: [TEST-01, TEST-02, MAP-01]
requirement_ids: [TEST-01, TEST-02, MAP-01]
task_count: 2
objective: "Extend unit/integration test coverage for every map view that consumes leafletIcons.ts so the MAP-01 regression cannot return silently. VoterMarkerLayer gains a render test asserting it passes the bundled voterIcon to <Marker>; leafletIcons.ts already has factory tests from 109-02 — this plan adds downstream consumer coverage."
must_haves:
  truths:
    - "VoterMarkerLayer has a render test asserting each mapped voter is wrapped in a <Marker> receiving `icon === voterIcon` (identity, not deep equality)."
    - "VoterMarkerLayer test asserts `voterIcon.options.iconUrl` does not match /unpkg/."
    - "leafletIcons.test.ts has coverage for every exported factory (added in 109-02) — this plan only adds consumer tests."
  artifacts:
    - path: web/src/components/canvassing/map/VoterMarkerLayer.test.tsx
      provides: "Render test + consumer-of-leafletIcons test."
      contains: "voterIcon"
  key_links:
    - from: "web/src/components/canvassing/map/VoterMarkerLayer.test.tsx"
      to: "web/src/components/canvassing/map/leafletIcons.ts"
      via: "import { voterIcon } from \"./leafletIcons\""
      pattern: "leafletIcons"
---

<objective>
Add unit/integration test coverage locking the MAP-01 fix in place at
the consumer level. Plan 109-02 tested the icon factory internals; this
plan tests that `VoterMarkerLayer` actually uses the bundled
`voterIcon` (not a stale local copy) and that its render path emits
`<Marker icon={voterIcon} />` for every mapped voter.

Purpose: guard against someone re-introducing a local icon definition
in `VoterMarkerLayer.tsx` or accidentally swapping back to unpkg URLs.

Output: extended `VoterMarkerLayer.test.tsx`.

Note on scope: `CanvassingMap.test.tsx` already has 12 tests from
phase 108 that exercise the marker render path, and Plan 109-02
Task 2 verifies they continue to pass after the icon refactor. Adding
a 13th test there just to assert `icon` identity is redundant because
the 12 existing tests capture `props.icon` via the react-leaflet mock
and phase 108's tests would fail immediately if the icon came back
undefined. This plan therefore focuses entirely on VoterMarkerLayer —
the only map view that did NOT already have a marker-identity assertion
before phase 109.

The E2E test for MAP-02 (list-vs-map layout) lives in Plan 109-05.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/109-map-rendering-asset-pipeline/109-CONTEXT.md
@web/src/components/canvassing/map/VoterMarkerLayer.tsx
@web/src/components/canvassing/map/VoterMarkerLayer.test.tsx
@web/src/components/canvassing/map/leafletIcons.ts
@web/src/components/canvassing/map/__mocks__/leaflet.ts
@web/src/components/field/CanvassingMap.test.tsx

<interfaces>
Existing `VoterMarkerLayer.test.tsx` uses the shared leaflet mock. To
assert icon identity we need the react-leaflet mock to capture the
props passed to `<Marker>` — mirror the pattern from
`CanvassingMap.test.tsx` (top-level `vi.mock("react-leaflet", ...)`
with a `capturedMarkerProps` array).

Post-109-02, `voterIcon` lives at
`web/src/components/canvassing/map/leafletIcons.ts`.

The test must mock `leafletIcons.ts` or import the real module — the
simplest path is to import the real module and compare by reference
identity (`expect(capturedProps.icon).toBe(voterIcon)`) because
`leafletIcons.ts` is a pure ES module and the L.Icon mock from
`__mocks__/leaflet.ts` means `new L.Icon(...)` returns a deterministic
value under test.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: VoterMarkerLayer render test — every voter gets a Marker with voterIcon</name>
  <files>
web/src/components/canvassing/map/VoterMarkerLayer.test.tsx
  </files>
  <behavior>
- Rendering `<VoterMarkerLayer voters={[v1, v2, v3]} />` where all three have valid coords yields exactly 3 captured `<Marker>` prop sets.
- Each captured marker's `icon` is the same identity as the exported `voterIcon` from `leafletIcons.ts`.
- Each captured marker's `position` matches its voter's `[latitude, longitude]` tuple.
- Voters with `null` latitude or longitude are filtered out (do NOT render a marker).
- When all voters have null coords, the component returns `null` — no markers captured.
  </behavior>
  <action>
Read the current `VoterMarkerLayer.test.tsx` to understand its existing
mock setup. Extend (don't replace) the existing tests to add:

1. A top-level `vi.mock("react-leaflet", ...)` that captures `Marker`
   props into a module-level `capturedMarkerProps: any[]` array
   (mirror the pattern at the top of
   `web/src/components/field/CanvassingMap.test.tsx` lines 26-85).
   If the existing file already uses `setupLeafletMocks()` which
   defines its own non-capturing Marker mock, REPLACE that helper call
   with the top-level capturing mock for this file (same resolution
   that 108-03 applied when extending CanvassingMap.test.tsx — recorded
   in 108-03-SUMMARY.md "Auto-fixed Issues" section).

2. New test: `"renders one Marker per valid voter with voterIcon"`:
   - beforeEach: `capturedMarkerProps.length = 0`.
   - Arrange three `VoterLocation` fixtures — two with coords, one
     with `latitude: null`.
   - Render `<VoterMarkerLayer voters={fixtures} />`.
   - Import `voterIcon` from `@/components/canvassing/map/leafletIcons`.
   - Assert `capturedMarkerProps.length === 2` (the null-coord voter
     filtered out).
   - Assert every `capturedMarkerProps[i].icon === voterIcon` (identity check).
   - Assert `capturedMarkerProps[0].position` deep-equals
     `[fixtures[0].latitude, fixtures[0].longitude]`.

3. New test: `"voterIcon bundles a non-unpkg asset (MAP-01 regression guard)"`:
   - Import `voterIcon`.
   - Assert `voterIcon.options.iconUrl` is a truthy string and does
     NOT match `/unpkg\.com/`.

4. New test: `"renders nothing when every voter has null coords"`:
   - Render with three null-coord voters.
   - Assert `capturedMarkerProps.length === 0`.
   - Assert the rendered container has no `data-testid="mock-marker"`
     children.

Run the tests — new tests MUST pass on the post-109-02 tree, because
109-02 already routed VoterMarkerLayer through the consolidated
`voterIcon`. If any test fails, investigate whether the react-leaflet
mock is correctly capturing props — the failure mode is usually a
stale `setupLeafletMocks()` shadowing the capturing mock (exactly the
issue 108-03 hit).

Commit: `test(109-04): lock VoterMarkerLayer voterIcon consumer contract (MAP-01, TEST-01)`.
  </action>
  <verify>
    <automated>cd web && npx vitest run src/components/canvassing/map/VoterMarkerLayer.test.tsx</automated>
  </verify>
  <done>
Three new tests added and passing. `voterIcon` identity check passes,
null-coord filtering test passes, regression-guard test passes. All
existing tests in the file still pass.
  </done>
</task>

<task type="auto">
  <name>Task 2: Extend leafletIcons.test.ts with integration-style assertions</name>
  <files>
web/src/components/canvassing/map/leafletIcons.test.ts
  </files>
  <action>
Plan 109-02 already created `leafletIcons.test.ts` with 8 factory unit
tests. This task adds two integration-style tests that exercise the
module AS AN ES MODULE with Vite's real asset-resolver (no leaflet mock
for the asset URLs — only the `L.Icon`/`L.DivIcon` constructors are
mocked to capture their options):

1. **"every icon's asset paths come from leaflet/dist/images (not remote)"** —
   import every factory, collect their `iconUrl`/`iconRetinaUrl`/
   `shadowUrl`/`html` asset references, assert none start with `http`
   or `//`. (Bundled Vite URLs typically look like
   `/src/assets/marker-icon-abc123.png` in tests or `data:` URIs; either
   is fine. The regression guard is "does not contain a remote scheme".)

2. **"all four icon factories share the same bundled marker-icon asset URL"** —
   assert `voterIcon.options.iconUrl === volunteerIcon.options.iconUrl`.
   This catches the regression where someone adds a new remote URL in
   only one factory. (If this assertion is undesirable for some future
   refactor, the test can be deleted with justification — but until
   then it's the tightest lock on asset consolidation.)

Commit: `test(109-04): integration-level regression guards for leafletIcons module (TEST-02)`.
  </action>
  <verify>
    <automated>cd web && npx vitest run src/components/canvassing/map/leafletIcons.test.ts</automated>
  </verify>
  <done>
Two additional tests in `leafletIcons.test.ts` pass. All existing
factory tests still pass. Commit landed.
  </done>
</task>

</tasks>

<verification>
- `cd web && npx vitest run src/components/canvassing/map/VoterMarkerLayer.test.tsx src/components/canvassing/map/leafletIcons.test.ts` all green.
- Three new consumer tests in VoterMarkerLayer.test.tsx.
- Two new integration tests in leafletIcons.test.ts.
- No remote URLs pass any regression-guard assertion.
</verification>

<success_criteria>
- [ ] VoterMarkerLayer render path has a test asserting `capturedProps.icon === voterIcon` (identity).
- [ ] Regression-guard test in VoterMarkerLayer.test.tsx asserts `voterIcon.options.iconUrl` does not match `/unpkg/`.
- [ ] leafletIcons.test.ts has a cross-factory asset-path test (shared bundled URL).
- [ ] All vitest runs green.
- [ ] Commits follow `test(109-04):` prefix.
</success_criteria>

<output>
After completion, create `.planning/phases/109-map-rendering-asset-pipeline/109-04-SUMMARY.md`.
</output>
