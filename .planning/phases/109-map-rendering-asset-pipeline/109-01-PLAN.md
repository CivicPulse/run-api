---
phase: 109-map-rendering-asset-pipeline
plan: 01
plan_id: 109-01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/109-map-rendering-asset-pipeline/109-ASSET-AUDIT.md
autonomous: true
requirements: [MAP-03]
requirement_ids: [MAP-03]
task_count: 2
objective: "Catalog every Leaflet-related asset (icon, sprite, CSS image, tile URL) across the frontend and document its resolution status under dev, preview, and production builds."
must_haves:
  truths:
    - "A single document lists every site where Leaflet assets are loaded."
    - "Every asset entry has a verified resolution result for dev, preview, and production."
    - "Any asset that fails in any environment is called out in an Open Issues section that Plan 109-02 can act on."
  artifacts:
    - path: .planning/phases/109-map-rendering-asset-pipeline/109-ASSET-AUDIT.md
      provides: "Complete map asset audit table + open issues list for MAP-03."
      contains: "| File | Asset | Import style | Dev | Preview | Production |"
  key_links:
    - from: ".planning/phases/109-map-rendering-asset-pipeline/109-ASSET-AUDIT.md"
      to: "web/src/components/canvassing/map/VoterMarkerLayer.tsx"
      via: "Explicit row identifying the unpkg.com remote URL"
      pattern: "unpkg"
    - from: ".planning/phases/109-map-rendering-asset-pipeline/109-ASSET-AUDIT.md"
      to: "web/src/components/field/CanvassingMap.tsx"
      via: "Rows for volunteerIcon, householdIcon, activeHouseholdIcon"
      pattern: "CanvassingMap"
---

<objective>
Produce `109-ASSET-AUDIT.md` — the MAP-03 deliverable — a complete catalog of
every Leaflet-related asset loaded by the frontend, with its import path,
serve strategy, and verified resolution status under `npm run dev`,
`npm run build && npm run preview`, and the production Docker image build.

The audit blocks Plan 109-02 (marker icon fixes) because the fix list comes
straight from the Open Issues section of this document.

Purpose: one source of truth listing every icon, sprite, CSS image, and
tile URL the maps depend on, so no asset regression slips through.

Output: `.planning/phases/109-map-rendering-asset-pipeline/109-ASSET-AUDIT.md`
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/109-map-rendering-asset-pipeline/109-CONTEXT.md
@.planning/REQUIREMENTS.md
@web/src/components/canvassing/map/MapProvider.tsx
@web/src/components/canvassing/map/VoterMarkerLayer.tsx
@web/src/components/field/CanvassingMap.tsx

<interfaces>
<!-- Known asset sites discovered during planning. The audit MUST cover at least these. -->

From web/src/components/field/CanvassingMap.tsx:
```typescript
// volunteerIcon — L.Icon, references /leaflet/marker-icon(-2x|-shadow).png served from web/public/leaflet/
const volunteerIcon = new L.Icon({
  iconUrl: "/leaflet/marker-icon.png",
  iconRetinaUrl: "/leaflet/marker-icon-2x.png",
  shadowUrl: "/leaflet/marker-shadow.png",
  // ...
})

// householdIcon + activeHouseholdIcon — L.DivIcon, HTML inlines <img src="/leaflet/marker-icon.png" ...>
```

From web/src/components/canvassing/map/VoterMarkerLayer.tsx:
```typescript
// voterIcon — L.Icon, references UNPKG REMOTE URLS
const voterIcon = new L.Icon({
  iconUrl: "https://unpkg.com/leaflet@1.9.4/dist/images/marker-icon.png",
  iconRetinaUrl: "https://unpkg.com/leaflet@1.9.4/dist/images/marker-icon-2x.png",
  shadowUrl: "https://unpkg.com/leaflet@1.9.4/dist/images/marker-shadow.png",
  // ...
})
```

From web/src/components/canvassing/map/MapProvider.tsx:
```typescript
import "leaflet/dist/leaflet.css"

// Tile layers:
const STREET_TILES = "https://{s}.tile.openstreetmap.org/{z}/{x}/{y}.png"
const SATELLITE_TILES = "https://server.arcgisonline.com/ArcGIS/rest/services/World_Imagery/MapServer/tile/{z}/{y}/{x}"
```

From web/public/leaflet/:
- marker-icon.png
- marker-icon-2x.png
- marker-shadow.png
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Grep every Leaflet asset site</name>
  <files>(read-only discovery)</files>
  <action>
Run a deterministic set of greps against `web/src/` and `web/public/` to
enumerate every Leaflet asset touch point. Record each hit with file,
line, and import style. Minimum grep set (use Grep tool, not shell):

1. `new L\.Icon|new L\.DivIcon|L\.icon\(|L\.divIcon\(` over `web/src/**/*.{ts,tsx}` — every icon construction site.
2. `iconUrl|iconRetinaUrl|shadowUrl` over `web/src/**/*.{ts,tsx}` — every asset path inside icon constructors.
3. `leaflet/dist/leaflet\.css|leaflet\.css` over `web/src/**/*.{ts,tsx,css}` — CSS imports that pull in Leaflet marker sprite URLs.
4. `url\(.*marker|url\(.*leaflet` over `web/src/**/*.{css,scss}` and `web/public/**/*.css` — CSS-side asset references.
5. `TileLayer|tileLayer\(|tile\.openstreetmap|arcgisonline|mapbox|maptiler` over `web/src/**/*.{ts,tsx}` — tile layer URL templates.
6. `ls web/public/leaflet/` — bundled static assets served by Vite `public/`.
7. `unpkg\.com|cdn\.jsdelivr|cdnjs` over `web/src/**/*.{ts,tsx}` — remote/CDN asset references (known fragile pattern).

For each hit, classify the import style as one of:
- `es-import` — `import icon from "…"` (Vite fingerprints + bundles, always safe)
- `public-static` — `/leaflet/foo.png` served from `web/public/` (works only if the file exists in `public/`)
- `css-url` — `url(…)` inside a CSS file imported by `leaflet/dist/leaflet.css` (Vite rewrites if bundled correctly)
- `remote-cdn` — remote URL like `https://unpkg.com/...` (fragile — offline break, rate limits, CDN outage)
- `tile-template` — tile layer URL with `{z}/{x}/{y}` placeholders (always remote, expected)

Keep the raw hit list in a scratch buffer; Task 2 formats it into the audit document.
  </action>
  <verify>
    <automated>MISSING — discovery task; verification is Task 2 writing the audit doc</automated>
  </verify>
  <done>Every L.Icon/L.DivIcon site in `web/src/` is enumerated with file, line, asset path, and classification. The `voterIcon` unpkg rows and the `CanvassingMap` `/leaflet/*` rows are both captured.</done>
</task>

<task type="auto">
  <name>Task 2: Write 109-ASSET-AUDIT.md</name>
  <files>.planning/phases/109-map-rendering-asset-pipeline/109-ASSET-AUDIT.md</files>
  <action>
Create `.planning/phases/109-map-rendering-asset-pipeline/109-ASSET-AUDIT.md`
with the following sections:

1. **Header** — phase, date, scope statement ("MAP-03 deliverable: every
   Leaflet-related asset catalogued with resolution status under dev,
   preview, and production").

2. **Method** — exact greps run in Task 1, the classification scheme, and
   the verification protocol below.

3. **Asset Catalog** — Markdown table with columns:
   `| File | Line | Asset path | Import style | Dev | Preview | Production | Notes |`
   One row per asset reference found in Task 1. At minimum the table must
   include rows for:
   - `web/src/components/field/CanvassingMap.tsx` `volunteerIcon` (3 asset paths: marker-icon, -2x, -shadow)
   - `web/src/components/field/CanvassingMap.tsx` `householdIcon` / `activeHouseholdIcon` inline `<img>` srcs
   - `web/src/components/canvassing/map/VoterMarkerLayer.tsx` `voterIcon` (3 unpkg URLs)
   - `web/src/components/canvassing/map/MapProvider.tsx` `leaflet/dist/leaflet.css` import (→ Leaflet's own CSS marker URLs)
   - `MapProvider.tsx` `STREET_TILES` OpenStreetMap template
   - `MapProvider.tsx` `SATELLITE_TILES` ArcGIS template
   - Any additional hits Task 1 discovered (document comprehensively — no "etc.")

4. **Verification Protocol** — actual commands run to fill the Dev /
   Preview / Production columns:
   - **Dev:** `cd web && npm run dev` + load `/field/{campaignId}/canvassing` + open DevTools Network tab + filter `marker-icon|marker-shadow|tile` + record which URLs return 200 vs 404/network error. Also check Console for broken-image warnings. Record the exact request URL Vite emitted.
   - **Preview:** `cd web && npm run build && npm run preview` + repeat the same DevTools pass. Document how Vite fingerprinted each bundled asset (hashed filename) vs served untouched (public/).
   - **Production:** Inspect the production Docker image build of `web/` (the `Dockerfile` + build step). Confirm `web/dist/` contains the expected hashed asset files. Since running production in docker for a single screenshot is out of scope, `preview` is a sufficient proxy for this audit — explicitly document that choice in the Method section and cite preview results for the Production column when they match.
   - For each failing asset, add an entry to the Open Issues section below.

5. **Open Issues** — bulleted list of every asset that failed in any
   environment or that uses a fragile pattern (`remote-cdn`). Each entry
   names the owning file, the asset path, the failure mode, and the
   proposed fix category:
   - `bundle-es-import` — convert to `import marker from "leaflet/dist/images/marker-icon.png"` so Vite fingerprints it
   - `copy-to-public` — copy the asset under `web/public/leaflet/` and reference as `/leaflet/foo.png`
   - `convert-to-divicon` — replace the L.Icon with L.DivIcon + inline SVG for truly decorative uses

   Plan 109-02 consumes this Open Issues list verbatim — every entry here
   becomes a task in that plan.

6. **Out-of-scope / Acknowledged** — tile templates (`{z}/{x}/{y}` remote
   URLs) are acknowledged as always-remote and not an audit failure; note
   offline tile caching is deferred to Phase 110.

Commit message: `docs(109-01): map asset pipeline audit (MAP-03)`
  </action>
  <verify>
    <automated>test -s .planning/phases/109-map-rendering-asset-pipeline/109-ASSET-AUDIT.md && grep -q "Open Issues" .planning/phases/109-map-rendering-asset-pipeline/109-ASSET-AUDIT.md && grep -q "unpkg" .planning/phases/109-map-rendering-asset-pipeline/109-ASSET-AUDIT.md && grep -q "VoterMarkerLayer" .planning/phases/109-map-rendering-asset-pipeline/109-ASSET-AUDIT.md && grep -q "CanvassingMap" .planning/phases/109-map-rendering-asset-pipeline/109-ASSET-AUDIT.md</automated>
  </verify>
  <done>
`109-ASSET-AUDIT.md` exists, contains every asset site from Task 1 in a
catalog table, each row has Dev/Preview/Production verification results,
every failure lands in the Open Issues section with a fix category, and
the file is committed with `docs(109-01): map asset pipeline audit (MAP-03)`.
  </done>
</task>

</tasks>

<verification>
- `109-ASSET-AUDIT.md` exists and is non-empty.
- The catalog table includes every grep hit from Task 1 — at minimum the 8 baseline rows listed in Task 2.
- Open Issues section is not empty (the `voterIcon` unpkg remote URL is a known finding).
- File is committed individually with conventional-commit prefix `docs(109-01):`.
</verification>

<success_criteria>
- [ ] MAP-03 audit document exists at the prescribed path.
- [ ] Every Leaflet asset site in `web/src/` appears in the catalog table.
- [ ] Dev, Preview, and Production columns are filled (with a documented rationale if Production cites Preview as proxy).
- [ ] Open Issues section enumerates at least the `voterIcon` unpkg rows as `remote-cdn` / `bundle-es-import`.
- [ ] Commit recorded with `docs(109-01): map asset pipeline audit (MAP-03)`.
</success_criteria>

<output>
After completion, create `.planning/phases/109-map-rendering-asset-pipeline/109-01-SUMMARY.md`
summarizing the audit headline findings and linking to `109-ASSET-AUDIT.md`.
</output>
