---
phase: 109-map-rendering-asset-pipeline
plan: 06
plan_id: 109-06
type: execute
wave: 5
depends_on: [109-01, 109-02, 109-03, 109-04, 109-05]
files_modified:
  - .planning/phases/109-map-rendering-asset-pipeline/109-VERIFICATION-RESULTS.md
autonomous: true
requirements: [MAP-01, MAP-02, MAP-03, TEST-01, TEST-02, TEST-03]
requirement_ids: [MAP-01, MAP-02, MAP-03, TEST-01, TEST-02, TEST-03]
task_count: 2
objective: "Phase 109 exit gate: run the 4-suite verification (ruff + pytest + vitest + tsc + Playwright via run-e2e.sh) and capture pass/fail evidence in 109-VERIFICATION-RESULTS.md. Mirror the 108-07 structure."
must_haves:
  truths:
    - "ruff check + ruff format --check report clean."
    - "pytest reports no unexpected failures (no delta from phase 108 baseline)."
    - "vitest reports no unexpected failures and new tests from 109-02/03/04 are included in the green count."
    - "TypeScript check (`tsc --noEmit`) is clean."
    - "Playwright via `web/scripts/run-e2e.sh` runs green twice consecutively, including the new 109-05 spec."
    - "109-VERIFICATION-RESULTS.md captures exact counts, timestamps, and jsonl references for every suite."
  artifacts:
    - path: .planning/phases/109-map-rendering-asset-pipeline/109-VERIFICATION-RESULTS.md
      provides: "Phase 109 exit gate evidence — pass/fail counts, timestamps, commands run, jsonl log references."
      contains: "Exit gate PASSED"
  key_links:
    - from: ".planning/phases/109-map-rendering-asset-pipeline/109-VERIFICATION-RESULTS.md"
      to: "web/e2e-runs.jsonl"
      via: "timestamps of the two consecutive green Playwright runs"
      pattern: "e2e-runs.jsonl"
---

<objective>
Phase 109 exit gate. Follow the same four-suite verification protocol
that phase 108 used in Plan 108-07: ruff + pytest + vitest + tsc +
Playwright via `web/scripts/run-e2e.sh`. Record results in
`109-VERIFICATION-RESULTS.md` with exact counts and timestamps.

Purpose: close phase 109 on a trustworthy baseline so phase 110 starts
clean.

Output: `.planning/phases/109-map-rendering-asset-pipeline/109-VERIFICATION-RESULTS.md`
marked "Exit gate PASSED" with evidence, OR the phase is not complete.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/109-map-rendering-asset-pipeline/109-CONTEXT.md
@.planning/phases/109-map-rendering-asset-pipeline/109-ASSET-AUDIT.md
@.planning/phases/108-house-selection-active-state/108-07-PLAN.md

<interfaces>
Four-suite verification commands (from 108-07):

```bash
# 1. Python linting
uv run ruff check .
uv run ruff format --check .

# 2. Python tests
uv run pytest

# 3. Frontend typecheck + unit/integration tests
cd web && npx tsc --noEmit
cd web && npx vitest run

# 4. Playwright E2E (MUST use wrapper, logs to e2e-runs.jsonl)
cd web && ./scripts/run-e2e.sh
# Run twice consecutively — both must be green.
```

Expected baseline (from phase 108 exit):
- pytest: 1118 pass / 0 fail / 0 error
- vitest: 721 pass / 0 fail / 24 todo (phase 109 adds ~6-9 new tests; expect 727-730 pass)
- Playwright: 304 pass / 0 fail / 66 skip (phase 109 adds ~3-4 new tests in canvassing-map-rendering.spec.ts; expect 307-308 pass)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run all four suites and collect evidence</name>
  <files>(verification only)</files>
  <action>
Run each suite in sequence and capture output to a scratch buffer:

1. **Ruff** — run `uv run ruff check .` and `uv run ruff format --check .`.
   Expected: 0 errors. Capture stdout/exit code.

2. **Pytest** — run `uv run pytest` from repo root. Expected: same
   counts as 108 baseline (1118 pass), no delta since phase 109 is
   frontend-only. Capture the summary line (e.g. `1118 passed in 42.3s`).

3. **Typecheck** — `cd web && npx tsc --noEmit`. Expected: exit 0, no
   errors.

4. **Vitest** — `cd web && npx vitest run`. Capture the summary line.
   Expected: baseline + new tests from 109-02/03/04. Compute the delta
   and record it.

5. **Playwright run 1** — `cd web && ./scripts/run-e2e.sh`. Expected:
   all pass, including `canvassing-map-rendering.spec.ts`. Record the
   timestamp from `web/e2e-runs.jsonl` (the last line after the run
   completes).

6. **Playwright run 2** — `cd web && ./scripts/run-e2e.sh` a second
   time (consecutive-green protocol from 107-09 / 108-07). Record the
   second timestamp.

If any suite fails, STOP and diagnose. Do NOT write
`109-VERIFICATION-RESULTS.md` claiming exit-gate pass if any suite
was not green. Stale Playwright auth state is the known failure mode
(107-09 lesson) — clear `web/playwright/.auth/*.json` and let the
wrapper re-mint tokens before assuming a real regression.
  </action>
  <verify>
    <automated>uv run ruff check . && uv run pytest -q && cd web && npx tsc --noEmit && npx vitest run && ./scripts/run-e2e.sh</automated>
  </verify>
  <done>
All four suites green. Two consecutive Playwright runs recorded in
`web/e2e-runs.jsonl`. Exact counts and timestamps captured in scratch
buffer for Task 2.
  </done>
</task>

<task type="auto">
  <name>Task 2: Write 109-VERIFICATION-RESULTS.md</name>
  <files>.planning/phases/109-map-rendering-asset-pipeline/109-VERIFICATION-RESULTS.md</files>
  <action>
Create the verification results document following the 108-07 /
107-09 template. Required sections:

1. **Header** — phase, date, status (PASSED / FAILED), baseline
   reference (phase 108 exit gate).

2. **Summary table**:
   ```
   | Suite | Command | Count | Delta from 108 | Status |
   |-------|---------|-------|----------------|--------|
   | Ruff  | ...     | 0 errors | 0 | PASS |
   | Pytest | ...    | 1118 pass / 0 fail | 0 | PASS |
   | Tsc   | ...     | 0 errors | 0 | PASS |
   | Vitest | ...    | N pass / 0 fail / 24 todo | +M | PASS |
   | Playwright | ... | N pass / 0 fail / 66 skip | +M | PASS |
   ```

3. **Playwright two-green-runs evidence** — exact timestamps from
   `web/e2e-runs.jsonl` for the two consecutive green runs. Include
   the raw jsonl lines as a code block.

4. **Requirement closure table** — one row per MAP-01/02/03 and
   TEST-01/02/03, with a pointer to the plan that closed it and the
   primary evidence artifact:
   ```
   | REQ | Plan | Evidence |
   |-----|------|----------|
   | MAP-01 | 109-02 | leafletIcons.ts + VoterMarkerLayer refactor + 109-02 preview screenshot |
   | MAP-02 | 109-03 | canvassing-map-wrapper--inert + Sheet z-index override + canvassing.test.tsx |
   | MAP-03 | 109-01 | 109-ASSET-AUDIT.md |
   | TEST-01 | 109-02, 109-04 | leafletIcons.test.ts + VoterMarkerLayer.test.tsx |
   | TEST-02 | 109-04 | leafletIcons.test.ts integration assertions |
   | TEST-03 | 109-05 | canvassing-map-rendering.spec.ts |
   ```

5. **Deviations / notes** — anything unexpected during the runs (stale
   auth clear, flake retries, etc.).

6. **Exit gate statement** — one line: "Phase 109 exit gate PASSED
   on {YYYY-MM-DD}" OR "NOT PASSED: {reason}".

Commit: `docs(109-06): phase 109 verification results (exit gate)`.
  </action>
  <verify>
    <automated>grep -q "Exit gate PASSED" .planning/phases/109-map-rendering-asset-pipeline/109-VERIFICATION-RESULTS.md && grep -q "MAP-01" .planning/phases/109-map-rendering-asset-pipeline/109-VERIFICATION-RESULTS.md && grep -q "MAP-02" .planning/phases/109-map-rendering-asset-pipeline/109-VERIFICATION-RESULTS.md && grep -q "MAP-03" .planning/phases/109-map-rendering-asset-pipeline/109-VERIFICATION-RESULTS.md && grep -q "e2e-runs.jsonl" .planning/phases/109-map-rendering-asset-pipeline/109-VERIFICATION-RESULTS.md</automated>
  </verify>
  <done>
`109-VERIFICATION-RESULTS.md` exists, contains the four-suite summary
table with exact counts, the two-green-runs timestamps, the requirement
closure table, and the exit-gate statement. Commit landed.
  </done>
</task>

</tasks>

<verification>
- All four suites green at the time of writing the results doc.
- Two consecutive Playwright runs recorded in `web/e2e-runs.jsonl`.
- `109-VERIFICATION-RESULTS.md` marked "Exit gate PASSED".
- Every MAP-* and TEST-* requirement has a closure row pointing at a concrete artifact.
</verification>

<success_criteria>
- [ ] ruff check + format --check clean.
- [ ] pytest reports no unexpected failures (≥ 1118 pass, 0 fail).
- [ ] tsc --noEmit clean.
- [ ] vitest reports no unexpected failures and includes the new 109-02/03/04 tests.
- [ ] Playwright via run-e2e.sh reports green on two consecutive runs including the new 109-05 spec.
- [ ] `109-VERIFICATION-RESULTS.md` captures exact counts, timestamps, and requirement closure.
- [ ] Commit recorded with `docs(109-06): phase 109 verification results (exit gate)`.
</success_criteria>

<output>
After completion, create `.planning/phases/109-map-rendering-asset-pipeline/109-06-SUMMARY.md`
and the exit gate is closed. The orchestrator will pick up STATE.md /
ROADMAP.md updates from here.
</output>
