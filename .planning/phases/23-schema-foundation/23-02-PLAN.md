---
phase: 23-schema-foundation
plan: 02
type: execute
wave: 2
depends_on: ["23-01"]
files_modified:
  - app/services/voter.py
  - app/services/import_service.py
  - app/services/walk_list.py
  - app/services/turf.py
  - tests/unit/test_voter_search.py
  - tests/unit/test_field_mapping.py
  - tests/unit/test_import_service.py
autonomous: true
requirements: [VMOD-08, VMOD-09]

must_haves:
  truths:
    - "build_voter_query references registration_city, registration_state, registration_zip, registration_county instead of old names"
    - "Import service CANONICAL_FIELDS and _VOTER_COLUMNS use registration_ prefix for address fields and include all new column names"
    - "Walk list sorting uses voter.registration_line1 instead of voter.address_line1"
    - "Turf household_key uses voter.registration_line1 and voter.registration_zip instead of old names"
    - "All existing unit tests pass with updated field references"
  artifacts:
    - path: "app/services/voter.py"
      provides: "Updated build_voter_query with registration_ field references"
      contains: "registration_city"
    - path: "app/services/import_service.py"
      provides: "Updated CANONICAL_FIELDS and _VOTER_COLUMNS with new column names"
      contains: "registration_line1"
    - path: "app/services/walk_list.py"
      provides: "Walk list sorting using renamed address field"
      contains: "registration_line1"
    - path: "app/services/turf.py"
      provides: "Household key using renamed address fields"
      contains: "registration_line1"
    - path: "tests/unit/test_voter_search.py"
      provides: "Updated test assertions for renamed filter fields"
      contains: "registration_city"
    - path: "tests/unit/test_field_mapping.py"
      provides: "Updated test assertions for renamed canonical fields"
      contains: "registration_line1"
    - path: "tests/unit/test_import_service.py"
      provides: "Updated test fixtures for renamed canonical fields"
      contains: "registration_city"
  key_links:
    - from: "app/services/voter.py"
      to: "app/models/voter.py"
      via: "build_voter_query references Voter.registration_city etc."
      pattern: "Voter\\.registration_city|Voter\\.registration_state|Voter\\.registration_zip"
    - from: "app/services/import_service.py"
      to: "app/models/voter.py"
      via: "CANONICAL_FIELDS keys and _VOTER_COLUMNS must match Voter model columns"
      pattern: "registration_line1|registration_city|registration_zip"
    - from: "app/services/walk_list.py"
      to: "app/models/voter.py"
      via: "Sort key extraction references voter.registration_line1"
      pattern: "v\\.registration_line1|voter\\.registration_line1"
    - from: "app/services/turf.py"
      to: "app/models/voter.py"
      via: "household_key references voter address fields"
      pattern: "voter\\.registration_line1|voter\\.registration_zip"
---

<objective>
Update all downstream Python services and tests to use the renamed column names from Plan 01. This eliminates all references to the old address field names (address_line1, city, state, zip_code, county) in the backend Python code.

Purpose: Without these changes, the voter query builder, import pipeline, walk list generation, and turf service would reference non-existent model attributes and fail at runtime.
Output: All services and tests compile and pass with the renamed fields.
</objective>

<execution_context>
@/home/kwhatcher/.claude/get-shit-done/workflows/execute-plan.md
@/home/kwhatcher/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/23-schema-foundation/23-CONTEXT.md
@.planning/phases/23-schema-foundation/23-RESEARCH.md
@.planning/phases/23-schema-foundation/23-01-SUMMARY.md

<interfaces>
<!-- After Plan 01, the Voter model has these renamed columns: -->
<!-- registration_line1, registration_line2, registration_city, registration_state, registration_zip, registration_county -->
<!-- registration_zip4, registration_apartment_type -->
<!-- mailing_line1..mailing_type (8 columns) -->
<!-- propensity_general, propensity_primary, propensity_combined -->
<!-- spoken_language, marital_status, military_status, party_change_indicator, cell_phone_confidence -->
<!-- household_party_registration, household_size, family_id -->

<!-- VoterFilter fields (after Plan 01): -->
<!-- registration_city, registration_state, registration_zip, registration_county (renamed from city, state, zip_code, county) -->

From app/services/voter.py (build_voter_query lines to update):
- Line 49-50: filters.city -> Voter.city  =>  filters.registration_city -> Voter.registration_city
- Line 52-53: filters.state -> Voter.state  =>  filters.registration_state -> Voter.registration_state
- Line 55-56: filters.zip_code -> Voter.zip_code  =>  filters.registration_zip -> Voter.registration_zip
- Line 58-59: filters.county -> Voter.county  =>  filters.registration_county -> Voter.registration_county

From app/services/import_service.py (CANONICAL_FIELDS keys to rename):
- "address_line1" -> "registration_line1" (key and aliases list)
- "address_line2" -> "registration_line2"
- "city" -> "registration_city"
- "state" -> "registration_state"
- "zip_code" -> "registration_zip"
- "county" -> "registration_county"
- _VOTER_COLUMNS set: same 6 renames, plus add all new column names

From app/services/walk_list.py:
- Line 86: v.address_line1 -> v.registration_line1

From app/services/turf.py:
- Line 101: voter.address_line1 -> voter.registration_line1
- Line 102: voter.zip_code -> voter.registration_zip
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update downstream services (voter, import, walk_list, turf)</name>
  <files>app/services/voter.py, app/services/import_service.py, app/services/walk_list.py, app/services/turf.py</files>
  <action>
**Update app/services/voter.py (build_voter_query):**
Rename 4 filter field references in the exact match section:
- `filters.city` -> `filters.registration_city` and `Voter.city` -> `Voter.registration_city`
- `filters.state` -> `filters.registration_state` and `Voter.state` -> `Voter.registration_state`
- `filters.zip_code` -> `filters.registration_zip` and `Voter.zip_code` -> `Voter.registration_zip`
- `filters.county` -> `filters.registration_county` and `Voter.county` -> `Voter.registration_county`

No other changes needed in voter.py -- CRUD operations use dynamic `model_dump(exclude_unset=True)` and `setattr()` which work with any field names.

**Update app/services/import_service.py:**

1. Rename 6 CANONICAL_FIELDS keys:
   - "address_line1" -> "registration_line1" (update the key; keep existing aliases, add "registration_line1" to the aliases list)
   - "address_line2" -> "registration_line2" (same pattern)
   - "city" -> "registration_city" (keep existing aliases like "residential_city", "residence_addresses_city", "mail_city"; add "registration_city")
   - "state" -> "registration_state" (keep existing aliases; add "registration_state")
   - "zip_code" -> "registration_zip" (keep existing aliases; add "registration_zip")
   - "county" -> "registration_county" (keep existing aliases; add "registration_county")

2. Add new CANONICAL_FIELDS entries for the new columns (each with reasonable aliases):
   - "registration_zip4": ["registration_zip4", "zip4", "zip_plus4", "residence_addresses_zip4"]
   - "registration_apartment_type": ["registration_apartment_type", "apartment_type", "apt_type", "residence_addresses_apttype"]
   - "mailing_line1": ["mailing_line1", "mail_address", "mailing_address", "mail_address_line1"]
   - "mailing_line2": ["mailing_line2", "mail_address2", "mail_address_line2"]
   - "mailing_city": ["mailing_city", "mail_city"]
   - "mailing_state": ["mailing_state", "mail_state"]
   - "mailing_zip": ["mailing_zip", "mail_zip", "mail_zipcode"]
   - "mailing_zip4": ["mailing_zip4", "mail_zip4"]
   - "mailing_country": ["mailing_country", "mail_country"]
   - "mailing_type": ["mailing_type", "mail_type"]
   - "propensity_general": ["propensity_general", "general_propensity"]
   - "propensity_primary": ["propensity_primary", "primary_propensity"]
   - "propensity_combined": ["propensity_combined", "combined_propensity"]
   - "spoken_language": ["spoken_language", "language", "voters_language"]
   - "marital_status": ["marital_status", "marital"]
   - "military_status": ["military_status", "military"]
   - "party_change_indicator": ["party_change_indicator", "party_change"]
   - "cell_phone_confidence": ["cell_phone_confidence", "cellphone_confidence"]
   - "household_party_registration": ["household_party_registration", "hh_party_registration"]
   - "household_size": ["household_size", "hh_size"]
   - "family_id": ["family_id", "familyid"]

3. Update _VOTER_COLUMNS set:
   - Remove: "address_line1", "address_line2", "city", "state", "zip_code", "county"
   - Add: "registration_line1", "registration_line2", "registration_city", "registration_state", "registration_zip", "registration_county"
   - Add all new column names: "registration_zip4", "registration_apartment_type", "mailing_line1", "mailing_line2", "mailing_city", "mailing_state", "mailing_zip", "mailing_zip4", "mailing_country", "mailing_type", "propensity_general", "propensity_primary", "propensity_combined", "spoken_language", "marital_status", "military_status", "party_change_indicator", "cell_phone_confidence", "household_party_registration", "household_size", "family_id"

**Update app/services/walk_list.py:**
- Line 86: Change `v.address_line1` to `v.registration_line1`

**Update app/services/turf.py:**
- In `household_key` function (line ~101): Change `voter.address_line1` to `voter.registration_line1`
- In `household_key` function (line ~102): Change `voter.zip_code` to `voter.registration_zip`
- In `parse_address_sort_key` docstring: Update reference from "address_line1" to "registration_line1"
  </action>
  <verify>
    <automated>cd /home/kwhatcher/projects/run-api && uv run python -c "
from app.services.voter import build_voter_query
from app.schemas.voter_filter import VoterFilter
from app.services.import_service import CANONICAL_FIELDS, _VOTER_COLUMNS

# Verify voter query builder compiles with new field names
q = build_voter_query(VoterFilter(registration_city='Austin', registration_state='TX', registration_zip='78701'))
print('Query builder OK')

# Verify CANONICAL_FIELDS uses new keys
assert 'registration_line1' in CANONICAL_FIELDS, 'Missing registration_line1 in CANONICAL_FIELDS'
assert 'registration_city' in CANONICAL_FIELDS, 'Missing registration_city in CANONICAL_FIELDS'
assert 'mailing_line1' in CANONICAL_FIELDS, 'Missing mailing_line1 in CANONICAL_FIELDS'
assert 'propensity_general' in CANONICAL_FIELDS, 'Missing propensity_general in CANONICAL_FIELDS'
assert 'address_line1' not in CANONICAL_FIELDS, 'Old address_line1 still in CANONICAL_FIELDS'
assert 'city' not in CANONICAL_FIELDS, 'Old city still in CANONICAL_FIELDS'
print('CANONICAL_FIELDS OK')

# Verify _VOTER_COLUMNS
assert 'registration_line1' in _VOTER_COLUMNS, 'Missing registration_line1 in _VOTER_COLUMNS'
assert 'mailing_line1' in _VOTER_COLUMNS, 'Missing mailing_line1 in _VOTER_COLUMNS'
assert 'address_line1' not in _VOTER_COLUMNS, 'Old address_line1 still in _VOTER_COLUMNS'
print('_VOTER_COLUMNS OK')
"</automated>
  </verify>
  <done>All 4 service files reference new registration_ column names. CANONICAL_FIELDS includes entries for all new columns with aliases. _VOTER_COLUMNS includes all new column names. No references to old address_line1/city/state/zip_code/county remain.</done>
</task>

<task type="auto">
  <name>Task 2: Update existing unit tests for renamed fields</name>
  <files>tests/unit/test_voter_search.py, tests/unit/test_field_mapping.py, tests/unit/test_import_service.py</files>
  <action>
**Update tests/unit/test_voter_search.py:**

1. In `test_or_logic` (line ~96): Change `city="Austin"` to `registration_city="Austin"` in VoterFilter constructor.

2. In `test_exact_match_fields` (line ~100-118):
   - Change VoterFilter constructor: `city="Austin"` -> `registration_city="Austin"`, `state="TX"` -> `registration_state="TX"`, `zip_code="78701"` -> `registration_zip="78701"`, `county="Travis"` -> `registration_county="Travis"`
   - Update assertions: `"city" in sql` -> `"registration_city" in sql`, `"zip_code" in sql` -> `"registration_zip" in sql`

3. In `test_combined_and_filters` (line ~145-151): Change `city="Austin"` to `registration_city="Austin"` in VoterFilter, update assertion from `"city"` to `"registration_city"`.

**Update tests/unit/test_field_mapping.py:**

1. In `test_standard_column_names` (line ~16): Change `assert result["ZIP"] == "zip_code"` to `assert result["ZIP"] == "registration_zip"`.

2. In `test_l2_party_and_address` (line ~37-38): Change `assert result["Residence_Addresses_AddressLine"] == "address_line1"` to `assert result["Residence_Addresses_AddressLine"] == "registration_line1"`.

3. In `test_case_insensitive_matching` (line ~47-50): Change `assert result["City"] == "city"` to `assert result["City"] == "registration_city"`.

4. In `test_address_fields` (line ~101-111): Update all 6 assertions:
   - `"address_line1"` -> `"registration_line1"`
   - `"address_line2"` -> `"registration_line2"`
   - `"city"` -> `"registration_city"`
   - `"state"` -> `"registration_state"`
   - `"zip_code"` -> `"registration_zip"`
   - `"county"` -> `"registration_county"`
   Also update the input column names in the suggest_field_mapping call to match the new canonical aliases (the old names like "address_line1" should still be in the aliases and still map correctly, but verify the expected output is the new canonical name).

**Update tests/unit/test_import_service.py:**

1. In `test_apply_mapping_to_rows` (line ~69-80): Update the basic_mapping fixture if it uses old canonical field names. The fixture maps `"City": "city"` -- change to `"City": "registration_city"`. Update assertion: `voter["city"]` -> `voter["registration_city"]`.

2. In the `basic_mapping` fixture (line ~61-67): Change `"City": "city"` to `"City": "registration_city"`.

After all updates, run the full unit test suite to confirm everything passes.
  </action>
  <verify>
    <automated>cd /home/kwhatcher/projects/run-api && uv run pytest tests/unit/test_voter_search.py tests/unit/test_field_mapping.py tests/unit/test_import_service.py -x -q</automated>
  </verify>
  <done>All 3 test files updated with new field names. `uv run pytest tests/unit/ -x -q` passes with 0 failures. No references to old address_line1/city/state/zip_code/county remain in test assertions for renamed fields.</done>
</task>

</tasks>

<verification>
- `uv run pytest tests/unit/ -x -q` passes (all unit tests green)
- `grep -r "Voter\.city\b\|Voter\.address_line1\b\|Voter\.zip_code\b\|Voter\.state\b\|Voter\.county\b" app/services/` returns no matches (old references eliminated)
- `grep -r '"address_line1"\|"city"\|"zip_code"' app/services/import_service.py` returns no matches as CANONICAL_FIELDS keys (aliases still contain "city" etc. which is correct -- they map incoming CSV columns to the new canonical name)
</verification>

<success_criteria>
1. build_voter_query uses Voter.registration_city, registration_state, registration_zip, registration_county
2. CANONICAL_FIELDS has entries for all 22+ new columns with reasonable aliases
3. _VOTER_COLUMNS contains all new column names and none of the old ones
4. walk_list.py sorts by v.registration_line1
5. turf.py household_key uses voter.registration_line1 and voter.registration_zip
6. All existing unit tests pass after field name updates
</success_criteria>

<output>
After completion, create `.planning/phases/23-schema-foundation/23-02-SUMMARY.md`
</output>
