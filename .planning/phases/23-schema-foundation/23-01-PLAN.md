---
phase: 23-schema-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - alembic/versions/006_expand_voter_model.py
  - app/models/voter.py
  - app/models/voter_contact.py
  - app/schemas/voter.py
  - app/schemas/voter_filter.py
autonomous: true
requirements: [VMOD-01, VMOD-02, VMOD-03, VMOD-04, VMOD-05, VMOD-06, VMOD-07, VMOD-08, VMOD-09, VMOD-10]

must_haves:
  truths:
    - "Voter model has all 22 new columns (propensity, mailing, demographics, household) plus 2 registration additions (zip4, apartment_type)"
    - "6 existing address columns are renamed with registration_ prefix in both model and migration"
    - "VoterPhone model has UniqueConstraint on (campaign_id, voter_id, value)"
    - "VoterResponse, VoterCreateRequest, VoterUpdateRequest schemas include all new and renamed fields"
    - "VoterFilter schema renames city/state/zip_code/county to registration_ prefix"
    - "Alembic migration applies cleanly with all new columns nullable"
  artifacts:
    - path: "alembic/versions/006_expand_voter_model.py"
      provides: "Migration with renames, adds, index changes, VoterPhone constraint, L2 template update"
      contains: "op.alter_column"
    - path: "app/models/voter.py"
      provides: "Expanded Voter model with ~24 new/renamed columns"
      contains: "registration_line1"
    - path: "app/models/voter_contact.py"
      provides: "VoterPhone with UniqueConstraint"
      contains: "uq_voter_phone_campaign_voter_value"
    - path: "app/schemas/voter.py"
      provides: "Updated Pydantic schemas with all new fields"
      contains: "mailing_line1"
    - path: "app/schemas/voter_filter.py"
      provides: "Renamed filter fields for registration address"
      contains: "registration_city"
  key_links:
    - from: "app/models/voter.py"
      to: "alembic/versions/006_expand_voter_model.py"
      via: "Column names must match between model and migration"
      pattern: "registration_line1|mailing_line1|propensity_general"
    - from: "app/schemas/voter.py"
      to: "app/models/voter.py"
      via: "Schema fields match model column names for model_validate()"
      pattern: "registration_line1|mailing_city|propensity_general"
    - from: "app/schemas/voter_filter.py"
      to: "app/models/voter.py"
      via: "Filter fields reference model attributes in build_voter_query"
      pattern: "registration_city|registration_state|registration_zip"
---

<objective>
Create the Alembic migration, update the Voter and VoterPhone ORM models, and update all Pydantic voter schemas to reflect the expanded voter model.

Purpose: Establish the database schema and Python type layer so that downstream services and tests can reference the new column names. This is the foundation for all Phase 23 work.
Output: Migration file, updated models, updated schemas.
</objective>

<execution_context>
@/home/kwhatcher/.claude/get-shit-done/workflows/execute-plan.md
@/home/kwhatcher/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/23-schema-foundation/23-CONTEXT.md
@.planning/phases/23-schema-foundation/23-RESEARCH.md

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From app/models/voter.py (current, will be modified):
- Voter model with __tablename__ = "voters"
- 6 address columns to rename: address_line1, address_line2, city, state, zip_code, county
- Existing indexes in __table_args__: ix_voters_campaign_source (unique), ix_voters_campaign_party, ix_voters_campaign_precinct, ix_voters_campaign_zip, ix_voters_campaign_last_name
- household_id already exists as String(255)

From app/models/voter_contact.py (current, will be modified):
- VoterPhone has __tablename__ = "voter_phones", no __table_args__ currently
- Columns: id, campaign_id, voter_id, value, type, is_primary, source, created_at, updated_at

From app/schemas/voter.py (current, will be modified):
- VoterResponse: address_line1, address_line2, city, state, zip_code, county (6 fields to rename, ~22 to add)
- VoterCreateRequest: same 6 fields to rename, ~22 to add
- VoterUpdateRequest: same 6 fields to rename, ~22 to add
- All schemas inherit from BaseSchema (from app.schemas.common)

From app/schemas/voter_filter.py (current, will be modified):
- VoterFilter: city, state, zip_code, county fields (4 to rename to registration_ prefix)
- Inherits from BaseModel (pydantic)

From alembic/versions/005_volunteer_management.py:
- Revision ID: 005, down_revision: 004
- New migration must use down_revision = "005"

From app/db/base.py:
- Base class for all models; all models must be imported there
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Alembic migration and update ORM models</name>
  <files>alembic/versions/006_expand_voter_model.py, app/models/voter.py, app/models/voter_contact.py</files>
  <action>
Create the Alembic migration file `alembic/versions/006_expand_voter_model.py` with revision ID "006" and down_revision "005":

**upgrade() must do the following in order:**

1. Rename 6 registration address columns using `op.alter_column(..., new_column_name=...)`:
   - address_line1 -> registration_line1
   - address_line2 -> registration_line2
   - city -> registration_city
   - state -> registration_state
   - zip_code -> registration_zip
   - county -> registration_county

2. Drop old index and create renamed index:
   - `op.drop_index("ix_voters_campaign_zip", table_name="voters")`
   - `op.create_index("ix_voters_campaign_reg_zip", "voters", ["campaign_id", "registration_zip"])`

3. Add 2 new registration address columns (both nullable):
   - registration_zip4: String(4)
   - registration_apartment_type: String(20)

4. Add 8 mailing address columns (all nullable):
   - mailing_line1: String(500)
   - mailing_line2: String(500)
   - mailing_city: String(255)
   - mailing_state: String(2)
   - mailing_zip: String(10)
   - mailing_zip4: String(4)
   - mailing_country: String(100)
   - mailing_type: String(20)

5. Add 3 propensity score columns (all nullable):
   - propensity_general: SmallInteger
   - propensity_primary: SmallInteger
   - propensity_combined: SmallInteger

6. Add 4 demographic columns (all nullable):
   - spoken_language: String(100)
   - marital_status: String(50)
   - military_status: String(50)
   - party_change_indicator: String(50)

7. Add 1 phone confidence column (nullable):
   - cell_phone_confidence: SmallInteger

8. Add 3 household columns (all nullable):
   - household_party_registration: String(50)
   - household_size: SmallInteger
   - family_id: String(255)

9. Create indexes for Phase 25 filter queries:
   - ix_voters_campaign_mailing_zip: (campaign_id, mailing_zip)
   - ix_voters_campaign_mailing_city: (campaign_id, mailing_city)
   - ix_voters_campaign_mailing_state: (campaign_id, mailing_state)

10. Add VoterPhone unique constraint:
    - First, deduplicate existing rows: `DELETE FROM voter_phones a USING voter_phones b WHERE a.id > b.id AND a.campaign_id = b.campaign_id AND a.voter_id = b.voter_id AND a.value = b.value`
    - Then: `op.create_unique_constraint("uq_voter_phone_campaign_voter_value", "voter_phones", ["campaign_id", "voter_id", "value"])`

11. Update L2 mapping template for renamed columns:
    - Execute SQL to update field_mapping_templates SET mapping values for the 6 renamed columns (Residence_Addresses_AddressLine -> registration_line1, etc.)

**downgrade() must reverse all operations** in reverse order:
- Drop unique constraint, drop new indexes, drop new columns, recreate old index, rename columns back.

**Update app/models/voter.py:**
- Rename the "Address" section to "Registration Address"
- Change column names: address_line1 -> registration_line1, address_line2 -> registration_line2, city -> registration_city, state -> registration_state, zip_code -> registration_zip, county -> registration_county
- Add registration_zip4: Mapped[str | None] = mapped_column(String(4))
- Add registration_apartment_type: Mapped[str | None] = mapped_column(String(20))
- Add "Mailing Address" section with 8 columns (see research for types)
- Add "Propensity Scores" section with 3 SmallInteger columns
- Add expanded "Demographics" section with spoken_language, marital_status, military_status, party_change_indicator, cell_phone_confidence
- Add expanded "Household" section with household_party_registration, household_size, family_id (household_id already exists, keep it)
- Update __table_args__: change "zip_code" to "registration_zip" in the zip index (rename to ix_voters_campaign_reg_zip), add 3 new mailing indexes
- Import SmallInteger from sqlalchemy

**Update app/models/voter_contact.py:**
- Add __table_args__ to VoterPhone with UniqueConstraint("campaign_id", "voter_id", "value", name="uq_voter_phone_campaign_voter_value")
- Import UniqueConstraint from sqlalchemy
  </action>
  <verify>
    <automated>cd /home/kwhatcher/projects/run-api && uv run python -c "from app.models.voter import Voter; v = Voter; assert hasattr(v, 'registration_line1'); assert hasattr(v, 'mailing_line1'); assert hasattr(v, 'propensity_general'); assert hasattr(v, 'family_id'); assert hasattr(v, 'registration_zip4'); print('Model OK')" && uv run python -c "from app.models.voter_contact import VoterPhone; assert any('uq_voter_phone' in str(c.name) for c in VoterPhone.__table__.constraints if hasattr(c, 'name') and c.name); print('VoterPhone constraint OK')"</automated>
  </verify>
  <done>Voter model has all 24 new/renamed columns in organized sections. VoterPhone has unique constraint. Migration file exists with renames, adds, index changes, dedup safety, L2 template update, and downgrade. All new columns are nullable.</done>
</task>

<task type="auto">
  <name>Task 2: Update Pydantic voter schemas and voter filter</name>
  <files>app/schemas/voter.py, app/schemas/voter_filter.py</files>
  <action>
**Update app/schemas/voter.py:**

In VoterResponse, VoterCreateRequest, and VoterUpdateRequest, apply these changes:

1. Rename 6 address fields:
   - address_line1 -> registration_line1
   - address_line2 -> registration_line2
   - city -> registration_city
   - state -> registration_state
   - zip_code -> registration_zip
   - county -> registration_county

2. Add 2 new registration fields (all `str | None = None`):
   - registration_zip4
   - registration_apartment_type

3. Add 8 mailing address fields (all `str | None = None`):
   - mailing_line1, mailing_line2, mailing_city, mailing_state, mailing_zip, mailing_zip4, mailing_country, mailing_type

4. Add 3 propensity score fields (all `int | None = None`):
   - propensity_general, propensity_primary, propensity_combined

5. Add 4 demographic fields (all type | None = None):
   - spoken_language: str | None = None
   - marital_status: str | None = None
   - military_status: str | None = None
   - party_change_indicator: str | None = None

6. Add 1 phone confidence field:
   - cell_phone_confidence: int | None = None

7. Add 3 household fields:
   - household_party_registration: str | None = None
   - household_size: int | None = None
   - family_id: str | None = None

Organize fields into comment-delimited sections matching the model: Core fields, Registration Address, Mailing Address, Political, Voting history, Propensity Scores, Demographics, Geographic, Household, Extras, Metadata.

**Update app/schemas/voter_filter.py:**

Rename 4 filter fields to match the model rename:
- city -> registration_city
- state -> registration_state
- zip_code -> registration_zip
- county -> registration_county

Do NOT add new filter dimensions (propensity ranges, ethnicity, etc.) -- that is Phase 25 (FILT-* requirements).
  </action>
  <verify>
    <automated>cd /home/kwhatcher/projects/run-api && uv run python -c "
from app.schemas.voter import VoterResponse, VoterCreateRequest, VoterUpdateRequest
from app.schemas.voter_filter import VoterFilter

# Check renamed fields exist on all 3 schemas
for Schema in [VoterResponse, VoterCreateRequest, VoterUpdateRequest]:
    fields = Schema.model_fields
    assert 'registration_line1' in fields, f'{Schema.__name__} missing registration_line1'
    assert 'mailing_line1' in fields, f'{Schema.__name__} missing mailing_line1'
    assert 'propensity_general' in fields, f'{Schema.__name__} missing propensity_general'
    assert 'family_id' in fields, f'{Schema.__name__} missing family_id'
    assert 'cell_phone_confidence' in fields, f'{Schema.__name__} missing cell_phone_confidence'
    assert 'address_line1' not in fields, f'{Schema.__name__} still has old address_line1'

# Check filter renames
ff = VoterFilter.model_fields
assert 'registration_city' in ff, 'VoterFilter missing registration_city'
assert 'registration_zip' in ff, 'VoterFilter missing registration_zip'
assert 'city' not in ff, 'VoterFilter still has old city field'
assert 'zip_code' not in ff, 'VoterFilter still has old zip_code field'

print('All schemas OK')
"</automated>
  </verify>
  <done>All 3 voter schemas include all new and renamed fields. VoterFilter uses registration_ prefix for address filter fields. Old field names (address_line1, city, state, zip_code, county) no longer exist in any schema.</done>
</task>

</tasks>

<verification>
- `uv run python -c "from app.models.voter import Voter"` succeeds (model compiles)
- `uv run python -c "from app.schemas.voter import VoterResponse"` succeeds (schemas compile)
- Migration file exists at `alembic/versions/006_expand_voter_model.py`
- All new columns are nullable (grep for `nullable=True` in migration)
- VoterPhone has unique constraint in model definition
</verification>

<success_criteria>
1. Voter model has registration_line1, mailing_line1, propensity_general, family_id, registration_zip4, registration_apartment_type and all other new columns
2. VoterPhone has UniqueConstraint on (campaign_id, voter_id, value)
3. VoterResponse, VoterCreateRequest, VoterUpdateRequest all include all new fields with correct types
4. VoterFilter uses registration_city, registration_state, registration_zip, registration_county
5. Migration file has upgrade/downgrade, all adds are nullable, includes defensive dedup before unique constraint
6. Old field names (address_line1, city, state, zip_code, county) do not appear in model or schemas
</success_criteria>

<output>
After completion, create `.planning/phases/23-schema-foundation/23-01-SUMMARY.md`
</output>
