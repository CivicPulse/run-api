---
phase: 56-schema-orphan-detection
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - alembic/versions/021_import_recovery_metadata.py
  - app/core/config.py
  - app/models/import_job.py
  - app/schemas/import_job.py
  - app/services/import_service.py
  - app/services/import_recovery.py
autonomous: true
requirements: [ORPH-01, ORPH-02, ORPH-03, ORPH-04]
gap_closure: false
---

# Phase 56 Plan 01

## Objective

Add application-owned import progress metadata, detect stale `PROCESSING` jobs with a configurable threshold, and persist recovery-ready diagnostics.

## Tasks

1. Add import recovery metadata columns and defaults via migration, model, schema, and settings updates.
2. Update the import service to persist `last_progress_at` on durable progress boundaries and record `source_exhausted_at` before final status transition.
3. Add orphan scan helpers that classify stale imports and persist `orphaned_at` plus `orphaned_reason`.

## Verification

- `uv run pytest tests/unit/test_import_recovery.py tests/unit/test_batch_resilience.py`
- Confirm `app/core/config.py` exposes `import_orphan_threshold_minutes`
- Confirm `app/services/import_recovery.py` returns persisted candidates with structured orphan reasons
