---
phase: 12-shared-infrastructure-campaign-foundation
plan: 02
type: execute
wave: 2
depends_on: ["12-01"]
files_modified:
  - web/src/routes/__root.tsx
  - web/src/routes/campaigns/$campaignId/settings.tsx
  - web/src/routes/campaigns/$campaignId/settings/index.tsx
  - web/src/routes/campaigns/$campaignId/settings/general.tsx
  - web/src/routes/campaigns/$campaignId/settings/danger.tsx
  - web/src/hooks/useCampaigns.ts
  - web/src/components/shared/DestructiveConfirmDialog.tsx
autonomous: true
requirements:
  - CAMP-01
  - CAMP-02
  - CAMP-08

must_haves:
  truths:
    - "Campaign owner/admin sees gear icon at bottom of sidebar; lower roles do not"
    - "Clicking gear icon navigates to settings page with vertical sidebar layout"
    - "Settings page does NOT show the campaign tab navigation bar"
    - "User can edit campaign name, description, and election date with form validation"
    - "Saving campaign edits shows success toast; unsaved changes trigger navigation warning"
    - "Campaign owner can delete campaign after typing campaign name to confirm"
    - "Campaign owner can transfer ownership to another admin member"
    - "Delete and transfer ownership are in a visually distinct Danger Zone section"
  artifacts:
    - path: "web/src/routes/campaigns/$campaignId/settings.tsx"
      provides: "Settings layout with vertical sidebar"
      min_lines: 30
    - path: "web/src/routes/campaigns/$campaignId/settings/general.tsx"
      provides: "Campaign edit form"
      min_lines: 50
    - path: "web/src/routes/campaigns/$campaignId/settings/danger.tsx"
      provides: "Delete campaign + transfer ownership"
      min_lines: 60
    - path: "web/src/components/shared/DestructiveConfirmDialog.tsx"
      provides: "Type-to-confirm destructive action dialog"
      exports: ["DestructiveConfirmDialog"]
    - path: "web/src/hooks/useCampaigns.ts"
      provides: "Update and delete campaign mutations added"
      contains: "useUpdateCampaign"
  key_links:
    - from: "web/src/routes/__root.tsx"
      to: "web/src/routes/campaigns/$campaignId/settings.tsx"
      via: "Settings gear icon Link in sidebar footer"
      pattern: "settings"
    - from: "web/src/routes/__root.tsx"
      to: "web/src/components/shared/RequireRole.tsx"
      via: "RequireRole minimum=admin wrapping settings link"
      pattern: "RequireRole"
    - from: "web/src/routes/campaigns/$campaignId/settings/general.tsx"
      to: "web/src/hooks/useCampaigns.ts"
      via: "useUpdateCampaign mutation"
      pattern: "useUpdateCampaign"
    - from: "web/src/routes/campaigns/$campaignId/settings/general.tsx"
      to: "web/src/hooks/useFormGuard.ts"
      via: "useFormGuard for unsaved changes protection"
      pattern: "useFormGuard"
    - from: "web/src/routes/campaigns/$campaignId/settings/danger.tsx"
      to: "web/src/hooks/useCampaigns.ts"
      via: "useDeleteCampaign and useTransferOwnership mutations"
      pattern: "useDeleteCampaign|useTransferOwnership"
---

<objective>
Build the campaign settings area: route structure with vertical sidebar layout, the General tab for editing campaign details, and the Danger Zone tab for campaign deletion and ownership transfer. Wire the settings gear icon into the sidebar with admin+ permission gating.

Purpose: Campaign owners and admins need to manage campaign details and perform destructive actions from a dedicated settings area.
Output: Settings layout route, General tab, Danger Zone tab, DestructiveConfirmDialog component, updated sidebar with gear icon, updated useCampaigns hook with update/delete/transfer mutations.
</objective>

<execution_context>
@/home/kwhatcher/.claude/get-shit-done/workflows/execute-plan.md
@/home/kwhatcher/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-shared-infrastructure-campaign-foundation/12-RESEARCH.md
@.planning/phases/12-shared-infrastructure-campaign-foundation/12-01-SUMMARY.md

<interfaces>
<!-- From Plan 01 outputs -->

From web/src/hooks/usePermissions.ts (created in Plan 01):
```typescript
export type CampaignRole = "viewer" | "volunteer" | "manager" | "admin" | "owner"
export function usePermissions(): { role: CampaignRole; hasRole: (minimum: CampaignRole) => boolean }
```

From web/src/components/shared/RequireRole.tsx (created in Plan 01):
```typescript
export function RequireRole({ minimum, children, fallback }: {
  minimum: CampaignRole
  children: React.ReactNode
  fallback?: React.ReactNode
}): JSX.Element
```

From web/src/hooks/useFormGuard.ts (created in Plan 01):
```typescript
export function useFormGuard({ form }: { form: UseFormReturn<any> }): {
  isDirty: boolean
  isBlocked: boolean
  proceed: () => void
  reset: () => void
}
```

From web/src/hooks/useCampaigns.ts (existing, to be extended):
```typescript
export function useCampaigns()
export function useCampaign(campaignId: string)
export function useCreateCampaign()
// WILL ADD: useUpdateCampaign, useDeleteCampaign, useTransferOwnership
```

From web/src/types/campaign.ts:
```typescript
export interface Campaign {
  id: string; name: string; type: CampaignType; election_date: string | null;
  candidate_name: string | null; party_affiliation: string | null;
  // ...other fields
}
export interface CampaignMember {
  user_id: string; display_name: string; email: string; role: string; synced_at: string;
}
```

From web/src/components/shared/ConfirmDialog.tsx:
```typescript
export function ConfirmDialog({
  open, onOpenChange, title, description, confirmLabel, cancelLabel,
  variant, onConfirm, loading, isPending
}: ConfirmDialogProps)
```

Backend endpoints:
- PATCH /api/v1/campaigns/{id} — requires admin+ — body: { name?, description?, election_date? }
- DELETE /api/v1/campaigns/{id} — requires owner
- POST /api/v1/campaigns/{id}/transfer-ownership — requires owner — body: { new_owner_id: string }
- GET /api/v1/campaigns/{id}/members — requires viewer+ — returns PaginatedResponse<CampaignMember>

Sidebar structure in __root.tsx:
- AppSidebar renders SidebarHeader, SidebarContent (with nav groups), SidebarRail
- Settings gear icon goes in SidebarFooter BEFORE SidebarRail
- Wrapped in RequireRole minimum="admin"
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Settings route structure, sidebar gear icon, and General tab</name>
  <files>web/src/routes/__root.tsx, web/src/routes/campaigns/$campaignId/settings.tsx, web/src/routes/campaigns/$campaignId/settings/index.tsx, web/src/routes/campaigns/$campaignId/settings/general.tsx, web/src/hooks/useCampaigns.ts</files>
  <action>
    1. **Add settings gear icon to sidebar** in `web/src/routes/__root.tsx`:
       - Import `Settings` from `lucide-react`, `RequireRole` from `@/components/shared/RequireRole`, and `SidebarFooter` from `@/components/ui/sidebar`.
       - In the `AppSidebar` component, add a `SidebarFooter` section AFTER `SidebarContent` and BEFORE `SidebarRail`.
       - Wrap in `<RequireRole minimum="admin">` so only admin+ roles see it.
       - Inside: `SidebarMenu > SidebarMenuItem > SidebarMenuButton` with `asChild` prop, containing a `Link` to `/campaigns/${campaignId}/settings`.
       - Icon: `<Settings />`. Label: "Settings".
       - Set `isActive` when `location.pathname.includes("/settings")`.

    2. **Create settings layout route** at `web/src/routes/campaigns/$campaignId/settings.tsx`:
       - This is the layout route for all settings pages. It does NOT inherit the campaign tab layout.
       - Use `createFileRoute("/campaigns/$campaignId/settings")`.
       - Render a two-column layout: left sidebar (nav links) + right content (`<Outlet />`).
       - Left sidebar contains vertical nav links styled like GitHub settings: "General", "Members", "Danger Zone".
       - Use shadcn `Button` with `variant="ghost"` for nav items, highlight active item.
       - Layout: `flex` container, left sidebar `w-48 shrink-0` with `border-r`, right content area `flex-1 p-6`.
       - Import `Link`, `Outlet`, `useRouterState` from `@tanstack/react-router`.
       - Nav items link to: `/campaigns/$campaignId/settings` (General), `/campaigns/$campaignId/settings/members` (Members), `/campaigns/$campaignId/settings/danger` (Danger Zone).
       - Show page title "Campaign Settings" above the nav.

    3. **Create settings index redirect** at `web/src/routes/campaigns/$campaignId/settings/index.tsx`:
       - This route is the default when navigating to `/campaigns/$campaignId/settings`.
       - Use `createFileRoute` and redirect to the general tab, OR render the general tab content directly.
       - Simplest approach: this file IS the general tab (no separate general.tsx needed -- the index IS general). Wait -- RESEARCH.md shows `general.tsx` as a separate file. To keep the settings layout clean, make `index.tsx` render a `<Navigate>` to `./general` or simply render the general content inline.
       - Actually, TanStack Router file-based routing: `settings/index.tsx` is the default child route of `settings.tsx` layout. The simplest pattern is to have `settings/index.tsx` redirect to `settings/general`.
       - Use: `export const Route = createFileRoute(...)({ beforeLoad: () => { throw redirect({ to: '/campaigns/$campaignId/settings/general' }) } })` or use `Navigate` component.

    4. **Create General settings tab** at `web/src/routes/campaigns/$campaignId/settings/general.tsx`:
       - Use `createFileRoute("/campaigns/$campaignId/settings/general")`.
       - Load campaign data via `useCampaign(campaignId)` from existing hook.
       - Build a form with react-hook-form + zod resolver (mode: "onBlur" per user decision):
         - Fields: `name` (required, 3-100 chars), `description` (optional textarea), `election_date` (optional date input).
         - Zod schema using v4 syntax: `z.object({ name: z.string().min(3).max(100), description: z.string().optional(), election_date: z.string().optional() })`.
       - Wire `useFormGuard({ form })` for unsaved changes protection.
       - Render `ConfirmDialog` when `isBlocked` is true (from useFormGuard) with "You have unsaved changes" / "Discard changes" / "Keep editing" per user decision.
       - Call `form.reset({ name, description, election_date })` after campaign data loads to establish clean baseline (prevents Pitfall 3).
       - Submit button with loading spinner (`isPending` from mutation). On success: `toast.success("Campaign updated")` via sonner. On error: `toast.error("Failed to update campaign")`.
       - Use `useUpdateCampaign` mutation (added in step 5).

    5. **Extend useCampaigns.ts** with three new mutations:
       - `useUpdateCampaign(campaignId: string)`: `useMutation` calling `api.patch(\`api/v1/campaigns/${campaignId}\`, { json: data })`. On success: invalidate `["campaigns", campaignId]` and `["campaigns"]`.
       - `useDeleteCampaign(campaignId: string)`: `useMutation` calling `api.delete(\`api/v1/campaigns/${campaignId}\`)`. On success: invalidate `["campaigns"]` and navigate to `/`.
       - `useTransferOwnership(campaignId: string)`: `useMutation` calling `api.post(\`api/v1/campaigns/${campaignId}/transfer-ownership\`, { json: { new_owner_id } })`. On success: invalidate `["campaigns", campaignId]` and `["campaigns", campaignId, "members"]`.

    Note on route structure: Per CONTEXT.md and RESEARCH.md, settings is a SIBLING route to the campaign tab layout, NOT a child. The `settings.tsx` file creates its own layout without inheriting the tab navigation. Verify that navigating to settings does NOT show campaign tabs.
  </action>
  <verify>
    <automated>cd /home/kwhatcher/projects/run-api/web && npx tsc --noEmit && npx vitest run --reporter=verbose</automated>
  </verify>
  <done>Settings route renders with vertical sidebar (General/Members/Danger Zone). Gear icon appears in sidebar for admin+ roles only. General tab loads campaign data into form with validation, form guard, and PATCH mutation. Campaign tabs do NOT appear on settings pages.</done>
</task>

<task type="auto">
  <name>Task 2: Danger Zone tab -- delete campaign (type-to-confirm) and transfer ownership</name>
  <files>web/src/components/shared/DestructiveConfirmDialog.tsx, web/src/routes/campaigns/$campaignId/settings/danger.tsx</files>
  <action>
    1. **Create DestructiveConfirmDialog** at `web/src/components/shared/DestructiveConfirmDialog.tsx`:
       - Extends the confirmation pattern for destructive actions requiring the user to type a confirmation string.
       - Props: `open`, `onOpenChange`, `title`, `description`, `confirmText` (the string user must type, e.g. campaign name), `confirmLabel` (button text, default "Delete"), `cancelLabel` (default "Cancel"), `onConfirm`, `isPending`.
       - Uses `AlertDialog`, `AlertDialogContent`, `AlertDialogHeader`, `AlertDialogTitle`, `AlertDialogDescription`, `AlertDialogFooter`, `AlertDialogCancel`, `AlertDialogAction` from shadcn.
       - Input field where user types the confirmation text. Show "Type **{confirmText}** to confirm" in description.
       - Confirm button disabled until input matches `confirmText` exactly OR while `isPending`.
       - Confirm button styled with destructive variant: `className="bg-destructive text-destructive-foreground hover:bg-destructive/90"`.
       - Reset input value when dialog opens (use `useEffect` on `open` prop or `onOpenChange`).
       - Per user decision: like GitHub/Vercel destructive action pattern.

    2. **Create Danger Zone tab** at `web/src/routes/campaigns/$campaignId/settings/danger.tsx`:
       - Use `createFileRoute("/campaigns/$campaignId/settings/danger")`.
       - Wrap entire content in `<RequireRole minimum="owner">` with a fallback message: "Only the campaign owner can access these settings."
       - Two sections, each in a Card with red/destructive border styling:

       **Section 1: Delete Campaign**
       - Heading: "Delete this campaign"
       - Description: "Once you delete a campaign, there is no going back. Please be certain."
       - "Delete campaign" button (destructive variant) opens `DestructiveConfirmDialog`.
       - `confirmText` = campaign name from `useCampaign(campaignId)`.
       - `onConfirm` calls `useDeleteCampaign` mutation. On success: toast "Campaign deleted" and navigate to `/`.

       **Section 2: Transfer Ownership**
       - Heading: "Transfer ownership"
       - Description: "Transfer this campaign to another admin. You will become an admin after transfer."
       - "Transfer ownership" button opens a dialog with a Select dropdown listing admin members from `GET /api/v1/campaigns/{id}/members` (filter to role === "admin").
       - Use `useMembers` hook if available from Plan 03, otherwise create a simple inline query: `useQuery({ queryKey: ["campaigns", campaignId, "members"], queryFn: ... })`.
       - After selecting a target admin, confirm dialog: "Transfer ownership to {name}? You will be demoted to admin."
       - `onConfirm` calls `useTransferOwnership` mutation. On success: toast "Ownership transferred" and invalidate queries.

    Note: The Danger Zone tab should feel visually distinct -- red/warning borders on the cards, clear destructive styling. This matches the GitHub repository settings Danger Zone pattern per user decision.
  </action>
  <verify>
    <automated>cd /home/kwhatcher/projects/run-api/web && npx tsc --noEmit && npx vitest run --reporter=verbose</automated>
  </verify>
  <done>Danger Zone tab renders delete campaign (with type-to-confirm) and transfer ownership sections. DestructiveConfirmDialog is reusable. Only campaign owners see the danger zone content. Delete requires typing campaign name. Transfer shows admin member selector.</done>
</task>

</tasks>

<verification>
TypeScript compiles cleanly:
```bash
cd /home/kwhatcher/projects/run-api/web && npx tsc --noEmit
```

All existing and new tests pass:
```bash
cd /home/kwhatcher/projects/run-api/web && npx vitest run --reporter=verbose
```

Settings route structure correct (no campaign tabs visible):
```bash
# Verify route files exist at correct paths
ls web/src/routes/campaigns/\$campaignId/settings.tsx web/src/routes/campaigns/\$campaignId/settings/general.tsx web/src/routes/campaigns/\$campaignId/settings/danger.tsx
```
</verification>

<success_criteria>
- Sidebar shows gear icon for admin+ roles, hidden for lower roles
- Settings page renders with vertical sidebar layout (not inheriting campaign tabs)
- General tab: campaign edit form with validation, form guard, update mutation, toast feedback
- Danger Zone: delete with type-to-confirm, transfer ownership with admin selector
- DestructiveConfirmDialog is a reusable shared component
- useCampaigns extended with update/delete/transfer mutations
- TypeScript compilation clean, all tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/12-shared-infrastructure-campaign-foundation/12-02-SUMMARY.md`
</output>
