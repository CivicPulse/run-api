---
phase: 26-frontend-updates
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - web/src/types/voter.ts
  - web/src/routes/campaigns/$campaignId/voters/index.tsx
  - web/src/routes/campaigns/$campaignId/voters/lists/$listId.tsx
  - web/src/hooks/useVoters.ts
  - app/api/v1/voters.py
  - app/services/voter.py
autonomous: true
requirements: [FRNT-05]

must_haves:
  truths:
    - "TypeScript Voter interface has all fields matching backend VoterResponse (registration_line1, mailing_*, propensity_*, spoken_language, marital_status, military_status, household_*, etc.)"
    - "TypeScript VoterFilter interface has all fields matching backend VoterFilter (registration_city, propensity ranges, ethnicities/spoken_languages/military_statuses arrays, mailing address fields)"
    - "VoterCreate and VoterUpdate types use registration_* field names (not address_line1/city/state/zip_code/county)"
    - "All downstream files compile without errors after the field rename"
    - "Backend distinct-values endpoint returns field value counts for whitelisted fields"
    - "shadcn accordion, slider, and collapsible components are installed"
  artifacts:
    - path: "web/src/types/voter.ts"
      provides: "Updated Voter, VoterFilter, VoterCreate, VoterUpdate interfaces"
      contains: "registration_line1"
    - path: "app/api/v1/voters.py"
      provides: "distinct-values endpoint"
      contains: "distinct-values"
    - path: "app/services/voter.py"
      provides: "distinct_values method"
      contains: "distinct_values"
    - path: "web/src/hooks/useVoters.ts"
      provides: "useDistinctValues hook"
      contains: "useDistinctValues"
  key_links:
    - from: "web/src/types/voter.ts"
      to: "app/schemas/voter.py"
      via: "field name alignment"
      pattern: "registration_line1.*string.*null"
    - from: "web/src/hooks/useVoters.ts"
      to: "app/api/v1/voters.py"
      via: "distinct-values API call"
      pattern: "distinct-values"
---

<objective>
Update all TypeScript voter types to match the backend schemas expanded in Phases 23-25, propagate the field rename (address_line1->registration_line1, city->registration_city, etc.) across all consuming files, install shadcn UI components needed for later plans, and add the backend distinct-values endpoint for dynamic filter options.

Purpose: This is the foundational plan -- every subsequent plan depends on correct types and renamed fields compiling cleanly. The distinct-values endpoint and shadcn components are co-located here to avoid a separate micro-plan.
Output: Updated voter.ts types, all downstream files compiling, distinct-values API endpoint working, shadcn accordion/slider/collapsible installed.
</objective>

<execution_context>
@/home/kwhatcher/.claude/get-shit-done/workflows/execute-plan.md
@/home/kwhatcher/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/26-frontend-updates/26-CONTEXT.md
@.planning/phases/26-frontend-updates/26-RESEARCH.md

@web/src/types/voter.ts
@web/src/routes/campaigns/$campaignId/voters/index.tsx
@web/src/routes/campaigns/$campaignId/voters/lists/$listId.tsx
@web/src/hooks/useVoters.ts
@app/api/v1/voters.py
@app/services/voter.py
@app/schemas/voter.py
@app/schemas/voter_filter.py

<interfaces>
<!-- Backend schemas the frontend types MUST match exactly -->

From app/schemas/voter.py (VoterResponse):
- registration_line1, registration_line2, registration_city, registration_state, registration_zip, registration_zip4, registration_county, registration_apartment_type (all str | None)
- mailing_line1, mailing_line2, mailing_city, mailing_state, mailing_zip, mailing_zip4, mailing_country, mailing_type (all str | None)
- propensity_general, propensity_primary, propensity_combined (all int | None)
- spoken_language, marital_status, military_status, party_change_indicator (all str | None)
- cell_phone_confidence (int | None)
- household_party_registration (str | None), household_size (int | None), family_id (str | None)

From app/schemas/voter_filter.py (VoterFilter):
- registration_city, registration_state, registration_zip, registration_county (all str | None)
- propensity_general_min/max, propensity_primary_min/max, propensity_combined_min/max (all int | None, ge=0, le=100)
- ethnicities, spoken_languages, military_statuses (all list[str] | None)
- mailing_city, mailing_state, mailing_zip (all str | None)

From app/schemas/voter.py (VoterCreateRequest):
- Uses registration_* names (NOT address_line1/city/state/zip_code/county)
- Includes all mailing, propensity, demographics, household fields

Field rename mapping (old frontend -> new backend):
- address_line1 -> registration_line1
- address_line2 -> registration_line2
- city -> registration_city
- state -> registration_state
- zip_code -> registration_zip
- county -> registration_county
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install shadcn components and update TypeScript types</name>
  <files>web/src/types/voter.ts, web/src/components/ui/accordion.tsx, web/src/components/ui/slider.tsx, web/src/components/ui/collapsible.tsx</files>
  <action>
1. Install shadcn UI components needed by later plans:
   ```bash
   cd web && npx shadcn@latest add accordion slider collapsible --yes
   ```

2. Rewrite `web/src/types/voter.ts` to match backend schemas exactly:

   **Voter interface** -- replace the 6 old address fields (address_line1, address_line2, city, state, zip_code, county) with the 8 registration_* fields. Add all new fields:
   - Registration: registration_line1, registration_line2, registration_city, registration_state, registration_zip, registration_zip4, registration_county, registration_apartment_type (all `string | null`)
   - Mailing: mailing_line1 through mailing_type (8 fields, all `string | null`)
   - Propensity: propensity_general, propensity_primary, propensity_combined (all `number | null`)
   - Demographics: spoken_language, marital_status, military_status, party_change_indicator (all `string | null`), cell_phone_confidence (`number | null`)
   - Household: household_party_registration (`string | null`), household_size (`number | null`), family_id (`string | null`)

   **VoterFilter interface** -- rename city->registration_city, state->registration_state, zip_code->registration_zip, county->registration_county. Add:
   - 6 propensity range fields (propensity_general_min/max, etc., all `number?`)
   - 3 multi-select arrays (ethnicities, spoken_languages, military_statuses, all `string[]?`)
   - 3 mailing address fields (mailing_city, mailing_state, mailing_zip, all `string?`)

   **VoterCreate interface** -- rename address_line1->registration_line1, address_line2->registration_line2, city->registration_city, state->registration_state, zip_code->registration_zip, county->registration_county. Add all new editable fields: middle_name, registration_zip4, registration_apartment_type, mailing_* (8 fields), spoken_language, marital_status, military_status, ethnicity (already exists).

   **VoterUpdate** -- keep as `Partial<VoterCreate>`.

   Use the complete interfaces from RESEARCH.md as the source of truth.
  </action>
  <verify>
    <automated>cd /home/kwhatcher/projects/run-api/web && npx tsc --noEmit 2>&1 | head -50</automated>
  </verify>
  <done>TypeScript types match backend schemas. tsc will show errors in downstream files that still reference old field names -- that is expected and handled in Task 2.</done>
</task>

<task type="auto">
  <name>Task 2: Propagate field renames, add distinct-values backend endpoint and useDistinctValues hook</name>
  <files>web/src/routes/campaigns/$campaignId/voters/index.tsx, web/src/routes/campaigns/$campaignId/voters/lists/$listId.tsx, web/src/hooks/useVoters.ts, app/api/v1/voters.py, app/services/voter.py</files>
  <action>
1. **Fix all TypeScript compiler errors** from the field rename. Use `cd web && npx tsc --noEmit` to find every error, then fix each:

   **voters/index.tsx:**
   - `voterSchema`: rename address_line1->registration_line1, city->registration_city, state->registration_state, zip_code->registration_zip
   - `VoterCreateForm`: update all form field registrations and Labels to use new field names (e.g., `register("registration_line1")`, Label "Address Line 1")
   - `buildColumns`: column `city` cell accessor: `row.original.city` -> `row.original.registration_city`
   - `buildFilterChips`: rename `filters.city` -> `filters.registration_city`, `filters.zip_code` -> `filters.registration_zip`, `filters.state` -> `filters.registration_state`. Update chip labels: "City" still works, "Zip" still works.
   - `filterUpdate` calls: update field names in chip onDismiss callbacks

   **voters/lists/$listId.tsx:**
   - `memberColumns` city accessor: `row.original.city` -> `row.original.registration_city`

   **voters/$voterId.tsx (voter detail page):**
   - Update `voter.address_line1` -> `voter.registration_line1`, `voter.address_line2` -> `voter.registration_line2`
   - Update `voter.city` -> `voter.registration_city`, `voter.state` -> `voter.registration_state`
   - Update `voter.zip_code` -> `voter.registration_zip`, `voter.county` -> `voter.registration_county`
   - NOTE: Do NOT restructure the detail page layout yet -- that is Plan 02's job. Only fix field name references so tsc passes.

2. **Add useDistinctValues hook** to `web/src/hooks/useVoters.ts`:
   ```typescript
   interface DistinctValueEntry { value: string; count: number }
   type DistinctValuesResponse = Record<string, DistinctValueEntry[]>

   export function useDistinctValues(campaignId: string, fields: string[]) {
     return useQuery({
       queryKey: ["voters", campaignId, "distinct-values", fields],
       queryFn: () =>
         api.get(`api/v1/campaigns/${campaignId}/voters/distinct-values`, {
           searchParams: { fields: fields.join(",") },
         }).json<DistinctValuesResponse>(),
       enabled: !!campaignId && fields.length > 0,
       staleTime: 5 * 60 * 1000,
     })
   }
   ```

3. **Add backend distinct-values endpoint** to `app/api/v1/voters.py`:
   - New GET endpoint at `/campaigns/{campaign_id}/voters/distinct-values`
   - Accepts `fields` query param (comma-separated string)
   - Whitelist: `{"ethnicity", "spoken_language", "military_status"}`
   - Returns 400 HTTPException for fields not in whitelist
   - Calls `_service.distinct_values(db, requested_fields)`

4. **Add distinct_values method** to `app/services/voter.py`:
   - For each requested field, query `SELECT {column}, COUNT(*) FROM voters WHERE {column} IS NOT NULL GROUP BY {column} ORDER BY COUNT(*) DESC`
   - Use `getattr(Voter, field)` to get column reference
   - Return dict of field -> list of `{"value": str, "count": int}`

5. **Fix backend GET /voters endpoint** field mapping in `app/api/v1/voters.py`:
   - The `list_voters` function constructs `VoterFilter(city=city, state=state, county=county)` but these are no longer valid VoterFilter fields
   - Update query params and VoterFilter construction: `city` param -> `registration_city` field, `state` -> `registration_state`, `county` -> `registration_county`
   - Keep the query param names as `city`/`state`/`county` for backward compat with any external callers, but map them to the correct VoterFilter fields: `VoterFilter(registration_city=city, registration_state=state, registration_county=county, ...)`

6. Verify everything compiles and tests pass.
  </action>
  <verify>
    <automated>cd /home/kwhatcher/projects/run-api/web && npx tsc --noEmit && npx vitest run --reporter=verbose 2>&1 | tail -30</automated>
  </verify>
  <done>Zero TypeScript errors. All existing unit tests pass. useDistinctValues hook exported. Backend distinct-values endpoint returns correct response for whitelisted fields and 400 for others. GET /voters endpoint maps city/state/county params to registration_city/registration_state/registration_county.</done>
</task>

</tasks>

<verification>
- `cd web && npx tsc --noEmit` exits 0 (zero type errors)
- `cd web && npx vitest run --reporter=verbose` passes all existing tests
- `curl` or test against GET /api/v1/campaigns/{id}/voters/distinct-values?fields=ethnicity returns JSON with value/count pairs
- `curl` against GET /api/v1/campaigns/{id}/voters/distinct-values?fields=invalid returns 400
- shadcn components exist: web/src/components/ui/accordion.tsx, slider.tsx, collapsible.tsx
</verification>

<success_criteria>
- Voter, VoterFilter, VoterCreate, VoterUpdate TypeScript interfaces match backend schemas exactly
- All 6 renamed fields (address_line1->registration_line1, city->registration_city, state->registration_state, zip_code->registration_zip, county->registration_county, address_line2->registration_line2) propagated to every consuming file
- tsc --noEmit passes with zero errors
- All existing vitest tests pass
- useDistinctValues hook available for Plan 03
- Backend distinct-values endpoint functional
- Accordion, Slider, Collapsible shadcn components installed
</success_criteria>

<output>
After completion, create `.planning/phases/26-frontend-updates/26-01-SUMMARY.md`
</output>
