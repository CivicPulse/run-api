# 81-01 Plan

## Goal

Remove the known Phase 14 accessible-name failures across voter, volunteer, survey, canvassing, and campaign-creation surfaces without regressing existing UI behavior.

## Tasks

1. Reproduce the documented `button-name` and `link-name` failures on the affected Select, icon-button, and stretched-link card patterns in the current `web/` code.
2. Update the impacted components and routes so every interactive control has an explicit accessible name while preserving the current visuals and click targets.
3. Add or extend focused UI tests covering the repaired surfaces so the same accessibility regressions fail fast in the repo.

## Verification

- `npm test -- --run web/src/routes/campaigns/new.test.tsx web/src/components/field/AssignmentCard.test.tsx web/src/routes/callback.test.tsx`

