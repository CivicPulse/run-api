# 81-03 Plan

## Goal

Reduce field-hub mobile cold-load cost enough to meet the launch budget, or produce evidence that the remaining budget gap is a documented product decision rather than an unexamined regression.

## Tasks

1. Inspect the field entry bundle and render path for avoidable work on initial load, especially optional tour/help flows and non-critical field-shell behavior.
2. Defer or split non-essential field-mode code from the critical path while preserving the existing field experience after hydration.
3. Re-run the available field-focused tests and note any remaining evidence needed for a follow-up perf validation run.

## Verification

- `npm test -- --run web/src/components/field/AssignmentCard.test.tsx web/src/components/field/FieldHeader.test.tsx web/src/routes/callback.test.tsx`
