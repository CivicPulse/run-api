---
phase: 05-volunteer-management
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/models/volunteer.py
  - app/models/shift.py
  - app/models/__init__.py
  - app/db/base.py
  - app/schemas/volunteer.py
  - app/schemas/shift.py
  - alembic/versions/005_volunteer_management.py
  - tests/unit/test_volunteers.py
  - tests/unit/test_shifts.py
  - tests/integration/test_volunteer_rls.py
autonomous: true
requirements:
  - VOL-01
  - VOL-03

must_haves:
  truths:
    - "Volunteer model stores profile, contact, emergency contact, skills, and status"
    - "Shift model stores type, location, capacity, status, and optional operational links"
    - "ShiftVolunteer join table stores signup status, check-in/out, waitlist position, and manager adjustments"
    - "All new tables have RLS policies isolating by campaign_id"
    - "Test stub files exist for all phase 5 test coverage"
  artifacts:
    - path: "app/models/volunteer.py"
      provides: "Volunteer, VolunteerStatus, VolunteerSkill, VolunteerTag, VolunteerTagMember, VolunteerAvailability models"
      contains: "class Volunteer"
    - path: "app/models/shift.py"
      provides: "Shift, ShiftType, ShiftStatus, ShiftVolunteer, SignupStatus models"
      contains: "class Shift"
    - path: "app/schemas/volunteer.py"
      provides: "Pydantic schemas for volunteer CRUD, skills, availability"
      contains: "class VolunteerCreate"
    - path: "app/schemas/shift.py"
      provides: "Pydantic schemas for shift CRUD, signup, check-in, hours"
      contains: "class ShiftCreate"
    - path: "alembic/versions/005_volunteer_management.py"
      provides: "Migration with all tables and RLS policies"
      contains: "CREATE POLICY"
  key_links:
    - from: "app/models/volunteer.py"
      to: "app/db/base.py"
      via: "Base import for Alembic detection"
      pattern: "import app.models.volunteer"
    - from: "app/models/shift.py"
      to: "app/db/base.py"
      via: "Base import for Alembic detection"
      pattern: "import app.models.shift"
---

<objective>
Create all SQLAlchemy models, Pydantic schemas, Alembic migration with RLS policies, and test stub files for Phase 5 volunteer management.

Purpose: Establish the data layer and contracts that service/API layers will build against in Plan 02.
Output: 6 new database tables (volunteers, volunteer_tags, volunteer_tag_members, volunteer_availability, shifts, shift_volunteers) with full RLS isolation, plus all request/response schemas and skip-marked test stubs.
</objective>

<execution_context>
@/home/kwhatcher/.claude/get-shit-done/workflows/execute-plan.md
@/home/kwhatcher/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-volunteer-management/05-CONTEXT.md
@.planning/phases/05-volunteer-management/05-RESEARCH.md

<interfaces>
<!-- Existing models and patterns the executor needs -->

From app/db/base.py:
```python
class Base(DeclarativeBase):
    pass
# All model modules imported at bottom for Alembic detection
```

From app/schemas/common.py:
```python
class BaseSchema(BaseModel):
    model_config = ConfigDict(from_attributes=True)

class PaginationResponse(BaseModel):
    next_cursor: str | None = None
    has_more: bool = False

class PaginatedResponse[T](BaseModel):
    items: list[T]
    pagination: PaginationResponse
```

From app/models/walk_list.py:
```python
class WalkListCanvasser(Base):
    __tablename__ = "walk_list_canvassers"
    walk_list_id: Mapped[uuid.UUID] = mapped_column(ForeignKey("walk_lists.id"), primary_key=True)
    user_id: Mapped[str] = mapped_column(ForeignKey("users.id"), primary_key=True)
    assigned_at: Mapped[datetime] = mapped_column(server_default=func.now())
```

From app/models/phone_bank.py:
```python
class SessionCaller(Base):
    __tablename__ = "session_callers"
    id: Mapped[uuid.UUID] = mapped_column(primary_key=True, default=uuid.uuid4)
    session_id: Mapped[uuid.UUID] = mapped_column(ForeignKey("phone_bank_sessions.id"), nullable=False)
    user_id: Mapped[str] = mapped_column(ForeignKey("users.id"), nullable=False)
    check_in_at: Mapped[datetime | None] = mapped_column(nullable=True)
    check_out_at: Mapped[datetime | None] = mapped_column(nullable=True)
```

From app/models/__init__.py:
```python
# All models imported and listed in __all__ for easy access
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create volunteer and shift models, schemas, and migration</name>
  <files>
    app/models/volunteer.py
    app/models/shift.py
    app/models/__init__.py
    app/db/base.py
    app/schemas/volunteer.py
    app/schemas/shift.py
    alembic/versions/005_volunteer_management.py
  </files>
  <action>
**Models (app/models/volunteer.py):**
Create Volunteer, VolunteerStatus, VolunteerSkill, VolunteerTag, VolunteerTagMember, VolunteerAvailability.

- `VolunteerStatus(StrEnum)`: PENDING, ACTIVE, INACTIVE
- `VolunteerSkill(StrEnum)`: CANVASSING, PHONE_BANKING, DATA_ENTRY, EVENT_SETUP, SOCIAL_MEDIA, TRANSLATION, DRIVING, VOTER_REGISTRATION, FUNDRAISING, GRAPHIC_DESIGN
- `Volunteer(Base)`: __tablename__="volunteers", indexes on campaign_id and user_id. Fields per CONTEXT.md: id (UUID pk), campaign_id (FK campaigns.id), user_id (FK users.id, nullable), first_name, last_name, phone, email, street, city, state (String(2)), zip_code, emergency_contact_name, emergency_contact_phone, notes (Text nullable), status (String(50), default PENDING, native_enum=False), skills (ARRAY(String), default list), created_by (FK users.id), created_at (server_default=func.now()), updated_at (server_default=func.now(), onupdate=func.now())
- `VolunteerTag(Base)`: __tablename__="volunteer_tags", id (UUID pk), campaign_id (FK campaigns.id), name (String(255)), created_at. Index on campaign_id.
- `VolunteerTagMember(Base)`: __tablename__="volunteer_tag_members", volunteer_id (FK volunteers.id, pk), tag_id (FK volunteer_tags.id, pk)
- `VolunteerAvailability(Base)`: __tablename__="volunteer_availability", id (UUID pk), volunteer_id (FK volunteers.id), start_at (datetime), end_at (datetime). Composite index on (volunteer_id, start_at, end_at).

**Models (app/models/shift.py):**
Create Shift, ShiftType, ShiftStatus, SignupStatus, ShiftVolunteer.

- `ShiftType(StrEnum)`: CANVASSING, PHONE_BANKING, GENERAL
- `ShiftStatus(StrEnum)`: SCHEDULED, ACTIVE, COMPLETED, CANCELLED
- `SignupStatus(StrEnum)`: SIGNED_UP, WAITLISTED, CHECKED_IN, CHECKED_OUT, CANCELLED, NO_SHOW
- `Shift(Base)`: __tablename__="shifts", indexes on campaign_id and (campaign_id, start_at). Fields per CONTEXT.md and RESEARCH.md: id, campaign_id, name, description, type (String(50), native_enum=False), status (String(50), default SCHEDULED), start_at, end_at, max_volunteers (Integer), location_name, street, city, state, zip_code, latitude (Float nullable), longitude (Float nullable), turf_id (FK turfs.id, nullable), phone_bank_session_id (FK phone_bank_sessions.id, nullable), created_by (FK users.id), created_at, updated_at
- `ShiftVolunteer(Base)`: __tablename__="shift_volunteers", id (UUID pk), shift_id (FK shifts.id), volunteer_id (FK volunteers.id), status (String(50), default SIGNED_UP), waitlist_position (Integer nullable), check_in_at (datetime nullable), check_out_at (datetime nullable), adjusted_hours (Float nullable), adjustment_reason (String(500) nullable), adjusted_by (FK users.id nullable), adjusted_at (datetime nullable), signed_up_at (server_default=func.now()). Index on shift_id. Unique constraint on (shift_id, volunteer_id).

**Update app/models/__init__.py:** Add imports for Volunteer, VolunteerTag, VolunteerTagMember, VolunteerAvailability, Shift, ShiftVolunteer to imports and __all__.

**Update app/db/base.py:** Add `import app.models.volunteer` and `import app.models.shift` at the bottom with the other model imports.

**Schemas (app/schemas/volunteer.py):**
- `VolunteerCreate(BaseSchema)`: first_name, last_name, phone (optional), email (optional), street/city/state/zip_code (all optional), emergency_contact_name (optional), emergency_contact_phone (optional), notes (optional), skills (list[str] default [])
- `VolunteerUpdate(BaseSchema)`: all fields optional
- `VolunteerResponse(BaseSchema)`: all fields + id, campaign_id, user_id, status, created_by, created_at, updated_at
- `VolunteerDetailResponse(VolunteerResponse)`: tags (list[str]), availability (list[AvailabilityResponse])
- `AvailabilityCreate(BaseSchema)`: start_at (datetime), end_at (datetime)
- `AvailabilityResponse(BaseSchema)`: id, volunteer_id, start_at, end_at
- `VolunteerTagCreate(BaseSchema)`: name (str)
- `VolunteerTagResponse(BaseSchema)`: id, campaign_id, name, created_at

**Schemas (app/schemas/shift.py):**
- `ShiftCreate(BaseSchema)`: name, description (optional), type (ShiftType), start_at, end_at, max_volunteers, location_name (optional), street/city/state/zip_code (all optional), latitude/longitude (all optional), turf_id (optional), phone_bank_session_id (optional)
- `ShiftUpdate(BaseSchema)`: all fields optional
- `ShiftResponse(BaseSchema)`: all fields + id, campaign_id, status, created_by, created_at, updated_at, signed_up_count (int), waitlist_count (int)
- `ShiftSignupResponse(BaseSchema)`: id, shift_id, volunteer_id, status, waitlist_position, check_in_at, check_out_at, signed_up_at
- `CheckInResponse(BaseSchema)`: shift_volunteer fields + hours (float | None, computed if checked out)
- `HoursAdjustment(BaseSchema)`: adjusted_hours (float), adjustment_reason (str)
- `VolunteerHoursSummary(BaseSchema)`: volunteer_id, total_hours (float), shifts_worked (int), shifts (list of shift summaries)

**Migration (alembic/versions/005_volunteer_management.py):**
Follow the exact pattern from 004_phone_banking.py. Create all 6 tables with proper column types. Add RLS policies:
- Direct RLS on volunteers, volunteer_tags, shifts (all have campaign_id)
- Subquery RLS on volunteer_tag_members (via volunteer_tags.campaign_id)
- Subquery RLS on volunteer_availability (via volunteers.campaign_id)
- Subquery RLS on shift_volunteers (via shifts.campaign_id)
- GRANT ALL on all 6 tables TO app_user
- ALTER TABLE ... ENABLE ROW LEVEL SECURITY on all 6 tables
- ALTER TABLE ... FORCE ROW LEVEL SECURITY on all 6 tables

Use `revision = "005"` and `down_revision = "004"` (match existing naming pattern -- check actual revision ID from 004_phone_banking.py).
  </action>
  <verify>
    <automated>cd /home/kwhatcher/projects/run-api && uv run python -c "from app.models.volunteer import Volunteer, VolunteerTag, VolunteerTagMember, VolunteerAvailability, VolunteerStatus, VolunteerSkill; from app.models.shift import Shift, ShiftVolunteer, ShiftType, ShiftStatus, SignupStatus; from app.schemas.volunteer import VolunteerCreate, VolunteerResponse, AvailabilityCreate; from app.schemas.shift import ShiftCreate, ShiftResponse, ShiftSignupResponse; print('All models and schemas import OK')"</automated>
  </verify>
  <done>All 6 model classes importable, all Pydantic schemas importable, migration file exists with RLS policies, models registered in __init__.py and base.py</done>
</task>

<task type="auto">
  <name>Task 2: Create test stub files for all Phase 5 test coverage</name>
  <files>
    tests/unit/test_volunteers.py
    tests/unit/test_shifts.py
    tests/integration/test_volunteer_rls.py
  </files>
  <action>
Follow the Phase 3 Wave 0 pattern: skip-marked test stubs with NotImplementedError bodies, no production imports at module level.

**tests/unit/test_volunteers.py:**
Skip-marked stubs covering VOL-01:
- test_create_volunteer_with_profile -- manager creates volunteer with full profile
- test_self_register_volunteer -- logged-in user self-registers (auto-links user_id)
- test_create_volunteer_walk_in -- manager creates walk-in volunteer (no user_id)
- test_update_volunteer_profile -- update profile fields
- test_volunteer_status_transitions -- pending->active->inactive only
- test_add_volunteer_skills -- add predefined skill categories
- test_add_volunteer_tags -- add free-form campaign-scoped tags
- test_remove_volunteer_tag -- remove tag from volunteer
- test_create_availability_slot -- add availability time window
- test_delete_availability_slot -- remove availability
- test_list_volunteers_with_filters -- search/filter volunteers
- test_get_volunteer_detail -- includes tags and availability
- test_volunteer_pending_cannot_signup -- pending status blocks shift signup

**tests/unit/test_shifts.py:**
Skip-marked stubs covering VOL-02 through VOL-06:
- test_create_shift_canvassing -- create canvassing shift with turf_id
- test_create_shift_phone_banking -- create phone banking shift with session_id
- test_create_shift_general -- create general shift (no operational link)
- test_update_shift -- update shift fields
- test_shift_status_transitions -- scheduled->active->completed, scheduled->cancelled
- test_volunteer_signup -- active volunteer signs up for shift
- test_signup_capacity_full_waitlisted -- signup when at capacity goes to waitlist
- test_signup_manager_override_capacity -- manager can add beyond capacity
- test_self_cancel_before_start -- volunteer cancels before shift.start_at
- test_self_cancel_after_start_blocked -- volunteer cannot cancel after start
- test_manager_remove_volunteer -- manager removes volunteer from shift
- test_waitlist_auto_promote -- cancellation promotes next waitlisted volunteer
- test_check_in_volunteer -- check-in records timestamp
- test_check_in_canvassing_creates_walk_list_canvasser -- side effect on canvassing shift
- test_check_in_phone_banking_creates_session_caller -- side effect on phone banking shift
- test_check_out_volunteer -- check-out records timestamp
- test_hours_calculation -- (check_out - check_in) in hours
- test_hours_with_adjustment -- adjusted_hours overrides computed hours
- test_volunteer_hours_summary -- aggregate hours across shifts
- test_emergency_contact_required_for_field_shift -- blocks signup without emergency contact

**tests/integration/test_volunteer_rls.py:**
Skip-marked stubs for RLS isolation:
- test_volunteer_isolation -- campaign A cannot see campaign B volunteers
- test_shift_isolation -- campaign A cannot see campaign B shifts
- test_volunteer_tag_isolation -- tags isolated by campaign
- test_shift_volunteer_isolation -- shift signups isolated
- test_volunteer_availability_isolation -- availability isolated

Each stub: `@pytest.mark.skip(reason="Phase 05 stub")` decorator, function body is `raise NotImplementedError`.
  </action>
  <verify>
    <automated>cd /home/kwhatcher/projects/run-api && uv run pytest tests/unit/test_volunteers.py tests/unit/test_shifts.py tests/integration/test_volunteer_rls.py --collect-only -q 2>&1 | tail -5</automated>
  </verify>
  <done>All test stubs collected by pytest (skip-marked), covering VOL-01 through VOL-06 plus RLS isolation</done>
</task>

</tasks>

<verification>
- All models import without error
- All schemas import without error
- Migration file references correct down_revision
- Test stubs collected by pytest (all skip-marked)
- Existing tests still pass: `uv run pytest tests/unit/ -x -q`
</verification>

<success_criteria>
- 6 SQLAlchemy model classes created across 2 model files
- 5 StrEnum types defined (VolunteerStatus, VolunteerSkill, ShiftType, ShiftStatus, SignupStatus)
- Pydantic schemas for all CRUD operations on volunteers and shifts
- Alembic migration with RLS policies for all 6 tables
- 38+ skip-marked test stubs across 3 test files
- Existing test suite unbroken
</success_criteria>

<output>
After completion, create `.planning/phases/05-volunteer-management/05-01-SUMMARY.md`
</output>
