---
phase: 60-parent-split-parallel-processing
plan: 03
type: execute
wave: 3
depends_on: ["60-01", "60-02"]
files_modified:
  - app/tasks/import_task.py
  - app/services/import_service.py
  - tests/unit/test_import_service.py
  - tests/unit/test_import_task.py
  - tests/integration/test_import_parallel_processing.py
autonomous: true
requirements: [CHUNK-04]
must_haves:
  truths:
    - "Each chunk worker processes only its assigned absolute row range."
    - "Each chunk worker uses its own database session and restores RLS after each batch commit."
    - "Multiple child tasks can run against one import without sharing the parent lock/session or writing parent aggregation state."
  artifacts:
    - path: "app/tasks/import_task.py"
      provides: "fully implemented `process_import_chunk` worker task with independent-session lifecycle"
    - path: "app/services/import_service.py"
      provides: "chunk progress sink support for row-bounded worker execution"
    - path: "tests/integration/test_import_parallel_processing.py"
      provides: "focused concurrency-shape verification for chunk workers"
  key_links:
    - from: "app/tasks/import_task.py"
      to: "async_session_factory"
      via: "fresh session per chunk worker"
      pattern: "async with async_session_factory"
    - from: "app/tasks/import_task.py"
      to: "app.services.import_service.process_import_range"
      via: "child worker row-bounded execution"
      pattern: "process_import_range"
    - from: "app/services/import_service.py"
      to: "commit_and_restore_rls"
      via: "per-batch chunk durability"
      pattern: "commit_and_restore_rls"
---

<objective>
Finish the Phase 60 runtime by implementing the child chunk worker over the shared row-bounded engine and proving the concurrent-worker shape.

Purpose: Complete CHUNK-04 per D-05 and D-06 by reusing the serial import loop semantics inside independent child tasks, while deliberately leaving Phase 61 aggregation and Phase 62 resilience features out of scope.
Output: An operational `process_import_chunk` task, chunk-aware progress persistence, and unit/integration verification that chunk workers are independent and range-bound.
</objective>

<execution_context>
@/home/kwhatcher/.codex/get-shit-done/workflows/execute-plan.md
@/home/kwhatcher/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/60-parent-split-parallel-processing/60-CONTEXT.md
@.planning/phases/60-parent-split-parallel-processing/60-RESEARCH.md
@.planning/phases/57-recovery-engine-completion-hardening/57-01-SUMMARY.md
@.planning/phases/59-chunk-schema-configuration/59-01-SUMMARY.md
@.planning/phases/60-parent-split-parallel-processing/60-01-PLAN.md
@.planning/phases/60-parent-split-parallel-processing/60-02-PLAN.md
@app/tasks/import_task.py
@app/services/import_service.py
@tests/unit/test_import_service.py
@tests/unit/test_import_task.py

<interfaces>
From app/models/import_job.py:
```python
class ImportChunk(Base):
    id: Mapped[uuid.UUID]
    campaign_id: Mapped[uuid.UUID]
    import_job_id: Mapped[uuid.UUID]
    row_start: Mapped[int]
    row_end: Mapped[int]
    status: Mapped[ImportChunkStatus]
    imported_rows: Mapped[int | None]
    skipped_rows: Mapped[int | None]
    last_committed_row: Mapped[int | None]
    error_report_key: Mapped[str | None]
    error_message: Mapped[str | None]
    last_progress_at: Mapped[datetime | None]
```

Expected child task contract from Plan 60-02:
```python
@procrastinate_app.task(name="process_import_chunk", queue="imports")
async def process_import_chunk(chunk_id: str, campaign_id: str) -> None: ...
```

Expected shared engine contract from Plan 60-01:
```python
async def process_import_range(
    ...,
    row_start: int = 1,
    row_end: int | None = None,
    chunk_id: str | None = None,
) -> None: ...
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add tests for independent chunk-worker execution</name>
  <files>tests/unit/test_import_service.py, tests/unit/test_import_task.py, tests/integration/test_import_parallel_processing.py</files>
  <behavior>
    - Test 1: `process_import_chunk()` opens its own session, sets campaign RLS before reads, and processes only `row_start..row_end`.
    - Test 2: chunk progress updates stay on `ImportChunk` fields (`imported_rows`, `skipped_rows`, `last_committed_row`, `last_progress_at`) and do not aggregate parent counters.
    - Test 3: a focused integration test proves multiple chunk workers can execute against the same import shape without sharing a parent session or lock.
  </behavior>
  <action>Create the worker-facing tests before finishing the child implementation. Extend `tests/unit/test_import_task.py` to assert a fresh `async_session_factory()` session per child, no parent advisory-lock reuse, and correct chunk status transitions (`QUEUED` -> `PROCESSING` -> terminal). Extend `tests/unit/test_import_service.py` to assert chunk-bound counters and chunk-specific error-prefix behavior. Add `tests/integration/test_import_parallel_processing.py` as a focused import-runtime integration file that verifies the concurrency shape with two chunk workers on one import, using repo-standard DB fixtures and mocks where storage or queue internals would otherwise make the test brittle.</action>
  <verify>
    <automated>uv run pytest tests/unit/test_import_service.py tests/unit/test_import_task.py tests/integration/test_import_parallel_processing.py -x</automated>
  </verify>
  <done>The tests encode the child-worker contract: independent session lifecycle, absolute row bounds, chunk-local progress, and no accidental parent aggregation or lock reuse.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement the child task over the shared ranged engine</name>
  <files>app/tasks/import_task.py, app/services/import_service.py, tests/unit/test_import_service.py, tests/unit/test_import_task.py, tests/integration/test_import_parallel_processing.py</files>
  <behavior>
    - Test 1: child workers call the shared row-bounded engine with the chunk's absolute row range and chunk progress sink.
    - Test 2: each chunk commit restores RLS and writes durable progress to `ImportChunk`, with chunk-specific error artifact prefixes.
    - Test 3: completed chunk workers mark only their chunk terminal in this phase; they do not finalize or aggregate the parent import.
  </behavior>
  <action>Finish `process_import_chunk()` in `app/tasks/import_task.py` so each worker creates its own session via `async_session_factory()`, sets RLS before any query, loads the chunk plus parent import, marks the chunk `PROCESSING`, and invokes the shared ranged engine with `row_start=chunk.row_start` and `row_end=chunk.row_end`. Extend `app/services/import_service.py` so the shared engine can persist chunk-local counters and chunk-specific error files while preserving `_process_single_batch()` plus `commit_and_restore_rls()` semantics. On success, the chunk can mark itself `COMPLETED`; on failure, mark that chunk `FAILED` with an explicit error. Do not add SQL SUM parent aggregation, merged error reports, `COMPLETED_WITH_ERRORS`, cancellation propagation, per-chunk crash resume, or secondary-work offloading; those remain deferred per D-11.</action>
  <verify>
    <automated>uv run pytest tests/unit/test_import_service.py tests/unit/test_import_task.py tests/integration/test_import_parallel_processing.py -x</automated>
  </verify>
  <done>Chunk workers execute through the same ranged import engine as the serial path, with independent sessions, per-batch durability, restored RLS, and chunk-local progress persisted on `ImportChunk` only.</done>
</task>

</tasks>

<verification>
Run `uv run pytest tests/unit/test_import_service.py tests/unit/test_import_task.py tests/integration/test_import_parallel_processing.py -x`.
Confirm no code in this plan updates parent imported/skipped/phones counters from child workers or attempts parent finalization.
</verification>

<success_criteria>
Phase 60 runtime support is complete: parent fan-out can defer child tasks, child workers process only their assigned rows with independent sessions and per-batch RLS restoration, and automated coverage proves the concurrent-worker shape without pulling in deferred Phase 61-63 behavior.
</success_criteria>

<output>
After completion, create `.planning/phases/60-parent-split-parallel-processing/60-03-SUMMARY.md`
</output>
