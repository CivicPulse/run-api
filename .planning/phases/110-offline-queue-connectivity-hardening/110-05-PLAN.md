---
plan_id: 110-05
phase: 110-offline-queue-connectivity-hardening
plan: 05
type: execute
wave: 4
autonomous: true
depends_on: [110-04]
files_modified:
  - web/src/components/field/ConnectivityPill.tsx
  - web/src/components/field/ConnectivityPill.test.tsx
  - web/src/components/field/ConnectivitySheet.tsx
  - web/src/components/field/ConnectivitySheet.test.tsx
  - web/src/components/field/FieldHeader.tsx
  - web/src/components/field/FieldHeader.test.tsx
  - web/src/routes/field/$campaignId.tsx
task_count: 3
requirements: [OFFLINE-02, OFFLINE-03, TEST-01, TEST-02]
requirement_ids: [OFFLINE-02, OFFLINE-03, TEST-01, TEST-02]
objective: >-
  Ship the glanceable ConnectivityPill + ConnectivitySheet that exposes the
  connectivity/sync/dead-letter state wired in plan 110-04 to volunteers — slot
  it into FieldHeader, keep OfflineBanner for prolonged-offline critical state
  only.
must_haves:
  truths:
    - "Pill visible in field-mode header on every route"
    - "Pill shows one of: Online (green), Offline (orange), Syncing (spinner), Syncing… slow (dim spinner), Last sync N min ago (idle)"
    - "Tapping pill opens Sheet with queue count + lastSyncAt + dead-letter cards"
    - "Each dead-letter card has Retry + Discard buttons that call store actions"
    - "Pill is min 44×44 touch target, AAA contrast"
  artifacts:
    - path: web/src/components/field/ConnectivityPill.tsx
      provides: "Badge-based pill rendering all 5 connectivity states"
    - path: web/src/components/field/ConnectivitySheet.tsx
      provides: "shadcn Sheet content rendering queue + dead-letter"
  key_links:
    - from: FieldHeader
      to: ConnectivityPill
      via: "new right-side slot"
    - from: ConnectivityPill onClick
      to: ConnectivitySheet
      via: "Sheet open state"
    - from: ConnectivitySheet Retry button
      to: offlineQueueStore.retryDeadLetter
      via: "button onClick"
---

<objective>
OFFLINE-02 demands a glanceable connectivity indicator in the field-mode shell.
Plan 110-04 put all the state in the store (`items`, `deadLetter`, `isSyncing`,
`isSlow`, `lastSyncAt`). This plan ships the UI that makes that state visible
and actionable, per CONTEXT.md §decisions:

- ConnectivityPill in `FieldHeader` right-side slot, always visible
- Tap → ConnectivitySheet with queue count, last-sync relative time, and
  dead-letter cards with Retry/Discard actions
- Keep `OfflineBanner` for the critical prolonged-offline case (CONTEXT
  §specifics — "Keep the banner for critical states")
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/110-offline-queue-connectivity-hardening/110-OFFLINE-AUDIT.md
@.planning/phases/110-offline-queue-connectivity-hardening/110-CONTEXT.md
@web/src/stores/offlineQueueStore.ts
@web/src/hooks/useSyncEngine.ts
@web/src/components/field/OfflineBanner.tsx
@web/src/routes/field/$campaignId.tsx
@web/src/components/ui/sheet.tsx
@web/src/components/ui/badge.tsx
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: ConnectivityPill component</name>
  <files>web/src/components/field/ConnectivityPill.tsx, web/src/components/field/ConnectivityPill.test.tsx</files>
  <behavior>
    - Test 1: Online + empty queue + no dead-letter → renders "Online" with `Wifi` icon, green dot.
    - Test 2: Offline → renders "Offline" with `WifiOff` icon, orange dot.
    - Test 3: Online + syncing + !isSlow → renders "Syncing N" with `Loader2` spinner (animate-spin).
    - Test 4: Online + syncing + isSlow → renders "Syncing N (slow)" dim.
    - Test 5: Online + !syncing + items.length > 0 → renders "N pending" orange dot (queue waiting to drain).
    - Test 6: Online + idle + lastSyncAt set → renders "Synced 2 min ago" (relative time via a small formatRelative helper).
    - Test 7: deadLetter.length > 0 → shows a red warning dot badge overlay regardless of primary state.
    - Test 8: Pill is a `<button>` with `aria-label` describing current state and min 44px height/width class.
    - Test 9: onClick fires a callback prop (Sheet open delegated to parent).
    - Test 10: AAA contrast — assertion on className tokens that resolve to meets-AAA colors (status-success-foreground, status-warning-foreground per existing tailwind tokens).
  </behavior>
  <action>
1. Create `web/src/components/field/ConnectivityPill.tsx`. Derive state from:
   - `useConnectivityStatus()` → isOnline
   - `useOfflineQueueStore` slice (items, deadLetter, isSyncing, isSlow, lastSyncAt)

2. Derivation order (most urgent first):
   - `deadLetter.length > 0` → show warning overlay dot (but primary label still reflects connectivity)
   - `!isOnline` → "Offline"
   - `isSyncing && !isSlow` → "Syncing N"
   - `isSyncing && isSlow` → "Syncing N (slow)"
   - `items.length > 0` → "N pending"
   - `lastSyncAt !== null` → "Synced {formatRelative(lastSyncAt)}"
   - default → "Online"

3. Use `shadcn/ui` Badge + Lucide icons (Wifi, WifiOff, Loader2, AlertCircle). Wrap in a `<button onClick={onClick}>` with `className="min-h-11 min-w-11 ..."` (44px = 11 tailwind units).

4. Write `ConnectivityPill.test.tsx` covering behaviors 1-10 with vi.mock for connectivity hook + setState shortcuts for the store.
  </action>
  <verify>
    <automated>cd web &amp;&amp; npx vitest run src/components/field/ConnectivityPill.test.tsx</automated>
  </verify>
  <done>All 10 pill tests pass.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: ConnectivitySheet + FieldHeader wiring</name>
  <files>web/src/components/field/ConnectivitySheet.tsx, web/src/components/field/ConnectivitySheet.test.tsx, web/src/components/field/FieldHeader.tsx, web/src/components/field/FieldHeader.test.tsx, web/src/routes/field/$campaignId.tsx</files>
  <behavior>
    - Sheet test 1: Renders with queue count + lastSyncAt relative time + "Dead letter" section.
    - Sheet test 2: Empty dead-letter → shows EmptyState "No failed syncs".
    - Sheet test 3: deadLetter with 2 items → renders 2 cards. Each card shows errorSummary, address (derived from resourceId or payload fallback), and Retry + Discard buttons.
    - Sheet test 4: Clicking Retry calls `retryDeadLetter(id)` and announces via toast. Button has aria-label.
    - Sheet test 5: Clicking Discard calls `discardDeadLetter(id)` after a confirm (use existing `ConfirmDialog` shared component). aria-label present.
    - Sheet test 6: Sheet uses shadcn Sheet primitive from `@/components/ui/sheet` (side="bottom" on mobile, side="right" on desktop via responsive prop or media query — planner's choice, prefer side="bottom" for field-mobile parity).
    - FieldHeader test: new right-side slot renders ConnectivityPill with the passed-in open handler.
    - Route integration test: `/field/$campaignId` renders FieldHeader with a working ConnectivityPill (spy on store).
  </behavior>
  <action>
1. Create `ConnectivitySheet.tsx`. Props: `open: boolean, onOpenChange: (open: boolean) => void`. Content sections:
   - **Header:** Current connectivity state (re-uses same derivation as pill)
   - **Queue summary:** "N outcomes pending" + lastSyncAt relative time
   - **Dead-letter list:** Map over store.deadLetter rendering `DeadLetterCard` (inline component):
     - Error summary
     - Payload address summary (best-effort — look up by entry_id against queryClient cache if present, else display resourceId)
     - Retry button → `retryDeadLetter` + toast
     - Discard button → ConfirmDialog → `discardDeadLetter`
   - If empty: `<EmptyState icon={CheckCircle2} title="No failed syncs" />` (reuse shared EmptyState).

2. Update `FieldHeader.tsx`: add an optional right-side slot for ConnectivityPill. Existing help button stays. Layout: `[back] [title] [pill] [help]`.

3. Update `/field/$campaignId.tsx`: add `const [connectivityOpen, setConnectivityOpen] = useState(false)`, pass `onConnectivityClick={() => setConnectivityOpen(true)}` to FieldHeader, render `<ConnectivitySheet open={connectivityOpen} onOpenChange={setConnectivityOpen} />` below the `<OfflineBanner />`.

4. Write all test files covering the behaviors above.

5. Keep `OfflineBanner` in the tree — it now only renders for the prolonged-offline critical case. Update OfflineBanner's render gate to only show if `!isOnline && items.length > 0` (i.e., user has unsynced data AND is offline — the "critical" definition from CONTEXT.md §specifics).
  </action>
  <verify>
    <automated>cd web &amp;&amp; npx vitest run src/components/field/ConnectivitySheet.test.tsx src/components/field/FieldHeader.test.tsx</automated>
  </verify>
  <done>Sheet, Header, and route integration tests green.</done>
</task>

<task type="auto">
  <name>Task 3: Visual verification checkpoint + commit</name>
  <files>web/src/components/field/ConnectivityPill.tsx, web/src/components/field/ConnectivitySheet.tsx, web/src/components/field/FieldHeader.tsx, web/src/routes/field/$campaignId.tsx</files>
  <action>
1. Run Playwright MCP (or similar browser automation) against the dev stack:
   - Navigate to `/field/{campaignId}/canvassing`
   - Screenshot desktop + mobile viewports
   - Simulate offline mode (Playwright context.setOffline(true)), record 2 outcomes, open pill sheet, screenshot
   - Simulate reconnect, observe pill transitions, screenshot
   - Save screenshots to `screenshots/110-05/` (gitignored)

2. Commit: `feat(110-05): ConnectivityPill + Sheet in field shell (OFFLINE-02/03)`
  </action>
  <verify>
    <automated>cd web &amp;&amp; npx tsc --noEmit &amp;&amp; npx vitest run</automated>
  </verify>
  <done>Visual screenshots captured, commit landed, tsc + full vitest green.</done>
</task>

</tasks>

<success_criteria>
- [ ] ConnectivityPill renders all 6 derived states
- [ ] ConnectivitySheet renders dead-letter with Retry/Discard
- [ ] FieldHeader slots pill correctly
- [ ] OfflineBanner scope narrowed to critical prolonged-offline
- [ ] Screenshots captured for desktop + mobile + offline state
- [ ] tsc + vitest clean
</success_criteria>

<output>
Create `.planning/phases/110-offline-queue-connectivity-hardening/110-05-SUMMARY.md`
</output>
