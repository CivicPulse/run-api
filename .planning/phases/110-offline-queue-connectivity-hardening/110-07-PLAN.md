---
plan_id: 110-07
phase: 110-offline-queue-connectivity-hardening
plan: 07
type: execute
wave: 5
autonomous: true
depends_on: [110-02, 110-03, 110-04, 110-05]
files_modified:
  - web/e2e/canvassing-offline-sync.spec.ts
task_count: 2
requirements: [OFFLINE-01, OFFLINE-02, OFFLINE-03, TEST-03]
requirement_ids: [OFFLINE-01, OFFLINE-02, OFFLINE-03, TEST-03]
objective: >-
  Ship the E2E spec that proves OFFLINE-01/02/03 end-to-end — the volunteer
  goes offline, records 3 outcomes, sees the pill transition, reconnects, and
  all 3 outcomes replay exactly once while the pill reflects each state.
must_haves:
  truths:
    - "Spec runs via web/scripts/run-e2e.sh"
    - "Test 1 (OFFLINE-01): 3 offline outcomes replay as 3 server requests on reconnect, no duplicates"
    - "Test 2 (OFFLINE-02): Pill shows Offline → Syncing → Synced transitions"
    - "Test 3 (OFFLINE-03): 5xx error puts item into backoff, recovery on retry"
    - "Test 4 (OFFLINE-03): 422 error puts item into dead-letter; volunteer sees it in Sheet and can Retry/Discard"
  artifacts:
    - path: web/e2e/canvassing-offline-sync.spec.ts
      provides: "4-test OFFLINE end-to-end suite"
  key_links:
    - from: Playwright context.setOffline(true)
      to: navigator.onLine
      via: "BrowserContext API"
    - from: Playwright page.route()
      to: drainQueue classifyError branches
      via: "mocked 422/500 responses"
---

<objective>
OFFLINE-01/02/03 each need TEST-03 E2E coverage per milestone requirement. The
flows are tightly coupled (queue → pill UI → sync) so one spec covering all
three in sequence is higher signal than three separate specs. This plan ships
that spec.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/110-offline-queue-connectivity-hardening/110-CONTEXT.md
@web/e2e/canvassing-wizard.spec.ts
@web/e2e/canvassing-house-selection.spec.ts
@web/scripts/run-e2e.sh
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write canvassing-offline-sync.spec.ts (4 tests)</name>
  <files>web/e2e/canvassing-offline-sync.spec.ts</files>
  <behavior>
    - **Test 1 — OFFLINE-01 happy path:** Log in as volunteer, navigate to a walk list. `context.setOffline(true)`. Record outcomes for 3 houses (use same selectors as canvassing-wizard.spec.ts). Assert pill shows "Offline" + "3 pending". `context.setOffline(false)`. Wait for pill to show "Synced". Assert via `page.waitForResponse` that exactly 3 POST /door-knocks fired, each with a unique client_uuid. Assert the queue is empty (probe via `window.localStorage.getItem("offline-queue")` returning `{items:[]}`).
    - **Test 2 — OFFLINE-02 state transitions:** Start online. Assert pill "Online". Go offline → "Offline". Record 1 outcome → "Offline · 1 pending". Go online → "Syncing 1" → "Synced N sec ago". Each transition verifiable via `getByRole("button", { name: /Offline|Syncing|Synced|Online/ })`.
    - **Test 3 — OFFLINE-03 5xx backoff recovery:** Use `page.route("**/door-knocks", route => ...)` to make the first POST return 503. Record an outcome (online). Assert pill shows pending + error toast. Wait 2s (backoff). Unroute. Trigger manual drain (go offline → online to refire). Assert 200 now lands and pill recovers.
    - **Test 4 — OFFLINE-03 dead-letter flow:** `page.route` → return 422 with a Pydantic error body. Record an outcome. Assert item moves to dead-letter (not active queue). Tap the pill to open ConnectivitySheet. Assert dead-letter card is visible with error summary and Retry + Discard buttons. Click Retry (unroute first so retry succeeds). Assert dead-letter empties.
  </behavior>
  <action>
1. Create `web/e2e/canvassing-offline-sync.spec.ts` using existing Playwright fixtures from `canvassing-wizard.spec.ts` (look up how it sets up the logged-in volunteer context and walk list navigation — reuse those helpers verbatim).

2. Key Playwright APIs to use:
   - `context.setOffline(true | false)` — toggles navigator.onLine and blocks network
   - `page.route("**/api/v1/**", route => route.fulfill(...))` — mock server responses for Test 3/4
   - `page.waitForResponse(url => url.includes("/door-knocks"))` — count successful POSTs
   - `page.evaluate(() => localStorage.getItem("offline-queue"))` — probe the queue directly
   - `expect(pill).toHaveAccessibleName(/Syncing/)` — state transitions via ARIA

3. Use `test.describe.serial` if the tests share setup (volunteer auth + walk list seeding). Otherwise use `test.beforeEach` for independent state.

4. All navigations use the Tailscale TLS URL from `E2E_DEV_SERVER_URL` env var per phase 108/109 wrapper convention.

5. Run the spec via `./scripts/run-e2e.sh canvassing-offline-sync.spec.ts` during development — wrapper logs to `e2e-runs.jsonl`.
  </action>
  <verify>
    <automated>cd web &amp;&amp; ./scripts/run-e2e.sh canvassing-offline-sync.spec.ts</automated>
  </verify>
  <done>All 4 tests pass. e2e-runs.jsonl entry for the run shows 4/0/0.</done>
</task>

<task type="auto">
  <name>Task 2: Commit</name>
  <files>web/e2e/canvassing-offline-sync.spec.ts</files>
  <action>
Commit: `test(110-07): E2E canvassing-offline-sync spec (OFFLINE-01/02/03)`
  </action>
  <verify>
    <automated>git log --oneline -1 | grep -q "110-07"</automated>
  </verify>
  <done>Commit landed.</done>
</task>

</tasks>

<success_criteria>
- [ ] 4 E2E tests in canvassing-offline-sync.spec.ts green
- [ ] Spec runs via run-e2e.sh wrapper (D-13 compliance)
- [ ] Spec covers all three OFFLINE requirements
</success_criteria>

<output>
Create `.planning/phases/110-offline-queue-connectivity-hardening/110-07-SUMMARY.md`
</output>
