---
plan_id: 110-06
phase: 110-offline-queue-connectivity-hardening
plan: 06
type: execute
wave: 5
autonomous: true
depends_on: [110-02, 110-03, 110-04, 110-05]
files_modified:
  - .planning/phases/110-offline-queue-connectivity-hardening/110-COVERAGE-AUDIT.md
  - web/src/**/*.test.ts
  - web/src/**/*.test.tsx
  - tests/**/test_*.py
task_count: 2
requirements: [TEST-01, TEST-02]
requirement_ids: [TEST-01, TEST-02]
objective: >-
  Produce the milestone-final coverage audit document that tabulates every
  source file modified across phases 106-110 vs its unit/integration/E2E test
  coverage. Close any gaps found with targeted test additions so TEST-01 and
  TEST-02 are literally true at the milestone exit gate.
must_haves:
  truths:
    - "110-COVERAGE-AUDIT.md exists with a row per modified source file for phases 106-110"
    - "Each row has columns Unit / Integration / E2E with concrete test file:line citations or gap notes"
    - "Any gap found is either closed in this plan or explicitly justified as N/A (e.g., pure config file)"
  artifacts:
    - path: .planning/phases/110-offline-queue-connectivity-hardening/110-COVERAGE-AUDIT.md
      provides: "Milestone v1.18 test coverage truth table"
  key_links:
    - from: "git log 106..110 --name-only"
      to: "coverage audit rows"
      via: "one row per unique source file"
---

<objective>
TEST-01 (unit coverage) and TEST-02 (integration coverage) are cross-cutting
obligations declared in REQUIREMENTS.md and anchored to phase 110. The
milestone-final gate (plan 110-08) will fail if this audit is not true. This
plan does the tabulation and backfills any gaps found.

**Output:** `110-COVERAGE-AUDIT.md` — the evidence TEST-01/02 are actually
satisfied across phases 106-110, not just claimed.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/REQUIREMENTS.md
@.planning/phases/106-test-baseline-trustworthiness/106-05-SUMMARY.md
@.planning/phases/107-canvassing-wizard-fixes/107-09-SUMMARY.md
@.planning/phases/108-house-selection-active-state/108-07-SUMMARY.md
@.planning/phases/109-map-rendering-asset-pipeline/109-06-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Generate coverage truth table + backfill gaps</name>
  <files>.planning/phases/110-offline-queue-connectivity-hardening/110-COVERAGE-AUDIT.md</files>
  <action>
1. **Collect modified source files** across all v1.18 phases using git:
```bash
# Find the commit right before phase 106 began
git log --oneline --all | grep -E "106-01" | tail -1
# Then diff name-only from that base to HEAD
git log --name-only --pretty=format: <base>..HEAD -- 'app/**/*.py' 'web/src/**/*.ts' 'web/src/**/*.tsx' | sort -u | grep -v test
```
Filter out test files, .md files, migrations, and fixtures. The result is the "touched source files" set.

2. **Classify each file by test coverage:**
   - **Unit:** is there a matching `.test.ts`/`.test.tsx` / `test_*.py` at the same path level? Cite file:line of at least one test that exercises the changed behavior.
   - **Integration:** is there a `tests/integration/test_*.py` (backend) or a TanStack Query mock-server test or a vitest test hitting a module boundary (frontend)? Cite file.
   - **E2E:** is there a `web/e2e/*.spec.ts` that exercises user-visible behavior from this file? Cite spec file + test name.

3. **Author `110-COVERAGE-AUDIT.md`** with:

```markdown
# Phase 110 — Milestone v1.18 Coverage Audit

**Scope:** Every source file modified in phases 106-110.
**Requirements:** TEST-01 (unit), TEST-02 (integration), TEST-03 (E2E — cross-ref plan 110-07).

## Coverage Matrix

| File | Phase | Unit | Integration | E2E | Status |
|---|---|---|---|---|---|
| app/services/walk_list.py | 107, 110 | tests/unit/test_walk_list_service.py::test_record_door_knock | tests/integration/test_walk_list_door_knock_idempotency.py | canvassing-wizard.spec.ts | ✓ |
| web/src/hooks/useCanvassingWizard.ts | 107, 108 | useCanvassingWizard.test.ts (45 tests) | useCanvassingWizard.state-machine.test.ts | canvassing-wizard.spec.ts, canvassing-house-selection.spec.ts | ✓ |
| ... |

## Gap Report

Files where coverage is missing:
- [ ] {file} — missing {unit|integration|e2e} — ACTION: {backfill in this plan / defer with justification}

## Gaps Closed in This Plan

List any tests added during Task 2 backfill.

## Conclusion

TEST-01 status: {SATISFIED | SATISFIED with {N} deferrals}
TEST-02 status: {SATISFIED | SATISFIED with {N} deferrals}
```

4. **Backfill gaps found.** For any file flagged missing unit/integration coverage, write the missing test(s). Typical backfill targets:
   - Glue code added but never unit-tested (e.g., small helper refactors in phases 107/108)
   - Config files that don't need tests → mark N/A in the table

Deferrals allowed ONLY if documented with a specific reason (e.g., "pure CSS
token file, no logic"). Anything non-trivial must get a test added here.

5. Aim to keep this plan under 2 hours of Claude execution time. If the gap
list explodes (>10 missing tests), STOP and propose plan 110-06 split — do not
silently narrow the audit.
  </action>
  <verify>
    <automated>test -f .planning/phases/110-offline-queue-connectivity-hardening/110-COVERAGE-AUDIT.md &amp;&amp; grep -q "TEST-01 status" .planning/phases/110-offline-queue-connectivity-hardening/110-COVERAGE-AUDIT.md &amp;&amp; grep -q "TEST-02 status" .planning/phases/110-offline-queue-connectivity-hardening/110-COVERAGE-AUDIT.md</automated>
  </verify>
  <done>Audit document exists + all gaps either closed or justified.</done>
</task>

<task type="auto">
  <name>Task 2: Commit</name>
  <files>.planning/phases/110-offline-queue-connectivity-hardening/110-COVERAGE-AUDIT.md</files>
  <action>
Commit: `docs(110-06): milestone v1.18 coverage audit (TEST-01/02)`. If tests
were added, also commit them with `test(110-06): backfill missing coverage`.
  </action>
  <verify>
    <automated>git log --oneline -3 | grep -q "110-06"</automated>
  </verify>
  <done>Commit(s) landed.</done>
</task>

</tasks>

<success_criteria>
- [ ] Every modified file across 106-110 has a row
- [ ] TEST-01 status declared SATISFIED in the doc
- [ ] TEST-02 status declared SATISFIED in the doc
- [ ] Any backfilled tests run green
</success_criteria>

<output>
Create `.planning/phases/110-offline-queue-connectivity-hardening/110-06-SUMMARY.md`
</output>
