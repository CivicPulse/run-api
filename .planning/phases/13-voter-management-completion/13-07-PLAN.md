---
phase: 13-voter-management-completion
plan: "07"
type: execute
wave: 2
depends_on: ["13-01", "13-02"]
files_modified:
  - web/src/routes/campaigns/$campaignId/voters/tags/index.tsx
autonomous: false
requirements: [VOTR-05]

must_haves:
  truths:
    - "Campaign tags index shows a DataTable with tag name, voter count, and action column"
    - "New Tag form creates a tag and shows it immediately in the list"
    - "Tag delete uses DestructiveConfirmDialog with confirmText equal to the tag name"
    - "Inline edit allows renaming a tag without navigating away"
  artifacts:
    - path: "web/src/routes/campaigns/$campaignId/voters/tags/index.tsx"
      provides: "Campaign tags management DataTable with create/edit/delete"
      min_lines: 80
  key_links:
    - from: "tags/index.tsx"
      to: "useCampaignTags / useCreateTag / useUpdateTag / useDeleteTag"
      via: "DataTable data source and action mutations"
      pattern: "useCampaignTags\\|useCreateTag"
---

<objective>
Build the campaign-level voter tags management page and add a visual verification checkpoint for the complete voter management feature.

Purpose: The tags route is the final data route in the voters section. It's relatively simple (DataTable + CRUD), but the checkpoint at the end ensures the entire voter management experience is visually coherent before the phase is considered done.

Output: Tags management page with full CRUD, visual checkpoint for the complete phase.
</objective>

<execution_context>
@/home/kwhatcher/.claude/get-shit-done/workflows/execute-plan.md
@/home/kwhatcher/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-voter-management-completion/13-RESEARCH.md
@.planning/phases/13-voter-management-completion/13-01-SUMMARY.md
@.planning/phases/13-voter-management-completion/13-02-SUMMARY.md

<interfaces>
<!-- Tag hooks (from web/src/hooks/useVoterTags.ts) -->

```typescript
export function useCampaignTags(campaignId: string): UseQueryResult<VoterTag[]>
export function useCreateCampaignTag(campaignId: string): UseMutationResult<VoterTag, unknown, { name: string }>
export function useUpdateCampaignTag(campaignId: string, tagId: string): UseMutationResult<VoterTag, unknown, { name: string }>
export function useDeleteCampaignTag(campaignId: string, tagId: string): UseMutationResult
// Note: hook names may differ — check actual exports in useVoterTags.ts
```

VoterTag type (web/src/types/voter-tag.ts):
```typescript
interface VoterTag {
  id: string
  campaign_id: string
  name: string
  // No color field — confirmed in RESEARCH.md
}
```

Per CONTEXT.md (Claude's discretion): color picker omitted since backend has no color field. Use a small colored dot from a pre-defined 8-color palette based on `tag.id % 8` for visual differentiation without backend changes.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build campaign tags management page</name>
  <files>web/src/routes/campaigns/$campaignId/voters/tags/index.tsx</files>
  <action>
    Replace the tags index stub with a full campaign tags management page.

    **Page header:** "Campaign Tags" h2 + RequireRole(manager): `+ New Tag` button (opens inline create row or a simple Dialog — use Dialog pattern for consistency with lists).

    **New Tag Dialog:**
    - Single field: tag name (required, min 1 char)
    - On submit: `createTag.mutateAsync({ name })`, toast.success("Tag created"), close dialog
    - Use react-hook-form + zod: `z.object({ name: z.string().min(1, "Tag name required") })`

    **Tags DataTable columns:**
    - color dot (Claude's discretion): a small `<div className="w-3 h-3 rounded-full" style={{ backgroundColor: PALETTE[hashTagId(tag.id)] }} />` — define PALETTE as 8 distinct Tailwind colors as hex values; hashTagId converts tag.id to an index (use `tag.id.charCodeAt(0) % 8` as a simple hash)
    - name: the tag name — clicking row or pencil icon opens inline edit
    - voter_count: right-aligned number showing how many voters have this tag (if available on VoterTag type; if not, omit column)
    - actions: kebab menu (RequireRole manager):
      - "Edit" — opens Edit Tag Dialog (same as New Tag but pre-populated with current name)
      - "Delete" — opens `<DestructiveConfirmDialog confirmText={tag.name} onConfirm={() => deleteTag.mutate()} />`

    **Edit Tag Dialog:** Same single-field form as New Tag but with `defaultValues: { name: tag.name }`. On submit: `updateTag.mutateAsync({ name })`.

    **Empty state:** `<EmptyState title="No tags yet" description="Create tags to organize your voters" action={<Button onClick={() => setCreateOpen(true)}>+ New Tag</Button>} />`

    Data: `const { data: tags = [], isLoading } = useCampaignTags(campaignId)`

    Note: Check actual hook export names in useVoterTags.ts — they may be useCreateTag, useUpdateTag, useDeleteTag, or prefixed with "Campaign". Use whatever is exported.
  </action>
  <verify>
    <automated>cd /home/kwhatcher/projects/run-api/web && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <done>tags/index.tsx compiles. DataTable renders tags with CRUD actions. New Tag dialog works. TypeScript 0 errors.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Checkpoint: Verify complete voter management experience</name>
  <action>Human verification of the complete voter management experience. See how-to-verify for steps.</action>
  <verify><automated>CHECKPOINT — human verification required</automated></verify>
  <done>User approves complete voter management experience</done>
  <files></files>
  <what-built>
    Complete voter management experience across all routes:
    - Plan 13-01: Hook bug fixes (set-primary URL, voter-lists URL, tag add body)
    - Plan 13-02: voters.tsx layout with sidebar nav (All Voters | Lists | Tags)
    - Plan 13-03: Voters index DataTable + VoterFilterBuilder collapsible panel + New Voter sheet
    - Plan 13-04: Voter detail sub-tabs (Overview, Contacts, Tags)
    - Plan 13-05: History tab + Edit Voter sheet with useFormGuard
    - Plan 13-06: Voter Lists index + static/dynamic detail + AddVotersDialog
    - Plan 13-07: Campaign Tags management
  </what-built>
  <how-to-verify>
    Start the dev server if not running: `cd /home/kwhatcher/projects/run-api/web && npm run dev`

    1. Navigate to any campaign → Voters sidebar group should show: All Voters | Lists | Tags

    2. All Voters page:
       - DataTable renders voter rows (name, party, city)
       - Click "Filters" button → filter panel slides open with Party, Age, Voting History, Tags, City sections
       - Click "More filters" → additional fields appear
       - Click "More filters" again → collapses
       - Click "+ New Voter" (manager role required) → right-side Sheet opens with create form
       - Type in filter fields → dismissible chips appear between panel and table

    3. Voter detail ($voterId):
       - Four tabs appear: Overview | Contacts | Tags | History
       - Overview tab: demographics in two-column layout
       - Contacts tab: Phone Numbers / Email Addresses / Mailing Addresses sections
         - Star icon visible on each contact row
         - Pencil icon (manager+) expands inline edit form on click
         - "+ Add phone" (manager+) expands inline add form
       - Tags tab: current voter tags as chips with × buttons; Add Tag selector shows campaign tags
       - History tab: existing notes displayed; textarea at top for adding new note
       - Page header: voter name + party badge + Edit button (manager+) + Add Interaction button
       - Clicking Edit opens right-side Sheet with pre-populated voter fields

    4. Lists page (/voters/lists):
       - DataTable with list name, type badge (Static/Dynamic), member count
       - "+ New List" → dialog type selector (Static / Dynamic cards)
       - Select Dynamic → filter builder appears in dialog
       - Click a static list → member table + Add Voters button
       - Click Add Voters → search dialog with voter search input and checkboxes
       - Click a dynamic list → filter summary chips + Edit Filters button + member table

    5. Tags page (/voters/tags):
       - DataTable with tag names and colored dots
       - "+ New Tag" dialog creates a tag
       - Kebab menu → Edit / Delete (Delete requires typing tag name)

    Report any visual issues, broken flows, or TypeScript errors in the browser console.
  </how-to-verify>
  <resume-signal>Type "approved" if the complete voter management experience looks correct, or describe specific issues to fix</resume-signal>
</task>

</tasks>

<verification>
- Tags page compiles: `cd web && npx tsc --noEmit`
- Full test suite: `cd web && npm run test -- --run 2>&1 | tail -10`
- All 11 VOTR requirements covered across plans 13-01 through 13-07
</verification>

<success_criteria>
- Campaign tags DataTable renders with CRUD actions
- New Tag dialog creates tags immediately visible in list
- Delete tag requires typing tag name (DestructiveConfirmDialog)
- Visual checkpoint approved — all voter management flows work end-to-end
- Full test suite passes (all plans combined)
</success_criteria>

<output>
After completion, create `.planning/phases/13-voter-management-completion/13-07-SUMMARY.md`
</output>
