---
phase: 58-e2e-core-tests
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - web/e2e/rbac.spec.ts
  - web/e2e/rbac.admin.spec.ts
  - web/e2e/rbac.manager.spec.ts
  - web/e2e/rbac.volunteer.spec.ts
  - web/e2e/rbac.viewer.spec.ts
autonomous: true
requirements: [E2E-01]

must_haves:
  truths:
    - "Viewer role cannot see any mutation buttons (New Voter, Edit, New Turf, New Call List, New Survey)"
    - "Volunteer role can see Add Interaction but not Edit voter or management buttons"
    - "Manager role can see all CRUD buttons but not campaign settings Members tab"
    - "Admin role has all manager caps plus member management and campaign settings"
    - "Owner role has all admin caps plus danger zone actions (Transfer Ownership, Delete Campaign)"
    - "Org admin has admin-equivalent access on campaigns they were not explicitly invited to"
    - "Org owner has owner-equivalent access on campaigns they were not explicitly invited to"
  artifacts:
    - path: "web/e2e/rbac.viewer.spec.ts"
      provides: "Viewer permission checklist E2E tests"
      contains: "test.describe"
    - path: "web/e2e/rbac.volunteer.spec.ts"
      provides: "Volunteer permission checklist E2E tests"
      contains: "test.describe"
    - path: "web/e2e/rbac.manager.spec.ts"
      provides: "Manager permission checklist E2E tests"
      contains: "test.describe"
    - path: "web/e2e/rbac.admin.spec.ts"
      provides: "Admin permission checklist E2E tests"
      contains: "test.describe"
    - path: "web/e2e/rbac.spec.ts"
      provides: "Owner permission checklist E2E tests"
      contains: "test.describe"
  key_links:
    - from: "web/e2e/rbac.viewer.spec.ts"
      to: "playwright/.auth/viewer.json"
      via: "Playwright viewer project storageState"
      pattern: "\\.viewer\\.spec\\.ts"
    - from: "web/e2e/rbac.admin.spec.ts"
      to: "playwright/.auth/admin.json"
      via: "Playwright admin project storageState"
      pattern: "\\.admin\\.spec\\.ts"
---

<objective>
Create the 5 RBAC permission-checklist E2E specs that verify each campaign role (viewer, volunteer, manager, admin, owner) can access permitted actions and is blocked from unpermitted ones, including org-level role cross-campaign access (RBAC-03 through RBAC-09).

Purpose: Proves the permission matrix is enforced in the UI -- the foundation of multi-tenant security.
Output: 5 spec files in `web/e2e/` covering all RBAC test cases from the testing plan.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/58-e2e-core-tests/58-CONTEXT.md
@.planning/phases/58-e2e-core-tests/58-RESEARCH.md
@.planning/phases/57-test-infrastructure/57-01-SUMMARY.md
@.planning/phases/57-test-infrastructure/57-02-SUMMARY.md

<interfaces>
<!-- Auth project routing from web/playwright.config.ts -->
<!-- Specs are routed to auth projects by filename suffix: -->
<!-- .spec.ts (no suffix) -> chromium project -> playwright/.auth/owner.json (owner1@localhost) -->
<!-- .admin.spec.ts -> admin project -> playwright/.auth/admin.json (admin1@localhost) -->
<!-- .manager.spec.ts -> manager project -> playwright/.auth/manager.json (manager1@localhost) -->
<!-- .volunteer.spec.ts -> volunteer project -> playwright/.auth/volunteer.json (volunteer1@localhost) -->
<!-- .viewer.spec.ts -> viewer project -> playwright/.auth/viewer.json (viewer1@localhost) -->

<!-- Existing RBAC spec pattern from web/e2e/role-gated.admin.spec.ts: -->
```typescript
import { test, expect } from "@playwright/test"

test.describe("Role-gated UI: org_admin user", () => {
  test("org settings page shows read-only name input and hides Save Changes", async ({ page }) => {
    await page.goto("/")
    await page.waitForURL(/\/(campaigns|org)/, { timeout: 15_000 })
    // ... navigate and assert visibility
  })
})
```

<!-- Navigation pattern to enter seed campaign: -->
```typescript
await page.goto("/")
await page.waitForURL(/\/(campaigns|org)/, { timeout: 15_000 })
const campaignLink = page.getByRole("link", { name: /macon|bibb|campaign/i }).first()
await campaignLink.click()
await page.waitForURL(/campaigns\/[a-f0-9-]+/, { timeout: 10_000 })
const campaignId = page.url().match(/campaigns\/([a-f0-9-]+)/)?.[1] ?? ""
```

<!-- UI Element Reference (role gates from source code): -->
<!-- Voters index: "+ New Voter" button -> RequireRole minimum="manager" -->
<!-- Voter detail: "Edit" button (Pencil icon) -> RequireRole minimum="manager" -->
<!-- Voter detail: "Add Interaction" button -> No role gate (all roles) -->
<!-- Canvassing: "New Turf" link -> Requires manager+ -->
<!-- Phone Banking: varies -> Requires manager+ -->
<!-- Surveys: varies -> Requires manager+ -->
<!-- Org dashboard: "Create Campaign" link -> RequireOrgRole minimum="org_admin" -->
<!-- Org settings: "Save Changes" button -> org_owner only -->
<!-- Org settings: Org Name input -> readonly for non-owner (readOnly attr + bg-muted class) -->
<!-- Campaign settings > Members: admin+ -->
<!-- Campaign danger: "Transfer Ownership" -> owner only -->
<!-- Campaign danger: "Delete Campaign" -> owner only -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create viewer, volunteer, and manager RBAC specs</name>
  <files>web/e2e/rbac.viewer.spec.ts, web/e2e/rbac.volunteer.spec.ts, web/e2e/rbac.manager.spec.ts</files>
  <read_first>
    - web/e2e/role-gated.admin.spec.ts (existing RBAC pattern -- replicate structure)
    - web/e2e/role-gated.volunteer.spec.ts (existing volunteer RBAC pattern)
    - web/playwright.config.ts (auth project routing regex)
    - docs/testing-plan.md lines 129-161 (RBAC-03, RBAC-04, RBAC-05 test cases)
    - web/src/routes/campaigns/$campaignId/voters/index.tsx (New Voter button role gate)
    - web/src/routes/campaigns/$campaignId/settings/members.tsx (members page role gate)
  </read_first>
  <action>
Create 3 RBAC spec files following the permission-checklist pattern per D-06. Each spec navigates to every major page in the seed campaign and asserts visibility/absence of mutation buttons.

**rbac.viewer.spec.ts** (runs under viewer auth via `.viewer.spec.ts` suffix) -- implements RBAC-03:
- `test.describe("RBAC: viewer permissions", () => { ... })` with 8-10 individual tests
- Test: Navigate to Voters page -> `page.getByRole("button", { name: /new voter/i })` should NOT be visible
- Test: Navigate to a seed voter detail -> `page.getByRole("button", { name: /edit/i })` should NOT be visible
- Test: Navigate to Canvassing -> `page.getByRole("link", { name: /new turf/i })` should NOT be visible
- Test: Navigate to Phone Banking > Call Lists -> "New Call List" button should NOT be visible
- Test: Navigate to Surveys -> "New Survey" button should NOT be visible
- Test: Navigate to Volunteers > Roster -> no edit/delete row actions visible
- Test: Navigate to Campaign Settings -> settings are read-only or inaccessible (check for presence of members link -- should NOT be visible for viewer)
- Test: Direct URL access to `/campaigns/${campaignId}/canvassing/turfs/new` -> should redirect or show permission error
- Test: On org dashboard, "Create Campaign" link should NOT be visible (viewer has no org role)
- Each test starts with `await page.goto("/")` then navigates into the seed campaign using `page.getByRole("link", { name: /macon|bibb|campaign/i }).first().click()`
- Use `await page.waitForURL(...)` with regex after each navigation
- Use `await expect(element).not.toBeVisible()` for negative assertions (not `.toBeHidden()`)

**rbac.volunteer.spec.ts** (runs under volunteer auth) -- implements RBAC-04:
- `test.describe("RBAC: volunteer permissions", () => { ... })` with 5-7 tests
- Test: Voter detail -> "Add Interaction" button IS visible, "Edit" button is NOT visible
- Test: Canvassing -> no "New Turf" or delete actions visible
- Test: Phone Banking > Sessions -> if session exists, calling screen accessible
- Test: Campaign Settings -> page inaccessible or members/danger sections hidden
- Test: On org dashboard, "Create Campaign" link NOT visible (volunteer has no org role)
- NOTE: Do NOT duplicate the existing `role-gated.volunteer.spec.ts` tests -- focus on the comprehensive RBAC-04 checklist that the existing file does not fully cover

**rbac.manager.spec.ts** (runs under manager auth) -- implements RBAC-05:
- `test.describe("RBAC: manager permissions", () => { ... })` with 8-10 tests
- Test: Voters -> "New Voter" button IS visible (positive assertion)
- Test: Voter detail -> "Edit" button IS visible
- Test: Canvassing -> "New Turf" link IS visible
- Test: Phone Banking -> "New Call List" and "New Session" buttons IS visible
- Test: Surveys -> "New Survey" button IS visible
- Test: Volunteers -> registration and management actions available
- Test: Campaign Settings -> Members section NOT accessible (admin+ only). Navigate to campaign settings and verify the Members nav link is NOT visible or the members page is inaccessible
- Test: On org dashboard, "Create Campaign" link NOT visible (manager has no org role)
  </action>
  <verify>
    <automated>cd /home/kwhatcher/projects/civicpulse/run-api/web && npx playwright test rbac.viewer rbac.volunteer rbac.manager --reporter=list 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - web/e2e/rbac.viewer.spec.ts exists and contains `test.describe("RBAC: viewer permissions"`
    - web/e2e/rbac.viewer.spec.ts contains `not.toBeVisible()` assertions for New Voter, Edit, New Turf buttons
    - web/e2e/rbac.viewer.spec.ts contains at least 8 `test(` calls
    - web/e2e/rbac.volunteer.spec.ts exists and contains `test.describe("RBAC: volunteer permissions"`
    - web/e2e/rbac.volunteer.spec.ts contains positive assertion for "Add Interaction" and negative for "Edit"
    - web/e2e/rbac.manager.spec.ts exists and contains `test.describe("RBAC: manager permissions"`
    - web/e2e/rbac.manager.spec.ts contains positive assertions for New Voter, Edit, New Turf buttons
    - web/e2e/rbac.manager.spec.ts contains negative assertion for campaign settings Members section
    - All three files use `import { test, expect } from "@playwright/test"`
    - No file contains `page.waitForTimeout()` (anti-pattern per research)
  </acceptance_criteria>
  <done>3 RBAC spec files created. Viewer spec has 8+ tests verifying all mutation buttons are hidden. Volunteer spec has 5+ tests verifying interaction recording but no management access. Manager spec has 8+ tests verifying operational CRUD access but no member management.</done>
</task>

<task type="auto">
  <name>Task 2: Create admin and owner RBAC specs with org-level role tests</name>
  <files>web/e2e/rbac.admin.spec.ts, web/e2e/rbac.spec.ts</files>
  <read_first>
    - web/e2e/role-gated.admin.spec.ts (existing admin RBAC -- this file will be superseded but kept per D-11)
    - web/e2e/rbac.viewer.spec.ts (Task 1 output -- follow same structure)
    - docs/testing-plan.md lines 163-192 (RBAC-06, RBAC-07, RBAC-08, RBAC-09 test cases)
    - web/src/routes/campaigns/$campaignId/settings/danger.tsx (Transfer Ownership, Delete Campaign role gates)
    - web/src/routes/index.tsx (org dashboard Create Campaign role gate)
    - web/src/routes/org/settings.tsx (org settings Save Changes role gate)
  </read_first>
  <action>
Create 2 RBAC spec files for the privileged roles. These specs verify elevated permissions plus org-level cross-campaign access.

**rbac.admin.spec.ts** (runs under admin auth via `.admin.spec.ts` suffix) -- implements RBAC-06 and RBAC-08:
- `test.describe("RBAC: admin permissions", () => { ... })` with 8-10 tests
- RBAC-06 tests (admin has all manager capabilities plus settings):
  - Test: Voters -> "New Voter" button IS visible
  - Test: Voter detail -> "Edit" button IS visible
  - Test: Canvassing -> "New Turf" link IS visible
  - Test: Campaign Settings > Members -> page loads, invite/role-change/remove actions visible. Navigate to `/campaigns/${campaignId}/settings/members` and verify the member list renders
  - Test: Campaign Settings > General -> campaign name/description fields are editable (not readonly)
  - Test: Campaign Settings > Danger Zone -> section visible but "Transfer Ownership" and "Delete Campaign" buttons should NOT be visible (owner-only per D-08). Navigate to `/campaigns/${campaignId}/settings/danger` and check
- RBAC-08 test (org_admin cross-campaign access -- admin1@localhost has org_admin role):
  - Test: On org dashboard, "Create Campaign" link IS visible (org_admin can create campaigns)
  - Test: Org settings page -> name input should be readonly with `bg-muted` class (org_admin, not org_owner). Assert `page.getByLabel(/organization name/i)` has attribute `readonly` and class matching `bg-muted`
  - Test: "Save Changes" button NOT visible on org settings (org_owner only)
  - Test: Organization Members sidebar link IS visible

NOTE: This file REPLACES the content of the existing `role-gated.admin.spec.ts`. Since the new file uses the same filename, it will supersede the old one. The old content (3 tests) is fully covered by the new comprehensive suite.

**rbac.spec.ts** (runs under owner/default auth via unsuffixed `.spec.ts`) -- implements RBAC-07 and RBAC-09:
- `test.describe("RBAC: owner permissions", () => { ... })` with 8-10 tests
- RBAC-07 tests (owner has all admin capabilities plus destructive actions):
  - Test: All manager capabilities available (New Voter, Edit, New Turf -- spot check 2-3)
  - Test: Campaign Settings > Members -> accessible, invite/role actions visible
  - Test: Campaign Settings > Danger Zone -> "Transfer Ownership" button IS visible
  - Test: Campaign Settings > Danger Zone -> "Delete Campaign" button IS visible
  - DO NOT actually click Transfer Ownership or Delete Campaign -- just assert visibility
- RBAC-09 tests (org_owner cross-campaign access -- owner1@localhost has org_owner role):
  - Test: On org dashboard, "Create Campaign" link IS visible
  - Test: Org settings page -> name input is NOT readonly (org_owner can edit). Assert `page.getByLabel(/organization name/i)` does NOT have attribute `readonly`
  - Test: "Save Changes" button IS visible on org settings
  - Test: Organization Members sidebar link IS visible
  </action>
  <verify>
    <automated>cd /home/kwhatcher/projects/civicpulse/run-api/web && npx playwright test rbac.admin rbac.spec --reporter=list 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - web/e2e/rbac.admin.spec.ts exists and contains `test.describe("RBAC: admin permissions"`
    - web/e2e/rbac.admin.spec.ts contains positive assertion for campaign settings Members page
    - web/e2e/rbac.admin.spec.ts contains negative assertion for "Transfer Ownership" button
    - web/e2e/rbac.admin.spec.ts contains test for org settings readonly input with `bg-muted`
    - web/e2e/rbac.admin.spec.ts contains at least 8 `test(` calls
    - web/e2e/rbac.spec.ts exists and contains `test.describe("RBAC: owner permissions"`
    - web/e2e/rbac.spec.ts contains positive assertions for "Transfer Ownership" and "Delete Campaign" buttons
    - web/e2e/rbac.spec.ts contains test for org settings editable name input (not readonly)
    - web/e2e/rbac.spec.ts contains test for "Save Changes" button visibility
    - Both files use `import { test, expect } from "@playwright/test"`
    - Neither file contains `page.waitForTimeout()` (anti-pattern)
  </acceptance_criteria>
  <done>2 RBAC spec files created. Admin spec has 8+ tests verifying all manager caps plus member management, settings access, and org_admin cross-campaign access. Owner spec has 8+ tests verifying all admin caps plus danger zone actions and org_owner cross-campaign access. All 5 RBAC files together cover RBAC-03 through RBAC-09 (skipping RBAC-01 and RBAC-02 per D-07).</done>
</task>

</tasks>

<verification>
Run all 5 RBAC specs together:
```bash
cd web && npx playwright test rbac --reporter=list
```
All tests should pass. Each role spec should verify the correct permission boundary.
</verification>

<success_criteria>
- 5 RBAC spec files exist in web/e2e/ with correct filename suffixes for auth routing
- Viewer spec proves all mutation buttons hidden (RBAC-03)
- Volunteer spec proves interaction recording allowed, management denied (RBAC-04)
- Manager spec proves operational CRUD allowed, member management denied (RBAC-05)
- Admin spec proves settings + member access, danger zone denied, org_admin cross-campaign (RBAC-06, RBAC-08)
- Owner spec proves full access including danger zone, org_owner cross-campaign (RBAC-07, RBAC-09)
- All specs use accessible selectors (getByRole, getByLabel, getByText) not CSS classes
- No hardcoded campaign IDs -- all extracted from URL after navigation
</success_criteria>

<output>
After completion, create `.planning/phases/58-e2e-core-tests/58-01-SUMMARY.md`
</output>
