---
phase: 58-e2e-core-tests
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - web/e2e/org-management.spec.ts
  - web/e2e/campaign-settings.spec.ts
autonomous: true
requirements: [E2E-02, E2E-03]

must_haves:
  truths:
    - "Owner can view org dashboard with campaign cards and stats"
    - "Owner can create a new campaign from org dashboard and is redirected to its dashboard"
    - "Owner can archive a campaign and it moves to an archived section"
    - "Owner can unarchive a campaign and it returns to active"
    - "Owner can edit org name and it updates across the UI"
    - "Owner can view org member directory with roles"
    - "Owner can edit campaign general settings (name, description)"
    - "Owner can invite a member, change their role, and remove them"
    - "Owner can transfer ownership of one throwaway campaign to another user"
    - "Owner can delete a separate throwaway campaign via danger zone type-to-confirm"
  artifacts:
    - path: "web/e2e/org-management.spec.ts"
      provides: "Org dashboard, campaign lifecycle, settings, and member management E2E tests"
      contains: "test.describe.serial"
    - path: "web/e2e/campaign-settings.spec.ts"
      provides: "Campaign settings CRUD, member management, ownership transfer, deletion E2E tests"
      contains: "test.describe.serial"
  key_links:
    - from: "web/e2e/org-management.spec.ts"
      to: "/api/v1/campaigns"
      via: "Campaign creation POST and archive/unarchive PATCH"
      pattern: "waitForResponse.*campaigns"
    - from: "web/e2e/campaign-settings.spec.ts"
      to: "/api/v1/campaigns/{id}/settings"
      via: "Settings CRUD and member management"
      pattern: "waitForResponse.*settings\\|members"
---

<objective>
Create org management and campaign settings E2E specs that validate the full organizational lifecycle (dashboard, campaign creation, archive/unarchive, settings, members) and campaign administration (settings CRUD, member invite/role-change/remove, ownership transfer, deletion).

Purpose: Proves the multi-tenant organization and campaign administration workflows function correctly end-to-end.
Output: 2 spec files in `web/e2e/` covering ORG-01 through ORG-08 and CAMP-01 through CAMP-06.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/58-e2e-core-tests/58-CONTEXT.md
@.planning/phases/58-e2e-core-tests/58-RESEARCH.md
@.planning/phases/57-test-infrastructure/57-01-SUMMARY.md

<interfaces>
<!-- Both specs run as owner (unsuffixed .spec.ts -> chromium -> owner1@localhost) -->

<!-- Existing campaign-archive.spec.ts pattern (from web/e2e/campaign-archive.spec.ts): -->
```typescript
// Campaign card targeting pattern
const campaignCard = page.locator("[class*='grid'] .block").first()
const actionsButton = campaignCard.getByRole("button", { name: /campaign actions/i })
await actionsButton.click()
await page.getByRole("menuitem", { name: /archive campaign/i }).click()
// Confirm dialog
const confirmButton = page.getByRole("button", { name: /^archive campaign$/i })
await confirmButton.click()
// Archived section
const archivedToggle = page.getByRole("button", { name: /archived/i })
await archivedToggle.click()
```

<!-- Connected journey campaign creation pattern (from web/e2e/connected-journey.spec.ts): -->
```typescript
campaignName = `E2E Journey ${Date.now()}`
await page.getByRole("link", { name: /new campaign/i }).first().click()
await page.waitForURL(/campaigns\/new/)
await page.getByLabel("Campaign Name *").fill(campaignName)
await page.getByRole("combobox").first().click()
await page.getByRole("option", { name: "Local" }).click()
const responsePromise = page.waitForResponse(
  (resp) => resp.url().includes("api/v1/campaigns") && resp.request().method() === "POST"
)
await page.getByRole("button", { name: /create campaign/i }).click()
const response = await responsePromise
expect(response.status()).toBeLessThan(300)
await page.waitForURL(/campaigns\/[a-f0-9-]+\/dashboard/, { timeout: 15_000 })
```

<!-- DestructiveConfirmDialog pattern: -->
```typescript
// Dialog requires typing the entity name to confirm
const confirmInput = page.getByPlaceholder(/type.*confirm/i).or(page.getByRole("textbox").last())
await confirmInput.fill("exact entity name")
await page.getByRole("button", { name: /delete/i }).click()
```

<!-- Key routes: -->
<!-- / -> org dashboard -->
<!-- /campaigns/new -> campaign creation form -->
<!-- /org/settings -> org settings page -->
<!-- /org/members -> org member directory -->
<!-- /campaigns/{id}/settings/general -> campaign general settings -->
<!-- /campaigns/{id}/settings/members -> campaign member management -->
<!-- /campaigns/{id}/settings/danger -> danger zone (transfer, delete) -->

<!-- Test users available (from Phase 57 provisioning): -->
<!-- owner1@localhost (org_owner), owner2@localhost, owner3@localhost -->
<!-- admin1@localhost (org_admin), admin2@localhost, admin3@localhost -->
<!-- manager1@localhost, manager2@localhost, manager3@localhost -->
<!-- volunteer1@localhost, volunteer2@localhost, volunteer3@localhost -->
<!-- viewer1@localhost, viewer2@localhost, viewer3@localhost -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create org-management E2E spec</name>
  <files>web/e2e/org-management.spec.ts</files>
  <read_first>
    - web/e2e/connected-journey.spec.ts (campaign creation pattern with test.step)
    - web/e2e/campaign-archive.spec.ts (archive/unarchive pattern with campaign card targeting)
    - docs/testing-plan.md lines 196-271 (ORG-01 through ORG-08 test cases)
    - web/src/routes/index.tsx (org dashboard -- campaign card grid, stats, Create Campaign button)
    - web/src/routes/org/settings.tsx (org settings -- name edit, save)
    - web/src/routes/org/members.tsx (org member directory)
  </read_first>
  <action>
Create `web/e2e/org-management.spec.ts` implementing ORG-01 through ORG-07 from the testing plan. Use `test.describe.serial` per D-09 since tests form an ordered lifecycle (create campaign -> archive -> unarchive -> delete as test assertion). Runs as owner (unsuffixed filename -> owner1@localhost).

Structure the spec with these serial tests:

1. **ORG-01: View org dashboard** -- Navigate to `/`, verify campaign card grid visible with seed campaign (Macon-Bibb), verify stats bar or campaign count visible, verify org name in header/sidebar. Use `await page.goto("/")` then `await page.waitForURL(/\/(campaigns|org)/, { timeout: 15_000 })`.

2. **ORG-02: Create a new campaign from org dashboard** -- Click `page.getByRole("link", { name: /new campaign/i })`. Fill name with `E2E Org Test ${Date.now()}` (unique timestamped name per Pitfall 4). Fill campaign type via combobox (select "Local"). Use `page.waitForResponse()` to catch the POST. Submit and verify redirect to new campaign dashboard. Store the campaign name in a `let` variable at describe scope for later tests.

3. **ORG-03: Archive the new campaign** -- Navigate back to org dashboard `/`. Find the campaign card by name (`page.getByText(campaignName)`). Open campaign actions menu (three-dot button). Click "Archive Campaign" menuitem. Confirm the dialog. Verify toast appears. Verify campaign moves to "Archived" section (click archived toggle, verify name appears there).

4. **ORG-04: Unarchive the campaign** -- From org dashboard, expand the "Archived" section if collapsed. Find the archived campaign card. Open actions menu. Click "Unarchive" or "Restore" menuitem. Confirm it returns to active campaign grid. Verify by checking the campaign name appears outside the archived section.

5. **ORG-05: Edit organization name** -- Navigate to `/org/settings`. Change org name by clearing the input and typing `E2E Test Org (Renamed)`. Click "Save Changes". Verify toast or success indicator. Verify the name updates (check the input value or header). Change it back to original name to avoid breaking other specs. Save again.

6. **ORG-06: View org member directory** -- Navigate to `/org/members`. Verify member list loads with at least one member visible. Verify display name and email columns exist. Use `page.getByRole("table")` or equivalent list structure to confirm members render.

7. **ORG-07: Verify test campaign deletion via danger zone** -- Navigate into the test campaign created in step 2. Go to settings > danger zone. Delete the campaign using the DestructiveConfirmDialog pattern (type campaign name to confirm). Verify redirect back to org dashboard. Verify the campaign no longer appears. This test doubles as the ORG lifecycle's final assertion -- proving the full create-archive-unarchive-delete lifecycle works end-to-end. Per D-10, this is not "cleanup" but a test of the deletion workflow itself.

SKIP ORG-07 (change member org role) -- this involves logging in as a different user mid-spec which is not possible with pre-authenticated storageState. The RBAC specs already cover org role effects.

SKIP ORG-08 (org switcher) -- requires a second org which does not exist in the seed data and cannot be created via UI alone.

Set `test.setTimeout(120_000)` on the describe block for the full lifecycle.
  </action>
  <verify>
    <automated>cd /home/kwhatcher/projects/civicpulse/run-api/web && npx playwright test org-management --reporter=list 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - web/e2e/org-management.spec.ts exists and contains `test.describe.serial("Org management`
    - File contains `test.setTimeout(120_000)` within the describe block
    - File contains at least 6 `test(` or `test.step(` calls covering ORG-01 through ORG-06
    - File contains campaign creation with `Date.now()` for unique names
    - File contains archive flow with `page.getByRole("menuitem", { name: /archive/i })`
    - File contains unarchive flow
    - File contains org settings edit with name change and revert
    - File contains org member directory navigation to `/org/members`
    - File contains final test that deletes the campaign via danger zone (tests deletion workflow, not cleanup per D-10)
    - File uses `import { test, expect } from "@playwright/test"`
    - File does NOT contain `page.waitForTimeout()` (anti-pattern)
  </acceptance_criteria>
  <done>Org management spec created with 7 serial tests covering dashboard view, campaign creation, archive, unarchive, org name edit, member directory, and campaign deletion as final lifecycle assertion. All lifecycle steps share state via describe-scoped variables.</done>
</task>

<task type="auto">
  <name>Task 2: Create campaign-settings E2E spec</name>
  <files>web/e2e/campaign-settings.spec.ts</files>
  <read_first>
    - web/e2e/org-management.spec.ts (Task 1 output -- follow same serial pattern)
    - docs/testing-plan.md lines 273-339 (CAMP-01 through CAMP-06 test cases)
    - web/src/routes/campaigns/$campaignId/settings/general.tsx (campaign name/description edit)
    - web/src/routes/campaigns/$campaignId/settings/members.tsx (invite, role change, remove)
    - web/src/routes/campaigns/$campaignId/settings/danger.tsx (transfer ownership, delete campaign)
    - web/e2e/connected-journey.spec.ts (campaign creation pattern for throwaway campaign)
  </read_first>
  <action>
Create `web/e2e/campaign-settings.spec.ts` implementing CAMP-01 through CAMP-06 from the testing plan. Use `test.describe.serial` per D-09. Runs as owner (unsuffixed). The spec creates throwaway campaigns for destructive tests (ownership transfer, deletion) per CAMP-05 and CAMP-06 preconditions.

IMPORTANT: Ownership transfer (CAMP-05) and campaign deletion (CAMP-06) each require a separate throwaway campaign because transfer changes who owns the campaign, preventing the original owner from deleting it.

Structure the spec with two serial describe blocks:

**Block A: Settings CRUD + Member Management + Campaign Deletion**

1. **Setup: Create throwaway campaign A** -- Navigate to org dashboard, create `E2E Settings A ${Date.now()}`. Store campaignId from the redirect URL. Store campaign name for deletion confirmation.

2. **CAMP-01: Edit campaign general settings** -- Navigate to `/campaigns/${campaignId}/settings/general`. Change campaign name to `E2E Settings A (Updated)`. Update description field if present. Click Save. Verify toast/success. Verify the new name appears in the sidebar or page heading. Revert name back to original. Save again.

3. **CAMP-02: Invite a new member** -- Navigate to `/campaigns/${campaignId}/settings/members`. Click "Invite" or "Add Member" button. Enter email `viewer2@localhost` (an existing ZITADEL user not yet in this throwaway campaign). Select role "volunteer" from dropdown/combobox. Submit. Verify the member appears in the member list (may show as pending or active). Use `page.waitForResponse()` for the invite API call.

4. **CAMP-03: Change a member's campaign role** -- On the members page, find the member just invited (viewer2@localhost). Change their role from "volunteer" to "manager" via the role dropdown/combobox on their row. Verify the role badge updates. Use `page.waitForResponse()` for the role change API call.

5. **CAMP-04: Remove a member** -- On the members page, find the member (viewer2@localhost). Click remove/delete action. Confirm the removal dialog. Verify the member disappears from the list. Use `page.waitForResponse()` for the remove API call.

6. **CAMP-06: Delete campaign A** -- Navigate to `/campaigns/${campaignId}/settings/danger`. Click "Delete Campaign". Type the campaign name in the DestructiveConfirmDialog confirmation input. Click the delete button. Verify redirect to org dashboard. Verify the campaign no longer appears in the campaign list. Per D-10, this is not "cleanup" but a test of the deletion workflow (CAMP-06).

**Block B: Ownership Transfer**

7. **Setup: Create throwaway campaign B** -- Navigate to org dashboard, create `E2E Settings B ${Date.now()}`. Store campaignId.

8. **CAMP-05: Transfer campaign ownership** -- Navigate to `/campaigns/${campaignId}/settings/danger`. Click "Transfer Ownership" button. In the dialog, select a target user from the dropdown (admin1@localhost or another owner user). Complete the type-to-confirm by typing the campaign name into the confirmation input. Click the confirm/transfer button. Verify success toast. After transfer, the current user (owner1@localhost) should no longer be owner. Verify by checking the danger zone buttons are no longer visible or the page reflects the change. No further action needed on this campaign -- the transferred campaign remains in the system and is not owned by this auth context anymore.

Set `test.setTimeout(120_000)` on each describe block.
  </action>
  <verify>
    <automated>cd /home/kwhatcher/projects/civicpulse/run-api/web && npx playwright test campaign-settings --reporter=list 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - web/e2e/campaign-settings.spec.ts exists and contains `test.describe.serial`
    - File contains at least 2 `test.describe.serial` blocks (Block A: settings+members+delete, Block B: ownership transfer)
    - File contains campaign creation with `Date.now()` for unique names
    - File contains settings edit (CAMP-01) with name change and revert
    - File contains member invite (CAMP-02) with email `viewer2@localhost` or similar test user
    - File contains member role change (CAMP-03) from one role to another
    - File contains member removal (CAMP-04) with confirmation dialog
    - File contains ownership transfer (CAMP-05) on a separate throwaway campaign B with DestructiveConfirmDialog pattern
    - File contains campaign deletion (CAMP-06) on throwaway campaign A as a test assertion (not cleanup per D-10)
    - File uses `import { test, expect } from "@playwright/test"`
    - File does NOT contain `page.waitForTimeout()` (anti-pattern)
    - File does NOT use hardcoded campaign IDs -- all extracted from URLs
  </acceptance_criteria>
  <done>Campaign settings spec created with two serial test blocks. Block A tests settings CRUD, member management, and deletion (CAMP-06) on throwaway campaign A. Block B tests ownership transfer (CAMP-05) on a separate throwaway campaign B. Each destructive action operates on its own campaign to avoid auth context conflicts.</done>
</task>

</tasks>

<verification>
Run both specs together:
```bash
cd web && npx playwright test org-management campaign-settings --reporter=list
```
All tests should pass. Org management creates, archives, unarchives, and deletes a campaign. Campaign settings tests settings edits, member management, ownership transfer, and deletion on separate throwaway campaigns.
</verification>

<success_criteria>
- org-management.spec.ts validates dashboard, campaign lifecycle, org settings, and member directory (ORG-01 through ORG-06)
- org-management.spec.ts final test deletes the campaign as a lifecycle assertion (not cleanup per D-10)
- campaign-settings.spec.ts validates settings CRUD, member invite/role/remove, ownership transfer, and deletion (CAMP-01 through CAMP-06)
- campaign-settings.spec.ts uses two separate throwaway campaigns: one for settings+members+deletion, one for ownership transfer
- Both specs use throwaway campaigns with timestamped names -- no mutation of seed data
- Both specs use serial ordering for lifecycle flows
- No hardcoded campaign IDs
</success_criteria>

<output>
After completion, create `.planning/phases/58-e2e-core-tests/58-02-SUMMARY.md`
</output>
