---
phase: 18-shift-management
plan: "04"
type: execute
wave: 4
depends_on: ["18-01", "18-02", "18-03"]
files_modified: []
autonomous: false
requirements: [SHFT-01, SHFT-02, SHFT-03, SHFT-04, SHFT-05, SHFT-06, SHFT-07, SHFT-08, SHFT-09, SHFT-10]

must_haves:
  truths:
    - "All vitest tests pass (existing + new)"
    - "TypeScript compiles without errors"
    - "Playwright e2e verifies shift list renders and navigation works"
    - "Shift management pages visually verified by user"
  artifacts: []
  key_links: []
---

<objective>
Run the full test suite to verify all shift management code compiles and tests pass, execute Playwright e2e verification, and present visual checkpoint for user approval.

Purpose: Final quality gate for Phase 18 -- the last phase of v1.2 Full UI. Confirms everything works end-to-end before milestone completion.
Output: Verified test results, Playwright e2e confirmation, user visual approval
</objective>

<execution_context>
@/home/kwhatcher/.claude/get-shit-done/workflows/execute-plan.md
@/home/kwhatcher/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/18-shift-management/18-CONTEXT.md
@.planning/phases/18-shift-management/18-RESEARCH.md
@.planning/phases/18-shift-management/18-01-SUMMARY.md
@.planning/phases/18-shift-management/18-02-SUMMARY.md
@.planning/phases/18-shift-management/18-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Full test suite verification and Playwright e2e</name>
  <files></files>
  <action>
1. Run TypeScript compilation check:
   ```
   cd web && npx tsc --noEmit
   ```
   Must exit 0. If errors, fix them before proceeding.

2. Run full vitest suite:
   ```
   cd web && npx vitest run --reporter=verbose
   ```
   All passing tests must remain passing. Todo stubs are acceptable. Any failures from this phase's code must be fixed.

3. Run Playwright e2e test (if web/e2e/shift-verify.spec.ts exists from Wave 0 stubs):
   ```
   cd web && npx playwright test e2e/shift-verify.spec.ts --reporter=list
   ```
   If the e2e spec does not exist, create a minimal smoke test at `web/e2e/shift-verify.spec.ts` that:
   - Navigates to a campaign's volunteer section
   - Clicks "Shifts" in sidebar nav
   - Verifies the shift list page renders (checks for "Shifts" heading or "No shifts yet" empty state)
   - If possible, navigates to shift detail (if shifts exist in seed data)

   Run with: `cd web && npx playwright test e2e/shift-verify.spec.ts --reporter=list`

4. If any test failures from this phase's files: diagnose and fix the issue, then re-run.

5. Document results: test counts (passing, failing, todo), TypeScript compilation status, Playwright results.
  </action>
  <verify>
    <automated>cd /home/kwhatcher/projects/run-api/web && npx tsc --noEmit && npx vitest run --reporter=verbose 2>&1 | tail -5</automated>
  </verify>
  <done>TypeScript compiles clean. All vitest tests pass (no regressions). Playwright e2e smoke test passes.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Visual verification of shift management UI</name>
  <files></files>
  <action>
Present the complete shift management UI for user visual verification. This is the final checkpoint for Phase 18 and the v1.2 Full UI milestone.

What was built:
- Shift list page at /volunteers/shifts/ with date-grouped cards, filters, create/edit dialog
- Shift detail page at /volunteers/shifts/{id}/ with Overview and Roster tabs
- Volunteer signup, check-in/out, assignment, and hours adjustment
- All 13 backend shift endpoints wired to frontend hooks

How to verify:
1. Start the dev server: `cd web && npm run dev`
2. Navigate to a campaign > Volunteers > Shifts in the sidebar
3. Verify the shift list page:
   - Empty state shows "No shifts yet" with Create Shift button
   - Click "Create Shift" -- verify Sheet opens with name, type, date/time, location, capacity fields
   - Create a shift and verify it appears in the list under the correct date group
   - Verify filter dropdowns (Status, Type) are present and functional
4. Click on a shift to view detail:
   - Verify Overview tab shows shift info, status badge, and contextual action buttons
   - Verify Edit button opens the same Sheet pre-filled (only for scheduled shifts)
   - Verify status transition buttons appear correctly
5. Click Roster tab:
   - Verify DataTable structure with Name, Status, Check In, Check Out, Hours, Actions columns
   - Verify "+ Assign Volunteer" button is present for managers
   - If volunteers are assigned, verify check-in/out inline buttons work
6. Test the Sign Up flow on the list page (if logged in as a non-manager volunteer)
7. Overall visual quality: cards look clean, badges are styled, responsive behavior works
  </action>
  <verify>User types "approved" or describes issues</verify>
  <done>User has visually verified the shift management UI and approved it for milestone completion.</done>
</task>

</tasks>

<verification>
- TypeScript: `npx tsc --noEmit` exits 0
- Vitest: `npx vitest run` all pass, no regressions
- Playwright: e2e smoke test passes
- Visual: User confirms shift pages render correctly
</verification>

<success_criteria>
- Zero TypeScript compilation errors
- Zero vitest test regressions (all previously passing tests still pass)
- Playwright e2e smoke test confirms basic navigation and rendering
- User visually approves shift management UI
</success_criteria>

<output>
After completion, create `.planning/phases/18-shift-management/18-04-SUMMARY.md`
</output>
