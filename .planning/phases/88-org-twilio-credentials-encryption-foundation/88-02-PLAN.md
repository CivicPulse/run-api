---
phase: 88-org-twilio-credentials-encryption-foundation
plan: 02
type: execute
wave: 2
depends_on:
  - 88-01
files_modified:
  - app/api/v1/org.py
  - app/services/org.py
  - app/schemas/org.py
  - tests/unit/test_org_api.py
  - tests/unit/test_org_twilio_api_contract.py
autonomous: true
requirements:
  - ORG-01
  - SEC-01
must_haves:
  truths:
    - "Org admins can read Twilio readiness state without seeing raw secrets."
    - "Org owners can update Twilio account settings and rotate only the secret fields they changed."
    - "API responses, validation failures, and route errors never echo submitted Twilio secrets."
  artifacts:
    - path: "app/api/v1/org.py"
      provides: "Secret-safe org read/update endpoints"
    - path: "app/services/org.py"
      provides: "Org-level Twilio config orchestration"
    - path: "app/schemas/org.py"
      provides: "Write-only update models and redacted response models"
    - path: "tests/unit/test_org_api.py"
      provides: "Regression coverage for org role gates and redacted contract"
  key_links:
    - from: "app/api/v1/org.py"
      to: "app/services/org.py"
      via: "GET/PATCH org Twilio delegation"
      pattern: "twilio"
    - from: "app/services/org.py"
      to: "app/services/twilio_config.py"
      via: "encrypt/decrypt/redact helpers"
      pattern: "twilio_config"
---

<objective>
Extend the org service and API contract so Phase 88 exposes org-scoped Twilio status and accepts safe partial updates without widening the permission model or leaking secrets.

Purpose: Fulfill ORG-01 and the request/response parts of SEC-01 using the existing org route patterns and role gates.
Output: Updated org schemas, org service methods, route handling, and backend contract tests for read/write flows.
</objective>

<execution_context>
@/home/kwhatcher/.codex/get-shit-done/workflows/execute-plan.md
@/home/kwhatcher/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/88-org-twilio-credentials-encryption-foundation/88-CONTEXT.md
@app/api/v1/org.py
@app/services/org.py
@app/schemas/org.py
@app/services/twilio_config.py
@tests/unit/test_org_api.py

<interfaces>
From `app/api/v1/org.py`:

```python
@router.get("", response_model=OrgResponse)
async def get_org(...):
    ...

@router.patch("", response_model=OrgResponse)
async def update_org(body: OrgUpdate, ...):
    ...
```

From `app/services/org.py`:

```python
class OrgService:
    async def get_org(self, db: AsyncSession, org_id: uuid.UUID) -> Organization | None
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Define the redacted org Twilio API contract</name>
  <files>app/schemas/org.py, tests/unit/test_org_twilio_api_contract.py</files>
  <behavior>
    - Test 1: `OrgResponse` can include nested Twilio status fields such as configured booleans, masked hints, and timestamps without any plaintext secret field.
    - Test 2: `OrgUpdate` accepts partial Twilio updates so an owner can rotate the auth token without resubmitting the account SID, and vice versa.
    - Test 3: Schema serialization and validation errors do not include submitted auth token values in rendered output.
  </behavior>
  <action>Refactor `app/schemas/org.py` to add dedicated nested Twilio request/response models inside the existing org transport contract, matching the Context instruction to prefer incremental org schema extension while using dedicated write-only models where secret safety requires it. Include fields for account SID, secret presence booleans, masked hints, last-updated timestamps, and readiness state. Mark secret-bearing input fields write-only by behavior and never include them in response models, defaults, or repr output.</action>
  <verify>
    <automated>uv run pytest tests/unit/test_org_twilio_api_contract.py -x</automated>
  </verify>
  <done>The org API contract has explicit, redacted Twilio request/response models that support partial rotation and do not expose raw secrets.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wire org GET/PATCH flows through the Twilio config seam</name>
  <files>app/api/v1/org.py, app/services/org.py, app/schemas/org.py, tests/unit/test_org_api.py</files>
  <behavior>
    - Test 1: `GET /api/v1/org` returns general org metadata plus redacted Twilio status for `org_admin` and `org_owner`.
    - Test 2: `PATCH /api/v1/org` lets `org_owner` update name and/or Twilio config in one request while preserving existing encrypted secrets when omitted.
    - Test 3: `PATCH /api/v1/org` rejects insufficient roles and never returns submitted auth token content in success or error payloads.
  </behavior>
  <action>Extend `app/services/org.py` with org-scoped Twilio read/update helpers that load the organization record, merge partial updates, call `app/services/twilio_config.py` for encryption and redaction, and persist only changed secret fields. Update `app/api/v1/org.py` to keep the existing role model intact: GET remains readable by `org_admin`, PATCH remains owner-only. Preserve the current route shape unless a narrowly scoped nested payload is needed for secret safety. Add focused route tests around permission gates, partial secret rotation, and secret-safe responses; reuse the existing org API mocking style instead of inventing new harnesses.</action>
  <verify>
    <automated>uv run pytest tests/unit/test_org_api.py tests/unit/test_org_twilio_api_contract.py -x</automated>
  </verify>
  <done>Existing org endpoints now serve redacted Twilio state and support owner-only partial secret updates without exposing plaintext values anywhere in the route contract.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser/client→org API | Untrusted credential input crosses into authenticated backend routes |
| route layer→service layer | Secret-bearing request data is handed off for persistence |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-88-04 | E | `app/api/v1/org.py` | mitigate | Keep `GET /api/v1/org` at `org_admin` and `PATCH /api/v1/org` at `org_owner`, reusing existing `require_org_role` enforcement instead of adding a parallel permission path. |
| T-88-05 | I | `app/schemas/org.py` / `app/api/v1/org.py` | mitigate | Use response models that expose only booleans, timestamps, and masked hints; never serialize auth token fields back to the client on success or failure. |
| T-88-06 | R | `app/services/org.py` | mitigate | Store `updated_at` / secret-updated metadata on the organization record so later auditing can tell when config changed without logging plaintext values. |
</threat_model>

<verification>
Run the focused backend test suite covering org API behavior and confirm the GET/PATCH payloads expose only redacted Twilio state while partial secret rotation keeps existing encrypted values when omitted.
</verification>

<success_criteria>
Phase 88 backend contract is complete when:
- `GET /api/v1/org` exposes redacted Twilio readiness state for org admins
- `PATCH /api/v1/org` supports owner-only partial Twilio updates
- no backend route path or schema returns raw auth-token values
</success_criteria>

<output>
After completion, create `.planning/phases/88-org-twilio-credentials-encryption-foundation/88-02-SUMMARY.md`
</output>
