---
phase: 88-org-twilio-credentials-encryption-foundation
plan: 03
type: execute
wave: 3
depends_on:
  - 88-02
files_modified:
  - web/src/types/org.ts
  - web/src/hooks/useOrg.ts
  - web/src/hooks/useOrg.test.ts
  - web/src/routes/org/settings.tsx
  - web/src/routes/org/settings.test.tsx
autonomous: true
requirements:
  - ORG-01
must_haves:
  truths:
    - "The existing `/org/settings` page shows Twilio readiness and masked credential status alongside general org settings."
    - "Org admins can inspect non-secret Twilio status, while only org owners can edit and save Twilio credentials."
    - "The browser never receives previously saved secret values after a successful save or page refresh."
  artifacts:
    - path: "web/src/types/org.ts"
      provides: "Frontend org/Twilio contract types"
    - path: "web/src/hooks/useOrg.ts"
      provides: "Org detail and update hooks for the Twilio settings surface"
    - path: "web/src/routes/org/settings.tsx"
      provides: "Integrated org settings page with Twilio card"
    - path: "web/src/routes/org/settings.test.tsx"
      provides: "UI regression coverage for owner/admin behavior and secret-safe rendering"
  key_links:
    - from: "web/src/hooks/useOrg.ts"
      to: "api/v1/org"
      via: "React Query org detail/update hooks"
      pattern: "api/v1/org"
    - from: "web/src/routes/org/settings.tsx"
      to: "web/src/hooks/useOrg.ts"
      via: "settings form state and save mutation"
      pattern: "useOrg"
---

<objective>
Extend the existing org settings page with a Twilio configuration card that consumes the new secret-safe contract and preserves the current owner/admin behavior.

Purpose: Deliver the first user-facing Twilio settings surface without creating a second admin area or letting saved secrets leak back into UI state.
Output: Frontend org types, org hooks, settings page updates, and focused hook/UI tests.
</objective>

<execution_context>
@/home/kwhatcher/.codex/get-shit-done/workflows/execute-plan.md
@/home/kwhatcher/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/88-org-twilio-credentials-encryption-foundation/88-CONTEXT.md
@web/src/types/org.ts
@web/src/hooks/useOrg.ts
@web/src/routes/org/settings.tsx
@web/src/hooks/useOrgPermissions.ts
@web/src/hooks/useOrg.test.ts

<interfaces>
From `web/src/types/org.ts`:

```ts
export interface UserOrg {
  id: string
  name: string
  zitadel_org_id: string
  role: string
}
```

From `web/src/hooks/useOrg.ts`:

```ts
export function useUpdateOrg() {
  return useMutation({
    mutationFn: (data: { name: string }) =>
      api.patch("api/v1/org", { json: data }).json(),
  })
}
```

From `web/src/hooks/useOrgPermissions.ts`:

```ts
const ORG_ROLE_LEVELS: Record<OrgRole, number> = {
  org_admin: 0,
  org_owner: 1,
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend frontend org types and hooks for Twilio settings</name>
  <files>web/src/types/org.ts, web/src/hooks/useOrg.ts, web/src/hooks/useOrg.test.ts</files>
  <behavior>
    - Test 1: Org detail hooks deserialize the nested Twilio status shape returned by `GET /api/v1/org`.
    - Test 2: Org update hooks send partial Twilio payloads without requiring unchanged secret fields.
    - Test 3: Mutation invalidation refreshes the org query after save so the UI redraws from redacted server state, not cached plaintext input.
  </behavior>
  <action>Refactor `web/src/hooks/useOrg.ts` to own the org detail read and update contract for `/org/settings` instead of leaving that route on an ad hoc inline query. Extend `web/src/types/org.ts` with the nested Twilio status/update types that match the backend contract. Update the existing hook tests to cover the new query shape, partial mutation payloads, and invalidation behavior. Keep the transport incremental and org-centered per the Context decisions; do not introduce a standalone Twilio-only client module unless the existing hook becomes unworkable.</action>
  <verify>
    <automated>npm --prefix web run test -- web/src/hooks/useOrg.test.ts</automated>
  </verify>
  <done>Frontend org hooks and types fully represent the redacted Twilio org contract and refresh cleanly after saves.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add the Twilio card to `/org/settings` with owner/admin gating</name>
  <files>web/src/routes/org/settings.tsx, web/src/routes/org/settings.test.tsx</files>
  <behavior>
    - Test 1: Org admins can view readiness state, masked hints, and timestamps but cannot edit write-only fields.
    - Test 2: Org owners can edit Account SID and secret rotation inputs, submit partial updates, and see save success/error states.
    - Test 3: Previously saved secret values are never rendered back into the form after load or after a successful save.
  </behavior>
  <action>Extend the existing `/org/settings` route instead of creating a parallel admin page. Keep General and Twilio settings on the same screen, with Twilio clearly grouped in its own card. Reuse the existing owner-only mutation UX pattern, but let org admins read the non-secret status card. Use inline descriptions to clarify which fields are write-only and what readiness/masked values mean. Preserve the established card-and-form layout style; avoid modal-heavy flows or any UI that implies number provisioning, webhook setup, SMS sending, or voice actions, since those are deferred to later phases.</action>
  <verify>
    <automated>npm --prefix web run test -- web/src/routes/org/settings.test.tsx web/src/hooks/useOrg.test.ts</automated>
  </verify>
  <done>The org settings page shows a Twilio configuration card with redacted status for admins, editable controls for owners, and no browser-side replay of stored secrets.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| backend API→browser state | Redacted org Twilio status enters client memory here |
| browser form state→mutation request | User-entered secret rotations are staged locally before submission |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-88-07 | I | `web/src/routes/org/settings.tsx` | mitigate | Treat auth token fields as blank write-only inputs on every render and redraw from server redacted status after save instead of reusing previously typed secret values. |
| T-88-08 | E | `web/src/routes/org/settings.tsx` / `web/src/hooks/useOrgPermissions.ts` | mitigate | Gate edit controls on `org_owner` while still allowing `org_admin` visibility of non-secret status, matching the backend role contract. |
| T-88-09 | T | `web/src/hooks/useOrg.ts` | mitigate | Send only changed Twilio fields in the mutation payload so omitted secrets do not overwrite encrypted values with empty strings. |
</threat_model>

<verification>
Run the focused frontend hook and route tests, then manually inspect the route component to confirm it uses the redacted server response rather than populating saved secret values into form fields.
</verification>

<success_criteria>
Phase 88 UI is complete when:
- `/org/settings` includes a Twilio card on the existing org settings route
- org admins can see status but only owners can save changes
- the UI never receives or re-renders persisted auth-token plaintext
</success_criteria>

<output>
After completion, create `.planning/phases/88-org-twilio-credentials-encryption-foundation/88-03-SUMMARY.md`
</output>
