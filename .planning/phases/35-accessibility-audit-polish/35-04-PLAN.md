---
phase: 35-accessibility-audit-polish
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - web/src/components/field/CanvassingCompletionSummary.tsx
  - web/src/routes/field/$campaignId/canvassing.tsx
autonomous: true
requirements:
  - A11Y-01
  - A11Y-02
  - A11Y-03
  - POLISH-01
  - POLISH-02
  - POLISH-03
gap_closure: true

must_haves:
  truths:
    - "Vite build completes without TypeScript errors introduced by phase 35"
    - "CanvassingCompletionSummary Link uses typed route pattern with params object"
    - "canvassing.tsx has no unused imports from phase 35 changes"
  artifacts:
    - path: "web/src/components/field/CanvassingCompletionSummary.tsx"
      provides: "Type-safe Link navigation back to field hub"
      contains: 'to="/field/$campaignId"'
    - path: "web/src/routes/field/$campaignId/canvassing.tsx"
      provides: "Canvassing wizard route without unused imports"
  key_links:
    - from: "web/src/components/field/CanvassingCompletionSummary.tsx"
      to: "/field/$campaignId route"
      via: "TanStack Router typed Link with params"
      pattern: 'to="/field/\\$campaignId".*params=.*campaignId'
---

<objective>
Fix two TypeScript errors introduced by phase 35 that prevent a clean Vite build.

Purpose: Close verification gaps from 35-VERIFICATION.md so the phase can be marked complete with a passing build.
Output: Two files fixed, `npx tsc -p tsconfig.app.json --noEmit` passes for the phase-35-introduced errors.
</objective>

<execution_context>
@/home/kwhatcher/.claude/get-shit-done/workflows/execute-plan.md
@/home/kwhatcher/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/35-accessibility-audit-polish/35-VERIFICATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix CanvassingCompletionSummary Link type error and remove unused import in canvassing.tsx</name>
  <files>web/src/components/field/CanvassingCompletionSummary.tsx, web/src/routes/field/$campaignId/canvassing.tsx</files>
  <read_first>
    web/src/components/field/CanvassingCompletionSummary.tsx
    web/src/routes/field/$campaignId/canvassing.tsx
  </read_first>
  <action>
    **Fix 1 — CanvassingCompletionSummary.tsx line 40:**
    Change the Link from template-literal path to TanStack Router typed route pattern.

    Before: `<Link to={`/field/${campaignId}`}>Back to Hub</Link>`
    After:  `<Link to="/field/$campaignId" params={{ campaignId }}>Back to Hub</Link>`

    This matches the established pattern used throughout the admin routes (e.g., `to="/campaigns/$campaignId/dashboard" params={{ campaignId: campaign.id }}`).

    **Fix 2 — canvassing.tsx line 23:**
    Remove `CheckCircle2` from the lucide-react import. The old inline completion card that used CheckCircle2 was replaced by the CanvassingCompletionSummary component.

    Before: `import { Loader2, AlertCircle, CheckCircle2, List } from "lucide-react"`
    After:  `import { Loader2, AlertCircle, List } from "lucide-react"`

    Do NOT fix pre-existing errors in other files (CompletionSummary.tsx, FieldHeader.tsx, AssignmentCard.tsx, etc.).
  </action>
  <verify>
    <automated>cd /home/kwhatcher/projects/run-api/web && npx tsc -p tsconfig.app.json --noEmit 2>&1 | grep -E "(CanvassingCompletionSummary|canvassing\.tsx.*(CheckCircle2|TS6133))" | wc -l</automated>
    Expected: 0 (no errors from these two files for the phase-35-introduced issues)
  </verify>
  <acceptance_criteria>
    - `grep 'to="/field/\$campaignId"' web/src/components/field/CanvassingCompletionSummary.tsx` returns a match
    - `grep 'params={{ campaignId }}' web/src/components/field/CanvassingCompletionSummary.tsx` returns a match
    - `grep 'CheckCircle2' web/src/routes/field/\$campaignId/canvassing.tsx` returns NO match
    - `grep 'Loader2, AlertCircle, List' web/src/routes/field/\$campaignId/canvassing.tsx` returns a match
    - CanvassingCompletionSummary.tsx still imports CheckCircle2 (it uses the icon in its own render)
  </acceptance_criteria>
  <done>
    CanvassingCompletionSummary.tsx Link uses typed route pattern `to="/field/$campaignId" params={{ campaignId }}`. canvassing.tsx no longer imports unused CheckCircle2. Both phase-35-introduced TypeScript errors are resolved.
  </done>
</task>

</tasks>

<verification>
- `cd web && npx tsc -p tsconfig.app.json --noEmit 2>&1 | grep -c "CanvassingCompletionSummary"` returns 0
- `cd web && npx tsc -p tsconfig.app.json --noEmit 2>&1 | grep -c "CheckCircle2"` returns 0
- No new errors introduced by these changes
</verification>

<success_criteria>
The two TypeScript errors introduced by phase 35 (TS2322 in CanvassingCompletionSummary.tsx, TS6133 in canvassing.tsx) are resolved. The fixes are minimal and do not touch pre-existing errors in other files.
</success_criteria>

<output>
After completion, create `.planning/phases/35-accessibility-audit-polish/35-04-SUMMARY.md`
</output>
