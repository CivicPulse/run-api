---
phase: 59-e2e-advanced-tests
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - web/e2e/call-lists-dnc.spec.ts
  - web/e2e/phone-banking.spec.ts
autonomous: true
requirements: [E2E-14, E2E-15]

must_haves:
  truths:
    - "Call lists/DNC spec exercises call list CRUD, DNC add/bulk/delete, and DNC enforcement in new call lists"
    - "Phone banking spec exercises session CRUD, caller assignment/removal, active calling with outcome recording, and progress tracking"
  artifacts:
    - path: "web/e2e/call-lists-dnc.spec.ts"
      provides: "Call list CRUD and DNC management with enforcement verification"
      contains: "CL-01"
    - path: "web/e2e/phone-banking.spec.ts"
      provides: "Phone banking session lifecycle with active calling flow"
      contains: "PB-01"
  key_links:
    - from: "web/e2e/phone-banking.spec.ts"
      to: "/api/v1/campaigns/{cid}/phone-banks"
      via: "createSessionViaApi for bulk session creation"
      pattern: "page.request.post.*phone-banks"
    - from: "web/e2e/call-lists-dnc.spec.ts"
      to: "/api/v1/campaigns/{cid}/dnc"
      via: "DNC API for add/bulk/check operations"
      pattern: "page.request.post.*dnc"
---

<objective>
Write the call lists/DNC E2E spec (E2E-14: CL-01 through CL-05, DNC-01 through DNC-06) and phone banking sessions E2E spec (E2E-15: PB-01 through PB-10).

Purpose: Validate call list lifecycle, DNC management with enforcement, and phone banking sessions with active calling flow.
Output: 2 new spec files for phone operations domain.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/59-e2e-advanced-tests/59-CONTEXT.md
@.planning/phases/59-e2e-advanced-tests/59-RESEARCH.md
@docs/testing-plan.md (Section 17: CL-01 to CL-05 lines 959-998; Section 18: DNC-01 to DNC-06 lines 1001-1044; Section 19: PB-01 to PB-10 lines 1047-1143)
@web/e2e/voter-crud.spec.ts (pattern reference)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write call lists and DNC E2E spec (E2E-14: CL-01 to CL-05, DNC-01 to DNC-06)</name>
  <files>web/e2e/call-lists-dnc.spec.ts</files>
  <read_first>
    - web/e2e/voter-crud.spec.ts (navigateToSeedCampaign, API helper pattern with cookie forwarding)
    - web/src/routes/campaigns/$campaignId/phone-banking/call-lists/index.tsx (call lists page, if exists)
    - web/src/routes/campaigns/$campaignId/phone-banking/dnc/index.tsx (DNC page, if exists)
    - web/src/routes/campaigns/$campaignId/phone-banking/index.tsx (phone banking root page)
    - docs/testing-plan.md (lines 959-1044: CL-01 through CL-05 and DNC-01 through DNC-06)
  </read_first>
  <action>
Create `web/e2e/call-lists-dnc.spec.ts` — validates call list CRUD and DNC management lifecycle.

**Structure:**
```typescript
import { test, expect } from "@playwright/test"

/**
 * Call Lists & DNC E2E Spec
 *
 * Validates call list CRUD (create, view, edit, delete) and DNC management
 * (add single, bulk import, enforcement, delete, search).
 *
 * Covers: CL-01 through CL-05, DNC-01 through DNC-06 (E2E-14)
 * Runs as: owner (unsuffixed spec -> chromium -> owner auth)
 */

// ── Helpers ──

async function navigateToSeedCampaign(page): Promise<string> { ... }

async function createCallListViaApi(page, campaignId, name): Promise<string> {
  // Cookie-forwarding POST to /call-lists with { name }
}

async function addDncViaApi(page, campaignId, phone, reason): Promise<string> {
  // Cookie-forwarding POST to /dnc with { phone_number, reason }
}

// ── Tests ──

test.describe.serial("Call Lists & DNC", () => {
  let campaignId = ""
  let callListId = ""
  const dncEntryIds: string[] = []
  test.setTimeout(120_000)

  test("Setup: navigate to seed campaign", ...)

  // ── Call List CRUD ──

  test("CL-01: Create a call list via UI", async ({ page }) => {
    // Navigate to Phone Banking > Call Lists
    // Click "New Call List"
    // Enter name "E2E Call List 1"
    // Select voter source (filter criteria or voter list)
    // Save
    // Verify call list appears in list with entry count
    // Store callListId
  })

  test("CL-02: View call list details", async ({ page }) => {
    // Click on "E2E Call List 1"
    // Verify detail page shows:
    // - List of entries (voter names, phone numbers)
    // - Status tabs or filters (pending, claimed, completed)
    // - Entry count
  })

  test("CL-03: Edit a call list name", async ({ page }) => {
    // Edit name to "E2E Call List 1 (Updated)"
    // Verify name update persists
  })

  test("CL-04: Delete a call list", async ({ page }) => {
    // Create throwaway call list "E2E Call List — Delete Me" via API
    // Delete via UI row action
    // Verify removed from list
  })

  test("CL-05: Create call list for further testing", async ({ page }) => {
    // Create "E2E Active Call List" with sufficient voters
    // This call list will be referenced by phone banking tests
    // Store ID
  })

  // ── DNC Management ──

  test("DNC-01: Add a single phone to DNC", async ({ page }) => {
    // Navigate to Phone Banking > DNC
    // Click "Add Number"
    // Enter phone number (e.g., "4785550001") and reason "Requested removal"
    // Save
    // Verify number appears in DNC list
  })

  test("DNC-02: Bulk add to DNC", async ({ page }) => {
    // Click "Bulk Import" or "Import DNC"
    // Create a small CSV in memory or use setInputFiles with inline content
    // Upload CSV with 5 phone numbers and reasons
    // Verify all 5 appear in DNC list
    // Alternatively, use API bulk endpoint for 5 numbers
  })

  test("DNC-03: Add voter phone numbers to DNC", async ({ page }) => {
    // Find 5 seed voters who have phone numbers
    // Add their phone numbers to DNC via API for speed
    // Verify they appear in DNC list
  })

  test("DNC-04: Verify DNC enforcement in call lists", async ({ page }) => {
    // Create a new call list that would include the DNC'd voters
    // Verify DNC'd phone numbers do NOT appear in call list entries
    // This validates the DNC filtering enforcement
  })

  test("DNC-05: Delete DNC entries", async ({ page }) => {
    // Delete 3 of the 5 DNC entries from DNC-03
    // Verify removed from DNC list
    // Create a new call list
    // Verify the 3 removed numbers NOW appear (no longer blocked)
  })

  test("DNC-06: Search DNC list", async ({ page }) => {
    // Type a phone number in DNC search field
    // Verify list filters to show matching entries
  })
})
```

Key notes:
- Per D-16: Create first call list via UI (CL-01), additional via API for speed (CL-04, CL-05)
- DNC bulk import (DNC-02) can use API bulk endpoint or CSV upload
- DNC enforcement (DNC-04) is the critical test — verifies DNC'd numbers are excluded from new call lists
- DNC-05 validates the reverse: removing DNC entries re-enables those numbers
  </action>
  <verify>
    <automated>test -f web/e2e/call-lists-dnc.spec.ts && grep -q "CL-01" web/e2e/call-lists-dnc.spec.ts && grep -q "CL-05" web/e2e/call-lists-dnc.spec.ts && grep -q "DNC-01" web/e2e/call-lists-dnc.spec.ts && grep -q "DNC-06" web/e2e/call-lists-dnc.spec.ts && grep -q "describe.serial" web/e2e/call-lists-dnc.spec.ts && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - web/e2e/call-lists-dnc.spec.ts exists
    - Contains `test.describe.serial` block
    - Contains test cases for CL-01 through CL-05 and DNC-01 through DNC-06 (11 test cases + 1 setup)
    - Contains `navigateToSeedCampaign` helper
    - Contains `createCallListViaApi` and/or `addDncViaApi` helpers
    - CL-01 creates a call list via UI form
    - CL-02 verifies call list detail page (entries, status, count)
    - DNC-01 adds a single phone number via UI
    - DNC-02 tests bulk DNC import
    - DNC-04 verifies DNC enforcement (blocked numbers excluded from call lists)
    - DNC-05 verifies DNC removal re-enables numbers
    - DNC-06 tests DNC search
    - File contains JSDoc header with "Covers: CL-01 through CL-05, DNC-01 through DNC-06 (E2E-14)"
  </acceptance_criteria>
  <done>Call lists/DNC spec covers full lifecycle: call list CRUD, DNC add/bulk/enforcement/delete/search</done>
</task>

<task type="auto">
  <name>Task 2: Write phone banking sessions E2E spec (E2E-15: PB-01 through PB-10)</name>
  <files>web/e2e/phone-banking.spec.ts</files>
  <read_first>
    - web/e2e/voter-crud.spec.ts (navigateToSeedCampaign, API helper pattern)
    - web/src/routes/campaigns/$campaignId/phone-banking/sessions/index.tsx (sessions list page, if exists)
    - web/src/routes/campaigns/$campaignId/phone-banking/sessions/$sessionId/index.tsx (session detail page)
    - web/src/routes/campaigns/$campaignId/phone-banking/sessions/$sessionId/call.tsx (active calling screen)
    - docs/testing-plan.md (lines 1047-1143: PB-01 through PB-10 test definitions)
  </read_first>
  <action>
Create `web/e2e/phone-banking.spec.ts` — validates phone banking session lifecycle with active calling flow.

This spec creates its own call list via API for session creation, making it self-contained.

**Structure:**
```typescript
import { test, expect } from "@playwright/test"

/**
 * Phone Banking E2E Spec
 *
 * Validates session CRUD, caller assignment, active calling with outcome
 * recording, skip functionality, progress tracking, and session management.
 *
 * Covers: PB-01 through PB-10 (E2E-15)
 * Runs as: owner (unsuffixed spec -> chromium -> owner auth)
 */

// ── Helpers ──

async function navigateToSeedCampaign(page): Promise<string> { ... }

async function createCallListViaApi(page, campaignId, name): Promise<string> {
  // Creates a call list via API
}

async function createSessionViaApi(page, campaignId, name, callListId): Promise<string> {
  // Cookie-forwarding POST to /phone-banks with { name, call_list_id }
}

// ── Tests ──

test.describe.serial("Phone Banking Sessions", () => {
  let campaignId = ""
  let callListId = ""
  let sessionId = ""
  const sessionIds: string[] = []
  test.setTimeout(180_000) // 10 test cases

  test("Setup: create call list for sessions", async ({ page }) => {
    campaignId = await navigateToSeedCampaign(page)
    // Create a call list via API for session creation
    callListId = await createCallListViaApi(page, campaignId, "E2E PB Call List")
  })

  test("PB-01: Create a phone banking session via UI", async ({ page }) => {
    // Navigate to Phone Banking > Sessions
    // Click "New Session"
    // Enter name "E2E Session 1"
    // Select the call list "E2E PB Call List"
    // Optionally select a survey script (if seed surveys exist)
    // Save
    // Verify session appears in sessions list
    // Store sessionId
  })

  test("PB-02: Assign callers to session", async ({ page }) => {
    // Navigate to session detail page for "E2E Session 1"
    // Click "Add Caller" (Popover+Command member picker)
    // Search for and select a campaign member (use seed users)
    // Add 2-3 callers
    // Verify all appear in callers section
  })

  test("PB-03: Remove a caller", async ({ page }) => {
    // Remove one of the assigned callers
    // Verify they are no longer listed
  })

  test("PB-04: Create 10 sessions via API for bulk testing", async ({ page }) => {
    // Per D-09: Create sessions via API for speed
    // Per D-16: PB-01 already created 1 via UI, create remaining 9 via API
    // Create "E2E Session 2" through "E2E Session 10"
    // Verify they appear in the sessions list (navigate and count)
    // Total: 10 sessions (1 UI + 9 API)
    for (let i = 2; i <= 10; i++) {
      const id = await createSessionViaApi(page, campaignId, `E2E Session ${i}`, callListId)
      sessionIds.push(id)
    }
    // Navigate to sessions list and verify count
  })

  test("PB-05: Verify caller access filtering", async ({ page }) => {
    // NOTE: Testing this fully would require logging in as different users
    // Since this spec runs as owner, verify the sessions list shows all sessions
    // The full cross-role test is best handled by role-specific specs
    // For this spec: verify session list shows correct session count
  })

  test("PB-06: Active calling — claim and record a call", async ({ page }) => {
    // Navigate to Session 1 detail page
    // Click "Start Calling" (navigate to call screen)
    // Verify calling screen shows:
    //   - Voter name and phone number for the claimed call
    //   - Outcome buttons (Contact Made, No Contact, etc.)
    //   - Survey questions if script attached
    // Select an outcome (e.g., "Supporter" or "Contact Made")
    // If survey present, fill in answers
    // Submit outcome
    // Verify screen advances to next call (auto-advance)
    //
    // NOTE: No real telephony. Test the UI flow of viewing call info,
    // selecting outcome, and submission per research anti-patterns.
  })

  test("PB-07: Skip a call", async ({ page }) => {
    // On calling screen, click "Skip"
    // Verify screen advances to next call without recording outcome
  })

  test("PB-08: Session progress tracking", async ({ page }) => {
    // After recording outcomes for several calls, verify:
    // - Progress indicator on call screen updates (e.g., "3/50 calls completed")
    // - Navigate back to session detail, verify progress reflected there
  })

  test("PB-09: Edit a session", async ({ page }) => {
    // Edit "E2E Session 1" — change name to "E2E Session 1 (Updated)"
    // Verify name update persists
  })

  test("PB-10: Delete a session", async ({ page }) => {
    // Create throwaway session via API
    // Delete via UI
    // Verify removal from sessions list
  })
})
```

Key notes:
- Self-contained: creates its own call list in Setup
- Per D-16: Create first session via UI (PB-01), remaining 9 via API (PB-04)
- Per D-09: 10 sessions total as per testing plan
- PB-06 (active calling) tests the UI flow without real telephony per research anti-patterns
- PB-05 (caller access) is limited since spec runs as owner; full cross-role testing is Phase 60 territory
- Session creation requires a call_list_id, so call list must be created first
  </action>
  <verify>
    <automated>test -f web/e2e/phone-banking.spec.ts && grep -q "PB-01" web/e2e/phone-banking.spec.ts && grep -q "PB-06" web/e2e/phone-banking.spec.ts && grep -q "PB-10" web/e2e/phone-banking.spec.ts && grep -q "createSessionViaApi\|createCallListViaApi" web/e2e/phone-banking.spec.ts && grep -q "describe.serial" web/e2e/phone-banking.spec.ts && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - web/e2e/phone-banking.spec.ts exists
    - Contains `test.describe.serial` block
    - Contains test cases for PB-01 through PB-10 (10 test cases + 1 setup)
    - Contains `createCallListViaApi` helper (creates its own call list)
    - Contains `createSessionViaApi` helper
    - PB-01 creates a session via UI form
    - PB-02 assigns callers via member picker
    - PB-04 bulk-creates sessions via API (9 sessions to total 10 per D-09)
    - PB-06 tests active calling flow (view voter info, select outcome, submit)
    - PB-07 tests call skip functionality
    - PB-08 verifies progress tracking
    - PB-10 deletes a session via UI
    - No real telephony automation per research anti-patterns
    - File contains JSDoc header with "Covers: PB-01 through PB-10 (E2E-15)"
  </acceptance_criteria>
  <done>Phone banking spec covers full session lifecycle: creation, caller management, active calling flow, progress tracking, edit, and delete</done>
</task>

</tasks>

<verification>
1. `ls web/e2e/call-lists-dnc.spec.ts web/e2e/phone-banking.spec.ts` — both exist
2. `grep -c "test(" web/e2e/call-lists-dnc.spec.ts` shows 12+ test cases
3. `grep -c "test(" web/e2e/phone-banking.spec.ts` shows 11+ test cases
4. Both specs are self-contained (no imports from other spec files)
</verification>

<success_criteria>
- Call lists/DNC spec covers CL-01 to CL-05 and DNC-01 to DNC-06
- DNC enforcement is tested (DNC-04)
- Phone banking spec covers PB-01 to PB-10 with 10 sessions per D-09
- Active calling UI flow is tested without real telephony
- Both specs are self-contained per D-14
</success_criteria>

<output>
After completion, create `.planning/phases/59-e2e-advanced-tests/59-04-SUMMARY.md`
</output>
