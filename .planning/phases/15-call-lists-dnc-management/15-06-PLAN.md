---
phase: 15-call-lists-dnc-management
plan: 06
type: execute
wave: 3
depends_on: [15-03, 15-04, 15-05]
files_modified: []
autonomous: false
requirements: [CALL-01, CALL-02, CALL-03, CALL-04, CALL-05, CALL-06, CALL-07, CALL-08]

must_haves:
  truths:
    - "Phone Banking sidebar section is visible in campaign nav with Call Lists and DNC List sub-items"
    - "User can create a call list, see it in the table, click into detail, and see entries"
    - "User can add, import, and remove DNC entries"
    - "Entry status labels in the detail view show UI vocabulary (Unclaimed, Claimed, etc.)"
    - "DNC search filters the table without a page reload"
  artifacts: []
  key_links: []
---

<objective>
Human visual verification of all Phase 15 functionality before marking the phase complete.

Purpose: Automated tests cover hook correctness. This checkpoint verifies the full interactive experience — navigation, form flows, table rendering, and status label mapping — which cannot be fully tested with unit tests.

Output: Human sign-off or issue list to address.
</objective>

<execution_context>
@/home/kwhatcher/.claude/get-shit-done/workflows/execute-plan.md
@/home/kwhatcher/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run full test suite and TypeScript check</name>
  <files></files>
  <action>
    Run the full test suite and TypeScript compilation check before the visual checkpoint.

    ```bash
    cd /home/kwhatcher/projects/run-api/web && npm run test -- --run --coverage
    cd /home/kwhatcher/projects/run-api/web && npx tsc --noEmit
    ```

    If any tests fail, investigate and fix before proceeding to the visual checkpoint.
    If TypeScript errors exist that are NEW (introduced in Phase 15), fix them.
    Pre-existing TypeScript errors from other phases may be noted but do not block.
  </action>
  <verify>
    <automated>cd /home/kwhatcher/projects/run-api/web && npm run test -- --run 2>&1 | tail -10</automated>
  </verify>
  <done>Test suite exits 0, no new TypeScript errors from Phase 15 files</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Complete Phase 15 implementation:
    - Phone Banking sidebar layout (phone-banking.tsx → layout with Call Lists + DNC List sub-nav)
    - Call Lists index (DataTable, create dialog with advanced settings toggle, edit dialog, delete confirmation)
    - Call list detail (stats row chips, entries DataTable with status filter tabs, voter name links)
    - DNC list page (search input, Add Number dialog, Import from file dialog, Remove actions)
    - Backend: CallListUpdate schema, updated PATCH handler, GET /entries endpoint with voter name join
  </what-built>
  <how-to-verify>
    Start the dev server: `cd /home/kwhatcher/projects/run-api && docker compose up -d && cd web && npm run dev`

    Navigate to a campaign and verify:

    **Navigation:**
    1. Confirm "Phone Banking" appears in the campaign sidebar after Voters
    2. Click "Phone Banking" → redirects to /phone-banking/call-lists automatically
    3. Confirm sub-nav shows "Call Lists" and "DNC List" items
    4. Clicking each nav item navigates correctly

    **Call Lists (CALL-01, CALL-03):**
    5. Click "New Call List" → dialog opens with Name field, Voter list dropdown, and collapsed "Advanced settings"
    6. Click "Advanced settings" → expands to show max_attempts, claim timeout, cooldown fields
    7. Fill in name + select a voter list → click Create → call list appears in table
    8. Click kebab menu → Edit → dialog opens pre-populated with current values
    9. Change the name → Save → table updates
    10. Click kebab menu → Delete → ConfirmDialog appears (NOT type-to-confirm input)
    11. Confirm delete → list removed from table

    **Call List Detail (CALL-02):**
    12. Click a call list name → navigates to detail page (full page navigation, not a sheet)
    13. Confirm stats row shows Total, Unclaimed, Claimed, Completed, Skipped chips
    14. Entries DataTable shows: Voter Name (linked), Phone, Status (UI label), Assigned Caller
    15. Confirm status labels are "Unclaimed/Claimed/Completed/Skipped" — NOT "available/in_progress/etc"
    16. Click a voter name → navigates to /voters/{id} (voter detail)
    17. Click status filter tabs → table updates to show only entries with that status

    **DNC List (CALL-04, CALL-05, CALL-06, CALL-07, CALL-08):**
    18. Click "DNC List" in sidebar → DNC list page loads
    19. Click "Add Number" → small dialog with phone number input and optional reason
    20. Add a number → appears in DNC table
    21. Type in search box → table filters client-side (no network request for existing entries)
    22. Click "Import from file" → dialog with file drop zone/picker opens
    23. Upload a CSV with phone numbers → toast shows "Imported X numbers" with skip/invalid counts
    24. DNC table auto-refreshes after import showing new entries
    25. Click "Remove" on a row → number disappears from table (no type-to-confirm needed)
  </how-to-verify>
  <resume-signal>Type "approved" if all checks pass, or describe any issues found</resume-signal>
</task>

</tasks>

<verification>
All 8 requirements satisfied:
- CALL-01: Create call list from voter universe with automatic DNC filtering
- CALL-02: Call list detail with entry statuses
- CALL-03: Edit and delete call lists
- CALL-04: View DNC list
- CALL-05: Add individual phone number to DNC
- CALL-06: Bulk import DNC numbers from file
- CALL-07: Remove number from DNC list
- CALL-08: Check if phone number is on DNC (client-side search)
</verification>

<success_criteria>
Human verifier types "approved" after confirming all navigation, form flows, table displays, and status label mappings work correctly.
</success_criteria>

<output>
After completion, create `.planning/phases/15-call-lists-dnc-management/15-06-SUMMARY.md`
</output>
