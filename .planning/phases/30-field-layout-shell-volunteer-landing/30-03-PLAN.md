---
phase: 30-field-layout-shell-volunteer-landing
plan: 03
type: execute
wave: 2
depends_on:
  - 30-01
  - 30-02
files_modified:
  - web/src/routes/field/$campaignId/index.tsx
  - web/src/components/field/AssignmentCard.tsx
  - web/src/components/field/AssignmentCardSkeleton.tsx
  - web/src/components/field/FieldEmptyState.tsx
  - web/src/routes/callback.tsx
autonomous: true
requirements:
  - NAV-02

must_haves:
  truths:
    - "Volunteer sees personalized greeting ('Hey Sarah!') and assignment cards on the landing hub"
    - "Assignment card shows type icon, name, progress count, progress bar, and 'Tap to start' CTA"
    - "Volunteer with both assignments sees two stacked cards"
    - "Volunteer with no assignments sees friendly empty state message"
    - "Loading state shows skeleton card placeholders"
    - "Error state shows retry button"
    - "Volunteer-only users auto-redirect to /field/{campaignId} on login"
  artifacts:
    - path: "web/src/routes/field/$campaignId/index.tsx"
      provides: "Field hub landing page"
      exports: ["Route"]
    - path: "web/src/components/field/AssignmentCard.tsx"
      provides: "Tappable assignment card with progress"
      exports: ["AssignmentCard"]
    - path: "web/src/components/field/AssignmentCardSkeleton.tsx"
      provides: "Shimmer loading placeholder"
      exports: ["AssignmentCardSkeleton"]
    - path: "web/src/components/field/FieldEmptyState.tsx"
      provides: "No-assignment friendly message"
      exports: ["FieldEmptyState"]
    - path: "web/src/routes/callback.tsx"
      provides: "Auto-redirect for volunteer-only users"
      contains: "volunteer.*field"
  key_links:
    - from: "web/src/routes/field/$campaignId/index.tsx"
      to: "web/src/hooks/useFieldMe.ts"
      via: "useFieldMe hook call"
      pattern: "useFieldMe"
    - from: "web/src/routes/field/$campaignId/index.tsx"
      to: "web/src/components/field/AssignmentCard.tsx"
      via: "component rendering"
      pattern: "AssignmentCard"
    - from: "web/src/routes/callback.tsx"
      to: "/field/{campaignId}"
      via: "navigate after role detection"
      pattern: "navigate.*field"
---

<objective>
Build the volunteer landing hub page with assignment cards, loading/empty/error states, pull-to-refresh, and auto-redirect for volunteer-only users.

Purpose: This is the main screen volunteers see -- their personalized hub showing active assignments as large tappable cards. Also adds the auto-redirect so volunteer-only users land here directly after login.
Output: Functional hub page with data fetching, assignment cards with progress bars, and volunteer auto-redirect in callback.
</objective>

<execution_context>
@/home/kwhatcher/.claude/get-shit-done/workflows/execute-plan.md
@/home/kwhatcher/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/30-field-layout-shell-volunteer-landing/30-CONTEXT.md
@.planning/phases/30-field-layout-shell-volunteer-landing/30-RESEARCH.md
@.planning/phases/30-field-layout-shell-volunteer-landing/30-01-SUMMARY.md
@.planning/phases/30-field-layout-shell-volunteer-landing/30-02-SUMMARY.md

<interfaces>
<!-- Types and hooks from Plan 01 -->

From web/src/types/field.ts (created in Plan 01):
```typescript
export interface CanvassingAssignment {
  walk_list_id: string
  name: string
  total: number
  completed: number
}

export interface PhoneBankingAssignment {
  session_id: string
  name: string
  total: number
  completed: number
}

export interface FieldMeResponse {
  volunteer_name: string
  campaign_name: string
  canvassing: CanvassingAssignment | null
  phone_banking: PhoneBankingAssignment | null
}
```

From web/src/hooks/useFieldMe.ts (created in Plan 01):
```typescript
export function useFieldMe(campaignId: string): UseQueryResult<FieldMeResponse>
```

From web/src/components/field/FieldHeader.tsx (created in Plan 02):
```typescript
interface FieldHeaderProps {
  campaignId: string
  title: string
  showBack?: boolean
}
export function FieldHeader({ campaignId, title, showBack }: FieldHeaderProps): JSX.Element
```

From web/src/routes/field/$campaignId.tsx (created in Plan 02):
- Layout route renders FieldHeader + Outlet
- The hub page (index.tsx) needs to communicate campaign_name to the layout for the title
- Approach: The layout route can also call useFieldMe to get campaign_name for the header title, OR the hub passes it up. Since the layout wraps all children, the layout should call useFieldMe for the title. Update the layout route in this plan to use useFieldMe for the header title.

From web/src/hooks/useUsers.ts:
```typescript
interface UserCampaign { campaign_id: string; campaign_name: string; role: string }
export function useMyCampaigns(): UseQueryResult<UserCampaign[]>
```

From web/src/hooks/usePermissions.ts:
```typescript
export const ROLE_HIERARCHY: Record<CampaignRole, number>
export function usePermissions(): { role: CampaignRole; hasRole: (minimum: CampaignRole) => boolean }
```

From web/src/routes/callback.tsx:
```typescript
// Current flow: handleCallback() -> navigate({ to: "/" })
// Need to add: detect volunteer-only role -> redirect to /field/{campaignId}
```

From web/src/components/ui/:
- Card, CardContent, CardHeader (shadcn)
- Progress (shadcn)
- Skeleton (shadcn)
- Button (shadcn)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: AssignmentCard, skeleton, and empty state components</name>
  <files>web/src/components/field/AssignmentCard.tsx, web/src/components/field/AssignmentCardSkeleton.tsx, web/src/components/field/FieldEmptyState.tsx</files>
  <action>
    1. Create `web/src/components/field/AssignmentCard.tsx`:
       - Props: type ("canvassing" | "phone-banking"), id (string -- walk_list_id or session_id), name (string), total (number), completed (number), campaignId (string)
       - Renders as a Link (from @tanstack/react-router) to `/field/${campaignId}/${type}`
       - Card styling: large tappable surface, rounded-xl, shadow-sm, border, p-4, hover:shadow-md transition. The entire card is the link (not a button inside a card).
       - Icon: Map for canvassing, Phone for phone-banking (from lucide-react). Render in a colored circle bg (blue-50 for canvassing, green-50 for phone banking).
       - Content layout (flex column):
         - Row 1: Icon circle + assignment name (text-base font-semibold) + right chevron (ChevronRight icon)
         - Row 2: Progress text: "{completed} of {total} {type === 'canvassing' ? 'doors' : 'calls'}" in text-sm text-muted-foreground
         - Row 3: shadcn Progress component with value={total > 0 ? (completed / total) * 100 : 0}
         - Row 4: "Tap to start" in text-xs text-muted-foreground, right-aligned
       - Minimum height: min-h-[100px] to feel substantial
       - Touch target: entire card is tappable (Link wrapper)

    2. Create `web/src/components/field/AssignmentCardSkeleton.tsx`:
       - Matches AssignmentCard shape exactly
       - Uses shadcn Skeleton component for: icon circle, name text, progress text, progress bar
       - Renders the same card container (rounded-xl, shadow-sm, border, p-4)
       - Shimmer effect via Skeleton's built-in animate-pulse

    3. Create `web/src/components/field/FieldEmptyState.tsx`:
       - Centered content with a friendly illustration (use ClipboardList icon at 48px in muted-foreground/30)
       - Heading: "No assignment yet" in text-lg font-semibold
       - Body: "Check with your campaign coordinator" in text-sm text-muted-foreground
       - Per user decision: exact message is "No assignment yet -- check with your campaign coordinator"
       - No action buttons (no self-service shift signup per locked decision)
  </action>
  <verify>
    <automated>cd /home/kwhatcher/projects/run-api/web && npx tsc --noEmit --pretty 2>&1 | head -30</automated>
  </verify>
  <done>
    - AssignmentCard renders as a tappable Link with icon, name, progress count, progress bar, and CTA
    - AssignmentCardSkeleton matches card shape with shimmer placeholders
    - FieldEmptyState shows friendly no-assignment message per locked decision
    - All components compile without TypeScript errors
  </done>
</task>

<task type="auto">
  <name>Task 2: Hub landing page with data fetching, pull-to-refresh, and auto-redirect in callback</name>
  <files>web/src/routes/field/$campaignId/index.tsx, web/src/routes/field/$campaignId.tsx, web/src/routes/callback.tsx</files>
  <action>
    1. Replace the placeholder `web/src/routes/field/$campaignId/index.tsx` with the full hub page:
       - Call useFieldMe(campaignId) to get assignment data
       - Personalized greeting: extract first name from volunteer_name (split on space, take first). Render as "Hey {firstName}!" in text-2xl font-bold. Per locked decision: warm style, not formal.
       - Loading state: show AssignmentCardSkeleton (render 1 or 2 skeletons)
       - Error state: "Couldn't load your assignments. Check your connection and try again." with a Retry button that calls refetch(). Per locked decision.
       - Success with assignments: render AssignmentCard for each non-null assignment (canvassing first, then phone banking). Cards stack vertically with gap-4.
       - Success with no assignments: render FieldEmptyState
       - Pull-to-refresh: Implement with native touch events on the main container:
         - Track touchstart Y position
         - On touchmove, if scrollTop === 0 and deltaY > 60px threshold, show a small pull indicator (rotating RefreshCw icon)
         - On touchend past threshold, call queryClient.invalidateQueries({ queryKey: ["field-me", campaignId] })
         - Simple state machine: idle -> pulling -> refreshing -> idle
         - Pull indicator renders above the greeting as a small centered spinner

    2. Update `web/src/routes/field/$campaignId.tsx` layout route:
       - Call useFieldMe(campaignId) in the layout to get campaign_name for the header title
       - Pass campaign_name (or "Field" as fallback while loading) as title prop to FieldHeader
       - On hub (isHub=true): title = data?.campaign_name || "Field", showBack = false
       - On sub-screens: title = derive from pathname ("Canvassing" or "Phone Banking"), showBack = true
       - The useFieldMe call here shares the same React Query cache key as the hub page, so no double fetch

    3. Update `web/src/routes/callback.tsx` for volunteer auto-redirect:
       - After handleCallback() succeeds (in the .then() block):
       - Extract the user from useAuthStore.getState()
       - Extract role using the same logic as usePermissions.ts (read ZITADEL role claim from user.profile)
       - If highest role is "volunteer":
         - Fetch campaigns: `await api.get("api/v1/me/campaigns").json<UserCampaign[]>()`
         - If campaigns.length > 0: navigate to `/field/${campaigns[0].campaign_id}`
         - If no campaigns: navigate to "/" (fallback)
       - If role is anything higher (manager, admin, owner): navigate to "/" as before
       - Handle the API call in a try/catch -- if it fails, fall back to navigate("/")
       - Import api from @/api/client and the role detection logic
       - IMPORTANT per research pitfall: ensure handleCallback() has fully completed and store is updated before making the API call. The .then() already ensures this.
  </action>
  <verify>
    <automated>cd /home/kwhatcher/projects/run-api/web && npx tsc --noEmit --pretty 2>&1 | head -30</automated>
  </verify>
  <done>
    - Hub page shows "Hey {firstName}!" greeting and assignment cards with progress
    - Loading shows skeleton cards, error shows retry button with correct message
    - Empty state shows "No assignment yet" message
    - Pull-to-refresh triggers data refetch
    - Layout route passes campaign_name to FieldHeader title
    - Volunteer-only users auto-redirect to /field/{campaignId} after login
    - Non-volunteer users redirect to / as before
  </done>
</task>

</tasks>

<verification>
- TypeScript: `cd /home/kwhatcher/projects/run-api/web && npx tsc --noEmit`
- E2E smoke: Navigate to /field/{campaignId} as authenticated user -- see greeting + cards or empty state
- Auto-redirect: Log in as volunteer-only user -- should redirect to /field/{campaignId}
- Pull-to-refresh: On mobile viewport, pull down on hub -- should trigger refetch
</verification>

<success_criteria>
- Volunteer landing hub shows personalized greeting and tappable assignment cards with progress bars
- Both canvassing and phone banking cards navigate to their respective sub-routes
- Loading/error/empty states all render correctly per locked decisions
- Volunteer-only users auto-redirect to field mode after login
- Campaign name appears in the field header on the hub screen
</success_criteria>

<output>
After completion, create `.planning/phases/30-field-layout-shell-volunteer-landing/30-03-SUMMARY.md`
</output>
