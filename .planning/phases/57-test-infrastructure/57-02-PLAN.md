---
phase: 57-test-infrastructure
plan: 02
type: execute
wave: 2
depends_on: ["57-01"]
files_modified:
  - web/e2e/auth-owner.setup.ts
  - web/e2e/auth-admin.setup.ts
  - web/e2e/auth-manager.setup.ts
  - web/e2e/auth-volunteer.setup.ts
  - web/e2e/auth-viewer.setup.ts
  - web/e2e/auth.setup.ts
  - web/e2e/auth-orgadmin.setup.ts
  - web/e2e/role-gated.orgadmin.spec.ts
  - web/e2e/role-gated.admin.spec.ts
  - .github/workflows/pr.yml
autonomous: true
requirements:
  - INFRA-02
  - INFRA-03

must_haves:
  truths:
    - "Each of the 5 auth setup files authenticates with the correct role credentials and saves to the correct storageState path"
    - "The orgadmin spec file is renamed to admin suffix so it matches the admin project testMatch"
    - "Old auth setup files (auth.setup.ts, auth-orgadmin.setup.ts) are removed to prevent confusion"
    - "CI workflow runs 4 sharded E2E test jobs in parallel with independent Docker Compose stacks"
    - "After all shards complete, a merge job combines blob reports into a single HTML report artifact"
  artifacts:
    - path: "web/e2e/auth-owner.setup.ts"
      provides: "Owner auth setup (owner1@localhost)"
      contains: "owner1@localhost"
    - path: "web/e2e/auth-admin.setup.ts"
      provides: "Admin auth setup (admin1@localhost)"
      contains: "admin1@localhost"
    - path: "web/e2e/auth-manager.setup.ts"
      provides: "Manager auth setup (manager1@localhost)"
      contains: "manager1@localhost"
    - path: "web/e2e/auth-volunteer.setup.ts"
      provides: "Volunteer auth setup (volunteer1@localhost)"
      contains: "volunteer1@localhost"
    - path: "web/e2e/auth-viewer.setup.ts"
      provides: "Viewer auth setup (viewer1@localhost)"
      contains: "viewer1@localhost"
    - path: "web/e2e/role-gated.admin.spec.ts"
      provides: "Renamed orgadmin spec to match admin project"
      min_lines: 1
    - path: ".github/workflows/pr.yml"
      provides: "Sharded E2E CI with merged reports"
      contains: "shardIndex"
  key_links:
    - from: "web/e2e/auth-owner.setup.ts"
      to: "web/playwright/.auth/owner.json"
      via: "storageState save"
      pattern: "owner\\.json"
    - from: ".github/workflows/pr.yml"
      to: "web/blob-report"
      via: "upload-artifact after shard"
      pattern: "blob-report"
    - from: ".github/workflows/pr.yml"
      to: "merge-reports"
      via: "npx playwright merge-reports"
      pattern: "merge-reports"
---

<objective>
Create 5 role-based auth setup files, migrate existing specs to the new auth structure, and configure CI for sharded E2E testing with merged reports.

Purpose: Complete the Playwright auth infrastructure (Plan 01 created the config, this plan creates the setup files it references) and enable CI parallelism for the growing E2E suite.
Output: 5 auth setup files, renamed orgadmin spec, cleaned up old auth files, sharded CI workflow with merged HTML report.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/57-test-infrastructure/57-CONTEXT.md
@.planning/phases/57-test-infrastructure/57-RESEARCH.md
@.planning/phases/57-test-infrastructure/57-01-SUMMARY.md

<interfaces>
<!-- Key contracts from Plan 01 that this plan depends on -->

From web/playwright.config.ts (created in Plan 01):
```typescript
// 5 setup projects that testMatch against these files:
// auth-owner.setup.ts -> playwright/.auth/owner.json
// auth-admin.setup.ts -> playwright/.auth/admin.json
// auth-manager.setup.ts -> playwright/.auth/manager.json
// auth-volunteer.setup.ts -> playwright/.auth/volunteer.json
// auth-viewer.setup.ts -> playwright/.auth/viewer.json
//
// Reporter: process.env.CI ? "blob" : "html"
// Blob reporter outputs to web/blob-report/ directory
```

From scripts/create-e2e-users.py (created in Plan 01):
```python
# Credentials per role (from E2E_USERS list):
# owner1@localhost / Owner1234!
# admin1@localhost / Admin1234!
# manager1@localhost / Manager1234!
# volunteer1@localhost / Volunteer1234!
# viewer1@localhost / Viewer1234!
```

From web/e2e/auth-orgadmin.setup.ts (existing pattern to follow):
```typescript
// Each auth setup file follows this exact structure:
// 1. Import test/expect from @playwright/test
// 2. Define authFile path using import.meta.dirname
// 3. setup() block: goto /login -> wait for ZITADEL -> fill credentials -> save storageState
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create 5 role-based auth setup files and migrate spec</name>
  <files>
    web/e2e/auth-owner.setup.ts,
    web/e2e/auth-admin.setup.ts,
    web/e2e/auth-manager.setup.ts,
    web/e2e/auth-volunteer.setup.ts,
    web/e2e/auth-viewer.setup.ts,
    web/e2e/auth.setup.ts,
    web/e2e/auth-orgadmin.setup.ts,
    web/e2e/role-gated.orgadmin.spec.ts,
    web/e2e/role-gated.admin.spec.ts
  </files>
  <read_first>
    - web/e2e/auth-orgadmin.setup.ts (pattern to follow for all 5 auth setup files)
    - web/e2e/auth-volunteer.setup.ts (second pattern reference)
    - web/e2e/auth.setup.ts (current default auth — note password change handling that new files do NOT need)
    - web/e2e/role-gated.orgadmin.spec.ts (file to rename)
    - web/playwright.config.ts (verify the storageState paths and testMatch patterns from Plan 01)
  </read_first>
  <action>
**Create 5 auth setup files** (per D-05, D-06, D-07). Each follows the EXACT pattern from `auth-orgadmin.setup.ts` (NOT `auth.setup.ts` — the old default has password change handling that the new users do NOT need since they are created with `passwordChangeRequired: false`).

**`web/e2e/auth-owner.setup.ts`** — Owner auth (per D-06, replaces old `auth.setup.ts`):
```typescript
import { test as setup, expect } from "@playwright/test"
import path from "path"

const authFile = path.join(import.meta.dirname, "../playwright/.auth/owner.json")

setup("authenticate as owner", async ({ page }) => {
  await page.goto("/login")
  await page.waitForURL(
    (url) => url.port === "8080" || url.pathname.includes("/ui/login"),
    { timeout: 30_000 },
  )

  const loginInput = page.getByLabel(/login ?name/i).or(page.locator("#loginName"))
  await loginInput.fill(
    process.env.E2E_OWNER_USERNAME ?? "owner1@localhost",
  )

  await page.getByRole("button", { name: /next/i }).click()

  const passwordInput = page
    .getByLabel(/password/i)
    .or(page.locator("#password"))
  await passwordInput.fill(
    process.env.E2E_OWNER_PASSWORD ?? "Owner1234!",
  )

  await page.getByRole("button", { name: /next/i }).click()

  await page.waitForURL(/\/(campaigns|org)/, { timeout: 30_000 })
  await expect(page.locator("body")).toBeVisible()
  await page.context().storageState({ path: authFile })
})
```

**`web/e2e/auth-admin.setup.ts`** — Admin auth (replaces old `auth-orgadmin.setup.ts`):
Same pattern, but:
- `authFile` path: `../playwright/.auth/admin.json`
- setup name: `"authenticate as admin"`
- env vars: `E2E_ADMIN_USERNAME` / `E2E_ADMIN_PASSWORD`
- defaults: `"admin1@localhost"` / `"Admin1234!"`

**`web/e2e/auth-manager.setup.ts`** — Manager auth (new):
Same pattern, but:
- `authFile` path: `../playwright/.auth/manager.json`
- setup name: `"authenticate as manager"`
- env vars: `E2E_MANAGER_USERNAME` / `E2E_MANAGER_PASSWORD`
- defaults: `"manager1@localhost"` / `"Manager1234!"`

**`web/e2e/auth-volunteer.setup.ts`** — Volunteer auth (update existing):
Same pattern, but:
- `authFile` path: `../playwright/.auth/volunteer.json` (unchanged)
- setup name: `"authenticate as volunteer"` (unchanged)
- env vars: `E2E_VOLUNTEER_USERNAME` / `E2E_VOLUNTEER_PASSWORD` (unchanged)
- defaults: change from `"e2e-volunteer@localhost"` / `"E2eVolunteer1!"` to `"volunteer1@localhost"` / `"Volunteer1234!"`

**`web/e2e/auth-viewer.setup.ts`** — Viewer auth (new):
Same pattern, but:
- `authFile` path: `../playwright/.auth/viewer.json`
- setup name: `"authenticate as viewer"`
- env vars: `E2E_VIEWER_USERNAME` / `E2E_VIEWER_PASSWORD`
- defaults: `"viewer1@localhost"` / `"Viewer1234!"`

**Rename spec file** (per D-07, research pitfall 7):
- Rename `web/e2e/role-gated.orgadmin.spec.ts` to `web/e2e/role-gated.admin.spec.ts`
- Use `git mv` for the rename to preserve history
- The file content does NOT need changes — only the filename suffix matters for routing

**Delete old auth setup files** (per D-07 — one unified auth system):
- Delete `web/e2e/auth.setup.ts` (replaced by `auth-owner.setup.ts`)
- Delete `web/e2e/auth-orgadmin.setup.ts` (replaced by `auth-admin.setup.ts`)
- Use `git rm` for tracked files
  </action>
  <verify>
    <automated>cd /home/kwhatcher/projects/civicpulse/run-api/web && test -f e2e/auth-owner.setup.ts && test -f e2e/auth-admin.setup.ts && test -f e2e/auth-manager.setup.ts && test -f e2e/auth-volunteer.setup.ts && test -f e2e/auth-viewer.setup.ts && test -f e2e/role-gated.admin.spec.ts && test ! -f e2e/auth.setup.ts && test ! -f e2e/auth-orgadmin.setup.ts && test ! -f e2e/role-gated.orgadmin.spec.ts && grep "owner1@localhost" e2e/auth-owner.setup.ts && grep "admin1@localhost" e2e/auth-admin.setup.ts && grep "manager1@localhost" e2e/auth-manager.setup.ts && grep "volunteer1@localhost" e2e/auth-volunteer.setup.ts && grep "viewer1@localhost" e2e/auth-viewer.setup.ts && echo "All auth files verified"</automated>
  </verify>
  <acceptance_criteria>
    - web/e2e/auth-owner.setup.ts exists and contains `owner1@localhost` and `Owner1234!` and `owner.json`
    - web/e2e/auth-admin.setup.ts exists and contains `admin1@localhost` and `Admin1234!` and `admin.json`
    - web/e2e/auth-manager.setup.ts exists and contains `manager1@localhost` and `Manager1234!` and `manager.json`
    - web/e2e/auth-volunteer.setup.ts exists and contains `volunteer1@localhost` and `Volunteer1234!` and `volunteer.json`
    - web/e2e/auth-viewer.setup.ts exists and contains `viewer1@localhost` and `Viewer1234!` and `viewer.json`
    - web/e2e/role-gated.admin.spec.ts exists (renamed from role-gated.orgadmin.spec.ts)
    - web/e2e/auth.setup.ts does NOT exist (deleted)
    - web/e2e/auth-orgadmin.setup.ts does NOT exist (deleted)
    - web/e2e/role-gated.orgadmin.spec.ts does NOT exist (renamed)
    - None of the 5 auth setup files contain password change handling (`Change Password` or `Old Password` or `Password confirmation`)
    - Each auth setup file uses `import.meta.dirname` for path resolution
    - Each auth setup file calls `page.context().storageState({ path: authFile })`
  </acceptance_criteria>
  <done>5 role-based auth setup files exist with correct credentials and storageState paths. Old auth files deleted. Orgadmin spec renamed to admin suffix. No password change handling in new auth files.</done>
</task>

<task type="auto">
  <name>Task 2: Configure CI workflow for sharded E2E tests with merged reports</name>
  <files>.github/workflows/pr.yml</files>
  <read_first>
    - .github/workflows/pr.yml (current CI workflow with monolithic integration-e2e job)
    - .planning/phases/57-test-infrastructure/57-RESEARCH.md (CI sharding section with full YAML examples)
  </read_first>
  <action>
**Update `.github/workflows/pr.yml`** (per D-08, D-09):

Replace the `integration-e2e` job with TWO new jobs: `e2e-tests` (sharded matrix) and `merge-e2e-reports` (report merger). Keep the `integration` tests (pytest) in a separate job since they don't need sharding.

**Split the current `integration-e2e` job into:**

1. **`integration`** job — Runs Python integration tests only (not sharded):
```yaml
  integration:
    name: Integration Tests
    runs-on: ubuntu-latest
    needs: [lint, test, frontend]
    steps:
      - uses: actions/checkout@v4

      - uses: astral-sh/setup-uv@v4

      - name: Start services
        run: docker compose up -d --build --wait
        env:
          ZITADEL_TLS_MODE: disabled
          ZITADEL_EXTERNAL_SECURE: "false"
          ZITADEL_DOMAIN: localhost
          DISABLE_TLS: "true"

      - name: Wait for ZITADEL readiness
        run: |
          for i in $(seq 1 60); do
            curl -sf http://localhost:8080/debug/ready && echo "ZITADEL ready" && exit 0
            echo "Waiting for ZITADEL... ($i/60)"
            sleep 2
          done
          echo "ZITADEL did not become ready" && exit 1

      - name: Run migrations and seed
        run: |
          docker compose exec -T api bash -c "PYTHONPATH=/home/app alembic upgrade head"
          docker compose exec -T api bash -c "PYTHONPATH=/home/app python scripts/seed.py"

      - name: Install Python test dependencies
        run: uv sync --frozen --dev

      - name: Run integration tests
        run: uv run pytest tests/integration/ -m integration -x -q
        env:
          DATABASE_URL: "postgresql+asyncpg://postgres:postgres@localhost:5433/run_api"

      - name: Stop services
        if: always()
        run: docker compose down -v
```

2. **`e2e-tests`** job — Sharded E2E tests (per D-08, 4 shards):
```yaml
  e2e-tests:
    name: E2E Tests (Shard ${{ matrix.shardIndex }}/4)
    runs-on: ubuntu-latest
    needs: [lint, test, frontend]
    strategy:
      fail-fast: false
      matrix:
        shardIndex: [1, 2, 3, 4]
        shardTotal: [4]
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: "22"

      - name: Start services
        run: docker compose up -d --build --wait
        env:
          ZITADEL_TLS_MODE: disabled
          ZITADEL_EXTERNAL_SECURE: "false"
          ZITADEL_DOMAIN: localhost
          DISABLE_TLS: "true"

      - name: Wait for ZITADEL readiness
        run: |
          for i in $(seq 1 60); do
            curl -sf http://localhost:8080/debug/ready && echo "ZITADEL ready" && exit 0
            echo "Waiting for ZITADEL... ($i/60)"
            sleep 2
          done
          echo "ZITADEL did not become ready" && exit 1

      - name: Run migrations and seed
        run: |
          docker compose exec -T api bash -c "PYTHONPATH=/home/app alembic upgrade head"
          docker compose exec -T api bash -c "PYTHONPATH=/home/app python scripts/seed.py"

      - name: Create E2E test users
        run: |
          docker compose exec -T api bash -c "PYTHONPATH=/home/app python scripts/create-e2e-users.py"

      - name: Install Playwright browsers
        working-directory: web
        run: |
          npm ci
          npx playwright install --with-deps chromium

      - name: Run E2E tests
        working-directory: web
        run: npx playwright test --shard=${{ matrix.shardIndex }}/${{ matrix.shardTotal }}
        env:
          E2E_OWNER_USERNAME: owner1@localhost
          E2E_OWNER_PASSWORD: "Owner1234!"
          E2E_ADMIN_USERNAME: admin1@localhost
          E2E_ADMIN_PASSWORD: "Admin1234!"
          E2E_MANAGER_USERNAME: manager1@localhost
          E2E_MANAGER_PASSWORD: "Manager1234!"
          E2E_VOLUNTEER_USERNAME: volunteer1@localhost
          E2E_VOLUNTEER_PASSWORD: "Volunteer1234!"
          E2E_VIEWER_USERNAME: viewer1@localhost
          E2E_VIEWER_PASSWORD: "Viewer1234!"

      - name: Upload blob report
        uses: actions/upload-artifact@v4
        if: ${{ !cancelled() }}
        with:
          name: blob-report-${{ matrix.shardIndex }}
          path: web/blob-report
          retention-days: 1

      - name: Upload Playwright traces
        uses: actions/upload-artifact@v4
        if: failure()
        with:
          name: playwright-traces-${{ matrix.shardIndex }}
          path: web/test-results/
          retention-days: 7

      - name: Stop services
        if: always()
        run: docker compose down -v
```

3. **`merge-e2e-reports`** job — Merge blob reports into HTML (per D-09):
```yaml
  merge-e2e-reports:
    name: Merge E2E Reports
    if: ${{ !cancelled() }}
    needs: [e2e-tests]
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: "22"

      - name: Install Playwright
        working-directory: web
        run: npm ci

      - name: Download blob reports
        uses: actions/download-artifact@v4
        with:
          path: web/all-blob-reports
          pattern: blob-report-*
          merge-multiple: true

      - name: Merge reports
        working-directory: web
        run: npx playwright merge-reports --reporter html ./all-blob-reports

      - name: Upload merged report
        uses: actions/upload-artifact@v4
        with:
          name: playwright-report
          path: web/playwright-report/
          retention-days: 7
```

**Important details:**
- The `docker-build` job `needs` should remain unchanged: `[lint, test, frontend]`
- Remove the old `integration-e2e` job entirely — it is fully replaced by the three new jobs
- The old env vars (`E2E_USERNAME`, `E2E_PASSWORD`, `E2E_ORGADMIN_USERNAME`, etc.) are replaced with the new role-based vars (`E2E_OWNER_USERNAME`, etc.)
- Each shard job gets its own Docker Compose stack (per research: GitHub Actions matrix jobs run on separate runners)
- `fail-fast: false` ensures all shards complete even if one fails
- Blob report artifact `retention-days: 1` (only needed for merge), final merged report `retention-days: 7`
- Trace artifacts are per-shard to avoid name collisions: `playwright-traces-${{ matrix.shardIndex }}`
  </action>
  <verify>
    <automated>cd /home/kwhatcher/projects/civicpulse/run-api && grep -c "shardIndex" .github/workflows/pr.yml && grep "merge-reports" .github/workflows/pr.yml && grep "blob-report" .github/workflows/pr.yml && grep "E2E_OWNER_USERNAME" .github/workflows/pr.yml && grep "fail-fast: false" .github/workflows/pr.yml && echo "CI workflow verified"</automated>
  </verify>
  <acceptance_criteria>
    - .github/workflows/pr.yml contains job `e2e-tests:` with `strategy.matrix.shardIndex: [1, 2, 3, 4]`
    - .github/workflows/pr.yml contains job `merge-e2e-reports:` with `needs: [e2e-tests]`
    - .github/workflows/pr.yml contains job `integration:` that runs Python integration tests
    - .github/workflows/pr.yml does NOT contain job named `integration-e2e:`
    - .github/workflows/pr.yml contains `--shard=${{ matrix.shardIndex }}/${{ matrix.shardTotal }}`
    - .github/workflows/pr.yml contains `fail-fast: false`
    - .github/workflows/pr.yml contains `E2E_OWNER_USERNAME: owner1@localhost`
    - .github/workflows/pr.yml contains `E2E_ADMIN_USERNAME: admin1@localhost`
    - .github/workflows/pr.yml contains `E2E_MANAGER_USERNAME: manager1@localhost`
    - .github/workflows/pr.yml contains `E2E_VOLUNTEER_USERNAME: volunteer1@localhost`
    - .github/workflows/pr.yml contains `E2E_VIEWER_USERNAME: viewer1@localhost`
    - .github/workflows/pr.yml contains `npx playwright merge-reports --reporter html`
    - .github/workflows/pr.yml contains `pattern: blob-report-*`
    - .github/workflows/pr.yml contains `merge-multiple: true`
    - .github/workflows/pr.yml contains `name: blob-report-${{ matrix.shardIndex }}`
    - .github/workflows/pr.yml does NOT contain `E2E_USERNAME:` (old default env var)
    - .github/workflows/pr.yml does NOT contain `E2E_ORGADMIN_USERNAME:` (old orgadmin env var)
  </acceptance_criteria>
  <done>CI workflow has integration tests job (non-sharded), 4-shard E2E matrix job with role-based env vars, and merge-reports job that produces a single HTML report artifact. Old monolithic integration-e2e job removed.</done>
</task>

</tasks>

<verification>
- All 5 auth setup files exist with correct credentials and storageState paths
- Old auth files (auth.setup.ts, auth-orgadmin.setup.ts) are deleted
- role-gated.orgadmin.spec.ts renamed to role-gated.admin.spec.ts
- CI workflow has 3 new jobs replacing the old integration-e2e job
- `cd web && npx playwright test --list` shows all 51 specs distributed across 5 projects (may need services running for auth setups)
</verification>

<success_criteria>
- 5 auth setup files authenticate with correct role credentials and save to correct storageState paths
- Orgadmin spec renamed to admin suffix, matching the admin project's testMatch pattern
- Old auth files removed — one unified auth system, no parallel legacy setup
- CI workflow runs 4 E2E shards in parallel, each with independent Docker Compose stack
- Merged HTML report produced from blob reports after all shards complete
- All env vars use new role-based naming convention (E2E_OWNER_*, E2E_ADMIN_*, etc.)
</success_criteria>

<output>
After completion, create `.planning/phases/57-test-infrastructure/57-02-SUMMARY.md`
</output>
