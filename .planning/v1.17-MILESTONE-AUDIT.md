---
milestone: v1.17
audited: 2026-04-09T21:00:00-04:00
status: gaps_found
scores:
  requirements: 0/19
  phases: 3/3
  integration: 2/5
  flows: 3/5
gaps:
  requirements:
    - id: "APPL-01"
      status: "unsatisfied"
      phase: "102"
      claimed_by_plans:
        - ".planning/phases/102-volunteer-applications-existing-account-intake/102-01-PLAN.md"
      completed_by_plans: []
      verification_status: "passed"
      evidence: "Public volunteer application endpoints still require authentication and the signup page only renders the form for authenticated users, so anonymous visitors cannot submit an application."
  integration:
    - id: "INT-APPLY-AUTH"
      requirements: ["APPL-01"]
      severity: "critical"
      evidence: "app/api/v1/volunteer_applications.py requires get_current_user on both public application endpoints and web/src/routes/signup/$token.tsx gates the form behind isAuthenticated."
  flows:
    - id: "FLOW-ANON-SUBMIT"
      requirements: ["APPL-01"]
      severity: "critical"
      evidence: "Anonymous visitors can resolve a signup link but cannot complete the volunteer application flow."
tech_debt:
  - phase: "101"
    items:
      - "Phase summary is missing requirements_completed frontmatter, so no phase 101 requirements can be marked fully satisfied in the 3-source audit matrix."
      - "No 101-VALIDATION.md Nyquist artifact present."
  - phase: "102"
    items:
      - "Verification report is minimal and does not include a per-requirement evidence table."
      - "Phase summary is missing requirements_completed frontmatter."
      - "No 102-VALIDATION.md Nyquist artifact present."
      - "Submission returns any existing application match, including rejected rows, so rejected applicants cannot submit a fresh application."
  - phase: "103"
    items:
      - "Verification report is minimal and does not include a per-requirement evidence table."
      - "Phase summary is missing requirements_completed frontmatter."
      - "No 103-VALIDATION.md Nyquist artifact present."
      - "Admin review UI/API do not surface duplicate or existing-account review context for REVW-02."
      - "Existing campaign members who apply via a signup link are auto-recorded as approved with no reviewer, weakening audit clarity."
nyquist:
  compliant_phases: []
  partial_phases: []
  missing_phases: ["101", "102", "103"]
  overall: "missing"
---

# Milestone Audit: v1.17 Easy Volunteer Invites

## Verdict

**Status:** gaps_found

`v1.17` is not ready for milestone completion. The milestone has one hard requirement failure and broad documentation traceability gaps:

- `APPL-01` is unsatisfied because the volunteer application flow is still authenticated-only.
- `REVW-02`, `SAFE-03`, and most remaining requirements are only partial in the audit matrix because the phase verification artifacts do not include the normal per-requirement evidence tables and none of the summaries include `requirements_completed` frontmatter.
- Nyquist validation discovery is incomplete for all in-scope phases.

## Scorecard

| Area | Score | Notes |
|---|---:|---|
| Requirements | 0/19 | 1 unsatisfied, 18 partial, 0 satisfied under the required 3-source cross-reference |
| Phases | 3/3 | All three phases have a `VERIFICATION.md` with `status: passed` |
| Integration | 2/5 | Signup-link lifecycle and approval/access wiring exist, but anonymous apply flow is broken |
| Flows | 3/5 | Admin lifecycle, authenticated apply, and approval gating work; anonymous intake does not |

## Phase Verification Summary

| Phase | Source | Verification Status | Notes |
|---|---|---|---|
| 101 | `.planning/milestones/v1.17-phases/101-signup-link-foundation-public-entry/101-VERIFICATION.md` | passed | Strongest artifact set; explicit goal/evidence table present |
| 102 | `.planning/phases/102-volunteer-applications-existing-account-intake/102-VERIFICATION.md` | passed | Minimal verification; no per-requirement table |
| 103 | `.planning/phases/103-review-queue-approval-and-access-activation/103-VERIFICATION.md` | passed | Minimal verification; no per-requirement table |

## Requirements Coverage

| Requirement | Phase | Final Status | Evidence |
|---|---|---|---|
| LINK-01 | 101 | partial | Implemented in phase 101, but summary frontmatter and REQUIREMENTS checkbox cross-check are missing |
| LINK-02 | 101 | partial | Implemented in phase 101, but summary frontmatter and REQUIREMENTS checkbox cross-check are missing |
| LINK-03 | 101 | partial | Implemented in phase 101, but summary frontmatter and REQUIREMENTS checkbox cross-check are missing |
| LINK-04 | 101 | partial | Implemented in phase 101, but summary frontmatter and REQUIREMENTS checkbox cross-check are missing |
| LINK-05 | 101 | partial | Implemented in phase 101, but summary frontmatter and REQUIREMENTS checkbox cross-check are missing |
| SAFE-01 | 101 | partial | Fail-closed link handling is verified in phase 101, but the 3-source matrix is incomplete |
| APPL-01 | 102 | unsatisfied | Public application endpoints require authentication and the signup page only renders the form for authenticated users |
| APPL-02 | 102 | partial | Signup-link attribution is stored, but the verification/summary traceability artifacts are incomplete |
| APPL-03 | 102 | partial | Duplicate-safe matching exists, but the verification/summary traceability artifacts are incomplete |
| APPL-04 | 102 | partial | Existing-account reuse works after sign-in, but there is no true public submit path |
| APPL-05 | 102 | partial | Existing-account prefill works after sign-in, but there is no true public submit path |
| SAFE-02 | 102 | partial | Rate limits exist on public endpoints, but the verification/summary traceability artifacts are incomplete |
| REVW-01 | 103 | partial | Review queue exists, but the verification/summary traceability artifacts are incomplete |
| REVW-02 | 103 | partial | Review queue omits duplicate/existing-account context required by the requirement |
| REVW-03 | 103 | partial | Approve flow is wired, but the verification/summary traceability artifacts are incomplete |
| REVW-04 | 103 | partial | Reject flow is wired, but the verification/summary traceability artifacts are incomplete |
| REVW-05 | 103 | partial | Approval creates membership/access, but the verification/summary traceability artifacts are incomplete |
| REVW-06 | 103 | partial | Pending/rejected applicants remain outside campaign access, but the verification/summary traceability artifacts are incomplete |
| SAFE-03 | 103 | partial | Duplicate access creation is avoided, but existing-member intake bypasses normal review semantics |

## Integration Findings

### Critical

1. `APPL-01` is not delivered.

Evidence:

- [app/api/v1/volunteer_applications.py](/home/kwhatcher/projects/civicpulse/run-api/app/api/v1/volunteer_applications.py)
- [web/src/routes/signup/$token.tsx](/home/kwhatcher/projects/civicpulse/run-api/web/src/routes/signup/$token.tsx)

Both public application endpoints depend on `get_current_user`, and the signup page only renders the application form when `isAuthenticated` is true. Anonymous visitors can open a valid signup link but cannot submit an application.

### Major

1. `REVW-02` is only partially delivered.

Evidence:

- [app/api/v1/volunteer_applications.py](/home/kwhatcher/projects/civicpulse/run-api/app/api/v1/volunteer_applications.py)
- [app/schemas/volunteer_application.py](/home/kwhatcher/projects/civicpulse/run-api/app/schemas/volunteer_application.py)
- [web/src/routes/campaigns/$campaignId/settings/members.tsx](/home/kwhatcher/projects/civicpulse/run-api/web/src/routes/campaigns/$campaignId/settings/members.tsx)

The admin review flow lists raw application records and source-link attribution, but it does not surface duplicate or existing-account review context.

## End-to-End Flow Review

| Flow | Status | Notes |
|---|---|---|
| Admin signup-link creation/list/disable/regenerate | complete | Phase 101 wiring is intact |
| Public signup-link resolution | complete | Valid links resolve safe campaign context and invalid links fail closed |
| Anonymous volunteer application submission | broken | Blocked on authentication requirement |
| Authenticated volunteer application submission | complete | Prefill, status lookup, attribution, and duplicate suppression are wired |
| Admin review, approve, reject, and access activation | mostly complete | Access activation is wired, but review context is incomplete |

## Tech Debt And Deferred Gaps

- Existing campaign members who hit a signup link are written directly as `approved` applications with no reviewer recorded.
- Re-applying after rejection appears blocked because `submit_application` returns any existing application row, not just an existing pending row.
- No milestone-level browser or integration E2E coverage was found for the full public-signup-to-approval flow.
- `REQUIREMENTS.md` still shows all v1.17 items as pending, which prevents any requirement from reaching `satisfied` in the required audit matrix.

## Nyquist Discovery

`workflow.nyquist_validation` is enabled, but no `101-VALIDATION.md`, `102-VALIDATION.md`, or `103-VALIDATION.md` artifact exists. Overall Nyquist status for the milestone is `missing`.

## Recommendation

Do not archive `v1.17` yet.

Close at least these blockers first:

1. Implement a true anonymous volunteer application intake path, or explicitly redefine `APPL-01` if authentication is required by design.
2. Add duplicate/existing-account review context to the admin review queue for `REVW-02`.
3. Backfill requirement traceability artifacts:
   - update `REQUIREMENTS.md` checkboxes/status,
   - add `requirements_completed` frontmatter to the phase summaries,
   - expand phase 102 and 103 verification files with per-requirement evidence tables.
4. Add or explicitly waive Nyquist validation artifacts for phases 101-103.
