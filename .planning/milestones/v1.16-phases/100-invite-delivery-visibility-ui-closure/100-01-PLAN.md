---
phase: 100-invite-delivery-visibility-ui-closure
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - web/src/types/invite.ts
  - web/src/hooks/useInvites.ts
  - web/src/hooks/useInvites.test.ts
  - web/src/routes/campaigns/$campaignId/settings/members.tsx
  - web/src/routes/campaigns/$campaignId/settings/members.test.tsx
autonomous: true
requirements:
  - AUD-04
must_haves:
  truths:
    - "The pending-invites UI consumes the backend's bare invite array instead of a paginated wrapper."
    - "The frontend invite contract includes delivery error and last-event timestamp fields already exposed by the API."
    - "Campaign admins can inspect latest invite delivery status, failure reason, and event timing directly in the members settings UI."
  artifacts:
    - path: "web/src/routes/campaigns/$campaignId/settings/members.tsx"
      provides: "Pending invite table with support-facing delivery details"
---

<objective>
Close the remaining milestone audit gap by realigning the pending-invites
frontend with the backend invite contract and surfacing support-grade delivery
outcome details in the existing admin workflow.
</objective>

<verification>
- `cd web && npm test -- useInvites.test.ts 'src/routes/campaigns/$campaignId/settings/members.test.tsx'`
</verification>
