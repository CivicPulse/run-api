---
phase: 95-provider-foundation-secret-hygiene
plan: 02
type: execute
wave: 2
depends_on:
  - 95-01
files_modified:
  - app/services/email_provider.py
  - tests/unit/test_mailgun_provider.py
  - tests/test_observability.py
autonomous: true
requirements:
  - EML-01
  - EML-03
  - SEC-01
must_haves:
  truths:
    - "Mailgun is implemented behind the transactional email provider seam rather than in invite-domain logic."
    - "Outbound provider payloads stay single-recipient and preserve explicit tenant context."
    - "Secret-bearing configuration and request details are not echoed through safe metadata, logs, or test-visible exception strings."
  artifacts:
    - path: "app/services/email_provider.py"
      provides: "Mailgun provider implementation and payload construction"
    - path: "tests/unit/test_mailgun_provider.py"
      provides: "Regression coverage for Mailgun request construction and config safety"
    - path: "tests/test_observability.py"
      provides: "Regression coverage for email-secret scrubbing or safe logging behavior"
  key_links:
    - from: "app/services/email_provider.py"
      to: "app/services/email_types.py"
      via: "typed request-to-provider payload translation"
      pattern: "TransactionalEmail"
    - from: "app/services/email_provider.py"
      to: "app/core/config.py"
      via: "Mailgun settings and region-aware base URL"
      pattern: "mailgun"
---

<objective>
Implement the first concrete provider so CivicPulse can construct Mailgun-bound transactional email requests safely while keeping secrets and tenant boundaries intact.
</objective>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/95-provider-foundation-secret-hygiene/95-CONTEXT.md
@.planning/phases/95-provider-foundation-secret-hygiene/95-RESEARCH.md
@.planning/phases/95-provider-foundation-secret-hygiene/95-01-PLAN.md
@app/core/config.py
@app/core/sentry.py
@tests/test_observability.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement the Mailgun provider adapter</name>
  <files>app/services/email_provider.py, tests/unit/test_mailgun_provider.py</files>
  <behavior>
    - Test 1: Mailgun payload construction is single-recipient and includes provider-safe subject/body fields
    - Test 2: Mailgun region/base URL selection follows configuration instead of hard-coded invite logic
    - Test 3: provider send failures or config errors do not include raw API keys or raw authorization headers in exception strings
  </behavior>
  <action>
    1. Add a Mailgun provider implementation behind the transactional email provider interface
    2. Translate typed rendered-email requests into Mailgun API payloads using `httpx`
    3. Keep request construction tenant-safe and single-recipient
    4. Add focused tests for payload shape, base URL selection, and non-secret error behavior
  </action>
  <verify>
    <automated>uv run pytest tests/unit/test_mailgun_provider.py -x -q</automated>
  </verify>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Lock secret-safe observability expectations for email foundation</name>
  <files>tests/test_observability.py, tests/unit/test_mailgun_provider.py</files>
  <behavior>
    - Test 1: email-provider-related exception text or extra payload scrubbing does not expose Mailgun API keys
    - Test 2: safe metadata helpers expose provider/domain readiness without raw credentials
  </behavior>
  <action>
    1. Extend observability coverage for email-secret leakage risks
    2. Keep the email foundation aligned with the repo’s existing scrub-or-don’t-log-raw-data posture
    3. Verify that provider helpers return only safe metadata for future operational surfaces
  </action>
  <verify>
    <automated>uv run pytest tests/unit/test_mailgun_provider.py tests/test_observability.py -x -q</automated>
  </verify>
</task>

</tasks>

<verification>
- `uv run pytest tests/unit/test_mailgun_provider.py tests/test_observability.py -x -q`
</verification>

<output>
After completion, create `.planning/phases/95-provider-foundation-secret-hygiene/95-02-SUMMARY.md`
</output>
