---
phase: 95-provider-foundation-secret-hygiene
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/core/config.py
  - app/services/email_types.py
  - app/services/email_templates.py
  - app/services/email_provider.py
  - tests/unit/test_email_templates.py
  - tests/unit/test_email_provider_factory.py
autonomous: true
requirements:
  - EML-01
  - EML-02
  - EML-03
  - SEC-01
must_haves:
  truths:
    - "The codebase owns a typed transactional-email contract that is provider-agnostic and single-recipient."
    - "Transactional templates render from CivicPulse code as both HTML and plain text."
    - "Provider selection and Mailgun configuration come from application settings rather than invite-specific hard-coding."
  artifacts:
    - path: "app/services/email_types.py"
      provides: "Typed transactional email request, rendered email, and tenant context contracts"
    - path: "app/services/email_templates.py"
      provides: "Code-owned template registry rendering HTML and plain text"
    - path: "app/services/email_provider.py"
      provides: "Provider abstraction and settings-backed factory"
    - path: "app/core/config.py"
      provides: "Environment-scoped Mailgun and app email settings"
  key_links:
    - from: "app/core/config.py"
      to: "app/services/email_provider.py"
      via: "settings-backed provider selection and Mailgun configuration"
      pattern: "mailgun|email_provider"
    - from: "app/services/email_templates.py"
      to: "app/services/email_types.py"
      via: "typed template rendering contract"
      pattern: "RenderedEmail|TransactionalEmail"
---

<objective>
Create the transactional email foundation contracts so later invite dispatch, audit, and webhook phases can build on a stable app-owned abstraction instead of wiring directly to Mailgun.
</objective>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/95-provider-foundation-secret-hygiene/95-CONTEXT.md
@.planning/phases/95-provider-foundation-secret-hygiene/95-RESEARCH.md
@app/core/config.py
@app/services/invite.py
@app/services/twilio_config.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Define the provider-agnostic transactional email contract</name>
  <files>app/core/config.py, app/services/email_types.py, app/services/email_provider.py, tests/unit/test_email_provider_factory.py</files>
  <behavior>
    - Test 1: provider selection resolves from settings and defaults to a no-op/disabled-safe mode when email is not configured
    - Test 2: typed outbound requests stay single-recipient and include explicit tenant context for later audit/reconciliation work
    - Test 3: Mailgun settings expose only non-secret config in repr or safe metadata helpers
  </behavior>
  <action>
    1. Add application settings for email provider selection plus Mailgun domain, sender, region/base URL, and API key
    2. Define typed request/render/provider contracts for transactional email
    3. Build a provider factory seam that chooses the configured provider without coupling invite-domain code to Mailgun
    4. Add focused unit tests for settings resolution and secret-safe provider metadata
  </action>
  <verify>
    <automated>uv run pytest tests/unit/test_email_provider_factory.py -x -q</automated>
  </verify>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add code-owned transactional template rendering</name>
  <files>app/services/email_types.py, app/services/email_templates.py, tests/unit/test_email_templates.py</files>
  <behavior>
    - Test 1: supported templates render both HTML and plain-text bodies from shared structured data
    - Test 2: template rendering remains deterministic and does not require provider-hosted assets
    - Test 3: rendered output includes the template key and single-recipient addressing semantics needed downstream
  </behavior>
  <action>
    1. Add a template registry keyed by transactional template identifiers
    2. Implement repo-owned renderers for HTML and text output
    3. Keep rendering pure and testable so later queue jobs can render without network access
    4. Add focused unit coverage for invite-oriented template rendering
  </action>
  <verify>
    <automated>uv run pytest tests/unit/test_email_templates.py -x -q</automated>
  </verify>
</task>

</tasks>

<verification>
- `uv run pytest tests/unit/test_email_provider_factory.py tests/unit/test_email_templates.py -x -q`
</verification>

<output>
After completion, create `.planning/phases/95-provider-foundation-secret-hygiene/95-01-SUMMARY.md`
</output>
