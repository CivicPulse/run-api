---
phase: 98-zitadel-delivery-setup-support-boundary
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .env.example
  - docker-compose.yml
  - docs/getting-started-admin.md
  - docs/zitadel-email-runbook.md
autonomous: true
requirements:
  - ZIT-01
  - ZIT-02
must_haves:
  truths:
    - "ZITADEL auth/system email can be configured through repo-documented SMTP settings without routing through CivicPulse runtime code."
    - "Operators have a concrete runbook for sender alignment, prerequisites, and smoke tests."
    - "Support can distinguish CivicPulse invite-email incidents from ZITADEL auth-email incidents."
  artifacts:
    - path: "docker-compose.yml"
      provides: "Optional ZITADEL DefaultInstance SMTP env passthrough"
    - path: ".env.example"
      provides: "Documented ZITADEL SMTP input variables"
    - path: "docs/zitadel-email-runbook.md"
      provides: "Operational boundary and smoke-test runbook"
---

<objective>
Wire concrete self-hosted ZITADEL SMTP configuration into the repo's environment model and document the operational boundary between ZITADEL-owned auth/system mail and CivicPulse-owned invite mail.
</objective>

<verification>
- `docker compose config >/tmp/run-api-docker-compose.rendered.yml`
- `rg -n "ZITADEL_SMTP_|DefaultInstance.SMTPConfiguration|Ownership Boundary|password reset|invite mail" .env.example docker-compose.yml docs/getting-started-admin.md docs/zitadel-email-runbook.md`
</verification>
