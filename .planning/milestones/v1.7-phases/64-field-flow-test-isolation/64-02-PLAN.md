---
phase: 64-field-flow-test-isolation
plan: 02
type: execute
wave: 2
depends_on:
  - 64-01
files_modified:
  - web/scripts/run-e2e.sh
  - .planning/REQUIREMENTS.md
autonomous: true
requirements:
  - E2E-20
must_haves:
  truths:
    - "FIELD-07 passes when executed in multiple relative positions with surrounding field-flow tests."
    - "Order-isolation verification is executable through a single strict command."
    - "Phase traceability reflects FIELD-07 order-isolation closure for E2E-20."
  artifacts:
    - path: "web/scripts/run-e2e.sh"
      provides: "strict FIELD-07 order matrix command mode"
      contains: "--strict-phase64-field07-order"
    - path: ".planning/REQUIREMENTS.md"
      provides: "updated E2E-20 traceability notes for Phase 64 order isolation"
      contains: "E2E-20"
  key_links:
    - from: "web/scripts/run-e2e.sh"
      to: "web/e2e/field-mode.volunteer.spec.ts"
      via: "targeted grep invocations for FIELD-07 and adjacent field tests"
      pattern: "FIELD-07"
    - from: ".planning/REQUIREMENTS.md"
      to: "Phase 64"
      via: "traceability/status row update"
      pattern: "Phase 64"
---

<objective>
Add a strict permutation verification gate that proves FIELD-07 order independence and record requirement traceability.

Purpose: satisfy locked verification bar D-07 and D-08 with repeatable automated execution.
Output: strict wrapper mode and updated requirement traceability.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/64-field-flow-test-isolation/64-CONTEXT.md
@.planning/phases/64-field-flow-test-isolation/64-RESEARCH.md
@web/scripts/run-e2e.sh
@web/e2e/field-mode.volunteer.spec.ts

<interfaces>
From `web/scripts/run-e2e.sh`:
```bash
run_phase63_no_skip_gate() {
  CMD="npx playwright test ... --grep \"PB-10\""
  CMD="npx playwright test ... --grep \"FIELD-08|FIELD-09|FIELD-10\""
}
```

This is the pattern to mirror for a new strict phase-64 gate mode.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add strict FIELD-07 order-matrix mode to run-e2e wrapper</name>
  <files>web/scripts/run-e2e.sh</files>
  <read_first>
    - web/scripts/run-e2e.sh
    - web/e2e/field-mode.volunteer.spec.ts
    - .planning/phases/64-field-flow-test-isolation/64-CONTEXT.md
  </read_first>
  <action>Add new flag and mode `--strict-phase64-field07-order` in `run-e2e.sh` that runs a broad but targeted permutation matrix per D-07 and D-08: (1) FIELD-07 only, (2) FIELD-03..07 sequence, (3) FIELD-08..10 then FIELD-07, and (4) FIELD-07 then FIELD-08..10. Use `--workers 1` and explicit `--project=volunteer` to keep deterministic ordering. Fail the mode if any targeted run exits non-zero or if FIELD-07 is skipped in any run.</action>
  <acceptance_criteria>
    - `web/scripts/run-e2e.sh` contains `--strict-phase64-field07-order` in argument parsing/help text.
    - Script defines a dedicated function for phase-64 strict order checks.
    - Strict mode exits with non-zero status when skips are detected for FIELD-07.
  </acceptance_criteria>
  <verify>
    <automated>cd web && ./scripts/run-e2e.sh --strict-phase64-field07-order</automated>
  </verify>
  <done>Single strict command proves FIELD-07 order-independence behavior across required permutations.</done>
</task>

<task type="auto">
  <name>Task 2: Update E2E-20 traceability after strict order gate wiring</name>
  <files>.planning/REQUIREMENTS.md</files>
  <read_first>
    - .planning/REQUIREMENTS.md
    - .planning/ROADMAP.md
    - .planning/phases/64-field-flow-test-isolation/64-CONTEXT.md
  </read_first>
  <action>Update `REQUIREMENTS.md` E2E-20 traceability and status wording to reflect Phase 64 FIELD-07 isolation closure: deterministic per-test fixture ownership, survey-present deterministic assertions, and strict order matrix verification command. Do not alter unrelated requirement rows.</action>
  <acceptance_criteria>
    - `REQUIREMENTS.md` contains updated E2E-20 wording referencing Phase 64 isolation outcomes.
    - E2E-20 traceability row points to Phase 64 as closure for this requirement path.
  </acceptance_criteria>
  <verify>
    <automated>cd web && ./scripts/run-e2e.sh field-mode.volunteer.spec.ts --project=volunteer --grep "FIELD-03|FIELD-04|FIELD-05|FIELD-06|FIELD-07|FIELD-08|FIELD-09|FIELD-10"</automated>
  </verify>
  <done>Requirement traceability reflects the implemented verification bar and aligned field-flow evidence.</done>
</task>

</tasks>

<verification>
Execute `--strict-phase64-field07-order`; confirm the wrapper reports pass with no FIELD-07 skips and no non-zero run in matrix permutations.
</verification>

<success_criteria>
- Strict mode exists and is runnable with one command.
- Permutation verification includes FIELD-07 before and after adjacent field flows.
- E2E-20 traceability reflects Phase 64 closure details.
</success_criteria>

<output>
After completion, create `.planning/phases/64-field-flow-test-isolation/64-02-SUMMARY.md`
</output>
