---
phase: 64-field-flow-test-isolation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - web/e2e/helpers.ts
  - web/e2e/field-mode.volunteer.spec.ts
autonomous: true
requirements:
  - E2E-20
must_haves:
  truths:
    - "FIELD-07 always runs against spec-owned disposable canvassing data, not shared seed state."
    - "FIELD-07 always exercises inline survey-present behavior and fails if survey UI is absent."
    - "FIELD-07 setup explicitly resets backend fixture ownership and client-side persistence before assertions."
  artifacts:
    - path: "web/e2e/helpers.ts"
      provides: "Disposable FIELD-07 fixture builder for walk-list/canvassing/survey entities"
      contains: "createDisposableCanvassingSurveyFixture"
    - path: "web/e2e/field-mode.volunteer.spec.ts"
      provides: "Deterministic FIELD-07 flow with strict survey-present assertions"
      contains: "FIELD-07"
  key_links:
    - from: "web/e2e/field-mode.volunteer.spec.ts"
      to: "web/e2e/helpers.ts"
      via: "FIELD-07 fixture setup call"
      pattern: "createDisposableCanvassingSurveyFixture"
    - from: "web/e2e/field-mode.volunteer.spec.ts"
      to: "inline survey UI"
      via: "Supporter/contact outcome triggering survey sheet"
      pattern: "getByText\(/survey|question/i\)"
---

<objective>
Isolate FIELD-07 from cross-spec data exhaustion by introducing disposable fixtures and a strict survey-present assertion path.

Purpose: eliminate test-order side effects and make FIELD-07 deterministic per locked decisions D-01 through D-06.
Output: reusable fixture helper(s) plus FIELD-07 refactor in the field mode spec.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/64-field-flow-test-isolation/64-CONTEXT.md
@.planning/phases/64-field-flow-test-isolation/64-RESEARCH.md
@web/e2e/helpers.ts
@web/e2e/field-mode.volunteer.spec.ts

<interfaces>
From `web/e2e/helpers.ts`:
```ts
export async function apiPost(page: Page, url: string, data?: unknown, timeout = 60_000): Promise<APIResponse>
export async function apiPatch(page: Page, url: string, data?: unknown, timeout = 60_000): Promise<APIResponse>
export async function apiGet(page: Page, url: string, timeout = 60_000): Promise<APIResponse>
```

From `web/e2e/field-mode.volunteer.spec.ts`:
```ts
test("FIELD-07: canvassing with inline survey ...", async () => { ... })
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add disposable FIELD-07 canvassing+survey fixture helper</name>
  <files>web/e2e/helpers.ts</files>
  <read_first>
    - web/e2e/helpers.ts
    - web/e2e/field-mode.volunteer.spec.ts
    - .planning/phases/64-field-flow-test-isolation/64-CONTEXT.md
  </read_first>
  <action>Create `createDisposableCanvassingSurveyFixture(page, campaignId, volunteerUserId, labelPrefix)` in `web/e2e/helpers.ts` that provisions spec-owned entities for FIELD-07 per D-01 and D-02: disposable walk-list path with assignable canvassing entries and a known attached survey per D-03. Use existing authenticated API helper pattern (no raw fetch) and return IDs/names consumed by FIELD-07. Keep this helper disposable and independent from long-lived seed sessions per D-09.</action>
  <acceptance_criteria>
    - `web/e2e/helpers.ts` contains `export async function createDisposableCanvassingSurveyFixture(`.
    - Helper return type includes identifiers for FIELD-07 setup (fixture IDs or names used by spec assertions).
    - Helper implementation uses existing API helpers (`apiPost`/`apiPatch`/`apiGet`) rather than direct untyped request calls.
  </acceptance_criteria>
  <verify>
    <automated>cd web && npx playwright test field-mode.volunteer.spec.ts --project=volunteer --grep "FIELD-07" --list</automated>
  </verify>
  <done>Disposable fixture helper exists and can be imported by FIELD-07 without additional codebase exploration.</done>
</task>

<task type="auto">
  <name>Task 2: Refactor FIELD-07 to strict survey-present deterministic flow</name>
  <files>web/e2e/field-mode.volunteer.spec.ts</files>
  <read_first>
    - web/e2e/field-mode.volunteer.spec.ts
    - web/e2e/helpers.ts
    - .planning/phases/64-field-flow-test-isolation/64-CONTEXT.md
  </read_first>
  <action>Rewrite FIELD-07 setup to consume only `createDisposableCanvassingSurveyFixture` data (D-01, D-02), explicitly reset client persistence before the test (remove `canvassing-store` and tour-local storage keys) per D-05 and D-06, and remove survey-absent fallback/skip behavior. FIELD-07 must assert survey-present path only (D-03, D-04): trigger contact outcome, confirm inline survey UI appears, submit at least one answer, and fail if survey is missing.</action>
  <acceptance_criteria>
    - `FIELD-07` block contains no `test.skip` branch for missing survey.
    - FIELD-07 explicitly clears canvassing and tour-local persistence before assertion path.
    - FIELD-07 asserts inline survey visibility and submission behavior on contact outcome.
  </acceptance_criteria>
  <verify>
    <automated>cd web && ./scripts/run-e2e.sh field-mode.volunteer.spec.ts --project=volunteer --grep "FIELD-07"</automated>
  </verify>
  <done>FIELD-07 behavior is deterministic, survey-present only, and independent from prior field spec consumption.</done>
</task>

</tasks>

<verification>
Run the targeted FIELD-07 command twice consecutively; both runs must pass with no skip path.
</verification>

<success_criteria>
- FIELD-07 no longer depends on mutable shared walk-list state.
- Survey-present path is the only accepted assertion path for FIELD-07.
- Client reset boundary required by D-05/D-06 is visible in the spec.
</success_criteria>

<output>
After completion, create `.planning/phases/64-field-flow-test-isolation/64-01-SUMMARY.md`
</output>
