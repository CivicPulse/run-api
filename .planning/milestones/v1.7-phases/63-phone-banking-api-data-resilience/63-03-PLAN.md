---
phase: 63-phone-banking-api-data-resilience
plan: 03
type: execute
wave: 3
depends_on:
  - 63-01
  - 63-02
files_modified:
  - web/scripts/run-e2e.sh
  - .github/workflows/pr.yml
  - .planning/REQUIREMENTS.md
autonomous: true
requirements:
  - E2E-15
must_haves:
  truths:
    - "Local and CI targeted verification fails if PB-10 or FIELD-08/09/10 are skipped."
    - "Phase 63 verification commands explicitly gate on no-skip behavior for affected tests."
    - "Requirement traceability reflects that E2E-15 is now enforced by executable no-skip verification."
  artifacts:
    - path: "web/scripts/run-e2e.sh"
      provides: "local strict no-skip gate for targeted phone-banking specs"
      contains: "skip-detection failure path"
    - path: ".github/workflows/pr.yml"
      provides: "CI strict no-skip gate for PB-10 and FIELD-08/09/10"
      contains: "targeted e2e command + skip check"
    - path: ".planning/REQUIREMENTS.md"
      provides: "updated E2E-15 status/traceability after strict verification is in place"
      contains: "E2E-15"
  key_links:
    - from: ".github/workflows/pr.yml"
      to: "web/scripts/run-e2e.sh"
      via: "CI invokes local wrapper with strict behavior"
      pattern: "run-e2e\.sh"
    - from: "web/scripts/run-e2e.sh"
      to: "Playwright JSON/CLI output"
      via: "parse/report skip count and exit non-zero when skip>0"
      pattern: "skip|skipped|json"
---

<objective>
Enforce a strict no-skip verification bar for PB-10 and FIELD-08/09/10 in local and CI paths.

Purpose: Satisfy D-09 by turning skipped affected tests into hard regressions instead of tolerated behavior.
Output: Runner/workflow gating plus requirement traceability updates anchored to executable checks.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/63-phone-banking-api-data-resilience/63-CONTEXT.md
@.planning/phases/63-phone-banking-api-data-resilience/63-RESEARCH.md
@.planning/phases/62-ci-auth-policy-automation/62-02-SUMMARY.md
@web/scripts/run-e2e.sh
@.github/workflows/pr.yml
@.planning/REQUIREMENTS.md

<interfaces>
From web/scripts/run-e2e.sh:
```bash
./scripts/run-e2e.sh <spec> [playwright args...]
```

From .github/workflows/pr.yml:
```yaml
- name: Run E2E tests
  run: npx playwright test --shard=${{ matrix.shardIndex }}/${{ matrix.shardTotal }}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add strict no-skip gating command path for targeted phone-banking specs</name>
  <files>web/scripts/run-e2e.sh</files>
  <action>Extend `web/scripts/run-e2e.sh` with a strict mode command path for Phase 63 targeted specs that executes PB-10 and FIELD-08/09/10 commands and fails if any of those tests are skipped (per D-09). Implement using Playwright reporter output parsing or deterministic log parsing in the wrapper; skip count > 0 must return non-zero with explicit message referencing PB-10/FIELD-08/09/10. Keep existing default runner behavior unchanged for non-strict runs.</action>
  <verify>
    <automated>cd web && ./scripts/run-e2e.sh --help</automated>
  </verify>
  <done>Local runner supports an explicit strict no-skip gate for affected Phase 63 tests and exits non-zero on any skip.</done>
</task>

<task type="auto">
  <name>Task 2: Wire CI to run targeted strict no-skip verification and update requirement traceability</name>
  <files>.github/workflows/pr.yml, .planning/REQUIREMENTS.md</files>
  <action>Add/adjust CI step(s) in `.github/workflows/pr.yml` to invoke the strict Phase 63 targeted gate from `run-e2e.sh` so PB-10 and FIELD-08/09/10 are validated as non-skipped in automation (D-09). Then update `.planning/REQUIREMENTS.md` to mark E2E-15 as complete once this strict gate and deterministic fixtures are in place, with traceability pointing to Phase 63.</action>
  <verify>
    <automated>rg -n "PB-10|FIELD-08|FIELD-09|FIELD-10|no-skip|strict" .github/workflows/pr.yml web/scripts/run-e2e.sh .planning/REQUIREMENTS.md</automated>
  </verify>
  <done>CI/local verification treats skips for affected tests as regressions and E2E-15 traceability is updated accordingly.</done>
</task>

</tasks>

<verification>
Confirm strict gate exists in both local wrapper and CI workflow, and requirement mapping reflects completion after enforcement.
</verification>

<success_criteria>
- Any skip in PB-10 or FIELD-08/09/10 fails targeted local/CI verification.
- Phase 63 closes INT-02 and BUG-01 with executable no-skip enforcement.
- E2E-15 is updated to complete with explicit traceability to Phase 63 outputs.
</success_criteria>

<output>
After completion, create `.planning/phases/63-phone-banking-api-data-resilience/63-03-SUMMARY.md`
</output>
