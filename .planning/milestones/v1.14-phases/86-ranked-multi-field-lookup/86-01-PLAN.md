# 86-01 Plan

## Goal

Ship ranked multi-field lookup on top of the search surface without breaking filter composition or cursor stability.

## Tasks

1. Replace the raw name-only search clause with search-surface filtering.
2. Add cross-field rank expressions for exact, prefix, contains, and trigram-fuzzy matches.
3. Extend cursor encoding/decoding to carry the search rank metadata needed for stable pagination.

## Verification

- `uv run pytest tests/unit/test_voter_search.py tests/unit/test_api_voters.py -q`
