# 87-01 Plan

## Goal

Refine the voter page so lookup is the default interaction and duplicate names are easier to disambiguate.

## Tasks

1. Preserve prior search data during refetches and pass the query abort signal through `useVoterSearch`.
2. Show explicit searching/result/no-match messaging while lookup is active.
3. Add secondary context under each voter name row and invalidate voter-list queries after contact edits.

## Verification

- `npm --prefix web test -- --run 'src/routes/campaigns/$campaignId/voters/index.test.tsx' 'src/hooks/useVoterContacts.test.ts' 'src/hooks/useVoters.test.ts'`
