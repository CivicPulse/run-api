# 85-01 Plan

## Goal

Create the campaign-scoped search surface and wire freshness updates into every relevant write path.

## Tasks

1. Add the `voter_search_records` model and migration, including RLS, grants, backfill, and trigram indexes.
2. Implement a refresh service that rebuilds denormalized search records from voters, phones, emails, and addresses.
3. Refresh search records after voter edits, contact edits, and import writes.

## Verification

- `uv run pytest tests/unit/test_voter_search.py tests/unit/test_voter_contacts.py tests/unit/test_import_service.py -q`
- `uv run python -m compileall app alembic/versions/028_voter_search_surface.py`
