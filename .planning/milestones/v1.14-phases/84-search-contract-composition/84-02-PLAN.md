# 84-02 Plan

## Goal

Expose lookup directly on the voter page while keeping it inside the existing filter/search flow.

## Tasks

1. Add a dedicated free-text lookup input to the voter page without requiring the advanced filter builder to open first.
2. Keep lookup composed with structured filters through the existing `VoterSearchBody` and `useVoterSearch` flow.
3. Make clear-search restore the prior browse/filter state cleanly while resetting pagination.

## Verification

- `npm --prefix web test -- --run 'src/routes/campaigns/$campaignId/voters/index.test.tsx'`
