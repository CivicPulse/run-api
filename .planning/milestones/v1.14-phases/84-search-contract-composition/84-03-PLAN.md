# 84-03 Plan

## Goal

Lock Phase 84 with regression coverage that proves the search contract and voter-page behavior stay stable.

## Tasks

1. Add unit coverage for the new lookup cursor/search contract and any normalization behavior introduced in the service layer.
2. Extend voter-page route tests to assert lookup state is sent through the existing search flow and can be cleared without stale results.
3. Verify Phase 84 artifacts and state updates are consistent with the roadmap requirements.

## Verification

- `uv run pytest tests/unit/test_voter_search.py tests/unit/test_api_voters.py -q`
- `npm --prefix web test -- --run 'src/routes/campaigns/$campaignId/voters/index.test.tsx'`
