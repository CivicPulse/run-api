---
phase: 49-procrastinate-integration-worker-infrastructure
plan: 03
type: execute
wave: 2
depends_on: ["49-01"]
files_modified:
  - scripts/worker.py
  - docker-compose.yml
  - k8s/apps/run-api-dev/worker-deployment.yaml
  - k8s/apps/run-api-prod/worker-deployment.yaml
  - Dockerfile
autonomous: true
requirements: [MEMD-02]

must_haves:
  truths:
    - "Worker script starts, initializes Sentry and logging, connects to PostgreSQL, and runs the Procrastinate worker loop"
    - "Worker exposes an HTTP health endpoint on port 8001 at /healthz for K8s probes"
    - "Worker runs as a separate Docker Compose service using the same image with command python scripts/worker.py"
    - "Worker has K8s Deployment manifests for both dev and prod namespaces with liveness/readiness probes"
  artifacts:
    - path: "scripts/worker.py"
      provides: "Standalone Procrastinate worker entrypoint with health endpoint"
      contains: "run_worker_async"
    - path: "docker-compose.yml"
      provides: "Worker service definition sharing same image as API"
      contains: "run-api-worker"
    - path: "k8s/apps/run-api-dev/worker-deployment.yaml"
      provides: "K8s Deployment for worker in civpulse-dev namespace"
      contains: "run-api-worker"
    - path: "k8s/apps/run-api-prod/worker-deployment.yaml"
      provides: "K8s Deployment for worker in civpulse-prod namespace"
      contains: "run-api-worker"
  key_links:
    - from: "scripts/worker.py"
      to: "app/tasks/procrastinate_app.py"
      via: "imports procrastinate_app, calls open_async and run_worker_async"
      pattern: "procrastinate_app\\.run_worker_async"
    - from: "docker-compose.yml"
      to: "scripts/worker.py"
      via: 'command: ["python", "scripts/worker.py"]'
      pattern: "scripts/worker\\.py"
    - from: "k8s/apps/run-api-dev/worker-deployment.yaml"
      to: "scripts/worker.py"
      via: 'command: ["python", "scripts/worker.py"]'
      pattern: "scripts/worker\\.py"
---

<objective>
Create the standalone worker script with health endpoint, add Docker Compose worker service, and create K8s Deployment manifests for dev and prod.

Purpose: Deploy the Procrastinate worker as an independent container that survives API pod restarts and has proper health monitoring.
Output: `scripts/worker.py`, updated `docker-compose.yml`, new `k8s/apps/run-api-dev/worker-deployment.yaml` and `k8s/apps/run-api-prod/worker-deployment.yaml`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/49-procrastinate-integration-worker-infrastructure/49-CONTEXT.md
@.planning/phases/49-procrastinate-integration-worker-infrastructure/49-RESEARCH.md
@.planning/phases/49-procrastinate-integration-worker-infrastructure/49-01-SUMMARY.md

@scripts/dev-entrypoint.sh
@docker-compose.yml
@k8s/apps/run-api-dev/deployment.yaml
@k8s/apps/run-api-prod/deployment.yaml
@Dockerfile
@app/core/sentry.py
@app/core/config.py

<interfaces>
<!-- From plan 01 outputs -->

From app/tasks/procrastinate_app.py (created in plan 01):
```python
procrastinate_app = procrastinate.App(
    connector=procrastinate.PsycopgConnector(
        conninfo=_make_conninfo(),
        kwargs={"options": "-c search_path=procrastinate,public"},
    ),
    import_paths=["app.tasks.import_task"],
)
```

From app/core/sentry.py:
```python
def init_sentry() -> None:
    """Initialize Sentry SDK if SENTRY_DSN is configured; no-op otherwise."""
```

Docker Compose api service pattern (shares same image, volumes, env):
```yaml
api:
  build:
    context: .
    target: dev
  command: bash /home/app/scripts/dev-entrypoint.sh
  container_name: run-api-api
  env_file: [.env, {path: .env.zitadel, required: false}]
  environment:
    DATABASE_URL: postgresql+asyncpg://postgres:postgres@postgres:5432/run_api
    S3_ENDPOINT_URL: http://minio:9000
  volumes: [./app:/home/app/app, ./scripts:/home/app/scripts, ...]
  depends_on: {postgres: {condition: service_healthy}, ...}
```

K8s dev Deployment pattern:
```yaml
apiVersion: apps/v1
kind: Deployment
metadata:
  name: run-api
  namespace: civpulse-dev
spec:
  replicas: 1
  template:
    spec:
      containers:
        - name: run-api
          image: ghcr.io/civicpulse/run-api:sha-778e907
          envFrom: [{configMapRef: run-api-config}, {secretRef: run-api-secret}]
          livenessProbe: {httpGet: {path: /health/live, port: 8000}}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create worker entrypoint script with HTTP health endpoint</name>
  <files>scripts/worker.py</files>
  <read_first>
    app/tasks/procrastinate_app.py,
    app/core/sentry.py,
    app/core/config.py,
    scripts/dev-entrypoint.sh
  </read_first>
  <action>
    Create `scripts/worker.py` per D-03 and D-04.

    Per D-03: The worker starts via a programmatic script that initializes Sentry + structlog, creates the ProcrastinateApp with PsycopgConnector, and calls `run_worker_async()`.

    Per D-04: Worker exposes an HTTP health endpoint at `:8001/healthz` for K8s liveness/readiness probes. Use raw `asyncio.start_server` with a minimal HTTP response handler to avoid adding new dependencies (no aiohttp needed).

    Create `scripts/worker.py` with the following content:

    ```python
    """Procrastinate worker entrypoint.

    Initializes Sentry + logging, starts a minimal HTTP health endpoint
    on port 8001, then runs the async Procrastinate worker loop.

    Run via: python scripts/worker.py
    """
    from __future__ import annotations

    import asyncio
    import signal
    import sys
    from pathlib import Path

    # Ensure the project root is on sys.path so `app.*` imports work
    # when running as `python scripts/worker.py` from the repo root.
    _project_root = str(Path(__file__).resolve().parent.parent)
    if _project_root not in sys.path:
        sys.path.insert(0, _project_root)

    from loguru import logger

    from app.core.sentry import init_sentry


    # ---------------------------------------------------------------
    # Minimal HTTP health endpoint (D-04)
    # ---------------------------------------------------------------

    HEALTH_PORT = 8001

    async def _handle_health(
        reader: asyncio.StreamReader,
        writer: asyncio.StreamWriter,
    ) -> None:
        """Respond to any HTTP request with 200 OK.

        Only intended for K8s liveness/readiness probes at GET /healthz.
        Reads the full request (up to 4KB) then sends a minimal response.
        """
        try:
            await asyncio.wait_for(reader.read(4096), timeout=5.0)
        except asyncio.TimeoutError:
            pass

        response = (
            "HTTP/1.1 200 OK\r\n"
            "Content-Type: text/plain\r\n"
            "Content-Length: 2\r\n"
            "Connection: close\r\n"
            "\r\n"
            "ok"
        )
        writer.write(response.encode())
        await writer.drain()
        writer.close()
        await writer.wait_closed()


    async def _start_health_server() -> asyncio.Server:
        """Start the TCP health server on HEALTH_PORT."""
        server = await asyncio.start_server(
            _handle_health, "0.0.0.0", HEALTH_PORT
        )
        logger.info("Health endpoint listening on :{}", HEALTH_PORT)
        return server


    # ---------------------------------------------------------------
    # Worker main
    # ---------------------------------------------------------------

    async def main() -> None:
        """Initialize observability, start health server, run worker."""
        init_sentry()
        logger.info("Starting Procrastinate import worker")

        # Import here (after sys.path is set) to avoid import errors
        from app.tasks.procrastinate_app import procrastinate_app

        health_server = await _start_health_server()

        try:
            async with procrastinate_app.open_async():
                logger.info("Procrastinate connected, waiting for jobs")
                await procrastinate_app.run_worker_async(
                    queues=["imports"],
                    name="import-worker",
                )
        finally:
            health_server.close()
            await health_server.wait_closed()
            logger.info("Worker shut down")


    if __name__ == "__main__":
        try:
            asyncio.run(main())
        except KeyboardInterrupt:
            logger.info("Worker interrupted")
    ```

    Key details:
    - `sys.path` manipulation ensures `app.*` imports work when running as a standalone script (same pattern as existing `scripts/seed.py` and `scripts/bootstrap-zitadel.py`)
    - Health endpoint uses raw `asyncio.start_server` — no new dependencies needed
    - `run_worker_async` with `queues=["imports"]` limits the worker to the imports queue
    - `name="import-worker"` provides worker identification in Procrastinate's internal tracking
    - The health server starts BEFORE `open_async` so K8s can see the port is listening even during connection setup
    - Graceful shutdown: `finally` block closes the health server
  </action>
  <verify>
    <automated>uv run ruff check scripts/worker.py && uv run python -c "import ast; ast.parse(open('scripts/worker.py').read()); print('syntax ok')"</automated>
  </verify>
  <acceptance_criteria>
    - `scripts/worker.py` exists
    - `scripts/worker.py` contains `from app.core.sentry import init_sentry`
    - `scripts/worker.py` contains `from app.tasks.procrastinate_app import procrastinate_app`
    - `scripts/worker.py` contains `procrastinate_app.run_worker_async(`
    - `scripts/worker.py` contains `queues=["imports"]`
    - `scripts/worker.py` contains `HEALTH_PORT = 8001`
    - `scripts/worker.py` contains `asyncio.start_server(`
    - `scripts/worker.py` contains `if __name__ == "__main__":`
    - `uv run ruff check scripts/worker.py` exits 0
  </acceptance_criteria>
  <done>Worker script created with Sentry init, structlog, Procrastinate worker loop, and HTTP health endpoint on port 8001.</done>
</task>

<task type="auto">
  <name>Task 2: Add Docker Compose worker service and K8s worker Deployment manifests</name>
  <files>
    docker-compose.yml,
    k8s/apps/run-api-dev/worker-deployment.yaml,
    k8s/apps/run-api-prod/worker-deployment.yaml,
    Dockerfile
  </files>
  <read_first>
    docker-compose.yml,
    k8s/apps/run-api-dev/deployment.yaml,
    k8s/apps/run-api-prod/deployment.yaml,
    Dockerfile
  </read_first>
  <action>
    **Part A: Add worker service to `docker-compose.yml`.**

    Insert the following service definition after the `web` service block (before the `postgres` service). The worker shares the same build/image as the API but uses a different command:

    ```yaml
      worker:
        build:
          context: .
          target: dev
        container_name: run-api-worker
        command: ["python", "scripts/worker.py"]
        env_file:
          - .env
          - path: .env.zitadel
            required: false
        environment:
          DATABASE_URL: postgresql+asyncpg://postgres:postgres@postgres:5432/run_api
          DATABASE_URL_SYNC: postgresql+psycopg2://postgres:postgres@postgres:5432/run_api
          S3_ENDPOINT_URL: http://minio:9000
        volumes:
          - ./app:/home/app/app
          - ./scripts:/home/app/scripts
        depends_on:
          postgres:
            condition: service_healthy
          api:
            condition: service_started
        restart: unless-stopped
    ```

    Key details:
    - Same `build.target: dev` and env_file as the api service
    - `command: ["python", "scripts/worker.py"]` overrides the default CMD
    - `depends_on.api.condition: service_started` ensures Alembic migrations (run in the api's dev-entrypoint.sh) complete before the worker starts trying to query Procrastinate tables
    - Volumes mount only `./app` and `./scripts` (not alembic, static, certs, etc. — worker doesn't need those)
    - Both `DATABASE_URL` and `DATABASE_URL_SYNC` are set for consistency (config.py reads both)
    - No port mapping needed — the health endpoint is only for K8s, not Docker Compose dev

    **Part B: Verify Dockerfile needs no changes.**

    Read the existing Dockerfile. The `dev` target already copies `scripts/` into the image:
    ```
    COPY --chown=app:app scripts/ ./scripts/
    ```
    And the `runtime` (prod) target does NOT copy scripts. We need to add `scripts/worker.py` to the runtime stage.

    Add to the Dockerfile `runtime` stage, after the `COPY alembic/` lines and before the `COPY --from=frontend` line:

    ```dockerfile
    # Worker entrypoint script (for K8s worker Deployment)
    COPY scripts/worker.py ./scripts/worker.py
    ```

    This ensures the production image includes the worker script. The existing `dev` stage already has it via `COPY scripts/ ./scripts/`.

    **Part C: Create `k8s/apps/run-api-dev/worker-deployment.yaml`.**

    Mirror the structure of the existing `deployment.yaml` but for the worker:

    ```yaml
    # Worker Deployment — runs Procrastinate background job processor
    apiVersion: apps/v1
    kind: Deployment
    metadata:
      name: run-api-worker
      namespace: civpulse-dev
    spec:
      replicas: 1
      selector:
        matchLabels:
          app: run-api-worker
      template:
        metadata:
          labels:
            app: run-api-worker
        spec:
          automountServiceAccountToken: false
          securityContext:
            runAsNonRoot: true
            runAsUser: 65534
            runAsGroup: 65534
            fsGroup: 65534
            seccompProfile:
              type: RuntimeDefault
          containers:
            - name: worker
              image: ghcr.io/civicpulse/run-api:sha-778e907
              command: ["python", "scripts/worker.py"]
              envFrom:
                - configMapRef:
                    name: run-api-config
                - secretRef:
                    name: run-api-secret
              ports:
                - containerPort: 8001
              livenessProbe:
                httpGet:
                  path: /healthz
                  port: 8001
                initialDelaySeconds: 10
                periodSeconds: 30
                failureThreshold: 3
              readinessProbe:
                httpGet:
                  path: /healthz
                  port: 8001
                initialDelaySeconds: 5
                periodSeconds: 10
                failureThreshold: 3
              resources:
                requests:
                  memory: "128Mi"
                  cpu: "100m"
                limits:
                  memory: "512Mi"
                  cpu: "500m"
              securityContext:
                allowPrivilegeEscalation: false
                readOnlyRootFilesystem: false
                capabilities:
                  drop: ["ALL"]
    ```

    Key differences from the API deployment:
    - `name: run-api-worker` (not `run-api`)
    - `app: run-api-worker` label (not `app: run-api`)
    - `command: ["python", "scripts/worker.py"]` (overrides default CMD)
    - Port 8001 (health endpoint, not 8000)
    - Probes hit `/healthz` on port 8001
    - No initContainers (migrations run in the API pod, not the worker)

    **Part D: Create `k8s/apps/run-api-prod/worker-deployment.yaml`.**

    Same as dev but with `namespace: civpulse-prod` and `ENVIRONMENT: production`:

    ```yaml
    # Worker Deployment — runs Procrastinate background job processor
    apiVersion: apps/v1
    kind: Deployment
    metadata:
      name: run-api-worker
      namespace: civpulse-prod
    spec:
      replicas: 1
      selector:
        matchLabels:
          app: run-api-worker
      template:
        metadata:
          labels:
            app: run-api-worker
        spec:
          automountServiceAccountToken: false
          securityContext:
            runAsNonRoot: true
            runAsUser: 65534
            runAsGroup: 65534
            fsGroup: 65534
            seccompProfile:
              type: RuntimeDefault
          containers:
            - name: worker
              image: ghcr.io/civicpulse/run-api:sha-778e907
              command: ["python", "scripts/worker.py"]
              env:
                - name: ENVIRONMENT
                  value: "production"
              envFrom:
                - configMapRef:
                    name: run-api-config
                - secretRef:
                    name: run-api-secret
              ports:
                - containerPort: 8001
              livenessProbe:
                httpGet:
                  path: /healthz
                  port: 8001
                initialDelaySeconds: 10
                periodSeconds: 30
                failureThreshold: 3
              readinessProbe:
                httpGet:
                  path: /healthz
                  port: 8001
                initialDelaySeconds: 5
                periodSeconds: 10
                failureThreshold: 3
              resources:
                requests:
                  memory: "128Mi"
                  cpu: "100m"
                limits:
                  memory: "512Mi"
                  cpu: "500m"
              securityContext:
                allowPrivilegeEscalation: false
                readOnlyRootFilesystem: false
                capabilities:
                  drop: ["ALL"]
    ```
  </action>
  <verify>
    <automated>grep -q "run-api-worker" docker-compose.yml && grep -q 'command:.*scripts/worker.py' docker-compose.yml && grep -q "run-api-worker" k8s/apps/run-api-dev/worker-deployment.yaml && grep -q "civpulse-dev" k8s/apps/run-api-dev/worker-deployment.yaml && grep -q "run-api-worker" k8s/apps/run-api-prod/worker-deployment.yaml && grep -q "civpulse-prod" k8s/apps/run-api-prod/worker-deployment.yaml && grep -q "scripts/worker.py" Dockerfile && echo "ALL CHECKS PASSED"</automated>
  </verify>
  <acceptance_criteria>
    - `docker-compose.yml` contains a `worker:` service block
    - `docker-compose.yml` worker service has `container_name: run-api-worker`
    - `docker-compose.yml` worker service has `command: ["python", "scripts/worker.py"]`
    - `docker-compose.yml` worker service has `depends_on.postgres.condition: service_healthy`
    - `docker-compose.yml` worker service has `depends_on.api.condition: service_started`
    - `docker-compose.yml` worker service has `DATABASE_URL: postgresql+asyncpg://postgres:postgres@postgres:5432/run_api`
    - `Dockerfile` runtime stage contains `COPY scripts/worker.py ./scripts/worker.py`
    - `k8s/apps/run-api-dev/worker-deployment.yaml` exists with `namespace: civpulse-dev`
    - `k8s/apps/run-api-dev/worker-deployment.yaml` contains `name: run-api-worker`
    - `k8s/apps/run-api-dev/worker-deployment.yaml` contains `command: ["python", "scripts/worker.py"]`
    - `k8s/apps/run-api-dev/worker-deployment.yaml` contains liveness probe on port 8001 path `/healthz`
    - `k8s/apps/run-api-dev/worker-deployment.yaml` does NOT have initContainers
    - `k8s/apps/run-api-prod/worker-deployment.yaml` exists with `namespace: civpulse-prod`
    - `k8s/apps/run-api-prod/worker-deployment.yaml` contains `name: run-api-worker`
    - `k8s/apps/run-api-prod/worker-deployment.yaml` contains `command: ["python", "scripts/worker.py"]`
  </acceptance_criteria>
  <done>Worker runs as separate container in Docker Compose and K8s (dev + prod) with health probes. Dockerfile includes worker script in production image.</done>
</task>

</tasks>

<verification>
- `docker-compose.yml` has 3 application services: api, web, worker
- `grep -c "run-api-worker" docker-compose.yml` returns at least 1
- Both K8s worker deployment files exist and reference port 8001
- `scripts/worker.py` is syntactically valid Python
- Dockerfile runtime stage includes worker.py
</verification>

<success_criteria>
Worker runs as a separate container in Docker Compose (same image, different command). K8s Deployment manifests exist for dev and prod with proper health probes on port 8001. Dockerfile production stage includes the worker script.
</success_criteria>

<output>
After completion, create `.planning/phases/49-procrastinate-integration-worker-infrastructure/49-03-SUMMARY.md`
</output>
