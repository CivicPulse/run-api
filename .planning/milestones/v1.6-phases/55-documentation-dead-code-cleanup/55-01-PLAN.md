---
phase: 55-documentation-dead-code-cleanup
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/49-procrastinate-integration-worker-infrastructure/49-01-SUMMARY.md
  - .planning/phases/50-per-batch-commits-crash-resilience/50-01-SUMMARY.md
  - app/services/storage.py
  - .planning/phases/52-l2-auto-mapping-completion/52-VERIFICATION.md
autonomous: true
requirements: []
gap_closure: true

must_haves:
  truths:
    - "Phase 49 SUMMARY frontmatter tracks all requirements that Phase 49 satisfied (BGND-01, BGND-02, MEMD-02)"
    - "Phase 50 SUMMARY frontmatter tracks all requirements that Phase 50 satisfied (RESL-01 through RESL-05)"
    - "StorageService has no unused list_objects method"
    - "Phase 52 VERIFICATION.md body text is consistent with its passed frontmatter status"
  artifacts:
    - path: ".planning/phases/49-procrastinate-integration-worker-infrastructure/49-01-SUMMARY.md"
      provides: "Complete requirements-completed frontmatter"
      contains: "requirements-completed: [BGND-01, BGND-02, MEMD-02]"
    - path: ".planning/phases/50-per-batch-commits-crash-resilience/50-01-SUMMARY.md"
      provides: "Complete requirements-completed frontmatter"
      contains: "requirements-completed: [RESL-01, RESL-02, RESL-03, RESL-04, RESL-05]"
    - path: "app/services/storage.py"
      provides: "Clean StorageService with no dead code"
    - path: ".planning/phases/52-l2-auto-mapping-completion/52-VERIFICATION.md"
      provides: "Consistent verification report with passed status throughout"
  key_links: []
---

<objective>
Close 6 tech debt items identified by the v1.6 milestone audit. Fix SUMMARY frontmatter gaps in phases 49 and 50, remove dead `list_objects` method from StorageService, and clean stale body text in Phase 52 VERIFICATION.md to match its `passed` frontmatter status.

Purpose: Ensure all v1.6 planning artifacts are internally consistent and the codebase has no dead code from the import feature implementation.
Output: 4 files updated (2 SUMMARY frontmatter, 1 Python source, 1 VERIFICATION body), all passing lint.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/v1.6-MILESTONE-AUDIT.md
@.planning/phases/55-documentation-dead-code-cleanup/55-CONTEXT.md
@.planning/phases/55-documentation-dead-code-cleanup/55-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix SUMMARY frontmatter and remove dead code</name>
  <files>
    .planning/phases/49-procrastinate-integration-worker-infrastructure/49-01-SUMMARY.md
    .planning/phases/50-per-batch-commits-crash-resilience/50-01-SUMMARY.md
    app/services/storage.py
  </files>
  <read_first>
    .planning/phases/49-procrastinate-integration-worker-infrastructure/49-01-SUMMARY.md
    .planning/phases/50-per-batch-commits-crash-resilience/50-01-SUMMARY.md
    app/services/storage.py
    .planning/v1.6-MILESTONE-AUDIT.md
  </read_first>
  <action>
    Three targeted edits per D-01 (all items are audit-defined with exact files and line numbers):

    **Edit 1: 49-01-SUMMARY.md frontmatter**
    Change line 42 from:
    ```
    requirements-completed: [BGND-02]
    ```
    to:
    ```
    requirements-completed: [BGND-01, BGND-02, MEMD-02]
    ```
    This adds BGND-01 (import returns 202 Accepted, background Procrastinate job) and MEMD-02 (worker as separate container) which were verified as satisfied in 49-VERIFICATION.md but missing from this SUMMARY's frontmatter. BGND-01 is satisfied because this plan created the Procrastinate App singleton and rewritten import task that enables the 202 flow. MEMD-02 is tracked here because the App singleton created in 49-01 is the foundation that 49-03's worker deployment depends on.

    **Edit 2: 50-01-SUMMARY.md frontmatter**
    Change line 42 from:
    ```
    requirements-completed: [RESL-01, RESL-02, RESL-04, RESL-05]
    ```
    to:
    ```
    requirements-completed: [RESL-01, RESL-02, RESL-03, RESL-04, RESL-05]
    ```
    This adds RESL-03 (crash resume from last committed batch). The `last_committed_row` column added in 50-01 is the foundation that enables crash resume — without it, there is no marker for where to resume from. The actual resume logic is in 50-02 (which already lists RESL-03), but 50-01's column is a prerequisite artifact.

    **Edit 3: Remove `list_objects` method from app/services/storage.py**
    Delete the entire `list_objects` method (lines 150-170, the `async def list_objects(self, prefix: str) -> list[str]:` method with its docstring and body). This method was created in Phase 50 Plan 01 for per-batch error file management but was never called — the actual implementation in 50-02 tracked batch error keys in a list during processing rather than listing them from S3 afterward. Zero callers confirmed by grep across app/, tests/, and scripts/ directories.

    After deleting the method, ensure there is exactly one blank line between the `delete_object` method (ends around line 148) and the `delete_objects` method (starts after the deleted block). The class structure should flow: `delete_object` -> blank line -> `delete_objects` -> blank line -> `ensure_bucket`.

    After the edit, run:
    ```bash
    uv run ruff check app/services/storage.py
    uv run ruff format app/services/storage.py
    ```
  </action>
  <verify>
    <automated>grep -n "requirements-completed" .planning/phases/49-procrastinate-integration-worker-infrastructure/49-01-SUMMARY.md | grep "BGND-01, BGND-02, MEMD-02" && grep -n "requirements-completed" .planning/phases/50-per-batch-commits-crash-resilience/50-01-SUMMARY.md | grep "RESL-01, RESL-02, RESL-03, RESL-04, RESL-05" && ! grep -n "list_objects" app/services/storage.py && uv run ruff check app/services/storage.py</automated>
  </verify>
  <acceptance_criteria>
    - 49-01-SUMMARY.md contains exactly `requirements-completed: [BGND-01, BGND-02, MEMD-02]`
    - 50-01-SUMMARY.md contains exactly `requirements-completed: [RESL-01, RESL-02, RESL-03, RESL-04, RESL-05]`
    - app/services/storage.py does NOT contain the string `list_objects` anywhere
    - app/services/storage.py does NOT contain the string `list_objects_v2` anywhere
    - app/services/storage.py does NOT contain the string `get_paginator` anywhere
    - `uv run ruff check app/services/storage.py` exits 0
    - `uv run ruff format --check app/services/storage.py` exits 0
    - `uv run pytest tests/unit/ -x -q` passes (no regressions from dead code removal)
  </acceptance_criteria>
  <done>Phase 49 and 50 SUMMARY frontmatter list all satisfied requirements. StorageService.list_objects is removed with zero regressions.</done>
</task>

<task type="auto">
  <name>Task 2: Clean Phase 52 VERIFICATION.md stale body text</name>
  <files>
    .planning/phases/52-l2-auto-mapping-completion/52-VERIFICATION.md
  </files>
  <read_first>
    .planning/phases/52-l2-auto-mapping-completion/52-VERIFICATION.md
  </read_first>
  <action>
    The frontmatter was previously updated to `status: passed` and `score: 3/3 success criteria verified (SC3 aligned with D-06)` but the body text was never updated to match. Fix 7 stale locations to be consistent with the passed status:

    **Location 1 (line 30):** Change body status line from:
    ```
    **Status:** gaps_found
    ```
    to:
    ```
    **Status:** passed
    ```

    **Location 2 (line 33 area, after the re-verification line):** Change score line from:
    ```
    **Score:** 2/3 success criteria verified
    ```
    to:
    ```
    **Score:** 3/3 success criteria verified (SC3 aligned with D-06)
    ```

    **Location 3 (line 41, SC3 row in Observable Truths table):** Change the SC3 row from showing status `FAILED` with the current reason text, to show the truth text as "The import wizard detects an L2 file from its headers and shows the mapping step with all columns pre-filled and editable (per D-06)" with status `VERIFIED (D-06 aligned)` and reason: "D-06 design decision changed 'skip mapping' to 'show pre-filled mapping step'. ROADMAP and REQUIREMENTS updated to match. Wizard correctly navigates to step 2 with all 47 columns pre-populated."

    **Location 4 (line 124 area, Behavioral Spot-Checks table):** Change the "Wizard skips to preview for L2 files" row from `FAIL` to `PASS (D-06 aligned)`. Update the description to: "Wizard shows pre-filled mapping step for L2 files" and the result to: "Navigates to step 2 with 47/47 columns pre-populated per D-06 design decision".

    **Location 5 (lines 131-134, Requirements Coverage L2MP-03 row):** Change status from `BLOCKED` to `SATISFIED (D-06 aligned)`. Update the evidence text to: "Detection works (format_detected: 'l2' returned, banner displayed). Wizard navigates to step 2 with all columns pre-filled and editable. D-06 design decision changed 'skip mapping' to 'show pre-filled mapping step' and REQUIREMENTS/ROADMAP were updated accordingly."

    **Location 6 (line 140-144 area, Anti-Patterns Found table):** Remove the row that flags step 2 navigation as a Blocker, OR replace the entire row with: File: `—`, Line: `—`, Pattern: `No anti-patterns found`, Severity: `—`, Impact: `—`. The intent is to show no anti-patterns remain after D-06 alignment.

    **Location 7 (lines 166-176, Gaps Summary section):** Replace the entire gaps summary text with:
    ```
    ## Gaps Summary

    **No gaps remain.** All 3 success criteria verified after D-06 requirements alignment.

    SC3 was initially flagged because the wizard navigates to step 2 (Map Columns) instead of skipping directly to step 3 (Preview). Design decision D-06 intentionally changed this behavior to show a pre-filled, reviewable mapping step. ROADMAP success criterion 3 and REQUIREMENTS.md L2MP-03 were updated to match this design intent. The phase goal "L2 voter files import with zero manual column mapping" is fully achieved — users see all 47 columns pre-populated with no manual adjustment needed.
    ```

    Also update the frontmatter `gaps` section: change the gap entry's `status` from `failed` to `closed_by_d06` and update the `reason` field to: "D-06 design decision changed 'skip mapping' to 'show pre-filled mapping step'. ROADMAP SC3 and L2MP-03 updated to match."
  </action>
  <verify>
    <automated>! grep -n "gaps_found" .planning/phases/52-l2-auto-mapping-completion/52-VERIFICATION.md && ! grep -n "BLOCKED" .planning/phases/52-l2-auto-mapping-completion/52-VERIFICATION.md && ! grep -n "2/3 success criteria" .planning/phases/52-l2-auto-mapping-completion/52-VERIFICATION.md && grep -c "D-06" .planning/phases/52-l2-auto-mapping-completion/52-VERIFICATION.md</automated>
  </verify>
  <acceptance_criteria>
    - 52-VERIFICATION.md body does NOT contain the string `gaps_found`
    - 52-VERIFICATION.md body does NOT contain the string `BLOCKED`
    - 52-VERIFICATION.md body does NOT contain the string `2/3 success criteria`
    - 52-VERIFICATION.md body does NOT contain the string `FAILED` (in the SC3 row or behavioral spot-checks)
    - 52-VERIFICATION.md body does NOT contain the string `1 gap blocks`
    - 52-VERIFICATION.md body line 30 area contains `**Status:** passed`
    - 52-VERIFICATION.md body contains `3/3 success criteria verified (SC3 aligned with D-06)`
    - 52-VERIFICATION.md body contains `VERIFIED (D-06 aligned)` in the SC3 truth row
    - 52-VERIFICATION.md body contains `SATISFIED (D-06 aligned)` in the L2MP-03 requirements row
    - 52-VERIFICATION.md body contains `No gaps remain` in the Gaps Summary section
    - The frontmatter `status: passed` is preserved (not changed)
    - The frontmatter `score:` line still reads `3/3 success criteria verified (SC3 aligned with D-06)`
  </acceptance_criteria>
  <done>Phase 52 VERIFICATION.md body text is fully consistent with its passed frontmatter status. No stale gaps_found, FAILED, BLOCKED, or gap-blocking language remains.</done>
</task>

</tasks>

<verification>
All 6 tech debt items from the v1.6 milestone audit are closed:
1. Phase 49 SUMMARY frontmatter: `grep "BGND-01, BGND-02, MEMD-02" .planning/phases/49-*/49-01-SUMMARY.md`
2. Phase 50 SUMMARY frontmatter: `grep "RESL-01, RESL-02, RESL-03, RESL-04, RESL-05" .planning/phases/50-*/50-01-SUMMARY.md`
3. Dead code removed: `! grep "list_objects" app/services/storage.py`
4. VERIFICATION.md consistent: `! grep "gaps_found\|BLOCKED\|FAILED" .planning/phases/52-*/52-VERIFICATION.md`
5-6. Secondary checks (49-02, 49-03, 50-02 already correct per research): No changes needed.
Python linting clean: `uv run ruff check app/services/storage.py` exits 0
Test suite green: `uv run pytest tests/unit/ -x -q` passes
</verification>

<success_criteria>
- All 4 files updated and committed
- Zero occurrences of stale/inconsistent text in modified files
- Ruff linting passes on storage.py
- Unit test suite passes with no regressions
- All 6 audit tech debt items are resolved
</success_criteria>

<output>
After completion, create `.planning/phases/55-documentation-dead-code-cleanup/55-01-SUMMARY.md`
</output>
