# 78-03 Plan

## Goal

Audit the adjacent ownership-check helpers touched by Phase 78 so the same UUID lookup bug class is not left latent in nearby volunteer/shift flows.

## Tasks

1. Scope volunteer tag and availability helpers to campaign ownership.
2. Scope shift-side volunteer helper lookups to the shift campaign before manager assignment, signup cancellation, check-in/out, and hours aggregation.
3. Record any remaining verification blocker explicitly instead of marking the phase complete on partial evidence.

## Verification

- `uv run pytest tests/unit/test_phase78_tenant_containment.py tests/unit/test_volunteers.py tests/unit/test_volunteer_gaps.py -q`
