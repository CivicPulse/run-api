# 79-01 Plan

## Goal

Add reproducible regressions for the Phase 79 launch blockers so error normalization and edge hardening can be verified against the shakedown contracts.

## Tasks

1. Add API tests for the leaked-error cases: duplicate phone, nonexistent foreign keys, deleted-row/offline-drain writes, and malformed cursor handling.
2. Add regression coverage for the expected status mapping (`404`/`409`/`422`) and assert response bodies do not leak SQL, constraint names, stack traces, or service DNS.
3. Add HTTP response tests for the required security headers and any repo-owned redirect behavior or ingress configuration emitted by this phase.

## Verification

- `uv run pytest tests -k "phase79 or security_header or cursor" -q`

