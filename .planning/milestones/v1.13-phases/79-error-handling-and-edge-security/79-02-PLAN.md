# 79-02 Plan

## Goal

Normalize the backend failure modes behind the shakedown P1s into safe, explicit API contracts without widening tenancy or existence leaks.

## Tasks

1. Add reusable app/core error translation for unhandled integrity/driver/value failures so clients receive sanitized problem responses instead of raw 500 payloads.
2. Update the affected routes/services to translate known domain cases correctly: duplicate contact conflicts, missing foreign targets, deleted-row/offline-drain writes, and malformed cursor inputs.
3. Preserve existing happy-path behavior and Phase 78 tenant checks while making the new 4xx responses stable enough for offline clients and operators to reason about.

## Verification

- Re-run the Phase 79 API/unit regressions after the backend mappings land.

