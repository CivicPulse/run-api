# 81-02 Plan

## Goal

Correct the remaining field/mobile launch blockers around touch-target sizing and volunteer field entry routing.

## Tasks

1. Audit the field-mode Resume, Start Over, Back to Hub, and related navigation controls against the intended mobile target-size standard and adjust shared button/link styling where needed.
2. Fix the volunteer post-login landing logic so users with active field assignments land on the correct field entry point instead of the generic org home or an arbitrary campaign.
3. Add focused route and component tests that lock in the mobile sizing and assigned-campaign redirect behavior.

## Verification

- `npm test -- --run web/src/routes/callback.test.tsx web/src/components/field/FieldHeader.test.tsx web/src/routes/field/$campaignId/phone-banking.test.tsx`

