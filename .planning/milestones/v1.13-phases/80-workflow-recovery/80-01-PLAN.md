# 80-01 Plan

## Goal

Fix campaign creation so existing ZITADEL project grants do not break production campaign setup and failures do not leave partial local or remote state behind.

## Tasks

1. Reproduce the campaign-create failure path where the organization already has or resolves an existing ZITADEL project grant.
2. Update the campaign/join/ZITADEL orchestration so project-grant resolution is idempotent and commit/rollback boundaries remain safe when remote role assignment or local persistence fails.
3. Add regression coverage for both the already-existing-grant success path and the failure cleanup/orphan-prevention path.

## Verification

- Re-run focused campaign and join service/API tests covering grant resolution and rollback behavior.
