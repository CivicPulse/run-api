# 80-03 Plan

## Goal

Close the remaining workflow-level validation and contract gaps in volunteer, phone-bank, and survey operations.

## Tasks

1. Reproduce and fix the volunteer signup/cancellation inconsistency, deciding whether the supported contract is idempotent cancel semantics or a narrower explicit state model.
2. Ensure phone-bank session creation fails cleanly for nonexistent `call_list_id`, and verify invalid `result_code` inputs surface as `422` rather than leaking lower-level failures.
3. Tighten survey question reorder validation so partial ID sets fail with `400` while full valid reorder submissions still succeed.

## Verification

- Re-run focused volunteer, phone-bank, and survey API/service regressions for the affected flows.
