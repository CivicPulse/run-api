# 80-02 Plan

## Goal

Restore CSV import confirm so queued jobs advance into real worker execution instead of failing immediately after confirmation.

## Tasks

1. Reproduce the queued-to-failure path across import detect/confirm/worker handoff and identify where state, payload, or worker assumptions drifted.
2. Fix the import confirm/worker contract so confirmed jobs transition from queued to processing/completed with the required pre-scan metadata and durable state updates.
3. Add regression coverage for the confirm path and worker execution handoff so the flow is stable under the shakedown scenario.

## Verification

- Re-run focused import API/unit tests for detect, confirm, and worker execution.
