---
phase: 74-data-integrity-concurrency
plan: 04
type: execute
wave: 3
depends_on: [74-01, 74-02, 74-03]
files_modified:
  - tests/unit/test_shifts.py
  - tests/unit/test_dnc.py
  - tests/unit/test_invite_service.py
  - tests/unit/test_api_members.py
  - tests/integration/test_data_integrity.py
autonomous: true
requirements: [DATA-01, DATA-02, DATA-03, DATA-04, DATA-05, DATA-06, DATA-07, DATA-08]
must_haves:
  truths:
    - "Full `uv run pytest -x` passes across unit + integration suites"
    - "No test regressions from any of Plans 01-03"
    - "Migration 027 round-trip verified one final time against clean DB"
    - "Phase 74 summary documents all closed issues (C9-C13, H18, H19)"
  artifacts:
    - path: ".planning/phases/74-data-integrity-concurrency/74-SUMMARY.md"
      provides: "Phase summary tying together all 4 plans"
  key_links:
    - from: "Full pytest suite"
      to: "All phase 74 requirements"
      via: "green CI signal"
      pattern: "passed"
---

<objective>
Full-phase regression sweep. Run the entire test suite, fix any unit tests broken by Plans 01-03 (expected: mock patches that still reference old signatures), re-verify migration 027 round-trip against a clean DB, and author the phase summary.

Purpose: Plans 01-03 touched services with extensive mocking in existing unit tests. Some mocks may reference attributes or call counts that shifted. This plan sweeps for and repairs any collateral damage before phase close-out.

Output: Green full suite, updated tests where needed, phase summary.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/74-data-integrity-concurrency/74-CONTEXT.md
@.planning/phases/74-data-integrity-concurrency/74-RESEARCH.md
@.planning/phases/74-data-integrity-concurrency/74-01-SUMMARY.md
@.planning/phases/74-data-integrity-concurrency/74-02-SUMMARY.md
@.planning/phases/74-data-integrity-concurrency/74-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run full suite, repair any broken unit tests</name>
  <files>tests/unit/test_shifts.py, tests/unit/test_dnc.py, tests/unit/test_invite_service.py, tests/unit/test_api_members.py, tests/integration/test_data_integrity.py</files>
  <action>
Run the full unit + integration suite:
```
uv run pytest tests/unit/ tests/integration/test_data_integrity.py -x -v 2>&1 | tee /tmp/phase74-test-run.log
```

For each failure:
1. Identify root cause. Most likely categories (per research P2):
   - DNC mocks referencing old per-row SELECT+INSERT sequence (already handled in Plan 02 but verify coverage)
   - Invite service mocks missing `zitadel.remove_project_role` patch (tests that exercise failure branches)
   - members.py transfer_ownership mocks missing new compensation call patches
   - Integration tests flaky due to ordering

2. Fix the test (not production code) by updating mock expectations. Do NOT loosen assertions to hide real bugs — if a production fix broke a test intentionally (old behavior invalid), update the assertion to match new correct behavior.

3. Re-run until green.

Also run the broader suite to catch anything outside these modules:
```
uv run pytest -x --ignore=tests/e2e 2>&1 | tail -50
```

Migration round-trip sanity check on a clean DB:
```
docker compose exec -T api bash -c "cd /home/app && uv run alembic downgrade base && uv run alembic upgrade head"
```

Run ruff across all phase-touched files:
```
uv run ruff check app/services/shift.py app/services/dnc.py app/services/invite.py app/api/v1/members.py app/models/ alembic/versions/027_data_integrity.py tests/unit/test_shifts.py tests/unit/test_dnc.py tests/unit/test_invite_service.py tests/unit/test_api_members.py tests/integration/test_data_integrity.py
```

Integration test reliability: if concurrent tests are flaky (<10/10 pass rate), add a small `await asyncio.sleep(0)` yield or increase iterations to 10. Do NOT weaken assertions.
  </action>
  <verify>
    <automated>uv run pytest tests/unit/ tests/integration/test_data_integrity.py -x 2>&amp;1 | tail -20</automated>
  </verify>
  <done>Full unit + integration suite green. Migration round-trip from base clean. Ruff clean across all phase-touched files.</done>
</task>

<task type="auto">
  <name>Task 2: Author phase 74 summary</name>
  <files>.planning/phases/74-data-integrity-concurrency/74-SUMMARY.md</files>
  <action>
Create `.planning/phases/74-data-integrity-concurrency/74-SUMMARY.md` following the standard summary template. Include:

- **Phase:** 74 Data Integrity & Concurrency
- **Requirements closed:** DATA-01..08
- **Issues closed (from CODEBASE-REVIEW-2026-04-04.md):** C9, C10, C11, C12, C13, H3, H18, H19
- **Files modified** (aggregate from 01/02/03 summaries): app/services/shift.py, app/services/dnc.py, app/services/invite.py, app/api/v1/members.py, app/models/voter_interaction.py, app/models/voter_contact.py, app/models/volunteer.py, app/models/invite.py, alembic/versions/027_data_integrity.py, plus 5 test files
- **Key decisions:** pessimistic SELECT FOR UPDATE (not optimistic), single-migration 027, raw-SQL partial index (Option 1 from research)
- **Follow-ups / deferred:** broader index audit, optimistic locking alternative
- **Patterns established:** compensating-tx pattern now in 3 code paths (join, invite, members); concurrent-test pattern introduced (`asyncio.gather` + separate session per coroutine)
- **Test additions:** 1 new integration file (8 tests) + 4 new unit tests

Update ROADMAP.md phase 74 entry: change `- [ ]` to `- [x]` and append `— completed 2026-04-04` (or today's date).
  </action>
  <verify>
    <automated>test -f .planning/phases/74-data-integrity-concurrency/74-SUMMARY.md &amp;&amp; grep -q "DATA-01" .planning/phases/74-data-integrity-concurrency/74-SUMMARY.md &amp;&amp; grep -q "Phase 74.*completed" .planning/ROADMAP.md</automated>
  </verify>
  <done>74-SUMMARY.md exists and references all 8 DATA-* requirements and all C-issues closed; ROADMAP.md phase 74 marked complete.</done>
</task>

</tasks>

<verification>
- `uv run pytest tests/unit/ tests/integration/test_data_integrity.py -x` green
- `uv run ruff check .` clean
- Migration 027 round-trip from base clean
- 74-SUMMARY.md and ROADMAP.md updated
</verification>

<success_criteria>
- Entire phase 74 test surface green
- No production code changes in this plan (test/doc only)
- Phase 74 formally closed in ROADMAP.md
</success_criteria>

<output>
Phase 74 complete — `.planning/phases/74-data-integrity-concurrency/74-SUMMARY.md` is the artifact.
</output>
