---
phase: 74-data-integrity-concurrency
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - alembic/versions/027_data_integrity.py
  - app/models/voter_interaction.py
  - app/models/voter_contact.py
  - app/models/volunteer.py
  - app/models/invite.py
  - tests/integration/test_data_integrity.py
autonomous: true
requirements: [DATA-04, DATA-05, DATA-06, DATA-07, DATA-08]
must_haves:
  truths:
    - "Migration 027 applies cleanly and reverses cleanly against dev DB"
    - "voter_interactions table has composite indexes on (campaign_id, voter_id) and (campaign_id, created_at)"
    - "VoterEmail rejects duplicate (campaign_id, voter_id, value) rows"
    - "VolunteerTag rejects duplicate (campaign_id, name) rows"
    - "Two pending invites to same (campaign_id, email) are blocked; re-invite after accept/revoke allowed"
    - "Integration test file exists with test stubs covering DATA-01/04/05/06/07"
  artifacts:
    - path: "alembic/versions/027_data_integrity.py"
      provides: "Indexes + unique constraints + partial unique invite index"
      contains: "revision: str = \"027_data_integrity\""
    - path: "tests/integration/test_data_integrity.py"
      provides: "Integration test scaffold for phase 74"
    - path: "app/models/voter_interaction.py"
      provides: "__table_args__ with two composite Indexes"
      contains: "ix_voter_interactions_campaign_voter"
    - path: "app/models/voter_contact.py"
      provides: "VoterEmail __table_args__ unique constraint"
      contains: "uq_voter_email_campaign_voter_value"
    - path: "app/models/volunteer.py"
      provides: "VolunteerTag unique constraint"
      contains: "uq_volunteer_tag_campaign_name"
    - path: "app/models/invite.py"
      provides: "UniqueConstraint removed (replaced by partial index at DB level)"
  key_links:
    - from: "alembic/versions/027_data_integrity.py"
      to: "model __table_args__ (voter_interaction, voter_contact, volunteer, invite)"
      via: "DDL matches ORM metadata"
      pattern: "uq_voter_email_campaign_voter_value|uq_volunteer_tag_campaign_name|ix_voter_interactions_campaign_voter|uq_pending_invite_email_campaign"
    - from: "migration 027"
      to: "revision 026_rls_hardening"
      via: "down_revision chain"
      pattern: "down_revision.*026_rls_hardening"
---

<objective>
Create migration 027_data_integrity.py and update SQLAlchemy models to add composite indexes (voter_interactions), unique constraints (VoterEmail, VolunteerTag), and a partial unique index on pending invites. Lay down the integration test file with stubs for all phase 74 requirements.

Purpose: Wave 0 foundation. Migration and model metadata must exist before subsequent plans write code against them. Also verifies the pre-existing DoNotCallEntry unique constraint (uq_dnc_campaign_phone at app/models/dnc.py:33) which C10 ON CONFLICT depends on.

Output: Migration file, 4 updated models, one new integration test file with function stubs.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/74-data-integrity-concurrency/74-CONTEXT.md
@.planning/phases/74-data-integrity-concurrency/74-RESEARCH.md
@.planning/phases/74-data-integrity-concurrency/74-VALIDATION.md
@app/models/voter_interaction.py
@app/models/voter_contact.py
@app/models/volunteer.py
@app/models/invite.py
@app/models/dnc.py
@alembic/versions/026_rls_hardening.py
@alembic/versions/017_procrastinate_schema.py
@tests/integration/conftest.py

<interfaces>
From app/models/dnc.py:33 (VERIFIED EXISTS — no migration needed for DNC constraint):
```python
__table_args__ = (
    UniqueConstraint("campaign_id", "phone_number", name="uq_dnc_campaign_phone"),
)
```

From alembic/versions/017_procrastinate_schema.py:124-126 (partial-index pattern):
```python
op.execute(
    "CREATE UNIQUE INDEX procrastinate_jobs_queueing_lock_idx_v1 "
    "ON procrastinate_jobs (queueing_lock) WHERE status = 'todo'"
)
```

Current invite unique constraint at app/models/invite.py:22-26 — unconditional, must be dropped:
```python
UniqueConstraint("email", "campaign_id", name="uq_pending_invite_email_campaign")
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update 4 models with new __table_args__ (per D-DATA-04, D-DATA-05, D-DATA-06, D-DATA-07)</name>
  <files>app/models/voter_interaction.py, app/models/voter_contact.py, app/models/volunteer.py, app/models/invite.py</files>
  <action>
Update 4 model files so SQLAlchemy metadata matches the new migration:

1. `app/models/voter_interaction.py` — add `__table_args__` immediately after `__tablename__` line. Add `Index` to sqlalchemy imports if not already present. Two composite indexes per DATA-04:
```python
from sqlalchemy import Index  # add to imports if needed

__table_args__ = (
    Index("ix_voter_interactions_campaign_voter", "campaign_id", "voter_id"),
    Index("ix_voter_interactions_campaign_created", "campaign_id", "created_at"),
)
```

2. `app/models/voter_contact.py` — on `VoterEmail` class (around line 42-59), add `__table_args__` mirroring VoterPhone's pattern at :14-39. Per DATA-06:
```python
__table_args__ = (
    UniqueConstraint(
        "campaign_id", "voter_id", "value",
        name="uq_voter_email_campaign_voter_value",
    ),
)
```
Ensure `UniqueConstraint` is imported from sqlalchemy.

3. `app/models/volunteer.py` — on `VolunteerTag` class (around line 86-97), add `__table_args__`. Per DATA-07:
```python
__table_args__ = (
    UniqueConstraint("campaign_id", "name", name="uq_volunteer_tag_campaign_name"),
)
```
Keep any existing `Index` entries inside the tuple. Ensure `UniqueConstraint` imported.

4. `app/models/invite.py` — DROP the unconditional `UniqueConstraint("email", "campaign_id", name="uq_pending_invite_email_campaign")` at lines 22-26. Per DATA-05 + research Option 1 (matches procrastinate): rely on DB-side partial index only. If removing the tuple leaves `__table_args__` empty, either delete `__table_args__` entirely or keep other existing constraints. Leave a comment pointing to migration 027.

Run `uv run ruff check app/models/` after edits; fix any import warnings. Run `uv run ruff format app/models/voter_interaction.py app/models/voter_contact.py app/models/volunteer.py app/models/invite.py`.
  </action>
  <verify>
    <automated>uv run ruff check app/models/voter_interaction.py app/models/voter_contact.py app/models/volunteer.py app/models/invite.py &amp;&amp; uv run python -c "from app.models.voter_interaction import VoterInteraction; from app.models.voter_contact import VoterEmail; from app.models.volunteer import VolunteerTag; from app.models.invite import Invite; print('ok')"</automated>
  </verify>
  <done>All 4 models import cleanly; ruff passes; VoterInteraction.__table_args__ contains 2 Indexes; VoterEmail/VolunteerTag have UniqueConstraint entries; Invite's unconditional UQ removed.</done>
</task>

<task type="auto">
  <name>Task 2: Create migration 027_data_integrity.py (per D-DATA-08)</name>
  <files>alembic/versions/027_data_integrity.py</files>
  <action>
Create new alembic migration following the skeleton in 74-RESEARCH.md Migration 027 section. Revision ID: `027_data_integrity`. down_revision: `026_rls_hardening`.

`upgrade()` must perform in this order:

1. DATA-04: Create `ix_voter_interactions_campaign_voter` and `ix_voter_interactions_campaign_created` on `voter_interactions` via `op.create_index`.

2. DATA-06 backfill + constraint: First run raw SQL `DELETE FROM voter_emails a USING voter_emails b WHERE a.id > b.id AND a.campaign_id = b.campaign_id AND a.voter_id = b.voter_id AND a.value = b.value` then `op.create_unique_constraint("uq_voter_email_campaign_voter_value", "voter_emails", ["campaign_id", "voter_id", "value"])`.

3. DATA-07 backfill + constraint: `DELETE FROM volunteer_tags a USING volunteer_tags b WHERE a.id > b.id AND a.campaign_id = b.campaign_id AND a.name = b.name` then `op.create_unique_constraint("uq_volunteer_tag_campaign_name", "volunteer_tags", ["campaign_id", "name"])`.

4. DATA-05 partial unique index: `op.drop_constraint("uq_pending_invite_email_campaign", "invites", type_="unique")` then `op.execute("CREATE UNIQUE INDEX uq_pending_invite_email_campaign ON invites (email, campaign_id) WHERE accepted_at IS NULL AND revoked_at IS NULL")`. Raw SQL per D-DATA-05 (matches procrastinate 017 pattern).

`downgrade()` must reverse in opposite order: drop partial index, recreate unconditional UQ, drop volunteer_tag UQ, drop voter_email UQ, drop both voter_interactions indexes.

Include module docstring referencing phase 74 and DATA-04/05/06/07/08.

Verify migration round-trips against dev DB:
```
docker compose exec api bash -c "cd /home/app && uv run alembic upgrade head"
docker compose exec api bash -c "cd /home/app && uv run alembic downgrade -1"
docker compose exec api bash -c "cd /home/app && uv run alembic upgrade head"
```

Confirm the partial index exists via psql:
```
docker compose exec postgres psql -U postgres -d run -c "\d invites" | grep uq_pending_invite
```

Run ruff on the new migration.
  </action>
  <verify>
    <automated>docker compose exec -T api bash -c "cd /home/app &amp;&amp; uv run alembic upgrade head &amp;&amp; uv run alembic downgrade -1 &amp;&amp; uv run alembic upgrade head" &amp;&amp; uv run ruff check alembic/versions/027_data_integrity.py</automated>
  </verify>
  <done>Migration applies and reverses cleanly; partial index visible in \d invites; uq_voter_email and uq_volunteer_tag constraints visible; voter_interactions has 2 new indexes; ruff passes.</done>
</task>

<task type="auto">
  <name>Task 3: Create tests/integration/test_data_integrity.py with stubs (Wave 0 test scaffold)</name>
  <files>tests/integration/test_data_integrity.py</files>
  <action>
Create new integration test file with stub implementations for all phase 74 requirements. Each test marked `@pytest.mark.integration` per D-test-strategy, uses `superuser_engine` fixture from `tests/integration/conftest.py:32-75`.

Required test functions (stubs OK for now — plans 02, 03, 04 will fill in bodies):

1. `test_shift_signup_race_no_overflow(superuser_engine)` — DATA-01. Uses `asyncio.gather` with 2 concurrent signups via independent `async_sessionmaker` sessions. Setup: shift with max_volunteers=1, 2 volunteers. Assert exactly 1 has status SIGNED_UP. Run N=5 iterations per research P6 reliability note. Include `await asyncio.sleep(0)` between coroutine steps to force interleaving.

2. `test_dnc_concurrent_import(superuser_engine)` — DATA-02 (optional per Claude discretion). Stub OK: mark `@pytest.mark.skip(reason="optional — Plan 02 fills in if runtime budget allows")` for now.

3. `test_voter_interactions_indexes_exist(superuser_engine)` — DATA-04. Query `pg_indexes` for `ix_voter_interactions_campaign_voter` and `ix_voter_interactions_campaign_created`; assert both present.

4. `test_reinvite_after_accept_allowed(superuser_engine)` — DATA-05. Insert pending invite, accept it (set accepted_at), insert new pending for same (campaign, email) — should succeed.

5. `test_duplicate_pending_invite_blocked(superuser_engine)` — DATA-05. Insert pending invite, attempt second pending for same (campaign, email) — expect IntegrityError.

6. `test_voter_email_unique_violation(superuser_engine)` — DATA-06. Insert VoterEmail, insert duplicate (campaign_id, voter_id, value) — expect IntegrityError.

7. `test_volunteer_tag_unique_violation(superuser_engine)` — DATA-07. Insert VolunteerTag, insert duplicate (campaign_id, name) — expect IntegrityError.

8. `test_dnc_has_unique_constraint(superuser_engine)` — verifies pre-existing `uq_dnc_campaign_phone` on `do_not_call` table (pre-req for Plan 02 C10 fix). Query `pg_indexes` or `pg_constraint` and assert it exists. If missing, test FAILS and Plan 02 must prepend to migration 027.

Use module-level `pytestmark = pytest.mark.asyncio` or per-function `@pytest.mark.asyncio`. Use `async_sessionmaker(superuser_engine, expire_on_commit=False)` for session creation per research concurrent-test-pattern. Do NOT share sessions across coroutines.

Test bodies should be COMPLETE implementations (not NotImplementedError). These are foundation tests run repeatedly by later plans.

Run the integration suite to confirm all pass after migration 027 applied:
```
uv run pytest tests/integration/test_data_integrity.py -x -v
```
  </action>
  <verify>
    <automated>uv run pytest tests/integration/test_data_integrity.py -x -v --no-header 2>&amp;1 | tail -30</automated>
  </verify>
  <done>All tests in test_data_integrity.py pass (except skipped DNC-concurrent). test_dnc_has_unique_constraint confirms prerequisite. ruff passes on new file.</done>
</task>

</tasks>

<verification>
- Migration 027 upgrades and downgrades round-trip cleanly on dev DB
- All 4 models import cleanly and match migration DDL
- Integration tests pass: shift race, indexes, invites, voter email, volunteer tag, DNC constraint pre-req
- `uv run ruff check .` passes
</verification>

<success_criteria>
- Migration 027 committed and applied
- 4 models updated with new __table_args__
- tests/integration/test_data_integrity.py exists with 8 test functions (7 active + 1 skipped)
- DoNotCallEntry unique constraint verified present (pre-req for C10)
- All tests green
</success_criteria>

<output>
After completion, create `.planning/phases/74-data-integrity-concurrency/74-01-SUMMARY.md`
</output>
