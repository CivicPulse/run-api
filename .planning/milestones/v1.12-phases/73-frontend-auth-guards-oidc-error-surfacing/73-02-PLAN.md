---
phase: 73-frontend-auth-guards-oidc-error-surfacing
plan: 02
type: execute
wave: 2
depends_on: [01]
files_modified:
  - app/api/v1/phone_banks.py
  - app/schemas/phone_banks.py
  - tests/integration/test_phone_banks.py
autonomous: true
requirements: [SEC-12]
must_haves:
  truths:
    - "Server returns current caller's check-in status for a given session"
    - "Endpoint returns 404 when user is not an assigned caller for the session"
    - "Endpoint response distinguishes checked_in=true/false based on check_in_at/check_out_at"
  artifacts:
    - path: app/api/v1/phone_banks.py
      provides: "GET /campaigns/{campaign_id}/phone-bank-sessions/{session_id}/callers/me endpoint"
      contains: "callers/me"
    - path: tests/integration/test_phone_banks.py
      provides: "Integration tests for callers/me endpoint (checked_in, checked_out, not_assigned cases)"
      contains: "check_in_status"
  key_links:
    - from: app/api/v1/phone_banks.py
      to: app/models/phone_bank.py SessionCaller
      via: "SQLAlchemy async query filtered by session_id + user_id"
      pattern: "SessionCaller.*user_id|SessionCaller.*session_id"
---

<objective>
Add the server-side check-in status endpoint that Plan 06 (H26 enforcement) will call from the active calling page. Source of truth is `SessionCaller.check_in_at` / `check_out_at` on the existing model. Implements D-04 (H26 server-side enforcement).

Purpose: Per SEC-12, the client's local `checkedIn` React state is untrusted. Moving the check to a real GET endpoint means direct URL navigation cannot bypass check-in — the call page's gate queries the server every mount.

Output: New `GET .../callers/me` endpoint + pydantic response shape + integration tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/73-frontend-auth-guards-oidc-error-surfacing/73-CONTEXT.md
@.planning/phases/73-frontend-auth-guards-oidc-error-surfacing/73-RESEARCH.md
@app/api/v1/phone_banks.py
@app/schemas/phone_banks.py
@app/models/phone_bank.py
@tests/integration/test_phone_banks.py

<interfaces>
<!-- Existing model: app/models/phone_bank.py SessionCaller -->
<!-- Fields: id, session_id, user_id, check_in_at: datetime|None, check_out_at: datetime|None -->
<!-- "Currently checked in" = check_in_at IS NOT NULL AND check_out_at IS NULL -->

<!-- Existing endpoints in app/api/v1/phone_banks.py (per research lines 129-133): -->
<!-- POST .../sessions/{id}/check-in  → returns SessionCallerResponse -->
<!-- POST .../sessions/{id}/check-out -->
<!-- GET  .../sessions/{id}/callers   → returns list of all callers -->

<!-- Existing schema (likely): app/schemas/phone_banks.py SessionCallerResponse -->
<!-- Research recommends "Option A": new GET .../sessions/{session_id}/callers/me -->
<!-- returning SessionCallerResponse, 404 when user is not an assigned caller. -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add integration tests for GET /sessions/{id}/callers/me</name>
  <files>tests/integration/test_phone_banks.py</files>
  <behavior>
    - Test: logged-in user who is an assigned caller AND currently checked in → 200, body has checked_in=true, check_in_at set, check_out_at null
    - Test: logged-in user who is assigned but has checked out → 200, body has checked_in=false (check_out_at set)
    - Test: logged-in user who is assigned but never checked in → 200, body has checked_in=false, check_in_at null
    - Test: logged-in user who is NOT an assigned caller → 404
    - Test: unauthenticated request → 401 (matches existing auth pattern)
  </behavior>
  <action>
Read tests/integration/test_phone_banks.py to follow its existing fixture patterns (session/campaign factories, auth headers, async client). Add a new test class or function group `TestCallerCheckInStatus` with the 5 cases above. Use existing factories — do not invent new ones.

Tests MUST fail initially (endpoint doesn't exist → 404 route-not-found for the 200 cases, or mis-shaped body). Endpoint is at `GET /api/v1/campaigns/{campaign_id}/phone-bank-sessions/{session_id}/callers/me` (path convention inferred from existing endpoints in phone_banks.py — verify during implementation and adjust if the existing path prefix differs).

Run: `uv run pytest tests/integration/test_phone_banks.py -k check_in_status -x` — expect failures.

Implements SEC-12 backend verification.
  </action>
  <verify>
    <automated>uv run pytest tests/integration/test_phone_banks.py -k "check_in_status or callers_me" -x --no-header 2>&1 | tail -30 || true</automated>
  </verify>
  <done>5 new tests exist in test_phone_banks.py targeting the callers/me endpoint; they currently fail (endpoint missing) with clear errors.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement GET callers/me endpoint + lint</name>
  <files>app/api/v1/phone_banks.py, app/schemas/phone_banks.py</files>
  <behavior>
    - GET returns 200 with SessionCallerResponse (or a new CheckInStatusResponse wrapping session_caller + checked_in boolean) when the current user is an assigned caller on that session
    - GET returns 404 when the user is not an assigned caller
    - GET returns 401 when unauthenticated
    - Route path mirrors existing conventions in phone_banks.py (adjacent to POST check-in / GET callers)
  </behavior>
  <action>
1. Inspect the exact path prefix for the existing `POST .../check-in` and `GET .../callers` endpoints in app/api/v1/phone_banks.py (research cites lines 303-381). Use the same prefix/router.

2. Add `GET /sessions/{session_id}/callers/me` (or whatever the existing prefix yields — e.g. `/campaigns/{campaign_id}/phone-bank-sessions/{session_id}/callers/me`). Implementation:
   - Depend on current user (follow existing dep pattern, e.g. `get_current_user`).
   - Query SessionCaller by (session_id, user_id) scoped through campaign_id join as existing routes do.
   - If no row → raise HTTPException(status_code=404, detail="Not an assigned caller for this session").
   - Return SessionCallerResponse (reuse existing schema) — add a computed `checked_in: bool` field if not already present. If adding the field, extend SessionCallerResponse in app/schemas/phone_banks.py: `checked_in: bool` with a validator / model_validator that returns `check_in_at is not None and check_out_at is None`.

3. Lint before commit: `uv run ruff check app/api/v1/phone_banks.py app/schemas/phone_banks.py` and `uv run ruff format app/api/v1/phone_banks.py app/schemas/phone_banks.py`.

4. Rerun tests from Task 1 — must now pass.

Reference decision D-04 (server-side enforcement) and research Open Question 1 (Option A recommended).
  </action>
  <verify>
    <automated>uv run ruff check app/api/v1/phone_banks.py app/schemas/phone_banks.py && uv run pytest tests/integration/test_phone_banks.py -k "check_in_status or callers_me" -x --no-header 2>&1 | tail -20</automated>
  </verify>
  <done>Endpoint returns 200/404/401 per behavior; all 5 integration tests pass; ruff clean.</done>
</task>

</tasks>

<verification>
All new integration tests green. Ruff clean on modified files. Endpoint path documented in SUMMARY for Plan 06 to consume.
</verification>

<success_criteria>
- `uv run pytest tests/integration/test_phone_banks.py -k "check_in_status or callers_me"` shows 5/5 pass
- Endpoint path recorded in SUMMARY.md (Plan 06 needs exact path)
- No ruff warnings on touched files
</success_criteria>

<output>
Create `.planning/phases/73-frontend-auth-guards-oidc-error-surfacing/73-02-SUMMARY.md` including the exact endpoint path and response shape (Plan 06 consumes both).
</output>
