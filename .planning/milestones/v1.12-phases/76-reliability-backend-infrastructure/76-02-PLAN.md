---
phase: 76-reliability-backend-infrastructure
plan: 02
type: execute
wave: 1
depends_on: ["76-01"]
files_modified:
  - app/core/config.py
  - docker-compose.yml
  - alembic.ini
autonomous: true
requirements: [REL-06, REL-11]
must_haves:
  truths:
    - "Settings class declares trusted_proxy_cidrs and rate_limit_unauthenticated exactly once each"
    - "Effective defaults match the later (Cloudflare / 60-per-minute) declarations"
    - "docker-compose DISABLE_RATE_LIMIT default is false — rate limiting ENABLED by default"
    - "alembic.ini sqlalchemy.url resolves from DATABASE_URL_SYNC env var via %(DATABASE_URL_SYNC)s interpolation"
  artifacts:
    - path: "app/core/config.py"
      provides: "Settings class with deduplicated fields"
      contains: "trusted_proxy_cidrs"
    - path: "docker-compose.yml"
      provides: "api service DISABLE_RATE_LIMIT default false"
    - path: "alembic.ini"
      provides: "sqlalchemy.url using %(DATABASE_URL_SYNC)s interpolation"
  key_links:
    - from: "tests/unit/test_settings_dedup.py"
      to: "app/core/config.py"
      via: "import Settings, read source text"
      pattern: "trusted_proxy_cidrs"
    - from: "tests/unit/test_alembic_ini_interpolation.py"
      to: "alembic.ini"
      via: "file read"
      pattern: "%(DATABASE_URL_SYNC)s"
---

<objective>
Fix three config-hygiene bugs: duplicate Settings field declarations (H11), docker-compose rate-limit-disabled-by-default (H12), and alembic.ini's hardcoded DB URL (H15). Addresses REL-06 (H11+H12) and REL-11 (H15). All three touch small, separate config files — bundled because they share the "config hygiene" theme and risk profile.

Purpose: Eliminates silent override bugs (dedup), locks rate limiting ON by default (defense-in-depth), and makes alembic honor the env var instead of silently falling back to a hardcoded localhost URL.
Output: Three config files updated, Wave 0 tests flipped to passing.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/76-reliability-backend-infrastructure/76-CONTEXT.md
@.planning/CODEBASE-REVIEW-2026-04-04.md
@app/core/config.py
@docker-compose.yml
@alembic.ini
@tests/unit/test_settings_dedup.py
@tests/unit/test_alembic_ini_interpolation.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Remove duplicate Settings field declarations (H11 / REL-06)</name>
  <files>app/core/config.py</files>
  <behavior>
    - Settings().trusted_proxy_cidrs returns the full Cloudflare CIDR list (22 entries)
    - Settings().rate_limit_unauthenticated == "60/minute"
    - Source file contains each field name EXACTLY once
    - rate_limit_authenticated_per_user == "300/minute" (untouched)
    - rate_limit_authenticated_per_ip == "600/minute" (untouched)
  </behavior>
  <action>
    Per D-06 (lock): remove the FIRST declarations at lines 45 and 46:
    - Delete line 45: `trusted_proxy_cidrs: list[str] = []`
    - Delete line 46: `rate_limit_unauthenticated: str = "30/minute"`

    Keep the later declarations at lines 69-93 which carry the Cloudflare CIDR list and `"60/minute"` default. Also keep `disable_rate_limit: bool = False` at line 44 (not duplicate).

    The "# Rate limiting" comment at line 43 becomes sparse (just `disable_rate_limit`); move the comment to sit directly above `disable_rate_limit` and leave the larger rate-limiting block at line 68 with its own `# Rate limiting` comment. Final shape:

    ```python
        # Rate limiting (disable flag only; defaults live below)
        disable_rate_limit: bool = False
        ...
        # Rate limiting
        trusted_proxy_cidrs: list[str] = [<Cloudflare list>]
        rate_limit_unauthenticated: str = "60/minute"
        rate_limit_authenticated_per_user: str = "300/minute"
        rate_limit_authenticated_per_ip: str = "600/minute"
    ```

    Run `uv run ruff check app/core/config.py && uv run ruff format app/core/config.py`.

    Run `uv run pytest tests/unit/test_settings_dedup.py -x` — should now PASS.
  </action>
  <verify>
    <automated>uv run pytest tests/unit/test_settings_dedup.py -x &amp;&amp; uv run ruff check app/core/config.py</automated>
  </verify>
  <done>Duplicate declarations removed, test_settings_dedup passes, ruff clean, no other tests regress (run `uv run pytest tests/unit/test_rate_limit_coverage.py tests/unit/test_security.py`).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Change docker-compose DISABLE_RATE_LIMIT default to false (H12 / REL-06)</name>
  <files>docker-compose.yml</files>
  <behavior>
    - docker-compose.yml api service env block shows `DISABLE_RATE_LIMIT: ${DISABLE_RATE_LIMIT:-false}`
    - Starting the stack without setting DISABLE_RATE_LIMIT leaves rate limiting ENABLED
    - Developers can still opt out via `DISABLE_RATE_LIMIT=true docker compose up`
  </behavior>
  <action>
    Per D-07 (lock): change line 25 of docker-compose.yml from:
    `DISABLE_RATE_LIMIT: ${DISABLE_RATE_LIMIT:-true}`
    to:
    `DISABLE_RATE_LIMIT: ${DISABLE_RATE_LIMIT:-false}`

    Add an inline comment: `# Rate limiting ENABLED by default per REL-06; opt out via env var`.

    Validate the YAML parses: `docker compose config > /dev/null`.
  </action>
  <verify>
    <automated>docker compose config &gt; /dev/null &amp;&amp; grep -E 'DISABLE_RATE_LIMIT.*:-false' docker-compose.yml</automated>
  </verify>
  <done>Default is false, YAML validates, inline comment explains the default.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Replace hardcoded alembic.ini DB URL with env interpolation (H15 / REL-11)</name>
  <files>alembic.ini</files>
  <behavior>
    - alembic.ini line 5 reads `sqlalchemy.url = %(DATABASE_URL_SYNC)s`
    - Running `DATABASE_URL_SYNC=... alembic current` resolves the URL from env
    - Running alembic WITHOUT DATABASE_URL_SYNC fails loudly (no silent localhost fallback)
  </behavior>
  <action>
    Per D-08 (lock): change alembic.ini line 5 from:
    `sqlalchemy.url = postgresql+psycopg2://postgres:postgres@localhost:5432/run_api`
    to:
    `sqlalchemy.url = %(DATABASE_URL_SYNC)s`

    Check that alembic/env.py already configures the interpolation. If env.py does NOT pass `DATABASE_URL_SYNC` into `config.set_main_option` or `os.environ`-based config, the interpolation will fail with KeyError. Read alembic/env.py: if it sets `config.set_main_option("DATABASE_URL_SYNC", os.environ["DATABASE_URL_SYNC"])` already, done. Otherwise ADD that line to env.py immediately after config is loaded.

    Note: ConfigParser's %(name)s interpolation reads from the DEFAULT section or from options set via set_main_option. Most common pattern is: in alembic/env.py, near the top: `config.set_main_option("DATABASE_URL_SYNC", os.environ["DATABASE_URL_SYNC"])`. If DATABASE_URL_SYNC is missing from env, this raises KeyError — which is the desired loud failure.

    Test: `DATABASE_URL_SYNC=postgresql+psycopg2://postgres:postgres@localhost:5433/run_api uv run alembic current` should succeed (using a valid dev URL from .env). Without the var, expect a loud error.

    Run `uv run pytest tests/unit/test_alembic_ini_interpolation.py -x`.
  </action>
  <verify>
    <automated>uv run pytest tests/unit/test_alembic_ini_interpolation.py -x &amp;&amp; grep -E '^sqlalchemy.url = %\(DATABASE_URL_SYNC\)s' alembic.ini</automated>
  </verify>
  <done>alembic.ini uses env interpolation, env.py sets main option if needed, test passes, `uv run alembic current` works with DATABASE_URL_SYNC set.</done>
</task>

</tasks>

<verification>
uv run pytest tests/unit/test_settings_dedup.py tests/unit/test_alembic_ini_interpolation.py -x
docker compose config > /dev/null
uv run ruff check app/core/config.py
</verification>

<success_criteria>
- Settings fields declared once, defaults match Cloudflare list and "60/minute"
- DISABLE_RATE_LIMIT defaults to false in docker-compose
- alembic.ini uses %(DATABASE_URL_SYNC)s interpolation
- Wave 0 tests for these three items now pass
- No regression in existing rate-limit / security tests
</success_criteria>

<output>
Create `.planning/phases/76-reliability-backend-infrastructure/76-02-SUMMARY.md` with before/after diffs for each of the three config files and test pass confirmation.
</output>
