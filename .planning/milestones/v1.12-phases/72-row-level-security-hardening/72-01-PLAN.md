---
phase: 72-row-level-security-hardening
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - tests/integration/conftest.py
  - tests/integration/test_rls_hardening.py
autonomous: true
requirements: [SEC-05, SEC-06]
must_haves:
  truths:
    - "Test collection succeeds for tests/integration/test_rls_hardening.py"
    - "Fixture two_orgs_with_campaigns inserts 2 organizations, 2 campaigns, and organization_members rows"
    - "All 7 RLS test stubs exist and FAIL (red) because migration 026 does not yet exist"
  artifacts:
    - path: "tests/integration/conftest.py"
      provides: "two_orgs_with_campaigns async fixture"
      contains: "two_orgs_with_campaigns"
    - path: "tests/integration/test_rls_hardening.py"
      provides: "Failing test stubs for SEC-05 and SEC-06"
      min_lines: 120
  key_links:
    - from: "tests/integration/test_rls_hardening.py"
      to: "two_orgs_with_campaigns fixture"
      via: "pytest fixture injection"
      pattern: "two_orgs_with_campaigns"
    - from: "tests/integration/test_rls_hardening.py"
      to: "app_user_engine fixture"
      via: "pytest fixture injection"
      pattern: "app_user_engine"
---

<objective>
Create Wave 0 test infrastructure for Phase 72 RLS Hardening: a new `two_orgs_with_campaigns` fixture and a failing `test_rls_hardening.py` covering SEC-05 (FORCE on campaigns/campaign_members/users) and SEC-06 (ENABLE+FORCE+policy on organizations/organization_members).

Purpose: Establish red tests BEFORE the migration exists, so Plan 02's migration task is verified by the failing-then-green cycle. Also delivers the fixture that the migration tests require.

Output: One new fixture in `tests/integration/conftest.py` and a new test file `tests/integration/test_rls_hardening.py` with 7 failing test stubs.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/72-row-level-security-hardening/72-CONTEXT.md
@.planning/phases/72-row-level-security-hardening/72-RESEARCH.md
@.planning/phases/72-row-level-security-hardening/72-VALIDATION.md
@tests/integration/conftest.py
@tests/integration/test_rls_api_smoke.py
@app/db/rls.py

<interfaces>
<!-- Helper the tests use to set session-var context -->
From app/db/rls.py:
```python
async def set_campaign_context(session: AsyncSession, campaign_id: str) -> None
```

<!-- Existing engine fixture (NOT a superuser - connects as app_user) -->
From tests/integration/conftest.py:
```python
@pytest_asyncio.fixture
async def app_user_engine() -> AsyncEngine: ...
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 72-01-01: Add two_orgs_with_campaigns fixture + failing RLS test stubs</name>
  <files>tests/integration/conftest.py, tests/integration/test_rls_hardening.py</files>
  <behavior>
    - Fixture two_orgs_with_campaigns yields a dict with keys: org_a_id, org_b_id, campaign_a_id, campaign_b_id, user_a_id, user_b_id. Each org has at least one organization_members row with a distinct user. Fixture uses the superuser engine to INSERT (bypassing RLS), cleans up in teardown.
    - test_force_on_campaigns: as app_user with Campaign A context, SELECT FROM campaigns returns only campaign_a_id (not campaign_b_id).
    - test_force_on_campaign_members: as app_user with Campaign A context, SELECT FROM campaign_members returns only rows for campaign_a_id.
    - test_force_on_users: as app_user with Campaign A context, SELECT FROM users returns only users linked via campaign_members for campaign_a_id.
    - test_organizations_cross_campaign_blocked: as app_user with Campaign A context, SELECT id FROM organizations returns org_a_id and NOT org_b_id.
    - test_organization_members_cross_campaign_blocked: as app_user with Campaign A context, SELECT FROM organization_members returns rows only for org_a_id.
    - test_organizations_empty_without_context: as app_user with nil-UUID context (no set_campaign_context call), SELECT FROM organizations returns zero rows.
    - test_migration_reversible: placeholder stub marked with pytest.skip("implemented in Plan 02") OR asserts False — will be filled in by Plan 02.
  </behavior>
  <action>
    1. Append fixture `two_orgs_with_campaigns` to `tests/integration/conftest.py`:
       - Use the existing superuser engine (whatever is already used by `two_campaigns_with_resources` in the same file, e.g. test DB engine) to INSERT directly (bypasses RLS).
       - INSERT two `organizations` rows (org_a, org_b) with unique names.
       - INSERT two `campaigns` rows (campaign_a linked to org_a, campaign_b linked to org_b). Populate all NOT NULL columns; copy defaults from `two_campaigns_with_resources` as reference.
       - INSERT two `users` rows (user_a, user_b) — zitadel-style string IDs.
       - INSERT `organization_members`: (org_a, user_a), (org_b, user_b).
       - INSERT `campaign_members`: (campaign_a, user_a), (campaign_b, user_b).
       - Yield dict: {"org_a_id": ..., "org_b_id": ..., "campaign_a_id": ..., "campaign_b_id": ..., "user_a_id": ..., "user_b_id": ...}.
       - Teardown: DELETE FROM campaign_members, organization_members, campaigns, users, organizations WHERE id IN (...).
    2. Create `tests/integration/test_rls_hardening.py` with:
       - Module docstring referencing SEC-05 and SEC-06.
       - `pytestmark = pytest.mark.integration`
       - Imports: `from sqlalchemy import text`, `from sqlalchemy.ext.asyncio import AsyncSession, async_sessionmaker`, `from app.db.rls import set_campaign_context`.
       - Seven `async def test_*` functions per the behavior block. Each test uses `app_user_engine` + `two_orgs_with_campaigns`, opens an `async_sessionmaker` session, calls `set_campaign_context(s, str(data["campaign_a_id"]))` where applicable, executes the SELECT via `text(...)`, and asserts isolation.
       - All tests MUST fail initially — either because `organizations` table has no RLS (returns both orgs) OR because FORCE is missing from core tables. Do NOT write tests that expect the pre-migration behavior.
       - Add clear comments: `# RED: fails until migration 026_rls_hardening runs`.
    3. Run `uv run ruff format tests/integration/conftest.py tests/integration/test_rls_hardening.py` and `uv run ruff check --fix tests/integration/conftest.py tests/integration/test_rls_hardening.py`.
    4. Verify collection: `uv run pytest tests/integration/test_rls_hardening.py --collect-only`.
    5. Run tests and confirm they FAIL (red): `uv run pytest tests/integration/test_rls_hardening.py -x` — expect failures (organizations returns both orgs, etc.). This is the intentional red state.

    Reference: `alembic/versions/001_initial_schema.py` for users/campaigns/campaign_members columns; `alembic/versions/009_organizations.py` and `015_organization_members.py` for organization* columns.
  </action>
  <verify>
    <automated>uv run pytest tests/integration/test_rls_hardening.py --collect-only 2>&1 | grep -E "collected 7 items|collected 8 items"</automated>
  </verify>
  <done>
    - `two_orgs_with_campaigns` fixture exists in tests/integration/conftest.py and yields the documented dict
    - tests/integration/test_rls_hardening.py exists with 7 test functions
    - `uv run pytest tests/integration/test_rls_hardening.py --collect-only` reports 7 collected items with no collection errors
    - `uv run pytest tests/integration/test_rls_hardening.py -x` fails (red state) — at least the organizations_cross_campaign_blocked test fails because RLS does not yet exist on organizations
    - `uv run ruff check tests/integration/test_rls_hardening.py tests/integration/conftest.py` passes
  </done>
</task>

</tasks>

<verification>
Wave 0 complete when:
- Test collection succeeds (7 items discovered)
- At least 2 tests FAIL when run (confirming red state)
- Ruff passes on modified files
- No new fixtures break existing test_rls_api_smoke.py or test_tenant_isolation.py (run: `uv run pytest tests/integration/test_rls_api_smoke.py tests/integration/test_tenant_isolation.py -x`)
</verification>

<success_criteria>
- Fixture `two_orgs_with_campaigns` can be injected into any integration test
- `test_rls_hardening.py` enumerates every requirement in SEC-05 and SEC-06 as a discrete failing test
- Plan 02 can turn every test green by writing the migration alone
</success_criteria>

<output>
After completion:
1. Commit: `git commit -m "test(72): add RLS hardening test stubs and two_orgs_with_campaigns fixture"` (files: conftest.py, test_rls_hardening.py)
2. Create `.planning/phases/72-row-level-security-hardening/72-01-SUMMARY.md` with the test stub list and fixture signature.
</output>
