---
phase: 71-tenant-isolation-service-route-scoping
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/integration/conftest.py
  - tests/integration/test_tenant_isolation.py
autonomous: true
requirements:
  - SEC-01
  - SEC-02
  - SEC-03
  - SEC-04
  - SEC-13

must_haves:
  truths:
    - "A shared fixture provides Campaign A + Campaign B with full resource set (voter list, import job, voter tag, voter, survey script, survey question) per campaign"
    - "An executable pytest file exists at tests/integration/test_tenant_isolation.py with one test per affected endpoint (≈15 tests)"
    - "Every test currently FAILS against the unfixed codebase (red state proves the vulnerability exists)"
    - "Tests use FastAPI TestClient + auth-bypass pattern from test_rls_api_smoke.py; no new harness is built"
  artifacts:
    - path: "tests/integration/conftest.py"
      provides: "two_campaigns_with_resources fixture (extends existing two_campaigns)"
      contains: "two_campaigns_with_resources"
    - path: "tests/integration/test_tenant_isolation.py"
      provides: "All six test classes (TestListCampaignsScoping, TestVoterListScoping, TestImportJobScoping, TestRevokeInviteScoping, TestVoterTagScoping, TestSurveyScoping)"
      min_lines: 250
  key_links:
    - from: "tests/integration/test_tenant_isolation.py"
      to: "tests/integration/conftest.py::two_campaigns_with_resources"
      via: "pytest fixture injection"
      pattern: "two_campaigns_with_resources"
    - from: "tests/integration/test_tenant_isolation.py"
      to: "tests/integration/test_rls_api_smoke.py::_make_app_for_campaign"
      via: "imported helper for FastAPI app with auth override"
      pattern: "_make_app_for_campaign"
---

<objective>
Wave 0 — Create the failing test infrastructure for Phase 71.

Purpose: Per Nyquist rule, tests MUST exist before the implementation tasks in Plans 02 and 03. These tests prove the current IDOR vulnerabilities exist (all red) and will turn green as the service/route fixes land.

Output:
- Extended `two_campaigns_with_resources` fixture in `tests/integration/conftest.py`
- New `tests/integration/test_tenant_isolation.py` with ~17 test cases across 6 classes
- All tests currently fail (proving the vulnerabilities); no production code touched in this plan
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/71-tenant-isolation-service-route-scoping/71-CONTEXT.md
@.planning/phases/71-tenant-isolation-service-route-scoping/71-RESEARCH.md
@.planning/phases/71-tenant-isolation-service-route-scoping/71-VALIDATION.md
@tests/integration/conftest.py
@tests/integration/test_rls_api_smoke.py
</context>

<interfaces>
<!-- Key assets the executor reuses. Do NOT rebuild these. -->

From tests/integration/conftest.py (existing `two_campaigns` fixture at ~line 82):
- Provides: campaign_a (id, org_id), campaign_b (id, org_id), user_a, user_b, campaign_member rows, one Invite per campaign
- Uses superuser_session for SQL-level inserts that bypass RLS

From tests/integration/test_rls_api_smoke.py:
- `_make_app_for_campaign(campaign_id, user_id, role="admin")` at ~line 191 — returns a FastAPI app with `get_current_user`, `get_db`, `get_campaign_db` overridden
- `two_campaigns_with_api_data` fixture at ~line 32 — pattern reference for extending two_campaigns with additional resource rows via raw SQL

Models to insert (SQL pattern — use superuser_session):
- VoterList: id, campaign_id, name, list_type='static', created_by, created_at
- ImportJob: id, campaign_id, status='UPLOADED', file_name, file_path, uploaded_by, created_at
- VoterTag: id, campaign_id, name, color, created_by, created_at
- Voter: id, campaign_id, first_name, last_name, ...
- SurveyScript: id, campaign_id, name, status='DRAFT', created_by, created_at
- SurveyQuestion: id, script_id, text, question_type='text', order_index, created_at

Endpoints under test (campaign_id in path):
- GET  /api/v1/campaigns
- GET/PATCH/DELETE /api/v1/campaigns/{campaign_id}/lists/{list_id}
- POST/DELETE /api/v1/campaigns/{campaign_id}/lists/{list_id}/members
- POST /api/v1/campaigns/{campaign_id}/imports/{import_id}/detect
- POST /api/v1/campaigns/{campaign_id}/imports/{import_id}/confirm-mapping
- POST /api/v1/campaigns/{campaign_id}/imports/{import_id}/cancel
- GET  /api/v1/campaigns/{campaign_id}/imports/{import_id}
- DELETE /api/v1/campaigns/{campaign_id}/invites/{invite_id}
- POST /api/v1/campaigns/{campaign_id}/voters/{voter_id}/tags
- GET/PATCH /api/v1/campaigns/{campaign_id}/surveys/{script_id}
- POST /api/v1/campaigns/{campaign_id}/surveys/{script_id}/questions
- PATCH /api/v1/campaigns/{campaign_id}/surveys/{script_id}/questions/{question_id}
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Extend conftest with two_campaigns_with_resources fixture</name>
  <files>tests/integration/conftest.py</files>
  <action>
Add a new pytest fixture `two_campaigns_with_resources` that depends on (or clones the logic of) the existing `two_campaigns` fixture at line ~82, then inserts one row per campaign for each of: VoterList, ImportJob, VoterTag, Voter, SurveyScript, SurveyQuestion.

Follow the SQL-INSERT-via-superuser_session pattern used by `two_campaigns_with_api_data` in tests/integration/test_rls_api_smoke.py:32. Use raw SQLAlchemy `text()` inserts to bypass RLS during setup and raw `text()` deletes in teardown.

Fixture yields a dict (or dataclass) exposing at minimum:
  - campaign_a_id, campaign_b_id
  - user_a_id, user_b_id
  - voter_list_a_id, voter_list_b_id
  - import_job_a_id, import_job_b_id
  - voter_tag_a_id, voter_tag_b_id
  - voter_a_id, voter_b_id
  - survey_script_a_id, survey_script_b_id
  - survey_question_a_id, survey_question_b_id
  - invite_a_id, invite_b_id  (from existing two_campaigns)

Use UUID4 for all ids, `datetime.now(UTC)` for timestamps. For enum columns, use the lowercase string values expected by the schema (inspect app/models/*.py if unsure).

Teardown: DELETE all inserted rows in reverse FK order within a single superuser_session transaction.

Verification: fixture must be importable and parametrizable by other tests in the same session.
  </action>
  <verify>
    <automated>cd /home/kwhatcher/projects/civicpulse/run-api &amp;&amp; docker compose exec -T api bash -c "uv run pytest tests/integration/conftest.py --collect-only -q 2>&amp;1 | grep -E 'two_campaigns_with_resources|error' || uv run python -c 'from tests.integration.conftest import two_campaigns_with_resources; print(\"OK\")'"</automated>
  </verify>
  <done>Fixture is defined, has no import errors, and a trivial test that accepts the fixture and asserts `resources.voter_list_a_id is not None` passes.</done>
</task>

<task type="auto">
  <name>Task 2: Create test_tenant_isolation.py with all ~17 failing tests</name>
  <files>tests/integration/test_tenant_isolation.py</files>
  <action>
Create a new integration test file organized as six test classes, one per C-finding group:

1. **TestListCampaignsScoping** (SEC-01, test id 71-01-01)
   - `test_list_campaigns_scoped` — build app for user_a, GET /api/v1/campaigns, assert campaign_a.id in returned ids AND campaign_b.id NOT in returned ids.

2. **TestVoterListScoping** (SEC-02, test id 71-01-02)
   - `test_voter_list_cross_campaign_404` — for each of GET, PATCH, DELETE on /campaigns/{A}/lists/{list_B}, POST and DELETE on /campaigns/{A}/lists/{list_B}/members, assert status_code == 404. Use parametrize or separate assertions in one test.
   - `test_voter_list_same_campaign_ok` — GET /campaigns/{A}/lists/{list_A} returns 200 (positive path regression guard).

3. **TestImportJobScoping** (SEC-03, test id 71-01-03)
   - `test_import_job_cross_campaign_404` — for each of POST /detect, POST /confirm-mapping, POST /cancel, GET /{import_id} on /campaigns/{A}/imports/{job_B}, assert 404.

4. **TestRevokeInviteScoping** (SEC-04, test id 71-01-04)
   - `test_revoke_invite_cross_campaign_404` — DELETE /campaigns/{A}/invites/{invite_B}, assert 404.
   - `test_revoke_invite_same_campaign_ok` — DELETE /campaigns/{A}/invites/{invite_A}, assert 2xx.

5. **TestVoterTagScoping** (SEC-13, test id 71-01-05)
   - `test_add_tag_cross_campaign_404` — POST /campaigns/{A}/voters/{voter_A}/tags with body {"tag_id": tag_B_id}, assert 404 (tag belongs to B).

6. **TestSurveyScoping** (SEC-13, test id 71-01-06)
   - `test_survey_script_question_cross_campaign_404` — parametrized assertions for GET/PATCH /campaigns/{A}/surveys/{script_B}, POST /campaigns/{A}/surveys/{script_B}/questions, PATCH /campaigns/{A}/surveys/{script_A}/questions/{q_B}, each must return 404.

Each test:
- Uses `two_campaigns_with_resources` fixture
- Builds FastAPI app via `_make_app_for_campaign(campaign_a_id, user_a_id, role="admin")` (import from test_rls_api_smoke or re-export via conftest)
- Uses `httpx.AsyncClient(transport=ASGITransport(app=app), base_url="http://testserver")` for requests
- Marks `@pytest.mark.integration` at class level
- Asserts EXACT `response.status_code == 404` on negative paths (not `!= 200`) to prove enumeration-safe error

Expected state after this task: Running the file shows all ~14 cross-campaign tests FAILING (currently returning 200/other because vulnerabilities are unfixed). Positive-path tests may pass or fail depending on fixture state — that's acceptable; they'll be green after Plans 02/03.

Include module docstring citing SEC-01..04 and SEC-13.
  </action>
  <verify>
    <automated>cd /home/kwhatcher/projects/civicpulse/run-api &amp;&amp; docker compose exec -T api bash -c "uv run pytest tests/integration/test_tenant_isolation.py --collect-only -q" 2>&amp;1 | tail -30</automated>
  </verify>
  <done>
    - File exists with 6 test classes
    - `pytest --collect-only` lists all ~17 test cases without collection errors
    - Running `uv run pytest tests/integration/test_tenant_isolation.py -x` shows cross-campaign negative tests FAILING with assertion errors on status_code (confirms red state = vulnerabilities not yet fixed)
    - `uv run ruff check tests/integration/test_tenant_isolation.py` passes
  </done>
</task>

</tasks>

<verification>
After both tasks complete:

```bash
# Collect-only must succeed with no errors
docker compose exec -T api bash -c "uv run pytest tests/integration/test_tenant_isolation.py --collect-only -q"

# Lint must pass
docker compose exec -T api bash -c "uv run ruff check tests/integration/ && uv run ruff format --check tests/integration/"

# Tests MUST be red (vulnerabilities not yet fixed)
docker compose exec -T api bash -c "uv run pytest tests/integration/test_tenant_isolation.py -v" | grep -E "FAILED|PASSED" | head -20
```

Expected outcome: ~14 failures (cross-campaign tests), positive-path tests may be mixed.
</verification>

<success_criteria>
- [ ] `two_campaigns_with_resources` fixture created in tests/integration/conftest.py
- [ ] Fixture inserts VoterList, ImportJob, VoterTag, Voter, SurveyScript, SurveyQuestion for both campaigns
- [ ] Fixture cleans up in teardown (no leaked rows after test session)
- [ ] `tests/integration/test_tenant_isolation.py` exists with 6 test classes
- [ ] At least 15 test cases collected (one per affected endpoint)
- [ ] All cross-campaign negative tests currently FAIL with status_code mismatch (proves red state)
- [ ] Ruff lint + format pass
- [ ] Commit created: `test(71-01): add failing tenant-isolation integration tests (Wave 0)`
</success_criteria>

<output>
After completion, create `.planning/phases/71-tenant-isolation-service-route-scoping/71-01-SUMMARY.md` documenting:
- Fixture resource ids and structure
- Test class → test id mapping (71-01-01 through 71-01-06)
- Exact list of currently-failing tests (red-state baseline)
- Any fixture schema gotchas encountered (enum casing, FK order)
</output>
