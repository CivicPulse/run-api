---
phase: 59-chunk-schema-configuration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/models/import_job.py
  - app/models/__init__.py
  - app/db/base.py
  - app/core/config.py
  - alembic/versions/022_import_chunks.py
  - tests/unit/test_batch_resilience.py
  - tests/unit/test_model_coverage.py
  - tests/integration/test_voter_rls.py
autonomous: true
requirements: [CHUNK-01, CHUNK-07]
must_haves:
  truths:
    - "Durable chunk records can be stored internally without changing ImportJobResponse or the current API surface."
    - "Chunking defaults exist in Settings with conservative values for threshold, chunk size, and max concurrent chunks."
    - "The import_chunks table is protected by campaign RLS and available to app_user sessions."
  artifacts:
    - path: "app/models/import_job.py"
      provides: "ImportChunkStatus enum and ImportChunk ORM model attached to ImportJob"
    - path: "app/core/config.py"
      provides: "import_chunk_size_default, import_max_chunks_per_import, and import_serial_threshold settings"
    - path: "alembic/versions/022_import_chunks.py"
      provides: "import_chunks table, indexes, RLS policy, and grants"
  key_links:
    - from: "app/db/base.py"
      to: "app/models/import_job.py"
      via: "model registration import for Alembic/runtime discovery"
      pattern: "import app\\.models\\.import_job"
    - from: "alembic/versions/022_import_chunks.py"
      to: "import_chunks"
      via: "ALTER TABLE + CREATE POLICY + GRANT"
      pattern: "ALTER TABLE import_chunks ENABLE ROW LEVEL SECURITY"
---

<objective>
Establish the durable schema and settings foundation for chunked imports while keeping chunks internal-only in Phase 59.

Purpose: Implement D-01, D-02, D-03, D-08, and D-09 before any runtime fan-out work so later phases can build on stable contracts instead of revisiting schema/config.
Output: ImportChunk ORM + migration, conservative chunk settings, and regression coverage for model registration and RLS.
</objective>

<execution_context>
@/home/kwhatcher/.codex/get-shit-done/workflows/execute-plan.md
@/home/kwhatcher/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/59-chunk-schema-configuration/59-CONTEXT.md
@.planning/phases/59-chunk-schema-configuration/59-RESEARCH.md
@.planning/phases/59-chunk-schema-configuration/59-VALIDATION.md
@.planning/phases/56-schema-orphan-detection/56-01-SUMMARY.md
@app/models/import_job.py
@app/models/__init__.py
@app/db/base.py
@app/core/config.py
@tests/unit/test_batch_resilience.py
@tests/unit/test_model_coverage.py
@tests/integration/test_voter_rls.py

<interfaces>
From app/models/import_job.py:
```python
class ImportStatus(enum.StrEnum):
    PENDING = "pending"
    UPLOADED = "uploaded"
    QUEUED = "queued"
    PROCESSING = "processing"
    CANCELLING = "cancelling"
    CANCELLED = "cancelled"
    COMPLETED = "completed"
    FAILED = "failed"

class ImportJob(Base):
    campaign_id: Mapped[uuid.UUID]
    total_rows: Mapped[int | None]
    imported_rows: Mapped[int | None]
    skipped_rows: Mapped[int | None]
    last_committed_row: Mapped[int | None]
    error_report_key: Mapped[str | None]
```

From app/core/config.py:
```python
class Settings(BaseSettings):
    import_batch_size: int = 1000
    import_orphan_threshold_minutes: int = 30
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add internal ImportChunk model and chunk settings</name>
  <files>app/models/import_job.py, app/models/__init__.py, app/db/base.py, app/core/config.py</files>
  <read_first>
app/models/import_job.py
app/models/__init__.py
app/db/base.py
app/core/config.py
.planning/phases/59-chunk-schema-configuration/59-CONTEXT.md
.planning/phases/59-chunk-schema-configuration/59-RESEARCH.md
  </read_first>
  <action>Add an internal-only `ImportChunkStatus` enum and `ImportChunk` ORM model in `app/models/import_job.py` per D-01, D-02, and D-03. Include `campaign_id`, `import_job_id`, `row_start`, `row_end`, `status`, `imported_rows`, `skipped_rows`, `last_committed_row`, `error_report_key`, `error_message`, `last_progress_at`, `created_at`, and `updated_at`, using `native_enum=False` and the same SQLAlchemy mapped-column style as `ImportJob`. Update `app/models/__init__.py` and `app/db/base.py` so `ImportChunk` and `ImportChunkStatus` are exported and registered. In `app/core/config.py`, add `import_chunk_size_default: int = 10000`, `import_max_chunks_per_import: int = 4`, and `import_serial_threshold: int = 10000` to satisfy D-08 and D-09 with conservative defaults. Do not modify `app/schemas/import_job.py` or any API response shape in this task.</action>
  <acceptance_criteria>
`rg "class ImportChunkStatus" app/models/import_job.py` finds the new enum.
`rg "class ImportChunk\\(Base\\)" app/models/import_job.py` finds the new model.
`rg "import_chunk_size_default: int = 10000|import_max_chunks_per_import: int = 4|import_serial_threshold: int = 10000" app/core/config.py` finds all three settings.
`rg "ImportChunk|ImportChunkStatus" app/models/__init__.py app/db/base.py` shows model export and registration.
  </acceptance_criteria>
  <verify>
    <automated>uv run pytest tests/unit/test_batch_resilience.py tests/unit/test_model_coverage.py -x</automated>
  </verify>
  <done>The codebase has a durable internal chunk model and conservative chunking settings, with no new API/schema surface for chunk details.</done>
</task>

<task type="auto">
  <name>Task 2: Create the import_chunks migration with campaign RLS</name>
  <files>alembic/versions/022_import_chunks.py</files>
  <read_first>
alembic/versions/021_import_recovery_metadata.py
alembic/versions/002_voter_data_models.py
app/models/import_job.py
.planning/phases/59-chunk-schema-configuration/59-RESEARCH.md
  </read_first>
  <action>Create `alembic/versions/022_import_chunks.py` to add an `import_chunks` table matching Task 1. Use a non-native `import_chunk_status` enum, foreign keys to `campaigns.id` and `import_jobs.id`, and indexes on `(import_job_id, row_start)` and `(campaign_id, status)`. In the upgrade, enable RLS on `import_chunks`, create an `import_chunks_isolation` policy keyed directly on `campaign_id = NULLIF(current_setting('app.current_campaign_id', true), '')::uuid`, and grant `SELECT, INSERT, UPDATE, DELETE` on `import_chunks` to `app_user`. In the downgrade, drop the policy, table, indexes, and enum cleanly.</action>
  <acceptance_criteria>
`rg "create_table\\(\" alembic/versions/022_import_chunks.py` finds `import_chunks`.
`rg "ALTER TABLE import_chunks ENABLE ROW LEVEL SECURITY|CREATE POLICY import_chunks_isolation|GRANT SELECT, INSERT, UPDATE, DELETE ON import_chunks TO app_user" alembic/versions/022_import_chunks.py` finds the required RLS/grant statements.
`rg "import_job_id|row_start|row_end|last_committed_row|error_report_key" alembic/versions/022_import_chunks.py` confirms the key schema fields exist in the revision.
  </acceptance_criteria>
  <verify>
    <automated>uv run pytest tests/integration/test_voter_rls.py -x</automated>
  </verify>
  <done>The migration creates a campaign-scoped import_chunks table with the same RLS/grant posture as other campaign-owned tables.</done>
</task>

<task type="auto">
  <name>Task 3: Add schema and RLS regression coverage for ImportChunk</name>
  <files>tests/unit/test_batch_resilience.py, tests/unit/test_model_coverage.py, tests/integration/test_voter_rls.py</files>
  <read_first>
tests/unit/test_batch_resilience.py
tests/unit/test_model_coverage.py
tests/integration/test_voter_rls.py
app/models/import_job.py
app/core/config.py
alembic/versions/022_import_chunks.py
  </read_first>
  <action>Extend the existing regression suites instead of inventing a new test file. In `tests/unit/test_batch_resilience.py`, add assertions that `Settings()` exposes defaults of `10000`, `4`, and `10000` for the new chunk settings and that `ImportChunk` exposes the durable fields added in Task 1. In `tests/unit/test_model_coverage.py`, keep the base-registration assertion green after the new model export/import changes. In `tests/integration/test_voter_rls.py`, update the fixture/setup and module docstring to include `import_chunks`, insert one chunk row per campaign, and assert an app-user session scoped to campaign A cannot see campaign B's chunk row.</action>
  <acceptance_criteria>
`rg "import_chunk_size_default|import_max_chunks_per_import|import_serial_threshold" tests/unit/test_batch_resilience.py` finds the new settings assertions.
`rg "ImportChunk" tests/unit/test_batch_resilience.py tests/unit/test_model_coverage.py tests/integration/test_voter_rls.py` finds model coverage in unit and integration tests.
`rg "import_chunks" tests/integration/test_voter_rls.py` shows fixture setup and RLS assertions for the new table.
  </acceptance_criteria>
  <verify>
    <automated>uv run pytest tests/unit/test_batch_resilience.py tests/unit/test_model_coverage.py tests/integration/test_voter_rls.py -x</automated>
  </verify>
  <done>Automated coverage proves ImportChunk is registered, settings defaults are stable, and campaign RLS applies to import_chunks rows.</done>
</task>

</tasks>

<verification>
Run `uv run pytest tests/unit/test_batch_resilience.py tests/unit/test_model_coverage.py tests/integration/test_voter_rls.py -x` after the plan completes.
Confirm the migration file contains explicit RLS SQL rather than relying on ORM-only behavior.
</verification>

<success_criteria>
Phase 59 has a durable internal chunk table, conservative chunk settings, and automated verification for model registration and RLS, while the user-facing import surface remains unchanged.
</success_criteria>

<output>
After completion, create `.planning/phases/59-chunk-schema-configuration/59-01-SUMMARY.md`
</output>
