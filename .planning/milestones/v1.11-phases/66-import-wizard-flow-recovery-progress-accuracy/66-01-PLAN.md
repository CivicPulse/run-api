---
phase: 66-import-wizard-flow-recovery-progress-accuracy
plan: 01
type: execute
wave: 1
depends_on: ["65-03"]
files_modified:
  - web/src/hooks/useImports.ts
  - web/src/routes/campaigns/$campaignId/voters/imports/new.tsx
  - web/src/hooks/useImports.test.ts
  - web/src/routes/campaigns/$campaignId/voters/imports/new.test.tsx
autonomous: true
requirements: [PROG-04, PROG-05]
must_haves:
  truths:
    - "The upload wizard always calls detect-columns for the newly created import job id."
    - "Route-level coverage proves the upload-to-detect handoff uses the fresh job id, not a stale URL value."
    - "Partial-success completion UI remains reachable for imports created through the wizard."
---

# Phase 66 Plan 01

Repair the upload wizard handoff so the detect-columns request always targets the fresh import job returned by `useInitiateImport()`, then lock it in with hook and route tests.
