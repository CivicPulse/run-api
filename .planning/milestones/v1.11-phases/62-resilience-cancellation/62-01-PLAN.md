---
phase: 62-resilience-cancellation
plan: 01
type: execute
wave: 1
depends_on: ["61-03"]
files_modified:
  - app/services/import_service.py
  - app/tasks/import_task.py
  - tests/unit/test_import_service.py
  - tests/unit/test_import_task.py
autonomous: true
requirements: [RESL-01, RESL-02]
must_haves:
  truths:
    - "A failed or cancelled chunk does not discard completed sibling work or force a premature parent failure."
    - "Queued chunk workers skip cleanly when the parent import is already cancelled."
    - "In-flight chunk workers stop only at durable batch boundaries after observing the parent cancellation signal."
  artifacts:
    - path: "app/tasks/import_task.py"
      provides: "queued-cancel guard and terminal chunk handoff for cancelled chunks"
    - path: "app/services/import_service.py"
      provides: "chunk-range cancellation checks at durable batch boundaries"
    - path: "tests/unit/test_import_service.py"
      provides: "coverage for chunk cancellation propagation without losing committed progress"
    - path: "tests/unit/test_import_task.py"
      provides: "coverage for queued chunk skip/cancel behavior"
---

<objective>
Implement chunk cancellation propagation and lock in failure isolation behavior for chunked imports.

Purpose: Fulfill RESL-01 and RESL-02 by making queued chunks skip on parent cancellation, in-flight chunks stop at the next batch boundary, and parent fan-in continue to preserve successful sibling work.
Output: Updated chunk worker/task cancellation behavior plus unit coverage for queued and in-flight chunk cancellation.
</objective>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add failing coverage for queued and in-flight chunk cancellation</name>
  <files>tests/unit/test_import_service.py, tests/unit/test_import_task.py</files>
  <action>Add unit tests proving that queued chunk tasks mark the chunk `CANCELLED` and skip execution when the parent job already has `cancelled_at`, and that `process_import_range(..., chunk=chunk)` stops after the next committed batch when parent cancellation appears mid-run. Keep assertions on durable progress (`last_committed_row`, imported/skipped counters) so completed work is preserved.</action>
  <verify>
    <automated>uv run pytest tests/unit/test_import_service.py tests/unit/test_import_task.py -x</automated>
  </verify>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement parent-driven chunk cancellation semantics</name>
  <files>app/services/import_service.py, app/tasks/import_task.py, tests/unit/test_import_service.py, tests/unit/test_import_task.py</files>
  <action>In `process_import_chunk()`, guard queued execution with a parent `cancelled_at` check that marks the chunk `CANCELLED` and skips range processing. In `process_import_range()`, refresh the parent after each committed batch for chunk targets; if the parent is cancelled, stop the loop and finalize the chunk as `CANCELLED` instead of `COMPLETED`, preserving already committed progress and leaving parent finalization to the existing Phase 61 helper.</action>
  <verify>
    <automated>uv run pytest tests/unit/test_import_service.py tests/unit/test_import_task.py -x</automated>
  </verify>
</task>

</tasks>

<output>
After completion, create `.planning/phases/62-resilience-cancellation/62-01-SUMMARY.md`
</output>
