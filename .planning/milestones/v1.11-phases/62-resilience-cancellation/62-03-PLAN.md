---
phase: 62-resilience-cancellation
plan: 03
type: execute
wave: 3
depends_on: ["62-02"]
files_modified:
  - app/services/import_service.py
  - tests/unit/test_import_service.py
  - tests/integration/test_import_parallel_processing.py
autonomous: true
requirements: [RESL-04]
must_haves:
  truths:
    - "Concurrent chunk workers acquire voter-row locks in a deterministic order before conflict upserts."
    - "Inline phone upserts follow a deterministic order while they remain part of the Phase 62 batch path."
    - "Deadlock mitigation lives inside the existing batch-processing seam rather than adding broader worker coordination."
  artifacts:
    - path: "app/services/import_service.py"
      provides: "deterministic voter and phone ordering ahead of conflict upserts"
    - path: "tests/unit/test_import_service.py"
      provides: "coverage for conflict-key ordering helpers or batch ordering behavior"
    - path: "tests/integration/test_import_parallel_processing.py"
      provides: "parallel-worker evidence that ordered batch processing preserves successful completion"
---

<objective>
Reduce cross-chunk deadlock risk by ordering batch upserts deterministically on the actual conflict keys.

Purpose: Fulfill RESL-04 by sorting voter and phone batch writes inside the current import batch path before PostgreSQL conflict upserts execute.
Output: Deterministic ordering in `process_csv_batch()` plus unit and integration coverage for the ordering contract.
</objective>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add coverage for deterministic upsert ordering</name>
  <files>tests/unit/test_import_service.py, tests/integration/test_import_parallel_processing.py</files>
  <action>Add focused tests that expose the order used for voter upserts and inline phone upserts, asserting that rows are processed in deterministic conflict-key order rather than input-file order. Keep the assertions narrow and tied to the conflict targets that `process_csv_batch()` actually uses.</action>
  <verify>
    <automated>uv run pytest tests/unit/test_import_service.py tests/integration/test_import_parallel_processing.py -x</automated>
  </verify>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement deterministic conflict-key ordering</name>
  <files>app/services/import_service.py, tests/unit/test_import_service.py, tests/integration/test_import_parallel_processing.py</files>
  <action>Sort voter rows before `INSERT ... ON CONFLICT` using the real voter conflict target and keep phone rows ordered deterministically before the `VoterPhone` upsert. Do this inside `process_csv_batch()` or tiny helper functions it owns so the mitigation stays local to the batch-processing seam.</action>
  <verify>
    <automated>uv run pytest tests/unit/test_import_service.py tests/integration/test_import_parallel_processing.py -x</automated>
  </verify>
</task>

</tasks>

<output>
After completion, create `.planning/phases/62-resilience-cancellation/62-03-SUMMARY.md`
</output>
