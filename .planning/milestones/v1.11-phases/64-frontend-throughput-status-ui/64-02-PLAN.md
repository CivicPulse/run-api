---
phase: 64-frontend-throughput-status-ui
plan: 02
type: execute
wave: 2
depends_on: ["64-01"]
files_modified:
  - web/src/lib/import-progress.ts
  - web/src/components/voters/ImportProgress.tsx
  - web/src/components/voters/ImportProgress.test.tsx
autonomous: true
requirements: [PROG-04]
---

<objective>
Add client-side throughput and ETA metrics to the live import progress card.
</objective>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Add focused coverage for import progress metrics and rendering</name>
  <files>web/src/components/voters/ImportProgress.test.tsx</files>
  <action>Add tests for throughput/ETA derivation and progress-card rendering so the component shows stable metrics only when enough data exists.</action>
  <verify><automated>npm --prefix web test -- --run web/src/components/voters/ImportProgress.test.tsx</automated></verify>
</task>
<task type="auto" tdd="true">
  <name>Task 2: Implement derived throughput and ETA in the import progress UI</name>
  <files>web/src/lib/import-progress.ts, web/src/components/voters/ImportProgress.tsx</files>
  <action>Create a pure helper for throughput and ETA calculation, then render those metrics in the progress card with sensible queued/calculating states.</action>
  <verify><automated>npm --prefix web test -- --run web/src/components/voters/ImportProgress.test.tsx</automated></verify>
</task>
</tasks>

<output>
After completion, create `.planning/phases/64-frontend-throughput-status-ui/64-02-SUMMARY.md`
</output>
