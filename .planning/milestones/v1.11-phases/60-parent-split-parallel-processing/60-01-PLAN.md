---
phase: 60-parent-split-parallel-processing
plan: 01
type: execute
wave: 1
depends_on: ["59-02"]
files_modified:
  - app/services/import_service.py
  - tests/unit/test_import_service.py
autonomous: true
requirements: [CHUNK-02, CHUNK-04]
must_haves:
  truths:
    - "A large import can be pre-scanned to derive an exact data-row count before chunk routing."
    - "The import engine can process an absolute row range without changing the serial path's batch durability semantics."
    - "Serial imports continue to use the same import engine through a wrapper rather than a forked implementation."
  artifacts:
    - path: "app/services/import_service.py"
      provides: "CSV data-row pre-scan helper plus a shared row-bounded import engine"
    - path: "tests/unit/test_import_service.py"
      provides: "coverage for pre-scan counting, absolute row bounds, and serial-wrapper delegation"
  key_links:
    - from: "app/services/import_service.py"
      to: "stream_csv_lines"
      via: "O(1)-memory CSV pre-scan"
      pattern: "count_csv_data_rows"
    - from: "app/services/import_service.py"
      to: "process_import_file"
      via: "serial wrapper delegating to shared range engine"
      pattern: "process_import_range"
---

<objective>
Establish the shared service seam for Phase 60 so parent orchestration and child chunk workers can both rely on one proven CSV-processing engine.

Purpose: Implement CHUNK-02 and the service-side portion of CHUNK-04 first, per D-05 and D-06, so later plans can add fan-out and child execution without duplicating the serial import loop.
Output: A streamed row-count helper, a row-bounded import engine, and unit coverage that locks the absolute-row contract in place.
</objective>

<execution_context>
@/home/kwhatcher/.codex/get-shit-done/workflows/execute-plan.md
@/home/kwhatcher/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/60-parent-split-parallel-processing/60-CONTEXT.md
@.planning/phases/60-parent-split-parallel-processing/60-RESEARCH.md
@.planning/phases/57-recovery-engine-completion-hardening/57-01-SUMMARY.md
@.planning/phases/59-chunk-schema-configuration/59-02-SUMMARY.md
@app/services/import_service.py
@tests/unit/test_import_service.py

<interfaces>
From app/services/import_service.py:
```python
def plan_chunk_ranges(
    total_rows: int,
    mapped_column_count: int,
    chunk_size_default: int,
) -> list[tuple[int, int]]: ...

class ImportService:
    async def process_import_file(
        self,
        import_job_id: str,
        session: AsyncSession,
        storage: StorageService,
        campaign_id: str,
    ) -> None: ...
```

Current serial-loop durability contract:
```python
await self._process_single_batch(...)
await commit_and_restore_rls(session, campaign_id)
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add pre-scan and ranged-engine tests before refactoring the service</name>
  <files>tests/unit/test_import_service.py</files>
  <behavior>
    - Test 1: `count_csv_data_rows()` skips the header and returns only 1-based data-row counts from streamed lines.
    - Test 2: `process_import_range()` skips rows before `row_start`, stops after `row_end`, and preserves inclusive absolute row numbering.
    - Test 3: `process_import_file()` delegates to the shared ranged engine with full-file bounds so the serial path remains a wrapper, not a fork.
  </behavior>
  <action>Extend `tests/unit/test_import_service.py` first with focused coverage for the new service seam. Mock `stream_csv_lines()` or storage downloads instead of materializing a real file. Use absolute file-row expectations per D-06: the first data row after the header is row 1, adjacent chunk ranges must not overlap, and the serial wrapper must invoke the shared engine with `row_start=1` and `row_end=None`. Keep existing batch and mapping tests intact.</action>
  <verify>
    <automated>uv run pytest tests/unit/test_import_service.py -x</automated>
  </verify>
  <done>Failing-or-missing coverage clearly defines the Phase 60 service contract for pre-scan counting and absolute row-bounded processing before production refactoring begins.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Refactor ImportService into a shared row-bounded engine</name>
  <files>app/services/import_service.py, tests/unit/test_import_service.py</files>
  <behavior>
    - Test 1: `count_csv_data_rows()` persists the same row numbering that `plan_chunk_ranges()` expects.
    - Test 2: ranged processing preserves `_process_single_batch()` durability semantics and batch boundaries while only ingesting the assigned rows.
    - Test 3: the serial wrapper still resets/finalizes parent `ImportJob` state exactly through the shared engine.
  </behavior>
  <action>Add `count_csv_data_rows(storage, file_key) -> int` that streams the file and counts data rows after the header to satisfy CHUNK-02 and D-07. Extract the streamed loop from `process_import_file()` into a shared helper such as `process_import_range(...)` that accepts absolute `row_start`, optional `row_end`, and a progress target that can later write either `ImportJob` or `ImportChunk` counters. Keep `_process_single_batch()` and `commit_and_restore_rls()` as the durability core. In this plan, continue wiring `process_import_file()` through the shared engine for full-file serial imports only; do not introduce chunk tasks, parent chunk creation, parent aggregation, cancellation propagation, or secondary-work offloading.</action>
  <verify>
    <automated>uv run pytest tests/unit/test_import_service.py -x</automated>
  </verify>
  <done>`ImportService` exposes a streamed pre-scan helper and one shared row-bounded processing engine, and the existing serial path is preserved by delegating through that engine rather than maintaining separate logic.</done>
</task>

</tasks>

<verification>
Run `uv run pytest tests/unit/test_import_service.py -x`.
Confirm `process_import_file()` still owns the serial wrapper behavior, while `process_import_range()` owns the streamed row iteration and absolute-row skip/break logic.
</verification>

<success_criteria>
Phase 60 has a tested service seam for deterministic row counting and bounded CSV processing, creating the contract the parent coordinator and child chunk workers will both consume.
</success_criteria>

<output>
After completion, create `.planning/phases/60-parent-split-parallel-processing/60-01-SUMMARY.md`
</output>
