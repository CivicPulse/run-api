---
phase: 60-parent-split-parallel-processing
plan: 02
type: execute
wave: 2
depends_on: ["60-01"]
files_modified:
  - app/tasks/import_task.py
  - tests/unit/test_import_task.py
autonomous: true
requirements: [CHUNK-02, CHUNK-03]
must_haves:
  truths:
    - "When a file qualifies for chunking, the parent task pre-scans or reuses `total_rows` before making the routing decision."
    - "The parent coordinator creates all `ImportChunk` rows up front from deterministic ranges and defers one child task per chunk."
    - "Large-file orchestration failures are explicit parent failures, not silent fallback to the serial path."
  artifacts:
    - path: "app/tasks/import_task.py"
      provides: "parent pre-scan/fan-out branch inside `process_import` plus a deferable child-task contract"
    - path: "tests/unit/test_import_task.py"
      provides: "coverage for parent pre-scan, deterministic chunk creation, eager child deferral, and fail-fast behavior"
  key_links:
    - from: "app/tasks/import_task.py"
      to: "app.services.import_service.count_csv_data_rows"
      via: "large-file pre-scan before routing"
      pattern: "count_csv_data_rows"
    - from: "app/tasks/import_task.py"
      to: "app.services.import_service.plan_chunk_ranges"
      via: "deterministic parent chunk planning"
      pattern: "plan_chunk_ranges"
    - from: "app/tasks/import_task.py"
      to: "process_import_chunk"
      via: "one deferred child job per chunk"
      pattern: "process_import_chunk"
---

<objective>
Turn `process_import` into the Phase 60 parent coordinator without changing the API entrypoint or leaking chunk state into the user-facing import surface.

Purpose: Implement D-01 through D-04 and D-07 through D-10 by extending the existing large-file branch inside `process_import`, creating deterministic chunk rows, and eagerly deferring one child task per chunk with explicit failure behavior.
Output: Parent-side pre-scan and fan-out orchestration in `process_import`, a defined child-task entrypoint, and unit coverage for no-fallback failure cases.
</objective>

<execution_context>
@/home/kwhatcher/.codex/get-shit-done/workflows/execute-plan.md
@/home/kwhatcher/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/60-parent-split-parallel-processing/60-CONTEXT.md
@.planning/phases/60-parent-split-parallel-processing/60-RESEARCH.md
@.planning/phases/57-recovery-engine-completion-hardening/57-01-SUMMARY.md
@.planning/phases/59-chunk-schema-configuration/59-01-SUMMARY.md
@.planning/phases/59-chunk-schema-configuration/59-02-SUMMARY.md
@.planning/phases/60-parent-split-parallel-processing/60-01-PLAN.md
@app/tasks/import_task.py
@tests/unit/test_import_task.py

<interfaces>
From app/tasks/import_task.py:
```python
@procrastinate_app.task(name="process_import", queue="imports")
async def process_import(import_job_id: str, campaign_id: str) -> None: ...
```

From app/models/import_job.py:
```python
class ImportChunkStatus(enum.StrEnum):
    PENDING = "pending"
    QUEUED = "queued"
    PROCESSING = "processing"
    COMPLETED = "completed"
    FAILED = "failed"
    CANCELLED = "cancelled"

class ImportChunk(Base):
    import_job_id: Mapped[uuid.UUID]
    row_start: Mapped[int]
    row_end: Mapped[int]
    status: Mapped[ImportChunkStatus]
```

From app/services/import_service.py after Plan 60-01:
```python
async def count_csv_data_rows(storage, file_key: str) -> int: ...
def plan_chunk_ranges(total_rows: int, mapped_column_count: int, chunk_size_default: int) -> list[tuple[int, int]]: ...
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add unit coverage for the parent coordinator branch</name>
  <files>tests/unit/test_import_task.py</files>
  <behavior>
    - Test 1: when `job.total_rows` is unknown and the pre-scan result is below the threshold, `process_import` stays on the serial path per D-09.
    - Test 2: when the pre-scan result exceeds the threshold, `process_import` creates deterministic chunk rows and defers one child per range per D-03 and D-04.
    - Test 3: if pre-scan, chunk creation, or child deferral fails before child processing begins, the parent import is marked failed and does not call the serial fallback path per D-07 and D-08.
  </behavior>
  <action>Extend `tests/unit/test_import_task.py` before changing production code. Cover three coordinator outcomes: serial-bypass preservation for below-threshold files, eager chunk fan-out for above-threshold files, and explicit fail-fast behavior on startup orchestration errors. Assert that `process_import_file()` is not called once the parent qualifies for chunking, that `ImportChunk` rows are created from `plan_chunk_ranges()` output in order, and that each successful defer updates the chunk to a queued/executable state. Keep recovery and advisory-lock assertions from Phase 57 intact.</action>
  <verify>
    <automated>uv run pytest tests/unit/test_import_task.py -x</automated>
  </verify>
  <done>The task test suite defines the required Phase 60 parent behaviors before the coordinator branch is implemented.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement parent pre-scan, chunk creation, and eager fan-out inside `process_import`</name>
  <files>app/tasks/import_task.py, tests/unit/test_import_task.py</files>
  <behavior>
    - Test 1: `process_import` persists `job.total_rows` after a successful pre-scan and reruns the serial threshold decision.
    - Test 2: above-threshold imports create all `ImportChunk` records up front and defer one child task per chunk.
    - Test 3: parent orchestration failures update the parent import to `FAILED` with an orchestration-specific error instead of continuing serial execution.
  </behavior>
  <action>Modify `process_import()` in `app/tasks/import_task.py` so the existing large-file branch becomes the parent coordinator per D-01 and D-02. Reuse `job.total_rows` when already present; otherwise call `service.count_csv_data_rows(...)`, persist the result, and only then reevaluate `should_use_serial_import(...)`. If the import remains below the threshold, continue on the existing serial path unchanged per D-09. If it qualifies for chunking, compute ranges from `plan_chunk_ranges(...)`, insert all `ImportChunk` rows up front per D-03, and defer one child job per chunk immediately per D-04. Define the child task entrypoint in this file so the defer target exists, but keep the child body minimal or deferred for Plan 60-03. If pre-scan, chunk insert, or initial deferral fails before chunk processing begins, mark the parent import failed with a clear orchestration error and do not call `service.process_import_file(...)`. Do not add parent progress aggregation, merged error reporting, cancellation propagation, or any API/schema changes.</action>
  <verify>
    <automated>uv run pytest tests/unit/test_import_task.py -x</automated>
  </verify>
  <done>`process_import` remains the sole queue entrypoint but now acts as the Phase 60 parent coordinator for large imports, with deterministic chunk creation and eager fan-out guarded by explicit fail-fast behavior.</done>
</task>

</tasks>

<verification>
Run `uv run pytest tests/unit/test_import_task.py -x`.
Spot-check the resulting code path for the three allowed behaviors only: serial unchanged below threshold, parent fan-out above threshold, explicit failure on startup orchestration errors.
</verification>

<success_criteria>
Large imports are pre-scanned and fanned out from the existing `process_import` task, while small imports stay on the serial path and orchestration failures remain explicit and debuggable.
</success_criteria>

<output>
After completion, create `.planning/phases/60-parent-split-parallel-processing/60-02-SUMMARY.md`
</output>
