---
phase: 61-completion-aggregation-error-merging
plan: 02
type: execute
wave: 2
depends_on: ["61-01"]
files_modified:
  - app/services/import_service.py
  - app/tasks/import_task.py
  - tests/unit/test_import_service.py
  - tests/unit/test_import_task.py
autonomous: true
requirements: [PROG-01, PROG-02, PROG-03, PROG-05]
must_haves:
  truths:
    - "Users see authoritative parent totals that are recomputed from durable chunk rows instead of drifting through per-worker parent updates."
    - "The parent import stays PROCESSING until exactly one locked finalizer publishes the authoritative terminal result."
    - "Partial-failure imports expose one merged parent error artifact after all chunks finish, without leaking chunk-level storage details."
    - "Mixed chunk success and failure ends in COMPLETED_WITH_ERRORS, all-failed ends in FAILED, and full success ends in COMPLETED."
  artifacts:
    - path: "app/services/import_service.py"
      provides: "locked chunk-summary aggregation, parent-status decision helper, and merged chunk error artifact finalization"
    - path: "app/tasks/import_task.py"
      provides: "post-terminal chunk handoff that calls the shared maybe-finalize helper on both success and failure paths"
    - path: "tests/unit/test_import_service.py"
      provides: "coverage for SQL fan-in summary, merged artifact generation, and parent status decisions"
    - path: "tests/unit/test_import_task.py"
      provides: "coverage for exactly-once handoff from terminal chunk workers into the locked finalizer"
  key_links:
    - from: "app/tasks/import_task.py"
      to: "app/services/import_service.py"
      via: "terminal chunk completion invokes maybe_finalize_chunked_import(job, campaign_id)"
      pattern: "maybe_finalize_chunked_import"
    - from: "app/services/import_service.py"
      to: "import_chunks"
      via: "aggregate query using SQL SUM/count over terminal chunk rows"
      pattern: "func\\.sum|ImportChunkStatus"
    - from: "app/services/import_service.py"
      to: "StorageService"
      via: "merge persisted chunk error_report_key CSVs into one parent error_report_key"
      pattern: "_merge_error_files|error_report_key"
---

<objective>
Implement the exactly-once fan-in finalizer that turns chunk-local results into one authoritative parent import outcome.

Purpose: Fulfill D-01 through D-13 and requirements PROG-01, PROG-02, PROG-03, and PROG-05 by aggregating counters from chunk rows, guarding finalization with a PostgreSQL advisory lock, and merging error CSVs only inside that locked path.
Output: Shared finalizer helpers in `ImportService`, terminal chunk handoff in `process_import_chunk()`, and unit coverage for locked parent completion semantics.
</objective>

<execution_context>
@/home/kwhatcher/.codex/get-shit-done/workflows/execute-plan.md
@/home/kwhatcher/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/61-completion-aggregation-error-merging/61-CONTEXT.md
@.planning/phases/61-completion-aggregation-error-merging/61-RESEARCH.md
@.planning/phases/61-completion-aggregation-error-merging/61-VALIDATION.md
@.planning/phases/60-parent-split-parallel-processing/60-03-SUMMARY.md
@.planning/phases/61-completion-aggregation-error-merging/61-01-SUMMARY.md
@app/services/import_service.py
@app/tasks/import_task.py
@tests/unit/test_import_service.py
@tests/unit/test_import_task.py

<interfaces>
From app/tasks/import_task.py:
```python
@procrastinate_app.task(name="process_import_chunk", queue="imports")
async def process_import_chunk(chunk_id: str, campaign_id: str) -> None: ...
```

From app/services/import_service.py:
```python
async def _merge_error_files(
    self,
    storage: StorageService,
    job: ImportJob | ImportChunk,
    error_keys: list[str],
) -> None: ...
```

Current chunk worker boundary from Phase 60:
```python
if chunk.status == ImportChunkStatus.PROCESSING:
    chunk.status = ImportChunkStatus.COMPLETED
    await session.commit()
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add unit tests for locked fan-in finalization and terminal chunk handoff</name>
  <files>tests/unit/test_import_service.py, tests/unit/test_import_task.py</files>
  <read_first>
    - tests/unit/test_import_service.py
    - tests/unit/test_import_task.py
    - app/services/import_service.py
    - app/tasks/import_task.py
    - app/models/import_job.py
  </read_first>
  <acceptance_criteria>
    - `rg -n "maybe_finalize_chunked_import|COMPLETED_WITH_ERRORS|all chunks are terminal|pg_try_advisory" tests/unit/test_import_service.py tests/unit/test_import_task.py` finds explicit unit coverage for the locked finalizer and task handoff.
    - `uv run pytest tests/unit/test_import_service.py tests/unit/test_import_task.py -x` fails or is missing before implementation.
  </acceptance_criteria>
  <behavior>
    - Test 1: `maybe_finalize_chunked_import()` exits without writes when any chunk is still non-terminal, per D-03 and D-07.
    - Test 2: mixed `COMPLETED` + `FAILED` chunk states produce parent `COMPLETED_WITH_ERRORS`, all-failed produces parent `FAILED`, and all-completed produces parent `COMPLETED`, per D-01 and D-02.
    - Test 3: only the worker that acquires the PostgreSQL advisory lock runs aggregation and error merge; lock losers return without mutating the parent, per D-06.
    - Test 4: `process_import_chunk()` calls the shared finalizer after committing either `COMPLETED` or `FAILED`, while keeping chunk-local progress ownership from D-14 intact.
  </behavior>
  <action>Extend `tests/unit/test_import_service.py` with service-level RED coverage for a new `maybe_finalize_chunked_import()` helper, using mocked SQLAlchemy execute results to represent terminal/non-terminal chunk summaries, chunk `error_report_key` lists, and advisory-lock success/failure. Extend `tests/unit/test_import_task.py` so `process_import_chunk()` must call the finalizer after its terminal-state commit in both the success path and the exception path. Keep the tests focused on D-01 through D-13: no cancellation propagation, no per-chunk resume, and no deadlock logic from later phases.</action>
  <verify>
    <automated>uv run pytest tests/unit/test_import_service.py tests/unit/test_import_task.py -x</automated>
  </verify>
  <done>The unit suite defines the exact locked finalization behavior, the parent status decision tree, and the task-to-service handoff before implementation begins.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement the shared locked finalizer and wire it from terminal chunk workers</name>
  <files>app/services/import_service.py, app/tasks/import_task.py, tests/unit/test_import_service.py, tests/unit/test_import_task.py</files>
  <read_first>
    - app/services/import_service.py
    - app/tasks/import_task.py
    - app/models/import_job.py
    - tests/unit/test_import_service.py
    - tests/unit/test_import_task.py
  </read_first>
  <acceptance_criteria>
    - `rg -n "maybe_finalize_chunked_import|pg_try_advisory_xact_lock|COMPLETED_WITH_ERRORS" app/services/import_service.py app/tasks/import_task.py` finds the locked finalizer, advisory-lock call, and new terminal status handling.
    - `rg -n "SUM\\(|func\\.sum|error_report_key" app/services/import_service.py` finds SQL aggregation and merged-artifact logic driven by chunk rows.
    - `uv run pytest tests/unit/test_import_service.py tests/unit/test_import_task.py -x` passes.
  </acceptance_criteria>
  <action>In `app/services/import_service.py`, add small helpers with concrete responsibilities: a transaction-scoped advisory-lock claimer for the parent import, a chunk summary query using SQL `SUM()`/counts over `ImportChunk` rows per D-05, a status decision helper implementing D-01/D-02, a parent summary-message builder per D-04/D-13, and `maybe_finalize_chunked_import(session, storage, job, campaign_id)` that re-checks parent terminal state, confirms all chunks are terminal per D-07, merges only non-null chunk `error_report_key` CSVs per D-08/D-11, and sets parent `imported_rows`, `skipped_rows`, `phones_created`, `error_report_key`, `error_message`, and `status` exactly once. In `app/tasks/import_task.py`, after a chunk commits `COMPLETED` or `FAILED`, refresh or reload the parent job under RLS and call `service.maybe_finalize_chunked_import(...)`; keep chunk workers from directly incrementing parent counters or setting terminal parent status outside the service helper, per D-14. If merged CSV generation fails, let finalization fail explicitly and keep the parent non-terminal rather than reporting success, per D-12.</action>
  <verify>
    <automated>uv run pytest tests/unit/test_import_service.py tests/unit/test_import_task.py -x</automated>
  </verify>
  <done>Chunk workers can safely race to finalize, but only one locked path aggregates chunk totals, merges chunk error CSVs, and assigns the correct parent terminal status and summary message.</done>
</task>

</tasks>

<verification>
Run `uv run pytest tests/unit/test_import_service.py tests/unit/test_import_task.py -x`.
Confirm the parent remains `PROCESSING` until `maybe_finalize_chunked_import()` commits a terminal outcome and that no chunk path writes parent totals incrementally.
</verification>

<success_criteria>
Phase 61 has one authoritative parent finalizer: it uses chunk rows as the source of truth, merges chunk error artifacts once, and produces `COMPLETED`, `FAILED`, or `COMPLETED_WITH_ERRORS` according to the full chunk set.
</success_criteria>

<output>
After completion, create `.planning/phases/61-completion-aggregation-error-merging/61-02-SUMMARY.md`
</output>
