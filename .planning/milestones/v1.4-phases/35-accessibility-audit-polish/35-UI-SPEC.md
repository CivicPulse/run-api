---
phase: 35
slug: accessibility-audit-polish
status: draft
shadcn_initialized: true
preset: new-york
created: 2026-03-16
---

# Phase 35 — UI Design Contract

> Visual and interaction contract for the accessibility audit and polish pass. Generated by gsd-ui-researcher, verified by gsd-ui-checker.

---

## Design System

| Property | Value |
|----------|-------|
| Tool | shadcn |
| Preset | new-york |
| Component library | radix |
| Icon library | lucide |
| Font | System default (font-sans via Tailwind) |

---

## Spacing Scale

Declared values (must be multiples of 4):

| Token | Value | Usage |
|-------|-------|-------|
| xs | 4px | Icon gaps, inline padding |
| sm | 8px | Compact element spacing, gap-2 |
| md | 16px | Default element spacing, px-4 |
| lg | 24px | Section padding, space-y-6 |
| xl | 32px | Layout gaps |
| 2xl | 48px | Major section breaks |
| 3xl | 64px | Page-level spacing |

Exceptions: All interactive touch targets must have a minimum 44x44px hit area (WCAG 2.5.5). Tailwind class: `min-h-11 min-w-11`. Elements that are visually smaller than 44px use invisible padding to expand the tap area.

---

## Typography

| Role | Size | Weight | Line Height |
|------|------|--------|-------------|
| Body | 14px (text-sm) | 400 (font-normal) | 1.5 |
| Label | 14px (text-sm) | 600 (font-semibold) | 1.5 |
| Heading | 20px (text-xl) | 600 (font-semibold) | 1.2 |
| Caption | 12px (text-xs) | 400 (font-normal) | 1.5 |

Two-weight scale: 400 (font-normal) for body and caption text, 600 (font-semibold) for labels and headings. No intermediate weights. This phase does not introduce new typography. Existing field mode typography is confirmed correct. FieldHeader title uses `text-sm font-semibold`. Completion headings use `text-xl font-semibold`. Body content uses `text-sm` or `text-base`.

---

## Color

| Role | Value | Usage |
|------|-------|-------|
| Dominant (60%) | `oklch(1 0 0)` / var(--background) | Background, surfaces |
| Secondary (30%) | `oklch(1 0 0)` / var(--card) | Cards (VoterCard, CompletionSummary, QuickStartCard) |
| Accent (10%) | `oklch(0.205 0 0)` / var(--primary) | Primary CTA buttons (Save Answers, Back to Hub) |
| Destructive | `oklch(0.577 0.245 27.325)` / var(--destructive) | End Session confirmation only |

Accent reserved for: Primary CTA buttons, active selection states (selected scale button), primary link actions.

### Semantic Color Badges (Audit Targets)

These existing color-coded badges must pass WCAG AA 4.5:1 contrast ratio. Current values and required adjustments:

| Badge Type | Current BG | Current Text | Contrast Action |
|------------|-----------|-------------|-----------------|
| Party: Democrat | `bg-blue-100` | `text-blue-800` | Verify >= 4.5:1; adjust to `text-blue-900` if needed |
| Party: Republican | `bg-red-100` | `text-red-800` | Verify >= 4.5:1; adjust to `text-red-900` if needed |
| Party: Other | `bg-gray-100` | `text-gray-700` | Verify >= 4.5:1; likely passes |
| Propensity High (>=70) | `bg-green-100` | `text-green-700` | Verify >= 4.5:1; adjust to `text-green-800` if needed |
| Propensity Mid (40-69) | `bg-yellow-100` | `text-yellow-700` | Verify >= 4.5:1; adjust to `text-yellow-800` if needed |
| Propensity Low (<40) | `bg-red-100` | `text-red-700` | Verify >= 4.5:1; adjust to `text-red-800` if needed |
| Outcome: Supporter | `bg-green-100` | `text-green-800` | Verify >= 4.5:1 |
| Outcome: Undecided | `bg-yellow-100` | `text-yellow-800` | Verify >= 4.5:1 |
| Outcome: Refused/Opposed | `bg-red-100` | `text-red-800` | Verify >= 4.5:1 |
| Outcome: Neutral | `bg-gray-100` | `text-gray-700` | Verify >= 4.5:1 |

Rule: Preserve color-coding intent. Only darken text or lighten background as needed. Do not change the semantic color family.

---

## Copywriting Contract

| Element | Copy |
|---------|------|
| Milestone 25% toast | "Great start! 25% done." |
| Milestone 50% toast | "Halfway there! Keep it up." |
| Milestone 75% toast | "Almost done! 75% complete." |
| Milestone 100% toast | "All done! Amazing work." |
| Canvassing completion heading | "Great work!" |
| Canvassing completion body | "You completed your walk list." |
| Phone banking completion heading | "Great work!" (existing, no change) |
| Phone banking completion body | "You completed your calling session." (existing, no change) |
| Back to Hub CTA | "Back to Hub" |

### ARIA Announcement Copy

| Transition | Announcement Text |
|------------|-------------------|
| Canvassing door advance | "Now at {address}, door {current} of {total}" |
| Phone banking call advance | "Now calling {voterName}, call {current} of {total}" |
| InlineSurvey open | "Survey questions for {voterName}" (aria-label on dialog) |
| OutcomeGrid button | "Record {outcomeLabel} for {voterName}" (aria-label per button) |
| FieldProgress | "{current} of {total} {unit}" (existing role="status", verify) |
| OfflineBanner | "You are offline. Changes will sync when connection returns." (existing aria-live, verify) |

### Destructive Actions

| Action | Confirmation Approach |
|--------|----------------------|
| End Canvassing Session | Existing pattern: confirm dialog before navigating away. No copy change. |
| End Phone Banking Session | Existing pattern: confirm dialog. No copy change. |

---

## ARIA & Landmark Contract

### Landmark Structure

| Element | Role | Attribute |
|---------|------|-----------|
| FieldHeader | `<nav>` | `aria-label="Field navigation"` |
| Main content area | `<main>` | `aria-label="Field mode content"` |
| FieldProgress | `<div>` | `role="status"` + `aria-live="polite"` (existing, verify) |
| OfflineBanner | `<div>` | `role="status"` + `aria-live="polite"` (existing, verify) |
| InlineSurvey SheetContent | add `role="dialog"` | `aria-label="Survey questions for {voterName}"` |
| Door/Call transition announcer | `<div>` | `aria-live="polite"` + `className="sr-only"` |

### Focus Management

| Interaction | Focus Behavior |
|-------------|---------------|
| InlineSurvey opens | Focus moves to first question or SheetTitle |
| InlineSurvey closes (skip/save) | Focus returns to the element that triggered it (outcome button area) |
| Wizard advances to next door/call | Focus stays on main content; screen reader hears live region announcement |
| CompletionSummary appears | Focus moves to heading |

---

## Touch Target Audit Contract

### Minimum Size

All interactive elements in field routes: 44x44px minimum bounding box (WCAG 2.5.5).

Tailwind pattern: `min-h-11 min-w-11` (44px = 2.75rem = h-11).

### Known Elements Requiring Verification

| Component | Element | Current Size | Action |
|-----------|---------|-------------|--------|
| OutcomeGrid | Outcome buttons | `min-h-11 min-w-11` | Verify maintained |
| FieldHeader | Back button | `min-h-11 min-w-11` | Verify maintained |
| FieldHeader | Help button | `min-h-11 min-w-11` | Verify maintained |
| FieldHeader | Avatar menu | `min-h-11 min-w-11` | Verify maintained |
| InlineSurvey | Scale buttons | `min-h-11 min-w-11` | Verify maintained |
| InlineSurvey | Skip/Save buttons | `min-h-11` | Verify; add `min-w-11` if missing |
| InlineSurvey | Radio labels | `min-h-11` | Verify maintained |
| DoorListView | List item tap areas | TBD | Verify >= 44px height |
| PhoneNumberList | Phone number buttons | TBD | Verify >= 44px tap area |
| QuickStartCard | Dismiss/action buttons | TBD | Verify >= 44px tap area |
| TooltipIcon | Icon trigger | TBD | Verify >= 44px tap area (may need invisible padding) |
| ResumePrompt | Action buttons | TBD | Verify >= 44px tap area |

### Automated CI Check

Playwright test scans all `button`, `a[href]`, `[role="button"]`, and `input` elements within `/field/` routes. Asserts `boundingBox.width >= 44 && boundingBox.height >= 44`. This test becomes a permanent CI fixture.

---

## Milestone Celebration Contract

### Thresholds and Copy

| Threshold | Emoji | Toast Message | Duration | Behavior |
|-----------|-------|---------------|----------|----------|
| 25% | party popper | "Great start! 25% done." | 3000ms | Auto-dismiss, non-blocking |
| 50% | fire | "Halfway there! Keep it up." | 3000ms | Auto-dismiss, non-blocking |
| 75% | rocket | "Almost done! 75% complete." | 3000ms | Auto-dismiss, non-blocking |
| 100% | trophy | "All done! Amazing work." | 3000ms | Auto-dismiss + show CompletionSummary |

### Implementation Pattern

- Use `sonner` toast (already configured in `web/src/components/ui/sonner.tsx`)
- Track fired milestones in sessionStorage key: `milestones-fired-{sessionType}-{assignmentId}`
- Value: JSON array of fired threshold numbers, e.g. `[25, 50]`
- Check threshold on each progress change; fire once per session only
- Applies to both canvassing (doors completed / total) and phone banking (calls completed / total)

### Canvassing CompletionSummary

New component modeled on existing phone banking `CompletionSummary`:

| Stat | Source |
|------|--------|
| Doors visited | Count of entries with status !== "pending" |
| Contacted | Count of contact outcomes (supporter, undecided, opposed, refused) |
| Not Home | Count of not_home outcomes |
| Other | Remaining outcome count |

Layout: Same as phone banking CompletionSummary -- centered Card with CheckCircle2 icon, heading, body, stats list, "Back to Hub" button. Focal point: The `CheckCircle2` icon (rendered at 48px in the accent/success color) paired with the "Great work!" heading serves as the primary visual anchor, drawing the eye to the center of the card before the user scans the stats below. This mirrors the established phone banking CompletionSummary visual hierarchy.

---

## Registry Safety

| Registry | Blocks Used | Safety Gate |
|----------|-------------|-------------|
| shadcn official | sheet, button, card, sonner, radio-group, textarea, label, avatar, dropdown-menu, dialog | not required |
| Third-party | none | not applicable |

---

## Checker Sign-Off

- [ ] Dimension 1 Copywriting: PASS
- [ ] Dimension 2 Visuals: PASS
- [ ] Dimension 3 Color: PASS
- [ ] Dimension 4 Typography: PASS
- [ ] Dimension 5 Spacing: PASS
- [ ] Dimension 6 Registry Safety: PASS

**Approval:** pending
