---
phase: 34-guided-onboarding-tour
plan: 00
type: execute
wave: 1
depends_on: []
files_modified:
  - web/package.json
  - web/src/stores/tourStore.test.ts
  - web/src/hooks/useTour.test.ts
  - web/e2e/tour-onboarding.spec.ts
autonomous: true
requirements:
  - TOUR-01
  - TOUR-02
  - TOUR-03
  - TOUR-04
  - TOUR-05
  - TOUR-06
must_haves:
  truths:
    - "driver.js is installed and importable"
    - "Test stub files exist so downstream plans have automated verify targets"
  artifacts:
    - path: "web/src/stores/tourStore.test.ts"
      provides: "Unit test stubs for tour store logic (TOUR-03, TOUR-06)"
      contains: "describe"
    - path: "web/src/hooks/useTour.test.ts"
      provides: "Unit test stubs for useTour hook (TOUR-02)"
      contains: "describe"
    - path: "web/e2e/tour-onboarding.spec.ts"
      provides: "E2E test stubs for tour integration (TOUR-01, TOUR-04, TOUR-05)"
      contains: "test"
  key_links: []
---

<objective>
Install driver.js and create test stub files that downstream plans (34-01, 34-02, 34-03) reference as automated verify targets. This is Wave 0 -- Nyquist compliance requires these stubs to exist before implementation tasks run.

Purpose: Satisfies the Nyquist rule that every task verify target must reference an existing test file. Also moves driver.js installation out of Plan 01 so it is available as a precondition.
Output: driver.js in node_modules, 3 test stub files with describe/test skeletons.
</objective>

<execution_context>
@/home/kwhatcher/.claude/get-shit-done/workflows/execute-plan.md
@/home/kwhatcher/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/34-guided-onboarding-tour/34-CONTEXT.md
@.planning/phases/34-guided-onboarding-tour/34-VALIDATION.md

<interfaces>
<!-- Existing test patterns to follow -->

From web/src/stores/offlineQueueStore.test.ts:
```typescript
import { describe, test, expect, beforeEach, vi } from "vitest"
import { useOfflineQueueStore } from "@/stores/offlineQueueStore"

describe("offlineQueueStore", () => {
  beforeEach(() => {
    useOfflineQueueStore.getState().clear()
    localStorage.clear()
    vi.restoreAllMocks()
  })

  test("push() adds item ...", () => {
    // ...
  })
})
```

From web/e2e/phase33-offline-sync.spec.ts:
```typescript
import { test, expect } from "@playwright/test"
// ...
test("offline sync ...", async ({ page }) => {
  // ...
})
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install driver.js and create unit test stubs</name>
  <files>web/package.json, web/src/stores/tourStore.test.ts, web/src/hooks/useTour.test.ts</files>
  <action>
1. Install driver.js:
```bash
cd web && npm install driver.js
```

2. Create `web/src/stores/tourStore.test.ts` with stub tests for TOUR-03 and TOUR-06:

```typescript
import { describe, test, expect, beforeEach } from "vitest"

// Store import will be added once tourStore.ts is created in Plan 01
// import { useTourStore, tourKey } from "@/stores/tourStore"

describe("tourStore", () => {
  beforeEach(() => {
    localStorage.clear()
  })

  describe("tourKey", () => {
    test.todo("builds key from campaignId and userId")
  })

  describe("completions (TOUR-03)", () => {
    test.todo("markComplete sets segment to true for given key")
    test.todo("isSegmentComplete returns false for unknown key")
    test.todo("isSegmentComplete returns true after markComplete")
    test.todo("completion state persists to localStorage")
    test.todo("completion state survives store rehydration")
  })

  describe("sessionCounts (TOUR-06)", () => {
    test.todo("incrementSession starts at 1 for new key")
    test.todo("incrementSession increments existing count")
    test.todo("getSessionCount returns 0 for unknown key")
    test.todo("sessionCounts persist to localStorage")
  })

  describe("shouldShowQuickStart (TOUR-06)", () => {
    test.todo("returns true when session count < 3 and not dismissed and not running")
    test.todo("returns false when session count >= 3")
    test.todo("returns false when dismissed this session")
    test.todo("returns false when tour is running")
  })

  describe("transient state", () => {
    test.todo("isRunning defaults to false")
    test.todo("isRunning is NOT persisted to localStorage")
    test.todo("dismissedThisSession is NOT persisted to localStorage")
  })
})
```

3. Create `web/src/hooks/useTour.test.ts` with stub tests for TOUR-02:

```typescript
import { describe, test } from "vitest"

// Hook import will be added once useTour.ts is created in Plan 01
// import { useTour } from "@/hooks/useTour"

describe("useTour (TOUR-02)", () => {
  describe("startSegment", () => {
    test.todo("creates driver.js instance with provided steps")
    test.todo("calls setRunning(true) on start")
    test.todo("calls markComplete on destroy")
    test.todo("calls setRunning(false) on destroy")
  })

  describe("cleanup", () => {
    test.todo("destroys active driver instance on unmount")
    test.todo("sets isRunning to false on unmount")
  })

  describe("replacement", () => {
    test.todo("destroys previous driver instance when starting new segment")
  })
})
```
  </action>
  <verify>
    <automated>cd /home/kwhatcher/projects/run-api/web && node -e "require('driver.js')" 2>/dev/null && echo "driver.js OK" || echo "FAIL: driver.js"; test -f src/stores/tourStore.test.ts && echo "tourStore.test.ts OK" || echo "FAIL: tourStore.test.ts"; test -f src/hooks/useTour.test.ts && echo "useTour.test.ts OK" || echo "FAIL: useTour.test.ts"</automated>
  </verify>
  <done>driver.js installed. Unit test stubs exist for tourStore (completions, sessionCounts, shouldShowQuickStart, transient state) and useTour (startSegment, cleanup, replacement).</done>
</task>

<task type="auto">
  <name>Task 2: Create e2e test stub for tour onboarding</name>
  <files>web/e2e/tour-onboarding.spec.ts</files>
  <action>
Create `web/e2e/tour-onboarding.spec.ts` with stub tests for TOUR-01, TOUR-04, TOUR-05:

```typescript
import { test, expect } from "@playwright/test"

test.describe("Tour onboarding (Phase 34)", () => {
  test.describe("Welcome tour (TOUR-01)", () => {
    test.fixme("auto-triggers on first visit to field hub", async ({ page }) => {
      // Will navigate to /field/{campaignId} and verify driver.js popover appears
    })

    test.fixme("does not auto-trigger on second visit", async ({ page }) => {
      // Will set localStorage completion flag then navigate, verify no popover
    })

    test.fixme("shows 4 steps with progress indicator", async ({ page }) => {
      // Will step through tour and verify step count
    })
  })

  test.describe("Help button replay (TOUR-04)", () => {
    test.fixme("replays welcome tour from hub help button", async ({ page }) => {
      // Will click help button on hub, verify driver.js popover appears
    })

    test.fixme("replays canvassing tour from canvassing help button", async ({ page }) => {
      // Will click help button on canvassing screen, verify correct segment
    })

    test.fixme("replays phone banking tour from phone banking help button", async ({ page }) => {
      // Will click help button on phone banking screen, verify correct segment
    })
  })

  test.describe("Quick-start cards (TOUR-05)", () => {
    test.fixme("shows quick-start card on canvassing for first 3 sessions", async ({ page }) => {
      // Will navigate to canvassing and verify blue card is visible
    })

    test.fixme("dismisses quick-start card on X click", async ({ page }) => {
      // Will click dismiss and verify card is hidden
    })

    test.fixme("hides quick-start card during active tour", async ({ page }) => {
      // Will trigger tour and verify card is not visible
    })
  })

  test.describe("Data-tour attributes (TOUR-01)", () => {
    test.fixme("hub has hub-greeting, assignment-card, help-button, avatar-menu attributes", async ({ page }) => {
      // Will navigate to hub and verify data-tour attributes exist
    })

    test.fixme("canvassing has household-card, outcome-grid, progress-bar, door-list-button, skip-button attributes", async ({ page }) => {
      // Will navigate to canvassing and verify data-tour attributes exist
    })

    test.fixme("phone banking has phone-number-list, outcome-grid, end-session-button attributes", async ({ page }) => {
      // Will navigate to phone banking and verify data-tour attributes exist
    })
  })
})
```

Use `test.fixme()` (Playwright's built-in skip mechanism for tests that are known to not work yet) instead of `test.skip()` because these stubs need implementation code to pass. fixme tests show in reports as "fixme" rather than "skipped", making it clear they need work.
  </action>
  <verify>
    <automated>cd /home/kwhatcher/projects/run-api/web && test -f e2e/tour-onboarding.spec.ts && grep -q "test.describe" e2e/tour-onboarding.spec.ts && echo "e2e stub OK" || echo "FAIL: e2e stub"</automated>
  </verify>
  <done>E2E test stub exists with fixme tests covering welcome tour trigger, help button replay, quick-start cards, and data-tour attributes across all 3 field screens.</done>
</task>

</tasks>

<verification>
- `cd web && npm ls driver.js` shows driver.js installed
- `cd web && npx vitest run src/stores/tourStore.test.ts` runs (all tests are .todo so they show as skipped, not failures)
- `cd web && npx vitest run src/hooks/useTour.test.ts` runs (all tests are .todo so they show as skipped)
- `cd web && npx playwright test e2e/tour-onboarding.spec.ts` runs (all tests are .fixme so they show as fixme, not failures)
</verification>

<success_criteria>
- driver.js is installed in web/package.json and importable
- web/src/stores/tourStore.test.ts exists with .todo stubs for completions, sessionCounts, shouldShowQuickStart
- web/src/hooks/useTour.test.ts exists with .todo stubs for startSegment, cleanup, replacement
- web/e2e/tour-onboarding.spec.ts exists with .fixme stubs for auto-trigger, replay, quick-start, data-tour attributes
- All 3 test files can be run without errors (stubs are skipped/todo, not failing)
</success_criteria>

<output>
After completion, create `.planning/phases/34-guided-onboarding-tour/34-00-SUMMARY.md`
</output>
