---
phase: 19-verification-validation-gaps
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/15-call-lists-dnc-management/15-VERIFICATION.md
  - .planning/phases/15-call-lists-dnc-management/15-VALIDATION.md
  - web/src/hooks/useCallLists.test.ts
  - web/src/hooks/useDNC.test.ts
  - web/src/routes/campaigns/$campaignId/phone-banking/dnc/index.test.tsx
  - web/e2e/phase-15-verification.spec.ts
autonomous: true
requirements:
  - CALL-01
  - CALL-02
  - CALL-03
  - CALL-04
  - CALL-05
  - CALL-06
  - CALL-07
  - CALL-08

must_haves:
  truths:
    - "Phase 15 has a VERIFICATION.md with observable truths table covering all 8 CALL requirements"
    - "Each CALL requirement is marked SATISFIED with code inspection evidence"
    - "All 13 it.todo stubs in Phase 15 test files are replaced with real test implementations"
    - "All 13 tests pass when run with vitest"
    - "Phase 15 VALIDATION.md updated to wave_0_complete: true"
    - "Playwright spec covers 2-3 key visual flows for Phase 15"
  artifacts:
    - path: ".planning/phases/15-call-lists-dnc-management/15-VERIFICATION.md"
      provides: "Formal verification document for all 8 CALL requirements"
      contains: "status: passed"
    - path: "web/src/hooks/useCallLists.test.ts"
      provides: "5 implemented hook tests replacing it.todo stubs"
      min_lines: 60
    - path: "web/src/hooks/useDNC.test.ts"
      provides: "4 implemented hook tests replacing it.todo stubs"
      min_lines: 50
    - path: "web/src/routes/campaigns/$campaignId/phone-banking/dnc/index.test.tsx"
      provides: "4 implemented component tests replacing it.todo stubs"
      min_lines: 60
    - path: "web/e2e/phase-15-verification.spec.ts"
      provides: "Playwright e2e tests for Phase 15 visual workflows"
      min_lines: 30
  key_links:
    - from: "useCallLists.test.ts"
      to: "useCallLists.ts"
      via: "import hooks under test"
      pattern: "import.*useCallLists"
    - from: "useDNC.test.ts"
      to: "useDNC.ts"
      via: "import hooks under test"
      pattern: "import.*useDNC"
    - from: "dnc/index.test.tsx"
      to: "dnc/index.tsx"
      via: "component render test"
      pattern: "DNCListPage"
---

<objective>
Implement all 13 Phase 15 Nyquist test stubs, create formal verification documentation, write a Playwright verification spec, and update VALIDATION.md to wave_0_complete: true.

Purpose: Close both the verification documentation gap AND the Nyquist test gap for Phase 15 -- the last two audit findings blocking 60/60 requirement satisfaction.
Output: 15-VERIFICATION.md + 3 test files with real implementations + phase-15-verification.spec.ts + updated 15-VALIDATION.md
</objective>

<execution_context>
@/home/kwhatcher/.claude/get-shit-done/workflows/execute-plan.md
@/home/kwhatcher/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/19-verification-validation-gaps/19-CONTEXT.md
@.planning/phases/19-verification-validation-gaps/19-RESEARCH.md

# Phase 18 VERIFICATION.md is the FORMAT TEMPLATE
@.planning/phases/18-shift-management/18-VERIFICATION.md

# Source files under test
@web/src/hooks/useCallLists.ts
@web/src/hooks/useDNC.ts
@web/src/routes/campaigns/$campaignId/phone-banking/dnc/index.tsx

# Existing stub files to replace
@web/src/hooks/useCallLists.test.ts
@web/src/hooks/useDNC.test.ts
@web/src/routes/campaigns/$campaignId/phone-banking/dnc/index.test.tsx

# VALIDATION.md to update
@.planning/phases/15-call-lists-dnc-management/15-VALIDATION.md

# Source files for code inspection evidence
@web/src/routes/campaigns/$campaignId/phone-banking/call-lists/index.tsx
@web/src/routes/campaigns/$campaignId/phone-banking/call-lists/$callListId.tsx
@web/src/types/call-list.ts
@web/src/types/dnc.ts

# Existing Playwright pattern reference
@web/e2e/shift-verify.spec.ts

<interfaces>
<!-- Hook APIs the tests must exercise -->

From web/src/hooks/useCallLists.ts:
- useCallLists(campaignId: string) -- GET /api/v1/campaigns/{id}/call-lists -> PaginatedResponse<CallListSummary>
- useCallList(campaignId, callListId) -- GET /api/v1/campaigns/{id}/call-lists/{id} -> CallListDetail
- useCallListEntries(campaignId, callListId, status?) -- GET /api/v1/campaigns/{id}/call-lists/{id}/entries -> PaginatedResponse<CallListEntry>
- useCreateCallList(campaignId) -- POST /api/v1/campaigns/{id}/call-lists { json: CallListCreate } -> CallListDetail
- useUpdateCallList(campaignId, callListId) -- PATCH /api/v1/campaigns/{id}/call-lists/{id} { json: CallListUpdate } -> CallListDetail
- useDeleteCallList(campaignId) -- DELETE /api/v1/campaigns/{id}/call-lists/{id} .then(() => undefined)
- useAppendFromList(campaignId, callListId) -- POST /api/v1/campaigns/{id}/call-lists/{id}/append-from-list { json: { voter_list_id } }

From web/src/hooks/useDNC.ts:
- useDNCEntries(campaignId) -- GET /api/v1/campaigns/{id}/dnc -> DNCEntry[] (NOT paginated)
- useAddDNCEntry(campaignId) -- POST /api/v1/campaigns/{id}/dnc { json: { phone_number, reason? } } -> DNCEntry
- useDeleteDNCEntry(campaignId) -- DELETE /api/v1/campaigns/{id}/dnc/{id} .then(() => undefined)
- useImportDNC(campaignId) -- POST /api/v1/campaigns/{id}/dnc/import { body: FormData } -> DNCImportResult
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement all 13 Nyquist test stubs for Phase 15</name>
  <files>web/src/hooks/useCallLists.test.ts, web/src/hooks/useDNC.test.ts, web/src/routes/campaigns/$campaignId/phone-banking/dnc/index.test.tsx</files>
  <action>
Replace the `it.todo` stubs in all 3 test files with real test implementations. Follow the established hook test pattern from RESEARCH.md exactly (vi.mock @/api/client, makeWrapper factory, mockApi typed cast, renderHook + waitFor pattern).

**useCallLists.test.ts (5 tests):**

1. `useCallLists fetches from /api/v1/campaigns/{id}/call-lists` -- Mock api.get returning PaginatedResponse with one CallListSummary item. renderHook useCallLists("campaign-1"). Assert isSuccess and mockApi.get called with correct URL.

2. `useCreateCallList posts to /api/v1/campaigns/{id}/call-lists with name and voter_list_id` -- Mock api.post returning { json: mockResolvedValue(mockCallList) }. renderHook useCreateCallList. Call mutate({ name: "Test", voter_list_id: "vl-1" }). Assert isSuccess and mockApi.post called with correct URL and { json: { name: "Test", voter_list_id: "vl-1" } }.

3. `useUpdateCallList patches /api/v1/campaigns/{id}/call-lists/{id} with JSON body` -- Mock api.patch returning { json: mockResolvedValue(updatedCallList) }. renderHook useUpdateCallList("campaign-1", "cl-1"). Call mutate({ name: "Updated" }). Assert mockApi.patch called with correct URL and { json: { name: "Updated" } }.

4. `useDeleteCallList sends DELETE to /api/v1/campaigns/{id}/call-lists/{id}` -- Mock api.delete as Promise.resolve() (because hook uses .then(() => undefined)). renderHook useDeleteCallList("campaign-1"). Call mutate("cl-1"). Assert mockApi.delete called with correct URL.

5. `useCallListEntries fetches from /api/v1/campaigns/{id}/call-lists/{id}/entries` -- Mock api.get returning PaginatedResponse with CallListEntry items. renderHook useCallListEntries("campaign-1", "cl-1"). Assert isSuccess and correct URL.

**CRITICAL PITFALL for delete hooks:** useDeleteCallList uses `.then(() => undefined)` NOT `.json()`. Mock must return a thenable: `mockReturnValue(Promise.resolve())` or `mockReturnValue({ then: (fn: Function) => Promise.resolve(fn()) })`.

**useDNC.test.ts (4 tests):**

1. `useDNCEntries fetches array (not paginated) from /api/v1/campaigns/{id}/dnc` -- Mock api.get returning DNCEntry[]. renderHook useDNCEntries("campaign-1"). Assert isSuccess and correct URL.

2. `useAddDNCEntry posts to /api/v1/campaigns/{id}/dnc with phone_number and reason` -- Mock api.post returning { json: mockResolvedValue(mockEntry) }. renderHook useAddDNCEntry. Call mutate({ phone_number: "5551234567", reason: "Requested" }). Assert correct URL and { json: { phone_number: "5551234567", reason: "Requested" } }.

3. `useImportDNC sends FormData multipart POST to /api/v1/campaigns/{id}/dnc/import` -- Mock api.post returning { json: mockResolvedValue({ added: 5, skipped: 2, invalid: 1 }) }. renderHook useImportDNC. Call mutate(new File(["data"], "numbers.csv", { type: "text/csv" })). Assert mockApi.post called with correct URL and { body: expect.any(FormData) }.

4. `useDeleteDNCEntry sends DELETE to /api/v1/campaigns/{id}/dnc/{id}` -- Same .then(() => undefined) pattern as useDeleteCallList. Mock api.delete as Promise.resolve(). Assert correct URL.

**dnc/index.test.tsx (4 component tests):**

Follow the component test pattern from sessions/index.test.tsx (vi.hoisted for store, mock @tanstack/react-router, mock hooks, mock DataTable).

1. `client-side search input filters DNC entries by phone_number substring` -- Mock useDNCEntries returning 3 entries with different phone numbers. Render component. Type a partial phone number in search input. Assert DataTable receives filtered data prop.

2. `search with digits only strips non-digit characters before comparing` -- Same setup. Type "(555) 123" in search input. Assert filtering uses "555123" (digits only) -- entries matching 555123 appear, others don't. This tests the `search.replace(/\D/g, "")` logic.

3. `empty search string shows all entries` -- Mock useDNCEntries returning 3 entries. Render without typing. Assert DataTable receives all 3 entries.

4. `search with no matching entries shows empty state message` -- Mock useDNCEntries returning entries. Type a non-matching search. Assert DataTable receives empty filtered array.

**Mock data shapes:** Use realistic but minimal data. CallListSummary: { id, name, status, total_entries, completed_entries, created_at }. DNCEntry: { id, phone_number, reason, created_at, campaign_id }. Match actual TypeScript type shapes from @/types/call-list.ts and @/types/dnc.ts.

**After implementation:** Run `cd /home/kwhatcher/projects/run-api/web && npm run test -- --run` and confirm all 13 previously-todo tests now pass with 0 failures.
  </action>
  <verify>
    <automated>cd /home/kwhatcher/projects/run-api/web && npm run test -- --run 2>&1 | tail -20</automated>
  </verify>
  <done>All 13 it.todo stubs replaced with real implementations. useCallLists.test.ts has 5 passing tests, useDNC.test.ts has 4 passing tests, dnc/index.test.tsx has 4 passing tests. Full vitest suite green with 0 todo items in Phase 15 files.</done>
</task>

<task type="auto">
  <name>Task 2: Create Phase 15 VERIFICATION.md, Playwright spec, and update VALIDATION.md</name>
  <files>.planning/phases/15-call-lists-dnc-management/15-VERIFICATION.md, web/e2e/phase-15-verification.spec.ts, .planning/phases/15-call-lists-dnc-management/15-VALIDATION.md</files>
  <action>
**Part A: Create 15-VERIFICATION.md** following Phase 18 format EXACTLY.

YAML frontmatter:
- phase: 15-call-lists-dnc-management
- verified: (current ISO timestamp)
- status: passed
- score: N/N must-haves verified
- re_verification: false
- gaps: []
- human_verification: 2-3 items for Playwright-verified workflows

Observable Truths table covering all 8 CALL requirements with code inspection evidence:
- CALL-01 (create call list): useCreateCallList hook in useCallLists.ts, CallListDialog in call-lists/index.tsx, voter_list_id selector, DNC filtering mention
- CALL-02 (call list detail): call-lists/$callListId.tsx route, STATUS_LABELS mapping, entries DataTable with status filter tabs
- CALL-03 (edit/delete): useUpdateCallList + useDeleteCallList hooks, kebab menu actions in call-lists/index.tsx, CallListDialog edit mode
- CALL-04 (DNC list view): dnc/index.tsx route, useDNCEntries hook, DataTable rendering
- CALL-05 (add DNC entry): useAddDNCEntry hook, AddNumber dialog in dnc/index.tsx
- CALL-06 (bulk import): useImportDNC hook with FormData, import dialog in dnc/index.tsx
- CALL-07 (remove DNC): useDeleteDNCEntry hook, Remove button per row
- CALL-08 (DNC search): Client-side search with `search.replace(/\D/g, "")` digit stripping in dnc/index.tsx

Required Artifacts, Key Link Verification, Requirements Coverage, Anti-Patterns, Build/Test sections -- all following Phase 18 format. Read actual source files for line counts and evidence.

**Part B: Create phase-15-verification.spec.ts** with Playwright tests for 2-3 visual workflows per CONTEXT.md:
1. Call list creation + detail view (navigate to call-lists, verify table renders, navigate to a detail page if possible)
2. DNC list with search filtering (navigate to DNC page, verify entries render, type in search)
3. DNC import dialog (navigate to DNC page, verify import button exists)

Follow shift-verify.spec.ts pattern (login helper, CAMPAIGN_ID, BASE, screenshots to test-results/).

**Part C: Update 15-VALIDATION.md** -- Change `wave_0_complete: false` to `wave_0_complete: true` in the YAML frontmatter. Update the Wave 0 Requirements checklist items from `[ ]` to `[x]`. Update task statuses from "pending" to "green" where tests are now implemented.
  </action>
  <verify>
    <automated>test -f /home/kwhatcher/projects/run-api/.planning/phases/15-call-lists-dnc-management/15-VERIFICATION.md && grep "status: passed" /home/kwhatcher/projects/run-api/.planning/phases/15-call-lists-dnc-management/15-VERIFICATION.md && grep "wave_0_complete: true" /home/kwhatcher/projects/run-api/.planning/phases/15-call-lists-dnc-management/15-VALIDATION.md && test -f /home/kwhatcher/projects/run-api/web/e2e/phase-15-verification.spec.ts</automated>
  </verify>
  <done>15-VERIFICATION.md exists with status: passed and all 8 CALL requirements SATISFIED. 15-VALIDATION.md updated to wave_0_complete: true. phase-15-verification.spec.ts exists with 2-3 visual workflow tests. Vitest suite still green after all changes.</done>
</task>

</tasks>

<verification>
- 15-VERIFICATION.md exists with YAML frontmatter status: passed
- All 8 CALL requirements listed as SATISFIED with code inspection evidence
- All 13 it.todo stubs replaced with real tests: `cd web && npm run test -- --run 2>&1 | grep -c "todo"` shows 0 for Phase 15 files
- 15-VALIDATION.md frontmatter shows wave_0_complete: true
- phase-15-verification.spec.ts has 2-3 test cases
- Full vitest suite: `cd web && npm run test -- --run` passes with 0 failures
- TypeScript compilation: `cd web && npx tsc --noEmit` passes
</verification>

<success_criteria>
Phase 15 has formal verification documentation, all Nyquist test stubs are implemented and passing, VALIDATION.md reflects wave_0_complete status, and Playwright spec provides visual regression coverage for key workflows.
</success_criteria>

<output>
After completion, create `.planning/phases/19-verification-validation-gaps/19-02-SUMMARY.md`
</output>
