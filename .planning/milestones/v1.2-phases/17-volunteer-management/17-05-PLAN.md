---
phase: 17-volunteer-management
plan: "05"
type: execute
wave: 3
depends_on:
  - "17-03"
  - "17-04"
files_modified: []
autonomous: false
requirements:
  - VLTR-01
  - VLTR-02
  - VLTR-03
  - VLTR-04
  - VLTR-05
  - VLTR-06
  - VLTR-07
  - VLTR-08
  - VLTR-09

must_haves:
  truths:
    - "Full test suite passes (backend + frontend)"
    - "TypeScript compiles with no errors"
    - "All volunteer pages render without runtime errors"
    - "User confirms visual correctness of roster, detail, tags, and register pages"
  artifacts: []
  key_links: []
---

<objective>
Run full test suite verification and visual checkpoint to confirm Phase 17 is complete.

Purpose: Final quality gate ensuring all backend tests pass, all frontend tests pass (with stubs as pending), TypeScript compiles, and a human verifies the visual output of all volunteer management pages.

Output: Verified phase ready for /gsd:verify-work.
</objective>

<execution_context>
@/home/kwhatcher/.claude/get-shit-done/workflows/execute-plan.md
@/home/kwhatcher/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/17-volunteer-management/17-CONTEXT.md
@.planning/phases/17-volunteer-management/17-03-SUMMARY.md
@.planning/phases/17-volunteer-management/17-04-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Full test suite and TypeScript verification</name>
  <files></files>
  <action>
    Run the complete verification suite to confirm Phase 17 is green:

    1. Backend tests: `cd /home/kwhatcher/projects/run-api && uv run python -m pytest tests/unit/ -x -v`
       - All existing tests plus test_volunteer_gaps.py from Plan 01 must pass

    2. Frontend TypeScript: `cd /home/kwhatcher/projects/run-api/web && npx tsc --noEmit`
       - Must exit 0 with no errors

    3. Frontend tests: `cd /home/kwhatcher/projects/run-api/web && npx vitest run --reporter=verbose`
       - All existing tests must pass (green)
       - Wave 0 stubs from Plan 01 show as todo/pending (expected)
       - No new failures introduced

    4. If any failures found:
       - Diagnose the root cause
       - Fix the issue (type error, import path, missing export, etc.)
       - Re-run the failing test to confirm green
       - Document the fix in the summary

    Do NOT modify test stubs (it.todo) -- those are pending placeholders for future implementation.
  </action>
  <verify>
    <automated>cd /home/kwhatcher/projects/run-api && uv run python -m pytest tests/unit/ -x --tb=short 2>&1 | tail -5 && cd /home/kwhatcher/projects/run-api/web && npx tsc --noEmit 2>&1 | tail -5 && npx vitest run 2>&1 | tail -10</automated>
  </verify>
  <done>Backend unit tests all pass. TypeScript compiles with 0 errors. Frontend vitest suite green (Wave 0 stubs pending as expected).</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Visual verification of all volunteer management pages</name>
  <files></files>
  <action>
    Present the volunteer management section to the user for visual verification. The user will check:
    - Sidebar navigation (Roster, Tags, Register)
    - Roster page (DataTable, filters, kebab menu)
    - Tags page (CRUD operations)
    - Register page (dual-mode form)
    - Volunteer detail page (header, tags, tabs)
    - Empty states across all pages
  </action>
  <verify>User types "approved" or describes issues</verify>
  <done>User confirms all volunteer management pages render correctly and function as expected.</done>
</task>

</tasks>

<verification>
- Backend tests: `uv run python -m pytest tests/unit/ -x --tb=short`
- Frontend types: `cd web && npx tsc --noEmit`
- Frontend tests: `cd web && npx vitest run`
- Visual verification by user
</verification>

<success_criteria>
- All automated tests pass
- TypeScript compiles with 0 errors
- User visually confirms all pages render correctly
- No console errors in browser dev tools
</success_criteria>

<output>
After completion, create `.planning/phases/17-volunteer-management/17-05-SUMMARY.md`
</output>
