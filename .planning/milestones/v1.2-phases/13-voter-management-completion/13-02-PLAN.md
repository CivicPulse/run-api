---
phase: 13-voter-management-completion
plan: "02"
type: execute
wave: 1
depends_on: []
files_modified:
  - web/src/routes/campaigns/$campaignId/voters.tsx
  - web/src/routes/campaigns/$campaignId/voters/lists/index.tsx
  - web/src/routes/campaigns/$campaignId/voters/lists/$listId.tsx
  - web/src/routes/campaigns/$campaignId/voters/tags/index.tsx
  - web/src/routes/campaigns/$campaignId/__root.tsx
autonomous: true
requirements: [VOTR-05, VOTR-07, VOTR-08, VOTR-09]

must_haves:
  truths:
    - "Navigating to /campaigns/$campaignId/voters shows a sidebar with All Voters | Lists | Tags nav items"
    - "Navigating to /campaigns/$campaignId/voters/lists renders the lists index stub without a 404"
    - "Navigating to /campaigns/$campaignId/voters/lists/$listId renders the list detail stub without a 404"
    - "Navigating to /campaigns/$campaignId/voters/tags renders the tags index stub without a 404"
    - "voters.tsx is a layout-only file with Outlet — it does NOT render the voter list content itself"
  artifacts:
    - path: "web/src/routes/campaigns/$campaignId/voters.tsx"
      provides: "VotersLayout with sidebar nav and Outlet"
      contains: "Outlet"
    - path: "web/src/routes/campaigns/$campaignId/voters/lists/index.tsx"
      provides: "Voter lists index route stub"
    - path: "web/src/routes/campaigns/$campaignId/voters/lists/$listId.tsx"
      provides: "Voter list detail route stub"
    - path: "web/src/routes/campaigns/$campaignId/voters/tags/index.tsx"
      provides: "Campaign tags index route stub"
  key_links:
    - from: "voters.tsx"
      to: "voters/index.tsx"
      via: "TanStack Router Outlet"
      pattern: "<Outlet />"
    - from: "__root.tsx sidebar"
      to: "/campaigns/$campaignId/voters/lists and /voters/tags"
      via: "Link components in nav"
      pattern: "voters/lists"
---

<objective>
Create the routing scaffold for the voters section: a layout wrapper (voters.tsx) with sidebar nav, new route stub files for /voters/lists/ and /voters/tags/, and sidebar nav updates.

Purpose: Wave 2 plans (voters index upgrade, voter detail, lists, tags) all depend on this routing scaffold being in place. Stubs allow the router to resolve the routes before content is built. This plan can run in parallel with 13-01 (type/hook fixes) since it touches no shared files.

Output: voters.tsx layout, four new route stub files, sidebar nav links added.
</objective>

<execution_context>
@/home/kwhatcher/.claude/get-shit-done/workflows/execute-plan.md
@/home/kwhatcher/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-voter-management-completion/13-RESEARCH.md

<interfaces>
<!-- Layout pattern from Phase 12 settings.tsx — mirror exactly -->

From web/src/routes/campaigns/$campaignId/settings.tsx (pattern to mirror):
```typescript
import { createFileRoute, Link, Outlet, useParams } from "@tanstack/react-router"

function SettingsLayout() {
  const { campaignId } = useParams({ from: "/campaigns/$campaignId/settings" })

  const navItems = [
    { to: `/campaigns/${campaignId}/settings`, label: "General", end: true },
    { to: `/campaigns/${campaignId}/settings/members`, label: "Members" },
    { to: `/campaigns/${campaignId}/settings/danger`, label: "Danger Zone" },
  ]

  return (
    <div className="space-y-6">
      <div>
        <h1 className="text-2xl font-semibold tracking-tight">Settings</h1>
      </div>
      <div className="flex gap-0">
        <nav className="w-48 shrink-0 border-r pr-4">
          <ul className="space-y-1">
            {navItems.map((item) => (
              <li key={item.to}>
                <Link
                  to={item.to}
                  params={{ campaignId }}
                  activeProps={{ className: "bg-muted text-foreground font-medium" }}
                  inactiveProps={{ className: "text-muted-foreground hover:text-foreground" }}
                  className="block rounded-md px-3 py-2 text-sm transition-colors"
                >
                  {item.label}
                </Link>
              </li>
            ))}
          </ul>
        </nav>
        <div className="flex-1 pl-8">
          <Outlet />
        </div>
      </div>
    </div>
  )
}

export const Route = createFileRoute("/campaigns/$campaignId/settings")({
  component: SettingsLayout,
})
```

TanStack Router route ID for voters layout:
- Route: `/campaigns/$campaignId/voters`
- File: `voters.tsx` (sibling of `voters/` directory)
- This makes voters.tsx the layout parent for all voters/* routes

Route IDs for new stubs:
- `/campaigns/$campaignId/voters/lists/` → `voters/lists/index.tsx`
- `/campaigns/$campaignId/voters/lists/$listId` → `voters/lists/$listId.tsx`
- `/campaigns/$campaignId/voters/tags/` → `voters/tags/index.tsx`
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create voters.tsx layout wrapper and new route stub files</name>
  <files>
    web/src/routes/campaigns/$campaignId/voters.tsx,
    web/src/routes/campaigns/$campaignId/voters/lists/index.tsx,
    web/src/routes/campaigns/$campaignId/voters/lists/$listId.tsx,
    web/src/routes/campaigns/$campaignId/voters/tags/index.tsx
  </files>
  <action>
    1. Create `web/src/routes/campaigns/$campaignId/voters.tsx` as a layout-only file. Mirror the settings.tsx pattern exactly:
       - Route ID: `/campaigns/$campaignId/voters`
       - Nav items: All Voters (to: `/campaigns/${campaignId}/voters`, end: true), Lists (to: `.../voters/lists`), Tags (to: `.../voters/tags`)
       - Render: sidebar nav (w-48, border-r, pr-4) + Outlet in flex-1 pl-8
       - Page title: "Voters" h1 above the flex container
       - DO NOT render any voter list content — only nav + Outlet

    2. Create `web/src/routes/campaigns/$campaignId/voters/lists/index.tsx` as a minimal stub:
       ```typescript
       import { createFileRoute } from "@tanstack/react-router"
       function VoterListsPage() { return <div>Voter Lists</div> }
       export const Route = createFileRoute("/campaigns/$campaignId/voters/lists/")({ component: VoterListsPage })
       ```

    3. Create `web/src/routes/campaigns/$campaignId/voters/lists/$listId.tsx` as a minimal stub:
       ```typescript
       import { createFileRoute } from "@tanstack/react-router"
       function ListDetailPage() { return <div>List Detail</div> }
       export const Route = createFileRoute("/campaigns/$campaignId/voters/lists/$listId")({ component: ListDetailPage })
       ```

    4. Create `web/src/routes/campaigns/$campaignId/voters/tags/index.tsx` as a minimal stub:
       ```typescript
       import { createFileRoute } from "@tanstack/react-router"
       function VoterTagsPage() { return <div>Voter Tags</div> }
       export const Route = createFileRoute("/campaigns/$campaignId/voters/tags/")({ component: VoterTagsPage })
       ```

    Note: Create the `voters/lists/` and `voters/tags/` directories if they don't exist.
  </action>
  <verify>
    <automated>cd /home/kwhatcher/projects/run-api/web && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <done>voters.tsx exists and contains Outlet. Four stub route files exist. TypeScript compiles without errors from new files.</done>
</task>

<task type="auto">
  <name>Task 2: Add Lists and Tags links to sidebar navigation</name>
  <files>web/src/routes/campaigns/$campaignId/__root.tsx</files>
  <action>
    Open web/src/routes/campaigns/$campaignId/__root.tsx (or the equivalent root/layout file that renders the campaign sidebar navigation).

    Locate the "Voters" nav entry in the sidebar. Currently it likely has a single link to `/campaigns/${campaignId}/voters`. Update it to show a nav group:

    ```
    Voters (section heading or top-level link)
      ├── All Voters  → /campaigns/${campaignId}/voters
      ├── Lists       → /campaigns/${campaignId}/voters/lists
      └── Tags        → /campaigns/${campaignId}/voters/tags
    ```

    Use the same Link + activeProps/inactiveProps pattern already established in the sidebar for other nav groups. If the sidebar uses indentation for sub-items, apply consistent indentation (pl-4 or similar) to Lists and Tags.

    If __root.tsx is not the correct file, locate the actual campaign sidebar layout file by checking `web/src/routes/` for a file that renders campaign navigation links.
  </action>
  <verify>
    <automated>cd /home/kwhatcher/projects/run-api/web && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <done>Sidebar shows All Voters, Lists, Tags links under the Voters group. TypeScript compiles clean.</done>
</task>

</tasks>

<verification>
- voters.tsx exists: `ls web/src/routes/campaigns/\$campaignId/voters.tsx`
- Stub files exist: `ls web/src/routes/campaigns/\$campaignId/voters/lists/ web/src/routes/campaigns/\$campaignId/voters/tags/`
- voters.tsx contains Outlet: `grep -n "Outlet" web/src/routes/campaigns/\$campaignId/voters.tsx`
- TypeScript clean: `cd web && npx tsc --noEmit`
- Tests still pass: `cd web && npm run test -- --run 2>&1 | tail -5`
</verification>

<success_criteria>
- voters.tsx is a layout-only file with sidebar nav and Outlet
- Four route stub files created (lists/index, lists/$listId, tags/index)
- Sidebar nav shows All Voters | Lists | Tags links
- TypeScript compiles with 0 errors
- No regressions in existing tests
</success_criteria>

<output>
After completion, create `.planning/phases/13-voter-management-completion/13-02-SUMMARY.md`
</output>
