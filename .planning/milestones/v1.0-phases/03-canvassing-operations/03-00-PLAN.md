---
phase: 03-canvassing-operations
plan: 00
type: execute
wave: 0
depends_on: []
files_modified:
  - tests/unit/test_turfs.py
  - tests/unit/test_walk_lists.py
  - tests/unit/test_canvassing.py
  - tests/unit/test_surveys.py
  - tests/integration/test_spatial.py
autonomous: true
requirements:
  - CANV-01
  - CANV-02
  - CANV-03
  - CANV-04
  - CANV-05
  - CANV-06
  - CANV-07
  - CANV-08

must_haves:
  truths:
    - "All 5 test stub files exist and are importable by pytest"
    - "Each stub has placeholder test functions that fail with NotImplementedError"
    - "pytest collection discovers all test files without import errors"
  artifacts:
    - path: "tests/unit/test_turfs.py"
      provides: "Test stubs for CANV-01 turf CRUD and GeoJSON validation"
      contains: "def test_"
    - path: "tests/unit/test_walk_lists.py"
      provides: "Test stubs for CANV-02, CANV-03, CANV-06 generation, clustering, assignment"
      contains: "def test_household_clustering"
    - path: "tests/unit/test_canvassing.py"
      provides: "Test stubs for CANV-04, CANV-05 door-knock recording, contact tracking"
      contains: "def test_contact_attempts"
    - path: "tests/unit/test_surveys.py"
      provides: "Test stubs for CANV-07, CANV-08 scripts, questions, responses"
      contains: "def test_survey_responses"
    - path: "tests/integration/test_spatial.py"
      provides: "Test stubs for CANV-01 PostGIS integration"
      contains: "def test_"
  key_links: []
---

<objective>
Create Wave 0 test stub files for all Phase 3 canvassing operations.

Purpose: Establish test scaffolding so that Plans 01-03 can run pytest-based verification against real test files. Stubs define the test contract -- each subsequent plan fills in the implementations as it builds the corresponding production code.
Output: 5 test files with placeholder test functions that pytest can collect.
</objective>

<execution_context>
@/home/kwhatcher/.claude/get-shit-done/workflows/execute-plan.md
@/home/kwhatcher/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-canvassing-operations/03-CONTEXT.md
@.planning/phases/03-canvassing-operations/03-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create all Wave 0 test stub files</name>
  <files>
    tests/unit/test_turfs.py,
    tests/unit/test_walk_lists.py,
    tests/unit/test_canvassing.py,
    tests/unit/test_surveys.py,
    tests/integration/test_spatial.py
  </files>
  <action>
Create 5 test stub files following the existing project test pattern (pytest classes with async test methods, `from __future__ import annotations`). Each stub must:
- Be importable by pytest without errors (no imports of production code that does not yet exist)
- Contain placeholder test functions marked with `pytest.mark.skip(reason="stub -- implemented in plan 03-XX")`
- Cover the test names referenced in VALIDATION.md's Per-Task Verification Map

1. `tests/unit/test_turfs.py`:
   - Class `TestTurfService` with stubs:
     - `test_create_turf_valid_geojson` -- CANV-01: Create turf with valid GeoJSON polygon
     - `test_create_turf_invalid_geojson_rejected` -- CANV-01: Invalid polygon raises error
     - `test_list_turfs_by_campaign` -- CANV-01: List turfs filtered by campaign
     - `test_update_turf_status_transitions` -- CANV-01: Valid status transitions enforced
     - `test_get_voter_count_in_turf` -- CANV-01: Spatial voter containment count
   - Each stub: `@pytest.mark.skip(reason="stub -- implemented in plan 03-01/03-02")` and body `raise NotImplementedError`

2. `tests/unit/test_walk_lists.py`:
   - Class `TestWalkListService` with stubs:
     - `test_generate_walk_list_from_turf` -- CANV-02: Generate walk list with spatial query
     - `test_walk_list_is_frozen_snapshot` -- CANV-02: Walk list entries don't change after creation
     - `test_household_clustering` -- CANV-03: Same-address voters grouped as household
     - `test_address_sort_order` -- CANV-03: Street-name then house-number ordering
     - `test_canvasser_assignment` -- CANV-06: Assign/remove canvassers to walk lists
     - `test_entry_status_update` -- CANV-02: Update entry to visited/skipped
   - Each stub: `@pytest.mark.skip(reason="stub -- implemented in plan 03-02")`

3. `tests/unit/test_canvassing.py`:
   - Class `TestCanvassService` with stubs:
     - `test_record_door_knock` -- CANV-04: Record door knock creates interaction
     - `test_door_knock_updates_entry_status` -- CANV-04: Entry auto-set to visited
     - `test_door_knock_increments_visited_count` -- CANV-04: Walk list visited_entries incremented
     - `test_contact_attempts` -- CANV-05: Multiple knocks tracked per voter
     - `test_attempt_number_derived` -- CANV-05: Attempt number computed from event count
   - Each stub: `@pytest.mark.skip(reason="stub -- implemented in plan 03-02")`

4. `tests/unit/test_surveys.py`:
   - Class `TestSurveyService` with stubs:
     - `test_create_survey_script` -- CANV-07: Create script in draft status
     - `test_script_lifecycle_transitions` -- CANV-07: draft->active->archived only
     - `test_question_crud_draft_only` -- CANV-07: Questions editable only in draft
     - `test_question_type_validation` -- CANV-07: MC options, scale range validated
     - `test_survey_responses` -- CANV-08: Record response with type validation
     - `test_batch_responses_emit_interaction` -- CANV-08: Batch creates SURVEY_RESPONSE event
     - `test_active_script_required_for_responses` -- CANV-08: Cannot record against draft/archived
   - Each stub: `@pytest.mark.skip(reason="stub -- implemented in plan 03-03")`

5. `tests/integration/test_spatial.py`:
   - Class `TestSpatialOperations` with stubs:
     - `test_postgis_extension_active` -- PostGIS enabled
     - `test_voter_geom_column_populated` -- Voter geom backfilled from lat/long
     - `test_st_contains_turf_voter` -- ST_Contains query finds voters in polygon
     - `test_gist_index_used` -- EXPLAIN shows GiST index scan
   - Each stub: `@pytest.mark.skip(reason="stub -- implemented in plan 03-01/03-02")`

IMPORTANT: Do NOT import any production code at module level. Stubs are skip-marked so they won't execute. Production imports go inside test bodies when the stubs are implemented by later plans.
  </action>
  <verify>
    <automated>cd /home/kwhatcher/projects/run-api && uv run pytest tests/unit/test_turfs.py tests/unit/test_walk_lists.py tests/unit/test_canvassing.py tests/unit/test_surveys.py tests/integration/test_spatial.py --collect-only -q 2>&1 | tail -20</automated>
  </verify>
  <done>
    All 5 test files exist, are collected by pytest without import errors, and contain skip-marked placeholder tests covering all VALIDATION.md requirements. Total test count matches expected stubs (5 + 6 + 5 + 7 + 4 = 27 stubs).
  </done>
</task>

</tasks>

<verification>
- `uv run pytest tests/unit/test_turfs.py --collect-only` shows 5 tests
- `uv run pytest tests/unit/test_walk_lists.py --collect-only` shows 6 tests
- `uv run pytest tests/unit/test_canvassing.py --collect-only` shows 5 tests
- `uv run pytest tests/unit/test_surveys.py --collect-only` shows 7 tests
- `uv run pytest tests/integration/test_spatial.py --collect-only` shows 4 tests
- All tests skip cleanly (no import errors)
</verification>

<success_criteria>
All Wave 0 test stubs exist and pytest can collect them. Plans 01-03 can now reference these test files in their verify blocks.
</success_criteria>

<output>
After completion, create `.planning/phases/03-canvassing-operations/03-00-SUMMARY.md`
</output>
