---
phase: 26-frontend-updates
plan: 02
type: execute
wave: 2
depends_on: [26-01]
files_modified:
  - web/src/routes/campaigns/$campaignId/voters/$voterId.tsx
  - web/src/components/voters/VoterEditSheet.tsx
autonomous: true
requirements: [FRNT-01, FRNT-03]

must_haves:
  truths:
    - "Voter detail page shows propensity scores as color-coded badge chips (green 67+, yellow 34-66, red 0-33, grey N/A)"
    - "Voter detail page shows mailing address card when mailing data exists"
    - "Voter detail page shows household card when household data exists"
    - "Cards with all-NULL fields are hidden entirely (adaptive layout)"
    - "Personal Info card shows language, marital status, military status below ethnicity"
    - "Voting history displayed as year-grouped table (Year | General | Primary) with checkmarks, descending sort"
    - "Edit sheet is wider (max-w-xl) with sectioned layout (Personal, Registration Address, Mailing Address)"
    - "Edit sheet has all editable fields: name (first/middle/last/suffix), DOB, party, gender, ethnicity, language, marital status, military status, registration address, mailing address"
    - "Mailing address section in edit sheet is collapsible -- collapsed by default, auto-expanded if voter has mailing data"
  artifacts:
    - path: "web/src/routes/campaigns/$campaignId/voters/$voterId.tsx"
      provides: "Expanded voter detail page with propensity, mailing, household, voting history"
      contains: "PropensityBadge"
    - path: "web/src/components/voters/VoterEditSheet.tsx"
      provides: "Expanded edit sheet with sections and all editable fields"
      contains: "max-w-xl"
  key_links:
    - from: "web/src/routes/campaigns/$campaignId/voters/$voterId.tsx"
      to: "web/src/types/voter.ts"
      via: "Voter interface field access"
      pattern: "voter\\.propensity_general"
    - from: "web/src/components/voters/VoterEditSheet.tsx"
      to: "web/src/hooks/useVoters.ts"
      via: "useUpdateVoter mutation"
      pattern: "useUpdateVoter"
---

<objective>
Expand the voter detail page with new cards (Propensity Scores, Mailing Address, Household), adaptive visibility, voting history table, and expanded demographics. Expand the edit sheet with all editable fields in sectioned layout with collapsible mailing address.

Purpose: Users need to see and edit the expanded voter data from Phases 23-25. The detail page is the primary place campaign staff assess individual voter records, and the edit sheet is how they correct data.
Output: Voter detail page with 7 cards (4 left, 3 right) and voting history table. Edit sheet with ~20 editable fields in 3 sections.
</objective>

<execution_context>
@/home/kwhatcher/.claude/get-shit-done/workflows/execute-plan.md
@/home/kwhatcher/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/26-frontend-updates/26-CONTEXT.md
@.planning/phases/26-frontend-updates/26-RESEARCH.md
@.planning/phases/26-frontend-updates/26-01-SUMMARY.md

@web/src/routes/campaigns/$campaignId/voters/$voterId.tsx
@web/src/components/voters/VoterEditSheet.tsx

<interfaces>
<!-- From voter.ts (updated in Plan 01) -->
Voter interface fields used in this plan:
- propensity_general, propensity_primary, propensity_combined: number | null
- registration_line1/line2/city/state/zip/zip4/county/apartment_type: string | null
- mailing_line1/line2/city/state/zip/zip4/country/type: string | null
- spoken_language, marital_status, military_status, party_change_indicator: string | null
- cell_phone_confidence: number | null
- household_party_registration: string | null, household_size: number | null, family_id: string | null
- voting_history: string[] | null (format: "General_2024", "Primary_2022")

VoterUpdate (Partial of VoterCreate) -- editable fields include:
- first_name, middle_name, last_name, suffix, date_of_birth, party, gender, ethnicity
- spoken_language, marital_status, military_status
- registration_line1/line2/city/state/zip/zip4/county/apartment_type
- mailing_line1/line2/city/state/zip/zip4/country/type

Read-only fields (detail page only, NOT in edit sheet):
- propensity_general/primary/combined, household_size/party_registration/family_id
- cell_phone_confidence, party_change_indicator

Available shadcn components (installed in Plan 01):
- Collapsible, CollapsibleContent, CollapsibleTrigger from @/components/ui/collapsible
- Separator from @/components/ui/separator
- Badge from @/components/ui/badge
- Card, CardHeader, CardTitle, CardContent from @/components/ui/card
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Expand voter detail page with new cards and voting history table</name>
  <files>web/src/routes/campaigns/$campaignId/voters/$voterId.tsx</files>
  <action>
Rewrite the Overview tab content in the voter detail page. Keep the existing page structure (header, tabs, edit sheet wiring) but replace the grid content.

**Layout:** 2-column grid (`md:grid-cols-2`). Left column: 4 cards. Right column: 3 cards + Recent Interactions card (existing).

**Helper functions to add at module level:**

1. `PropensityBadge({ score, label })` -- color-coded Badge:
   - score >= 67: `bg-green-100 text-green-800`
   - score >= 34: `bg-yellow-100 text-yellow-800`
   - score < 34: `bg-red-100 text-red-800`
   - null/undefined: `bg-gray-100 text-gray-500`, shows "N/A"

2. `parseVotingHistory(history: string[] | null)` -- parses `["General_2024", "Primary_2022"]` into `Array<{year: number, general: boolean, primary: boolean}>` sorted descending by year.

3. `hasAnyValue(...values: unknown[])` -- returns true if any value is non-null and non-undefined. Used for adaptive card visibility.

**Left column cards (top to bottom):**

1. **Propensity Scores** (hide if ALL three are null):
   - Row of 3 PropensityBadge chips: General, Primary, Combined
   - Compact card, no grid layout needed

2. **Personal Information** (always visible -- has at least DOB/age):
   - Existing fields: DOB, Age, Gender, Ethnicity
   - New fields below ethnicity: Language (spoken_language), Marital Status, Military Status
   - Keep existing `dl > grid grid-cols-2` layout

3. **Registration Address** (renamed from "Address"):
   - Fields: Street (registration_line1 + line2), City, State, ZIP, ZIP+4, County, Apartment Type
   - Hide if all registration address fields are null

4. **Registration & Districts** (renamed from "Registration"):
   - Keep: Party, Precinct, Registration Date, Source, Congressional/Senate/House districts
   - Add: Party Change Indicator
   - Add **Voting History table** at the bottom of this card (inside a sub-section with a Separator):
     - Table with columns: Year | General | Primary
     - Use Check icon (lucide) for voted, Minus icon for not voted
     - Sorted descending by year
     - If no voting history, show "No voting history recorded" text

**Right column cards (top to bottom):**

5. **Mailing Address** (hide if ALL mailing fields are null):
   - Same structure as Registration Address: Street, City, State, ZIP, ZIP+4, Country, Type

6. **Household** (hide if ALL household fields are null):
   - Fields: Household Size, Party Registration, Family ID
   - Also show Cell Phone Confidence if non-null

7. **Recent Interactions** (existing card -- keep as-is, move to bottom of right column)

**Adaptive visibility pattern:**
Wrap each card section in a conditional: only render if `hasAnyValue(field1, field2, ...)` returns true. Import Check and Minus from lucide-react for the voting history table.
  </action>
  <verify>
    <automated>cd /home/kwhatcher/projects/run-api/web && npx tsc --noEmit</automated>
  </verify>
  <done>Voter detail page shows Propensity Scores card with color-coded badges, expanded Personal Info with demographics, Registration Address, Registration & Districts with voting history table, Mailing Address card, Household card. Cards with all-null fields are hidden. Layout is 4 left / 3 right columns.</done>
</task>

<task type="auto">
  <name>Task 2: Expand VoterEditSheet with sections, all editable fields, and collapsible mailing address</name>
  <files>web/src/components/voters/VoterEditSheet.tsx</files>
  <action>
Rewrite VoterEditSheet to include all editable fields with sectioned layout.

**Sheet width:** Change `sm:max-w-md` to `sm:max-w-xl` on SheetContent (both the form view and the "unsaved changes" blocker view).

**Zod schema expansion** -- `editVoterSchema`:
- Personal: first_name, middle_name, last_name, suffix, date_of_birth, party, gender, ethnicity, spoken_language, marital_status, military_status (all `z.string().optional()`)
- Registration Address: registration_line1, registration_line2, registration_city, registration_state, registration_zip, registration_zip4, registration_county, registration_apartment_type (all `z.string().optional()`)
- Mailing Address: mailing_line1, mailing_line2, mailing_city, mailing_state, mailing_zip, mailing_zip4, mailing_country, mailing_type (all `z.string().optional()`)

**Form default values:** Map each field from `voter.{field} ?? ""`.

**Form layout with Separator headers:**

Section 1: **Personal** (Separator + "Personal" text label)
- Row 1: First Name | Middle Name (grid-cols-2)
- Row 2: Last Name | Suffix (grid-cols-2)
- Row 3: Date of Birth (full width, type="date")
- Row 4: Party (Select dropdown, existing pattern)
- Row 5: Gender (Select dropdown, existing pattern)
- Row 6: Ethnicity (Input)
- Row 7: Language (Input, spoken_language)
- Row 8: Marital Status (Input)
- Row 9: Military Status (Input)

Section 2: **Registration Address** (Separator + label)
- Row 1: Line 1 (full width)
- Row 2: Line 2 (full width)
- Row 3: City | State (grid-cols-2)
- Row 4: ZIP | ZIP+4 (grid-cols-2)
- Row 5: County (full width)
- Row 6: Apartment Type (full width)

Section 3: **Mailing Address** (Collapsible section)
- Use shadcn Collapsible component
- Default open state: `voter.mailing_line1 || voter.mailing_city` etc. -- auto-expand if voter has any mailing data
- Trigger: show "+ Add mailing address" when collapsed, "Mailing Address" header when expanded
- **CRITICAL:** Use `forceMount` on CollapsibleContent and add `data-[state=closed]:hidden` class to prevent form field destruction on collapse
- Same field layout as Registration Address but with mailing_* field names, plus mailing_country and mailing_type

**Save button:** Keep existing pattern. The `onSubmit` handler already filters empty strings before sending.
  </action>
  <verify>
    <automated>cd /home/kwhatcher/projects/run-api/web && npx tsc --noEmit</automated>
  </verify>
  <done>Edit sheet is wider (max-w-xl). Has 3 sections with Separator headers. Personal section has all demographic fields. Registration Address section has all 8 fields. Mailing Address is collapsible -- collapsed by default, auto-expanded if mailing data exists, uses forceMount to preserve form state.</done>
</task>

</tasks>

<verification>
- `cd web && npx tsc --noEmit` exits 0
- Voter detail page renders all new cards with correct data
- Cards with all-null sections are hidden
- Voting history table shows year-grouped data with check/minus icons
- Propensity badges show correct colors for score thresholds
- Edit sheet opens at wider width with all 3 sections
- Mailing address section collapses/expands without losing form data
</verification>

<success_criteria>
- Voter detail page displays propensity scores, mailing address, demographics, household data, and voting history table per FRNT-01
- Edit sheet includes all editable fields with sections and collapsible mailing address per FRNT-03
- Cards are hidden when all their fields are null (adaptive layout)
- TypeScript compiles with no errors
</success_criteria>

<output>
After completion, create `.planning/phases/26-frontend-updates/26-02-SUMMARY.md`
</output>
