---
phase: 29-integration-polish-tech-debt-cleanup
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - web/src/types/import-job.ts
  - web/src/routes/campaigns/$campaignId/voters/imports/index.tsx
  - web/src/routes/campaigns/$campaignId/voters/imports/new.tsx
  - web/src/components/voters/ImportProgress.tsx
autonomous: true
requirements: []
must_haves:
  truths:
    - "Import history table displays Filename column correctly using original_filename from backend"
    - "Import history table does not show a blank Errors column"
    - "ImportProgress component displays without TypeScript errors after error_count removal"
    - "Import completion summary in new.tsx does not reference error_count"
  artifacts:
    - path: "web/src/types/import-job.ts"
      provides: "ImportJob interface aligned with backend ImportJobResponse"
      contains: "original_filename"
    - path: "web/src/routes/campaigns/$campaignId/voters/imports/index.tsx"
      provides: "Import history table with correct accessorKey and no Errors column"
    - path: "web/src/components/voters/ImportProgress.tsx"
      provides: "Progress component without error_count references"
  key_links:
    - from: "web/src/types/import-job.ts"
      to: "web/src/routes/campaigns/$campaignId/voters/imports/index.tsx"
      via: "ImportJob type used for ColumnDef"
      pattern: "accessorKey.*original_filename"
    - from: "web/src/types/import-job.ts"
      to: "web/src/components/voters/ImportProgress.tsx"
      via: "ImportJob type for job prop"
      pattern: "job\\.imported_rows"
---

<objective>
Align the ImportJob TypeScript interface with the backend ImportJobResponse schema and fix all consumers.

Purpose: The frontend ImportJob type has three mismatches with the backend: `filename` should be `original_filename`, `error_count` has no backend counterpart (renders blank), and `source_type`/`field_mapping` fields are missing. This causes a blank Filename column and a permanently-empty Errors column in the import history table.

Output: Updated ImportJob interface, fixed import history table columns, cleaned ImportProgress and import new.tsx error_count references.
</objective>

<execution_context>
@/home/kwhatcher/.claude/get-shit-done/workflows/execute-plan.md
@/home/kwhatcher/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/29-integration-polish-tech-debt-cleanup/29-CONTEXT.md
@.planning/phases/29-integration-polish-tech-debt-cleanup/29-RESEARCH.md

<interfaces>
<!-- Current ImportJob interface (web/src/types/import-job.ts) -->
```typescript
export interface ImportJob {
  id: string
  campaign_id: string
  filename: string           // RENAME to original_filename
  status: ImportStatus
  total_rows: number | null
  imported_rows: number
  skipped_rows: number
  error_count: number        // REMOVE -- no backend counterpart
  error_report_key: string | null
  phones_created: number | null
  created_at: string
  updated_at: string
}
```

<!-- Backend ImportJobResponse (app/schemas/import_job.py) fields:
  original_filename: str
  source_type: str
  field_mapping: dict | None = None
  error_message: str | None = None
  created_by: str
  NOTE: NO error_count, NO mapping_template_id
-->

<!-- useInitiateImport hook (web/src/hooks/useImports.ts L57-61) -->
<!-- mutationFn takes { filename: string } as LOCAL parameter name,
     maps to searchParams: { original_filename: data.filename }
     DO NOT rename this parameter -- it is not an ImportJob field reference -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update ImportJob interface and fix import history table</name>
  <files>web/src/types/import-job.ts, web/src/routes/campaigns/$campaignId/voters/imports/index.tsx</files>
  <action>
1. In `web/src/types/import-job.ts`:
   - Rename `filename: string` to `original_filename: string`
   - Remove `error_count: number` field entirely
   - Add after `error_report_key`:
     - `error_message: string | null` (exists on backend)
   - Add after `phones_created`:
     - `source_type: string` (exists on backend)
     - `field_mapping: Record<string, string> | null` (exists on backend)
     - `created_by: string` (exists on backend)
   - Do NOT add `mapping_template_id` -- it does not exist on backend ImportJobResponse

2. In `web/src/routes/campaigns/$campaignId/voters/imports/index.tsx`:
   - Line 50: Change `accessorKey: "filename"` to `accessorKey: "original_filename"`
   - Lines 68-70: Remove the entire column definition for `error_count` / "Errors" (the object with `accessorKey: "error_count"` and `header: "Errors"`)
  </action>
  <verify>
    <automated>cd /home/kwhatcher/projects/run-api/web && npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <done>ImportJob interface matches backend ImportJobResponse. Import history table shows Filename column using original_filename and no longer has an Errors column.</done>
</task>

<task type="auto">
  <name>Task 2: Remove error_count references from ImportProgress and new.tsx</name>
  <files>web/src/components/voters/ImportProgress.tsx, web/src/routes/campaigns/$campaignId/voters/imports/new.tsx</files>
  <action>
1. In `web/src/components/voters/ImportProgress.tsx`:
   - Line 53: Remove the entire `<span>` block that displays `job.error_count` with "errors" label (lines 52-55 approximately: the span containing `job.error_count` and "errors" text)
   - Lines 69-72: Remove the conditional block `{job.error_count > 0 && (...)}` inside the failed state section. Keep the "Import failed" message and the rest of the failed block.

2. In `web/src/routes/campaigns/$campaignId/voters/imports/new.tsx`:
   - Lines 322-326: Remove the conditional block `{jobQuery.data.error_count > 0 && (...)}` that displays "{error_count} rows had errors". Keep the error_report_key download link block that follows it (lines 327+).
   - Line 124: Do NOT change `filename: file.name` -- this is a local parameter to useInitiateImport, not an ImportJob field reference.
  </action>
  <verify>
    <automated>cd /home/kwhatcher/projects/run-api/web && npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <done>No TypeScript errors. ImportProgress and import new.tsx no longer reference error_count. The error_report_key download link is preserved.</done>
</task>

</tasks>

<verification>
- `cd /home/kwhatcher/projects/run-api/web && npx tsc --noEmit` exits cleanly with no errors
- `grep -r "error_count" web/src/` returns zero matches (field fully removed from codebase)
- `grep -r "accessorKey.*filename" web/src/routes/campaigns/\$campaignId/voters/imports/` shows only `original_filename`
</verification>

<success_criteria>
- ImportJob interface has `original_filename` (not `filename`), no `error_count`, and includes `source_type`, `field_mapping`, `error_message`, `created_by`
- Import history table shows Filename column correctly and has no Errors column
- ImportProgress and new.tsx compile without error_count references
- TypeScript compilation succeeds with zero errors
</success_criteria>

<output>
After completion, create `.planning/phases/29-integration-polish-tech-debt-cleanup/29-01-SUMMARY.md`
</output>
