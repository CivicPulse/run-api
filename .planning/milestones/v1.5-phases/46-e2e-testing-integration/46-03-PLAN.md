---
phase: 46-e2e-testing-integration
plan: 03
type: execute
wave: 2
depends_on: [46-01, 46-02]
files_modified:
  - web/e2e/turf-creation.spec.ts
  - web/e2e/phone-bank.spec.ts
  - web/e2e/volunteer-signup.spec.ts
  - .github/workflows/pr.yml
autonomous: true
requirements: [TEST-01, TEST-02, TEST-03]

must_haves:
  truths:
    - "Turf creation E2E test draws a polygon on the map and verifies the saved turf appears on reload"
    - "Phone bank E2E test creates a session, assigns callers, and records call outcomes"
    - "Volunteer signup E2E test creates a volunteer record and verifies it appears in the volunteer list"
    - "GitHub Actions CI workflow starts Docker Compose, runs seed, executes integration tests and E2E tests, and uploads failure artifacts"
    - "Test failures block PR merges to main"
  artifacts:
    - path: "web/e2e/turf-creation.spec.ts"
      provides: "Turf creation E2E test"
      contains: "test("
    - path: "web/e2e/phone-bank.spec.ts"
      provides: "Phone bank E2E test"
      contains: "test("
    - path: "web/e2e/volunteer-signup.spec.ts"
      provides: "Volunteer signup E2E test"
      contains: "test("
    - path: ".github/workflows/pr.yml"
      provides: "CI workflow with integration + E2E test job"
      contains: "integration-e2e"
  key_links:
    - from: ".github/workflows/pr.yml"
      to: "docker compose"
      via: "service startup + seed + test execution"
      pattern: "docker compose up"
    - from: ".github/workflows/pr.yml"
      to: "web/e2e/"
      via: "npx playwright test"
      pattern: "playwright test"
    - from: ".github/workflows/pr.yml"
      to: "tests/integration/"
      via: "uv run pytest"
      pattern: "pytest tests/integration"
---

<objective>
Write the remaining 3 E2E specs (turf creation, phone bank, volunteer signup) and add a GitHub Actions CI job that runs the full integration + E2E test suite against Docker Compose.

Purpose: Complete the 6 critical E2E flow coverage (TEST-01) and gate PR merges on test passage (D-09, D-10, D-11).
Output: 3 E2E spec files, updated pr.yml workflow
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/46-e2e-testing-integration/46-CONTEXT.md
@.planning/phases/46-e2e-testing-integration/46-RESEARCH.md
@.planning/phases/46-e2e-testing-integration/46-01-SUMMARY.md
@.planning/phases/46-e2e-testing-integration/46-02-SUMMARY.md

<interfaces>
<!-- Auth setup from Plan 01 -->
From web/e2e/auth.setup.ts:
- Authenticates via ZITADEL, saves to playwright/.auth/user.json
- All chromium project specs automatically load stored auth state

From web/playwright.config.ts:
- setup project: testMatch /.*\.setup\.ts/
- chromium project: storageState "playwright/.auth/user.json", dependencies ["setup"]
- baseURL: "https://localhost:4173"
- actionTimeout: 30_000

<!-- Existing CI workflow structure (from .github/workflows/pr.yml) -->
```yaml
# Jobs: lint, test, frontend, docker-build (needs: [lint, test, frontend])
# Uses: actions/checkout@v4, astral-sh/setup-uv@v4, actions/setup-node@v4
```

<!-- Seed data relevant to these 3 specs -->
# Turfs: 5 turfs with PostGIS polygons in Macon-Bibb County
# Phone bank: 3 sessions, 3 call lists
# Volunteers: 20 volunteers, 10 shifts
# Campaign: Macon-Bibb County (name used in navigation)

<!-- ZITADEL credentials for CI -->
# E2E_USERNAME: admin@localhost
# E2E_PASSWORD: Admin1234!
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Turf creation, phone bank, and volunteer signup E2E specs</name>
  <files>web/e2e/turf-creation.spec.ts, web/e2e/phone-bank.spec.ts, web/e2e/volunteer-signup.spec.ts</files>
  <read_first>
    - web/e2e/auth.setup.ts (auth pattern from Plan 01)
    - web/e2e/login.spec.ts (E2E spec pattern from Plan 01 -- follow same style)
    - web/playwright.config.ts (project config)
    - scripts/seed.py (seed data: turfs, phone bank sessions, volunteers, campaign structure)
    - web/src/routes/ (list directory to find route paths)
  </read_first>
  <action>
Per D-01, full CRUD journeys. Per D-03, real Docker Compose stack (NO page.route mocks). Per D-04, one spec file per flow. Per D-14, Playwright auto-waiting only.

1. Create `web/e2e/turf-creation.spec.ts`:
   - Uses stored auth state (chromium project)
   - Test "create turf via map draws polygon and saves":
     a. Navigate to turfs page
     b. Click "New Turf" or "Create Turf" button
     c. Fill in turf name (e.g., "E2E Test Turf")
     d. For the map interaction: since Playwright cannot easily simulate Leaflet polygon drawing, use the "Advanced" GeoJSON textarea toggle (per MAP-08, this exists as a fallback)
     e. Paste a valid GeoJSON polygon into the textarea:
        ```json
        {"type":"Polygon","coordinates":[[[-83.65,32.84],[-83.64,32.84],[-83.64,32.85],[-83.65,32.85],[-83.65,32.84]]]}
        ```
     f. Save the turf
     g. Wait for success toast or redirect
     h. Verify the new turf "E2E Test Turf" appears in the turf list
   - Test "edit existing turf updates name":
     a. Navigate to turfs page
     b. Click on "E2E Test Turf" (created above) or a seed turf
     c. Edit the turf name
     d. Save
     e. Verify updated name in turf list
   - Test "view turf shows voter count badge":
     a. Navigate to turfs page
     b. Verify at least one turf shows a voter count badge (seed data has voters in turfs)

2. Create `web/e2e/phone-bank.spec.ts`:
   - Uses stored auth state
   - Test "create phone bank session and view callers":
     a. Navigate to phone banking page
     b. Click "New Session" or similar
     c. Select a call list from seed data
     d. Fill session details (name, date)
     e. Save session
     f. Verify session appears in session list
   - Test "view existing session shows call list entries":
     a. Navigate to phone banking page
     b. Click on an existing seed session
     c. Verify call list entries or caller assignments are visible
   - Test "record call outcome on a session":
     a. Navigate into a phone bank session
     b. Find a caller entry
     c. Record a call outcome (e.g., "Answered", "No Answer")
     d. Verify the outcome is saved (UI updates)

3. Create `web/e2e/volunteer-signup.spec.ts`:
   - Uses stored auth state
   - Test "create volunteer record":
     a. Navigate to volunteers page
     b. Click "Add Volunteer" button
     c. Fill in volunteer details: first name "E2E", last name "Volunteer", email "e2e-vol@test.com", phone "5551234567"
     d. Select the "tracked only" option (not ZITADEL invite, per Phase 44 UX-02 distinction)
     e. Save
     f. Verify new volunteer appears in the volunteer list
   - Test "view volunteer detail shows contact info":
     a. Navigate to volunteers page
     b. Click on a volunteer (seed data or just-created)
     c. Verify name, email, or phone visible on detail page
   - Test "assign volunteer to shift":
     a. Navigate to shifts or volunteer detail page
     b. Assign a volunteer to an existing shift
     c. Verify assignment saved

All specs follow the same patterns as Plan 01 specs:
- `test.describe` grouping
- `expect(locator).toBeVisible()` for auto-waiting assertions
- `page.waitForResponse()` for API call completion
- NO `page.route()`, NO `page.waitForTimeout()`
  </action>
  <verify>
    <automated>cd /home/kwhatcher/projects/civicpulse/run-api/web && npx tsc --noEmit e2e/turf-creation.spec.ts e2e/phone-bank.spec.ts e2e/volunteer-signup.spec.ts --esModuleInterop --module esnext --moduleResolution bundler --target esnext --skipLibCheck 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - web/e2e/turf-creation.spec.ts contains `test.describe` and at least 2 `test(` calls
    - web/e2e/turf-creation.spec.ts contains GeoJSON polygon string `"type":"Polygon"`
    - web/e2e/turf-creation.spec.ts does NOT contain `page.route`
    - web/e2e/phone-bank.spec.ts contains `test.describe` and at least 2 `test(` calls
    - web/e2e/phone-bank.spec.ts does NOT contain `page.route`
    - web/e2e/volunteer-signup.spec.ts contains `test.describe` and at least 2 `test(` calls
    - web/e2e/volunteer-signup.spec.ts does NOT contain `page.route`
    - All three files import from `@playwright/test`
    - `ls web/e2e/turf-creation.spec.ts web/e2e/phone-bank.spec.ts web/e2e/volunteer-signup.spec.ts` all exist
  </acceptance_criteria>
  <done>Three E2E specs cover turf creation (GeoJSON textarea + save + verify), phone bank (session create + call recording), and volunteer signup (create + detail + shift assignment) -- all against real backend with no API mocks</done>
</task>

<task type="auto">
  <name>Task 2: GitHub Actions CI workflow with Docker Compose integration and E2E tests</name>
  <files>.github/workflows/pr.yml</files>
  <read_first>
    - .github/workflows/pr.yml (existing workflow: lint, test, frontend, docker-build jobs)
    - docker-compose.yml (service definitions, ports, healthchecks)
    - web/playwright.config.ts (updated config with setup project)
    - .planning/phases/46-e2e-testing-integration/46-RESEARCH.md (CI workflow pattern, pitfalls)
  </read_first>
  <action>
Per D-09, add a single GitHub Actions job to the existing `pr.yml` workflow. Per D-10, test failures block merges. Per D-11, upload Playwright artifacts. Per D-13, fresh DB per suite run.

Add a new job `integration-e2e` to `.github/workflows/pr.yml` AFTER the existing `docker-build` job. The new job:

```yaml
  integration-e2e:
    name: Integration & E2E Tests
    runs-on: ubuntu-latest
    needs: [lint, test, frontend]
    steps:
      - uses: actions/checkout@v4

      - uses: astral-sh/setup-uv@v4

      - uses: actions/setup-node@v4
        with:
          node-version: "22"

      - name: Start services
        run: docker compose up -d --build --wait
        env:
          ZITADEL_TLS_MODE: disabled
          ZITADEL_EXTERNAL_SECURE: "false"
          ZITADEL_DOMAIN: localhost
          DISABLE_TLS: "true"

      - name: Wait for ZITADEL readiness
        run: |
          for i in $(seq 1 60); do
            curl -sf http://localhost:8080/debug/ready && echo "ZITADEL ready" && exit 0
            echo "Waiting for ZITADEL... ($i/60)"
            sleep 2
          done
          echo "ZITADEL did not become ready" && exit 1

      - name: Run migrations and seed
        run: |
          docker compose exec -T api bash -c "PYTHONPATH=/home/app alembic upgrade head"
          docker compose exec -T api bash -c "PYTHONPATH=/home/app python scripts/seed.py"

      - name: Install Python test dependencies
        run: uv sync --frozen --dev

      - name: Run integration tests
        run: uv run pytest tests/integration/ -m integration -x -q
        env:
          DATABASE_URL: "postgresql+asyncpg://postgres:postgres@localhost:5433/run_api"

      - name: Install Playwright browsers
        working-directory: web
        run: |
          npm ci
          npx playwright install --with-deps chromium

      - name: Run E2E tests
        working-directory: web
        run: npx playwright test
        env:
          E2E_USERNAME: admin@localhost
          E2E_PASSWORD: "Admin1234!"

      - name: Upload Playwright report
        uses: actions/upload-artifact@v4
        if: ${{ !cancelled() }}
        with:
          name: playwright-report
          path: web/playwright-report/
          retention-days: 7

      - name: Upload Playwright traces
        uses: actions/upload-artifact@v4
        if: failure()
        with:
          name: playwright-traces
          path: web/test-results/
          retention-days: 7

      - name: Stop services
        if: always()
        run: docker compose down -v
```

Key details:
- `needs: [lint, test, frontend]` -- runs after fast checks pass (same as docker-build)
- `docker compose up -d --build --wait` -- builds fresh images and waits for healthchecks (per D-13)
- ZITADEL readiness loop with 60 attempts x 2s = 2min timeout (ZITADEL takes 30-60s to start)
- Integration tests use port 5433 (external Docker mapping) since pytest runs on host, not in container
- Playwright gets `E2E_USERNAME` and `E2E_PASSWORD` env vars for auth.setup.ts
- Artifact upload on failure for debugging traces (per D-11)
- `docker compose down -v` in `always()` for clean teardown

Keep ALL existing jobs (lint, test, frontend, docker-build) unchanged. Only ADD the new `integration-e2e` job.
  </action>
  <verify>
    <automated>cd /home/kwhatcher/projects/civicpulse/run-api && python3 -c "import yaml; yaml.safe_load(open('.github/workflows/pr.yml')); print('valid yaml')"</automated>
  </verify>
  <acceptance_criteria>
    - .github/workflows/pr.yml contains job `integration-e2e:`
    - .github/workflows/pr.yml contains `docker compose up -d --build --wait`
    - .github/workflows/pr.yml contains `alembic upgrade head`
    - .github/workflows/pr.yml contains `scripts/seed.py`
    - .github/workflows/pr.yml contains `uv run pytest tests/integration/ -m integration -x -q`
    - .github/workflows/pr.yml contains `npx playwright test`
    - .github/workflows/pr.yml contains `E2E_USERNAME`
    - .github/workflows/pr.yml contains `upload-artifact@v4` (at least 2 occurrences for report + traces)
    - .github/workflows/pr.yml contains `docker compose down -v`
    - .github/workflows/pr.yml still contains existing jobs: `lint:`, `test:`, `frontend:`, `docker-build:`
    - .github/workflows/pr.yml contains `needs: [lint, test, frontend]` for integration-e2e job
  </acceptance_criteria>
  <done>GitHub Actions pr.yml has new integration-e2e job that starts Docker Compose stack, runs migrations + seed, executes integration tests and Playwright E2E tests, uploads failure artifacts, and tears down services. All existing jobs preserved.</done>
</task>

</tasks>

<verification>
- `ls web/e2e/turf-creation.spec.ts web/e2e/phone-bank.spec.ts web/e2e/volunteer-signup.spec.ts` -- all 3 files exist
- `grep -c "page.route" web/e2e/turf-creation.spec.ts web/e2e/phone-bank.spec.ts web/e2e/volunteer-signup.spec.ts` -- all return 0
- `python3 -c "import yaml; yaml.safe_load(open('.github/workflows/pr.yml'))"` -- valid YAML
- `grep "integration-e2e" .github/workflows/pr.yml` -- new job exists
- Full E2E count: `ls web/e2e/login.spec.ts web/e2e/voter-search.spec.ts web/e2e/voter-import.spec.ts web/e2e/turf-creation.spec.ts web/e2e/phone-bank.spec.ts web/e2e/volunteer-signup.spec.ts` -- all 6 specs exist
</verification>

<success_criteria>
All 6 critical E2E flows have dedicated spec files running against real Docker Compose stack (TEST-01 complete). CI workflow gates PR merges on integration + E2E test passage (D-09, D-10). Playwright failure artifacts uploaded for debugging (D-11). Combined with Plan 02's RLS smoke tests and converted stubs, all three requirements (TEST-01, TEST-02, TEST-03) are satisfied.
</success_criteria>

<output>
After completion, create `.planning/phases/46-e2e-testing-integration/46-03-SUMMARY.md`
</output>
