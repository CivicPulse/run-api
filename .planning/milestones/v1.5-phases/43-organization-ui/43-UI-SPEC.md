---
phase: 43
slug: organization-ui
status: draft
shadcn_initialized: true
preset: new-york
created: 2026-03-24
revised: 2026-03-24
---

# Phase 43 — UI Design Contract

> Visual and interaction contract for the Organization UI phase. Generated by gsd-ui-researcher, verified by gsd-ui-checker.

---

## Design System

| Property | Value |
|----------|-------|
| Tool | shadcn |
| Preset | new-york |
| Component library | Radix UI (via shadcn) |
| Icon library | Lucide |
| Font | System font stack (shadcn new-york default) |

---

## Spacing Scale

Declared values (must be multiples of 4):

| Token | Value | Usage |
|-------|-------|-------|
| xs | 4px | Icon gaps, inline badge padding |
| sm | 8px | Compact element spacing, card internal gaps |
| md | 16px | Default element spacing, page padding, card content padding |
| lg | 24px | Section padding, wizard step spacing |
| xl | 32px | Layout gaps between major sections |
| 2xl | 48px | Stats bar to card grid gap |
| 3xl | 64px | Page-level top/bottom margins (not used in this phase) |

### Touch Targets

| Element | Minimum Size | Notes |
|---------|-------------|-------|
| Org switcher dropdown trigger | 44px height | Ensure full trigger area is tappable |
| Campaign card three-dot menu | 44x44px | Icon button with padding to meet target |
| Wizard step dots | 44x44px | Tap area around each dot, even if visual dot is smaller |

---

## Typography

This phase uses exactly 4 font sizes and 2 font weights.

| Role | Size | Weight | Line Height | Usage |
|------|------|--------|-------------|-------|
| Body / Metadata | 14px (`text-sm`) | 400 (normal) | 1.5 (21px) | Body text, labels, wizard step labels, card metadata (election date, member count), role matrix cell text |
| Heading / Stats | 24px (`text-2xl`) | 700 (bold) | 1.2 (29px) | Page headings, stats bar numbers |
| Display | 30px (`text-3xl`) | 700 (bold) | 1.2 (36px) | Org name display on dashboard |
| Small | 12px (`text-xs`) | 400 (normal) | 1.5 (18px) | Badge text, help text, timestamps |

De-emphasis strategy: Card metadata (election date, member count) and role matrix cells use 14px at weight 400 with `text-muted-foreground` color to visually de-emphasize without introducing a separate size. Labels and wizard step labels also use 14px weight 400 with default foreground.

**Sizes declared:** 12px, 14px, 24px, 30px (4 sizes)
**Weights declared:** 400, 700 (2 weights)

---

## Color

| Role | Value | Usage |
|------|-------|-------|
| Dominant (60%) | `var(--background)` oklch(1 0 0) | Page background, main content area |
| Secondary (30%) | `var(--card)` oklch(1 0 0) / `var(--muted)` oklch(0.97 0 0) | Campaign cards, stats bar background, member directory table header, settings sections |
| Accent (10%) | `var(--primary)` oklch(0.205 0 0) | Primary CTA buttons, active sidebar items, wizard active step indicator, org switcher active state |
| Destructive | `var(--destructive)` oklch(0.577 0.245 27.325) | Archive campaign confirmation button, danger zone section border and button |

Accent reserved for:
- "Create Campaign" button on org dashboard
- Wizard "Continue to Review" / "Continue to Invite" / "Create Campaign" step buttons
- Active step dot in wizard progress indicator
- Org switcher currently-selected org highlight
- "Save Changes" button on org settings page

Status badge colors:
- Active campaign: `variant="default"` (primary background) -- matches existing pattern
- Archived campaign: `variant="secondary"` (muted background) -- matches existing pattern

---

## Focal Points

| Screen | Primary Focal Point | Secondary Focal Point |
|--------|--------------------|-----------------------|
| Org dashboard (`/`) | "Create Campaign" button (accent, top-right of campaign grid area) | Stats bar (campaign count + member count) |
| Campaign wizard (`/campaigns/new`) | Active wizard step content area | Step indicator dots |
| Member directory (`/org/members`) | Member table (first visible rows) | "Add to Campaign" row actions |
| Org settings (`/org/settings`) | Org name input field | Danger zone section (visual weight from destructive border) |

---

## Copywriting Contract

### Org Dashboard (/)

| Element | Copy |
|---------|------|
| Page heading | `{Org Name}` (dynamic, org name as page title) |
| Stats bar | `{N} active campaigns · {M} members` |
| Primary CTA | `Create Campaign` |
| Empty state heading | `No campaigns yet` |
| Empty state body | `Create your first campaign to start running field operations.` |
| Empty state CTA | `Create Campaign` |
| Archived section toggle | `Archived ({N})` |

### Org Switcher (header)

| Element | Copy |
|---------|------|
| Trigger label | `{Current Org Name}` with chevron-down icon |
| Dropdown item | `{Org Name}` per org |
| Loading state | Spinner icon, no text |

### Campaign Card

| Element | Copy |
|---------|------|
| Status badge (active) | `Active` |
| Status badge (archived) | `Archived` |
| Election date | `Election: {formatted date}` |
| Member count | `{N} members` |
| Three-dot menu trigger | Icon-only `MoreHorizontal`, `aria-label="Campaign actions"` |
| Three-dot menu: archive | `Archive Campaign` |
| Three-dot menu: open | `Open Campaign` |

### Archive Confirmation Dialog

| Element | Copy |
|---------|------|
| Title | `Archive {Campaign Name}?` |
| Body | `Archived campaigns become read-only. Team members will still be able to view data but cannot make changes.` |
| Dismiss button | `Keep Campaign` |
| Confirm button | `Archive Campaign` (destructive variant) |

### Campaign Creation Wizard (/campaigns/new)

| Element | Copy |
|---------|------|
| Page heading | `Create Campaign` |
| Step 1 label | `Campaign Details` |
| Step 2 label | `Review` |
| Step 3 label | `Invite Team` |
| Step 1 fields | Name*, Type*, Jurisdiction, Election Date, Candidate Name |
| Step 2 heading | `Review your campaign` |
| Step 2 org line | `This campaign will be created in {Org Name}.` |
| Step 3 heading | `Add team members` |
| Step 3 body | `Select existing org members to add to this campaign. You can always add more later.` |
| Step 3 empty | `No other members in your organization yet.` |
| Back button | `Back` |
| Next button (step 1) | `Continue to Review` |
| Next button (step 2) | `Continue to Invite` |
| Submit button (step 3) | `Create Campaign` |
| Skip link (step 3) | `Skip — create without inviting` |
| Error: creation failed | `Failed to create campaign. Check your connection and try again.` |
| Wizard step indicator | Numbered dots: `1 · 2 · 3` with active dot filled |

### Member Directory (/org/members)

| Element | Copy |
|---------|------|
| Page heading | `Members` |
| Table columns | `Name`, `Email`, then one column per campaign name |
| Role cell (has role) | `{role}` (e.g., `admin`, `manager`, `viewer`) |
| Role cell (no membership) | `--` (em-dash) |
| Add-to-campaign action | `Add to Campaign` |
| Add-to-campaign dialog title | `Add {Member Name} to campaigns` |
| Add-to-campaign dialog body | `Select campaigns and assign a role for each.` |
| Add-to-campaign confirm | `Add Member` |
| Empty state heading | `No members` |
| Empty state body | `Organization members will appear here as they join.` |
| Error: add failed | `Failed to add member. Please try again.` |

### Org Settings (/org/settings)

| Element | Copy |
|---------|------|
| Page heading | `Organization Settings` |
| Org name label | `Organization Name` |
| Org name placeholder | `e.g. Macon-Bibb County Democrats` |
| ZITADEL ID label | `Organization ID` |
| ZITADEL ID help text | `Read-only identifier from your identity provider.` |
| Save button | `Save Changes` |
| Save success toast | `Organization updated.` |
| Save error | `Failed to update organization. Please try again.` |
| Danger zone heading | `Danger Zone` |
| Danger zone description | `These actions are permanent and cannot be undone.` |
| Transfer ownership (disabled) | `Transfer Ownership` — tooltip: `Coming soon` |
| Delete org (disabled) | `Delete Organization` — tooltip: `Coming soon` |

---

## Component Inventory

Components needed for this phase, mapped to existing shadcn components or new custom components:

### Existing shadcn Components (no installation needed)

| Component | Usage in Phase 43 |
|-----------|-------------------|
| `Card`, `CardHeader`, `CardContent`, `CardTitle`, `CardDescription` | Campaign cards, stats bar, settings sections |
| `Badge` | Campaign status badges (active/archived) |
| `Button` | CTAs, navigation, form actions |
| `Dialog`, `DialogContent`, `DialogHeader`, `DialogTitle`, `DialogDescription`, `DialogFooter` | Archive confirmation, add-to-campaign |
| `DropdownMenu`, `DropdownMenuContent`, `DropdownMenuItem`, `DropdownMenuTrigger` | Campaign card three-dot menu, org switcher |
| `Input` | Org name edit, campaign wizard fields |
| `Label` | Form field labels |
| `Select`, `SelectContent`, `SelectItem`, `SelectTrigger`, `SelectValue` | Campaign type, role assignment |
| `Checkbox` | Team invite member selection |
| `Separator` | Section dividers |
| `Skeleton` | Loading states for dashboard, member directory |
| `Table`, `TableBody`, `TableCell`, `TableHead`, `TableHeader`, `TableRow` | Member directory role matrix |
| `Tabs`, `TabsContent`, `TabsList`, `TabsTrigger` | Settings page sections (if needed) |
| `Tooltip`, `TooltipContent`, `TooltipProvider`, `TooltipTrigger` | Disabled danger zone actions |
| `Sidebar` (all sub-components) | Org nav items in sidebar |
| `Avatar`, `AvatarFallback` | Member directory avatars |

### New Custom Components

| Component | Description |
|-----------|-------------|
| `OrgSwitcher` | Header dropdown for switching between orgs. Renders current org name + ChevronDown icon. Dropdown lists all user orgs. Click triggers ZITADEL re-auth with org hint. |
| `RequireOrgRole` | Org-level role gate component. Wraps children, renders null if user lacks required org role. Same pattern as existing `RequireRole`. |
| `CampaignWizard` | 3-step wizard container. Manages step state, validation per step, and form data across steps. Uses react-hook-form with zod. |
| `WizardStepIndicator` | Numbered dot progress indicator (1 · 2 · 3). Active step: primary fill. Completed: primary fill + check. Upcoming: muted border only. |
| `StatsBar` | Horizontal bar above campaign grid. Shows campaign count + member count. Muted background, rounded corners. |
| `CampaignCard` | Enhanced version of existing campaign card. Adds member count, election date, three-dot menu with archive action. Three-dot trigger uses `aria-label="Campaign actions"`. |
| `RoleMatrixTable` | Table component for member directory. Dynamic columns based on campaign count. Horizontal scroll on overflow. |
| `AddToCampaignDialog` | Dialog with campaign checkbox list + role select per campaign. Confirm adds member to selected campaigns. |
| `DangerZone` | Red-bordered section with disabled destructive actions. Follows campaign settings danger zone pattern. |

---

## Interaction Contracts

### Org Switcher
- Trigger: Click org name in header -> dropdown opens
- Selection: Click org -> brief loading spinner replaces dropdown -> ZITADEL signinRedirect with org hint param -> callback -> navigate to `/`
- Close: Click outside or press Escape
- Keyboard: Arrow keys to navigate items, Enter to select

### Campaign Card Grid
- Layout: CSS grid `grid-cols-1 md:grid-cols-2 lg:grid-cols-3` with `gap-4` (16px)
- Card click: Navigate to `/campaigns/{id}/dashboard`
- Three-dot menu: Click to open dropdown (stopPropagation to avoid card navigation). Trigger uses `aria-label="Campaign actions"` for screen readers.
- Archive action: Opens confirmation dialog
- Hover: `hover:border-primary/50` transition (matches existing pattern)

### Campaign Creation Wizard
- Step navigation: "Continue to Review" advances from step 1, "Continue to Invite" advances from step 2, "Back" retreats, browser back goes to previous step
- Validation: Per-step validation on "Continue" click. Step 1 requires name + type. Step 2 is review only. Step 3 is optional.
- Skip: Step 3 "Skip" link submits without team invites
- Form guard: `useFormGuard` prevents accidental navigation away from partially filled wizard
- Submit: "Create Campaign" on step 3 submits, shows spinner, navigates to new campaign dashboard on success

### Member Directory
- Table: Sortable by name column. Horizontal scroll when campaigns exceed viewport width.
- Add-to-campaign: Row action button opens dialog. Dialog has checkbox per campaign + role dropdown per checked campaign.
- Loading: Skeleton rows (5 rows) during fetch

### Org Settings
- Org name edit: Inline input, "Save Changes" button. Disabled until value changes from original.
- ZITADEL org ID: Read-only input with `readOnly` attribute and muted styling
- Danger zone: Visually separated with destructive border-left. Buttons disabled with "Coming soon" tooltip.
- Permission: Org name field editable only for org_owner. Other org_admin users see read-only.

### Archive Campaign
- Trigger: Three-dot menu -> "Archive Campaign"
- Confirmation: Dialog with campaign name in title. Destructive-variant confirm button. Dismiss button reads "Keep Campaign".
- Result: Campaign moves to archived section. Toast: `{Campaign Name} archived.`
- Undo: Not supported in this phase (archived section has no "unarchive" action yet)

### Archived Campaigns Section
- Default: Collapsed with "Archived (N)" toggle
- Expand: Click toggle to reveal archived campaign cards in muted/faded style
- Cards: Same layout but no three-dot menu, just "Open Campaign" link

---

## Loading States

| View | Loading Skeleton |
|------|-----------------|
| Org dashboard | Stats bar skeleton (1 line, 200px wide) + 3 card skeletons in grid (h-40 each) |
| Member directory | Table header + 5 skeleton rows |
| Org settings | 2 input field skeletons + 1 section skeleton |
| Campaign wizard | No skeleton (form renders immediately, data pre-fetched) |
| Org switcher dropdown | Spinner icon replacing chevron during org switch |

---

## Registry Safety

| Registry | Blocks Used | Safety Gate |
|----------|-------------|-------------|
| shadcn official | Card, Badge, Button, Dialog, DropdownMenu, Input, Label, Select, Checkbox, Separator, Skeleton, Table, Tabs, Tooltip, Sidebar, Avatar | not required |

No third-party registries. `components.json` confirms `"registries": {}`.

---

## Checker Sign-Off

- [ ] Dimension 1 Copywriting: PASS
- [ ] Dimension 2 Visuals: PASS
- [ ] Dimension 3 Color: PASS
- [ ] Dimension 4 Typography: PASS
- [ ] Dimension 5 Spacing: PASS
- [ ] Dimension 6 Registry Safety: PASS

**Approval:** pending
