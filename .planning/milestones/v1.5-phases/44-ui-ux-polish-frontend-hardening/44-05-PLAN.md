---
phase: 44-ui-ux-polish-frontend-hardening
plan: 05
type: execute
wave: 1
depends_on: []
files_modified:
  - web/src/routes/org/settings.tsx
  - web/src/routes/org/members.tsx
autonomous: true
requirements: [UX-03, UX-04, OBS-05, OBS-06]
gap_closure: true

must_haves:
  truths:
    - "org/settings.tsx has a TooltipIcon next to the Organization ID label explaining the ZITADEL identifier"
    - "org/members.tsx empty state reads 'No members yet' with action-oriented description and a Users icon"
    - "Both org/members.tsx and org/settings.tsx have per-route errorComponent set to RouteErrorBoundary"
  artifacts:
    - path: "web/src/routes/org/settings.tsx"
      provides: "TooltipIcon on Org ID + errorComponent"
      contains: "TooltipIcon"
    - path: "web/src/routes/org/members.tsx"
      provides: "Corrected empty state + errorComponent"
      contains: "No members yet"
  key_links:
    - from: "web/src/routes/org/settings.tsx"
      to: "web/src/components/shared/TooltipIcon.tsx"
      via: "import"
      pattern: "import.*TooltipIcon.*shared/TooltipIcon"
    - from: "web/src/routes/org/settings.tsx"
      to: "web/src/components/shared/RouteErrorBoundary.tsx"
      via: "errorComponent option"
      pattern: "errorComponent.*RouteErrorBoundary"
    - from: "web/src/routes/org/members.tsx"
      to: "web/src/components/shared/RouteErrorBoundary.tsx"
      via: "errorComponent option"
      pattern: "errorComponent.*RouteErrorBoundary"
---

<objective>
Close 3 verification gaps from Phase 44 caused by branch divergence between Phase 43 org routes and Phase 44 worktrees.

Purpose: Complete the Phase 44 must-haves for tooltip coverage (UX-03, UX-04), empty state wording (OBS-06), and per-route error boundaries (OBS-05) on the two org route files that were missed.
Output: Updated org/settings.tsx and org/members.tsx with all gaps closed.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/44-ui-ux-polish-frontend-hardening/44-VERIFICATION.md

<interfaces>
<!-- Existing shared components to import. No new components needed. -->

From web/src/components/shared/TooltipIcon.tsx:
```typescript
interface TooltipIconProps {
  content: string
  side?: "top" | "right" | "bottom" | "left"
}
export function TooltipIcon({ content, side = "top" }: TooltipIconProps)
```

From web/src/components/shared/RouteErrorBoundary.tsx:
```typescript
interface RouteErrorProps {
  error: Error
  reset: () => void
}
export function RouteErrorBoundary({ error, reset }: RouteErrorProps)
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add TooltipIcon and errorComponent to org/settings.tsx</name>
  <files>web/src/routes/org/settings.tsx</files>
  <read_first>
    - web/src/routes/org/settings.tsx
    - web/src/components/shared/TooltipIcon.tsx
    - web/src/components/shared/RouteErrorBoundary.tsx
  </read_first>
  <action>
Two changes to web/src/routes/org/settings.tsx:

1. Add two imports at the top of the file:
   - `import { TooltipIcon } from "@/components/shared/TooltipIcon"`
   - `import { RouteErrorBoundary } from "@/components/shared/RouteErrorBoundary"`

2. Add a TooltipIcon next to the "Organization ID" Label (line 98 area). Change:
   ```
   <Label htmlFor="org-id">Organization ID</Label>
   ```
   to:
   ```
   <div className="flex items-center gap-1">
     <Label htmlFor="org-id">Organization ID</Label>
     <TooltipIcon content="This is your organization's unique identifier in the authentication system. Share it with support if you need assistance with account or access issues." />
   </div>
   ```

3. Add errorComponent to the Route definition. Change:
   ```
   export const Route = createFileRoute("/org/settings")({
     component: OrgSettingsPage,
   })
   ```
   to:
   ```
   export const Route = createFileRoute("/org/settings")({
     component: OrgSettingsPage,
     errorComponent: RouteErrorBoundary,
   })
   ```
  </action>
  <verify>
    <automated>cd /home/kwhatcher/projects/civicpulse/run-api/web && grep -q "TooltipIcon" src/routes/org/settings.tsx && grep -q "errorComponent: RouteErrorBoundary" src/routes/org/settings.tsx && grep -q "unique identifier in the authentication system" src/routes/org/settings.tsx && npx tsc --noEmit 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - org/settings.tsx contains `import { TooltipIcon } from "@/components/shared/TooltipIcon"`
    - org/settings.tsx contains `import { RouteErrorBoundary } from "@/components/shared/RouteErrorBoundary"`
    - org/settings.tsx contains `<TooltipIcon content="This is your organization's unique identifier in the authentication system. Share it with support if you need assistance with account or access issues." />`
    - org/settings.tsx contains `errorComponent: RouteErrorBoundary`
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>org/settings.tsx has TooltipIcon on Organization ID label and per-route errorComponent wired to RouteErrorBoundary</done>
</task>

<task type="auto">
  <name>Task 2: Fix empty state wording and add errorComponent to org/members.tsx</name>
  <files>web/src/routes/org/members.tsx</files>
  <read_first>
    - web/src/routes/org/members.tsx
    - web/src/components/shared/RouteErrorBoundary.tsx
  </read_first>
  <action>
Three changes to web/src/routes/org/members.tsx:

1. Add two imports at the top of the file:
   - `import { Users } from "lucide-react"` (for empty state icon)
   - `import { RouteErrorBoundary } from "@/components/shared/RouteErrorBoundary"`

2. Update the empty state block (lines 68-79 area). Change:
   ```
   <div className="flex flex-col items-center justify-center py-12">
     <h2 className="text-lg font-semibold">No members</h2>
     <p className="text-sm text-muted-foreground mt-1">
       Organization members will appear here as they join.
     </p>
   </div>
   ```
   to:
   ```
   <div className="flex flex-col items-center justify-center py-12">
     <Users className="h-10 w-10 text-muted-foreground/60 mb-3" />
     <h2 className="text-lg font-semibold">No members yet</h2>
     <p className="text-sm text-muted-foreground mt-1">
       Add members to your organization.
     </p>
   </div>
   ```

3. Add errorComponent to the Route definition. Change:
   ```
   export const Route = createFileRoute("/org/members")({
     component: MembersPage,
   })
   ```
   to:
   ```
   export const Route = createFileRoute("/org/members")({
     component: MembersPage,
     errorComponent: RouteErrorBoundary,
   })
   ```
  </action>
  <verify>
    <automated>cd /home/kwhatcher/projects/civicpulse/run-api/web && grep -q "No members yet" src/routes/org/members.tsx && grep -q "Add members to your organization" src/routes/org/members.tsx && grep -q "Users" src/routes/org/members.tsx && grep -q "errorComponent: RouteErrorBoundary" src/routes/org/members.tsx && npx tsc --noEmit 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - org/members.tsx contains `import { Users } from "lucide-react"`
    - org/members.tsx contains `import { RouteErrorBoundary } from "@/components/shared/RouteErrorBoundary"`
    - org/members.tsx contains `No members yet` (not just "No members")
    - org/members.tsx contains `Add members to your organization.`
    - org/members.tsx contains `<Users className=`
    - org/members.tsx contains `errorComponent: RouteErrorBoundary`
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>org/members.tsx has corrected empty state with Users icon, action-oriented copy, and per-route errorComponent wired to RouteErrorBoundary</done>
</task>

</tasks>

<verification>
All 3 verification gaps from 44-VERIFICATION.md are closed:
- Gap 1 (tooltip): `grep "TooltipIcon" web/src/routes/org/settings.tsx` returns match
- Gap 2 (empty state): `grep "No members yet" web/src/routes/org/members.tsx` returns match
- Gap 3 (errorComponent): `grep "errorComponent: RouteErrorBoundary" web/src/routes/org/settings.tsx web/src/routes/org/members.tsx` returns matches in both files
- TypeScript: `cd web && npx tsc --noEmit` exits 0
</verification>

<success_criteria>
- org/settings.tsx has TooltipIcon on Organization ID label with authentication system explanation text
- org/members.tsx empty state shows Users icon, "No members yet" title, and "Add members to your organization." description
- Both org/settings.tsx and org/members.tsx have `errorComponent: RouteErrorBoundary` in route definitions
- TypeScript compiles clean with no errors
</success_criteria>

<output>
After completion, create `.planning/phases/44-ui-ux-polish-frontend-hardening/44-05-SUMMARY.md`
</output>
