---
phase: 45-wcag-compliance-audit
plan: 02
type: execute
wave: 2
depends_on: ["45-01"]
files_modified:
  - web/src/components/canvassing/TurfForm.tsx
  - web/src/routes/campaigns/$campaignId/canvassing/turfs/new.tsx
  - web/src/routes/campaigns/$campaignId/canvassing/turfs/$turfId.tsx
autonomous: true
requirements:
  - A11Y-04

must_haves:
  truths:
    - "Keyboard user can skip past the Leaflet map to the turf form fields"
    - "Screen reader user can create a turf boundary via the GeoJSON textarea without interacting with the map"
    - "GeoJSON panel validates on blur and announces errors via aria-invalid"
  artifacts:
    - path: "web/src/components/canvassing/TurfForm.tsx"
      provides: "Accessible turf form with skip-nav and GeoJSON fallback panel"
      contains: "aria-expanded"
    - path: "web/src/routes/campaigns/$campaignId/canvassing/turfs/new.tsx"
      provides: "Turf creation page with skip-map link"
      contains: "turf-form"
  key_links:
    - from: "web/src/components/canvassing/TurfForm.tsx"
      to: "web/src/components/shared/SkipNav.tsx"
      via: "skip-nav link targeting turf-form section"
      pattern: "skip|turf-form"
    - from: "web/src/components/canvassing/TurfForm.tsx"
      to: "GeoJSON textarea"
      via: "aria-expanded toggle and aria-controls"
      pattern: "aria-expanded|aria-controls"
---

<objective>
Make turf map pages accessible by adding a skip-nav link past the Leaflet map and enhancing the existing GeoJSON textarea toggle with proper ARIA attributes for screen reader and keyboard operability.

Purpose: Users who cannot interact with the map (screen reader users, keyboard-only users) can still create, edit, and delete turf boundaries through the GeoJSON fallback panel.
Output: Accessible TurfForm with skip-map link, ARIA-compliant GeoJSON panel toggle.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/45-wcag-compliance-audit/45-CONTEXT.md
@.planning/phases/45-wcag-compliance-audit/45-RESEARCH.md
@.planning/phases/45-wcag-compliance-audit/45-01-SUMMARY.md

@web/src/components/canvassing/TurfForm.tsx
@web/src/routes/campaigns/$campaignId/canvassing/turfs/new.tsx
@web/src/routes/campaigns/$campaignId/canvassing/turfs/$turfId.tsx
@web/src/components/shared/SkipNav.tsx

<interfaces>
From web/src/components/shared/SkipNav.tsx (created in Plan 01):
```typescript
export function SkipNav({ targetId = 'main-content' }: { targetId?: string }): JSX.Element;
```

From web/src/components/canvassing/TurfForm.tsx (existing):
```typescript
interface TurfFormProps {
  defaultValues?: TurfResponse;
  onSubmit: (data: { name: string; description?: string; boundary: Record<string, unknown> }) => void;
  isPending: boolean;
  submitLabel: string;
  campaignId: string;
  turfId?: string;
}
export function TurfForm(props: TurfFormProps): JSX.Element;
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add skip-map link and ARIA-compliant GeoJSON panel to TurfForm</name>
  <files>web/src/components/canvassing/TurfForm.tsx</files>
  <read_first>web/src/components/canvassing/TurfForm.tsx, web/src/components/shared/SkipNav.tsx, web/src/components/canvassing/map/TurfMapEditor.tsx</read_first>
  <action>
Edit `web/src/components/canvassing/TurfForm.tsx`:

1. Add a skip-map link BEFORE the TurfMapEditor component (per D-12):
   - Render an `<a>` element with `href="#turf-form-fields"`
   - Use same skip-nav styling pattern: `className="sr-only focus:not-sr-only focus:relative focus:block focus:rounded focus:bg-background focus:px-4 focus:py-2 focus:text-foreground focus:shadow-lg focus:ring-2 focus:ring-ring focus:mb-2"`
   - Text: "Skip map, edit turf details"

2. Add `id="turf-form-fields"` to a wrapper `<div>` around the form fields BELOW the map section (starting from the "Advanced JSON" toggle button area). This is the skip target.

3. Enhance the existing "Show/Hide Advanced JSON" toggle button (per D-13, D-14):
   - Replace the existing `<button>` with a shadcn `<Button variant="outline" size="sm">`
   - Add `aria-expanded={showAdvanced}` attribute
   - Add `aria-controls="geojson-panel"` attribute
   - Change text to: `{showAdvanced ? "Hide GeoJSON editor" : "Edit as GeoJSON"}`

4. Wrap the existing conditional `{showAdvanced && <Textarea .../>}` block:
   - Add `id="geojson-panel"` to a wrapper div
   - Add `role="region"` and `aria-label="GeoJSON editor"` to the wrapper
   - Add a `<Label htmlFor="boundary">GeoJSON Polygon</Label>` before the Textarea
   - Add `aria-invalid={!!errors.boundary}` to the Textarea
   - Add `aria-describedby={errors.boundary ? 'boundary-error' : undefined}` to the Textarea
   - Change the boundary error `<p>` below the map to have `id="boundary-error"` and `role="alert"`

5. Add `aria-label` attributes to form error messages:
   - Name error `<p>`: add `role="alert"`
   - Boundary error `<p>`: add `id="boundary-error"` and `role="alert"`

6. Add `aria-label="Turf overlaps warning"` to the overlaps warning `<p>` element
  </action>
  <verify>
    <automated>cd /home/kwhatcher/projects/civicpulse/run-api && grep -q 'aria-expanded' web/src/components/canvassing/TurfForm.tsx && grep -q 'aria-controls="geojson-panel"' web/src/components/canvassing/TurfForm.tsx && grep -q 'turf-form-fields' web/src/components/canvassing/TurfForm.tsx && grep -q 'Skip map' web/src/components/canvassing/TurfForm.tsx && echo "PASS"</automated>
  </verify>
  <acceptance_criteria>
    - TurfForm.tsx contains skip link with text "Skip map, edit turf details" and href="#turf-form-fields"
    - TurfForm.tsx contains `id="turf-form-fields"` on a div below the map
    - TurfForm.tsx contains `aria-expanded={showAdvanced}` on the toggle button
    - TurfForm.tsx contains `aria-controls="geojson-panel"` on the toggle button
    - TurfForm.tsx contains `id="geojson-panel"` with `role="region"` wrapper
    - TurfForm.tsx contains `aria-invalid` on the Textarea
    - TurfForm.tsx contains `role="alert"` on error message elements
    - `cd web && npx tsc --noEmit` passes
  </acceptance_criteria>
  <done>Turf form has skip-map link, ARIA-compliant GeoJSON toggle panel, labeled form errors, and all CRUD operations are operable without interacting with the Leaflet map</done>
</task>

<task type="auto">
  <name>Task 2: Add heading hierarchy and landmark labels to turf route pages</name>
  <files>web/src/routes/campaigns/$campaignId/canvassing/turfs/new.tsx, web/src/routes/campaigns/$campaignId/canvassing/turfs/$turfId.tsx</files>
  <read_first>web/src/routes/campaigns/$campaignId/canvassing/turfs/new.tsx, web/src/routes/campaigns/$campaignId/canvassing/turfs/$turfId.tsx</read_first>
  <action>
1. Edit `web/src/routes/campaigns/$campaignId/canvassing/turfs/new.tsx` (per D-09):
   - Ensure the page has an `<h1>` heading ("Create New Turf" or similar) -- check if one exists already
   - If the heading is an `<h2>` or other, change to `<h1>`
   - Wrap the page content in a `<section aria-labelledby="page-heading">` if not already wrapped
   - Add `id="page-heading"` to the h1

2. Edit `web/src/routes/campaigns/$campaignId/canvassing/turfs/$turfId.tsx` (per D-09):
   - Same heading hierarchy check -- ensure `<h1>` for "Edit Turf" or similar
   - Add `id="page-heading"` to the h1
   - Wrap content in `<section aria-labelledby="page-heading">` if needed
  </action>
  <verify>
    <automated>cd /home/kwhatcher/projects/civicpulse/run-api && grep -q 'h1' web/src/routes/campaigns/\$campaignId/canvassing/turfs/new.tsx && grep -q 'h1' web/src/routes/campaigns/\$campaignId/canvassing/turfs/\$turfId.tsx && echo "PASS"</automated>
  </verify>
  <acceptance_criteria>
    - turfs/new.tsx contains an `<h1>` element for the page title
    - turfs/$turfId.tsx contains an `<h1>` element for the page title
    - Both files pass TypeScript compilation: `cd web && npx tsc --noEmit`
  </acceptance_criteria>
  <done>Turf creation and edit pages have correct heading hierarchy with h1 page titles</done>
</task>

</tasks>

<verification>
- `cd web && npx tsc --noEmit` passes
- TurfForm has skip-map link, aria-expanded toggle, geojson-panel region
- Turf route pages have h1 headings
</verification>

<success_criteria>
- Keyboard user can Tab to "Skip map" link on turf pages and jump past the Leaflet map
- GeoJSON panel toggle has aria-expanded and aria-controls for screen readers
- GeoJSON textarea has aria-invalid and aria-describedby for validation errors
- Turf pages have correct h1 heading hierarchy
</success_criteria>

<output>
After completion, create `.planning/phases/45-wcag-compliance-audit/45-02-SUMMARY.md`
</output>
