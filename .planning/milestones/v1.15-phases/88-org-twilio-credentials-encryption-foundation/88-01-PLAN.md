---
phase: 88-org-twilio-credentials-encryption-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/core/config.py
  - app/models/organization.py
  - app/schemas/org.py
  - app/services/twilio_config.py
  - alembic/versions/029_org_twilio_config_encryption.py
  - tests/unit/test_twilio_config_service.py
autonomous: true
requirements:
  - SEC-01
must_haves:
  truths:
    - "Twilio secret material persists only as encrypted org-scoped fields with reusable metadata."
    - "Backend code has a single service seam that can encrypt, decrypt, and redact org provider credentials."
    - "Encryption key selection is explicit and configurable so later secret rotation work has a stable anchor."
  artifacts:
    - path: "app/models/organization.py"
      provides: "Org-level Twilio config columns and metadata fields"
    - path: "alembic/versions/029_org_twilio_config_encryption.py"
      provides: "Database migration for encrypted Twilio persistence"
    - path: "app/services/twilio_config.py"
      provides: "Encryption, decryption, masking, and provider-config helpers"
    - path: "app/core/config.py"
      provides: "Application-managed encryption key settings"
  key_links:
    - from: "app/core/config.py"
      to: "app/services/twilio_config.py"
      via: "settings-backed keyring loader"
      pattern: "twilio.*encryption"
    - from: "app/services/twilio_config.py"
      to: "app/models/organization.py"
      via: "encrypted field read/write helpers"
      pattern: "Organization.*twilio"
---

<objective>
Create the org-scoped Twilio persistence and encryption foundation so later route and UI work can store secrets safely without inventing a second secret path.

Purpose: Satisfy the at-rest protection and reusable service-seam parts of SEC-01 before any API surface starts accepting credentials.
Output: Org model + migration changes, encryption config wiring, and a dedicated Twilio config service with unit coverage.
</objective>

<execution_context>
@/home/kwhatcher/.codex/get-shit-done/workflows/execute-plan.md
@/home/kwhatcher/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/88-org-twilio-credentials-encryption-foundation/88-CONTEXT.md
@app/core/config.py
@app/models/organization.py
@app/schemas/org.py

<interfaces>
From `app/models/organization.py`:

```python
class Organization(Base):
    id: Mapped[uuid.UUID]
    zitadel_org_id: Mapped[str]
    name: Mapped[str]
    created_at: Mapped[datetime]
    updated_at: Mapped[datetime]
```

From `app/schemas/org.py`:

```python
class OrgResponse(BaseSchema):
    id: uuid.UUID
    name: str
    zitadel_org_id: str
    created_at: datetime

class OrgUpdate(BaseSchema):
    name: str | None = Field(None, min_length=1, max_length=200)
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add encrypted org-level Twilio persistence contract</name>
  <files>app/models/organization.py, alembic/versions/029_org_twilio_config_encryption.py, app/schemas/org.py</files>
  <behavior>
    - Test 1: Organization metadata can represent a configured-but-redacted Twilio state without storing plaintext token fields.
    - Test 2: Migration creates nullable org-scoped Twilio columns and metadata fields that support future provider secrets, not just one token.
  </behavior>
  <action>Extend the organization persistence model per the Context decisions on org-scoped storage and reusable secret metadata. Add concrete columns for non-secret Twilio account identity plus encrypted secret blob, key identifier, masked hint, readiness timestamps, and updated-at metadata; do not add plaintext auth-token columns or schema fields that would ever round-trip the raw token. Create Alembic migration `alembic/versions/029_org_twilio_config_encryption.py` that adds the new columns with safe null defaults and reversible downgrade logic. Extend org schemas only with redacted/status-facing structures needed downstream; avoid any schema repr or field definition that could accidentally expose raw secret values.</action>
  <verify>
    <automated>uv run pytest tests/unit/test_org_model.py tests/unit/test_twilio_config_service.py -x</automated>
  </verify>
  <done>Database and ORM layers can represent encrypted Twilio configuration for an organization, and the response-layer schema surface contains only non-secret state.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Build the reusable Twilio encryption and redaction service seam</name>
  <files>app/core/config.py, app/services/twilio_config.py, tests/unit/test_twilio_config_service.py</files>
  <behavior>
    - Test 1: Encrypting a Twilio auth token produces ciphertext plus a key identifier, and decrypting with the configured key recovers the original token.
    - Test 2: Redaction helpers return masked hints and status booleans without ever including plaintext token material in returned objects or exception strings.
    - Test 3: Missing or malformed encryption settings fail closed with explicit non-secret errors.
  </behavior>
  <action>Add application-managed encryption settings to `app/core/config.py` for a current key id and key material source per the Context discretion on primitive choice. Implement `app/services/twilio_config.py` as the dedicated provider-agnostic seam that owns key loading, encryption/decryption helpers, redacted status derivation, and a future-ready provider credential object for downstream Twilio clients. Use the already-available `cryptography` dependency rather than hand-rolled crypto. Keep secret-bearing objects out of repr/log strings, and structure errors so malformed inputs or key issues cannot leak submitted token content.</action>
  <verify>
    <automated>uv run pytest tests/unit/test_twilio_config_service.py -x</automated>
  </verify>
  <done>The codebase has one backend module that safely encrypts, decrypts, and redacts org-scoped Twilio credentials using configured application keys.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| environment→backend config | Encryption key material enters the app process here |
| backend service→database | Secret-bearing data crosses from memory into persistent storage |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-88-01 | I | `app/services/twilio_config.py` | mitigate | Use application-managed authenticated encryption, store only ciphertext + key id + masked hint, and keep plaintext out of returned DTOs and exception messages. |
| T-88-02 | T | `alembic/versions/029_org_twilio_config_encryption.py` / `app/models/organization.py` | mitigate | Persist explicit encrypted columns only; do not add plaintext fallbacks or convenience fields that could be populated unsafely. |
| T-88-03 | D | `app/core/config.py` | mitigate | Fail closed when encryption settings are absent or malformed so the app never silently writes plaintext provider secrets. |
</threat_model>

<verification>
Run the focused unit suite for the new encryption service and org model contract, then inspect the migration and model definitions to confirm no plaintext auth-token field exists.
</verification>

<success_criteria>
Phase 88 has a concrete encryption foundation when:
- organizations can persist non-secret Twilio account identity plus encrypted secret metadata
- encryption keys are configurable and tied to explicit key identifiers
- a dedicated backend seam can produce redacted status objects without leaking secrets
</success_criteria>

<output>
After completion, create `.planning/phases/88-org-twilio-credentials-encryption-foundation/88-01-SUMMARY.md`
</output>
