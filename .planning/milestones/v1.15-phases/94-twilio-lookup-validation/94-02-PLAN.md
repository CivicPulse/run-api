---
phase: 94-twilio-lookup-validation
plan: 02
type: execute
wave: 2
depends_on: ["94-01"]
files_modified:
  - app/services/voter_contact.py
  - app/api/v1/voter_contacts.py
  - app/schemas/voter_contact.py
  - app/services/sms.py
  - tests/unit/test_voter_contacts.py
  - tests/unit/test_sms_api.py
  - tests/unit/test_sms_service.py
autonomous: true
requirements:
  - LOOK-01
  - SMS-01
  - COMP-01
must_haves:
  truths:
    - "Phone create and update flows return lookup-derived validation summaries without blocking save when Twilio Lookup is degraded"
    - "Phone contact reads expose enough validation state for inline UI badges and manual refresh"
    - "SMS eligibility checks reuse cached lookup results and block unsafe line types before send"
  artifacts:
    - path: "app/services/voter_contact.py"
      provides: "Lookup-aware phone CRUD behavior"
      contains: "validation"
    - path: "app/api/v1/voter_contacts.py"
      provides: "Phone CRUD and refresh routes returning validation summaries"
      contains: "phones"
    - path: "app/services/sms.py"
      provides: "SMS eligibility backed by cached lookup state"
      contains: "check_eligibility"
---

<objective>
Wire cached lookup intelligence into the real contact CRUD and SMS-eligibility paths so the platform uses one reusable validation source of truth.
</objective>

<context>
@.planning/phases/94-twilio-lookup-validation/94-CONTEXT.md
@.planning/phases/94-twilio-lookup-validation/94-RESEARCH.md
@.planning/phases/94-twilio-lookup-validation/94-VALIDATION.md
@.planning/phases/94-twilio-lookup-validation/94-01-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend voter-contact CRUD and reads with validation summaries</name>
  <files>
    app/services/voter_contact.py,
    app/api/v1/voter_contacts.py,
    app/schemas/voter_contact.py,
    tests/unit/test_voter_contacts.py
  </files>
  <behavior>
    - Test 1: add/update phone saves still succeed when lookup refresh fails, but the response marks validation as pending or unavailable
    - Test 2: get contacts returns validation summary data alongside phone rows
    - Test 3: explicit refresh lookup action updates a stale cache row for a phone contact
  </behavior>
  <action>
    1. Inject the shared phone-validation service into phone add/update/list flows
    2. Extend response schemas with compact validation summary fields
    3. Add or expose a refresh endpoint/action for stale-cache recovery
    4. Add focused service/API unit coverage
  </action>
  <verify>
    <automated>cd /home/kwhatcher/projects/civicpulse/run-api && uv run pytest tests/unit/test_voter_contacts.py -x -q</automated>
  </verify>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Make SMS eligibility consume cached lookup intelligence</name>
  <files>
    app/services/sms.py,
    tests/unit/test_sms_api.py,
    tests/unit/test_sms_service.py
  </files>
  <behavior>
    - Test 1: mobile lookup results allow SMS when consent and opt-out rules are otherwise satisfied
    - Test 2: landline, voip, and unknown/review-needed lookup results block SMS with structured reason codes
    - Test 3: stale cache at send time triggers one refresh attempt before final eligibility is decided
  </behavior>
  <action>
    1. Reuse the phone-validation service inside SMS eligibility checks
    2. Preserve consent and opt-out gating while layering lookup safety on top
    3. Return machine-readable reason codes and summary data for UI preflight surfaces
  </action>
  <verify>
    <automated>cd /home/kwhatcher/projects/civicpulse/run-api && uv run pytest tests/unit/test_sms_api.py tests/unit/test_sms_service.py -x -q</automated>
  </verify>
</task>

</tasks>

<verification>
- `uv run pytest tests/unit/test_voter_contacts.py tests/unit/test_sms_api.py tests/unit/test_sms_service.py -x -q`
</verification>

<output>
After completion, create `.planning/phases/94-twilio-lookup-validation/94-02-SUMMARY.md`
</output>
