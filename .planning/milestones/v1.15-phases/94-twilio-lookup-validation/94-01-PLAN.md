---
phase: 94-twilio-lookup-validation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - alembic/versions/035_phone_validation_cache.py
  - app/models/phone_validation.py
  - app/models/__init__.py
  - app/db/base.py
  - app/schemas/voter_contact.py
  - app/services/phone_validation.py
  - tests/unit/test_phone_validation_models.py
  - tests/unit/test_phone_validation_service.py
autonomous: true
requirements:
  - LOOK-01
must_haves:
  truths:
    - "Phone validation cache stores campaign-scoped normalized lookup intelligence separately from voter source contact data"
    - "A shared lookup service can read fresh cache rows, refresh stale rows, and return a compact validation summary"
    - "Twilio Lookup failures produce warning/pending state instead of hard contact-save failure semantics"
  artifacts:
    - path: "app/models/phone_validation.py"
      provides: "Campaign-scoped phone validation cache model"
      contains: "class PhoneValidation"
    - path: "app/services/phone_validation.py"
      provides: "Lookup cache and refresh service"
      contains: "class PhoneValidationService"
    - path: "alembic/versions/035_phone_validation_cache.py"
      provides: "Migration for validation cache storage"
      contains: "phone_validations"
  key_links:
    - from: "app/services/phone_validation.py"
      to: "app/services/twilio_config.py"
      via: "org-scoped Twilio client reuse"
      pattern: "client_for_org"
    - from: "app/services/phone_validation.py"
      to: "app/models/phone_validation.py"
      via: "cache read/write and TTL checks"
      pattern: "PhoneValidation"
---

<objective>
Create the cache and service foundation for Twilio Lookup validation so contact flows and SMS safety checks can share a stable source of truth.
</objective>

<context>
@.planning/phases/94-twilio-lookup-validation/94-CONTEXT.md
@.planning/phases/94-twilio-lookup-validation/94-UI-SPEC.md
@.planning/phases/94-twilio-lookup-validation/94-RESEARCH.md
@.planning/phases/94-twilio-lookup-validation/94-VALIDATION.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add campaign-scoped phone validation cache storage</name>
  <files>
    alembic/versions/035_phone_validation_cache.py,
    app/models/phone_validation.py,
    app/models/__init__.py,
    app/db/base.py,
    tests/unit/test_phone_validation_models.py
  </files>
  <behavior>
    - Test 1: cache rows store campaign_id, normalized phone number, validity, carrier, line_type, stale/error metadata, raw lookup payload, and validated_at
    - Test 2: campaign_id + normalized_phone_number is unique for reuse within a campaign
    - Test 3: source contact fields remain outside the cache model
  </behavior>
  <action>
    1. Add the `phone_validations` table and SQLAlchemy model
    2. Store both normalized summary fields and raw Twilio payload safely
    3. Register the model in the app model/base exports
    4. Add focused model/migration tests
  </action>
  <verify>
    <automated>cd /home/kwhatcher/projects/civicpulse/run-api && uv run pytest tests/unit/test_phone_validation_models.py -x -q</automated>
  </verify>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Build the shared Twilio Lookup cache service</name>
  <files>
    app/schemas/voter_contact.py,
    app/services/phone_validation.py,
    tests/unit/test_phone_validation_service.py
  </files>
  <behavior>
    - Test 1: fresh cache rows are reused without a provider call
    - Test 2: stale or missing rows trigger a refresh and update the cache
    - Test 3: Twilio failures return warning/pending summary state without raising contact-blocking errors
  </behavior>
  <action>
    1. Add validation summary schemas shared by contact and SMS consumers
    2. Implement `PhoneValidationService` with normalization, TTL, cache read/write, and Twilio client integration
    3. Add unit coverage for fresh, stale, and provider-failure scenarios
  </action>
  <verify>
    <automated>cd /home/kwhatcher/projects/civicpulse/run-api && uv run pytest tests/unit/test_phone_validation_service.py -x -q</automated>
  </verify>
</task>

</tasks>

<verification>
- `uv run pytest tests/unit/test_phone_validation_models.py tests/unit/test_phone_validation_service.py -x -q`
</verification>

<output>
After completion, create `.planning/phases/94-twilio-lookup-validation/94-01-SUMMARY.md`
</output>
