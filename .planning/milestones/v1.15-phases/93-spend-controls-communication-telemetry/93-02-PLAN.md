---
phase: 93-spend-controls-communication-telemetry
plan: 02
type: execute
wave: 2
depends_on: ["93-01"]
files_modified:
  - app/api/v1/org.py
  - app/api/v1/webhooks.py
  - app/services/communication_budget.py
  - app/services/voice.py
  - app/services/sms.py
  - app/schemas/org.py
  - tests/unit/test_org_api.py
  - tests/unit/test_sms_api.py
  - tests/unit/test_sms_webhooks.py
  - tests/unit/test_call_records.py
autonomous: true
requirements:
  - BUD-01
  - OBS-01
must_haves:
  truths:
    - "Voice call start, single SMS send, and bulk SMS queue launch all check the same soft-budget service before starting"
    - "Communication actions append provisional telemetry rows and reconcile final cost/status from callbacks"
    - "Org API returns and updates spend-policy / summary data for admins"
  artifacts:
    - path: "app/services/voice.py"
      provides: "Voice budget gating before new browser calls"
      contains: "budget"
    - path: "app/services/sms.py"
      provides: "SMS budget gating before send or queue launch"
      contains: "budget"
    - path: "app/api/v1/org.py"
      provides: "Org spend settings and telemetry response surface"
      contains: "twilio"
---

<objective>
Wire the spend/telemetry foundation into the real voice, SMS, webhook, and org-admin API paths.
</objective>

<context>
@.planning/phases/93-spend-controls-communication-telemetry/93-CONTEXT.md
@.planning/phases/93-spend-controls-communication-telemetry/93-RESEARCH.md
@.planning/phases/93-spend-controls-communication-telemetry/93-VALIDATION.md
@.planning/phases/93-spend-controls-communication-telemetry/93-01-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Enforce soft budgets in voice and SMS launch paths</name>
  <files>
    app/services/voice.py,
    app/services/sms.py,
    app/services/communication_budget.py,
    tests/unit/test_sms_api.py,
    tests/unit/test_call_records.py
  </files>
  <behavior>
    - Test 1: browser voice call start is blocked when the org is over budget
    - Test 2: single SMS send is blocked when the org is over budget
    - Test 3: bulk SMS queue launch is blocked before enqueue when the org is over budget
  </behavior>
  <action>
    1. Inject the shared budget gate into voice and SMS launch flows
    2. Return structured, machine-readable error reasons
    3. Record provisional ledger rows when work is allowed to start
  </action>
  <verify>
    <automated>cd /home/kwhatcher/projects/civicpulse/run-api && uv run pytest tests/unit/test_sms_api.py tests/unit/test_call_records.py -x -q</automated>
  </verify>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Reconcile telemetry from Twilio lifecycle callbacks and expose admin API data</name>
  <files>
    app/api/v1/webhooks.py,
    app/api/v1/org.py,
    app/schemas/org.py,
    app/services/communication_budget.py,
    tests/unit/test_org_api.py,
    tests/unit/test_sms_webhooks.py
  </files>
  <behavior>
    - Test 1: SMS and voice callbacks refine provisional ledger rows with final status/cost when data arrives
    - Test 2: org API returns spend summary, pending-cost state, budget thresholds, and recent activity rows
    - Test 3: org owners can update the soft-budget configuration safely
  </behavior>
  <action>
    1. Extend webhook flows to call ledger reconciliation helpers
    2. Extend org schemas/service/API to include spend-policy and recent-activity data
    3. Add targeted admin API and webhook unit coverage
  </action>
  <verify>
    <automated>cd /home/kwhatcher/projects/civicpulse/run-api && uv run pytest tests/unit/test_org_api.py tests/unit/test_sms_webhooks.py -x -q</automated>
  </verify>
</task>

</tasks>

<verification>
- `uv run pytest tests/unit/test_org_api.py tests/unit/test_sms_api.py tests/unit/test_sms_webhooks.py tests/unit/test_call_records.py -x -q`
</verification>

<output>
After completion, create `.planning/phases/93-spend-controls-communication-telemetry/93-02-SUMMARY.md`
</output>
