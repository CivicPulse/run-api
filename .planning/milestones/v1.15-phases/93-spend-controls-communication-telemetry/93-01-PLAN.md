---
phase: 93-spend-controls-communication-telemetry
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - alembic/versions/034_communication_spend_foundation.py
  - app/models/communication_ledger.py
  - app/models/organization.py
  - app/models/__init__.py
  - app/db/base.py
  - app/schemas/org.py
  - app/services/communication_budget.py
  - tests/unit/test_communication_budget_models.py
  - tests/unit/test_communication_budget_service.py
autonomous: true
requirements:
  - BUD-01
  - OBS-01
must_haves:
  truths:
    - "Organizations store soft-budget configuration fields needed for spend gating"
    - "A unified communication ledger exists for voice and SMS spend/metadata rows"
    - "A shared budget service can compute totals and return machine-readable gate decisions"
  artifacts:
    - path: "alembic/versions/034_communication_spend_foundation.py"
      provides: "Migration for org budget fields and communication ledger storage"
      contains: "communication_ledgers"
    - path: "app/models/communication_ledger.py"
      provides: "Append-only communication telemetry model"
      contains: "class CommunicationLedger"
    - path: "app/services/communication_budget.py"
      provides: "Shared budget gate and telemetry helpers"
      contains: "class CommunicationBudgetService"
  key_links:
    - from: "app/services/communication_budget.py"
      to: "app/models/organization.py"
      via: "soft-budget policy read/write"
      pattern: "twilio_soft_budget_cents"
    - from: "app/services/communication_budget.py"
      to: "app/models/communication_ledger.py"
      via: "append-only ledger writes and aggregate spend queries"
      pattern: "CommunicationLedger"
---

<objective>
Create the data and service foundation for spend controls and communication telemetry so later plans can enforce budgets consistently across voice and SMS.
</objective>

<context>
@.planning/phases/93-spend-controls-communication-telemetry/93-CONTEXT.md
@.planning/phases/93-spend-controls-communication-telemetry/93-RESEARCH.md
@.planning/phases/93-spend-controls-communication-telemetry/93-VALIDATION.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add org budget fields and communication ledger storage</name>
  <files>
    alembic/versions/034_communication_spend_foundation.py,
    app/models/communication_ledger.py,
    app/models/organization.py,
    app/models/__init__.py,
    app/db/base.py,
    tests/unit/test_communication_budget_models.py
  </files>
  <behavior>
    - Test 1: organizations have nullable soft-budget fields with safe defaults
    - Test 2: communication ledger stores org, campaign, voter, channel, event_type, provider_sid, provisional/final price, and pending-cost state
    - Test 3: provider_sid + event_type is indexable for callback reconciliation
  </behavior>
  <action>
    1. Add org fields such as `twilio_soft_budget_cents`, `twilio_budget_warning_percent`, and `twilio_budget_updated_at`
    2. Create `communication_ledgers` as an append-only table with voice/SMS dimensions and cost fields
    3. Register the model in `app/models/__init__.py` and `app/db/base.py`
    4. Add focused model/migration tests
  </action>
  <verify>
    <automated>cd /home/kwhatcher/projects/civicpulse/run-api && uv run pytest tests/unit/test_communication_budget_models.py -x -q</automated>
  </verify>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create the shared budget and telemetry service</name>
  <files>
    app/schemas/org.py,
    app/services/communication_budget.py,
    tests/unit/test_communication_budget_service.py
  </files>
  <behavior>
    - Test 1: service computes org spend totals with provisional and final cost handling
    - Test 2: service returns structured gate results for healthy / near_limit / over_limit states
    - Test 3: service can append provisional telemetry rows and later reconcile final cost
  </behavior>
  <action>
    1. Add budget/telemetry response schemas needed by org API and UI
    2. Implement `CommunicationBudgetService` methods for totals, gate decisions, provisional writes, and reconciliation
    3. Add unit coverage for aggregate totals and gate semantics
  </action>
  <verify>
    <automated>cd /home/kwhatcher/projects/civicpulse/run-api && uv run pytest tests/unit/test_communication_budget_service.py -x -q</automated>
  </verify>
</task>

</tasks>

<verification>
- `uv run pytest tests/unit/test_communication_budget_models.py tests/unit/test_communication_budget_service.py -x -q`
</verification>

<output>
After completion, create `.planning/phases/93-spend-controls-communication-telemetry/93-01-SUMMARY.md`
</output>
