---
phase: 92-two-way-sms-opt-out-handling
plan: 03
type: execute
wave: 2
depends_on: ["92-01"]
files_modified:
  - app/api/v1/webhooks.py
  - app/services/sms.py
  - app/services/twilio_webhook.py
  - app/services/voter_interaction.py
  - tests/unit/test_sms_webhooks.py
  - tests/unit/test_sms_service.py
autonomous: true
requirements:
  - SMS-03
  - SMS-04
  - COMP-01
must_haves:
  truths:
    - "Inbound SMS replies create or update the correct campaign conversation and append immutable inbound message rows"
    - "STOP, START, and related keywords update SMS opt-out state without mutating voice DNC entries"
    - "Delivery status callbacks update outbound message rows idempotently using webhook_events"
    - "Unread counts and last-message summary fields stay correct under duplicate webhook delivery"
  artifacts:
    - path: "app/api/v1/webhooks.py"
      provides: "Real SMS inbound and delivery-status callback handling"
      contains: "sms_inbound_callback"
    - path: "tests/unit/test_sms_webhooks.py"
      provides: "Webhook tests for inbound threading and STOP/START semantics"
      contains: "test_sms_inbound"
  key_links:
    - from: "app/api/v1/webhooks.py"
      to: "app/services/sms.py"
      via: "conversation threading, status updates, and keyword handling"
      pattern: "SMSService"
    - from: "app/api/v1/webhooks.py"
      to: "app/services/twilio_webhook.py"
      via: "signature validation and webhook idempotency"
      pattern: "check_idempotency"
    - from: "app/services/sms.py"
      to: "app/services/voter_interaction.py"
      via: "optional summary audit events for voter history"
      pattern: "record_interaction"
---

<objective>
Build the inbound SMS and delivery-status processing path: Twilio callbacks should thread replies into the right campaign conversation, keep inbox summary state current, and enforce STOP/START semantics through the SMS preference store.

Purpose: This plan makes the inbox real by ensuring inbound data flows back into the campaign UI and that compliance state stays synchronized from Twilio's webhook traffic.
Output: SMS webhook handlers, service methods for inbound threading/status reconciliation, and tests.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/92-two-way-sms-opt-out-handling/92-CONTEXT.md
@.planning/phases/92-two-way-sms-opt-out-handling/92-RESEARCH.md
@.planning/phases/92-two-way-sms-opt-out-handling/92-VALIDATION.md
@.planning/phases/92-two-way-sms-opt-out-handling/92-01-SUMMARY.md

<interfaces>
From app/api/v1/webhooks.py:
```python
@router.post("/sms/inbound")
async def sms_inbound_callback(request: Request, org: Organization = Depends(verify_twilio_signature)) -> str: ...

@router.post("/sms/status")
async def sms_status_callback(request: Request, org: Organization = Depends(verify_twilio_signature)) -> str: ...
```

Expected Plan 01/02 SMSService helpers:
```python
class SMSService:
    async def get_or_create_conversation(...)
    async def record_message(...)
    async def apply_opt_out_keyword(...)
    async def update_delivery_status(...)
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement inbound threading and STOP/START webhook handling</name>
  <files>
    app/api/v1/webhooks.py,
    app/services/sms.py,
    tests/unit/test_sms_webhooks.py,
    tests/unit/test_sms_service.py
  </files>
  <read_first>
    app/api/v1/webhooks.py,
    app/services/twilio_webhook.py,
    app/services/sms.py,
    app/services/org_phone_number.py,
    app/models/sms_conversation.py,
    app/models/sms_message.py,
    app/models/sms_opt_out.py,
    app/models/voter_contact.py
  </read_first>
  <behavior>
    - Test 1: inbound non-keyword messages append an inbound `SMSMessage` row and increment unread count on the matched conversation
    - Test 2: STOP or related keywords set opt-out status to opted_out and mark the conversation blocked
    - Test 3: START or UNSTOP clear the SMS opt-out and re-enable the conversation
    - Test 4: duplicate inbound webhook deliveries return 200 without creating duplicate rows or unread increments
  </behavior>
  <action>
    1. Replace the placeholder `sms_inbound_callback` in `app/api/v1/webhooks.py` with real logic:
       - parse `MessageSid`, `Body`, `From`, `To`
       - call `check_idempotency(db, provider_sid=MessageSid, event_type="sms.inbound", org_id=org.id, ...)`
       - normalize the sender phone and resolve the org phone number via `To`
       - find or create the matching conversation through `SMSService`
       - append an inbound message row and update conversation summary fields
    2. Implement keyword handling in `SMSService`:
       - recognize at least `STOP`, `STOPALL`, `UNSUBSCRIBE`, `CANCEL`, `END`, `QUIT`, `START`, `UNSTOP`
       - route STOP-family keywords to `apply_opt_out_keyword(... status="opted_out")`
       - route START-family keywords to `apply_opt_out_keyword(... status="active")`
       - keep the voice DNC domain untouched
    3. Optionally mirror summary events into `voter_interactions` using `InteractionType.CONTACT_UPDATED` or a new SMS-specific event only if the implementation adds the enum intentionally; do not make this the canonical store
    4. Add unit tests in `tests/unit/test_sms_webhooks.py` covering inbound message creation, keyword handling, and duplicate-delivery safety
  </action>
  <verify>
    <automated>cd /home/kwhatcher/projects/civicpulse/run-api && uv run pytest tests/unit/test_sms_webhooks.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "sms.inbound" app/api/v1/webhooks.py` returns >= 1
    - `grep -c "STOPALL\\|UNSUBSCRIBE\\|UNSTOP" app/services/sms.py` returns >= 1
    - `grep -c "sms_inbound_callback" app/api/v1/webhooks.py` returns 1
    - `uv run pytest tests/unit/test_sms_webhooks.py -x -q` exits 0
  </acceptance_criteria>
  <done>Inbound Twilio replies thread into campaign conversations and update SMS opt-out state idempotently.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement outbound delivery-status reconciliation</name>
  <files>
    app/api/v1/webhooks.py,
    app/services/sms.py,
    tests/unit/test_sms_webhooks.py,
    tests/unit/test_sms_service.py
  </files>
  <read_first>
    app/api/v1/webhooks.py,
    app/services/twilio_webhook.py,
    app/services/sms.py,
    app/models/sms_message.py,
    app/models/sms_conversation.py
  </read_first>
  <behavior>
    - Test 1: `sms/status` updates outbound message `provider_status` for queued/sent/delivered/undelivered/failed states
    - Test 2: terminal error callbacks store `ErrorCode` / `ErrorMessage` on the message row
    - Test 3: duplicate status webhooks are ignored safely because `webhook_events` deduplicates by MessageSid + status value
    - Test 4: conversation `last_message_status` stays synchronized with the newest outbound state
  </behavior>
  <action>
    1. Replace the placeholder `sms_status_callback` in `app/api/v1/webhooks.py`:
       - parse `MessageSid`, `MessageStatus`, `ErrorCode`, `ErrorMessage`, `To`, `From`
       - dedupe with `check_idempotency(... event_type=f"sms.status.{status}")`
       - call a concrete `SMSService.update_delivery_status(...)`
       - return empty 200
    2. Implement `update_delivery_status` in `SMSService`:
       - locate the outbound message by `twilio_message_sid`
       - update `provider_status`, `error_code`, `error_message`, and relevant timestamps
       - refresh the conversation's denormalized `last_message_status` if the updated message is the latest one
    3. Extend unit tests so the status webhook path and duplicate handling are covered
  </action>
  <verify>
    <automated>cd /home/kwhatcher/projects/civicpulse/run-api && uv run pytest tests/unit/test_sms_webhooks.py tests/unit/test_sms_service.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "sms/status" app/api/v1/webhooks.py` returns >= 1
    - `grep -c "update_delivery_status" app/services/sms.py` returns >= 1
    - `grep -c "sms.status." app/api/v1/webhooks.py` returns >= 1
    - `uv run pytest tests/unit/test_sms_webhooks.py tests/unit/test_sms_service.py -x -q` exits 0
  </acceptance_criteria>
  <done>SMS delivery-status callbacks reconcile outbound message state without duplicate side effects.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Twilio inbound callbacks | External payloads crossing into tenant-scoped message state |
| Keyword-triggered preference changes | Must affect only SMS preference state for the owning org |
| Delivery status callbacks | Must not duplicate side effects under retry delivery |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-92-05 | Tampering | inbound callback processing | mitigate | signature validation + MessageSid idempotency + deterministic conversation matching |
| T-92-06 | Repudiation | STOP/START handling | mitigate | persist keyword/source/message_sid in `sms_opt_outs` |
| T-92-08 | Information Disclosure | conversation matching | mitigate | match inside org-owned number scope and selected campaign conversation only |
</threat_model>

<verification>
- `uv run pytest tests/unit/test_sms_webhooks.py tests/unit/test_sms_service.py -x -q`
- `uv run ruff check app/api/v1/webhooks.py app/services/sms.py`
</verification>

<success_criteria>
- Inbound replies appear in the campaign inbox model
- STOP/START semantics update SMS state without touching voice DNC
- Delivery statuses reconcile cleanly and idempotently
</success_criteria>

<output>
After completion, create `.planning/phases/92-two-way-sms-opt-out-handling/92-03-SUMMARY.md`
</output>
