apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: run-api
  namespace: civpulse-prod
spec:
  entryPoints:
    - web
  routes:
    - match: Host(`run.civpulse.org`)
      kind: Rule
      services:
        - name: run-api
          port: 8000
