apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: run-api
  namespace: run-api
spec:
  entryPoints:
    - web
  routes:
    - match: Host(`run.civpulse.org`)
      kind: Rule
      services:
        - name: run-api
          port: 8000
