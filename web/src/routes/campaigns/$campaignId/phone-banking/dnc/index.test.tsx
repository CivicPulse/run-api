import { describe, it } from "vitest"

describe("DNCListPage", () => {
  it.todo("client-side search input filters DNC entries by phone_number substring")
  it.todo("search with digits only strips non-digit characters before comparing")
  it.todo("empty search string shows all entries")
  it.todo("search with no matching entries shows empty state message")
})
